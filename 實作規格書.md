# 實作規格書

> 引擎面規格：引擎讀到 `data/` 之後**必須做出什麼行為**。每一句都應該寫得成測試斷言。
>
> **角色歸屬**：驗收意圖層。各 Phase 的實作契約（檔案、API 簽名、接線）住 `開發設計方針.md`；操作層驗收步驟住 `測試指南.md`。

## 本檔範圍與邊界

**判準：企劃書的句子錯了，設計討論會吵起來；本檔的句子錯了，兩個實作者會寫出不同的程式。**

| 寫什麼 | 去哪 |
|---|---|
| 規則的形狀與**為什麼** | `溫泉小鎮企劃.md`（本檔只指章節，不重述理由） |
| **引擎必須做什麼**、各 Phase 驗收意圖 | **本檔** |
| 資料欄位定義 | `data/SCHEMA.md`（本檔不重複欄位表） |
| 可調數值 | `data/tuning.json`（本檔只寫鍵名，不寫數字） |
| 怎麼實作（檔案、API、signal） | `開發設計方針.md` |
| 操作層驗收步驟 | `測試指南.md` |
| 還沒拍板的東西 | `待決事項.md` |

討論新東西的分流：**還沒拍板 → 待決事項；規則與為什麼 → 企劃書；具體內容 → `subdocs/` 與 `data/`；驗收意圖 → 本檔；實作契約 → 方針；數值 → tuning。** 上游往下編譯（企劃書 → 本檔 → 方針），永遠不反向；實作時發現規格有洞，開回 `待決事項.md` 等拍板，不在本檔自行補設計。

本檔內的 **⚠️ 提案** 標記＝規格層自行補上的未拍板決定，等使用者確認後轉正或改寫。

---

## 〇、架構基線

以正式商用遊戲（規模基準：《蘇丹的遊戲》量級的卡牌敘事遊戲）為前提，以下六條是全部系統共用的硬要求：

1. **資料驅動**：引擎不含任何內容。所有事件、卡片、地點、數值住 `data/`；改內容不改程式。程式內不出現寫死的玩家可見字串（i18n 後置，但不能被程式寫死的字串擋路）。
2. **單一事實來源的遊戲狀態**：所有 runtime 狀態集中在一個可序列化的狀態物件（見第一節）。UI 只讀狀態、發出意圖；UI 不持有、不修改狀態。觸控與滑鼠共用同一套「拖曳」意圖（AGENTS.md：觸控保留架構）。
3. **決定論**：無隨機（企劃書第四節）。同一存檔＋同一操作序列＝同一結果。這是回歸測試的地基。
4. **存檔就緒**：狀態物件從第一天就設計成可完整序列化／還原，即使存檔 UI 排在後期 Phase。存檔分兩層（見第十六節）。
5. **headless 可驗證**：規則層不依賴任何 UI 節點即可運行。每個 Phase 都要能用 headless 腳本自動走查。
6. **效果與條件是封閉語彙**：`condition` / `requires` / `on_place` / `on_enter` 的運算子集合是有限的、由本檔第十四節列舉。資料裡出現語彙之外的鍵＝資料錯誤，載入時報錯，不靜默忽略。

---

## 一、遊戲狀態（GameState）

單一狀態物件，內容窮舉如下。**不在這張表上的東西就不是遊戲狀態**——要新增先改本表。

| 群 | 內容 |
|---|---|
| 頂層流程 | `flow_mode`：`opening`／`run`／`ending`；只有對應模式的規則入口可改狀態 |
| 時間 | 當前天數、當前時段、當前章節（由天數導出，不獨立儲存） |
| 手牌 | 佔格卡的有序清單；每張發狂卡各自的剩餘倒數天數 |
| 備用區 | 寄放卡清單 |
| 超載 | 不存狀態；由 `hand_slots_used() > tuning.hand_size` 即時計算 |
| 旗標 | `flag` 鍵值集合 |
| 開關 | 已翻開的 `switch` 集合；`switch_progress` 累計值表 |
| 關係 | 每個 NPC 的關係值（見第十二節） |
| 投入 | `npc_action_counts`：每個 NPC 累計吃掉的主角行動數（見第十二節） |
| 縱慾 | 本輪已縱慾次數（主動＋強制合計，供升級曲線查表） |
| 夜間 | 當夜是否已選地點；當夜流程是否正停在「讀完再進隔天」的中繼狀態 |
| 委託 | `delegates_used_today`：今日已受託的人物卡集合；`pending_delegation_reports`：隔日上午才回報的委託接點 |
| 遭遇 | 當前遭遇、階段（`intro`／`round`）、回合、累積佔格數與本次已嘗試卡片；不在遭遇時為空 |
| 開局 | 本輪 `opening_choice_id`；尚未初始化本輪或選擇「不上車」時為空 |
| 結局 | `active_ending`：結局 id、觸發接點、凍結後的組合參數、長版／摘要版、當前頁碼；不在結局時為空 |
| 慶典代付 | `selected_festival_proxy_npc`：D29 邀請或「不邀」逾期結算時固定的人物 id；此前為空 |
| 選擇 | 已結算的 `choice_group`（組 id → 選了哪個槽或「不選」） |
| 事件 | 已觸發過 `on_enter` 的 beat 集合；已放置過的槽集合 |
| 跨輪 | 輪數、**知識集合**（從取得當下就住 meta）、夜間地點到訪集合、夜間一次性 beat 集合、`delegation_tutorial_seen`、歷輪結局記錄、魔法物品集合（目前正式資料為空；存於 meta 層，見第十六節） |

引擎啟動時，`DataLoader`（`scripts/data_loader.gd`）先載入並通過引用檢查，才建立 GameState。載入失敗＝不進遊戲，顯示錯誤。

---

## 二、日期與時段狀態機

> 企劃書第二節。章節結構（1–15／16–32／33–45）與**時段名序列（morning／afternoon／evening／night）都是設計已定的結構常數**，beats 資料以絕對天數與時段名寫成，**皆不可調**——引擎集中定義一處，不放 tuning。`tuning.phases_per_day` 降級為**一致性驗證**：載入時與結構常數（行動格數 2）不一致即報錯，不是可自由調整的玩法數值。

一天的時段序列固定為：

| 時段 | 性質 | 行為 |
|---|---|---|
| `morning` | **行動格** | 玩家選一個開放中的白天地點，或跳過 |
| `afternoon` | **行動格** | 同上 |
| `evening` | 演出時段 | 播當日 `when.phase == "evening"` 的 fixed beat 與殘響（見第十一節）。**evening 不是行動格**：槽可以存在，但 `accepts` 含 `protagonist` 的槽在 evening 一律由引擎強制呈灰（無 `reject_reason` 時用通用文案）——第 22 天「叫她搭那班車下山」那個永久灰槽因此合法；免費槽（比對等，不收主角卡）照常可用 |
| `night` | 夜間層 | 玩家選一個夜間地點，或直接睡（見第九節） |

- 時段推進由引擎驅動：行動格在「主角卡被放置」或「玩家明示跳過」時結束；evening 播完自動進 night；night 結束進下一天 morning。
- 章節由當前天數導出。同一天之內章節不變。
- **第 45 天沒有 night**（企劃書第十五節）：序列為 morning → afternoon → **evening（結局 coda）** → `end_run`。coda 播第 45 天 evening 的 fixed beat（`d45_then`，含免費比對槽——知識升級在這裡發生），播畢直接結束本輪。夜間地點清單在第 45 天永遠不出現。
- ⚠️ 提案：**強制縱慾佔用行動格的時機**——見第八節。

**推進斷言**：從第 1 天 morning 起，依「morning → afternoon → evening → night → 次日 morning」循環推進，恰好在第 45 天 evening 播畢後離開日常循環進入結局。全程章節切換恰好發生兩次（第 16 天、第 33 天的 morning 開始時）。

---

## 三、手牌、備用區與超載

> 企劃書第三節。格數＝`tuning.hand_size`；備用區容量＝`tuning.reserve_capacity`（`null`＝無上限）。

### 手牌

- 佔格卡進入手牌時佔一格；`slotless: true` 的卡（目前只有 `knowledge`）**永不進手牌**，直接進知識集合。
- 每種卡型別的操作矩陣（引擎必須逐條強制）：

| 型別 | 可丟棄 | 可寄放備用區 | 可放進槽 | 備註 |
|---|---|---|---|---|
| `protagonist` | `discardable:false` | ✗ | ✓ | 放置＝花掉該時段（第六節） |
| `madness` | `discardable:false` | ✗ | ✓（只進 `accepts` 含 `madness` 的槽） | 唯一動詞＝縱慾（第八節） |
| `knowledge` | `discardable:false`（不在手牌） | ✗ | ✓ | 不消耗；`on_place` 明寫 `lose` 才離場 |
| `person` | `discardable:false`（人不是物） | ✗ | ✓ | 委託（第一輪第 17 天起，Phase 4） |
| `mood` / `routine` | 依卡片 `discardable` | ✗ | 依 `accepts` | 目前正式卡均為 false |
| `inference` | 依卡片 `discardable` | ✗ | ✓ | 驗證後轉知識或被推翻，不跨輪 |
| 其餘（`info` / `equipment` / `consumable` / `document` / `group`） | 依卡片 `discardable` | 依 `stashable` | ✓ | 同型別內可不同，不從 type 推導 |

- **除 `madness` 外，所有卡都是 unique**：`gain` 冪等——已持有（手牌或知識）時再 `gain`＝no-op，不建立第二個實例（主角卡跨輪重入第 1 天到站事件靠這條保證恰好一張）。`madness` 是唯一的多實例卡，不受此規則影響。堆疊卡等真需求出現再以明確欄位開放。
- 「可丟棄」的單一機器真值是 `cards.json.discardable` 必填 boolean；上表只列型別不變式與現況。遭遇錯答、主動丟棄、逃離支付一律讀欄位，不得按 type 寫第二份判斷。
- 「丟棄」在第一輪日常中**沒有入口**——只有遭遇中的「主動丟一張清一格」用得到（第十節）。日常 UI 不提供丟棄。

### 備用區

- 是**旅館主角房間這個地點上的功能**，不是全域 UI（企劃書第三節表格）。
- 存入條件全部成立才可用：人在房間、花一個行動格、**手牌已滿**（沒滿不准放）。
- 取回：人在房間、花一個行動格。
- 收與不收照企劃書第三節那張表；引擎按卡型別擋，擋下時顯示理由（三態語彙，第五節）。

### 超載

- 觸發：手牌已滿時又被塞入佔格卡（發狂卡、遭遇強塞等）。**卡照樣上桌**——超載中手牌實際張數可以超過 `hand_size`。
- 超載中的限制：① 不能開新的夜間標記；② 遭遇起手少一回合（第十節）。
- 解除：回房間花一格，把卡搬進備用區，手牌降回上限以內。

P4 只兌現不需另存檔的最小切面：`is_overloaded()` 純查詢，以及既定的「不能新進夜間地點」與「強制遭遇追加一次 `per_round_slot_cost`」兩個結果。備用區容量、整理 UI 與解除流程仍留待後續 Phase；不得為 P4 先做半套備用區。

---

## 四、地點與面板

> 企劃書第二、十五節；欄位見 `SCHEMA.md > locations.json`。

- 白天地圖只列 `layer` 為 `day`／`both`、`chapter` ≤ 當前章、且 `phases` 含當前時段的地點。夜間地圖見第九節。
- **地圖上看不到地點裡面有什麼。** 地圖按鈕只有地點名。要知道那裡發生什麼事，只能走進去。
- 進入地點＝開面板。**面板內容＝該地點所有「當下成立」的 beat**，成立＝
  1. `when` 匹配當前天與時段（**無 `when` 的 beat 是夜間標記內容**，見第九節，不進白天面板）；
  2. beat 級 `condition` 求值為真（不成立＝這個 beat 整個不存在）；
  3. beat 級 `requires` 不擋顯示、只擋互動（與槽級一致，第五節）。

### 面板的兩個階段：演出，然後常態

進入地點之後，面板**先演出、後互動**，兩個階段不重疊。白天與夜間同一套，不分岔。

**階段一，演出。** 成立的 beat **依資料順序一個一個演**，不是一次全部攤在畫面上。一個 beat 演完，**玩家主動按一下才演下一個**——不自動計時、不自動連播。每個 beat 演出時結算它的 `on_enter`。

- 為什麼要逐一：正式版每個 beat 會有畫面搭配，一次攤開就沒有演出可言。而且後面的 beat 可能讀前面 `on_enter` 寫的東西，逐一演出讓因果在畫面上看得到。
- 已經看過的 beat（`on_enter` 結算過）**照樣重播文字，但不重複結算**。玩家重看場景是免費的。
- **演出期間卡槽不可互動。** 理由：`on_enter` 會解鎖後面的槽，演出沒走完就開放互動，玩家會對著一個馬上要改變的面板做決定。

**階段二，常態。** 全部 beat 演完後，面板顯示：

1. 地點描述（`locations.json` 的 `desc`）；
2. 在場的人與東西（`occupant` 槽，顯示槽上 authored 的 `label`）；
3. 可放卡的槽，帶型別標示與預覽（第五節）。

- `fixed: true` 的 beat：進入地點就會演到，不需要放卡、不吃行動格。當日該時段的 fixed beat 若掛在玩家沒去的地點——**照樣結算 `on_enter` 嗎？否**：fixed beat 的效果只在玩家見到它時發生；玩家沒去＝錯過，走殘響（第十一節）。例外：`when.phase == "evening"` 的 fixed beat 屬強制演出，一律播出（第二節）。
- **`on_enter`**：beat 演出給玩家看的那一刻結算一次，**每輪至多一次**（記入 GameState 事件群）。支援的效果鍵與 `on_place` 同一張表（第十四節）。
- **beat 的呈現管道只有一個入口**：地點面板、evening 演出、夜間層、結局 coda 全部走同一條「進入 beat」流程——一次性判定、`on_enter` 結算、文字呈現在同一處處理。任何管道播出 beat 而沒結算 `on_enter`＝實作錯誤（第 1 天到站事件發主角卡就在 evening，靠這條保證）。
- 面板中槽的順序照資料順序渲染，引擎不重排。

---

## 五、卡槽三態（硬規則）

> 企劃書第十七節；`SCHEMA.md > 三態可見性`。

每個槽在任何時刻恰好處於三態之一，求值順序固定：

1. `condition` 不成立 → **不顯示**（槽不存在）。
2. `condition` 成立、`requires` 不成立 → **灰掉**：顯示、不可放卡、顯示 `reject_reason`。
3. 兩者成立 → **可選**。

- 求值時機：面板開啟時求值一次；**同面板內任何狀態變化（放卡、效果結算）後全面重求值**。跨面板不需要即時性——重新開面板就是重求值。
- 沒寫 `condition`＝恆成立；沒寫 `requires`＝恆成立。
- 灰掉而沒寫 `reject_reason`＝資料錯誤（載入時報警告）。文案規則（指缺的東西、不指答案）屬資料層，引擎不驗證。
- beat 級 `requires` 語意相同：成立前整個 beat 呈灰卡狀態＋理由，內部槽不可互動。
- 放卡進槽的合法性＝三態為「可選」**且**卡符合 `accepts`（型別名或卡 id 擇一匹配）。`accepts: []`＝純顯示槽，永遠不可放卡。

### 槽要先告訴玩家它收什麼（P1-G）

放卡是不可逆的（第六節）。**不可逆的動作，玩家在按下去之前必須看得懂自己要做什麼。** 這一節管的就是那個「之前」。

- **每個可放卡的槽常駐顯示型別**：把 `accepts` 的每個值換算成型別，再查 `data/card_types.json` 取顯示名。`accepts` 寫的是型別名就直接用；寫的是卡 id 就查該卡的 `type`。
- **標型別，不標卡名。** `accepts` 目前 199 個槽裡有 32 個寫的是具體卡 id，直接顯示卡名等於把答案貼在格子上。顯示「情報卡」，玩家自己去想是哪一張。
- 一個槽的 `accepts` 換算出多個型別時，用「、」串接（例：第 22 天堆沙包那四個觀察槽是「裝備卡、情報卡」）。**不要用「／」**——`document` 的顯示名本身就叫「書籍／文件卡」。
- **右鍵（觸控長按）叫出預覽**：列出玩家此刻手上哪幾張卡放得進這一格。預覽是唯讀的，**不結算任何東西**，關掉就沒事發生。
- 預覽清單走規則層的同一個入口，UI 不自己算 `accepts`（`開發設計方針.md > P1-G`）。灰掉的槽照樣可以預覽，看到的是空清單加上 `reject_reason`。
- **槽級 `condition` 為受支援欄位**（已拍板）：求值器同一套，資料層可用它做「拿掉而非灰掉」的表現（`ch2_d16_d22.json` 的觀察槽註記等的就是這個）。目前資料多寫在 beat 級，槽級沒寫＝恆成立。

---

## 六、放置語意與行動格結算

> 企劃書第三節；`SCHEMA.md > 核心概念`。同一個拖曳動作，五種意思由「放什麼卡」決定，引擎不需要模式切換。

| 放進槽的卡 | 語意 | 行動格 |
|---|---|---|
| `protagonist` | 把這個時段花在這裡 | **消耗當前行動格** |
| `person`（不含主角卡） | 委託（Phase 4） | 不消耗 |
| `equipment` / `consumable` / `info` / `document` | 使用 | 不消耗 |
| `info` / `knowledge` / `inference` 放進不收主角卡的槽 | 比對（第七節） | 不消耗 |
| `madness` | 縱慾（第八節） | **消耗**（規則本身如此，非放置機制） |

- **主角卡放置在行動格＝消耗該行動格**，不用每個槽標（`SCHEMA.md > on_place`）。結算順序：先套 `on_place` 全部效果 → 同面板重求值 → 結束該行動格。行動格結束後玩家仍留在面板（可繼續做免費動作：比對、choice_group），離開面板或無事可做時推進時段。
- **放進去就結算，不可反悔（已拍板）。** 理由是遊戲的核心張力——同時發生太多事，你只能在場一次；能收回就沒有這個張力了。**但「反悔」跟「點錯」是兩件事**：防點錯的責任放在放置**之前**（第五節的型別標示與右鍵預覽），不是事後給後悔藥。
- **沒有任何動作可做時（沒有合法卡片放置，也沒有尚未結算的 OPEN `choice_group`），推進按鈕要明顯提示玩家**（改字或高亮）。**不自動推進**——玩家可能還想走去別的地點看看，時段什麼時候結束是他的決定。這一條解的是「不知道還有沒有事可做」，不是替玩家做決定。
- **night 時段放主角卡不消耗行動格**——夜間沒有行動格（帳是白天付的，企劃書第十五節）；夜間的稀缺性由「一晚一地點」的夜間流程管（第九節）。資料裡夜間槽收主角卡是合法的（例：第 10 夜「拿走／不拿」）。
- 一個行動格只能放一次主角卡（放出後主角卡回到手牌釘死位，同一時段不能再放第二處）。
- **槽的身份鍵＝`beat_id + slot_id`**（`slot.id` 只在 beat 內唯一，資料裡跨 beat 大量重複）。**槽預設一次性**：結算過即記入已放置集合，之後恆為不可放（呈現方式：收起）；可重複的槽等真需求出現時以 `repeatable` 欄位明寫，現在不做。`choice_group` 的鍵同理＝`beat_id + group_id`。
- **放置合法性全部在規則層檢查，順序固定**：①**持有**——傳入的卡此刻必須在手牌或知識集合（UI 是零規則顯示層，不能靠它擋非法卡 id）→ ②三態為可選（含一次性未放過）→ ③`accepts` → ④主角卡在行動格時再查 `action_spent`。任一步不過＝回傳一致的拒絕理由，**且 GameState 零變化**。檢查全過才進效果結算；UI 與 headless 走查呼叫同一個規則層入口，不各自實作。
- 「不收主角卡的槽必須跟收主角卡的槽同面板」是**資料規約**（`SCHEMA.md` 已載）；引擎不強制，**驗證腳本要 lint**（第十七節）。
- `on_place` 支援的效果鍵見第十四節，結算為原子操作：全部效果套完才重求值、才允許下一個互動。

---

## 七、比對

> 企劃書第三節「比對」；機制上**沒有比對系統**——比對＝一個 `accepts` 不含 `protagonist` 的槽。

引擎不需要為比對寫任何專屬邏輯，以下只是行為斷言：

- 放情報／知識卡進這種槽不消耗行動格（第六節表）。
- 「知識卡 × 新情報 → 升級知識卡」由資料達成：`on_place.gain` 新知識＋`lose` 舊知識。引擎的 `lose` 必須能作用於知識集合（不只手牌）。
- 通用比對盤是第二輪解鎖（企劃書第三節），第一輪範圍內不實作。

---

## 八、發狂時鐘與縱慾

> 企劃書第七節。鍵：`madness_cap` / `madness_countdown_days` / `madness_vision_threshold` / `indulgence.*`。機制層模擬見 `subdocs/驗證/發狂卡機制模擬.md`。

### 產生與倒數

- 產生：開一個收費夜間標記時，手牌加入 `location.madness_cost` 張發狂卡（第九節）。其他來源由資料的 `on_place.madness` / `on_enter` 效果給。
- **各走各的鐘**：每張發狂卡有獨立倒數，初值＝`madness_countdown_days`。
- **已拍板（P2 規格時定案）**：**倒數在每天 morning 開始時全體 −1；產生當天不倒數。** 產生一律發生在 night，而 tick 發生在次日 morning，所以「產生當天不倒數」由順序自然成立，不需要額外旗標。依此第 6 夜開的卡在第 13 天歸零（機制模擬 A 的第一次強制縱慾就落在那一天）。
- 卡面顯示剩餘天數（UI 層）。

### 主動縱慾

- 發狂卡唯一的動詞。出口＝`accepts` 含 `madness` 的槽；沒有縱慾選單。
- 放發狂卡進出口槽：消耗一個行動格、消掉那張卡（玩家挑哪張）、套該槽 `on_place`。
- **消耗行動格發生在縱慾流程，不在 `try_place` 的放置機制**（`驗證後已知問題.md > B-01`）。第六節那張表的 `madness` 那一列講的是規則，不是放置機制。
- 泡湯特例：消耗 `indulgence.soak_phase_cost` 格、清 `indulgence.soak_cards_cleared` 張。**已拍板：泡湯只能在 morning 發動，一次吃掉整天。** 現行 `phases_per_day` 是 2 而 `soak_phase_cost` 也是 2，所以「兩格」本來就等於一整天；擋在 morning 是為了讓這件事在資料改動之後仍然成立，不靠兩個數字剛好相等。

### 強制縱慾（倒數歸零）

- 任何一張卡歸零的那一天，遊戲代替玩家執行縱慾：消耗該日一個行動格、消掉那張卡。
- **已拍板**：**執行時機＝該日第一個尚未使用的行動格**；同日多張歸零＝各佔一格，依序吃掉當日行動格；當日行動格不夠時，餘下的順延到次日行動格（帳一定要收到，不豁免）。
- 出口挑選演算法（企劃書第七節已定）：**條件成立的出口中，代價最重的那個；泡湯永不入池。** 「代價權重」＝出口槽的 `indulgence.weight`（`SCHEMA.md > 縱慾出口`）。同權重時取資料順序第一個——**挑選必須是決定論的**，不得引入亂數。
- **挑選池只看條件，不看玩家人在哪裡。** 強制縱慾是主角自己動手，不是玩家走過去；出口槽的 `condition` 與 `requires` 照常求值，但**地點可達性不參與**。這也是「保底」為什麼只要求「池非空」而不是「玩家走得到」。
- 無條件出口（砸東西、食慾）恆成立——**任何時刻挑選池非空**是規則完整性要求，驗證腳本要證明（第十七節 lint 4）。
- 升級曲線：本輪第 N 次縱慾（主動＋強制合計）查 `indulgence.forced_light_count` / `forced_normal_until` 決定強度級：**N ≤ `forced_light_count` ＝輕；N ≤ `forced_normal_until` ＝正常；其餘＝最重。** 強度級的副作用由出口槽的 `on_place_by_level` 提供追加效果（`SCHEMA.md > 縱慾出口`）——**基底 `on_place` 照常先套，再套當次強度級那一組**。用「基底＋追加」而不是三份完整替換，因為企劃書第七節寫的是「副作用**疊加**」，而且三份替換會讓內容期改一個字要改三處。

### 視野與上限

- 手牌發狂卡張數 ≥ `madness_vision_threshold` 是一個 `condition` 語彙（`madness_at_least`），用於「不顯示」態——不夠不正常的人看不見，不是灰掉（`SCHEMA.md` 已載）。
- 張數達到 `madness_cap` 的瞬間：主角卡翻面 → 發瘋 BE → 本輪立即結束，進入 BE 結局流程（不播複製品後日談，企劃書第十七節）。

---

## 九、夜間層

> 企劃書第十五節。欄位形狀住 `data/SCHEMA.md > locations.json`；本節只定玩家可見行為與規則真值。

### 夜間地圖、名稱與狀態

- 一般夜間地點在 `earliest_night` ≤ 當前天數後出現在清單；**`earliest_night` 是下限不是期限**。地點級 `requires` 不成立時仍顯示，但「進入」灰掉並顯示該地點的 `reject_reason`。通用「條件不足」只能當資料漏填的保底；正式資料的阿宏鏈必須寫出具體、但不洩漏答案的理由。
- `teaser_only: true` 是刻意看得到、永遠進不去的標記：從該 row 的 `chapter` 起顯示，永遠 LOCKED，不受 `earliest_night` 解鎖。`earliest_night` 雖不控制 teaser 顯示，仍必須落在同一 `chapter`，作為資料作者的章節一致性守衛。它不能被記為到訪、不能對位、不能收費。現行載體是第三章的「走廊的盡頭」，理由為「還沒有走到那裡。」。
- 清單採**穩定順序**：`earliest_night` 由早至晚，同夜照 `locations.json` 的資料順序。到訪、對位或改名只更新原位置，不因狀態重新排序；P3 不做座標地圖，也不處理待決 29。
- `locations.json.name` 是玩家站在外面就觀察得到的**引子名稱**，不是進場後的答案。尚未對位以前一律使用它；進場發現什麼由 beat 的 `title` / `text` 表達。P3-A 要審完 28 個名稱，只改掉直接洩漏結果者，不新增第三套名稱欄位。
- 夜間地點狀態文字固定如下：

| 地點類型 | 未到訪 | 已到訪、未有對位知識 | 已到訪、已有對位知識 |
|---|---|---|---|
| 有 `day_counterpart` | `[尚未到訪]` | `[已到訪，尚未對位]` | `[已對位]` |
| `day_counterpart == null` | `[尚未到訪]` | `[已到訪]` | 不適用 |

- 尚未到訪時**不顯示 `madness_cost`，也永遠不顯示「免費」**。若依目前狀態可確定這次進場的立即效果會觸發發瘋 BE，詳情顯示不帶數字的警告「再往前，你可能回不來。」；這是風險提示，不揭露價碼。
- 地圖項目本身只負責開詳情。詳情可唯讀查看名稱、狀態與門檻理由；只有按下可用的「進入」才算到訪。看詳情不收費、不消耗今晚，也不改 GameState。

### 到訪、收費與一夜一地點

- 玩家一晚選一個地點，或直接睡；夜間層不強制探索。**主動進入地點、或被 night-layer fixed beat 強制帶去地點，都會用掉今晚唯一一次地點選擇。**之後其他地點不能進入。
- **「或」是互斥的：睡覺解析出內容、停在「進入隔天」的那一拍，同樣不能再進地點。**否則玩家可以先睡看完外溢文字、再探索一個地點，一夜拿到兩份夜間內容。判準是「今夜尚未選、也尚未睡」。
- `madness_cost` 只在該 row 的**終身第一次主動到訪**收取：
  1. 先把 location id 寫進 meta 層 `night_locations_seen`。
  2. 若本次是主動進入且 `madness_cost > 0`，依資料產生發狂卡。
  3. 再解析地點內容。
- 先記到訪再發卡是硬順序：發卡途中觸發發瘋 BE，該地點仍永久算到訪。之後任何輪次重走該 row 都不再收 `madness_cost`；**免費來自到訪，不來自對位**。
- `madness_cost == 0` 的 row 同樣會記到訪、同樣消耗今晚，只是不發卡。畫面不為它建立「免費」狀態。
- 已到訪只免除標記的首次進場費。beat、選項與未來遭遇自己的效果仍可在後續到訪造成發狂或其他代價；走過不等於永久安全。
- 合法地點即使當夜沒有主內容、章節變體或附加 beat，仍可進入：顯示通用結果「走了很久。什麼都沒有遇到。」，記到訪、依首次規則收費並消耗今晚。通用結果不是假 beat，不得寫入 `beats_entered`。

### 夜間內容與章節變體

- 夜間內容有三種形態：**章節變體 beat**（無 `when`、有 `chapter`——同地點的變體集，一次只取一個）、**定日夜 beat**（`when.phase == "night"` 指定某天：引導夜、颱風夜、第 15 夜等）、**附加 beat**（無 `when` 無 `chapter`，靠 `condition` 控制，與主內容並列，例：第 10 夜起的「有東西可以拿走」）。
- **夜間解析順序**：
  1. 入夜先解析當夜的 **fixed 定日 beat**。掛在 night-layer 地點上的 fixed（現行 D1／D2／D15）是一次性強制到訪：不論該地點是否以前主動去過，beat 本身只在整份存檔第一次播；播出時記地點到訪、免除 `madness_cost`，並用掉今晚。掛在旅館等 day-layer 地點上的 fixed（現行 D24）是故事演出，不算夜間地點到訪、不收費、不用掉今晚，仍按既有 `beats_entered` 規則每輪可結算一次。
  2. 玩家選定地點 → 該地點的主內容：**有當日定日 beat 就用它（取代變體）**；否則取章節變體 **`chapter` ≤ 當前章節中最大者**（`SCHEMA.md > 夜間標記的章節變體`）。
  3. 主內容之外，**並列所有 condition 成立的附加 beat**。全部皆無＝顯示上一節的通用空結果。
  4. **「直接睡」不是空動作**：解析掛在旅館（`sanquan`）上的當夜定日 beat，condition 通過才播——颱風外溢四夜（`d23–26_night_bleed`）走這條：不是你去找它，是它跑出來了（企劃書第十五節）。無可播內容時才是安靜的一夜。
- 所有入口共用同一份夜間 resolver；`PanelBuilder`、睡覺流程與合法性檢查不得各自掃 `when`。同一個 beat 重播文字、同一輪效果只結算一次，沿用第四節既有契約。
- D3 的 `n3_map_opens` 改成一次性環境引導：無槽、無發狂效果、不算任何地點到訪、不用掉今晚。第二輪起不再重播，直接顯示夜間清單；不得保留目前「同一夜三個主角槽都可結算」的形狀。

### 對位

- 對位只適用於 `day_counterpart` 非 null 的 12 個夜間 row。玩家必須**曾到訪該夜間 row**，再於正確的白天地點確認「把兩邊對起來」；白天地圖不顯示提示、錯誤地點什麼都不出現且沒有代價。
- 對位是免費確認，不消耗行動格、不收卡、不要求把臨時線索放進手牌。確認後取得 `night_reveal` 指向的 slotless knowledge 卡。
- 每個實際白天地點只對應一張對位知識卡。若多個夜間 row 指向同一個 `day_counterpart`，白天地點只顯示一個確認按鈕：確認後，所有已到訪 row 同時成為已對位；尚未到訪的 row 保持 `[尚未到訪]`，日後第一次到訪時因知識已存在而自動成為已對位。
- **不儲存「已對位集合」。**某 row 的已對位狀態永遠由兩個真值即時計算：`night_locations_seen` 含該 row，且 meta `knowledge` 含該 row 的 `night_reveal`。不得另存一份會漂移的 matched state。
- 已對位的一對一 row 顯示白天地點名；多對一 row 顯示「白天地點名・夜間引子名」，例如「靜和園後棟・很長的走廊」。對位知識卡本身只陳述「夜裡可以走回該白天地點」，不得列出尚未到訪的其他夜間分區。

### 例外

- 颱風封閉那幾天夜間層外溢、不產生發狂卡——**由資料達成**（外溢 beat 掛在旅館與白天時段、不掛收費地點；夜裡那份走「睡覺解析」），引擎無特例。
- 第 45 夜不存在（第二節）。
- 夜間主流程的按鈕文字與停拍固定：尚未選地點時為「直接睡」；主動或強制到訪後為「結束今晚」；睡覺有內容時第一次點只播內容，按鈕改為「進入隔天」，第二次才換日；安靜睡覺可一次直接進隔天。到訪後按推進不得再額外呼叫睡覺解析。

---

## 十、遭遇

> 企劃書第十六節。遭遇仍是「卡片對卡槽」：要求是特殊槽，玩家從持有集合（`hand ∪ knowledge`）選卡回應；知識不佔容量，不另做戰鬥資源或第二套牌。精確資料形狀見 `SCHEMA.md > encounter`。第一輪正規遭遇 2 次（第 8 夜、第 45 天）。

回合循環（每回合）：

1. 遭遇提出當前要求（`demand` 文字）。
2. 遭遇佔走手牌 `per_round_slot_cost` 格（實作為不可互動的佔位，不產生假卡）。**可用格歸零＝敗**，進入該遭遇的失敗分支；這不是第三節的超載，也不開備用區選擇。
3. 玩家回應，三選一：
   - **打出一張卡**：同一要求可有多組合理回應；`responses[]` 的 `accepts` 是隱藏正解集合，不是「只有這些卡能點」。命中後套該回應的文字／效果、依資料決定是否消耗卡並轉往下一要求，且**一律釋放本回合佔格**；未命中才套 `fallback`。一般 fallback 保留本回合佔格，依資料消耗可丟棄卡；若 `fallback.requires_discardable == true`，不可丟棄卡根本不是合法提交，必須以 `card_not_submittable` 拒絕且不標 attempted、不推進。
   - **主動丟一張卡清一格**：`allow_discard == true` 時才存在；是唯一的日常外丟棄入口，發狂卡不可丟（第三節矩陣）。
   - **走掉**：`escape_cost` 為整數時支付該數量的可丟棄卡後結束遭遇；為 `null` 時不可逃。
4. 全部回合通過＝滿足它，遭遇結束，套結算效果。

- **遭遇中沒有行動格**：縱慾不可用；桌上每張發狂卡都是被佔死的格。
- 強制遭遇由規則層在進入其定日時段時自動建立 active state，先停在 `stage == "intro"`；此時 `advance_phase()`、睡覺、地圖移動與其他**非遭遇** mutation 入口全部拒絕。UI 播完開場後只能呼叫 `acknowledge_encounter_intro()` 轉入第一回合，不能自行啟動或結束遭遇。
- `acknowledge_encounter_intro()` 即進第一回合，只有這一次增加 `per_round_slot_cost`，不另收進場 cost；後續每進一個新回合各收一次。**只有超載進場是明示例外**：先額外增加一次 `per_round_slot_cost` 作為超載 penalty，再進第一回合，因此刻意承受兩次，不是雙計 bug。強制 D8 不受「超載不可新進夜間地點」阻擋。
- 同一張 base card 在同一場遭遇只可提交一次；被正解或錯答消耗的普通卡離開本輪，未被消耗的卡仍留在原位。
- 人物卡、主角卡與其他不可丟棄卡不因通用遭遇結果永久失去。人物真正永久失去只能由另行規格化的特殊事件明寫；P4 不建立通用入口。
- 勝利、失敗、逃離都必須清空遭遇暫態並各自套一次出口效果；失敗揭露由資料文字承擔。若手上已沒有尚未嘗試的非發狂卡，且不能合法丟棄或支付逃離，立即進失敗出口，不等待不存在的操作。
- 結束後按資料 `after_finish` 決定留在原時段（`stay`）或由規則層恰好推進一次（`advance_phase`）；不得在 UI 或 beat id 寫 D45 特判。

### 第 8 夜：門後之物

- 第一輪必定發生，不能直接睡、跳過或延後；之後每輪第 8 夜都重演。它是 night-layer fixed 的明示例外，不受一般「fixed 必須 meta-once」約束。
- 強制進入仍走共用夜間到訪規則：收費前先讀取終身 seen 狀態；只有此前未 seen 才照 `locations.json` 收一次 marker cost，已 seen 則跳過。遭遇本身沒有第二筆入場費。
- 「你叫什麼名字？」與主角固定回答是不可操作的開場演出，不把主角卡送進錯答／消耗流程。
- 三回合的正式接受卡與效果以 `故事線_第一輪_第一章.md > 第 8 天 > D8 response matrix` 為內容單一真值。第一個可操作要求在第一輪沒有正解，且三回合 fallback 都要求可丟棄卡；只要仍有合法可丟棄卡，進第二回合前必須失去或主動支付一張。若完全沒有合法提交／丟棄／逃離路徑則直接失敗，不宣稱憑空必失去一張。

### 第 45 天：末日注視

- 下午 fixed 由規則層自動進入，單回合、不可逃、不可主動丟棄；每輪只選一張卡、只得到一個對應回應，`after_finish == "advance_phase"` 保證結束後只推進一次到 evening 收尾。
- 每個可用回應都有獨立文字與效果，不用一個泛用「正確」文案代替。
- 消耗與轉化由回應資料明寫：推理卡可依既有設計轉化；主角卡、人物卡與普通證據可不消耗。人物卡回應不等於人物永久消失。

---

## 十一、殘響與錯過

> 企劃書第十七節。`echo` 欄形狀：`{day, text, condition}`（精確欄位見 `SCHEMA.md > beats/*.json`）。

- 殘響三級是**資料的寫法**不是引擎分支：「完全不知道＝什麼都沒有」靠不寫 echo 或 `condition` 擋掉；「聽說了」＝echo 顯示；「到場了」＝echo 的 `condition` 用「到場旗標之否定」自動不成立。
- 引擎行為：`echo.day` 那天，若 `echo.condition` 成立，播出 `echo.text` 一次。
- **沒有 `day` 欄的 `echo` 是資料錯誤**，不是「當天播」。引擎掃的是 `echo.day == day`，缺欄即永遠不播。`verify_data` 要當錯誤擋下來（lint 8）。
- ⚠️ 提案：**播出管道＝當日 evening 演出時段**，殘響逐條以一行文字呈現（與 evening 的 fixed beat 同一條演出流）。不做地點面板內嵌、不做日誌回顧（手帳規格：知識卡清單＋空白條目，`待決事項.md` 第 24 項已結案）。

### evening 演出流的結算順序

同一天的 evening 可能有多個 `fixed` beat，而後面的 beat 讀得到前面的 beat 用 `on_enter` 寫下的 flag。**順序必須是確定的，否則同一份資料會有兩種結果。**

- **順序＝資料的陣列順序**：同一檔案內按 `beats[]` 出現順序；跨檔案按 `data/beats/` 的檔名字典序。
- 先播完當日全部 evening `fixed` beat（逐個結算 `on_enter`），**再**播殘響行。殘響的 `condition` 因此讀得到當晚 fixed beat 的結果。
- 資料端的義務：**依賴前一格 flag 的 beat 必須排在後面**，並在 `note` 註明。招牌案例是第 27 天的 `d27_evening_awei_confirm` → `d27_evening_cover_knowledge`。

> **為什麼寫死成陣列順序而不是拓撲排序**：拓撲排序要引擎讀懂 `condition` 與 `on_enter` 的依賴，而那兩者都是任意運算式。陣列順序讓資料作者看得見自己寫的順序，代價是他要自己排對——這個代價比一個看不見的排序器便宜。

---

## 十二、關係系統

> 企劃書第五節。

- 資料現況：`on_place.relation` 寫 `{npc, delta}`（整數增減）；`condition` 寫 `relation_at_least {npc, state}`（狀態名）。
- **單軸暫行案（已拍板，2026-08-13）**：prototype 範圍先做單軸整數記帳，`relation_at_least` 靠一張「狀態名 → 最低整數值」的全域序數表求值（表住 `data/relation_scale.json`）。企劃書第五節的兩軸狀態機（立場×感情互相矛盾）留到文本階段實作，屆時資料語彙升級——這是已知且刻意的債。
- 「不邀任何人時系統挑誰」的判準是**本輪花最多格子在誰身上**（判準用時間不用好感度，企劃書第五節）。可實作的契約：
  - GameState 持有 `npc_action_counts`（NPC id → 本輪已投入的主角行動數，run 層、入存檔、跨輪重置）。
  - 計入哪位 NPC 由槽的 `attention_npc` 欄位明寫（`SCHEMA.md > slots[]`）；**未標註＝不計**，不從 occupant 或地點推導。資料標註隨內容期逐步補，機制從 P1-D 就在。
  - **只有放置成功且實際消耗主角行動時才累加**；失敗、免費槽、夜間放置不計。
  - 同分決勝：按 `npcs.json` 的穩定陣列順序取先者，結果可重現；P5 首輪慶典候選子序列為阿婕 → 阿薇 → 阿財。

---

## 十三、委託

> 企劃書第六節。委託能力在玩家第一次取得人物卡時教學，不綁死第 17 天；處方委託是第一輪第 17～19 天可處理的首個完整案例。

本節與 P4 資料契約**明文取代**企劃書 2026-08-25 前的「夥伴容量、扣除信任、任意數日後回報」舊模型；正式故事不得再保留「兩天後」等舊委託節奏。

- 委託＝把持有的 `person` 卡放進有 `delegation` 的槽。它不消耗主角行動格，也不增加 `npc_action_counts`；人物卡不離開手牌、不另占夥伴容量，仍照一般佔格規則占一格。
- 每位人物**一天最多受託一次**。成功後卡面狀態顯示「今日已受託」；`delegates_used_today` 只在隔日上午換日時清空，不因時段推進而清空。
- 結果預設當場知道；資料只能選 `immediate` 或極少數 `next_morning`。延遲必須在選擇前明示。隔日上午先完成既有發狂倒數／強制縱慾結算，再依加入順序播委託回報；回報後人物當天可再次受託。
- 同一任務的「主角親自做」與所有 NPC 路線共用 `choice_group`，任一路線成功後其餘全部互斥。主角路線消耗行動格且結果較完整；人物路線保留主角行動、回傳符合該 NPC 視角的結果與副作用。結果確定、沒有成功率骰。
- 信任、劇情旗標與人物卡持有狀態只作資格判斷，不是派出時扣除的貨幣。若玩家之後要查證委託結果，查證是另一個 beat／choice_group，不能回頭重開已結算的原任務。
- 委託預覽只揭露任務、回報時間與已知傾向／風險，不提前列出完整獎勵或文本。關係可能依事件增加、下降或不變，完全由該槽效果明寫；沒有全域委託好感增減。
- 候選只列事件資料明寫且有敘事意義的人物，不從職業或屬性自動推導。未取得人物卡者隱藏；已取得但今日用過者保留並鎖定為「今日已受託」；其他條件不足者保留並顯示資料的理由。
- 人物卡的取得、失去與重新取得都只能由明確故事效果造成，不因關係數值跨門檻自動翻轉。多數人物須先經歷相應劇情才願意幫忙；阿婕因開局友好可較早取得，但傷害信任的事件（例如對她使用縱慾卡）會阻止取得，直到明示的修復事件發生。
- 首輪處方案例持續到第 19 天或任一路線完成。第 17 天可能一張人物卡都沒有，此時只能親自處理；三人的精確取得 beat／slot／flag、回報時間與 D17～19 路線映射以 `故事線_第一輪_第二章.md > 第 17 天 > P4 人物卡取得與處方委託映射` 為內容單一真值。不得在第 17 天自動發放全部人物卡。
- `next_morning` 回報存的是資料接點，不複製效果快照；讀檔後以 beat／slot id 回查同一份資料。結束本輪會清空尚未回報的 run 層委託，不把它帶進下一輪。

---

## 十四、條件與效果語彙（封閉集合）

### `condition` / `requires` 運算子

語彙表與範例住 `SCHEMA.md > condition / requires 語彙`，本節只定求值語意：

| 運算子 | 求值 |
|---|---|
| `day` | 當前天數 == 值 |
| `day_at_least` | 當前天數 ≥ 值（「最早第 N 天」的載體，例：第 10 夜的附加 beat） |
| `has_card` | 手牌或知識集合含該卡 id（⚠️ 提案：備用區**不算**——寄放的卡不在身上） |
| `has_knowledge` | 知識集合含該 id |
| `switch` | 該開關已翻開 |
| `switch_progress_at_least` | 該開關累計值 ≥ n |
| `flag` | 該旗標為真 |
| `relation_at_least` | 見第十二節 ⚠️ |
| `madness_at_least` | 手牌發狂卡張數 ≥ n |
| `night_seen` | meta 層夜間到訪集合含該 location id |
| `opening_choice` | 當前 run 的開局選擇 id == 值 |
| `ending_seen` | meta 歷輪摘要曾出現該 ending id |
| `count_at_least` | `of` 內條件成立數 ≥ n（遞迴求值） |
| `not` / `all` / `any` | 邏輯組合（遞迴） |

- 未知運算子＝載入錯誤（架構基線第 6 條）。
- 求值無副作用、可重入——同一狀態下求值多少次結果相同。

### `on_place` / `on_enter` 效果鍵

兩者共用同一張表（`SCHEMA.md > on_place 效果`）：`text` / `gain` / `lose` / `switch` / `switch_progress` / `relation` / `madness` / `flag` / `festival_proxy` / `ending`。

- 結算順序固定：`text` 呈現 → `lose` → `gain` → 其餘鍵。`lose` 先於 `gain`，讓「升級知識卡」不會瞬間超格。
- `gain` 的卡按型別入位：`knowledge` 進知識集合；佔格卡進手牌，**手牌滿則觸發超載**（第三節）。
- `madness: n`＝加入 n 張新發狂卡（各自起錶）。
- `festival_proxy`＝D29 一次固定慶典代付者；形狀與候選規約只住 SCHEMA。
- `ending: <ending_id>`＝其餘效果完成後，以該 id 啟動結局；只能引用 `data/endings.json` 的正式 id。
- 未知效果鍵＝載入錯誤。

---

## 十五、結局流程

> 企劃書第十七節。結局分類與玩家看見的後日談分開：引擎只有四個穩定 ending id，正常結局內部再以資料填空，不為每種人生組合建立新 id。

| ending id | 玩家入口 | 演出 |
|---|---|---|
| `ending_replaced` | 第 45 天 evening coda 播畢 | 主角被取走；第一次完整播複製品的後半生，之後只播當輪摘要 |
| `ending_madness_be` | 發狂卡達 `madness_cap` | 停在本輪主角身上，不播複製品後日談 |
| `ending_inventory_be` | 資料的 `ending` 效果觸發 | 走進庫存，提前結束，不播複製品後日談 |
| `ending_refuse_boarding` | 已解鎖的開局選項「不上車」 | 在外地繼續生活、四十出頭以同樣的癌症早死，再被庇佑送回開局 |

第一輪可達的是前三種；「不上車」只有歷輪曾完成 `ending_replaced` 才解鎖。只走過 BE 雖已知道自己回溯，仍未被第 45 天替換，不得解鎖這個結果。

### 正常結局是組合，不是列舉

`ending_replaced` 建立快照時一次求出下列欄位，之後不再讀會被清空的 run state：

| 欄位 | 值與來源 |
|---|---|
| `partner_variant` | 阿婕／阿薇／無；只讀 D29 的邀請結果，不再疊關係完成度 |
| `livelihood_variant` | 依序讀 D35 答應叔叔、D43 回前老闆、D43 接周先生、皆未答應；同一路線再讀開關帶 |
| `inn_appearance_variant` | D26 修招牌／修湯屋／修二樓三選一留下的旅館樣貌 |
| `most_invested_npc` | 供「不邀」路線回顧；必須等於 D29 已凍結的 `selected_festival_proxy_npc` |

精確判定與文字片段住 `data/endings.json`。引擎只按資料的有序規則求值，不認識具名人物或生計分支。第一次完成 `ending_replaced` 才播完整二十年；是否播長版只查歷輪是否已有同 ending id，不直接看輪數。玩家若先走多輪 BE，第一次正常結局仍必須播長版。

### 兩階段收尾

1. `start_ending(ending_id, source)` 原子凍結結果、建立 `active_ending`、把 `flow_mode` 改成 `ending`，立即封鎖所有非結局 mutation；run 尚未清除。
2. UI 只讀結局 view model 並逐頁播放。逐字顯示中按一次＝立即顯示完整當頁；完整後再按一次＝下一頁，不能同一次輸入同時補完並跳頁。
3. 第一次看到某 ending id 時不可整段跳過，但可立即補完當頁；歷輪已看過同 id 才顯示獨立的「跳到摘要／結束」。`active_ending.page_index` 入序列化，還原後停在同一頁。
4. 全部必播頁完成後才可呼叫 `complete_ending()`：追加歷輪摘要、取得知識「我回來過」、增加輪數、清 run、把 `flow_mode` 改成 `opening`。任一步不得依賴 UI listener 補做。

`on_place`／`on_enter` 的 `ending: <id>` 是封閉效果鍵，其他同效果塊內容先結算，ending 最後啟動；資料壞引用直接載入失敗。D45 正常結局由「最後一日 evening coda 已完成」的通用輪末規則啟動，不在 UI 依 beat id 特判。

由 `gain`／`madness` 達上限衍生的發瘋 BE 也服從同一條：同一效果塊先完整結算，再凍結 ending snapshot。不能在 `madness` 鍵當下清 run 或切到 opening，否則排列在後面的 flag 會污染下一輪。

P5 的正式文本是**結構版草稿**：實作者可依故事骨架先寫可玩的氣氛版本，以便檢查重量與節奏，但不得新增世界規則或改變角色結果；正式文案、插圖、動畫與音效留內容期。

---

## 十六、迴圈、繼承與存檔分層

> 企劃書第七、十八節。

### 輪結束結算

`complete_ending()` 是唯一正式輪結束入口。它先把 active snapshot 追加進 meta `ending_history`，再取得知識卡 `k_i_returned`、增加輪數，最後整包清除 run 並進入 opening。歷輪每筆只存穩定結果 id：`run_number`、`ending_id`、`opening_choice_id`、`ended_day`、`ended_phase`、三個正常結局 variant、`most_invested_npc`、`knowledge_gained_this_run[]`；不複製 flags、手牌或已排版文字。

保留：meta 層全部（知識集合、`night_locations_seen`、`night_once_beats_seen`、教學、歷輪摘要與魔法物品集合）。清空：普通手牌、備用區、旗標、開關、關係、發狂卡與縱慾計數、投入帳、時段／夜間狀態、委託 daily／pending、active encounter、choice／事件紀錄、開局選擇與慶典代付結果。推論卡不跨輪。結局暫態在 history 寫入後清空，不能帶到 opening。

一般物品不穿越時間。極少數魔法物品可以在 `cards.json` 明示為迴圈保留：取得時加入 meta 集合，下一輪初始化手牌時恢復；明示永久失去時同步移除 meta。P5 正式資料沒有此類物品，只以 fixture 驗機制。所有可能同時恢復的佔格魔法物品加主角卡不得超過 `tuning.hand_size`。

⚠️ **冪等只擋得住「卡還在手上」那種情況。** 被 `lose` 收走過的卡，第二輪重演同一格時會真的發回來——升級型比對就是這個形狀（`d45_then` 把 `k_not_today` 換成 `k_already_on_list`）。**只有知識卡會遇到**，因為只有它跨輪保留；其餘卡型隨 run 層一起清空，第二輪本來就要重拿。發舊版的地方要加條件守衛，形狀見 `data/SCHEMA.md > 帶條件的卡片項目`。

### 存檔分層（硬要求）

| 層 | 內容 | 生命週期 |
|---|---|---|
| **run 層** | 第一節表格中「跨輪」以外的全部 | 一輪 |
| **meta 層** | 輪數、知識集合、`night_locations_seen`、`night_once_beats_seen`、教學、歷輪結局記錄、魔法物品集合 | 跨輪永久 |

兩層在序列化格式上分開，run 層可以整包丟棄而 meta 層不受影響——這就是「回到第一天」的實作。存檔 UI 與槽位設計留待存檔 Phase，但**分層從 P1-A 就生效**。

### 開局：出門前的十分鐘

- P5 啟動後，新的 meta 存檔與每次 `complete_ending()` 都進 `flow_mode == "opening"`。這是故事內的出門前十分鐘，不是「新遊戲／繼續／設定」標題畫面；正式標題與存讀入口留 P6／UI 正式化。
- 三個選項住 `data/opening_choices.json`，依資料順序顯示：帶父母舊相簿、回撥未接來電、不上車。前兩項永遠可選；不上車永遠看得見，但歷輪未完成 `ending_replaced` 時灰掉，理由不可暴露替換真相。
- 選項先預覽與確認，取消完整狀態零變化。成功選相簿／電話才初始化新 run、恢復主角卡與魔法物品、套開局效果、寫 `opening_choice_id`，然後進第 1 天；不上車不初始化 run，直接建立 `ending_refuse_boarding`。
- 相簿路線取得新的佔格卡 `item_family_album`；電話路線寫 `outside_job_waiting:true`。D7 拒信依開局選擇換一句回聲；D11 比對接受家庭相簿或既有畢業照；D43 前老闆與周先生路線共用 `choice_group:"leaving"` 且都消耗下午主角行動，分別寫獨立結果。
- 「不上車」是完整結局：每次都追加 history、增加輪數，再回 opening；第一次播外地人生與早死，重複時可用摘要。它不啟動小鎮 run、不產生本輪慶典代付者。

### D29 不邀的代付結算

慶典代付候選由 NPC 資料明示，首輪只有阿婕、阿薇、阿財，且沿用 `npcs.json` 中的這個順序；不是人物卡通用資格。玩家明示選不邀，或在 invitation choice 未結算下離開該時段，都必須走同一個資料化預設選項並立即固定 `selected_festival_proxy_npc`。候選中取 `npc_action_counts` 最大者，同分按 NPC 資料順序；全為 0 時讀 beat 的 fallback（阿婕）。後續 D31、D39 與結局只讀已凍結 id，不得重算。未來 NPC 只有同時具備資料資格、至少一個 `attention_npc` 投入槽與代付結果文字，才可加入候選。

### P5 明確不做

- 待決 25／26 的完整第二輪差異包與新增事件；P5 只證明第二輪可重入並讓既有 meta 規則生效。
- 玩家可打開的歷輪回顧 UI；P5 先保存摘要，P6 讓它進正式存檔，內容期再做回顧畫面。
- 輪內回到章節開頭／固定錨點的主動回溯；能力來源、錨點與快照規則尚未定，排在 P6 之後另立 Phase。
- 正式標題畫面、存檔槽、最終文案、美術、音訊與演出。

---

## 十七、資料完整性驗證

現有 `scripts/verify_data.gd` 已驗：JSON 可讀、id 唯一、跨檔引用存在。隨引擎 Phase 推進要加的 lint（驗收掛在對應 Phase）：

1. 語彙封閉性：所有 `condition` / `requires` / `on_place` / `on_enter` 的鍵都在第十四節表內。
2. 三態完整性：有 `requires` 的槽必須有 `reject_reason`。
3. 時段規約：免費槽（不收主角卡）所在面板必須有收主角卡的槽或屬 fixed beat（`SCHEMA.md` 規約）。（evening 收主角卡的槽由引擎強制呈灰，第二節——不是資料錯誤，不 lint。）
4. 縱慾完整性：任何一天、任何狀態下，無條件出口至少一個可達（砸東西、食慾對應的槽存在且無條件）。**判準是「進得了強制縱慾挑選池」，不是「玩家走得到」**（第八節）——所以只驗槽的 `condition` / `requires`，不驗地點開放時段。
5. 行動格供需：每一天每個行動時段，至少存在一個當天成立、收主角卡的槽（已有雛形——最近一個 commit 就是引擎數出三個空時段補的）。
6. **SCHEMA 對齊**：`SCHEMA.md` 未記載但資料已在用的欄位——beat 級 `on_enter` / `requires` / `reject_reason` / `encounter`、夜間地點的 `earliest_night` / `requires`、`echo` 的精確形狀——SCHEMA 要補列（已完成，2026-08-13）。
7. **夜間可達性**：定日夜 beat 的 `when.day` ≥ 所掛地點的 `earliest_night`；非 fixed 的夜間 beat 必須掛在 night layer 地點或旅館（`sanquan`）上，否則沒有合法入口。
8. **殘響可播出性**（第十一節）：`echo` 必須有 `day`，缺欄＝永遠不播；`echo.day` 必須大於該 beat 自己的 `when.day`（殘響是事後的痕跡，不能跟事件同時或早於事件）。**同一段 `echo.text` 不得在多個 beat 重複**——重複代表複本，只有一份會播。
9. **卡片型別封閉性**（P1-G／P4-A）：`cards.json` 用到的每個 `type` 都要在 `card_types.json` 有一筆，反向亦然；每張正式卡必須有 boolean `discardable`，遭遇丟棄與支付只讀此欄。
10. **縱慾出口資料完整性**（P2）：`accepts` 含 `madness` 的槽必須有 `indulgence` 物件且 `weight` 是整數；`on_place_by_level` 的鍵只能是 `light` / `normal` / `heavy`，值的內部鍵照第十四節的效果鍵表；`indulgence.auto` 為 `false` 的槽全作至多一個（泡湯）。
11. **夜間對位完整性**（P3-A）：有 `day_counterpart` 的 12 個 night row 必須有合法 `night_reveal`，且同一 `day_counterpart` 共用同一張 knowledge 卡；`day_counterpart == null` 者 `night_reveal` 必須為 null。knowledge 卡必須 `type == "knowledge"` 且 `slotless == true`。
12. **夜間地點狀態完整性**（P3-A）：night row 的 `chapter` 必須等於 `earliest_night` 所屬章節；`teaser_only` row 必須有 `reject_reason`，且 `madness_cost`／`day_counterpart`／`night_reveal` 必須為 null 或省略。
13. **舊夜間旗標退場**（P3-B）：正式資料不得再引用 `opened_n_*`；夜間路徑前置一律改用 `night_seen`，真正的本輪故事旗標不在此限。掃描範圍含 location、beat 與 `slots[]` 的 `condition`／`requires`／`on_enter`／`on_place`。
14. **夜間一次性 beat 完整性**（P3-C／P4-A）：`meta_once: true` 只能用於定日 night fixed beat；night-layer fixed 必須是 `meta_once: true`，但 `encounter.repeat_each_run == true` 是明示例外，且不得同時 meta-once。旅館 fixed 可依資料選擇是否 meta-once，D3 引導為 true、D24 故事事件為 false。**同一個 `when.day` 的 night-layer fixed 至多一個**——兩個會互相覆蓋當夜的地點選擇。
15. **委託資料完整性**（P4-A）：`delegation` 只能住在接受單一明確 `person` card id 的槽；同 beat 內所有委託槽與親自處理槽必須共用非空 `choice_group`。`result_timing` 只能是 `immediate`／`next_morning`；後者必須有 `report`、且事件最晚成立日小於第 45 天，前者不得有 report。鎖定條件必須有 `reject_reason`，引用的 person card 必須存在且 `type == "person"`。
16. **遭遇資料完整性**（P4-A）：遭遇至少一回合，round id 唯一且所有 `next_round` 可達；`per_round_slot_cost` 為正整數，`escape_cost` 為 null 或非負整數；`allow_discard` 若存在必須是 boolean，省略視為 true；`after_finish` 必須是 `stay`／`advance_phase`；`fallback.requires_discardable` 若存在必須是 boolean。每個 response 的 `accepts` 非空、引用合法且同回合不重疊，fallback 與三種出口齊全；`consume_card` 不得替通用事件建立人物永久失去。`repeat_each_run` 只可用於**有明確 `when.day` 的 fixed encounter（含 dated night beat）**。`charge_first_visit: true` 必須同時滿足 `fixed == true`、`when.day` 是明確整數、`when.phase == "night"`、所掛 `location.layer == "night"`；這是該欄位的窄限制，不代表所有 night beat 都必須掛 night-layer location。D8 同時符合兩組限制。
17. **結局資料完整性**（P5-A）：四個 ending id 恰各一筆；類型、長版／摘要版、必填片段與正常結局三組有序 variant 規則完整。所有 condition、文字與引用合法，每組規則必須有唯一 fallback；正式效果的 `ending` 只能引用既有 id。玩家字串不得直接使用破梗的內部 id。
18. **開局與選擇完整性**（P5-A）：三個正式 opening choice id 唯一、順序固定、效果／ending 二擇一；不上車必須以歷輪 `ending_replaced` 為門檻並有鎖定理由。標為 `default_if_unresolved` 的 choice group 恰有一個預設槽，該槽必須同組且可由規則層無卡結算；`choice_requires_card:true` 必須有非空 accepts、不得兼任 default，無卡選定要被規則層擋住。D29 invitation 必須具有預設，D43 兩個工作槽必須要求主角卡。
19. **跨輪與慶典代付完整性**（P5-A）：迴圈保留卡必須 unique 且引用合法，全部可能恢復的佔格數加主角卡不得超過手牌上限。慶典代付候選必須是合法 NPC、至少被一個 `attention_npc` 槽引用，並在 endings 資料有對應片段；D29 零投入 fallback 必須是候選。首輪正式候選恰為阿婕、阿薇、阿財。

---

# Phase 1 — 最小可玩迴圈（驗收意圖）

> 目標：**「選地點 → 放卡 → 推進」的全文字可玩版，45 天一路走到底。** 資料層從「讀得動」升級到「玩得動」。
> 不含：夜間標記經濟（P1 夜間層是過場 stub）、發狂時鐘、備用區與超載、委託、遭遇、關係求值以外的關係表現、存檔 UI、開局選單、任何美術與音訊。
> 各子階段的實作契約見 `開發設計方針.md` 對應段；操作步驟見 `測試指南.md` 對應段。
> **通則驗收（每個子階段收尾都重跑）**：GameState 序列化往返相等——欄位隨子階段增加，P1-A 那條斷言不是一次性的，新增的手牌／旗標／事件／選擇欄位都要接進往返測試。

## P1-A 遊戲狀態與時段狀態機

範圍：GameState（第一節的狀態窮舉，跨輪群先立空位）＋第二節狀態機。夜間與 evening 先作為「顯示一行文字即過」的空時段。

驗收：
- 從第 1 天 morning 推進到第 45 天 evening 播畢後進入結局 stub，全程不 crash。
- 章節恰在第 16、33 天切換；第 45 天無 night 時段。
- `tuning.phases_per_day` 與結構常數不一致時載入報錯（一致性驗證，第二節——它不是可調數值）。
- GameState 可序列化成 Dictionary 再還原，往返相等（存檔就緒斷言）。

## P1-B 卡片與手牌

範圍：卡片實體化（`cards.json` → runtime）、手牌容器、知識集合分離、型別操作矩陣（第三節）中 P1 用得到的行：protagonist 釘死、knowledge 不佔格。備用區、超載本 Phase 不做（手牌滿時 `gain` 先直接照收並記警告）。

驗收：
- `gain` / `lose` 正確進出手牌與知識集合，佔格計數正確。
- 主角卡無法被丟棄或移出；知識卡永不佔格。
- 手牌渲染為最底一排（全文字可）；知識另列。

## P1-C 地圖、面板與三態

範圍：白天地點清單（layer / chapter / phases 過濾）、面板聚合（第四節成立規則）、槽渲染（occupant / 空槽）、三態與 `reject_reason`（第五節）、fixed beat 自動播出與 `on_enter` 一次性結算。

驗收：
- 第 1 天 evening 到站事件（`d1_arrival`）自動播出、`on_enter` 使主角卡入手，且重看不重複結算。
- 任一含 `requires` 的槽在條件不足時灰掉並顯示理由、不可放卡；條件補齊後同面板重求值變可選。
- `condition` 不成立的 beat 與槽完全不出現。
- 第 3 天下午（阿宏／夫妻／帳本三搶一）三個 beat 的面板全部按資料呈現。

## P1-D 放置與效果結算

範圍：拖放（或全文字的「選卡放入」）、放置合法性四步檢查（持有→三態→accepts→action_spent，第六節）、`on_place` 全效果鍵結算（第十四節，含順序）、主角卡消耗行動格、免費槽不消耗（第六節）、`npc_action_counts` 投入帳（第十二節）、`condition` / `requires` 全運算子求值器。

驗收：
- 放主角卡結算效果並消耗該行動格；同一時段不能再放第二次主角卡。
- 比對槽（`accepts` 不含 protagonist）放情報卡不消耗行動格。
- 全部十三個 `condition` 運算子（`not`／`all`／`any` 各算一個，含 `day_at_least`）各有至少一條 headless 斷言，含 `count_at_least` 混搭與邏輯巢狀。
- 未知語彙鍵在載入時報錯（架構基線第 6 條）。
- registry 存在但未持有的卡放置失敗，GameState 零變化；UI 與直接呼叫規則層 API 結果相同。
- 帶 `attention_npc` 的槽放主角卡成功後，該 NPC 投入帳恰 +1；失敗、免費槽、重複嘗試不計。

## P1-E choice_group

範圍：SCHEMA 的 `choice_group` 語意——互斥、選後同組收起（不是灰掉）、不選合法、結算即定案不可反悔（已拍板）。組內 `accepts` 是**多一條入口**，不是不同結果：帶卡選與直選結算同一個槽的同一個 `on_place`，結果相同。**選擇是原子操作**：兩條路走同一個結算入口，除卡片消耗外留下完全相同的狀態；重複選擇＝no-op，不重複結算。**被選項之後呈唯讀展示**（RESOLVED——你選了什麼要看得到），choice 的顯示規則優先於一次性槽的收起規則。

驗收：
- 選定一個槽後，同組其餘槽消失；choice 記錄寫入 GameState。
- 直接離開面板＝不選，之後重開面板選項仍在（除非資料的 `when` 已過）。
- 無卡玩家可直接選（不放卡結算）；有卡玩家放卡走加分路徑，兩個入口同一個組都通（拿第 22 天「你覺得哪裡不對」驗）。

## P1-F 45 天全程走通與迴圈 stub

範圍：evening 殘響播出（第十一節提案管道）、夜間層 stub（夜間地圖顯示但只有免費地形可進、三步解析照第九節做真的、不開標記不給發狂卡）、第 45 天 evening 結局 coda（真 beat `d45_then` 含免費比對槽）＋結局 stub 文字、輪結束結算（第十六節：meta 保留、run 清空）、回到第 1 天（開局選單 stub 直接跳過）。

驗收：
- headless 自動走查腳本（貪心策略：每個行動格放主角卡進第一個可選槽）從第 1 天跑到第 45 天結局 stub，exit 0。
- 錯過的 beat 的 echo 在正確的天播出（拿第 3 天下午 `d3_pm_sanquan` 那條驗：錯過則第 5 天 evening 播出，到場則不播）。第一章三次殘響全數播出：第 5 天（`d3_pm_sanquan.echo`）、第 8 天 evening（`d8_echo_bathhouse`）、第 13 天（`d12_pm_awei.echo`）。
- 第二輪開始時：知識集合完整保留、手牌只剩主角卡、旗標開關關係全空、天數回到 1。
- 夜間可達：第 24 夜的旅館 fixed 定日 beat 在入夜強制播出（解析第 1 步）；颱風夜條件成立時，直接睡會播對應的 bleed beat（解析第 4 步）。
- 第二輪重入到站事件後主角卡仍恰好一張（`gain` 冪等）。
- 全程 `verify_data` 與第十七節 lint 1–3、5、7、8 全綠。
- 同一天多個 evening `fixed` beat 依陣列順序結算，且殘響行排在全部 evening fixed beat 之後（第十一節）。拿第 27 天驗：`d27_evening_awei_confirm` 先寫 `dodger_awei_cleared`，`d27_evening_cover_knowledge` 才讀得到。

## P1-G 面板互動模型

範圍：把 P1-C／P1-D 做出來的「開面板即一次攤開全部內容並結算」改成第四節的兩階段模型——**進入地點＝beat 依序演出，演完才開放卡槽互動**；卡槽常駐標示型別、右鍵預覽可放的卡（第五節）；沒有卡放得下去時高亮推進按鈕（第六節）。白天與夜間同一套。

> **為什麼回頭改**：原本的第四、六節把「開面板即結算」寫成硬規則卻沒寫理由，實際玩起來的後果是**光是好奇點進一個地點，狀態就已經動了**——玩家連看一眼都要付代價，而放卡之前又完全看不到那一格收什麼。不可逆本身是對的（那是本作的核心張力），錯的是把不可逆放在「進門」而不是「出手」。P1-G 只搬這條界線，不給後悔藥。
>
> **不回頭改 P1-C／P1-D 的段落**：那兩節已通過驗收並歸檔，改掉會讓「哪一版通過驗收」講不清楚。互動模型的驗收意圖一律住本節。

驗收：
- 進入任一地點，成立的 beat **依資料順序逐一演出**，玩家按一下才推進到下一個；演出未完成前卡槽不可互動。拿第 32 天上午的寺廟驗：該面板掛著 4 個帶 `on_enter` 的 fixed beat，但**後三個是互斥的條件變體**（`invited_ajie` ／ `invited_awei` ／ 兩者皆無），所以一次只會演到 `d32_festival` 加上當下成立的那一個，**不多不少**。要驗的是「成立的全部演到、不成立的一個都不演」，不是筆數。
- `on_enter` 在該 beat 演出的那一刻結算，且每輪至多一次。**已看過的 beat 重進地點會重播文字，`beats_entered` 不變、效果不重複套用**（拿任一帶 `gain` 的 fixed beat 驗：重進之後手牌張數不變）。
- 每個可放卡的槽常駐顯示型別名，名稱取自 `data/card_types.json`。拿第 22 天下午堆沙包的觀察槽驗：`accepts` 是 `equip_polaroid` 與 `info_acai_box`，要顯示成「裝備卡、情報卡」。
- 右鍵（觸控長按）叫出預覽，列出手上放得進該槽的卡；**預覽關掉之後 GameState 逐欄位不變**。灰掉的槽也能預覽，回傳空清單加 `reject_reason`。
- 當前時段沒有任何動作可做時（沒有合法的（槽, 卡）組合，且沒有尚未結算的 OPEN `choice_group`），推進按鈕呈提示狀態；**時段不會自己推進**，玩家仍可繼續逛其他地點。
- 夜間與白天走同一條演出流程（同一個規則層入口），不是兩份實作。
- `verify_data` 新增 lint 9：`cards.json` 用到的 `type` 與 `card_types.json` 的 `id` 雙向封閉，任一邊缺就報錯。
- headless 走查腳本（`playthrough_greedy.gd`）在新模型下仍 exit 0，90 個行動時段的分類統計不變。

## P1-H 手牌可讀性

範圍：**把手牌列從「一行 id 疊在一起」改成看得懂的東西。** 每張卡各自呈現、點得開、顯示卡的顯示名而不是 id；知識移出手牌列自成入口；卡片內容看得到。

> **為什麼要有這一段**：P1-A～G 收尾之後第一次從頭玩，手牌列印的是 `protagonist info_husband_version info_wife_version routine_debt` 這樣一長串 id 接在一起。**玩家看不出兩張「說法卡」差在哪**，而那兩張卡的差異正是第一章調查線的全部內容——資料裡的 `text` 早就寫好了，只是畫面沒有任何路徑看得到它。這一段解的是「手上有什麼」讀不出來，不是介面好不好看。
>
> **邊界（寫在這裡是因為它很容易被順手擴大）**：P1-H **不碰 UI 型態**（`待決事項.md` 23：地點清單／拖卡／自由桌面），不做美術、字體、配色、動畫、拖放，不碰 i18n。判準只有一條——**只解決「玩家看不出手上有什麼」**。要做正式版面是內容期的 UI 正式化，不是這裡。

驗收：
- 手牌上的每一張卡**各自可見、各自可點**，顯示的是 `cards.json` 的 `name`（顯示名），畫面上任何地方都不再出現卡的 id。
- 卡的顯示名放不下時**截斷並顯示省略符號**；完整名字在詳情裡看得到。
- 點手牌上的一張卡 → 開出**唯讀**詳情，內容是該卡的顯示名與 `text`。關掉之後 `serialize()` **逐欄不變**。
- **詳情裡沒有任何放置入口。** 放卡一律從槽那邊發起（P1-G 的兩階段模型），維持單一心智模型：一個行動格只出手一次，而出手的地方是槽不是手牌。
- 知識**不佔手牌格也不排進手牌格線**，另有一個入口，**入口本身顯示目前知識張數**；點開列出全部知識卡的顯示名與 `text`，同樣唯讀、關掉後狀態不變。知識累積到單頁放不下時，清單必須可捲動，最後一張仍可由玩家到達。
- 手牌區固定 7 欄，只建立目前持有卡片的格子，**不依 `tuning.hand_size` 畫空框**。`tuning.hand_size` 繼續只管遊戲容量與既有的 `手牌 n / 上限`；改值後重啟，上限必須反映而不改程式。卡片超過兩列時在手牌外框內垂直捲動，不得撐大外框或侵入 `ContentView`。
- 多實例卡（發狂卡）在手牌上**各自獨立呈現、不合併計數**。P1 期間資料裡拿不到發狂卡，這一條**隨 P2-A 一起驗**；P1-H 只負責讓版面與 `qa_id` 現在就相容。
- 手牌列的**外框位置與高度不變**。只改內部排版，不動 `ContentView` 的範圍。
- 既有的佔格計數（`手牌 n / 上限`）照舊顯示，位置可移動、格式不變。

---

# Phase 2 — 發狂時鐘與縱慾（驗收意圖）

> 目標：**把發狂卡從「一種卡型」升級成「一條會自己走的時間軸」。** 第八節整節真值化，加上第九節裡「開收費標記就發卡」那一條。
> 不含：備用區與超載（`待決事項.md` 34 未定，而且第一輪碰不到 cap，壓力測不到）、夜間標記的其餘經濟（對位改名、meta 跨輪免費、地點級 `requires` 真值化——那些是 P3）、真結局流程（P5）。
> **P2 的範圍邊界一句話**：發狂卡的**產生、倒數、清除、上限**四件事做真的；夜間層除了「發卡」以外維持 P1-F 的 stub。
> 各子階段的實作契約見 `開發設計方針.md > Phase 2`；操作步驟見 `測試指南.md > Phase 2`。
> **通則驗收（每個子階段收尾都重跑）**：`test_boot.gd`、序列化往返相等（新增 `madness_clock` 的錶值、縱慾次數、待執行的強制縱慾佇列）、既有十套 headless 無迴歸。

## P2-A 發狂卡的產生與倒數

範圍：`madness_clock` 從「只有結構」變成真的走錶；夜間開收費標記時發卡。

> 本節記錄 P2-A 當時的過渡契約；P3-B 會把「本輪已開」改為 meta 終身到訪，P3-C 會改 fixed 到訪語意。現行規則以第九節與 P3 區塊為準。

驗收：
- 每天 morning 開始時，桌上每張發狂卡的剩餘天數 −1。**產生當天不倒數**——卡在 night 產生，tick 在次日 morning，順序自然成立，不得為此加旗標。
- 第 6 夜開的卡在第 13 天 morning 歸零（`madness_countdown_days` 為 7 時）。這一條同時驗「各走各的鐘」：同時存在的多張卡，剩餘天數各自獨立。
- 進入一個 `madness_cost` > 0 的夜間地點，且該地點本輪尚未開過 → 手牌加入該數量的發狂卡實例，每張的錶都是滿值。**已開過的地點再進不再發卡。**
- **一夜最多開一個標記**（第九節，待決 44）——玩家選了地點之後，同一夜不能再開第二個。
- 免費標記（`madness_cost` 為 0）不發卡；入夜強制播出的 fixed 定日 beat 不算開標記、不發卡（第九節解析第 1 步）。
- 發狂卡佔手牌格；`hand_slots_used()` 把它們算進去。
- 序列化往返：錶值、實例編號計數器、本輪已開標記集合全部還原得回來。

## P2-B 縱慾出口與主動縱慾

範圍：六個出口進資料；主動縱慾的規則層唯一入口；泡湯特例。

驗收：
- **六個出口在資料裡都有載體**（企劃書第七節那張表）：砸東西、食慾、奢侈、暴力對人、性慾、泡湯。出口＝`accepts` 含 `madness` 的槽，沒有縱慾選單。落點與條件見 `開發設計方針.md > P2-B`。
- 放一張發狂卡進出口槽：**消耗一個行動格、消掉玩家挑的那一張、套該槽 `on_place`**。同一個行動格之後不能再放主角卡。
- 拒絕路徑與第六節同一套四步檢查，任一步不過＝GameState 零變化。
- **泡湯**：只在 morning 發動；消耗 `indulgence.soak_phase_cost` 格（等於當天剩下的全部行動格）、只清 `indulgence.soak_cards_cleared` 張。afternoon 時該槽呈灰＋理由。
- 手上沒有發狂卡時，出口槽**不顯示**（`condition` 用 `madness_at_least: 1`）——這是三態裡「不顯示」那一態的正當用法：沒有那張卡的人不需要知道有這件事。
- 主動縱慾也計入本輪縱慾次數，並照第八節的曲線決定強度級（主動與強制**共用同一個計數**）。
- `verify_data` 新增 lint 10（第十七節），且 lint 4 在真資料上通過。

## P2-C 強制縱慾

範圍：倒數歸零的自動執行、出口挑選演算法、升級曲線。

驗收：
- 任一張卡歸零的那一天，遊戲代替玩家執行縱慾：**吃掉該日第一個尚未使用的行動格、消掉那張卡**。玩家在那一格不能再放主角卡。
- 同日多張歸零：各佔一格，依序吃。**當日行動格不夠時，餘下的順延到次日**——帳不豁免、不合併、不跳過。
- 出口挑選：條件成立的出口中取 `indulgence.weight` 最大者；同分取資料順序第一個。**泡湯永不入池。**
- **挑選只看條件，不看玩家位置**（第八節）。
- 強度級照 `forced_light_count` / `forced_normal_until` 查表；基底 `on_place` 先套，再套 `on_place_by_level` 對應那一組。
- **第一次一定是輕的，而且要明講下次更重**（企劃書第七節：「一張帳單，也是一則預告」）。
- 全程決定論：同一個存檔重跑，挑到的出口、天數、順序完全相同。

## P2-D 視野門檻與發瘋 BE

範圍：`madness_at_least` 在真資料上生效；`madness_cap` 觸發 BE。

驗收：
- 桌上發狂卡張數 ≥ `madness_vision_threshold` 時，掛該條件的內容出現；低於門檻時**不顯示**（不是灰掉）。
- 張數達到 `madness_cap` 的**那一瞬間**：本輪立即結束，進入發瘋 BE。**不播後日談骨架**（企劃書第十七節）。
- BE 走既有的 `end_run()` 收尾路徑，只多帶一個結局類型；**不新增第二條輪結束路徑**。完整結局流程留 P5，屆時本 stub 由 `complete_ending()` 取代。
- BE 之後回到第 1 天 morning，meta 層（知識集合）照第十六節保留。
- 觸發點在「加入第 N 張」的那一步，不是下一次 tick——**多發幾張同時到 cap 也只結束一次**。

## P2-E headless 重演三種玩家

範圍：把 `subdocs/驗證/發狂卡機制模擬.md` 第三節那張表變成會紅的測試。

驗收：三種玩家（A 深潛／B 典型／C 謹慎，定義見該檔第二節）在引擎上重演，逐項對上模擬的數字：

| | A 深潛 | B 典型 | C 謹慎 |
|---|---:|---:|---:|
| 桌面峰值 | 4（第 11 天） | 3（第 8 天） | 1（第 6 天） |
| 觸及 cap | 從未 | 從未 | 從未 |
| 強制縱慾次數 | 12 | 12 | 0 |
| 其中最重級 | 5 | 5 | — |
| 主動縱慾次數 | 0 | 0 | 14 |
| 吃掉的行動格 | 12 / 90 | 12 / 90 | 14 / 90 |
| 桌上 ≥3 張的天數 | 19 / 45 | 22 / 45 | 0 / 45 |
| 開到的收費標記 | 14 / 14 | 14 / 14 | 14 / 14 |

- 視野窗口也要對上：A 第 8–23 天＋第 42–44 天；B 第 8–26 天＋第 42–44 天；C 無。
- **數字不合就是紅的，不是「模擬過期了」**——真的過期時改的是模擬那份文件，然後這裡跟著改，並在該檔第八節記一次差異。
- 這一條同時是 P2 全部規則的整合測試：倒數、發卡、挑選、曲線、行動格帳，四樣任一條錯了數字都對不上。

## P3-A 夜間資料真值化

範圍：把第九節已拍板的夜間資料契約完整落到 schema 可驗證的正式資料；本階段不改玩家流程。

驗收：
- 10 個 distinct `day_counterpart` 各有且只有一張 slotless knowledge 對位卡；12 個可對位 night row 的 `night_reveal` 全部指向正確卡，同一白天地點的多個 row 共用卡；16 個夜間限定 row 維持 null。
- 對位卡名稱只指出「夜裡可以走回哪個白天地點」，文字不列出其他尚未到訪的夜間分區。
- 28 個夜間 `name` 全數按「進場前可觀察的引子」審過；發現結果留在 beat，不新增 reveal-name 欄位。
- 6 個阿宏地點級門檻都有具體 `reject_reason`；`info_ahong_private` 等真正內容門檻保留，不因改路線狀態而刪掉。
- `n_corridor_end` 改為明示的 teaser-only row；第三章可見、永遠不能進入。
- `n_corridor_end`、`chapter`／`earliest_night` 與對位資料契約由 lint 11～12 守住，並有壞資料 fixture 證明每條真的抓得到錯誤。

## P3-B 夜間狀態與進入規則

範圍：meta 到訪／一次性狀態、終身首次收費、一夜一地點與原子進入入口。

驗收：
- `night_locations_seen` 與 `night_once_beats_seen` 進 meta 序列化、跨輪保留；`end_run()` 不清。當夜選擇與睡覺中繼狀態在 run 層，換夜／結束本輪會重置。
- 首次主動進入收費 row：先記 seen，再依 `madness_cost` 發卡，再交給內容流程；重進、第二輪重進都不再發 marker cost。
- 首次進場撞到發瘋 BE：結局收尾後該 row 仍在 meta seen。
- fixed 強制到訪：記 seen、免 marker cost、消耗今晚；即使 row 以前已主動到訪，尚未播過的 fixed 仍要播一次。
- 同一夜一旦主動或強制到訪，第二個地點入口全部拒絕；只看詳情不算選擇。
- **每一種拒絕都分辨得出來**：不在夜晚、地點不存在、不是夜間地點、teaser、時候未到、門檻不足、今夜已選、今夜已睡，八種各回自己的機器碼，且任何一種拒絕之後完整狀態逐字不變。同時「時候未到」與「門檻不足」都成立時，玩家看到的是「時候未到」。
- `night_seen` 只讀 meta seen；正式阿宏鏈跨輪不必重走已走過的前級。
- 正式資料裡所有 `opened_n_*` 退場：阿宏前三級的路徑讀取改用 `night_seen`，9 個純寫入旗標直接刪除；`opened_n_gathering` 的真正語意是「本輪已看過聚會常態內容」，改名為本輪故事旗標 `saw_n_gathering_intro`，不得改成跨輪 `night_seen`。`info_ahong_private`、`k_ahong_point_*` 等內容門檻仍保留。
- lint 1 認得 `night_seen`，lint 13 在正式資料全綠，並有壞資料 fixture 證明未知地點 id 與殘留 `opened_n_*` 都抓得到。
- 空內容地點仍完整走進場管線：記到訪、依首次規則收費、消耗今晚，且不製造不存在的 beat id。**通用結果文字的呈現屬 P3-C**，本階段不驗。

## P3-C 夜間內容流程

範圍：唯一 resolver、fixed／地點／睡覺三條流程與夜間按鈕停拍。

驗收：
- 同一份夜間候選解析＋規則求值決定定日主內容、章節變體與附加 beat；`PanelBuilder`、睡覺與合法性檢查沒有各自的 `when` 掃描。所有定日候選都必須明確匹配當天與 `when.phase == "night"`，不能混入同地點的白天 beat。
- 主內容候選優先序為「當日定日 beat > 當前章節以下由高至低的章節變體 > 空結果」。`condition` 不成立的候選不存在，繼續往下 fallback；`requires` 不成立則保留該候選並呈 LOCKED，不 fallback。附加 beat 依資料順序排列，只納入 `condition` 成立者。
- `DataFacts.beat_matches_time()` 只負責有 `when` beat 的日期／時段匹配；無 `when` 的夜間章節變體與 add-on 只由夜間候選解析負責。面板與睡覺共用同一份已求值內容；睡覺跳過 `requires` 不成立的內容。
- D1／D2／D15 fixed 整份存檔只強制播一次；後續輪次同日不強播，該地點可正常選擇並解析章節內容。D24 每輪仍播且不消耗地點選擇。
- D3 引導第一輪只播一次、不算到訪、不用掉今晚；第二輪直接進夜間清單。
- `n3_map_opens` 在本階段改成無槽、無發狂效果、meta-once 的旅館環境引導；D1／D2／D15 的 night-layer fixed 標為 meta-once，D24 旅館 fixed 不標。lint 14 同批啟用。
- 合法但零內容的地點顯示通用結果（`empty_result`），不寫入 `beats_entered`、不製造假 beat id。
- 未選地點按「直接睡」：有睡覺內容時先播、停在「進入隔天」；無內容時可直接換日。已到訪後按「結束今晚」不得再解析睡覺內容。
- K-30、K-33、K-69 的根因在本階段消失；睡覺文字確實進 `FlowText`，不會同一次點擊立刻被換日清掉。

## P3-D 夜間地點清單與詳情 UI

範圍：沿用 P1／P2 文字清單與既有確認元件，補齊夜間狀態、詳情與入口；不做正式地圖美術。

驗收：
- 清單只按 `earliest_night`＋資料順序穩定排列；狀態改變不跳位。
- 未到訪／已到訪未對位／已對位，以及夜間限定 row 的兩態，逐字符合第九節；畫面任何地方都沒有「免費」或未到訪價碼。
- 地點項目可點開唯讀詳情；門檻不成立時詳情可讀、理由可見、進入鍵 disabled。按 disabled 入口與只看詳情都讓完整序列化狀態零變化。
- 今晚已主動選擇、被 fixed 強制帶入，或已按「直接睡」播完睡覺內容而停在「進入隔天」之後，夜間清單仍保留全部當下可見 row 與原排序；每一項仍可查看詳情，但所有進入鍵都 disabled。
- 可確定進場會觸發 BE 時顯示不帶數字的風險警告；其他未到訪 row 不因 `madness_cost` 不同而洩漏差異。
- teaser-only row 第三章出現、可看理由、永遠沒有可用進入路徑。
- 夜間主按鈕的「直接睡／結束今晚／進入隔天」與流程狀態一致；不能靠 UI 繞過規則層的一夜一地點。

## P3-E 對位系統

範圍：白天地點內的免費對位確認、knowledge 發放、衍生狀態與改名。

驗收：
- 白天地圖不顯示可對位提示；只有打開正確 `day_counterpart` 的常態面板後出現「把兩邊對起來」。錯誤地點沒有按鈕。
- 確認按鈕沿用既有確認互動；確認前零變化，確認後只新增對應 knowledge，不消耗行動格、不吃卡。
- 一對一 row 對位後顯示白天地點名；多對一 row 顯示白天地點名＋夜間分區後綴。
- 同一白天地點已有多個已到訪 row 時，只出現一個按鈕，一次取得共用 knowledge；所有已到訪 row 立即顯示已對位。
- 共用 knowledge 已存在、另一 row 尚未到訪時不提前改它的 `[尚未到訪]`；日後首次到訪後自動顯示已對位，不再要求第二次確認。
- serialize 裡不存在 matched／aligned 集合；刪除 knowledge 或 seen 任一方，衍生的已對位狀態就不成立，證明沒有第三份狀態。
- **對位確認的拒絕同樣分辨得出來**：不在白天時段、地點不存在、傳的是夜間地點、一個 counterpart row 都沒到訪、共用 knowledge 已在手、資料自相矛盾，六種各回自己的機器碼，拒絕後完整狀態逐字不變。「按鈕沒出現」不等於「入口擋得住」——這個入口繞過畫面也叫得動，兩邊的判準必須一致。

## P3-F 全流程驗收

範圍：把 P3-A～P3-E 接進第一輪、第二輪、既有 P1／P2 與 UI 模擬工具鏈。

驗收：
- 28 個 night row 全部進狀態矩陣：開放時機、門檻、首次／重進、名稱、狀態、teaser 與空內容均至少有規則層證據；12 個可對位 row 與 10 張卡全部有資料衍生斷言，不用手寫另一份映射表。
- 第一輪以新 fixed／seen 流程重跑兩條具名策略，不再以資料順序的「第一個收費 row」當策略：路徑效率案例在 D14 先走 `n_ahong_3`、D15 fixed 免費取得 `n_plaza`，走滿現有收費 row 的 marker cost 基準為 13；最大壓力案例在 D14 主動走 `n_plaza`，基準為 14。結果同步更新發狂卡模擬與 P2-E 基準，不把其中一條冒充唯一正解。
- 第二輪重演證明已到訪 row 不再收 marker cost、未到訪 row 仍在首次進入時收費；本條只驗終身首次收費規則，不驗第二輪壓力。第二輪主要發狂卡供給等待第二輪新增／深化內容決定，P3 不補內容。
- D1／D2／D15／D24、D3 引導、直接睡有內容／無內容、主動到訪與 fixed 到訪都各有流程案例。
- K-34、K-35、K-68 剩餘畫面證據、K-70 的地圖斷言在本階段收尾。K-72 不屬 P3 阻塞，不得順手擴大範圍。
- 每個子階段既有 headless、`test_boot.gd`、序列化往返與 UI 契約無迴歸；P3-F 另跑完整第一輪＋第二輪整合重演。
- P3-F 完成前至少由人操作一次夜間清單的 28 項可讀性、三種按鈕停拍、灰地點理由與對位命名；機器全綠不能代替這四項體感判斷。

---

# Phase 4 — 委託與遭遇（驗收意圖）

> 目標：把「同一時間只能在場一次」落成兩種共用卡槽語言的抉擇：人物代替主角調查，以及遭遇吞噬玩家有限手牌。P4 不新增第二套卡組、戰鬥數值、隨機成功率或人物永久死亡系統。
> 各子階段的實作契約見 `開發設計方針.md` 對應段；操作步驟見 `測試指南.md` 對應段。

## P4-A 資料與 SCHEMA 真值化

範圍：先定委託／遭遇 schema、翻譯欄位與 lint，再把 D8、D17～19、D45 改成正式案例；不接玩家操作。

驗收：
- `SCHEMA.md` 精確記載 delegation、encounter、延遲 report、`cards.discardable`、`fallback.requires_discardable`、`after_finish` 與可翻譯文字；正式資料可由通用規則讀懂，不靠 beat id 特判。
- D17 不再自動取得三張人物卡；三人的取得前置、信任破壞／修復、回報 timing 與處方任務持續窗精確符合第二章故事線的 P4 映射表。所有人物得失都由明示效果造成。
- D8 的三回合 response matrix 精確符合第一章故事線的內容真值；固定姓名開場不算一回合，所有 fallback 啟用可丟棄卡篩選。D45 改為單回合、不可逃且每種卡路線有獨立回應。
- lint 14 的 per-run 遭遇例外、lint 15～16 與對應壞資料 fixture 全部成立；既有 lint 不因新欄位被放寬。

## P4-B 委託規則

範圍：run 狀態、原子委託入口、當場／隔日上午結算、序列化與換日重置；不做正式委託面板。

驗收：
- `delegate()` 成功原子地完成條件檢查、choice_group 結算、今日已受託記帳與即時效果或延遲排程；失敗後完整狀態逐字不變。
- 每位人物一天只能成功受託一次，不同人物互不影響；跨時段仍鎖，隔日上午回報完成並重置後可再用。人物卡全程留在手牌。
- 委託不消耗主角行動、不增加 `npc_action_counts`；親自做與所有人物路線仍互斥，且結果完全確定。
- pending report 序列化往返相等；同日上午多筆按派出順序結算，既有發狂／強制縱慾順序不被改寫；結束本輪清空 pending。
- 拒絕至少分辨：時段不合法、beat／slot 不存在、不是委託槽、未持有人物卡、人物型別錯誤、不在 accepts、今日已受託、條件不足、choice 已結算、資料衝突；每種都有穩定機器碼。

## P4-C 委託 UI 與首個案例

範圍：人物卡狀態、委託候選／預覽／確認／結果與第一次取得人物卡的教學；接通 D17～19 處方案例。

驗收：
- 面板只列資料明寫的候選；未取得者隱藏，今日已受託者以同一位置顯示鎖定狀態，其他缺條件者顯示資料理由。排序不因狀態變化跳動。
- 確認前清楚顯示任務、`立即回報`／`隔日上午回報` 與傾向／風險；不洩漏完整結果。取消零變化，成功後即時刷新人物卡和互斥槽。
- 第一張人物卡在任何日期取得只發出 `delegation_tutorial_available`，不直接改 meta；UI 實際顯示並關閉／略過後才由 `mark_delegation_tutorial_seen()` 寫入 `delegation_tutorial_seen`。純規則或 headless `gain_card()` 不得提前吃掉教學；第 17 天零人物卡時不彈假教學、不出空候選，親自做仍可完成。
- 阿婕、阿珠、阿財三種取得路徑與信任破壞／修復案例均由故事效果驅動；D17～19 任一路線完成後處方委託消失，其餘路線不可再選。
- 即時結果當場進 FlowText；延遲結果在下一日上午既有結算完成後播出，讀檔前後呈現一致。

## P4-D 遭遇規則

範圍：遭遇狀態機、佔格、回應／錯答／丟棄／逃離與序列化；不做正式遭遇畫面。

驗收：
- 定日時段到達時由規則層建立 `stage == "intro"` 的 active encounter；此時推進時段、睡覺、地圖與其他非遭遇 mutation 全部拒絕。只有 `acknowledge_encounter_intro()` 能轉入第一回合，第一回合只增加一次佔格；後續每回合各增加一次，超載 penalty 另加一次。
- 可提交集合精確等於 `hand ∪ knowledge`，持有判定共用 `has_card()`，容量只計 hand；view model 標出來源與是否可選，不洩漏答案集合。正解釋放本回合佔格，錯答保留；可用格歸零只結算一次失敗並清空暫態。
- `allow_discard` 為 true 時主動丟棄只接受可丟棄卡，false 時入口拒絕；逃離精確支付 `escape_cost`，null 時拒絕。任何拒絕都不改狀態。
- 一般 fallback 收到不可丟棄卡時不移除該卡，仍可依資料標記 attempted／推進；但 `requires_discardable == true` 時必須以 `card_not_submittable` 原子拒絕，不標 attempted、不推進。若已無合法提交、丟棄或逃離路徑，立即失敗而非死鎖。
- 勝利／失敗／逃離各自效果只套一次；結束後沒有殘留佔格或 attempted 集合。遭遇不消耗行動、不開縱慾與備用區。
- 人物／主角等不可丟棄卡不會被通用 `consume_card` 永久刪除；推理卡轉化等特殊結果只依明示效果發生。
- `is_overloaded()` 僅由手牌佔格數推導，不序列化；超載禁止玩家新進夜間地點，強制 D8 仍啟動但追加 penalty。
- active encounter 序列化往返相等；讀檔後 intro／round 階段、回合、佔格與已嘗試卡一致。出口按 `after_finish` 留在原時段或由規則層恰好推進一次。

## P4-E 遭遇 UI 與 D8／D45

範圍：共用卡片互動的遭遇畫面，以及兩場正式案例的演出與接線。

驗收：
- 畫面顯示要求、累積壓力佔格、可提交的手牌與知識，以及資料允許時的主動丟棄／逃離狀態；知識來源清楚且不佔容量，不把 `accepts` 或下一回合內容提前顯示。
- D8 第一輪不能睡過或跳過；共用終身首次到訪收費只發一次，第二輪仍重演遭遇但不再收費。固定姓名問答先演出，再進三個互動要求。
- D8 第一個互動要求第一輪無正解；若仍有可丟棄卡，必須失去或支付一張才可能進第二回合；零可丟棄卡且無其他合法出口時直接失敗。第二、第三個要求依第一章 matrix 接受早期路線卡，後續輪次知識卡確實改寫可選回應。
- D45 下午即使玩家直接按推進也由規則層攔截並先啟動遭遇；只收一次回應、不可逃也不可主動丟棄，選後由 `after_finish` 恰好推進一次到 evening；不同卡得到不同文本／效果，人物卡路線不永久失去人物。
- 鍵鼠可完成所有操作；disabled 原因、確認取消與 FlowText 停拍清楚，UI 不可繞過規則層入口。

## P4-F 全流程與跨輪驗收

範圍：把 P4-A～P4-E 接進 45 天第一輪、第二輪、既有 P1～P3 與 UI 模擬工具鏈。

驗收：
- 第一輪能在零人物卡、阿婕、阿珠、阿財至少四種狀態下走過 D17～19；委託／親自做互斥、每日人物使用與兩種回報時間都有整合證據。
- D8、D45 各走多條卡片回應，涵蓋正解、錯答、格滿失敗、可逃／不可逃、不可丟棄卡與特殊轉化；任何路徑都能繼續至結局。
- 第二輪 D8 仍發生且不重收首次地點費；run 層委託、遭遇暫態與 pending report 不跨輪，既有 meta 知識／夜間到訪照常保留。
- 全套 headless、`test_boot.gd`、序列化往返、45 天 greedy／具名策略與 UI 模擬無迴歸；資料 catalog 重數以實際輸出回報，不在文件預填會漂移的數字。
- P4-F 完成前由人操作一次委託預覽／立即與延遲回報、D8 壓迫感、D45 一次性選擇與鍵鼠焦點；機器全綠不能代替體感判斷。

---

# Phase 5 — 結局、開局與迴圈（驗收意圖）

> 目標：讓任何正式結局都能完整收尾、留下跨輪摘要，再從故事內開局進入下一輪；第一輪三種結局與解鎖後的「不上車」都可走通，並證明第二輪能重新開始。
> 不含：待決 25／26 的第二輪差異內容、玩家歷輪回顧畫面、正式標題／存讀 UI、輪內錨點回溯，以及最終文案、美術、音訊與演出。
> 各子階段的實作契約見 `開發設計方針.md` 對應段；操作步驟見 `測試指南.md` 對應段。

## P5-A 結局、開局與跨輪資料

範圍：先建立結局、開局、跨輪物品與 D29 逾期選擇的資料契約、翻譯欄位和 lint；補齊第一輪正式映射，不接流程或 UI。

驗收：
- `data/endings.json` 只定義四個穩定 ending id；正常結局的伴侶、生計、旅館外觀與不邀回顧都用有序規則及文字片段組合，不枚舉笛卡兒積。
- `data/opening_choices.json` 依序定義相簿、電話、不上車；相簿發 `item_family_album`，電話寫 `outside_job_waiting`，不上車引用 `ending_refuse_boarding` 並以歷輪 `ending_replaced` 鎖定。
- 正式卡表新增佔格裝備 `item_family_album` 與不佔格知識 `k_i_returned`；`loop_persistent` 是卡片明示能力，第一輪正式卡沒有任何一張為 true，僅 fixture 覆蓋。
- D7 依開局選擇提供拒信回聲；D11 接受家庭相簿或畢業照；D26 的招牌／湯屋／二樓修繕成為正式三選一；D43 前老闆與周先生共用 `leaving` 選擇組、各寫獨立結果，並以 `choice_requires_card:true` 強制提交主角卡後才成立。
- D29 邀請組有資料化逾期預設，明示不邀與未結算離開走同一條；正式慶典代付候選只有阿婕、阿薇、阿財，零投入 fallback 是阿婕。
- 結局文字可先是明確標示的結構版氣氛草稿，但每個入口、variant 與摘要都有可播放內容；不得在草稿補出新世界規則或改寫角色命運。
- lint 17～19 與壞資料 fixture 全部成立；lint 18 同時守住 card-required choice 的型別、非空 accepts 與不得兼任逾期 default。引擎後續不必靠 ending id、NPC id、beat id 或選項 id 特判資料內容。

## P5-B 頂層流程與結局狀態機

範圍：`opening`／`run`／`ending` 頂層狀態、結局快照、逐頁播放規則、序列化與原子入口；不接正式畫面。

驗收：
- 新 meta 狀態從 `flow_mode == "opening"` 開始；只有目前 mode 的 mutation API 可成功，其他入口以穩定機器碼拒絕且完整狀態逐字不變。
- `start_ending()` 一次凍結 ending id、來源、結束日／時段、正常結局 variants、當輪新增知識與頁面清單，進 `ending` 後立即阻擋推進時段、睡覺、地圖、放卡、委託、遭遇和其他 run mutation。
- `ending_view()` 只暴露玩家可見文字、頁次、逐字狀態與目前可用操作；不得洩漏內部 condition、ending id、未採用 variant 或後續頁內容。
- 逐字中按繼續只補完當頁；當頁完整後的下一次輸入才翻頁。同一次輸入不得同時補完與翻頁。
- 歷輪未看過該 ending id 時沒有整段跳過；看過後才提供獨立的跳至摘要／結束入口。直接偽造跳過呼叫也必須被規則層擋住。
- `active_ending` 序列化往返後保留快照、頁碼與當頁顯示進度；讀檔不重算 variants、不重播已完成頁，也不能重複啟動同一結局。
- `ending` 效果在同效果塊的其他鍵完成後才啟動；D45 evening coda 完成由通用輪末規則觸發 `ending_replaced`，不由 UI 或 beat id 特判。

## P5-C 四類結局與組合後日談

範圍：正常替換結局、兩種 BE、不上車的內容解析與完成門檻；仍不清除 run。

驗收：
- `ending_replaced` 依資料規則求出唯一的伴侶、生計、開關帶、旅館外觀與 `most_invested_npc`；D29 邀請或已凍結的慶典代付結果是人物來源，結局當下不得重算投入。
- 第一次完成 `ending_replaced` 必播完整後半生；是否為第一次只查 history 是否已有同 id。玩家先走任意多輪 BE，首次正常結局仍是長版。
- `ending_madness_be` 與 `ending_inventory_be` 各自只播本輪主角收尾，不誤接替換後日談；完成後仍走相同歷輪結算入口。
- `ending_refuse_boarding` 描述主角留在外地生活、四十出頭因癌症早死，再被送回開局；不初始化小鎮 run、不建立 D45 替換或慶典代付者。首次播完整短篇，歷輪已看過時可播摘要。
- 四類結局只有必播頁完成或合法 skip 後才進 `ready_to_complete`；正式 `complete_ending()` 與 history／輪數結算在 P5-D 一次落地，不在內容 resolver 偷清 run。
- 結構版文字由資料組合後語氣連貫，至少實際播放一次確認沒有缺頁、重頁、空白頁與互斥人生同時出現。

## P5-D 開局、歷輪摘要與跨輪重置

範圍：開局選擇、唯一輪結束結算、history、知識／魔法物品繼承、run 初始化與第二輪重入；不做存檔槽或歷輪回顧 UI。

驗收：
- opening view 依資料順序回傳三個選項及鎖定原因；預覽、取消與鎖定選項的確認都零變化。相簿／電話成功才原子初始化 run；不上車成功則直接進 ending。
- `complete_ending()` 依固定順序追加結構化 history、取得 `k_i_returned`、增加輪數、清空整包 run 與 active ending，再回 opening；任何正式結局都使用同一入口。
- history 每筆欄位完整且只存穩定摘要，不存整包 flags、手牌、全文或排版結果；`k_i_returned` 重複取得冪等，但不上車解鎖只看 `ending_replaced` history。
- 相簿 run 有主角卡與 `item_family_album`；電話 run 有主角卡與 `outside_job_waiting`；上一輪的普通手牌、備用區、旗標、開關、關係、發狂／縱慾、投入、委託、遭遇、選擇與慶典代付結果都不殘留。
- `loop_persistent` fixture 證明魔法物品取得即寫 meta、下一輪恢復、明示永久失去會同步移除；普通物品不跨輪，恢復後仍服從卡片既有佔格規則。
- D29 invitation choice 若逾期，規則層先執行唯一預設並固定慶典代付者，才允許離開時段；明示不邀與逾期的結果相同，D31／D39／結局皆只讀凍結值。
- 第二輪可由 opening 再進第 1 天，既有 meta 知識、夜間 seen、教學、歷輪摘要照常保留；P5 不因缺少第二輪新內容而偽造差異事件。

## P5-E 開局與結局 UI

範圍：沿用文字流程與既有確認元件，做故事內開局、結局逐頁畫面、鎖定與跳過操作；不做標題、存檔槽或歷輪回顧畫面。

驗收：
- 啟動直接顯示「出門前的十分鐘」及三個選項；不上車未解鎖時仍占原位置、disabled 且理由不劇透。選項可預覽、確認、取消，鍵鼠皆可完成。
- 進入 ending 後所有 run 畫面與快捷入口都不可操作；只顯示當前結局文字與合法控制，不以畫面遮罩代替規則層封鎖。
- 逐字頁第一次輸入立即完整顯示整頁，第二次才下一頁；長按、連點與鍵盤重複事件都不能一鍵跨兩頁。
- 首見 ending 沒有整段跳過；重見才顯示獨立跳過控制，且跳到資料指定的摘要或結束點，不漏掉必要結算。
- 結局完成後回到 opening，畫面不殘留上一輪地圖、手牌、遭遇、委託或 ending overlay；不上車流程來回亦同。
- UI 文案只顯示翻譯字串與故事名稱，不呈現 `ending_replaced`、`festival_proxy_eligible` 等內部鍵。

## P5-F 多結局與跨輪全流程驗收

範圍：把 P5-A～P5-E 接進第一輪、重複結局與第二輪，重跑 P1～P4 關鍵流程和 UI 模擬。

驗收：
- 第一輪至少各走通正常替換、發狂 BE、庫存 BE；三條都能寫入正確 history、取得「我回來過」、增加輪數並回 opening，只有正常替換解鎖不上車。
- 正常替換至少覆蓋有邀請與不邀、各生計優先序、三種旅館外觀及開關帶組合；同時成立多條生計時只採資料排序最高者。
- 先 BE、再正常結局的案例仍播正常長版；再次完成同 ending id 才出現摘要／跳過。不同行 ending id 各自計算首見。
- 解鎖後不上車可在不建立 run 的前提下完成一輪 history；首次與重複演出不同，完成後仍回同一 opening，且可再選相簿或電話開始下一輪。
- 連續至少三輪證明所有 P4 run 狀態都清乾淨、P1～P3 meta 狀態與歷輪摘要保留；D8 可重演、終身首次費不重收，委託與 active encounter 不跨輪。
- 魔法物品 fixture 的帶入與永久失去、D29 逾期預設、ending 讀檔續播、完成冪等、非法跨 mode 呼叫都有規則層與序列化證據。
- 全套 headless、`test_boot.gd`、45 天具名策略、資料 lint 與 UI 模擬無迴歸；P5-F 完成前由人操作一次三種開局狀態、長／短結局、逐字補完／翻頁、跳過與跨輪回場。

---

## 附：本檔對資料層的既知落差清單

寫作本檔時發現、需要後續處理的落差（不在此改，各自有家）：

| 落差 | 去哪處理 |
|---|---|
| ~~SCHEMA 未記載欄位~~ | 已補列（2026-08-13） |
| ~~關係單軸 vs 兩軸~~ | 已拍板單軸暫行（第十二節） |
| ~~第 1、2 夜 beat 重複兩份~~ | 已清理（保留 `ch1_nights` 版，2026-08-13） |
| ~~強制縱慾出口的「代價權重」資料欄位未存在~~ | 已定：`slots[].indulgence.weight`（`SCHEMA.md > 縱慾出口`，2026-08-14） |
| ~~縱慾強度級的副作用變體資料形狀~~ | 已定：`slots[].on_place_by_level`（同上） |
| 六個縱慾出口在 `data/beats/` 完全不存在（第八節） | **P2-B 建 `data/beats/indulgence_exits.json`**。落點與條件見 `開發設計方針.md > P2-B` |
| 奢侈出口的「手上有錢」沒有卡可掛（第八節） | 卡片清單（`待決事項.md` 22）。**在那之前奢侈出口恆為 LOCKED**，不影響保底（砸東西與食慾無條件） |
| 食慾出口的「日常卡往心境卡掉」沒有卡可掛（第八節） | 同上。P2-B 先用旗標與關係代價頂著 |
| ~~結局效果鍵 `ending` 未定~~ | 已定：封閉效果鍵，引用 `data/endings.json`（P5 規格） |
| ~~開局選單資料形狀未定~~ | 已定：`data/opening_choices.json`（P5 規格） |
| ~~委託與遭遇資料形狀~~ | 已定：`slots[].delegation`／`beats[].encounter`（`SCHEMA.md`，P4 規格） |
| 夜間地點 `reject_reason` 文案未填（六個阿宏地點，第九節） | 資料層之後填，引擎先用通用文案 |
| `locations.json` 的 `desc`（地點描述）48 個地點全未填（第四節、P1-G） | 資料層之後填，引擎缺欄位時只顯示 `name` |
| `accepts` 幾乎不用型別名，199 個槽裡 32 個寫具體卡 id（第五節） | 不改資料。型別由卡 id 反查 `cards.json.type` 取得，引擎負責換算 |
