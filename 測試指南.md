# 測試指南

> 測試流程與手動驗收步驟清單。
> **撰寫時機**：各子階段的測試項目於**該階段規格定稿時即寫入**（acceptance-test 先行），供實作時據以建測試與自我驗收——不延到驗證時才補。
> **角色歸屬**：verifier-owned。實作者照清單建測試、不得放寬清單、不改本檔（只列建議）。

## 本檔範圍與邊界

- **`實作規格書.md` 的驗收＝意圖層**：什麼必須為真，不寫操作步驟。
- **本檔＝操作層**：起什麼、按什麼、看什麼。同一條斷言不抄兩份，每個清單段落 link 回規格書對應段。
- 規格定稿時，測試項目可能引用尚未定的文本（stub 文字、卡名）；先用中性描述（「顯示其理由」「播出該段文字」），實作後不回頭改寫成實際字樣。
- 行號會漂移，一律以標題或 id 定位（`AGENTS.md > 文件查閱規則`）。

## Godot 測試環境

外部工具路徑見 `AGENTS.md > 專案外部工具路徑`。

### Headless 驗證命令

> 已知限制：sandbox 內直接跑會因無法開 `user://logs/godot*.log` 而 crash（signal 11）。用 escalated 權限執行；搬動 `.tscn` / `.gd` 後先 `--import` 重建快取（`AGENTS.md > Godot Headless 驗證`）。

```powershell
# 資料完整性（既有，隨 lint 擴充）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://scripts/verify_data.gd

# 正式啟動路徑（每個子階段收尾都要跑，見下方說明）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_boot.gd

# 各子階段的驗收測試
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_game_state_p1a.gd
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_hand_p1b.gd
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_p1c.gd

# 45 天貪心自動走查（P1-F 起）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/playthrough_greedy.gd
```

全部以 exit code 0 為綠。`test_game_state_p1a` 的輸出裡有一則 `Parse JSON failed`，那是壞資料 fixture 的預期輸出，不是失敗——**看 exit code，不看有沒有 ERROR 字樣**。

### `test_boot.gd` 為什麼獨立一條

其他測試都自己 `new()` 出 GameState 與 Data 再 `Engine.register_singleton()`，走的是**注入路徑**；正式遊戲走 `project.godot` 的 **autoload 路徑**。兩條組裝方式不同，而 2026-08-13 之前只有前者被驗過——結果是 P1-A 到 P1-C 三套全綠，**而遊戲根本開不起來**（`data.gd` 用 `Engine.get_singleton` 找 autoload，永遠拿到 null，`Data.ok` 恆為 false，主畫面停在錯誤字串）。

`test_boot.gd` 是 autoload 路徑的唯一守門員。它驗三件事：autoload 掛得上、`Data.ok` 為真、`main.tscn` 進得到正常分支（ErrorLabel 空、狀態列與推進鈕可見、狀態列的字是 `_refresh_status()` 寫的）。

⚠️ **這一檔裡不准手動註冊 singleton。** 註冊了就等於把它變成第二個注入路徑測試，守門員失效。

GUI 啟動：

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64.exe --path .
```

## 手動驗收清單

### Phase 1 — 最小可玩迴圈

> 意圖層：`實作規格書.md > Phase 1` 各小節。實作契約：`開發設計方針.md > Phase 1`。
> 全文字 UI；「推進」＝主畫面的時段推進操作。

##### 誰來驗：runner 與人的分工

本節共 **72 條**。**13 條標 `headless`，已全綠。** 其餘 **59 條**是手動驗收，每一條前面標一個記號：

> **數字會漂移，改動時要同步。** 60／13／47（2026-08-14）→ 62／13／49 → 63／13／50（2026-08-15 三輪審核修正）→ **72／13／59**（2026-08-15 新增 P1-H 手牌可讀性 9 條：8 條 🤖 ＋ 1 條 👤）。**同一組數字住四個地方**——本節、`PROJECT_BRIEF.md`（進度摘要與下一步）、`開發設計方針.md > UI 模擬驗證`（為什麼要有它、實作順序）。**加減 checkbox 就要四處一起改**，而且要重數不要用加減法：`grep -cE '^- \[[ x]\] (🤖|👤)'`（**方括號裡要吃 `x`**，否則打過勾的條目數不到）。

| 記號 | 誰驗 | 意思 |
|---|---|---|
| 🤖 | UI 模擬 runner | 事實判斷，可自動化也可重跑。工具契約住 `開發設計方針.md > UI 模擬驗證` |
| 👤 | 人 | 需要判斷力，或條目本身寫得模糊 |
| 🤖👤 | 兩者 | runner 驗事實，人另外看一次感受 |

**UI 模擬 runner 工具鏈已建（`tests/ui_sim/`）；「跳到第 N 天」由 `make_states.gd` 動態產生。** 59 條手動驗收的實際分類是：**UI runner 接 55 條**、**純人工 3 條**（P1-C 的重播感受、P1-G 的夜間節奏、P1-H 的整體可讀性）、**命令列 1 條**（跑 `verify_data` / `test_boot`，不點畫面）。

那 55 條裡，**P1-A～P1-H 的 53 條機器契約已實作完成**；再加上 P2-A 的 1 條、P2-B 的 4 條、P2-C 的 3 條、P2-D 的 2 條，**目前共 63 條契約、82 個案例變體**（含全流程無注入走查與 11 條負向反證）。**「全綠」現在是可重現的**——K-48 已於 2026-08-20 定位到真因並結案（實體滑鼠會在「送出移動」與「讀 hover」之間插隊），守衛住在 `qa_hover_regression.gd`，由 launcher 第 1 步強制跑。⚠️ **新增案例時 `run_ui_sim.ps1` 寫死的契約總數要一起改**，那個數字現在是 63，不改的話 launcher 會在跑任何案例之前就整支失敗。完整名單見 `驗證後已知問題.md > K-71`（K-58 的名單漏了 headless 套數與走查基準，已補）。

**打勾狀態（2026-08-15）**：標 🤖 的 **52 條已全數打勾**（51 條 runner ＋ 1 條命令列）。標 🤖👤 的 **4 條留空**——runner 那一半已綠，等人看過感受那一半再打勾；**P1-F 的 D45 coda 也在這 4 條裡**，它 runner 那一半（跨輪旗標歸零）2026-08-15 才補完，人那一半仍未做。標 👤 的 **3 條留空**（其中 1 條是 P1-H 的整體可讀性）。標 `headless` 的 13 條也留空，雖然 headless 實跑 exit 0（**2026-08-20 起是 16 套**：`verify_data.gd` ＋ `tests/headless/` 的 15 檔）。

**最近一次全套實跑（2026-08-20，commit `9e342de`，P2-E 驗收）**：**82 variants／63 catalog contracts／63 completed／0 failed checks**，另 11 條負向反證全部以預期原因失敗。同一輪 16 套 headless 全部 exit 0。

> **2026-08-16 那一次留著當教訓**：commit `8a1ccb8` 乾淨環境連跑三輪、每輪 66 variants／53 contracts，三輪結果**不一致**——第 1、3 輪 53 completed／0 failed，**第 2 輪 52 completed／4 failed checks**（`p1af_23_choice_leave`，兩次讀到的 `beats_entered` 差一個 `d22_pm_sandbags`）。真因是桌面上的實體滑鼠，2026-08-20 以 K-48 結案。⚠️ **不要把單一輪的全綠寫成「100% 通過」**；守衛住在 `qa_hover_regression.gd`。

> **runner 報綠 ≠ 條目驗到了。** 綠的意思只是「案例自己寫的斷言通過了」。2026-08-15 收掉的三條就是這樣被抓出來的（P1-D attention、P1-E 直選／帶卡等價、P1-F 跨輪旗標歸零）——**三條的 runner 全都是綠的**，但斷言比條目窄，其中 `reset_flag_slots_locked` 這個 evidence 名字最誇張，它驗的是 occupant 槽，連旗標都沒碰。**打勾看條目，不看 runner 的顏色**；evidence token 的名字要對得起案例實際做的事。

⚠️ **深天數條目由 `make_states.gd` 產生動態狀態模板並透過 `--state` 注入驗證**——第 32 天要從第 1 天走 130 次以上的互動過去，有了動態 checkpoint 生成器即可大幅降低驗收成本。

**疊字與遮蓋類的問題不是獨立條目。** K-28（結局文字被狀態列蓋掉）、K-40（文字溢出容器）**那些文字 `Label` 沒有互動語意**——沒有人要點它們，所以「讀不讀得到」推不出操作結果，去點也不會失敗。（**不要讀成「Label 不吃點擊」**：`location_panel.gd` 的槽 Label 就設成 `MOUSE_FILTER_STOP` 並接了右鍵預覽。）這一類由 runner 的**矩形診斷**負責，**每個案例都跑一次**（矩形相交＋容器溢出檢查），不用在清單上各佔一行。做法見 `開發設計方針.md > UI 模擬驗證 > 矩形診斷`。

**兩件 runner 永遠接不走的事**：

- **手感。** 演出逐一按起來累不累、推進提示看不看得懂、不劇透有沒有真的做到——清單上寫得出「顯示什麼」，寫不出「玩起來怎麼樣」。
- **完整走查至少要有人親自跑過一次。** runner 的完整走查證明流程不斷；人跑那一次證明它是一款遊戲。

**記號後面的括號**標的是這一條需要什麼情境狀態（`實作規格書.md` 沒有寫，這裡才寫）：`（新局）`＝從第 1 天開始；`（跳 Dn）`＝注入狀態到第 n 天；`（跳 Dn ×2）`＝同一天要兩份不同狀態各跑一次。
> **每個子階段收尾都重跑**：`test_boot.gd`（正式啟動路徑，見上方說明）；序列化往返相等測試（欄位隨子階段增加）；破壞性測試一律用 `tests/fixtures/broken/`，不改正式 `data/`。
> 接線隨 Phase 越來越多，而正式啟動路徑斷掉的時候，子階段測試不會紅——所以 boot 那一條不能只在 P1-A 跑一次。

#### P1-A 遊戲狀態與時段狀態機

- [x] 🤖（新局）啟動遊戲：狀態列顯示第 1 天、morning、第一章。
- [x] 🤖（新局）連續推進：時段依 morning → afternoon → evening → night → 次日 morning 循環，無跳格、無重複。
- [x] 🤖（跳 D15／D32）推進到第 16 天與第 33 天 morning：章節顯示分別切為二、三，且各只切一次。**注入狀態要落在切換的前一天**，否則跳過去就看不到「切了幾次」。
- [x] 🤖（跳 D45）推進到第 45 天：afternoon 之後進 evening（結局 coda），**不出現 night**，coda 播畢進入結局流程（P1-A 階段 coda 為 stub 顯示即可）。
- [ ] headless：以 `tests/fixtures/broken/` 的壞 JSON fixture 載入 → 回報載入錯誤、不進遊戲（不改正式 `data/`）。
- [ ] headless：GameState 序列化往返相等斷言綠（實作者建於走查腳本或獨立測試）。
- [ ] headless：`tuning.phases_per_day` 與結構常數不一致的 fixture → 載入報錯（一致性驗證）。
- [ ] headless：`test_boot.gd` 全綠——autoload 掛得上、`Data.ok` 為真、`main.tscn` 走正常分支。**這一條驗的是「遊戲開得起來」，跟上面那些走不同的組裝路徑。**

#### P1-B 卡片與手牌

- [x] 🤖（新局 ×2）主畫面常駐手牌列：顯示佔格數／上限，上限值改 `hand_size` 後重啟即反映。**這一條要啟動兩次**（改值前後各一）。⚠️ **不准改正式 `data/`**——複製一份到 `res://_qa/data_variant/`、只改副本，啟動時把資料根傳給 `Data.load_data(data_dir)`。做法見 `開發設計方針.md > UI 模擬驗證 > 資料變體`。
- [x] 🤖（跳 D9）知識清單與手牌列分開呈現；知識卡永不出現在手牌列。**要跳到手上已經有知識卡的狀態**，新局驗不到分流。
- [ ] headless：`gain_card` / `lose_card` 進出正確（含 knowledge 分流、主角卡不可 lose）斷言綠。

#### P1-C 地圖、面板與三態

- [ ] 🤖👤（新局）第 1 天 evening：到站事件自動逐行播出，播畢主角卡入手（手牌列 1 格）。**runner 驗文字與手牌；人另外看一次逐行播出的節奏。**
- [ ] 👤（新局）回頭重看同一事件：**文字重播一次，會不會讓人以為又發生了一次？** P1-G 已經定案「允許重進、文字重播、效果不重複」，所以**技術事實由 P1-G 第 3 條驗，本條不重複驗**。這裡留下的是感受——重播的文字有沒有讓玩家誤以為事件重來，那是斷言測不出來的。（原文寫「若 UI 允許」，那是 P1-G 之前的不確定，現在已經允許。）
- [x] 🤖（跳 D2）第 2 天 morning：地圖只列當前時段開放、當前章節已開的白天地點。
- [x] 🤖（跳 D2）**演出完成、進入常態階段之後**：已在場的人事物以 occupant 呈現、不可放卡；空槽可互動。（原文是「開任一面板」，P1-G 之後開面板先進演出階段，改寫為常態階段之後。**這一條沒有作廢**，只是判斷的時機往後移。）
- [x] 🤖（跳 D3）找一個 `requires` 不成立的槽：呈灰、點擊不放卡、顯示其理由文字。**這一條 runner 特別強**：真的去點，才知道灰掉的槽是不是真的擋得住點擊。
- [x] 🤖（跳 D3）該槽條件補齊後（同面板內取得所需卡）：不關面板即變為可放。
- [x] 🤖（跳 D3）`condition` 不成立的 beat 完全不出現（對照 `data/` 該日資料驗）。
- [x] 🤖（跳 D3）第 3 天 afternoon：該時段三個互搶的 beat（`d3_pm_sanquan` ／ `d3_pm_oldstreet` ／ `d3_pm_temple`，分別在山泉閣、老街、通興廟）各自**逐一演出後進入常態階段，內容照資料**。**這一條沒有作廢**，只是每個面板都先演出再看內容。三個搶同一個行動格，所以一輪只做得成一個——要驗三個就要三份狀態，或一份狀態逐一開面板但不放卡。

#### P1-D 放置與效果結算

- [x] 🤖（跳 D3）放主角卡進可選槽：效果文字播出、`on_place` 全部效果落帳（以手牌／知識清單變化驗）。
- [x] 🤖（跳 D3）同一行動格內，任何地點都不能再放第二次主角卡。**要真的走去第二個地點點下去**，不是只看按鈕有沒有灰。
- [x] 🤖（跳 D3）放置後同面板即時重求值（新開啟的槽出現、失效的槽轉灰或消失）。
- [x] 🤖（跳 D3）比對槽（不收主角卡）放情報卡：結算但**不消耗行動格**——放完仍可在同時段放主角卡。⚠️ **原本寫的第 9 天下午納骨塔驗不完整**：那一格的情報卡是比對本身給的，證不出「比對之後仍放得下主角卡」。改用第 3 天下午山泉閣的 `show_version` 比對槽——那份狀態自然持有夫妻情報卡，比對完還有 `help_ahong` 可以放主角卡，同一格就把「不耗行動」與「之後仍可放」兩件事一起驗掉（K-42）。
- [x] 🤖（跳 D9）對不符 `accepts` 的槽放卡：被拒收，狀態無任何變化。⚠️ **這一條不能用 runner 直接做**——`location_panel.gd` 只為 `placeable_cards()` 回傳的卡建按鈕，不相容的卡沒有可點的控制項，而正式 UI **不該為了測試長出那顆按鈕**。拆成三條：① runner 驗「未列出的卡沒有 `place::` 控制項」② runner 驗「預覽列出的每張卡放下去都成功」③ **規則層的拒收維持 headless 驗**（`test_p1d.gd` 已有）。詳見 `開發設計方針.md > UI 模擬驗證 > 負向驗證不擴充正式 UI`。
- [ ] headless：registry 存在但未持有的卡直接呼叫規則層放置 → 失敗、GameState 零變化，且與 UI 操作結果一致。
- [x] 🤖（跳 D22）對帶 `attention_npc` 的槽放主角卡成功 → 該 NPC 投入帳 +1；**對已結算的該槽再點一次 → 不變**。**投入帳不在畫面上**，靠點擊前後的 `serialize()` 差異驗。⚠️ **原文的「放置失敗或免費槽 → 不變」在 D22 的畫面上驗不到，採三分法**（同上面那條拒收的處理方式）：① 成功 +1 與重複點擊不計 → runner，已綠 ② **免費槽不計 → headless**——投入帳只在「主角卡」且「行動格時段」兩者同時成立時才 +1（`game_state.gd` 的 `try_place` 結算段），所以決定性的測試是「帶 `attention_npc` 的槽在非行動格放主角卡」，而真實資料裡帶 `attention_npc` 的槽只有 `d22_pm_sandbags.work` 一個、還是白天主角槽，D22 面板上不存在帶 attention 的免費槽可點；這一半 `test_p1d.gd` 已用合成 beat 驗過 ③ **拒收不計 → headless**（`test_p1d.gd` 補齊拒收斷言）。
- [ ] headless：全部十三個 condition 運算子（`not`／`all`／`any` 各算一個，含 `day_at_least`）各至少一條斷言綠；含未知語彙鍵的 fixture → 載入報錯。

#### P1-E choice_group

指定驗收例：第 22 天「你覺得哪裡不對」那一組（兩個入口都要通）。

- [x] 🤖（跳 D22）選定組內一個選項：同組其餘選項**消失**（不是灰掉）。
- [x] 🤖（跳 D22）重開面板：只剩被選的那個，**以唯讀展示（RESOLVED）呈現、不可再操作**；無反悔路徑。**「不可再操作」要真的去點它一次**才算驗過。
- [x] 🤖（跳 D22 ×2）用卡選與直選同一選項後（分別存檔驗）：除卡片消耗外，兩條路留下的狀態完全一致。**同一個狀態跑兩次、走不同入口，比對兩份 `serialize()`。**（兩個變體皆由 `d22_afternoon__with_polaroid.json` 起跑，分別以直選與帶卡放置選定 `obs_walk`，比對全狀態 serialize() 100% 逐字相同）。
- [ ] headless：對已結算的組重複呼叫選擇 → no-op，效果不重複套用。
- [x] 🤖（跳 D22）不選就離開面板：無任何結算；重開面板整組選項仍在。
- [x] 🤖（跳 D22 ×2）手上無相關卡：可直接點選選項完成結算。**要一份「手上沒有那張卡」的狀態**，貪心走查給的那條路上可能已經拿到了。
- [x] 🤖（跳 D22）手上有相關卡：可放卡選同一個選項，**結果與直選相同**（同一個 `on_place`；「加分」是多一條入口，不是不同結果）。

#### P1-F 45 天全程走通與迴圈 stub

- [ ] headless：`playthrough_greedy.gd` 從第 1 天跑到結局 stub，exit 0（第 1 天上午下午、第 32 天下午允許空格，名單同 `verify_data.gd` 的 `by_design`）。
- [x] 🤖（跳 D5 ×2）evening 殘響：刻意錯過第 3 天下午 `d3_pm_sanquan` → 第 5 天 evening 播出殘響行；到場則不播。（原本寫的第 5 天阿宏家那條是同文複本且沒有 `day` 欄，已於 2026-08-13 刪除，見待決 1。）**到場與錯過各要一份狀態**——這一條的意義全在對照。
- [x] 🤖（跳 D5／D8／D13）evening 殘響：第一章三次全部播得出來——第 5 天（`d3_pm_sanquan.echo`，`day: 5`）、第 8 天（`d8_echo_bathhouse`，`fixed` 掛 evening，**玩家當天沒去浴場也要播**）、第 13 天（`d12_pm_awei.echo`）。
- [x] 🤖（跳 D27 ×2）evening 結算順序：第 27 天 evening 先播 `d27_evening_awei_confirm`（寫 `dodger_awei_cleared`）再播 `d27_evening_cover_knowledge`（讀它）。收留過阿薇且當天下午去過診所或靜和園的存檔 → 當晚拿到 `k_town_covers`；兩者只成立一個 → 不拿到。**兩份狀態：兩者都成立、只成立一個。**
- [x] 🤖（跳 D10）night：夜間地圖免費地點可進；解析正確——第 1 夜走廊自動強制播出（不選地點）、定日 beat 優先於章節變體、附加 beat 並列（第 10 夜起「地標」同時看得到「有東西可以拿走」）；收費地點呈灰、附 stub 理由；**不產生任何發狂卡**。第 1 夜那一段要另外用新局驗。
- [x] 🤖（跳 D24）night：直接睡＝解析旅館當夜定日 beat——第 24 夜 fixed beat 入夜強制播；（Phase 2 起補驗）颱風夜條件成立時睡覺播 bleed beat。（⚠️ **2026-08-20 驗證：後半只驗到規則層。** bleed beat 的旗標會落帳，但 `main.gd._on_advance_pressed()` 把 `sleep_night()` 的文字丟掉，玩家一個字都看不到。住 `驗證後已知問題.md > K-69`，排 P3 修。）
- [x] 🤖（跳 D10）夜間放主角卡（第 10 夜「拿走／不拿」）：結算成功且**不消耗行動格**。
- [ ] 🤖👤（跳 D45 ＋完整走查）第 45 天 evening 結局 coda：`d45_then` 播出、其比對槽放入對應情報卡可完成知識升級**且不吃行動格**；coda 播畢回到第 1 天 morning——知識清單完整保留；手牌只剩主角卡；旗標／開關／關係歸零（以任一靠旗標開啟的槽回到未開狀態驗）。（`_coda_full` 結尾推進至第二輪第 8 天上午進入靜和園，斷言 `d8_morning_jinghe.go_with_ajie` 回到 LOCKED 未解鎖狀態且點擊無效，證實跨輪旗標歸零）。**runner 那一半已完成並驗過；這一條仍不打勾，因為 🤖👤 的另一半要人親自走過。**
- [ ] headless：`run_ended` 一輪**恰好發射一次**——結局之後連按幾次推進，signal 計數仍是 1，天數停在第 1 天 morning 並照常往下走。驗的是 `advance_phase()` 自己收尾（`開發設計方針.md > P1-F` 結局 coda）：若收尾被留給 listener，走查腳本會停在第 45 天 evening 重發 signal，變成無窮迴圈。
- [x] 🤖（完整走查續走）第二輪重看到站事件：主角卡仍恰好一張（gain 冪等，不重複發卡）。**接在上一條後面**——第二輪的到站事件要從第一輪走完之後接下去看。
- [x] 🤖（命令列）`verify_data.gd`（含 lint 1–3、5、7、8）與 `test_boot.gd` 全綠。**這一條不點畫面，是跑命令**，記號給的是「不用人」不是「用 runner」。

#### P1-G 面板互動模型

> P1-G 改掉了 P1-C／P1-D 的面板行為，**上面 P1-C、P1-D 兩節裡「開面板即看到全部內容」那幾條已經作廢**，以本節為準。驗收意圖見 `實作規格書.md > P1-G`。
>
> **本節 15 條手動，其中 14 條是 UI 模擬 runner 的第一批案例**（最後那條夜間節奏是純人工，runner 接不走）。 做它們的目的有兩個：驗 P1-G，以及證明 runner 這套工具本身可信。**這一批先做、其餘 33 條後做**，就是為了讓管線先證明自己（`開發設計方針.md > UI 模擬驗證 > 實作順序`）。兩批都已完成，47 條契約現在一起跑。
>
> 兩條要用**右鍵**（第 7、8 條），所以 `qa_step.gd` 的 `button_index` 必須從第一版就參數化，不能寫死左鍵。

- [ ] 🤖👤（跳 D32 ×2）演出逐一：進入第 32 天上午的寺廟，成立的 beat **一個一個演**，每次要按一下才推進；全部演完才進常態階段。該面板有 4 個 fixed beat，但後三個依 `invited_ajie` ／ `invited_awei` ／ 兩者皆無互斥，**沒邀人的存檔會演到 `d32_festival` ＋ `d32_festival_alone` 兩個**，邀了阿婕就換成 `d32_festival_ajie`。**驗成立與不成立各自的行為，不要數筆數。** ⚠️ 兩份狀態（沒邀人／邀阿婕）都要跑；貪心走查只會給你其中一條。**人另外看一次節奏**——按幾下才演完，玩起來是慢還是剛好。
- [x] 🤖（跳 D32）演出期間鎖互動：演出還沒走完時，畫面上沒有任何可放卡的槽（不是灰掉，是還沒建立）。**驗的是節點不存在**，UI dump 裡找不到任何 `place::` 開頭的 `qa_id` 才算過；找到但 `disabled` ＝不合格。
- [x] 🤖（跳 D17 上午）重進不重複結算：進山泉閣的 `d17_morning_phone`（`fixed`，`on_enter` 一次給 `npc_ajie`／`npc_azhu`／`npc_acai` 三張）→ 離開 → 再進去。**文字要重播，但手牌張數不變**。用進出前後的 `serialize()` 差異驗手牌，用 UI dump 驗文字重播。⚠️ **原本寫的「第 1 天傍晚公車站」走不到**——`main.gd._route_view()` 的 evening 分支把地圖與地點面板都關掉、直接自動播出，**玩家沒有離開再重進的路徑**。改用白天可重進的 fixed beat；一次三張卡，多結算一次會非常明顯。
- [x] 🤖（跳 D22）槽型別標示：第 22 天下午堆沙包的四個觀察槽，型別顯示成「裝備卡、情報卡」（用「、」不是「／」）。**分隔符要逐字比對**——`document` 的顯示名本身含斜線，用錯符號會讀成三個型別。
- [x] 🤖（跳 D3）槽型別標示：隨便找一個收主角卡的槽，顯示「主角卡」；名稱與 `data/card_types.json` 一致。
- [x] 🤖（跳 D9）不劇透：`accepts` 寫具體卡 id 的槽（例：第 9 天下午納骨塔的比對槽）**只顯示「情報卡」，不顯示卡名**。斷言寫成「Label 文字**不含**該卡的 `name`」，不是「文字等於情報卡」——後者擋不住卡名被附在後面。
- [x] 🤖（跳 D22）右鍵預覽：對一個可放的槽右鍵，列出手上放得進去的卡；關掉之後**天數、時段、手牌、知識、旗標全部不變**。「全部不變」用整份 `serialize()` 逐欄位比對，不要只挑幾個欄位看（K-21 就是只比六個欄位那條）。**右鍵的目標是槽 Label（`slot::<beat>::<slot>`），不是「預覽」按鈕**——兩個入口都走 `_show_preview()`，但這一條驗的是右鍵那隻手。關閉走 `dialog_confirm::preview`。
- [x] 🤖（跳 D22）右鍵預覽（灰槽）：對灰掉的槽右鍵，清單是空的並顯示 `reject_reason`。同上，目標是槽 Label。
- [x] 🤖（跳 D22）左鍵「預覽」按鈕與右鍵**結果相同**：同一個槽，兩個入口叫出來的清單與理由逐字相同。**這一條是新增的**——兩個入口共用 `_show_preview()`，但共用不等於驗過，而它們分岔的那天玩家會看到兩種答案。
- [x] 🤖（跳 D22）預覽與實放一致（**正向**）：預覽列出的每一張卡，實際放下去一定成功。
- [x] 🤖（跳 D22）預覽與實放一致（**反向**）：預覽沒列出來的卡，畫面上**找不到對應的 `place::` 控制項**。⚠️ 反向不能寫成「放下去一定被拒」——那顆按鈕不存在，runner 點不了。規則層的拒收由 headless 顧（同 P1-D 那條的三分法）。
- [ ] 🤖👤（跳 D35／D40／D43）推進提示：把當前時段能放的卡全部放完，且完成所有尚未結算的選擇題 → 推進按鈕變成提示狀態，**但時段沒有自己走掉**；此時仍可以走去別的地點看內容。第 35 天下午、第 40 天上午、第 43 天下午即使沒有可放的卡，仍應因 OPEN `choice_group` 保持可做，不得提前亮提示。**三天各一份狀態。** runner 驗按鈕文字與「時段沒有自己走掉」；**人另外判斷提示看不看得懂**——這一條解的是「不知道還有沒有事可做」，那是感受，斷言測不出來。
- [x] 🤖（跳 D10 夜）夜間同一套：**選一個夜間地點進去**（第 10 夜「拿走／不拿」那格），走的是同一條演出流程——演完才開放卡槽互動。⚠️ **原本寫的「第 1 夜進『很長的走廊』」是錯的**：`n_corridor_ch1` 是 `fixed: true`、**零個槽**的定日夜 beat，入夜強制播出、**不需要選地點**（規格書第九節解析第 1 步），沒有「進」這個動作。而且我原本加的「逐一、演完才開放互動」超出規格書 P1-G 的驗收——那條只要求**同一個規則層入口**，而 `play_night_fixed()` 內部確實是逐個呼叫 `play_beat()`，現況已經滿足。
- [ ] 👤（新局）夜間 fixed beat 的節奏：入夜強制播出的那幾段（第 1、2 夜走廊與出口）目前是**一次全部倒進 `FlowText`**，不是像白天那樣一段按一下。**這是節奏問題，不是規格違反**——強制播的 beat 沒有「進門」這個動作，而 P1-G 搬的是「進門的不可逆」。**要不要改成逐一按，由人玩過再決定**；覺得該改就開一條 K 條目，不要偷偷擴大規格。
- [x] 🤖（跳 D2）地點描述：`locations.json` 填了 `desc` 的地點，常態階段顯示得出來；沒填的只顯示地點名，不報錯。⚠️ **48 個 `desc` 目前全未填**，所以現在只驗得到「沒填時退回顯示地點名」那一半；填了之後要回來補驗另一半。
- [ ] headless：`verify_data.gd` 新增的 lint 9 全綠（`cards.json` 的 `type` 與 `card_types.json` 的 `id` 雙向封閉）。
- [ ] headless：`playthrough_greedy.gd` 在新模型下 exit 0，90 個時段分類統計與 P1-F 收尾時相同。
- [ ] headless：走查腳本走的是 `play_beat()` 那條路——白天 28 個帶 `on_enter` 的 fixed beat 在走查中確實結算（拿 `beats_entered` 筆數驗，別只看 exit code）。

#### P1-H 手牌可讀性

> 驗收意圖見 `實作規格書.md > P1-H`；實作契約見 `開發設計方針.md > P1-H`。
>
> **先修 K-50 再驗這一節。** UI 模擬目前會被上一輪殘留的 Godot 行程污染，紅了分不出是版面改壞還是工具在鬧。
>
> 一般案例使用 `d10_night__knowledge.json`：它同時有手牌卡與知識卡；`d9_afternoon.json` 的 `meta.knowledge` 實際是空的，不得拿來驗知識。長清單另用 `p1h_knowledge_full.json`，長卡名另用 `long_card_name` data variant，產生方式見 `開發設計方針.md > P1-H > UI 壓力 fixture`。

- [x] 🤖 `p1h_01_hand_cards`（跳 D10 知識狀態）手牌列每一張卡各自一個可點控制項；先用 `hand_card::<hand 裡的 id>` 精準找到 Button，再斷言 `button.text == cards.json.name`。**畫面可見文字找不到任何卡 id**。禁止用全畫面 `contains(name)` 代替逐鈕精確相等——卡名互為子字串會假綠。
- [x] 🤖 `p1h_02_card_detail`（跳 D10 知識狀態）分別點 `info_husband_version` 與 `info_wife_version`：詳情含各自完整 `name` 與 `text`；每次關掉後**整份 `serialize()` 逐欄不變**。這兩張的差別只在 `text`，兩張都要驗。
- [x] 🤖 `p1h_03_detail_readonly`（跳 D10 知識狀態）詳情彈窗開著時，整棵 UI tree **不存在任何 `place::` 或 `choose::` 控制項**，包含隱藏節點；找到但 disabled 仍不合格。關閉鈕必須是 `dialog_confirm::card_detail`。
- [x] 🤖 `p1af_06_hand_knowledge_split`（跳 D10 知識狀態；改寫既有契約）`hand_card::` 控制項裡沒有任何 `meta.knowledge` 的 id；`knowledge_entry` 存在且文字中的張數精確等於知識鍵數，不是只做 `contains`。
- [x] 🤖 `p1h_04_knowledge_detail`（`p1h_knowledge_full.json`）點知識入口：對照 `meta.knowledge`，全部卡的完整 `name` 與 `text` 一張不少；內容超過視口時可用真實滾輪捲到最後一張，最後一張確實可見。彈窗開啟中幾何診斷全綠，關閉後狀態逐欄不變。
- [x] 🤖 `p1af_05_hand_capacity`（新局 ×2；沿用既有契約）正式資料與隔離 `hand_size` 變體各跑一次，`hand_slots` 必須**精確等於** `手牌 <hand_slots_used> / <Data.tuning("hand_size")>`。本條只驗規則容量與文字，不宣稱 GridContainer 有空格容量；正式 `data/` 不得修改。
- [x] 🤖 `p1h_05_name_truncation`（跳 D10 知識狀態＋`long_card_name` data variant）`info_husband_version` 的 Button 必須實際顯示省略符號，開詳情仍看得到測試變體中的未截斷全名。這是必要契約，**不得因正式資料目前剛好塞得下而跳過**；不得靠改欄數製造截斷。
- [x] 🤖 `p1h_06_handbar_geometry`（跳 D10 知識狀態）`GridContainer.columns == 7`、卡片 Button 數等於 `run.hand` 筆數、沒有空框；在 1280×720 下，`HandBar` 仍是 P1-G 基線 `Rect2(20, 580, 1240, 140)`，`ContentView` 仍是 `Rect2(0, 170, 1280, 400)`。`hand_slots` 格式按 `p1af_05` 精確比對，且整體幾何診斷全綠。
- [ ] 👤（跳 D10 知識狀態）整體可讀性：人看一次兩列七格擠不擠、卡名截斷後還認不認得出來、知識入口會不會被忽略。機器部分已由上面 6 條新契約與 2 條既有契約覆蓋；本條不再重複算一條機器契約。

> P1-H 本批在既有 47 條上新增 6 條機器契約，完成後 matrix 總數必須是 **53**。`p1af_05`／`p1af_06` 是改寫既有契約，不重複計數；👤 條目不進 matrix。

> **發狂卡的多實例顯示隨 P2-A 一起驗**（P1 的資料拿不到發狂卡）。P1-H 只保證版面與 `qa_id` 相容：`hand_card::` 用 `hand` 陣列裡的原始 id（`madness#1`），所以同名三張不會撞。**P2-A 收尾時沒有補這一條的驗收——追蹤在 `驗證後已知問題.md > K-51`。**

### Phase 2 — 發狂時鐘與縱慾

> 意圖層：`實作規格書.md > Phase 2` 各小節。實作契約：`開發設計方針.md > Phase 2`。
> **P2 的數值全部住 `data/tuning.json`**（`madness_countdown_days` 7、`madness_cap` 7、`madness_vision_threshold` 3、`indulgence.*`）。下面寫「第 13 天歸零」這種話時，指的是**現行數值下**的結果；改了數值要重算，不是測試錯了。
> **每個子階段收尾都重跑**：`test_boot.gd`、序列化往返（新增 `madness_clock` 錶值、`indulgence_count`、`forced_pending`、`night_markers_opened`）、P1 十套 headless 無迴歸。

#### P2-A 發狂卡的產生與倒數

> 以下是 P2-A 完成時的歷史驗收。P3-B／C 會更新「同一地點」「fixed」「序列化集合」三條的正式期望；實作 P3 時應改寫既有測試，不得同時維持兩套規則。

- [x] 第 6 夜進一個收費標記：手牌立刻多一張發狂卡，卡面顯示 7 天。**當天不減**——推進到 night 結束、看卡面仍是 7。
- [x] 次日（第 7 天）morning：卡面變 6。之後每天 morning 減 1。
- [x] 第 13 天 morning：那張卡歸零。（P2-A 階段只驗歸零被偵測到，執行是 P2-C。）
- [x] 各走各的鐘：第 6 夜與第 7 夜各開一個標記 → 第 8 天 morning 兩張分別是 5 與 6，不同步。
- [x] 同一個收費地點第二次進入：**不再發卡**，手牌張數不變。
- [x] 免費地點（`madness_cost` 為 0）：進去不發卡。
- [x] 第 1 夜「很長的走廊」入夜強制播出：**不算開標記、不發卡**（規格書第九節解析第 1 步）。
- [x] 一夜一個：選了一個夜間地點之後，同一夜其他地點進不去。
- [x] 手牌列的佔格數把發狂卡算進去（開兩張之後，佔格數比開之前多 2）。
- [x] headless：序列化往返——存檔後讀回，每張卡的剩餘天數、實例編號、已開標記集合完全相同。

#### P2-B 縱慾出口與主動縱慾

> **打勾的證據基礎（2026-08-16）**：規則層由 `tests/headless/test_p2b.gd` 驗；**11 條操作項另外由 `tests/ui_sim/` 的 4 條 UI 契約（9 個變體）以真實輸入事件驗過**——`p2b_01_exit_visibility`（0 張／有卡兩個變體）、`p2b_02_exit_place`（放卡與拒收主角卡）、`p2b_03_soak`（上午成功、下午呈灰、格數不足三個變體）、`p2b_04_exit_thresholds`（第 16 天門檻、關係門檻、奢侈呈灰三個變體）。⚠️ **這 9 個變體只跑過單輪**——K-48（UI 模擬不可重現）仍開著，要連跑三輪才算穩。

- [x] 手上 0 張發狂卡時逛山泉閣：**看不到任何出口槽**（不是灰掉，是不存在）。
- [x] 手上有 1 張之後再逛山泉閣：砸東西、食慾、泡湯三個槽出現。
- [x] 槽型別標示（沿用 P1-G 規則）：出口槽顯示「發狂卡」，名稱取自 `data/card_types.json` 的 `madness`。
- [x] 放一張發狂卡進「砸東西」：效果文字播出、那張卡消失、**該行動格用掉**——同時段任何地點都不能再放主角卡。
- [x] 上午放進「泡湯」：**當天上午下午都用掉**、手上只少一張，不是全清。**時段不會自己跳**——玩家按推進進到下午，下午的行動格已經是用掉的狀態（灰的），再按一次才進 evening。（2026-08-16 改：原條目寫「直接進 evening」，那個實作會在放置管線中途發 `phase_changed`，兩個缺陷都由此而來，見 `驗證後已知問題.md > K-54` 的同批修正。）
- [x] 下午點「泡湯」：呈灰＋理由（只能上午發動）。
- [x] 上午只剩一格可用時（例如強制縱慾已吃掉上午）點泡湯：呈灰＋理由（格數不夠）。
- [x] 第 15 天逛老街的「奢侈」：呈灰＋理由。**這是已知的資料缺口**（錢卡還不存在，`實作規格書.md > 附` 有記），灰掉是正確行為，不是 bug。
- [x] 第 10 天逛山泉閣：**看不到「暴力對人」**（擋在第 16 天之後）。第 16 天起看得到。
- [x] 與阿婕關係未達「疑似」時：看不到性慾那個槽；達到之後看得到。
- [x] 對出口槽放非發狂卡（例如主角卡、情報卡）：被拒收，狀態無任何變化。
- [x] **同一個出口槽可以重複用**：砸過東西之後，換一個行動時段還能再砸，跨日也一樣。出口 beat 是常駐的（`day_from` 1 → `day_to` 45），**它不可以被當成一次性槽**——擋住玩家的是行動格，不是「這個槽用過了」（K-54）。
- [x] headless：`indulge()` 拒絕路徑逐條驗（卡不在手上、非縱慾槽、同一時段行動格已用、泡湯在下午、格數不足）→ 每條都是 GameState 零變化。
- [x] headless：`verify_data.gd` 新增的 lint 10 全綠；lint 4（縱慾完整性）在真資料上通過。
- [x] headless：`when.day_from` / `day_to` 與 `phase` 陣列解析正確——出口 beat 在第 1、23、45 天的上午都成立；`x_soak` 在下午不成立。
- [x] headless：有 `day` 又有 `day_from` 的 fixture → 載入報錯（用 `tests/fixtures/broken/`，不改正式 `data/`）。

#### P2-C 強制縱慾

> **打勾的證據基礎（2026-08-20）**：11 條全部由 `tests/headless/test_p2c.gd`（10 組）在**規則層**驗過，`6e5e51a` 上 14 套 headless 全部 exit 0。「泡湯永不入池」那一條另做了突變檢查——把 `pick_exit()` 的 `auto == false` 過濾拿掉，測試 6 會紅（該測試注入一個 `weight: 999` 的非自動槽，過濾器不在就會被挑中）。
> **操作層已補（2026-08-20，K-61 結案）**：`tests/ui_sim/` 新增 3 條 P2-C 契約，全部以真實輸入事件驗——`p2c_01_forced_text`（效果文字播在地圖上、且關掉地點面板回來仍留存）、`p2c_02_forced_action_spent`（被吃掉的行動格在**兩個**地點都沒有主角卡放入入口）、`p2c_03_forced_two_same_day`（同日兩張歸零，上午下午各吃一格）。契約總數 58 → 61、變體 76 → 79。

- [x] 第 6 夜開一張、之後不主動清：第 13 天 morning 一進到上午，**遊戲自己執行縱慾**——文字播出、那張卡消失、上午那一格被吃掉，玩家該時段不能再放主角卡。
- [x] 第一次是輕的，而且文字明講下次更重。
- [x] 同日兩張歸零：**各吃一格**——上午一次、下午一次，那一天玩家完全沒有行動格。
- [x] 同日三張歸零：吃掉當天兩格，**第三張順延到次日上午**。次日上午再被吃一格。
- [x] 出口挑選：第一章（第 16 天前）只有砸東西與食慾在池裡，所以挑到的一定是**食慾**（權重 2 > 1）。
- [x] 出口挑選：第 16 天之後、關係未達「疑似」時，挑到的是**暴力對人**（權重 4）。
- [x] 出口挑選：與阿婕達「疑似」之後，挑到的是**性慾**（權重 5）。這一格就是企劃書說的「你越放著不管，主角越往你最不想他去的地方走」。
- [x] **泡湯永遠不會被自動挑中**——把其他出口全部擋掉的存檔也一樣（此時應挑到砸東西或食慾，不是泡湯）。
- [x] 強制縱慾與玩家所在環境無關：**照樣執行**。挑選不看地點，也不看 `when` 時段。（**2026-08-20 改字面**：原文寫「玩家人在別的地點」，但引擎白天根本沒有「玩家所在地點」這個狀態欄位——只有夜間的 `night_location_chosen`。照原文找欄位的人會找不到。現行斷言驗的是「在下午觸發也完整跑完扣格、消卡、累計兩個計數」。）
- [x] 主動與強制共用同一個計數：主動清掉 3 張之後，第 4 次（強制）的強度級要照 `forced_light_count` / `forced_normal_until` 算，不是從 1 重數。
- [x] headless：決定論——同一個存檔重跑兩次，挑到的出口、天數、順序逐項相同。（⚠️ **現行斷言只到「同一狀態連續呼叫 `pick_exit()` 結果相同」**；完整存檔重跑的逐項比對留 P2-E 的整輪重演。）

#### P2-D 視野門檻與發瘋 BE

- [x] 桌上 2 張時：掛 `madness_at_least: 3` 的內容**看不到**（不是灰掉）。
- [x] 桌上 3 張時：同一格內容出現。清掉一張之後又消失。

> ⚠️ **這兩條的勾要打折看（2026-08-20 驗證）**：證據仍然只到規則層與 view model（`test_p2d.gd` 四段：0 張、2 張、3 張、扣回 2 張）。`tests/ui_sim/` 的 `p2d_01_vision_visibility` 兩個變體判定依據是 `serialize()` 的 `flags.awei_heard_it`，**不是畫面上有沒有那個節點**——所以「不是灰掉」這句話目前沒有畫面層證據。做不到的原因是 K-69（睡覺解析的文字被丟掉，畫面上兩種情況都是空的）。追蹤住 `驗證後已知問題.md > K-68`。
- [x] headless：用 fixture 把 `madness_cap` 調到 2，開兩張 → **立刻進發瘋 BE**，本輪結束。
- [x] headless：BE **不播後日談骨架**。
- [x] headless：BE 之後回到第 1 天 morning，知識集合完整保留、手牌只剩主角卡。
- [x] headless：`run_ended` **恰好發射一次**——一次發多張卡同時衝破 cap 也只結束一輪（拿 `madness_cost` 2 以上的 fixture 驗）。
- [x] headless：正式資料上（cap 7）**第一輪碰不到 BE**——三種玩家重演都不觸發（與 P2-E 同一份測試可共用斷言）。

#### P2-E headless 重演三種玩家

> 這一條全部是 headless。基準表住 `實作規格書.md > P2-E`，來源是 `subdocs/驗證/發狂卡機制模擬.md > 三`。

- [x] headless：`test_p2_sim.gd` 三種玩家全部 exit 0，八個指標逐項對上基準表（桌面峰值、觸及 cap、強制次數、最重級次數、主動次數、吃掉的行動格、≥3 張的天數、開到的標記數）。
- [x] headless：視野窗口對上——A 第 8–23 天＋第 42–44 天；B 第 8–26 天＋第 42–44 天；C 無。
- [x] headless：A 深潛的逐項時間軸與模擬文件第三節那份逐行相同（峰值 4 落在第 11 天、第一次最重落在第 27 天）。26 個事件（14 次開標記 ＋ 12 次強制縱慾）逐項並排比對過。
- [x] headless：測試走的是真的規則層入口（`open_night_marker()` / `indulge()` / `advance_phase()`），不是另寫一份模型。**這一條用 code review 驗，不是跑出來的。**（2026-08-20 review：走 `open_night_marker()`／`try_place()`→`indulge()`／`advance_phase()`／`play_evening()`／`play_night_fixed()`／`sleep_night()`／`play_beat()`／`build_panel()`，沒有另寫模型。⚠️ 兩處自行重算住 `驗證後已知問題.md > K-72`，低。）
- [x] headless：`playthrough_greedy.gd` 仍 exit 0。90 個時段的分類統計**會因為強制縱慾吃格而改變**——把新數字記進 commit message，當作下一次的基準。（**新基準（2026-08-20）**：成功放置主角卡 47／強制縱慾消耗 12／純比對槽 22／刻意留空 3／純選擇題 3／HIDDEN 2／LOCKED 1 ＝ 90。⚠️ `9e342de` 的 commit message 漏記，已改記進 `PROJECT_BRIEF.md > 資料層現況`；理由住 `驗證後已知問題.md > K-71`。）
- [x] headless：`verify_data.gd`（含 lint 1–5、7–10）與 `test_boot.gd` 全綠。

### Phase 3 — 夜間層真值化

> 意圖層：`實作規格書.md > P3-A`～`P3-F`。實作契約：`開發設計方針.md > Phase 3`。
> **順序固定**：先跑當前子階段的 `test_p3?.gd`，再跑 `test_boot.gd` 與既有全套；P3-F 才跑雙輪完整重演。新增 UI 契約時，總數依 catalog 實際重數並同步五處，不在規格階段預寫數字。
> 下列 `[ ]` 是尚未實作／驗收，不是文件未定稿。實作者不得修改本節打勾；完成後由 verifier 依證據更新。

#### P3-A 夜間資料真值化

> **驗收狀態（2026-08-21）**：11 條裡 10 條通過，只剩 baseline 產生順序那條未過（K-107，已降為不修的紀律條目 B-04）。verifier 自跑證據：17 套 headless 全部 exit 0、`verify_data` 的 lint 11／12 各 0 錯誤、`test_p3a` **17 項**全綠（含 11 個獨立負向 fixture）、UI 全套 82 變體／63 契約／0 failed／11 個負向如預期失敗。**負向 fixture 另做過變異測試**——把對應的 lint 檢查逐段拿掉，該紅的都紅（詳見 `驗證後已知問題.md > 附：本檔的驗證來源`）。

- [x] headless：跑 `verify_data.gd` 與 `test_p3a.gd`；從真資料算出 10 個 distinct `day_counterpart`、12 個可對位 night row、16 個夜間限定 row，並逐筆驗 `night_reveal` 共用關係與 knowledge 卡型別。測試不可內建 12 筆映射表。（2026-08-21：三個數字都從 `loader.locations` 現算，`lint_night_alignment()` 也不含硬編數量；測試內無 12 筆對照表。）
- [x] headless：對每張對位卡讀 `name`／`text`；名稱含對應白天地點名，文字不含同一 counterpart 下尚未到訪的其他夜間 row 名稱。（2026-08-21：`41d2d8e` 起改為動態掃描全部 28 個夜間名稱；此前是寫死 4 個名字＋死變數，落檔 K-103。）
- [x] code review：逐一把 28 個 night `name` 與其 primary beat 對照；至少確認 `n_ahong_2` 的清單名稱不再先講「血還是新的」，進場 beat 仍保留發現。（2026-08-21：28 行對照表落在 `驗證後已知問題.md > 附：本檔的驗證來源` 的 K-103～K-109 那一則；verifier 抽驗 5 筆——`n_plaza`→`n15_everyone`、`n_source_ch1`、`n_landmark_ch1`、`n_gathering_ch2`、`n_behind_temple_ch3`——名稱與 beat id 均與真資料相符。）
- [x] headless：6 個帶 location `requires` 的阿宏 row 全都有非空、彼此語意對應的 `reject_reason`；通用 fallback 不算通過。（2026-08-21：`41d2d8e` 把 `n_ahong_5`／`_6` 分成「第一段／第二段」，測試改語意斷言並加 `r5 == r6` 反證，不再逐字拷貝文案。落檔 K-109。）
- [x] headless：`n_corridor_end` 第三章被分類為 teaser-only，帶理由且價碼／對位欄為 `null` 或省略。（2026-08-21：資料與 lint 12 都對得上。）**「第一、二章不列入 teaser 可見集合」那半句本階段驗不到**——可見性住 `available_locations()`，而 P3-A 不得改夜間進入流程。該半句由 P3-D 既有那條「teaser 第一、二章不存在，第三章出現」承接，本階段不重複列；落檔 K-111。
- [x] headless 負向 fixture（**每一類一個獨立 fixture，不得合併**，否則第一個報錯會遮蔽後面的）：同一 counterpart 指到兩張卡 → lint 11 紅；**兩個不同 counterpart 指到同一張卡 → lint 11 紅**；對位卡非 knowledge／非 slotless → lint 11 紅；teaser 缺理由或帶價碼 → lint 12 紅；chapter 與 earliest 不一致 → lint 12 紅。（2026-08-21：五個 fixture 各自獨立，且每個都斷言**特定錯誤字串**而非只驗非空。）
- [x] headless 負向 fixture：一般 night row 缺 `earliest_night`／`chapter`／`madness_cost` 任一欄，或型別錯誤，或 `madness_cost` 為負 → lint 12 紅。**其中一筆要造成「`madness_cost` 拼成 `madness_costs`」**——這是會安靜把收費地點變成免費地點、直接污染 P3-F 13／14 供給基準的那條路徑。（2026-08-21：`ce7c41a` 只做了拼錯那一筆，落檔 K-110；後續補上 `p3a_missing_earliest_night`／`p3a_missing_chapter`／`p3a_invalid_type_field`／`p3a_negative_madness_cost` 四個獨立 fixture，各自斷言自己的錯誤字串。**型別那一個又修過一次**：原本寫 `"chapter": "one"`，但 `int("one") == 0` 會被範圍檢查代勞，型別檢查其實沒被證明過；改成 `"chapter": "2"` 與 `earliest_night: 15.5` 兩個 row，數值都落在合法範圍，只有型別／整數性檢查抓得到——落檔 K-113。四項全部以變異測試確認過：把對應檢查拿掉就紅。）
- [x] headless 負向 fixture：teaser row 只寫 `chapter` 或只寫 `earliest_night` → lint 12 紅。（2026-08-21：`41d2d8e` 先補 lint 沒補 fixture，落檔 K-108；後續 `p3a_teaser_missing_time_column` 用兩個 row 各缺一欄，`test_p3a.gd` **分兩條斷言逐 row 比對**。同批把時間欄位檢查從 teaser／一般兩個分支上提為共用，兩種 row 走同一套判斷，錯誤訊息也統一。）
- [x] code review：`subdocs/卡牌/知識卡.md` 已含 10 張新對位卡且與 `cards.json` 的 id／name／text 一致；`CODEX_HANDOFF.md` 已更新為當前階段。（2026-08-21：索引重建為 21 張，10 張新卡 id／name／text 逐筆對得上；`CODEX_HANDOFF.md` 已寫到 P3-A 完成、下一步 P3-B。）
- [x] headless：P3-A 變更前後用同一份 P2 night checkpoint 跑一次，`available_locations()`、首次發卡與 fixed 流程結果相同，證明本階段沒有提前改玩家流程。**這份 checkpoint 的產生方式是規定好的，不是實作者臨場決定**（契約住 `開發設計方針.md > P3-A`）：由 `make_states.gd` 的 `p3a_night_baseline` 情境產生，前置狀態為第 14 天 night、當夜尚未動作（四條後置條件當場斷言），檔名固定 `_qa/p3a_baseline/p3a_night_baseline.json` 與 `p3a_night_baseline.expected.json`，不進版控。（2026-08-21：契約逐項對上；**verifier 另以 worktree 開在 `870db64`（P3-A 前的資料）＋現行 generator 重跑，產出與現檔逐字相同**，`ce7c41a` 與 `41d2d8e` 兩次各驗一遍，所以「P3-A 沒改玩家流程」這個結論獨立成立。）
- [ ] code review：baseline 的「前」那一半**是在動 P3-A 資料之前產生的**，`CODEX_HANDOFF.md` 記了當時的 commit sha；「後」那一半是**載入同一份狀態 JSON**比對，不是重跑 generator。事後補產的 baseline 一律不算——舊資料已經不在，拿到的「前」其實是「後」，這條驗收會恆真。**未過，且已判定不用這條擋（K-107 → B-04）**：兩次的 baseline 都是資料改完之後才寫出來的（`41d2d8e` 這次是 `locations.json` 21:48:11 → baseline 21:51:10）。更根本的是**跑一次 UI 模擬就會把 baseline 重生一遍**——`run_ui_sim.ps1` 第 2 步呼叫 `make_states.gd`，而 baseline 產生掛在 `generate_all_states()` 裡（K-112）。所以「前」那一半在這個專案結構下留不住。**上一條的勾靠的是 verifier 用 `870db64` 的資料另開 worktree 重跑、拿到逐字相同的檔，不是靠本條。**

#### P3-B 夜間狀態與進入規則

> **驗收狀態（2026-08-22）**：15 條裡 **14 條全過、1 條只過前半**（第 15 條的「玩家文案住檔頭 const 表」在 P3-B 沒有消費端，改由 P3-D 承接，落檔 K-115）。verifier 自跑證據：**18 套 headless 全部 exit 0**、`test_p3b` **12 組全綠**、`verify_data` 的 lint 13 掃正式資料 0 筆、相關三個 UI 契約（`p1af_29_night_paid` 3 變體／`p2d_02_be_screen`／`p1af_33_full_walk`）failed 0。**第一輪 review 抓到 8 條「修正做了、測試沒跟上」，`7c5184b` 全數補齊，並逐條做過變異測試**——把實作拿掉七種變異全部如預期轉紅（詳見 `驗證後已知問題.md > 附：本檔的驗證來源`）。

- [x] headless：初態四個 P3 狀態欄位正確；序列化 JSON 往返後逐欄相同。`night_locations_seen`／`night_once_beats_seen` 在 meta，另外兩筆在 run。（2026-08-22：`test_p3b` 第 4 組。四欄位初態、`serialize()`／`deserialize()` 往返後 `night_locations_seen`／`night_once_beats_seen` 在 meta、`night_location_chosen`／`night_sleep_pending` 在 run，逐欄相同。）
- [x] headless：首次進 paid row 前後比對——`night_location_chosen` 寫入、seen 寫入、madness 實例增加 `madness_cost`；回傳提示存在。（2026-08-22：`test_p3b` 第 3 組。D6 夜首進 `n_ahong_1`：chosen 與 meta seen 寫入、手上多一張 `madness#1`、回傳提示行含「發狂卡」。）
- [x] headless：同一輪下一夜重進同 row、`end_run()` 後第二輪重進同 row，兩次都不新增 madness；seen 始終保留。（2026-08-22：同輪隔夜重進在第 3 組；**跨輪重進在第 4 組**，`7c5184b` 補上。兩次都 `ok=true`、`lines` 為空、手牌張數不變，seen 始終保留。**已變異測試**：把收費集合改回 run 層（`end_run()` 清、meta seen 保留），只有跨輪那條轉紅，證明它不是 meta 保留那條的重複。）
- [x] headless：用 fixture 讓首次 marker cost 正好撞 cap；捕捉 `run_ended` 恰好一次，確認新一輪已開始但 meta seen 仍含該 row；透過 `main.gd` 真入口驗 BE 畫面後沒有再開舊夜地點面板或播放舊夜內容。（2026-08-22：`test_p3b` 第 5 組驗規則層——撞 cap 觸發 BE、回到第 1 天 morning、meta seen 仍含 `n_source`、且不回傳 marker 提示行；**`main.gd` 真入口那半在第 6 組**，`7c5184b` 補上，驗 `FlowText` 為 `[發瘋 BE]` 且地點面板未被開起來。**已變異測試**：拿掉 `_is_showing_ending` guard 立刻轉紅（`panel_vis=true`）。）
- [x] headless：直接呼叫私有 helper `_record_forced_night_visit()`，驗 seen＋chosen 寫入但 madness 不增；helper 保持 private，不新增 UI 可用的 `forced` 參數。row 先前已 seen 時仍允許尚未播放的 fixed 內容在 P3-C 接線。（2026-08-22：`test_p3b` 第 8 組。seen＋chosen 寫入、手牌不變；`enter_night_location()` 簽名無 `forced` 參數，helper 仍為 `_` 前綴私有。row 先前已 seen 時的 fixed 播放留給 P3-C 接線。）
- [x] headless：同夜成功進第一個地點後，對第二個呼叫 `enter_night_location()` → `ok=false` 且前後完整 serialize 相同；只呼叫 `location_summary()`／詳情 view model 也零變化。（2026-08-22：`test_p3b` 第 1 組 `already_chosen` 案例，`ok=false` 且前後 `serialize()` 逐字相同。）
- [x] headless：**尚未選地點但 `night_sleep_pending == true` 時**（睡覺已播內容、停在「進入隔天」），對任一合法地點呼叫 `enter_night_location()` → `ok=false`、`reason_code == "already_slept"`，前後完整 serialize 相同。**這條擋的是「先睡看完外溢文字、再探索一個地點」的一夜兩份內容。**（2026-08-22：`test_p3b` 第 1 組 `already_slept` 案例，`reason_code == "already_slept"`、前後 `serialize()` 逐字相同。）
- [x] headless：`night_seen` 在本輪 seen 與第二輪都成立；未 seen 為 false；未知 location id 的 fixture 被引用檢查擋下。（2026-08-22：求值真值表在第 9 組、跨輪成立由第 4 組的 meta 保留承接；**未知地點 id 與「引用的不是夜間地點」兩個負向 fixture 在第 11 組**，`7c5184b` 補上。**已變異測試**：刪掉 `data_loader.gd` 的 `night_seen` 引用檢查分支，兩條各自轉紅。）
- [x] headless：阿宏前三級路線由前一 row 的 meta seen 開門，不靠 run flag；`n_ahong_5`～`_7` 的三張 `k_ahong_point_*` 既有門檻與 `info_ahong_private` 仍在。（2026-08-22：`test_p3b` 第 12 組，`7c5184b` 補上的 12 項結構斷言。`n_ahong_2`／`_3`／`_4` 的 `requires` 逐筆等於 `{"night_seen": 前一 row}`、不是 run flag；`n_ahong_5`～`_7` 的 `k_ahong_point_1`～`_3` 門檻仍在且三張都是 slotless 知識卡；`info_ahong_private` 仍在。**已變異測試**：把 `n_ahong_2` 的 gate 改回 run flag 立刻轉紅。）
- [x] headless：正式資料 9 個純寫入 `opened_n_*` 已刪；聚會常態內容改寫 run flag `saw_n_gathering_intro`，沒有把它誤換成跨輪 `night_seen`。**`n_gathering_ch2` 與 `n_gathering_ch3` 兩個章節變體的 `on_enter` 都要寫**——第三章首次進聚會也要設得起來，否則旗標名與語意對不上。（2026-08-22：`data/` 全域 grep `opened_n_` 為 0 筆（只剩 `SCHEMA.md` 的說明文字，不在掃描範圍），lint 13 掃正式資料 0 筆。`saw_n_gathering_intro` 由 verifier 逐筆核對——寫入端 `ch2_nights.json:39`（`n_gathering_ch2`）與 `ch3_nights.json:12`（`n_gathering_ch3`）**兩個變體都寫**，讀取端 `ch2_nights.json:47`（`n_gathering_d31.condition`）讀的是 run flag，沒有被誤換成 `night_seen`。）
- [x] headless：lint 13 掃正式資料為 0；注入 condition／requires／on_enter **或槽級 `slots[].condition`／`requires`／`on_place`** 任一處 `opened_n_*` 都會紅。槽級那一筆要獨立驗——只掃 beat 級三個鍵的話，旗標可以從槽級長回來而 lint 全綠。（2026-08-22：`test_p3b` 第 10 組，`7c5184b` 補上**五處各自獨立的 fixture**——`location.requires`、`beat.on_enter`、`slots[].requires`、`slots[].on_place`、`slots[].condition`。**已變異測試**：讓 `_scan_legacy_flags()` 不再遞迴進 Array（等同只掃 beat 級），三個槽級 fixture **各自分開轉紅**，location 與 beat 那兩筆仍綠——K-87 要的「槽級獨立驗」確實兌現。）
- [x] headless 拒絕矩陣：`enter_night_location()` 的**八個 `reason_code` 逐一各一個案例**，每個都驗「`ok == false`」＋「`reason_code` 等於預期字串」＋「前後完整 `serialize()` 逐字相同」三件事。只驗布林值的不算通過。六條新的：`not_night`（morning／afternoon／evening 各呼叫一次）、`unknown_location`（不存在的 id）、`not_night_layer`（白天地點 id）、`teaser`（第三章的 `n_corridor_end`）、`too_early`（`earliest_night` 未到的 row）、`locked`（阿宏 row 前級未走，且 `reason_text` 逐字等於 `locations.json` 的 `reject_reason`、不是通用字串）。`already_chosen`／`already_slept` 由上面兩條既有條目覆蓋。（2026-08-22：`test_p3b` 第 1 組，八碼各一案例共 16 條斷言，每碼都驗 `ok == false` ＋ `reason_code` 等於預期字串 ＋ 前後完整 `serialize()` 逐字相同。`locked` 那條的 `reason_text` 逐字等於 `locations.json` 的 `reject_reason`，不是通用字串。）
- [x] headless：`too_early` 與 `locked` 的**先後順序**有專門案例——取一個「`earliest_night` 未到、同時 gate 也不成立」的 row，驗回的是 `too_early` 而非 `locked`。檢查順序寫反時，八個 code 各自的單獨案例仍可能全綠，這條才擋得住。（2026-08-22：`test_p3b` 第 2 組。取 `n_ahong_2`（`earliest_night: 11`，且 `requires` 的 `night_seen: n_ahong_1` 未成立）於第 6 夜呼叫，回的是 `too_early`。）
- [x] code review：全專案規則入口已由 `open_night_marker()` 換為 `enter_night_location()`，沒有 wrapper、雙寫或仍以 `night_markers_opened` 斷言的測試。（2026-08-22：`scenes/`／`scripts/`／`tests/`／`data/` 全域 grep `open_night_marker` 與 `night_markers_opened` 各 **0 筆**。`7c5184b` 連 `test_p2a.gd:3`／`test_p2_sim.gd:5` 的檔頭註解與 `test_p2_sim.gd` 的 timeline 標籤字串一起清成 `enter_night_location`，沒有 wrapper、雙寫或仍以 `night_markers_opened` 斷言的測試。）
- [x] code review：`enter_night_location()` 回的是 `{ok, reason_code, reason_text, lines}`，機器碼與玩家文案分兩欄（K-03 契約）；UI 沒有把 `reason_code` 直接印在畫面上，玩家文案住檔頭 const 表。（2026-08-22：**只過前半**。雙欄形狀成立——`enter_night_location()` 回 `{ok, reason_code, reason_text, lines}`，`reason_text` 只有 `locked` 帶資料的 `reject_reason`；`main.gd` 沒有把 `reason_code` 印在畫面上（拒絕時直接 return，不開面板）。**後半「玩家文案住檔頭 const 表」在本階段沒有消費端**：P3-B 的 `main.gd` 仍是「點清單即進場」，畫面上沒有地方顯示拒絕理由，原本寫的 `NIGHT_REJECT_MESSAGES` 是宣告了沒人讀的死常數，已於 `7c5184b` 刪除。code→玩家文案的翻譯由 P3-D 的「詳情可讀、理由可見」那條承接；落檔 K-115。）

#### P3-C 夜間內容流程

- [x] headless：用合成 location 分別造當夜 dated primary、同地點白天 dated 反證、ch1/ch2/ch3 variant、兩個 add-on；在三章逐次呼叫 `night_beat_candidates()`，驗 primaries 優先序、白天 beat 不入列與 add-on 資料順序。（2026-08-22：`test_p3c` 第 1 組，合成 loader 8 個 beat；第 3 章 `[定日, ch3, ch2, ch1]`、第 2 章排除 ch3、第 11 天排除定日、白天 beat 不入列、`day_from`／`day_to` 區間 beat 不入列、addons 保持原序，共 6 條斷言。）
- [x] headless：`resolved_night_content()` 對 exact dated `condition=false` 會退回當章 variant、`condition=true` 會覆蓋 variant；primary `requires=false` 仍選中並呈 LOCKED、不退回，睡覺則不播放；add-on 依 condition 過濾且維持順序。（2026-08-22：`test_p3c` 第 2 組，注入 5 個合成 beat；退回／覆蓋／addon 過濾／`requires=false` 仍選中／`PanelBuilder.build()` 產 LOCKED／`sleep_night()` 回空，共 6 條斷言。）
- [x] headless：D31 第一次才進 `n_gathering` 時解析章節常態內容並寫 `saw_n_gathering_intro`，不播 `n_gathering_d31`；本輪較早已看過常態內容才播 D31 特殊內容；`end_run()` 後即使 meta seen 保留，未在新一輪先看常態內容仍不得播特殊內容。（2026-08-22：`test_p3c` 第 3 組，四條斷言含跨輪那條。）
- [x] code review：`PanelBuilder.build()`、`sleep_night()`、`_is_beat_time_valid()` 都使用 `resolved_night_content()`；`DataFacts.beat_matches_time()` 只管有 `when` 的日期／時段，三處沒有自行比較 night `when.day`／`when.phase` 的掃描。（2026-08-22：三處逐一讀過；`beat_matches_time()` 已縮成「無 `when` 一律 false」並改為三參數。grep `get("when")` 於 `panel_builder.gd`／`game_state.gd`／`main.gd` 皆 0 命中，剩餘命中全在 `data_loader.gd` 的 lint 與 `beats_at()`。K-30 據此結案。）
- [x] headless：paid night panel 回傳真實 beat id；全資料沒有 `<location>_locked` 假 id。合法零內容 row 回 `empty_result`，且 `beats_entered` 不新增鍵。（2026-08-22：`test_p3c` 第 5 組。第一版掃 `loader.beats` 找 `_locked` 是空斷言，`01ccff4` 改成對 `PanelBuilder.build()` 的輸出斷言——`n_ahong_1`／`n_source` 每個回傳 beat id 都要在 `beats_by_id` 裡且不得帶 `_locked` 後綴，**beats 為空直接 fail**。`empty_result` 與 `beats_entered` 不新增鍵同組覆蓋。原空斷言見 K-119。）
- [x] headless 雙輪：D1、D2、D15 第一輪各強制播一次、記 seen、免 cost、用掉今晚；第二輪同日不強播。另造「前一夜已主動到訪、fixed 日才到」狀態，fixed 仍播一次。（2026-08-22：`test_p3c` 第 6 組 13 條斷言，含 D14 主動進 `n_plaza` 後 D15 fixed 仍播、以及第二輪 D1／D2／D15 皆不重播且 chosen 保持空。）
- [x] headless 雙輪：第一輪與第二輪各播一次 `d24_night_laozeng`；每次播出後，其 `on_enter.flag` `laozeng_patrol_d24` 於該輪成立，且未寫 night location chosen；關閉面板／重建 route 不重播同一晚文字。（2026-08-22：`test_p3c` 第 7 組 5 條斷言；同夜重複呼叫 `play_night_fixed()` 回空由 `beats_entered` 擋下。）
- [x] headless 雙輪：D3 第一輪引導無槽、無 madness、seen 零變化、chosen 空；第二輪完全不播且清單直接可用。（2026-08-22：`test_p3c` 第 8 組 4 條斷言。`n3_map_opens` 已改掛 `sanquan`、`fixed`＋`meta_once`、`slots` 清空。）
- [x] headless 負向 fixture：非 night fixed 使用 `meta_once`、night-layer fixed 缺 `meta_once`、`meta_once` beat 缺 exact night `when`、**同一個 `when.day` 掛兩個不同 night-layer 地點的 fixed**，四種都由 lint 14 抓到。最後一種現行資料碰不到（D1／D2／D15 各在不同天），擋的是資料擴充——兩個會互相覆蓋 `night_location_chosen`，兩個地點都被記成到訪。（2026-08-22：`test_p3c` 第 9 組，**七個**獨立合成 loader 各驗自己的錯誤字串；`verify_data.gd` 掃正式資料 0 錯誤。`01ccff4` 補了陣列 phase 的三種（fixture 5／6／7）並讓 lint 認陣列，原漏洞見 K-118；verifier 刪掉陣列分支後 fixture 5 與 7 如預期轉紅。）
- [x] 🤖 UI：未選地點按「直接睡」，有睡覺內容時本次只把全文寫進 `FlowText`，day／phase 不變、按鈕變「進入隔天」；再按才進 morning。（2026-08-22：兩份證據。`p2d_01_vision_three` 以兩次真實點擊驗第一次停在 D24、`night_sleep_pending` 為 true、畫面讀得到內文，第二次才進 D25；按鈕文字由 `01ccff4` 的 `test_p3c` 第 12 組補上——載入真 `main.tscn` 讀 `AdvanceButton.text`，停拍時為「進入隔天」。⚠️ 第 12 組是場景級證據不是 ui_sim 契約，catalog 仍 63，差額見 K-123。）
- [x] 🤖 UI：無睡覺內容的夜按「直接睡」，一次進 morning，不出現空白停拍。（2026-08-22：`p2d_01_vision_zero` 一次點擊即進 D25，且畫面上讀不到睡覺內文。）
- [x] 🤖 UI：主動或 fixed 到訪後按鈕為「結束今晚」；按下只換日，不播放旅館 sleep beat。以一個同夜有 sleep 內容的 fixture 證明 K-33 不會復發。（2026-08-22：`test_p3c` 第 11 組 Case C 用 D24＋3 張發狂卡（同夜確實有 sleep 內容）驗 chosen 非空時 `resolve_night_advance()` 回 `advance:true` 且 lines 為空，K-33 據此結案；按鈕文字由第 12 組第 3 段驗為「結束今晚」。⚠️ 同樣是場景級證據，見 K-123。見 K-116。
- [ ] 🤖 UI：睡覺內容節點在 2 張 madness 時不存在、3 張時出現、清回 2 張又不存在，補完 K-68 卡住的畫面層證據。⚠️ **三腳成立兩腳**：`p2d_01_vision_zero`／`_three` 已改讀畫面文字（2 張看不到、3 張看得到），K-69 結案讓節點真的存在了；缺「清回 2 張又不存在」——現有兩個案例是兩個獨立定態 fixture，不是同一狀態的轉換。見 K-68（P3-D 補）。

#### P3-D 夜間地點清單與詳情 UI

> **驗收狀態（2026-08-22）**：13 條**全數打勾**。分兩批：`0047aaa` 實作 view-model、詳情面板、K-91 斷言反轉、K-68 第三腳、K-115 拒絕文案接線；`70f4fc5` 補齊 9 條 🤖 UI 缺口的專屬 ui_sim 契約（6 契約／10 變體，catalog 63→69）。verifier 自跑證據：**20 套 headless 全部 exit 0**（含 `test_p3d` 7 組）、UI sim **92 變體／69 契約／0 failed checks**（11 條負向反證仍如期失敗）。**verifier 獨立變異測試**：釘死 `location_summary` 的 `is_aligned=false` → `p3d_02` 兩變體如期轉 5 紅；拿掉 `already_chosen`/`already_slept` 阻斷 → `test_p3d` 轉 2 紅，均還原乾淨。K-68／K-91／K-94／K-115 隨本階段結案。

- [x] headless：把目前可見 night row 的 summary 依 `earliest_night`＋資料順序列出；依序改 seen／knowledge 後，id 順序逐字不變。（`test_p3d` #1）
- [x] 🤖 UI：未到訪可對位 row 顯示引子名＋`[尚未到訪]`；paid 與 cost 0 各取一筆，按鈕文字皆沒有數字與「免費」。（`p3d_01_status_no_number`：`n_ahong_1` paid／`n_woodtags` cost0，`_has_digits` 掃字元）
- [x] 🤖 UI：已到訪可對位 row 顯示 `[已到訪，尚未對位]`；已到訪夜間限定 row 只顯示 `[已到訪]`，沒有「尚未對位」。（`p3d_01_status_unaligned`／`p3d_01_status_nightonly`）
- [x] 🤖 UI：已對位一對一 row 顯示白天地點名＋`[已對位]`；多對一 row 顯示白天地點名・夜間引子名＋`[已對位]`。（`p3d_02_aligned_1to1`／`p3d_02_aligned_multi`；變異測試證實會咬）
- [x] 🤖 UI：點任一 visible row 只開詳情；在按「進入」之前完整 serialize 不變、其他地點仍可查看。成功按「進入」後才寫 seen／chosen 並進演出。（`p1af_29_night_resolution`／`p1af_29_night_paid`）
- [x] 🤖 UI：成功主動進場或 fixed 強制帶入後，清單仍保留全部當下可見 row 與原排序；每一項詳情仍可開，但所有 `night_enter::<id>` 都 disabled。（`p1af_29`：K-91 反轉後驗「其他 row 仍可見、進入鍵 disabled、點擊零變化」）
- [x] 🤖 UI：按「直接睡」播完睡覺內容、停在「進入隔天」的那一拍，清單同樣全部可見、詳情可開，但所有 `night_enter::<id>` 都 disabled；真實點擊後完整 serialize 零變化。（`p3d_05_sleep_pending`）
- [x] code review：`p1af_29_night_resolution` 與 `p1af_29_night_paid` 的「已選過地點後其他地點不可見」兩條斷言**已反轉**為「其他地點仍可見、`night_enter::<id>` 全部 disabled」，斷言訊息一起改（舊訊息把舊行為寫成了設計規則）；`make_states.gd` 與 `playthrough_greedy.gd` 不再依賴「chosen 非空時清單收斂成一個」。（`0047aaa` 反轉，grep 確認兩檔已無「收斂成一個」依賴）
- [x] 🤖 UI：requires 不成立的阿宏 row 可開詳情、具體理由看得到、`night_enter::<id>` disabled；真實點擊 disabled 控制項後狀態零變化。（`p3d_03_gated_ahong`）
- [x] 🤖 UI：teaser 第一、二章不存在，第三章出現；詳情顯示 `[尚未到訪]` 與理由，沒有任何可成功進入的輸入路徑。（`p3d_03_gated_teaser`；第一、二章不存在由 `test_p3d` #3 headless 驗）
- [x] 🤖 UI：用隔離 tuning／狀態讓首次 marker cost 必定達 cap，詳情顯示「再往前，你可能回不來。」且不顯示數字；seen 後 warning 消失。（`p3d_04_near_cap_warning`：警告有無＋無數字；seen 後消失由 `test_p3d` #5 驗）
- [x] 🤖 UI：合法零內容地點可進、顯示通用結果、消耗今晚；地圖按鈕不再帶「（無內容）」且不 disabled。（`map_list` 移除 `_SUFFIX_EMPTY` 與 disabled；`test_p3d` #6 驗零內容地點可進）
- [x] 🤖 UI：1280×720 下長名稱、狀態、reason、warning 全部可讀或可捲到；幾何診斷 0 overlap／0 clipping／0 offscreen。（`p3d_06_night_list_geometry`：`long_night_name` data 變體＋既有幾何診斷）

#### P3-E 對位系統

- [x] 🤖 UI：夜間 row 已 seen、白天尚未持有其 reveal 卡；白天地圖按鈕本身沒有提示。進正確 day counterpart、演出結束進常態後才看見 `night_align::<day_id>`。
- [x] 🤖 UI：同一狀態依序打開至少兩個錯誤白天地點，沒有 alignment button、沒有理由提示、完整 serialize 不變。
- [x] 🤖 UI：點 alignment button 只開確認彈窗；取消後 knowledge／hand／action／slots／choices 全部不變。
- [x] 🤖 UI：再次打開並確認，knowledge 恰好多一張正確 reveal 卡，action_spent 仍 false，按鈕消失；知識清單能打開卡片完整 name／text。
- [x] headless：一個 counterpart 的兩個 night row 都 seen 時，offer 只回一個 knowledge id；確認一次後兩個 summary 都已對位。
- [x] 🤖 UI：多對一只看過第一個 row 就確認；第二個 row 在清單仍是 `[尚未到訪]`。之後夜裡首次進第二個 row，離開詳情回清單即成 `[已對位]`，沒有第二次確認。
- [x] headless：直接注入 knowledge 但沒有 seen，row 仍不是已對位；只有 seen 沒 knowledge 也不是。serialize 的 run／meta keys 都不存在 aligned／matched 字樣。
- [x] headless：不同 `night_reveal` 被注入同一 counterpart 的 fixture 不產生兩個按鈕，而是在 lint／offer 報資料錯誤。
- [x] headless 拒絕矩陣：**繞過 UI 直接呼叫 `confirm_night_alignment()`**，六個 `reason_code` 逐一各一個案例，每個都驗「`ok == false`」＋「`reason_code` 等於預期字串」＋「前後完整 `serialize()` 逐字相同」：`not_day_phase`（evening 與 night 各一次）、`unknown_location`、`not_day_layer`（傳夜間 row id）、`no_seen_row`（counterpart 一個都沒到訪）、`already_known`（共用 knowledge 已在手時再按一次）、`data_conflict`（同 counterpart 兩個不同 `night_reveal` 的 fixture，且同時 `push_error`）。「按鈕沒出現」驗的是 `alignment_offer()`，不能拿來代替這條——這個入口可以被直接呼叫。
- [x] headless：同一組狀態下 `alignment_offer().available == false` 與 `confirm_night_alignment().ok == false` **逐項對得上**，六個 code 各驗一次。兩邊分歧代表判準有兩份實作，就是第三份對位狀態偷偷長出來的前兆。

#### P3-F 全流程驗收

- [x] headless：`test_p3f.gd` 從正式資料動態建立 28-row matrix，逐類覆蓋一般／gated／paid／cost 0／alignable／night-only／multi-row／teaser／empty；測試沒有複製完整 mapping 表。（2026-08-25 `72b84ca`：測試 1 動態數出 28／可對位 12／夜間限定 16／收費 14／免費 13＋teaser 1／門檻 6，10 張 reveal 全在 cards.json 且型別 knowledge，皆由 `loader.locations` 迴圈衍生。）
- [x] headless：第一輪用明示 id 的兩條策略重演，不依賴 `locations.json` 排序：路徑效率案例 D14 走 `n_ahong_3`、D15 fixed 取得 `n_plaza`，走滿收費 row 共收 13 marker cost；最大壓力案例 D14 主動走 `n_plaza`，共收 14。兩條都與更新後的模擬文件一致；不直接改 expected 或對調資料順序。（2026-08-25：測試 2／3 分別斷言 13／14 marker cost、消除 11／12、留手 2；模擬文件已於 `98949f1` 更新為第四次重跑並與此兩基準逐項對齊，K-122 結案。）
- [x] headless：第一輪真實 `end_run()` 後跑第二輪；seen paid 不發卡、unseen paid 首次仍發、seen cost 0 不發、D1／D2／D15 不重播、D24 重播。本條只驗終身首次收費，不以現有第二輪供給判斷壓力是否足夠。（2026-08-25：測試 4 五路徑抽樣 Path 1～5 全綠。）
- [x] headless：同一第一輪終態 serialize 後載入兩次，走相同第二輪操作序列，最終狀態與事件時間軸逐字相同。（2026-08-25：測試 5 兩獨立 GameState 實例 serialize 逐字相同、時間軸逐項相同。）
- [x] 🤖 UI：P3-C～E 所有 UI 契約以真實 input event 跑過；catalog／matrix／launcher 完成數與文件總數完全一致，負向反證仍以預期原因失敗。（2026-08-25：UI sim **97 變體／74 catalog／74 executed／failed 0**、11 條負向反證全如期失敗；K-123 的按鈕三態已塞進 `p3d_05_sleep`（「直接睡」→「進入隔天」），`72b84ca` 結案。）
- [x] headless：`verify_data.gd`、P1／P2 既有 16 套、P3-A～F、`test_boot.gd`、`playthrough_greedy.gd` 全部 exit 0。（2026-08-25：`run_all_headless.ps1` 22 套逐套 exit 0，ALL HEADLESS TESTS PASSED。）
- [x] code review：K-30／33／34／35／69 的舊形狀已不存在；K-68 剩餘畫面證據與 K-70 地圖斷言有真實 UI 證據。K-72 未被順手擴張。（**K-37 已於 P1-F 結案，不列入本階段**。）（2026-08-25：grep 確認 `open_night_marker` 全退場、builder／gs／main 無 night `when` 掃描；K-34／35 由測試 1 明示斷言；K-68→`p2d_01_vision_three`、K-70→`p2d_02_be_screen` 兩契約綠；`72b84ca` 僅動 test_p3f／run_all_headless／p1af_cases 三檔，未擴 `test_p2_sim`。）
- [ ] 👤（完整第一輪＋第二輪抽查）夜間清單 28 項掃讀不會因狀態改變跳位；三種按鈕停拍不誤導；灰地點理由看得懂；多對一名稱能分辨但不劇透。四項逐項記錄，不以「整體 OK」代替。**（2026-08-25 仍未落檔：機器全綠不代替此四項體感判斷，需真人玩過後逐項寫入。P3-F 因此維持 🟦 待人工體感，機器層七條已全打勾。）**

### Phase 4 — 委託與遭遇

> 本節是 verifier 的驗收清單。實作者跑完只提供 exit code、報告路徑與 catalog 實際數字；不得自行修改本節或打勾。

#### P4-A 資料與 SCHEMA 真值化

- [ ] `verify_data`：delegation／encounter 新欄位全數被 schema、引用檢查與封閉效果鍵認得；不是「未知鍵被忽略」。
- [ ] lint 15 正向：每個委託槽只收一個明確 person id，與親自處理槽共用 choice_group；immediate 無 report，next_morning 有 report。
- [ ] lint 15 負向 fixture 各自抓到：泛型 person／非人物卡、缺 choice_group、timing 非法、report 缺或多、next_morning 可成立於 D45、缺鎖定理由、引用不存在。
- [ ] lint 16 正向：D8／D45 round graph 全可達且可結束；response accepts 不重疊、三出口與 fallback 完整，`allow_discard`／`escape_cost` 符合兩場設計。
- [ ] lint 16 負向 fixture 各自抓到：零回合／重複 id、懸空或不可達 next_round、無出口 cycle、接受集合空／重疊／壞引用、`allow_discard` 等型別錯、非法永久人物消耗、repeat 用在無明確 `when.day` 的 beat、charge 用在非 night-layer location。
- [ ] lint 14 回歸：D8 的 `repeat_each_run` 可不 meta-once；任一非遭遇 night fixed 仍會因缺 meta-once 報錯，repeat 與 meta-once 同時出現也報錯。
- [ ] 資料 review：D17 不再自動發阿婕／阿珠／阿才；人物得失都有具名事件來源。處方任務可在 D17～19 完成，任一路線完成後不再出現。
- [ ] 資料 review：D8 固定姓名段不在 rounds；首個互動要求第一輪無可得正解，第二／三個各有至少兩條早期取得路徑。D8 `allow_discard:true`；D45 一回合、不可逃、`allow_discard:false`，每條正式回應有獨立文字／效果。
- [ ] 全套既有資料驗證、headless 與 `test_boot.gd` exit 0；回報 catalog 實際數字，不預填文件常數。

#### P4-B 委託規則

- [ ] headless：同一 person 上午成功後，下午／evening 再委託回 `already_delegated_today`；另一 person 同日仍可成功。跨時段不清 daily set。
- [ ] headless：換日上午完成既有發狂倒數／強制縱慾後，pending reports 依派出順序各套一次；daily set 清空，該日上午可再委託。
- [ ] headless：immediate 當場套效果且不進 pending；next_morning 派出時只套當下效果，隔日才套 report。人物卡兩條路都留在 hand。
- [ ] headless：委託成功不改主角 action_spent／actions_remaining，不增加 `npc_action_counts`；親自做與任一人物路線共用 choice 後互斥。
- [ ] headless：關係增加、下降、不變各一案例，證明沒有全域自動 delta；相同初始狀態與選擇得到完全相同結果，沒有 RNG。
- [ ] headless：pending serialize 往返後 due_day、接點、順序一致；人物卡在回報前被事件移除仍照回報；`end_run()` 清 pending／daily。
- [ ] 拒絕矩陣：逐一直接呼叫 `delegate()` 驗 `not_action_phase`、`unknown_beat`、`unknown_slot`、`not_delegation`、`not_held`、`not_person`、`not_accepted`、`already_delegated_today`、`locked`、`already_resolved`、`data_conflict`；每例 code 精確且前後 serialize 逐字相同。

#### P4-C 委託 UI 與首個案例

- [ ] 🤖 UI：未取得人物卡的候選完全不出現；取得後在資料原位置出現。今日用過後仍在原位、顯示「今日已受託」且 disabled，隔日上午恢復。
- [ ] 🤖 UI：條件不足候選顯示資料理由；取消確認後完整 serialize 不變。所有候選狀態切換都不改排序。
- [ ] 🤖 UI：確認畫面只顯示任務、立即／隔日上午、傾向／風險；不顯示完整 gain／lose、relation delta 或 report 正文。
- [ ] 🤖 UI：立即結果當場進 FlowText；延遲結果派出當下不劇透、下一日上午在既有結算後播。兩者成功後 choice 與手牌狀態立即刷新。
- [ ] 🤖 UI：人物卡從零變第一張時出現一次教學；不同日期／不同首張人物都能觸發，之後取得第二張與第二輪都不重播。D17 零人物卡時沒有空教學或空候選。
- [ ] headless＋UI：阿婕無破壞信任事件時可早期取得；對她使用縱慾卡後不可取得，明示修復事件後才恢復。阿珠只由 D9 揭露路線、阿才只在 D17～19 共事後取得。
- [ ] headless＋UI：D17、D18、D19 各能完成處方；親自做與三名人物路線任一成功後，其餘槽收起且後續日不再重複。

#### P4-D 遭遇規則

- [ ] headless：一般 `start_encounter()` 直接進第一 round，總共只加一次 cost，不另收 start cost；後續每進新 round 各加一次。超載進場才明示為 penalty 一次＋第一 round 一次。正解只釋放本 round cost，fallback 保留；可用格歸零只套一次 failure 並清 active state。
- [ ] headless：同一 round 至少兩組 response 都能命中各自效果；`encounter_view()` 不含 accepts、fallback、未到回合或完整結果。
- [ ] headless：普通可丟棄卡錯答會消耗；人物／主角／知識等不可丟棄卡答錯仍留手，但記 attempted、保留壓力、照 next_round 推進且同場不能再交。
- [ ] headless：`allow_discard:true` 時主動丟棄只移除合法卡與它自己的格，不改 blocked、不推回合；發狂卡與不可丟棄卡拒絕零變化。false 時直接呼叫入口回 `discard_disabled` 且零變化。
- [ ] headless：escape cost 恰好支付才成功；少／多張、重複 id、非法卡全部拒絕零變化。`escape_cost:null` 回 `cannot_escape`。
- [ ] headless：victory／failure／escape 的效果各只套一次，結束後 active dictionary 空、blocked／attempted 無殘留；遭遇全程不消耗 action、不開縱慾或備用區。
- [ ] headless：active encounter 序列化往返後 beat、round、blocked、attempted 一致；從還原狀態繼續與未存檔路徑結果相同。
- [ ] headless：只剩已嘗試卡／發狂卡、無合法 discard 且付不起 escape 時立即套一次 failure；`escape_cost:0` 或尚可合法丟棄／支付時不得提早失敗。
- [ ] 拒絕矩陣：逐字驗封閉碼 `encounter_active`、`no_active_encounter`、`unknown_beat`、`unknown_card`、`not_held`、`madness_blocked`、`already_attempted`、`discard_disabled`、`not_discardable`、`cannot_escape`、`wrong_escape_count`、`duplicate_payment`、`data_conflict`；每例 code 精確且前後 serialize 逐字相同。
- [ ] 拒絕順序：為四個 mutation 入口各做至少一組「兩個拒絕條件同時成立」案例，逐字符合方針 P4-D 的固定優先序；不得只驗 `ok == false`。

#### P4-E 遭遇 UI 與 D8／D45

- [ ] 🤖 UI：遭遇畫面只顯示當前要求、壓力 placeholder、既有手牌與當下可用操作；不顯示正解集合或下一回合。鍵鼠都能選卡、確認、取消。
- [ ] 🤖 UI：已嘗試但未消耗的卡留在手上且不可再用；發狂卡顯示被壓死。disabled 理由可讀，取消任何確認都零變化。
- [ ] D8 第一輪：night 不能直接睡、跳過或延後；固定姓名段先播且不消耗主角卡，之後才進第一個互動要求。
- [ ] D8 首次到訪：只由共用夜間入口收一筆 marker cost；遭遇沒有第二筆。第二輪 D8 仍發生但不重收首次費。另直接注入 `n_manydoors` 已 seen 狀態再觸發 D8，證明 helper 先保存 `was_seen`、不因強制進場重收。
- [ ] D8 路徑：第一個互動要求第一輪必失去一張普通卡；第二／三要求各至少兩種正解可走。涵蓋典型損失一張與兩張、格滿失敗、後續輪 knowledge 改寫。
- [ ] D45：afternoon 只收一次回應、不可逃也不可主動丟棄；escape／discard 控制都不顯示，直呼兩個入口也各自拒絕。選後 encounter 清空並只推進一次到 evening；不同卡的文本／效果不同，人物卡回應後仍在手且人物未永久失去。
- [ ] 🤖 UI：D8 勝／敗都回夜間「結束今晚」；D45 回 evening 收尾。任何出口都沒有殘留遮罩、placeholder 或不可操作 hand。

#### P4-F 全流程與跨輪驗收

- [ ] headless：第一輪以零人物卡、阿婕、阿珠、阿才四種狀態各走完 D17～19，覆蓋親自做、immediate、next_morning 與每日人物一次限制。
- [ ] headless：D8／D45 response matrix 從正式資料衍生，涵蓋正解、錯答、failure、escape 可／不可、discard、不可丟棄卡、特殊轉化；不複製完整 mapping。
- [ ] headless：第一輪真實 `end_run()` 後第二輪，教學／knowledge／night seen 保留；daily、pending、active encounter 全清。D8 重演且不重收費。
- [ ] headless：相同第一輪終態 serialize 後載入兩次，走相同第二輪決策，最終狀態與事件／回報時間軸逐字相同。
- [ ] `playthrough_greedy.gd` 可從 D1 走到結局；P1～P3 既有測試、P4-A～F、`test_boot.gd` 全部 exit 0。若 expected 改變，報告列出是哪些遭遇損失造成。
- [ ] 🤖 UI：P4-C／E 全部用真實 input event 跑過；catalog／matrix／launcher／文件完成數一致，負向反證仍以預期原因失敗。
- [ ] code review：資料只有一份委託與遭遇真值；UI 無規則、GameState 無 beat id／人物 id 特判、沒有 away／partner capacity／成功率／通用永久人物失去等未授權狀態。
- [ ] 👤 人工四項逐條記錄：委託預覽是否足夠但不劇透；立即／隔日回報停拍是否自然；D8 壓迫與一至兩張損失是否合理；D45 一次性選擇與鍵鼠焦點是否清楚。不得以「整體 OK」代替。
