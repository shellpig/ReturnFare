# 測試指南

> 測試流程與手動驗收步驟清單。
> **撰寫時機**：各子階段的測試項目於**該階段規格定稿時即寫入**（acceptance-test 先行），供實作時據以建測試與自我驗收——不延到驗證時才補。
> **角色歸屬**：verifier-owned。實作者照清單建測試、不得放寬清單、不改本檔（只列建議）。

## 本檔範圍與邊界

- **`實作規格書.md` 的驗收＝意圖層**：什麼必須為真，不寫操作步驟。
- **本檔＝操作層**：起什麼、按什麼、看什麼。同一條斷言不抄兩份，每個清單段落 link 回規格書對應段。
- 規格定稿時，測試項目可能引用尚未定的文本（stub 文字、卡名）；先用中性描述（「顯示其理由」「播出該段文字」），實作後不回頭改寫成實際字樣。
- 行號會漂移，一律以標題或 id 定位（`AGENTS.md > 文件查閱規則`）。

## Godot 測試環境

外部工具路徑見 `AGENTS.md > 專案外部工具路徑`。

### Headless 驗證命令

> 已知限制：sandbox 內直接跑會因無法開 `user://logs/godot*.log` 而 crash（signal 11）。用 escalated 權限執行；搬動 `.tscn` / `.gd` 後先 `--import` 重建快取（`AGENTS.md > Godot Headless 驗證`）。

```powershell
# 資料完整性（既有，隨 lint 擴充）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://scripts/verify_data.gd

# 正式啟動路徑（每個子階段收尾都要跑，見下方說明）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_boot.gd

# 各子階段的驗收測試
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_game_state_p1a.gd
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_hand_p1b.gd
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/test_p1c.gd

# 45 天貪心自動走查（P1-F 起）
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/headless/playthrough_greedy.gd
```

全部以 exit code 0 為綠。`test_game_state_p1a` 的輸出裡有一則 `Parse JSON failed`，那是壞資料 fixture 的預期輸出，不是失敗——**看 exit code，不看有沒有 ERROR 字樣**。

### `test_boot.gd` 為什麼獨立一條

其他測試都自己 `new()` 出 GameState 與 Data 再 `Engine.register_singleton()`，走的是**注入路徑**；正式遊戲走 `project.godot` 的 **autoload 路徑**。兩條組裝方式不同，而 2026-08-13 之前只有前者被驗過——結果是 P1-A 到 P1-C 三套全綠，**而遊戲根本開不起來**（`data.gd` 用 `Engine.get_singleton` 找 autoload，永遠拿到 null，`Data.ok` 恆為 false，主畫面停在錯誤字串）。

`test_boot.gd` 是 autoload 路徑的唯一守門員。它驗三件事：autoload 掛得上、`Data.ok` 為真、`main.tscn` 進得到正常分支（ErrorLabel 空、狀態列與推進鈕可見、狀態列的字是 `_refresh_status()` 寫的）。

⚠️ **這一檔裡不准手動註冊 singleton。** 註冊了就等於把它變成第二個注入路徑測試，守門員失效。

GUI 啟動：

```powershell
C:\_work\Godot_v4.6.3\Godot_v4.6.3-stable_win64.exe --path .
```

## 手動驗收清單

### Phase 1 — 最小可玩迴圈

> 意圖層：`實作規格書.md > Phase 1` 各小節。實作契約：`開發設計方針.md > Phase 1`。
> 全文字 UI；「推進」＝主畫面的時段推進操作。
> **每個子階段收尾都重跑**：`test_boot.gd`（正式啟動路徑，見上方說明）；序列化往返相等測試（欄位隨子階段增加）；破壞性測試一律用 `tests/fixtures/broken/`，不改正式 `data/`。
> 接線隨 Phase 越來越多，而正式啟動路徑斷掉的時候，子階段測試不會紅——所以 boot 那一條不能只在 P1-A 跑一次。

#### P1-A 遊戲狀態與時段狀態機

- [ ] 啟動遊戲：狀態列顯示第 1 天、morning、第一章。
- [ ] 連續推進：時段依 morning → afternoon → evening → night → 次日 morning 循環，無跳格、無重複。
- [ ] 推進到第 16 天與第 33 天 morning：章節顯示分別切為二、三，且各只切一次。
- [ ] 推進到第 45 天：afternoon 之後進 evening（結局 coda），**不出現 night**，coda 播畢進入結局流程（P1-A 階段 coda 為 stub 顯示即可）。
- [ ] headless：以 `tests/fixtures/broken/` 的壞 JSON fixture 載入 → 回報載入錯誤、不進遊戲（不改正式 `data/`）。
- [ ] headless：GameState 序列化往返相等斷言綠（實作者建於走查腳本或獨立測試）。
- [ ] headless：`tuning.phases_per_day` 與結構常數不一致的 fixture → 載入報錯（一致性驗證）。
- [ ] headless：`test_boot.gd` 全綠——autoload 掛得上、`Data.ok` 為真、`main.tscn` 走正常分支。**這一條驗的是「遊戲開得起來」，跟上面那些走不同的組裝路徑。**

#### P1-B 卡片與手牌

- [ ] 主畫面常駐手牌列：顯示佔格數／上限，上限值改 `data/tuning.json` 的 `hand_size` 後重啟即反映。
- [ ] 知識清單與手牌列分開呈現；知識卡永不出現在手牌列。
- [ ] headless：`gain_card` / `lose_card` 進出正確（含 knowledge 分流、主角卡不可 lose）斷言綠。

#### P1-C 地圖、面板與三態

- [ ] 第 1 天 evening：到站事件自動逐行播出，播畢主角卡入手（手牌列 1 格）。
- [ ] 回頭重看同一事件（若 UI 允許）：`on_enter` 不重複結算，手牌不變。
- [ ] 第 2 天 morning：地圖只列當前時段開放、當前章節已開的白天地點。
- [ ] 開任一面板：已在場的人事物以 occupant 呈現、不可放卡；空槽可互動。
- [ ] 找一個 `requires` 不成立的槽：呈灰、點擊不放卡、顯示其理由文字。
- [ ] 該槽條件補齊後（同面板內取得所需卡）：不關面板即變為可放。
- [ ] `condition` 不成立的 beat 完全不出現（對照 `data/` 該日資料驗）。
- [ ] 第 3 天 afternoon：該時段三個互搶的 beat（阿宏／夫妻／帳本）面板全部照資料呈現。

#### P1-D 放置與效果結算

- [ ] 放主角卡進可選槽：效果文字播出、`on_place` 全部效果落帳（以手牌／知識清單變化驗）。
- [ ] 同一行動格內，任何地點都不能再放第二次主角卡。
- [ ] 放置後同面板即時重求值（新開啟的槽出現、失效的槽轉灰或消失）。
- [ ] 比對槽（不收主角卡）放情報卡：結算但**不消耗行動格**——放完仍可在同時段放主角卡。
- [ ] 對不符 `accepts` 的槽放卡：被拒收，狀態無任何變化。
- [ ] headless：registry 存在但未持有的卡直接呼叫規則層放置 → 失敗、GameState 零變化，且與 UI 操作結果一致。
- [ ] 對帶 `attention_npc` 的槽放主角卡成功 → 該 NPC 投入帳 +1；放置失敗或免費槽 → 不變。
- [ ] headless：全部十三個 condition 運算子（`not`／`all`／`any` 各算一個，含 `day_at_least`）各至少一條斷言綠；含未知語彙鍵的 fixture → 載入報錯。

#### P1-E choice_group

指定驗收例：第 22 天「你覺得哪裡不對」那一組（兩個入口都要通）。

- [ ] 選定組內一個選項：同組其餘選項**消失**（不是灰掉）。
- [ ] 重開面板：只剩被選的那個，**以唯讀展示（RESOLVED）呈現、不可再操作**；無反悔路徑。
- [ ] 用卡選與直選同一選項後（分別存檔驗）：除卡片消耗外，兩條路留下的狀態完全一致。
- [ ] headless：對已結算的組重複呼叫選擇 → no-op，效果不重複套用。
- [ ] 不選就離開面板：無任何結算；重開面板整組選項仍在。
- [ ] 手上無相關卡：可直接點選選項完成結算。
- [ ] 手上有相關卡：可放卡選同一個選項，**結果與直選相同**（同一個 `on_place`；「加分」是多一條入口，不是不同結果）。

#### P1-F 45 天全程走通與迴圈 stub

- [ ] headless：`playthrough_greedy.gd` 從第 1 天跑到結局 stub，exit 0（第 1 天上午下午、第 32 天下午允許空格，名單同 `verify_data.gd` 的 `by_design`）。
- [ ] evening 殘響：刻意錯過第 3 天下午 `d3_pm_couple` → 第 5 天 evening 播出殘響行；到場則不播。（原本寫的第 5 天阿宏家那條是同文複本且沒有 `day` 欄，已於 2026-08-13 刪除，見待決 1。）
- [ ] evening 殘響：第一章三次全部播得出來——第 5 天（`d3_pm_couple.echo`）、第 8 天（`d8_echo_bathhouse`，`fixed` 掛 evening，**玩家當天沒去浴場也要播**）、第 13 天（`d12_pm_awei.echo`）。
- [ ] evening 結算順序：第 27 天 evening 先播 `d27_evening_awei_confirm`（寫 `dodger_awei_cleared`）再播 `d27_evening_cover_knowledge`（讀它）。收留過阿薇且當天下午去過診所或靜和園的存檔 → 當晚拿到 `k_town_covers`；兩者只成立一個 → 不拿到。
- [ ] night：夜間地圖免費地點可進；解析正確——第 1 夜走廊自動強制播出（不選地點）、定日 beat 優先於章節變體、附加 beat 並列（第 10 夜起「地標」同時看得到「有東西可以拿走」）；收費地點呈灰、附 stub 理由；**不產生任何發狂卡**。
- [ ] night：直接睡＝解析旅館當夜定日 beat——第 24 夜 fixed beat 入夜強制播；（Phase 2 起補驗）颱風夜條件成立時睡覺播 bleed beat。
- [ ] 夜間放主角卡（第 10 夜「拿走／不拿」）：結算成功且**不消耗行動格**。
- [ ] 第 45 天 evening 結局 coda：`d45_then` 播出、其比對槽放入對應情報卡可完成知識升級**且不吃行動格**；coda 播畢回到第 1 天 morning——知識清單完整保留；手牌只剩主角卡；旗標／開關／關係歸零（以任一靠旗標開啟的槽回到未開狀態驗）。
- [ ] headless：`run_ended` 一輪**恰好發射一次**——結局之後連按幾次推進，signal 計數仍是 1，天數停在第 1 天 morning 並照常往下走。驗的是 `advance_phase()` 自己收尾（`開發設計方針.md > P1-F` 結局 coda）：若收尾被留給 listener，走查腳本會停在第 45 天 evening 重發 signal，變成無窮迴圈。
- [ ] 第二輪重看到站事件：主角卡仍恰好一張（gain 冪等，不重複發卡）。
- [ ] `verify_data.gd`（含 lint 1–3、5、7、8）與 `test_boot.gd` 全綠。

#### P1-G 面板互動模型

> P1-G 改掉了 P1-C／P1-D 的面板行為，**上面 P1-C、P1-D 兩節裡「開面板即看到全部內容」那幾條已經作廢**，以本節為準。驗收意圖見 `實作規格書.md > P1-G`。

- [ ] 演出逐一：進入第 32 天上午的寺廟，成立的 beat **一個一個演**，每次要按一下才推進；全部演完才進常態階段。該面板有 4 個 fixed beat，但後三個依 `invited_ajie` ／ `invited_awei` ／ 兩者皆無互斥，**沒邀人的存檔會演到 `d32_festival` ＋ `d32_festival_alone` 兩個**，邀了阿婕就換成 `d32_festival_ajie`。**驗成立與不成立各自的行為，不要數筆數。**
- [ ] 演出期間鎖互動：演出還沒走完時，畫面上沒有任何可放卡的槽（不是灰掉，是還沒建立）。
- [ ] 重進不重複結算：進一個帶 `gain` 的 fixed beat（例：第 1 天傍晚公車站到站事件）→ 離開 → 再進去。**文字要重播，但手牌張數不變**。
- [ ] 槽型別標示：第 22 天下午堆沙包的四個觀察槽，型別顯示成「裝備卡、情報卡」（用「、」不是「／」）。
- [ ] 槽型別標示：隨便找一個收主角卡的槽，顯示「主角卡」；名稱與 `data/card_types.json` 一致。
- [ ] 不劇透：`accepts` 寫具體卡 id 的槽（例：第 9 天下午納骨塔的比對槽）**只顯示「情報卡」，不顯示卡名**。
- [ ] 右鍵預覽：對一個可放的槽右鍵，列出手上放得進去的卡；關掉之後**天數、時段、手牌、知識、旗標全部不變**。
- [ ] 右鍵預覽（灰槽）：對灰掉的槽右鍵，清單是空的並顯示 `reject_reason`。
- [ ] 預覽與實放一致：預覽說可以放的卡，實際放下去一定成功；預覽沒列出來的，放下去一定被拒。
- [ ] 推進提示：把當前時段能放的卡全部放完，且完成所有尚未結算的選擇題 → 推進按鈕變成提示狀態，**但時段沒有自己走掉**；此時仍可以走去別的地點看內容。第 35 天下午、第 40 天上午、第 43 天下午即使沒有可放的卡，仍應因 OPEN `choice_group` 保持可做，不得提前亮提示。
- [ ] 夜間同一套：第 1 夜進「很長的走廊」，走的是同一條演出流程（逐一、可重播、演完才開放互動）。
- [ ] 地點描述：`locations.json` 填了 `desc` 的地點，常態階段顯示得出來；沒填的只顯示地點名，不報錯。
- [ ] headless：`verify_data.gd` 新增的 lint 9 全綠（`cards.json` 的 `type` 與 `card_types.json` 的 `id` 雙向封閉）。
- [ ] headless：`playthrough_greedy.gd` 在新模型下 exit 0，90 個時段分類統計與 P1-F 收尾時相同。
- [ ] headless：走查腳本走的是 `play_beat()` 那條路——白天 28 個帶 `on_enter` 的 fixed beat 在走查中確實結算（拿 `beats_entered` 筆數驗，別只看 exit code）。

### Phase 2 — 發狂時鐘與縱慾

> 意圖層：`實作規格書.md > Phase 2` 各小節。實作契約：`開發設計方針.md > Phase 2`。
> **P2 的數值全部住 `data/tuning.json`**（`madness_countdown_days` 7、`madness_cap` 7、`madness_vision_threshold` 3、`indulgence.*`）。下面寫「第 13 天歸零」這種話時，指的是**現行數值下**的結果；改了數值要重算，不是測試錯了。
> **每個子階段收尾都重跑**：`test_boot.gd`、序列化往返（新增 `madness_clock` 錶值、`indulgence_count`、`forced_pending`、`night_markers_opened`）、P1 十套 headless 無迴歸。

#### P2-A 發狂卡的產生與倒數

- [ ] 第 6 夜進一個收費標記：手牌立刻多一張發狂卡，卡面顯示 7 天。**當天不減**——推進到 night 結束、看卡面仍是 7。
- [ ] 次日（第 7 天）morning：卡面變 6。之後每天 morning 減 1。
- [ ] 第 13 天 morning：那張卡歸零。（P2-A 階段只驗歸零被偵測到，執行是 P2-C。）
- [ ] 各走各的鐘：第 6 夜與第 7 夜各開一個標記 → 第 8 天 morning 兩張分別是 5 與 6，不同步。
- [ ] 同一個收費地點第二次進入：**不再發卡**，手牌張數不變。
- [ ] 免費地點（`madness_cost` 為 0）：進去不發卡。
- [ ] 第 1 夜「很長的走廊」入夜強制播出：**不算開標記、不發卡**（規格書第九節解析第 1 步）。
- [ ] 一夜一個：選了一個夜間地點之後，同一夜其他地點進不去。
- [ ] 手牌列的佔格數把發狂卡算進去（開兩張之後，佔格數比開之前多 2）。
- [ ] headless：序列化往返——存檔後讀回，每張卡的剩餘天數、實例編號、已開標記集合完全相同。

#### P2-B 縱慾出口與主動縱慾

- [ ] 手上 0 張發狂卡時逛山泉閣：**看不到任何出口槽**（不是灰掉，是不存在）。
- [ ] 手上有 1 張之後再逛山泉閣：砸東西、食慾、泡湯三個槽出現。
- [ ] 槽型別標示（沿用 P1-G 規則）：出口槽顯示「發狂卡」，名稱取自 `data/card_types.json` 的 `madness`。
- [ ] 放一張發狂卡進「砸東西」：效果文字播出、那張卡消失、**該行動格用掉**——同時段任何地點都不能再放主角卡。
- [ ] 上午放進「泡湯」：**當天上午下午都用掉**，直接進 evening；手上只少一張，不是全清。
- [ ] 下午點「泡湯」：呈灰＋理由（只能上午發動）。
- [ ] 上午只剩一格可用時（例如強制縱慾已吃掉上午）點泡湯：呈灰＋理由（格數不夠）。
- [ ] 第 15 天逛老街的「奢侈」：呈灰＋理由。**這是已知的資料缺口**（錢卡還不存在，`實作規格書.md > 附` 有記），灰掉是正確行為，不是 bug。
- [ ] 第 10 天逛山泉閣：**看不到「暴力對人」**（擋在第 16 天之後）。第 16 天起看得到。
- [ ] 與阿婕關係未達「疑似」時：看不到性慾那個槽；達到之後看得到。
- [ ] 對出口槽放非發狂卡（例如主角卡、情報卡）：被拒收，狀態無任何變化。
- [ ] headless：`indulge()` 拒絕路徑逐條驗（卡不在手上、槽已放過、泡湯在下午、格數不足）→ 每條都是 GameState 零變化。
- [ ] headless：`verify_data.gd` 新增的 lint 10 全綠；lint 4（縱慾完整性）在真資料上通過。
- [ ] headless：`when.day_from` / `day_to` 與 `phase` 陣列解析正確——出口 beat 在第 1、23、45 天的上午都成立；`x_soak` 在下午不成立。
- [ ] headless：有 `day` 又有 `day_from` 的 fixture → 載入報錯（用 `tests/fixtures/broken/`，不改正式 `data/`）。

#### P2-C 強制縱慾

- [ ] 第 6 夜開一張、之後不主動清：第 13 天 morning 一進到上午，**遊戲自己執行縱慾**——文字播出、那張卡消失、上午那一格被吃掉，玩家該時段不能再放主角卡。
- [ ] 第一次是輕的，而且文字明講下次更重。
- [ ] 同日兩張歸零：**各吃一格**——上午一次、下午一次，那一天玩家完全沒有行動格。
- [ ] 同日三張歸零：吃掉當天兩格，**第三張順延到次日上午**。次日上午再被吃一格。
- [ ] 出口挑選：第一章（第 16 天前）只有砸東西與食慾在池裡，所以挑到的一定是**食慾**（權重 2 > 1）。
- [ ] 出口挑選：第 16 天之後、關係未達「疑似」時，挑到的是**暴力對人**（權重 4）。
- [ ] 出口挑選：與阿婕達「疑似」之後，挑到的是**性慾**（權重 5）。這一格就是企劃書說的「你越放著不管，主角越往你最不想他去的地方走」。
- [ ] **泡湯永遠不會被自動挑中**——把其他出口全部擋掉的存檔也一樣（此時應挑到砸東西或食慾，不是泡湯）。
- [ ] 強制縱慾發生時玩家人在別的地點：**照樣執行**。挑選不看玩家位置。
- [ ] 主動與強制共用同一個計數：主動清掉 3 張之後，第 4 次（強制）的強度級要照 `forced_light_count` / `forced_normal_until` 算，不是從 1 重數。
- [ ] headless：決定論——同一個存檔重跑兩次，挑到的出口、天數、順序逐項相同。

#### P2-D 視野門檻與發瘋 BE

- [ ] 桌上 2 張時：掛 `madness_at_least: 3` 的內容**看不到**（不是灰掉）。
- [ ] 桌上 3 張時：同一格內容出現。清掉一張之後又消失。
- [ ] headless：用 fixture 把 `madness_cap` 調到 2，開兩張 → **立刻進發瘋 BE**，本輪結束。
- [ ] headless：BE **不播後日談骨架**。
- [ ] headless：BE 之後回到第 1 天 morning，知識集合完整保留、手牌只剩主角卡。
- [ ] headless：`run_ended` **恰好發射一次**——一次發多張卡同時衝破 cap 也只結束一輪（拿 `madness_cost` 2 以上的 fixture 驗）。
- [ ] headless：正式資料上（cap 7）**第一輪碰不到 BE**——三種玩家重演都不觸發（與 P2-E 同一份測試可共用斷言）。

#### P2-E headless 重演三種玩家

> 這一條全部是 headless。基準表住 `實作規格書.md > P2-E`，來源是 `subdocs/驗證/發狂卡機制模擬.md > 三`。

- [ ] headless：`test_p2_sim.gd` 三種玩家全部 exit 0，八個指標逐項對上基準表（桌面峰值、觸及 cap、強制次數、最重級次數、主動次數、吃掉的行動格、≥3 張的天數、開到的標記數）。
- [ ] headless：視野窗口對上——A 第 8–23 天＋第 42–44 天；B 第 8–26 天＋第 42–44 天；C 無。
- [ ] headless：A 深潛的逐項時間軸與模擬文件第三節那份逐行相同（峰值 4 落在第 11 天、第一次最重落在第 27 天）。
- [ ] headless：測試走的是真的規則層入口（`open_night_marker()` / `indulge()` / `advance_phase()`），不是另寫一份模型。**這一條用 code review 驗，不是跑出來的。**
- [ ] headless：`playthrough_greedy.gd` 仍 exit 0。90 個時段的分類統計**會因為強制縱慾吃格而改變**——把新數字記進 commit message，當作下一次的基準。
- [ ] headless：`verify_data.gd`（含 lint 1–5、7–10）與 `test_boot.gd` 全綠。
