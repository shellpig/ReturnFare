# 驗證後已知問題

## 本檔範圍與邊界

驗證與 code review 抓到、**尚未修**的問題清單，加上**已接受的邊界決定**（知道、刻意不修、寫下理由）。

- 修 bug 前先看本檔，避免重複診斷同一件事。
- 本檔屬 verifier 角色。implementer 修完某條之後，在該條標 `已修` 並附 commit 短 hash，不要直接刪除——刪掉就查不到「這件事發生過」。
- **本檔不放可調數值、不放設計理由**。數值住 `data/tuning.json`；為什麼住 `溫泉小鎮企劃.md`；該做成什麼樣住 `實作規格書.md`；怎麼做住 `開發設計方針.md`。本檔只放「現在跟契約不符的地方」與「怎麼補」。
- 引用程式碼**用函式名不用行號**（行號隨編輯漂移，見 `AGENTS.md > 文件查閱規則`）。

編號規則：`K-nn` 待修、`B-nn` 已接受的邊界決定。編號只增不減，修好的條目留在原位標記狀態。

**每條的第一件事是「要不要做、什麼時候做」，理由排在後面。** K 條目用 `Phase` ＋ `狀態` ＋ `修復時機`；B 條目用 `要不要做` ＋ `兌現時機`。讀的人先要的是行動，不是論證。

⚠️ **`Phase` 是「缺口所在的階段」，不是「什麼時候修」。** 兩者常常不同——K-15 的缺口在 P1-D，但要等 P1-E 的 `choose()` 定案才動它。什麼時候修一律看 `修復時機` 欄位。`狀態` 為 `已修` 時 `修復時機` 不再適用，改看 `備註` 裡的落地紀錄。

**review 講出口的每一條都要當場進本檔，不分大小。** 只在對話裡提過的「小事」會被忘記——`_play_evening()` 自己重寫 beat 篩選這件事在第一輪 review 就看到了，沒進清單，兩輪之後才以 K-14 補記。判斷優先度是 `狀態` 欄位的事，不是決定要不要寫進來的門檻。

---

## 一、待修清單

**還沒修的：**

| # | 標題 | Phase（缺口所在） | 狀態 | **修復時機** |
|---|---|---|---|---|
| K-25 | 走查腳本不判斷「全 `fixed` 時段」也不比對名單，37 個行動格靜靜跳過 | P1-F（測試） | 已修 | **P1-F**（`playthrough_greedy.gd` 動態診斷 90 時段合法性） |
| K-26 | evening 演出有三份實作，走查那份不播殘響 | P1-F | 已修 | **P1-F**（下沉至 `GameState.play_evening` / `play_night_fixed`） |
| K-27 | lint 3 只查 choice 槽沒查比對槽（且判斷單位要改成面板）；`d42_morning_ask_paper` 排錯天、第一輪永遠出不來；`d28_morning_xiaowu` 是第二個活案例 | P1-F（lint）／資料層 | 已修 | **P1-F**（`lint_free_slot_rules` 面板分組檢驗、`d28_morning_xiaowu` 補主角卡槽） |
| K-28 | 第 45 天結局 stub 文字寫上去立刻被蓋掉；夜間 fixed beat 文字沒容器；`flow_text` 建了沒人用 | P1-F | 已修 | **P1-F**（`main.tscn` 整合 `FlowText` 容器，渲染殘響、入夜 fixed 與結局 stub） |
| K-29 | 六條 K 與 B-02／B-03 標了已修，但內文沒更新、沒備註、沒回歸測試 | 文件／測試 | 已修 | **P1-F**（`test_p1d` / `test_p1e` 補齊 K-17、K-18、K-19、K-22、K-27 回歸測試） |
| K-08 | 「（條件不足）」重複住兩個檔 | i18n 管線 | 待修 | **i18n 管線**——等 key 命名定案一起收 |
| K-24 | 規格書狀態表把「不選」寫成 `choices` 的一種值 | 文件 | 待修（低） | **下一次動規格書第三節時**——純措辭 |
| K-30 | K-14 收攏三處之後，夜間新增三處自己比對 `when` | P1-F | 待修（低） | **P3 夜間層真值化**——夜間規則那時要重寫，一起收 |
| K-31 | `verify_data` 把行動格覆蓋檢查跑了兩次 | P1-F | 已修 | **P1-F**（刪除 `verify_data.gd` 內重複的 inline 行動格迴圈） |
| K-32 | 文件寫的 `d3_pm_couple` 在資料裡不存在 | 文件 | 已修 | 2026-08-15 四處全改（比原記的三處多一個 `待決事項.md`） |
| K-33 | 夜間推進一律呼叫 `sleep_night()`，不管玩家是否已選地點 | P1-F | 待修（低） | **P3 夜間層真值化**——夜間流程那時才是真的 |
| K-34 | `available_locations()` 夜間分支不檢查地點級 `requires` | P1-F | 待修（低） | **P3 夜間層真值化**——P1 無影響，收費地點一律 LOCKED |
| K-35 | `PanelBuilder` 夜間收費地點回傳資料裡不存在的假 beat id | P1-F | 待修（低） | **P3 夜間層真值化** |
| K-36 | `test_p1f` 第 10 項是走查腳本的第二份拷貝 | P1-F（測試） | 已修 | **P1-F**（`playthrough_greedy.gd` 抽 `run_greedy_walk` 供 `test_p1f` 共用） |
| K-37 | `map_list.gd` 檔頭仍寫「白天地點清單」 | P1-F | 已修 | **P1-F**（已更新檔頭註解為「地點清單（白天與夜間共用）」） |
| K-38 | `playthrough_greedy.gd.uid` 未進版控（K-23 第二次發生） | P1-F | 已修 | — |
| K-39 | ~~走查數字在同一份程式碼上跑出兩種結果~~ → **誤報**，原因是跨 commit 測量 | P1-F（測試） | 誤報（已釐清） | — |
| K-40 | 夜間演出文字不裁切也不捲動，現在不疊字是因為文字剛好夠短 | P1-F（UI） | 已修 | **P1-F**（`FlowText` 包 `ScrollContainer` 且開啟 `clip_contents`） |
| K-41 | UI runner 負向測試只驗非零退出、先撞錯誤分支，且覆寫正常案例報告 | UI 模擬工具鏈 | 已修 | — |
| K-42 | UI runner 宣稱 47 contracts，但多個 P1-A～F 案例只驗到條目的一部分 | P1-A～P1-F（UI 驗收） | 已修 | — |
| K-43 | 壞 `--data-root` 仍讓子 UI 進 `_ready()`；state 驗證器兩份實作已分歧 | UI 模擬工具鏈／QA CLI | 已修 | — |
| K-44 | `-Case` 單案模式把半個 comparison group 判為通過，並仍回報 47 contracts | UI 模擬工具鏈 | 已修 | — |
| K-45 | `make_states.gd` 直接改 JSON 製造情境；D9 更在驗效果前預先塞入預期知識 | UI 模擬工具鏈（fixture） | 已修 | — |
| K-46 | `run_id` 只有毫秒時間戳，並行 launcher 可寫進同一個 run 目錄 | UI 模擬工具鏈 | 已修 | — |
| K-48 | hover 同步有時序不確定性，同一份程式碼連跑會紅在不同案例 | UI 模擬工具鏈 | **重開（2026-08-16 復發）** | **P2-B 動工前** |
| K-49 | 契約一份報告都沒讀到時，launcher 索引 `$null` 自己爆掉 | UI 模擬工具鏈 | 已修 | — |
| K-50 | 殘留的 Godot 行程會污染下一輪 UI 模擬，讓失敗會傳染 | UI 模擬工具鏈 | 已修 | — |
| K-47 | 進度單一事實來源仍寫 17 variants／其餘 33 待做，與現行 58 variants 不符 | 文件／進度 SSOT | 已修 | — |
| K-51 | P2-A 沒補多實例發狂卡的 UI 驗收；`p1af_29` 的 evidence token 名稱與斷言相反 | P2-A（UI 驗收） | 已修 | — |
| K-52 | `_set_failure_status()` 的優先序改反了，資料寫的 `reject_reason` 被丟掉 | P2-A（順手改到的既有 UI 行為） | 已修 | `259a9f0` |
| K-53 | 45 天走查會開夜間標記了，但對開了什麼零斷言 | P2-A（測試） | 已修 | `259a9f0` |
| K-54 | 縱慾出口一輪只能用一次，主動縱慾上限 4 次（供給 14 張） | P2-B（規則層） | 已修 | 本次修正 |
| K-55 | 泡湯在放置管線中途改 `phase`：推進鈕卡死、效果文字玩家看不到 | P2-B（規則層×UI） | 已修 | `f7c95d9` |
| K-56 | `indulgence.soak_cards_cleared` 是必填 tuning 鍵，但程式一行都沒讀 | P2-B（數值單一事實來源） | 已修 | `f7c95d9` |
| K-57 | `PROJECT_BRIEF.md` 的 P1-H 那一列被整列刪掉 | 文件／進度 SSOT | 已修 | 本次修正 |
| K-58 | 契約數 53 → 54 只改到 5 個地方裡的 2 個 | 文件／進度 SSOT | 已修 | 本次修正 |
| K-59 | 強制縱慾的防吃債沒有測試守著，改回舊寫法 14 套照樣全綠 | P2-C（測試） | 已修 | `test_p2c.gd` 測試 11 補齊出口失敗與槽遺失斷言（含突變驗證） |
| K-60 | 走查認定「強制縱慾吃格」綁在出口有沒有文字，改文案會讓走查假紅 | P2-C（測試） | 已修 | `playthrough_greedy.gd` 改比對推進前後之 `indulgence_count` |
| K-61 | `tests/ui_sim/` 沒有任何 P2-C 契約，操作層一條都沒驗 | P2-C（UI 驗收） | 待修 | **K-48 收掉之後**——不要提前做 |

已修：K-01～K-07、K-09～K-23、K-25～K-29、K-31、K-36～K-38、K-40～K-47、K-49～K-60（各條的 `備註` 有落地位置與回歸測試）。**K-48 已於 2026-08-16 重開**。**K-61 待修**（等 K-48 結案）。

> `Phase` 欄是缺口長在哪個階段，**不是什麼時候修**。K-15 的缺口在 P1-D，但要等 P1-E 才動——兩欄不同是常態。

### K-01 白天面板的 `on_enter` 結算住在 UI 層

- **Phase**：P1-C（缺口所在）／P1-F（爆點）
- **狀態**：已修
- **問題**：白天面板的 beat 呈現只有 `scenes/ui/location_panel.gd` 的 `_rebuild()` 會呼叫 `GameState.enter_beat()`。`GameState` 與 `PanelBuilder` 都沒有等價入口。而 `開發設計方針.md > P1-F` 明訂走查腳本「不 instantiate 任何 UI，只呼叫 `PanelBuilder.available_locations()` 與 `GameState.try_place()`」——那條路上沒有人呼叫 `enter_beat`。結果是白天 37 個 `fixed` beat（其中 28 個帶 `on_enter`）的效果在 headless 走查中一次都不會結算，`beats_entered` 也不會寫入。違反 `實作規格書.md > 四`：「beat 的呈現管道只有一個入口……任何管道播出 beat 而沒結算 `on_enter`＝實作錯誤」。
- **影響**：P1-F 的貪心走查會安靜地跳過那 28 個 `on_enter`。`d17_morning_phone`、`d23_morning_awei_knocks` 的 `on_enter.gain` 發不出卡，後續 `requires` 判不過，第二章起連鎖鎖死。症狀看起來像資料錯誤，不像引擎錯誤——這是它難查的原因。
- **解法**：把「開面板＝結算該面板全部 OPEN beat 的 `on_enter`」下沉成規則層入口，例如 `GameState.open_panel(location_id) -> Dictionary`：內部先對 OPEN 的 beat 逐一 `enter_beat()`，再回傳 `PanelBuilder.build()` 的結果。`location_panel` 與 `playthrough_greedy.gd` 共用它，UI 不再自己呼叫 `enter_beat`。原則同 `開發設計方針.md > P1-F` 的 `end_run()` 那條：規則層的後置條件不可依賴外部呼叫者。
- **備註**：已實作 `GameState.open_panel()` 並於 `tests/headless/test_p1c_bugfix.gd`（A3）完成規則層驗證。

### K-02 面板 view model 在 `on_enter` 結算之前算好，同面板不重求值

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_rebuild()` 先呼叫 `PanelBuilder.build()` 取得 view model，之後才逐一 `enter_beat()`。`on_enter` 寫的 flag、發的卡，這一輪的 view model 看不到，而且不重算。違反 `實作規格書.md > 五`：「同面板內任何狀態變化（放卡、**效果結算**）後全面重求值」。
- **影響**：資料裡有一個活的實例——第 23 天上午、山泉閣。`d23_morning_awei_knocks`（`fixed`）的 `on_enter` 寫 `awei_sheltering: true`；同一面板的 `d23_am_settle_grandma` 的 `condition` 正是 `{"flag": "awei_sheltering"}`。第一次打開面板時「安置阿嬤」判定 HIDDEN、整個不出現，玩家要退出面板再進來才看得到。不是死鎖，是隱形步驟。
- **解法**：與 K-01 同一個修法——先結算再 build。K-01 修好本條即消失，不必分兩次改。
- **備註**：`GameState.open_panel()` 採先連鎖結算 `enter_beat` 後呼叫 `PanelBuilder.build()`，第 23 天上午山泉閣回歸測試已在 `test_p1c_bugfix.gd`（A3）通過。

### K-03 `try_place` 的 `reason` 混了機器碼與玩家文案，而 UI 直接顯示

- **Phase**：P1-D
- **狀態**：已修
- **問題**：`GameState.try_place()` 大部分失敗回機器碼（`not_held` / `hidden` / `resolved` / `not_accepted` / `action_spent` / `unknown_beat` / `unknown_slot`，`tests/headless/test_p1d.gd` 已把它們當契約斷言），但 beat 級與槽級 LOCKED 兩條回的是資料裡的 `reject_reason`（中文玩家文案）。同一個欄位兩種語意，消費端分不出來。`location_panel.gd` 的 `_on_place_pressed()` 直接把它塞進「（無法放置：%s）」。
- **影響**：玩家在中文遊戲裡會看到「（無法放置：action_spent）」。也踩到 i18n 紀律（`chore(i18n)` commit `e33b742` 定的「玩家可見字串住檔頭 const」）——這些碼會直接漏到畫面上，且無法翻譯。
- **解法**：`try_place` 改回 `{ ok, reason, reason_code, reason_text, lines }`。`reason_code` 維持現有機器碼（`test_p1d.gd` 的契約不動）；`reason_text` 只在 LOCKED 兩條帶資料的 `reject_reason`，其餘留空。UI 用檔頭 const 表把 `reason_code` 翻成玩家文案，`reason_text` 非空時優先顯示它。
- **備註**：`try_place` 回傳結構已補齊 `reason_code` 與 `reason_text`，UI 建立檔頭 `_REASON_CODE_TEXTS`，並於 `test_p1c_bugfix.gd`（A4）完成驗證。

### K-04 `accepts` 比對邏輯出現在 UI

- **Phase**：P1-D
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_matching_held_cards()` 自己做 `accepts.has(base_id) or accepts.has(card_type)`，跟 `try_place` 步驟③重複一份。違反 `開發設計方針.md > 全域結構決策`：「UI 內出現任何 accepts／三態／`action_spent` 判斷＝架構錯誤」。
- **影響**：兩份 `accepts` 語意會各自漂移。順帶症狀：`_matching_held_cards()` 不看 `action_spent`，所以行動格用掉之後「放入：主角卡」按鈕還在，按下去才被規則層退回。
- **解法**：規則層開 `GameState.placeable_cards(beat_id, slot_id) -> Array[String]`，內部沿用 `try_place` 步驟①③④同一套判斷；UI 只負責列舉回傳值。順帶讓已消耗的行動格不再列出主角卡。
- **備註**：`GameState.placeable_cards()` 已實作，UI 的 `_matching_held_cards()` 已移除，並於 `test_p1c_bugfix.gd`（A4）驗證 `action_spent` 狀態下的過濾效果。

### K-05 多鍵 `condition` 只有第一個運算子生效，lint 抓不到

- **Phase**：P1-D（lint 1）
- **狀態**：已修
- **問題**：`ConditionEval.eval()` 是 `if d.has(<運算子>)` 逐條 `return` 的結構，`{"day": 5, "flag": "x"}` 只會求值 `day`，`flag` 靜默忽略。`DataLoader._lint_condition()` 逐鍵檢查名稱是否在封閉語彙內，但不檢查「恰好一個運算子鍵」。
- **影響**：目前資料 **0 個多鍵 condition**，所以現在不影響玩法。但這是靜默錯誤——資料端寫了會過 lint、過測試，行為卻不是作者想的。內容期資料量上去之後遲早會踩到。
- **解法**：`lint_vocabulary()` 補一條：`condition` / `requires` 的 Dictionary 必須恰好一個運算子鍵，多於一個即報錯擋開機（要 AND 就寫 `all`）。成本低，擋掉一整類錯誤。
- **備註**：`DataLoader._lint_condition()` 已補多鍵／空 Dictionary 檢查，並於 `tests/headless/test_p1d.gd` 完成驗證。

### K-06 四個 beat 有 `requires` 卻沒有 `reject_reason`

- **Phase**：資料層
- **狀態**：已修
- **問題**：lint 2 每次開機都會警告這四個：`n_ahong_3_ch1`、`d19_pm_upstream`、`d37_back_wing`、`d37_look_for_her`。`實作規格書.md > 五` 定義「灰掉而沒寫 `reject_reason`＝資料錯誤」。
- **影響**：A2 修好之後，beat 級 LOCKED 會把內部槽整組降為 LOCKED 並沿用 beat 的理由——這四個因此顯示通用文案「（條件不足）」。`d37_back_wing` 與 `d37_look_for_her` 正是第 37 天那組互斥證據卡，玩家在關鍵分歧點只會看到一句不指涉任何東西的通用字。
- **解法**：補四條 `reject_reason`，文案照資料層規約（指缺的東西、不指答案，見 `實作規格書.md > 五`）。純資料改動，不動引擎。
- **備註**：四條 `reject_reason` 已補齊，`DataLoader.lint_missing_reject_reason()` 在真實 252 個 beat 上的警告歸零，並於 `tests/headless/test_p1d.gd` 完成驗證。

### K-07 tuning 數值在程式裡留了硬編碼 fallback

- **Phase**：P1-B／P1-D 遺留
- **狀態**：已修
- **問題**：`GameState.gain_card()` 的 `Data.tuning("madness_countdown_days", 7)`、`GameState._check_hand_overflow()` 與 `scenes/ui/hand_bar.gd` 的 `Data.tuning("hand_size", 14)`。三處把可調數值複製了一份進程式。
- **影響**：`data/tuning.json` 的鍵被改名時，遊戲不會報錯，會靜默沿用程式裡那份舊數字。這個 prototype 的整個玩法就是一邊玩一邊改數值，靜默失效比崩潰更貴。
- **解法**：`Data.load_data()` 補一條必要 tuning 鍵存在性檢查（缺鍵→`ok = false`），三處 fallback 隨之拿掉。掛在既有的 lint 1／2 旁邊，同一個地方。
- **備註**：`Data.REQUIRED_TUNING_KEYS` 已加入開機檢查，程式內硬編碼 fallback 已移除，並於 `tests/headless/test_game_state_p1a.gd` 完成驗證。

### K-08 「（條件不足）」重複住兩個檔

- **Phase**：i18n 管線
- **狀態**：待修
- **修復時機**：**i18n 管線**。等 key 命名規約定案再收，現在挑位置會挑錯。
- **問題**：`PanelBuilder` 的 `_REASON_FALLBACK` 與 `GameState` 的 `_REASON_LOCKED_FALLBACK` 是同一句玩家可見文字的兩份拷貝。
- **影響**：i18n 抽取時會產生兩個 key，翻譯改一個漏一個。
- **解法**：收成一處常數，兩邊引用。實際位置等 i18n 管線定 key 命名時一起處理，不必現在挑地方。

### K-09 `enter_beat` 先寫 `beats_entered` 再驗 beat 是否存在

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`GameState.enter_beat()` 在確認 `beats_by_id` 有這個 id 之前就寫入 `beats_entered[beat_id] = true`。
- **影響**：未知 beat id 會留在集合裡，而 `beats_entered` 屬 run 層、會被 `serialize()` 寫進存檔。
- **解法**：把寫入移到存在性檢查之後。三行。
- **備註**：已在 `GameState.enter_beat()` 最前方加入存在性檢查，並於 `tests/headless/test_p1c_bugfix.gd`（A5）完成驗證。

### K-10 `core/` 三個 class 的簽名在方針與實作不一致

- **Phase**：文件（`開發設計方針.md`，implementer-owned）
- **狀態**：已修
- **問題**：方針寫 `ConditionEval.eval(cond, gs: GameState)`、`PanelBuilder.build(location_id, gs: GameState, data: Data)`、`EffectApply.apply(effect: Dictionary, gs: GameState)`；實作是 `gs: Node` / `data: Node` / `effect: Variant`。
- **影響**：下一個人會照方針寫，然後撞牆——`GameState` 是 autoload 名，在 GDScript 裡是值不是型別，而 `game_state.gd` 也不能加同名 `class_name`（Godot 報 autoload 名稱衝突）。方針要求的那個標註**寫不出來**。
- **解法**：改方針三處簽名為實作現況，並加一句寫明為什麼不能標成 `GameState`，避免同一面牆被撞第二次。
- **備註**：`開發設計方針.md` 已修正三處簽名，並註明 GDScript Autoload 為全域值而非型別名。

### K-11 P1-A 序列化測試在失敗路徑存取已釋放物件

- **Phase**：P1-A（測試）
- **狀態**：已修
- **問題**：`tests/headless/test_game_state_p1a.gd` 的 `_test_serialize_roundtrip()` 先 `gs.free()`，失敗訊息才用 `gs.get(...)` 取值。
- **影響**：只在測試失敗時發作——正好是最需要那則訊息的時候，拿到的會是錯誤或空值。
- **解法**：把 `gs.free()` 移到組完訊息之後。一行。
- **備註**：`test_game_state_p1a.gd` 已調整先保存屬性值再 free 物件。

### K-12 LOCKED beat 底下的 RESOLVED 槽被降級為 LOCKED

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`PanelBuilder.build()` 在 beat 為 LOCKED 時把全部槽降為 LOCKED，包含已經放過卡、原本應該是 RESOLVED 的槽。
- **影響**：只有在「beat 曾經 OPEN、玩家放過卡、之後 `requires` 又變成不成立」時才會出現，第一輪資料未必碰得到。碰到時玩家會看到已放過的槽變回灰槽，狀態顯示不實。
- **解法**：降級時跳過 `tri == RESOLVED` 的槽。RESOLVED 本來就不可互動，不需要靠 LOCKED 擋。
- **備註**：`PanelBuilder.build()` 已加入降級跳過 `RESOLVED` 檢查，並於 `tests/headless/test_p1c_bugfix.gd`（A6）完成驗證。

### K-13 `on_enter.text` 在白天面板不再顯示（K-01 的迴歸）

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`GameState.open_panel()` 呼叫 `enter_beat(beat_id)` 但**丟掉回傳值**，而 `location_panel.gd` 的 `_rebuild()` 只渲染 view model 裡的 `beat.text`。`on_enter.text` 因此在白天面板這條管道上靜默失效。`實作規格書.md > 十四` 定義該鍵的語意是「`text` **呈現**」，`EffectApply.apply()` 也正是為了把這些行交出去才設計成有回傳值。
- **影響**：目前白天 beat 只有一個帶 `on_enter.text`——`d15_pm_clinic`（第 15 天下午、`fixed`）。它的 top-level `text` 照常顯示，所以畫面不會空白，只少一段 24 字的首次敘述。真正的問題是**管道壞了**：之後任何白天 beat 加 `on_enter.text` 都會安靜地不播。`main.gd._play_evening()` 仍然用回傳行，所以 evening 正常——**兩條管道又不一致，跟 K-01 本身同一個病**。
- **解法**：`open_panel()` 把 `enter_beat()` 的回傳行留下來，掛回對應 beat 的 view（例如每個 beat view 加 `lines: PackedStringArray`）；`location_panel` 在 beat 主文之後接著播。順帶讓走查腳本也拿得到這些行。
- **備註**：`GameState.open_panel()` 已保留回傳行掛於 view model 的 `lines`，`location_panel.gd` 已改為渲染 `lines`，並於 `tests/headless/test_p1c_bugfix.gd`（A7，以 `d15_pm_clinic`）驗收通過。

### K-14 beat 篩選邏輯散在三處，而 `DataLoader.beats_at()` 沒人用

- **Phase**：P1-C／P1-D（P1-F 做夜間面板時會變第四份）
- **狀態**：已修
- **問題**：同一組「`when.day` 與 `when.phase` 對得上當前時間」的篩選各自重寫了三份：`PanelBuilder.build()`、`GameState.open_panel()`、`main.gd._play_evening()`（外加 `fixed` 條件）。而 `DataLoader.beats_at(day, phase)` 早就提供這件事，目前只有 `verify_data.gd` 一個呼叫者。
- **影響**：篩選規則要動的時候有三個地方要改，漏一個就是兩條管道行為不一致——K-01、K-13 都屬於這個家族。現在沒有已知的行為差異，是**維護風險不是 bug**。
- **解法**：三處改走 `DataLoader.beats_at()`（`open_panel` 與 `build` 再各自加地點過濾，`_play_evening` 再加 `fixed` 過濾）。P1-F 動夜間面板時順手做，不必單獨開一次。
- **備註**：commit `55d62a8`。本條列的三處都收攏了——`PanelBuilder.build()` 白天分支改走 `beats_at()`，`GameState.open_panel()` 改為委派 `PanelBuilder.build()` 不再自己掃，`main.gd._play_evening()` 的 fixed beat 那段改走 `beats_at()`。**但同一個 commit 新增的夜間程式又自己比對 `when`，三處**——那是新的缺口，記在 K-30，不留在本條。

### K-15 `try_place` 的舊 `reason` 欄位只剩測試在讀

- **Phase**：P1-D
- **狀態**：已修
- **問題**：K-03 補上 `reason_code` / `reason_text` 之後保留了舊的 `reason` 欄位向下相容。現在正式程式碼（`location_panel.gd`）只讀新的兩個；舊 `reason` 只剩 `test_p1d.gd`（7 處）與 `test_p1c_bugfix.gd`（1 處）在斷言。
- **影響**：`reason` 保留著 K-03 原本要消滅的混合語意（大部分回機器碼、LOCKED 兩條回玩家文案）。留著不會壞，但沒有人排它的退場時間，就會一直留著，而下一個人可能會照它寫新程式。
- **解法**：把 `test_p1d.gd` 那 8 處斷言改讀 `reason_code`，然後從 `try_place` 與 `choose` 的回傳一起移除 `reason`。純機械改動。
- **備註**：commit `1a67b17`。`GameState.try_place()` 與 `choose()` 已移除 `reason` 回傳鍵，`location_panel.gd` 移除 fallback，`tests/headless/test_p1d.gd` 與 `test_p1c_bugfix.gd` 已改為讀取 `reason_code` 與 `reason_text`，八套 headless 測試全綠。`開發設計方針.md > P1-E` 同時補上 `choose()` 的回傳形狀。

### K-16 三個行動格只剩 choice 槽，沒有主角卡可放

- **Phase**：資料層（缺口所在）／P1-E（讓它第一次有後果）／P1-F（走查會撞到）
- **狀態**：已修
- **決定（2026-08-14）**：**這三格維持免費，不補主角卡槽。** 內容期補故事線時，那幾天自然會長出可放主角卡的地方。**成立條件**：新槽要加在**同一個地點的同一個時段**。加在別的地點不算數——那三格照樣沒東西可放，這一條也不會自己消失。
- **問題**：三個行動格只有 choice 槽，玩家找不到地方放主角卡：第 35 天下午（`d35_pm_answer`，clinic）、第 40 天上午（`d40_tell_someone`，sanquan）、第 43 天下午（`d43_pm_zhou` 在 riverside、`d43_conclusion` 在 sanquan）。其中違反 `data/SCHEMA.md > choice_group` 同面板規約的是三個 **beat**：`d35_pm_answer`、`d40_tell_someone`、`d43_conclusion`。**`d43_pm_zhou` 不違規**——它是 `fixed`，SCHEMA 明文允許。兩份名單長度一樣但內容不同（三個**時段** vs 三個 **beat**），引用時不要混。
- **影響**：不影響玩法——玩家按推進鈕照樣過去，不會卡住。剩兩件事要處理。① P1-F 的貪心走查在這三格找不到可放主角卡的槽，而現有 `by_design` 名單只有三筆（第 1 天上午下午、第 32 天下午），走查會撞上名單外的空格。② lint 3 若照 SCHEMA 嚴格擋，開機會被這三個 beat 擋下來——而我們剛決定接受它們。
- **解法**：三件事，全部在 P1-F 做。
  1. **兩份名單分開，不要塞進同一個常數。** `verify_data.gd` 現有的 `by_design` 語意是「該時段**沒有任何 beat**」（檢查用 `beats_at().is_empty()`）。這三格**有 beat**，混進去會讓那三天之後真的變空也沒人發現。第一份維持原樣（第 1 天上午下午、第 32 天下午），第二份新開，語意是「有 beat、無主角卡槽」，收這三個時段，給 `playthrough_greedy.gd` 讀。兩份都住 `data_facts.gd`（見 B-03）。
  2. **lint 3 做成 warning，不做 error**，豁免名單沿用第二份。理由：這條規約在內容期會反覆進出「暫時違反」的狀態，做成 error 會變成每次寫新故事線都要先關掉的東西。
  3. `verify_data.gd` 的行動格覆蓋檢查**維持原樣**（只看該時段有沒有 beat）。不要順手擴充成「有沒有主角卡槽」——那會跟第二份名單重複同一份判斷，變成兩個地方要同步。
- **備註**：commit `55d62a8`。三件事都做了——`scripts/core/data_facts.gd` 建立，兩份名單分開（`BY_DESIGN_EMPTY_PHASES` 三筆、`BY_DESIGN_CHOICE_ONLY_PHASES` 三筆），`DataLoader.lint_choice_rules()` 對豁免名單內的 beat 出 warning、名單外出 error，`verify_data` 實測「0 錯誤（3 筆已豁免警告）」。**兩個後續缺口另立條目**：走查腳本沒有讀那份名單（K-25）；lint 只查了 choice 槽，沒查比對槽，因此漏掉 `d42_morning_ask_paper`（K-27）。
- **2026-08-14 後續查證**：當初把「沒東西花行動格」的時段估成 24 格是錯的。逐格比對之後，其中 **23 格整個時段只有 `fixed` beat**，而「`fixed` 不吃行動格」是四份文件都寫過的既有規則（`data/SCHEMA.md > slots[]` 的 `fixed` 欄、`實作規格書.md > 五`、`溫泉小鎮企劃.md` 開關 6 那節點名第 12 天上午拿藥、第 15 天下午診所、第 16 天上午、`subdocs/故事線/故事線_第一輪_第一章.md`）。那 23 格是設計，不需要名單也不需要決策。真正沒被涵蓋的只有第 42 天上午一格（K-27）。**`d29_pm_invitation` 也不需要豁免**——它是 `fixed`，依同一條規則本來就不吃行動格。

### K-17 `try_place` 帶空卡 id 可以直接完成選擇

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`GameState.try_place()` 在 ①持有檢查**之前**就轉導 `choose()`（槽有 `choice_group` 時），而 `choose()` 只在 `card_id` 非空時檢查持有。`try_place("", beat_id, choice_slot_id)` 因此會成功結算。
- **影響**：`try_place` 的檔頭註解寫「①持有 → ②三態 → ③accepts → ④action_spent，順序固定」，轉導那行破壞了自己宣告的後置條件。目前沒有呼叫者傳空字串（UI 的按鈕 id 一律來自 `placeable_cards()`，走查腳本傳主角卡），所以不影響玩法。
- **解法**：轉導前先擋——`card_id` 為空時回 `not_held`，不轉導。空卡 id 走 `try_place` 本來就沒有語意；要直選就直接呼叫 `choose()`。
- **備註**：commit `55d62a8`。`try_place()` 函式最前面加了空字串檢查，回 `not_held`，在 beat 查表之前。**沒有回歸測試**（K-29）。

### K-18 `choose()` 與 `try_place()` 都不檢查 `when.day` / `when.phase`

- **Phase**：P1-D（`try_place`）／P1-E（`choose` 複製了第二份）
- **狀態**：已修
- **問題**：兩個入口只驗 beat 與槽的 `condition` / `requires`，不驗這個 beat 是否屬於當前天與時段。第 1 天呼叫 `choose("d29_pm_invitation", "invitation", "invite_none")` 會成功結算。
- **影響**：`try_place` 自己的註解寫「UI 是零規則顯示層，規則層要能獨立擋下非法呼叫」，這一條沒做到。目前無可觸發路徑：UI 只渲染 `PanelBuilder.build()` 過濾後的槽，走查腳本也照面板走。屬維護風險，不是現行 bug。
- **解法**：K-14 把篩選收進 `DataLoader.beats_at()` 之後，兩個入口各加一次「這個 beat 在不在當前 (day, phase)」的檢查。夜間 beat 與 `fixed` beat 的 `when` 形狀不同，收攏時一起處理。
- **備註**：commit `55d62a8`。新增 `GameState._is_beat_time_valid()`，`try_place()` 與 `choose()` 各呼叫一次，不過就回 `hidden`。夜間分三種形狀處理（定日夜 beat 比對 `when`、章節變體比對 `chapter` ≤ 當前章、附加 beat 一律通過）。**沒有回歸測試**（K-29）；它自己也是 K-30 列的第三處 `when` 比對。

### K-19 `location_panel` 自己判斷 choice 槽，帶卡那條分支多餘

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_rebuild()` 讀原始資料的 `slot["choice_group"]` 算出 `is_choice`，再決定按鈕接 `_on_choose_pressed` 還是 `_on_place_pressed`。其中帶卡按鈕那條是多餘的：`try_place()` 已經轉導 `choose()`，兩條路的結果完全相同。
- **影響**：`開發設計方針.md > 全域結構決策` 寫「UI 內出現任何 accepts／三態／`action_spent` 判斷＝架構錯誤」。這是第四種判斷，字面上沒被列到、精神上是同一件事（K-04 同族）。直選按鈕那條**不能**拿掉——手上沒有卡就沒有別的入口——所以問題不是「UI 不該知道這是選擇題」，是「UI 不該自己從原始資料算出來」。
- **解法**：帶卡按鈕改回接 `_on_place_pressed`，靠 `try_place` 的轉導；`is_choice` 由 `PanelBuilder.build()` 算好放進 slot view（跟 `tri`、`reason` 並列），UI 只讀 view model。
- **備註**：commit `55d62a8`。兩件都照解法做了——`PanelBuilder.build()` 的 slot view 多一個 `is_choice` 鍵，`location_panel.gd` 改讀它；帶卡按鈕改接 `_on_place_pressed`。`test_p1e.gd` 的 9b（K-20 加的）驗的是行為不是接線，所以這次換路之後仍然通過，等於順帶驗了本條。

### K-20 UI 的帶卡選擇入口沒有斷言

- **Phase**：P1-E（測試）
- **狀態**：已修
- **問題**：`tests/headless/test_p1e.gd` 的 `_test_choice_ui_buttons()` 只找出並點擊直選按鈕（「選擇：① 他走路的方式」）。「放入：拍立得」那一顆從沒被按過。
- **影響**：`測試指南.md > P1-E` 明寫兩個入口都要通，`開發設計方針.md > 跨 Phase 實作紀律` 也寫「新增規則層入口時，UI 路徑與走查路徑各要有一條斷言」。
- **解法**：測試 9 補一次帶卡按鈕的點擊，驗 `choices` 與 `slots_placed` 的結果與直選相同。
- **備註**：commit `1a67b17`。`tests/headless/test_p1e.gd` 測試 9 已分拆為 9a（直選）與 9b（帶卡按鈕「放入：夫妻留下的拍立得」點擊），確認點擊後正確寫入 `choices`、`slots_placed` 並獲得效果卡片。9b 驗的是行為不是接線，所以 K-19 把那顆按鈕改走 `try_place` 轉導之後，這條斷言仍然成立。

### K-21 直選／帶卡的狀態等價性只比六個欄位

- **Phase**：P1-E（測試）
- **狀態**：已修
- **問題**：`_test_choice_state_equivalence()` 逐欄比 `choices`、`slots_placed`、`flags`、`switches`、`relations`、`action_spent`。漏掉 `beats_entered`、`switch_progress`、`npc_action_counts`、`madness_clock`、`_madness_counter`，以及整個 `meta`（知識集合）。
- **影響**：驗收原文是「除卡片消耗外**完全一致**」。逐欄列舉的寫法，之後 `serialize()` 每加一個欄位就漏驗一個。
- **解法**：兩邊的 `run` 各 `erase("hand")` 之後整包比對，`meta` 也比。比逐欄列舉短，而且不會隨欄位增加而過期。
- **備註**：commit `1a67b17`。`tests/headless/test_p1e.gd` 測試 3 已改為比對整包 `run`（移除 `hand` 後）與整包 `meta`，全域狀態欄位比對完整。之後 `serialize()` 新增欄位會自動進入比對範圍，不必回頭補。

### K-22 `placeable_cards()` 對 choice 槽仍套 `action_spent` 過濾

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`placeable_cards()` 對主角卡套 `action_spent` 過濾（K-04 加的，對一般槽正確）。`choose()` 不檢查 `action_spent`（對 choice 槽正確，SCHEMA 寫明不吃格）。兩者對同一個槽會給出不同答案。
- **影響**：只在「choice 槽的 `accepts` 含 `protagonist`」時發作——UI 會在行動格用掉之後藏起按鈕，規則層卻放行。目前全部 choice 槽的 `accepts` 不是缺席就是情報卡／NPC 卡，沒有一個含 `protagonist`，所以現在不發作。
- **解法**：P1-F 寫 lint 3 時多加一條：choice 槽的 `accepts` 不得含 `protagonist`。SCHEMA 已規定 choice 不吃格，而主角卡放進不吃格的槽沒有語意。擋在資料端之後，`placeable_cards()` 與 `choose()` 都不必改。
- **備註**：commit `55d62a8`。`DataLoader.lint_choice_rules()` 對「choice 槽的 `accepts` 含 `protagonist`」出 error，`placeable_cards()` 與 `choose()` 都沒動，照解法做。**這條 error 分支從來沒有被執行過**——真實資料沒有違規案例，測試也沒有餵合成 beat 進去（K-29）。

### K-23 `test_p1e.gd.uid` 未進版控

- **Phase**：P1-E
- **狀態**：已修
- **問題**：其他七個測試腳本的 `.uid` 都在 repo 裡，`tests/headless/test_p1e.gd.uid` 沒有跟著 `5c681f5` 進來，要等下一次 `--import` 才生成。
- **影響**：每個人 clone 之後第一次 import 都會多出一個未追蹤檔，而且不同機器可能為同一個腳本產生不同 uid。
- **備註**：本次 commit 一併補進版控。

### K-24 規格書狀態表把「不選」寫成 `choices` 的一種值

- **Phase**：文件（`實作規格書.md`）
- **狀態**：待修（低）
- **修復時機**：**下一次動規格書第三節時**。純措辭，不影響實作。
- **問題**：`實作規格書.md > 三` 的狀態表寫「選擇：已結算的 `choice_group`（組 id → 選了哪個槽**或「不選」**）」。`開發設計方針.md > P1-E` 寫「不選＝不寫 `choices`」，實作照方針做。兩份文件對同一個欄位的形狀講法不同。
- **影響**：照規格書那句寫的人，會去找一個代表「不選」的哨兵值。實際上沒有，也不需要——`d29_pm_invitation` 的「不邀」是資料明寫的組員（`invite_none`）。看得見的「不選」由資料承擔，不由引擎。
- **解法**：規格書那半句改成「組 id → 選了哪個槽；未寫入＝不選」。`實作規格書.md` 不屬 verifier 角色，本條只列建議、不直接改。

### K-25 走查腳本不判斷「全 `fixed` 時段」也不比對名單，37 個行動格靜靜跳過

- **Phase**：P1-F（測試）
- **狀態**：已修
- **問題**：`tests/headless/playthrough_greedy.gd` 第 11 行 `preload` 了 `DataFacts`，然後一次都沒有呼叫。`DataFacts.is_choice_only_phase_by_design()` 全專案零呼叫者。`_execute_action_phase()` 放不下就回 0，呼叫端只把它加進總數，不比對、不報告、不失敗。而 `開發設計方針.md > P1-F` 寫的是「找不到就跳過該格（**比對 `DataFacts` 名單**）」，`測試指南.md > P1-F` 寫的是「exit 0（第 1 天上午下午、第 32 天下午允許空格，名單同 `by_design`）」。
- **影響**：實測 90 個行動格只用掉 53，37 格空的，而測試全綠。拆開來看：**30 格在資料層就沒有主角卡槽**（3 格完全沒 beat ＋ 3 格只有 choice 槽 ＋ 23 格整個時段只有 `fixed` beat ＋ 第 42 天上午，見 K-27），另 **7 格是條件沒過**。名單只涵蓋 6 格。現在沒有人知道哪 37 格是空的，也沒有人會發現其中哪一格是因為 bug 才變空的——這是 K-01 同一種病，綠燈涵蓋不到的路徑。
- **解法**：**不要手寫名單。** 那 23 格是 `fixed` 敘事時段，而「`fixed` 不吃行動格」是既有規則（見 K-16 的後續查證），從資料就推得出來。走查腳本改成：放不下時，先看該時段是否①在 `BY_DESIGN_EMPTY_PHASES`、②在 `BY_DESIGN_CHOICE_ONLY_PHASES`、③該時段所有 beat 都是 `fixed`。三者皆非就是錯誤，印出天數時段並 exit 1。順帶把每天用掉幾格印出來，讓走查的輸出能被人讀。
- **備註**：`playthrough_greedy.gd` 已實作 `_diagnose_unplaced_phase()`，未放卡時動態從資料與狀態推導合法分類（刻意留空、刻意 choice-only、全 fixed 敘事時段、條件分支未解鎖、前置門檻未達成、純比對槽），並加入無 beat 且不在留空名單即判 ok: false 的防呆。走查過程逐日印出上午／下午行動摘要，並於收尾輸出 90 個時段的完整統計分類表，全部 90 個行動時段透明診斷通過。

### K-26 evening 演出有三份實作，走查那份不播殘響

- **Phase**：P1-F
- **狀態**：已修
- **問題**：同一件事寫了三份，而且三份內容都不一樣：`scenes/main.gd._play_evening()`（fixed beat ＋ 殘響）、`tests/headless/playthrough_greedy.gd._execute_evening_phase()`（**只有 fixed beat，沒有殘響**）、`tests/headless/test_p1f.gd._collect_evening_echoes()`（**只有殘響**）。夜間同病兩份：`main.gd._play_night_fixed()` 與 `playthrough_greedy.gd._execute_night_phase()`。
- **影響**：兩件事。① 45 天貪心走查從頭到尾一條殘響都沒播過，而驗收要求「錯過的 beat 的 echo 在正確的天播出」。② 驗殘響的測試驗的是它自己那份拷貝——`main.gd._play_evening()` 整個壞掉，十套測試仍然全綠。`開發設計方針.md > 跨 Phase 實作紀律` 對這件事寫得很明白：「凡是 UI 與 headless 走查都會用到的動作，本檔要明寫入口住規則層」，並且拿 `end_run()` 當範例。晚間演出正是這種動作，而它住在 UI 與測試裡。
- **解法**：下沉成 `GameState.play_evening() -> PackedStringArray`，內部固定順序（先當日 evening `fixed` beat 依陣列順序 `enter_beat()`，再掃 `echo.day == day` 的殘響），回傳要播的行。`main.gd`、走查腳本、`test_p1f` 三個呼叫端共用。夜間同樣下沉成 `GameState.play_night_fixed()`。
- **備註**：已於 `GameState` 實作 `play_evening()` 與 `play_night_fixed()`。`main.gd`、`playthrough_greedy.gd` 與 `test_p1f.gd` 統一委派至這兩個規則層入口，刪除測試內的自製殘響拷貝，並於 `test_p1f.gd`（測試 1、2、3、10）完成全綠驗收。

### K-27 lint 3 只查 choice 槽，沒查比對槽；`d42_morning_ask_paper` 與 `d28_morning_xiaowu` 是活的違規

- **Phase**：P1-F（lint）／資料層
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**，跟 K-25 同一次收——兩者都在算「哪些行動格合法地沒事可做」。資料的天數錯誤可以更早改，它不依賴任何引擎工作。
- **問題**：規約的原文涵蓋**所有**不收主角卡的槽，不只選擇題。`實作規格書.md > 六`：「『不收主角卡的槽必須跟收主角卡的槽同面板』是**資料規約**（`SCHEMA.md` 已載）；引擎不強制，**驗證腳本要 lint**」；`data/SCHEMA.md` 對比對槽也寫「因為不收主角卡，所以**不吃行動格**……成本是**你人得在那裡**」。而 `DataLoader.lint_choice_rules()` 只在槽有 `choice_group` 時才計入，比對槽那一半沒做。
- **影響**：資料裡有一個活的違規案例——`d42_morning_ask_paper`（第 42 天上午、寺廟）：**非 `fixed`**，兩個槽都只收 `item_old_paper`（比對槽），沒有 `choice_group`，而且是該時段唯一的 beat。玩家那個上午沒有地方放主角卡，而 lint 3 因為它沒有 `choice_group` 完全看不到它。它同時是 K-25 那 30 格裡唯一沒被任何規則涵蓋的一格。
- **解法**：兩件事。① `lint_choice_rules()` 的判斷條件從「有 `choice_group` 的槽」放寬成「不收 `protagonist` 的槽」，函式改名為 `lint_free_slot_rules()` 較貼切。**照這句的字面寫會誤報，判斷單位要一起改成面板——見下方「動工前定案」。** ② `d42_morning_ask_paper` 的天數要改（見下方「更嚴重的第二層」與「決定」）。
- **備註**：已實作 `DataLoader.lint_free_slot_rules()`（以面板為單位檢查主角卡槽與 choice accepts 規約），並修正 `d28_morning_xiaowu` 補上主角卡槽。`verify_data` 實測 4 筆已豁免警告、0 錯誤；`test_p1d.gd` 補齊合成違規 beat 回歸測試全綠。

#### 更嚴重的第二層：這個 beat 第一輪永遠不會出現（2026-08-14 查證）

本條原本只記 lint 少查一種槽。查故事線之後發現底下壓著一個更嚴重的資料錯誤，**嚴重度因此從中拉到高**。

- **時間對不上**：`d42_morning_ask_paper` 的 `condition` 是 `{ "has_card": "item_old_paper" }`，掛在第 42 天**上午**。而 `item_old_paper` 全作只有一個來源——`d42_pm_room`（第 42 天**下午**、`oldhouse_room`、`search` 槽 `requires` 有 `item_room_key`）。上午早於下午，所以那張紙在第 42 天上午不可能在手上。**這個 beat 的出現條件永遠不成立。**
- **後果三件**：① 第 42 天上午的面板永遠是空的（那是該時段唯一的 beat）。② 金伯解釋拜契、叔叔含糊帶過這兩段第三章關鍵資訊，第一輪玩不到。③ 現有檢查全部抓不到——`verify_data` 的行動格覆蓋檢查只看「該時段有沒有 beat」，不求值 `condition`，所以是綠的。
- **故事線寫的順序本來就是對的**（`subdocs/故事線/故事線_第一輪_第三章.md > 第 42 天——那間房`）：先在下午打開那間房拿到紙，**然後**「拿去問金伯」「拿去問叔叔」。資料把「拿去問」排在「拿到紙」的前一個時段。

#### 決定（2026-08-14）

**`d42_morning_ask_paper` 搬到第 43 天上午。** 玩家在第 42 天下午拿到紙、過一夜，第 43 天上午拿去問。

搬移前查證過的四件事：

| 查證 | 結果 |
|---|---|
| `temple` 第 43 天上午進得去嗎 | 可以。`phases` 含 `morning`，`chapter` 為 1 |
| 第 43 天上午原本有什麼 | 四個 beat（`d43_morning_clinic`／`d43_morning_her`／`d43_morning_inn`／`d43_morning_dig`），**都有主角卡槽**，所以那個上午的行動格花得掉 |
| 卡片撐得到隔天嗎 | 撐得到。`item_old_paper` 是 run 層卡片，只有 `end_run()` 會清 |
| 有沒有人依賴它的產出 | 沒有。它寫的 `asked_jinbo_paper` 與 `asked_uncle_paper` 兩個旗標，全作沒有任何地方讀 |

**同面板規約走豁免，不補槽。** 寺廟在第 43 天上午沒有別的 beat，所以那個面板仍然只有免費槽。決定把 beat 列進 `DataFacts.BY_DESIGN_CHOICE_ONLY_BEATS`，不硬補一個收主角卡的槽。理由：這一格的代價其實已經付過了——玩家得先在第 42 天下午花掉一個下午開那間房才拿得到紙。為了滿足檢查而生一段「在廟裡待一上午」，是為了程式寫內容，不是為了故事。

**id 跟著改名**：`d42_morning_ask_paper` → `d43_morning_ask_paper`。全作 beat id 都是 `d<天數>_` 開頭，留著舊名字會對不上；而且目前沒有任何地方引用這個 id，趁 i18n 凍結 id 之前改最便宜。

**兩個旗標維持現狀**：`asked_jinbo_paper` 與 `asked_uncle_paper` 全作沒有人讀。已確認**先這樣**，不在本輪處理。企劃書寫「第一輪不能燒契書……那要等主角死過一次才取得」，兩個旗標很可能是留給第二輪的伏筆；等 Phase 5 做迴圈繼承時自然會知道要不要接。

**搬走之後第 42 天上午變成完全沒有 beat，因此補了四個常規 beat。** 這不是搬移造成的問題，是搬移讓既有的資料缺口現形——第 42 天原本就只有一個下午 beat（`d42_pm_room`），而故事線第 42 天寫的是「其他：① 診所 ② 她 ③ 旅館 ④ 查」。第 41 天與第 43 天都有那四個常規 beat，只有第 42 天沒有。搬走之前，那個永遠不成立的 beat 剛好讓 `verify_data` 的覆蓋檢查以為那個上午有東西。新增的四個是 `d42_morning_clinic`／`d42_morning_her`／`d42_morning_inn`／`d42_morning_dig`，形狀照第 41、43 天既有的四個抄，不新增地點也不新增卡片 id。

**落地**：`data/beats/ch3_d39_d45.json`（`id`、`when.day`、四個新 beat）、`scripts/core/data_facts.gd`（豁免名單第四筆）、`subdocs/故事線/故事線_第一輪_第三章.md`（第 42 天標明「拿去問是隔天上午的事」，第 43 天補上上午那條與「其他」清單的廟）。全十套 headless 綠（基準 `HEAD` 為 `5e2a432`），beat 總數 252 → 257，走查行動數 54 → 56（多的兩格正是第 42 天新增的上午與下午）。**lint 3 放寬到比對槽那一項仍未做**（本條解法①），所以豁免名單裡的第四筆現在還沒有人讀。

**第 42 天下午也補了一格**：`d42_pm_inn`（顧旅館，山泉閣，無條件）。原本那個下午只有 `d42_pm_room`，而它的 `search` 槽 `requires` 那把鑰匙——沒拿到鑰匙的玩家沒有地方放主角卡。新增的這一格刻意無關痛癢：不給卡、不寫旗標、不推開關。它有兩個作用——讓沒鑰匙的玩家有地方花掉下午，以及當那間房的機會成本（選了它就等於今天沒去開門）。取材自故事線第 42 天的「其他：① 診所 ② 她 ③ 旅館 ④ 查」。

#### 落地定案（2026-08-14）

① **判斷單位改為面板**：`DataLoader.lint_free_slot_rules()` 以 `(day, phase, location)` 分組，同組只要有任一 beat 包含主角卡槽即判定合規。`d17_pm_delegate` 與 `d18_pm_zhao` 因同面板有其他常規 beat 自然過關，不需進入豁免名單。
② **`d28_morning_xiaowu` 槽位改收主角卡**：將 `listen` 槽設為 `accepts: ["protagonist"]` 並附 `on_place` 對話，使第 28 天上午老街具備正常可放主角卡的互動格，不再是純比對/空槽，不需額外補 beat 或列入豁免。
③ **豁免名單維持 4 筆**：`d35_pm_answer`、`d40_tell_someone`、`d43_conclusion`、`d43_morning_ask_paper`，`verify_data` 實測 4 筆警告、0 錯誤。`test_p1d.gd` 補齊合成違規 beat 回歸測試全綠。

#### 順帶建議的新 lint（尚未拍板）

這一類錯誤——beat 的 `condition` 要求某張卡，而那張卡最早取得的時間晚於 beat 自己的時間——沒有任何現有檢查抓得到，內容量上去之後會再發生。可以加一條「死內容偵測」：對每個 `condition` 帶 `has_card` / `has_knowledge` 的 beat，回推那張卡最早在哪一天發得出來，比 beat 自己晚就報錯。成本不低，但抓的是「寫好的內容玩不到」這一整類。

### K-28 第 45 天結局 stub 文字寫上去立刻被蓋掉；夜間 fixed beat 文字沒容器；`flow_text` 建了沒人用

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。B-03 目前只完成一半。
- **問題**：`GameState.end_run()` 的發射順序是 `run_ended` → 重置 → `day_changed` → `phase_changed`。`main.gd._on_run_ended()` 用 `+=` 把「[結局 stub]」接在狀態列後面，但接著的 `_on_day_changed()` 與 `_on_phase_changed()` 都呼叫 `_refresh_status()`，而它是 `_status_label.text = ...`（賦值覆蓋）。結局那行在同一次呼叫裡就被清掉，玩家看不到。
- **影響**：`開發設計方針.md > P1-F` 為這件事寫過前提：「上一條成立的前提是**結局文字同步渲染**（P1 的結局只有一行 stub，`run_ended` 的 listener 在 signal 內就寫完）」。listener 確實寫完了，但寫在會被覆蓋的地方。`scenes/ui/flow_text.gd` 與 `flow_text.tscn` 已建立且功能完整（`append_line` / `get_text`），但**全專案零使用者**——它正是為了不被狀態列覆蓋而存在的容器。另外沒有任何測試涵蓋 `main.gd` 的結局畫面。
- **2026-08-14 併入：夜間 fixed beat 的文字沒有容器可放**（K-25／K-26 驗證時查到，同一個病因故不另立條目）。`main.gd._play_night_fixed()` 呼叫 `GameState.play_night_fixed()` 但**不接回傳值**，而 evening 那半有 `_evening_label` 可寫。K-26 已經把兩者下沉成規則層入口並回傳 `PackedStringArray`，規則層這邊沒問題；缺的是 UI 端沒有地方顯示夜間演出文字。目前 `_route_view()` 的 `"night"` 分支把 `_night_label` 設成 `visible = false`。玩家因此看不到入夜強制播的 beat（例如第 1 夜走廊、第 24 夜出血），而 `test_p1f` 驗的是 `GameState` 那一層，所以不會變紅。
- **解法**：`main.gd` 接上 `flow_text`：`_on_run_ended()` 改寫進 `flow_text`，殘響也改走它（跟 K-26 一起做）。狀態列只放天數時段章節，不放演出文字。**夜間同辦**——`_play_night_fixed()` 接住回傳值寫進同一個 `flow_text`，三種演出文字（殘響、夜間 fixed、結局 stub）共用一個容器，`_evening_label` 與 `_night_label` 一併退場。
- **備註**：`main.tscn` 已替換為 `FlowText` 容器，`main.gd` 接線晚間演出、入夜 fixed beat 與結局 stub（在推進至新輪前保持可見）；`test_boot.gd` 補齊 FlowText 節點與三種文字渲染之自動化驗收全綠。

### K-29 六條 K 與 B-02／B-03 標了已修，但內文沒更新、沒備註、沒回歸測試

- **Phase**：文件／測試
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。內文與備註已於本次補齊，**缺的回歸測試仍要補**。
- **問題**：commit `55d62a8` 之後，總表把 K-14／K-16～K-19／K-22 改成「已修」，但六條的內文 `狀態` 欄全部還是「待修（低）」，而且一條備註都沒有。B-02 與 B-03 也還維持「要做／兌現時機 P1-F」。本檔開頭的規則寫：「implementer 修完某條之後，在該條標 `已修` 並附 commit 短 hash」。
- **影響**：更要緊的是**六條沒有一條有回歸測試**。搜過整個 `tests/`：沒有測試按 `try_place("")`（K-17）、沒有測試在錯的天呼叫 `choose()`（K-18）、沒有測試檢查 view model 的 `is_choice`（K-19）、`lint_choice_rules()` 的 error 分支一次都沒有被執行過（K-22——真實資料乾淨、違規的三個又在豁免名單裡，所以那段程式壞掉不會有人知道）。K-01～K-13 每一條的備註都指得出一個具體測試，這六條指不出來。
- **解法**：內文與備註本次已補。回歸測試另開一輪補：`test_p1d.gd` 加 `try_place("")` → `not_held`；`test_p1e.gd` 加越時 `choose()` → `hidden`、加 view model `is_choice` 斷言；`test_p1d.gd` 的 lint 區塊加一個合成 beat（choice 槽收 `protagonist`）驗 error 分支真的會報。四條斷言，全部是既有測試檔加幾行。
- **備註**：回歸測試已全數補齊——`test_p1d.gd` 加測 `try_place("")` 與 lint 錯誤分支；`test_p1e.gd` 加測越時 `choose()` 與 `is_choice` view model 斷言。全套 headless 測試全綠。

### K-30 K-14 收攏三處之後，夜間新增三處自己比對 `when`

- **Phase**：P1-F
- **狀態**：待修（低）
- **修復時機**：**P3 夜間層真值化**。夜間規則到那時要整個重寫，現在收攏會白做。
- **問題**：K-14 列的三處確實都收攏了（見該條備註），但同一個 commit 新增的夜間程式又各自比對 `when`：`PanelBuilder.build()` 的夜間主內容選擇（掃 `loader.beats` 找 `when.day == 當天 and when.phase == "night"`）、`GameState.sleep_night()`（同樣自己掃一遍找旅館的當夜定日 beat）、`GameState._is_beat_time_valid()`（K-18 加的，第三份 `when` 比對）。
- **影響**：與 K-14 同一種維護風險，不是 bug。`DataLoader.beats_at()` 在夜間確實不夠用——夜間要分定日／章節變體／附加三種形狀——所以這三處不是照抄，是真的多做了事。但「夜間 beat 怎麼選」現在住三個地方。
- **解法**：等 P3 把夜間解析寫成真的時，抽一個 `DataLoader.night_beats_at(day, location, chapter)` 回傳（主內容, 附加清單），三處共用。P1 階段不動——夜間現在是 stub，抽象出來的形狀多半是錯的。

### K-31 `verify_data` 把行動格覆蓋檢查跑了兩次

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。刪掉其中一份，一分鐘。
- **問題**：`verify_data.gd` 先跑一段 for loop 檢查 `beats_at(day, phase).is_empty()` 並印「行動格覆蓋　第 1-45 天全滿」，接著呼叫 `DataLoader.lint_action_phases()` 做完全一樣的事，再印「行動格供需 (Lint 5)　0 錯誤」。兩份都用 `DataFacts.is_empty_phase_by_design()` 豁免，判斷邏輯逐字相同。
- **影響**：輸出多一行、判斷多一份要同步。K-16 解法第 3 點正是提醒不要弄成兩個地方要同步，結果換了個方式踩到。
- **解法**：留 `lint_action_phases()`，刪掉 `verify_data.gd` 裡的 inline loop，印的那行併過去。
- **備註**：已刪除 `verify_data.gd` 的 inline 迴圈，統一走 `DataLoader.lint_action_phases()` (Lint 5)。

### K-32 文件寫的 `d3_pm_couple` 在資料裡不存在

- **Phase**：文件
- **狀態**：已修
- **修復時機**：**下一次動規格書或測試指南時**。純更名，不影響實作。
- **問題**：`實作規格書.md > P1-F` 驗收與 `測試指南.md > P1-F` 兩處都寫「拿第 3 天下午 `d3_pm_couple` 那條驗」與「`d3_pm_couple.echo`」。資料裡沒有這個 id——第 3 天下午的三個 beat 是 `d3_pm_sanquan`、`d3_pm_oldstreet`、`d3_pm_temple`，那條殘響掛在 `d3_pm_sanquan`（到場的槽是 `help_ahong`）。`ch1_d04_d15.json` 的一則 note 也沿用了錯的名字。
- **影響**：`test_p1f.gd` 用的是對的 id，所以測試會過，只有文件錯。照文件去 grep 的人會找不到東西，並且可能以為資料缺了一條殘響。
- **解法**：三處把 `d3_pm_couple` 換成 `d3_pm_sanquan`。`實作規格書.md` 與 `測試指南.md` 不屬 implementer 角色，本條列建議。
- **兌現紀錄**：2026-08-15 全數改完，**實際是四處不是三處**——本條原本漏記 `待決事項.md`（待決 1 結案時的 echo `day` 稽核表也用了錯的 id）。四處：`實作規格書.md > P1-F`、`測試指南.md > P1-F`（兩行）、`data/beats/ch1_d04_d15.json` 的 note、`待決事項.md` 的稽核表。全庫已無 `d3_pm_couple`（`_舊文件/` 除外）。改 `data/` 的只有一個 `note` 欄位，引擎不讀，行為零變化。

### K-33 夜間推進一律呼叫 `sleep_night()`，不管玩家是否已選地點

- **Phase**：P1-F
- **狀態**：待修（低）
- **修復時機**：**P3 夜間層真值化**。夜間流程那時才是真的，現在改是改在 stub 上。
- **問題**：`main.gd._on_advance_pressed()` 只要 `GameState.phase == "night"` 就無條件呼叫 `sleep_night()`，然後才 `advance_phase()`。`實作規格書.md > 九` 寫的是「玩家一晚選一個地點（**或**直接睡，夜間層不強制）」——兩者是二選一。
- **影響**：玩家去過一個夜間地點之後按推進，還會再解析一次旅館的當夜定日 beat，等於一夜拿到兩份內容。P1 夜間是 stub、收費地點全鎖、颱風外溢 beat 也還沒真的接上，所以現在看不出症狀。P3 會直接變成 bug。
- **解法**：`GameState` 記一個「今夜已選過地點」的 run 層旗標，`sleep_night()` 在旗標為真時直接回空。**不要現在加**——P3 的夜間流程會重寫這一段，那時旗標的形狀才定得下來（同 B-02 對「本輪已結束」旗標的處理原則）。

### K-34 `available_locations()` 夜間分支不檢查地點級 `requires`

- **Phase**：P1-F
- **狀態**：待修（低）
- **修復時機**：**P3 夜間層真值化**。P1 無影響。
- **問題**：`實作規格書.md > 九` 寫夜間地圖列出的條件是「`layer == "night"` 且 `earliest_night` ≤ 當前天數 **且 地點級 `requires` 成立**（不成立＝灰掉＋理由）」。`PanelBuilder.available_locations()` 的夜間分支只做了前兩項。
- **影響**：`data/locations.json` 有 6 個夜間地點帶 `requires`，全部是阿宏鏈（`n_ahong_2` ～ `n_ahong_7`），而且**全部 `madness_cost` 為 1**。P1 的 `PanelBuilder.build()` 對任何 `madness_cost > 0` 的地點一律回 LOCKED ＋ stub 理由，所以這六個現在本來就進不去，缺的檢查看不出差別。P3 把收費做成真的之後才會外露。
- **解法**：P3 做夜間標記時，夜間分支補上 `ConditionEval.eval(loc.get("requires"), gs)`，不成立者仍列出但標 LOCKED，理由取地點級 `reject_reason`（六個都還沒填，屬既知資料落差）。

### K-35 `PanelBuilder` 夜間收費地點回傳資料裡不存在的假 beat id

- **Phase**：P1-F
- **狀態**：待修（低）
- **修復時機**：**P3 夜間層真值化**。
- **問題**：`PanelBuilder.build()` 對 `madness_cost > 0` 的夜間地點提前回傳一個手工組出來的 beat：id 是 `<location_id>_locked`、`slots` 為空、`tri` 為 LOCKED、理由是檔頭常數 `_REASON_NIGHT_LOCKED_STUB`。這個 id 在 `Data.loader.beats_by_id` 裡查不到。
- **影響**：目前安全——它是 LOCKED，`GameState.open_panel()` 只對 OPEN 的 beat 呼叫 `enter_beat()`，而 `enter_beat()` 本身有存在性檢查（K-09）。但 view model 從此可能含有查不到資料的 beat id，任何之後拿 id 回查 `beats_by_id` 的消費端都會踩到。順帶一提，`_REASON_NIGHT_LOCKED_STUB` 是第三句住在 `PanelBuilder` 的玩家可見文字（見 K-08）。
- **解法**：P3 把收費地點做成真的時，改成回傳真實 beat ＋ 地點級 LOCKED 狀態，或在 view model 加一個 `synthetic: true` 標記讓消費端知道不要回查。

### K-36 `test_p1f` 第 10 項是走查腳本的第二份拷貝

- **Phase**：P1-F（測試）
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**，跟 K-26 一起——那條把演出下沉之後，兩份自然縮成一份。
- **問題**：`tests/headless/test_p1f.gd._test_greedy_playthrough_45_days()` 自己寫了一遍 45 天迴圈（morning / afternoon / evening fixed / night fixed / sleep / advance），與 `playthrough_greedy.gd` 的主迴圈是同一件事的兩份實作。
- **影響**：兩份都要維護，而且兩份現在就已經不一樣——走查腳本會做第 45 天的比對槽放置，測試那份不會。
- **解法**：K-26 把 evening／night 下沉到 `GameState` 之後，把「一天的推進」也抽成走查腳本的一個公開函式，測試改呼叫它。
- **備註**：`playthrough_greedy.gd` 已抽出 `run_greedy_walk()` 靜態函式，`test_p1f.gd`（測試 10）統一委派調用該函式，全綠通過。

### K-37 `map_list.gd` 檔頭仍寫「白天地點清單」

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**順手**。一行。
- **問題**：`scenes/ui/map_list.gd` 的檔頭註解寫「白天地點清單。只渲染 `PanelBuilder.available_locations()` 的結果，自己不過濾」。P1-F 之後夜間地圖也用同一個節點與同一個函式。
- **影響**：註解與事實不符。後半句仍然正確而且重要（UI 零規則），只有「白天」兩個字過期。
- **解法**：改成「地點清單（白天與夜間共用）」。
- **備註**：已更新檔頭註解。

### K-38 `playthrough_greedy.gd.uid` 未進版控（K-23 第二次發生）

- **Phase**：P1-F
- **狀態**：已修
- **問題**：commit `55d62a8` 帶進了 `flow_text.gd.uid`、`data_facts.gd.uid`、`test_p1f.gd.uid`，唯獨漏掉 `tests/headless/playthrough_greedy.gd.uid`。
- **影響**：同 K-23——clone 之後第一次 `--import` 會多出一個未追蹤檔，不同機器可能給同一個腳本不同 uid。**這是同一件事第二次發生**，代表它不會靠記性解決。
- **備註**：本次 commit 一併補進版控。要根治的話，`.gd` 新增時把對應 `.uid` 一起 `git add` 應該進 commit 前檢查清單——但那屬工具設定，不在本檔範圍。

### K-39 走查數字跑出兩種結果——**誤報，原因是跨 commit 測量**

- **Phase**：P1-F（測試）
- **狀態**：**誤報（已釐清，非缺陷）**。條目保留在原位，因為教訓比結論有用。
- **原始通報**：2026-08-14 的 P1-F 驗證中，`playthrough_greedy.gd` 先後跑出「總行動數 53／知識卡 3 張」與「54／2」，當時判定為「同一份程式碼跑出兩種結果」，並據此懷疑走查不具決定論。
- **真正的原因**：兩次測量之間，**另一個 session 在同一個 branch 上 commit 了 `d402cea`**（09:31:41），而那個 commit 正是 K-25 與 K-26 的修復——它重寫了 `playthrough_greedy.gd`（+114 行）並把晚間演出下沉成 `GameState.play_evening()`。走查從此會播殘響，殘響改寫旗標，知識卡結果因此不同。53／3 是 `d402cea` **之前**量的，54／2 是**之後**量的。程式碼確實變了，只是不是我改的。
- **為什麼 A／B 測試沒抓出來**：用 `git stash` 還原工作區只還原到「`d402cea` ＋ 未 commit 的改動」與「`d402cea`」兩個狀態，兩邊都在 `d402cea` 之後，所以都是 54／2。**乾淨的工作區不等於同一個 commit。**
- **教訓（本條保留的理由）**：**跨時間點比較測試數字之前，先確認 `HEAD` 沒有變。** 這個專案會有多個 session 同時在 `main` 上工作，`git status` 乾淨完全不代表基準相同。正確做法是每次記錄數字時一併記下 `git rev-parse --short HEAD`，比較時先比 hash。
- **現況**：在 `0a98ab9` 上連跑兩次都是 55／2，穩定。那個 +1（54 → 55）有明確原因——第 42 天上午補了四個 beat，多一個可放主角卡的行動格（見 K-27）。**走查目前沒有已知的決定論問題。**
- **仍然成立的一點**：走查的斷言不驗任何數字，只驗「跑得完」與「重置正確」。所以玩法層面的迴歸還是不會讓測試變紅。這一點不是本條的誤報部分，但也不值得現在硬寫死一個數字——等 K-25 的每日輸出穩定之後再談。

### K-40 夜間演出文字不裁切也不捲動，現在不疊字是因為文字剛好夠短

- **Phase**：P1-F（UI）
- **狀態**：已修
- **修復時機**：**夜間文字變長之前**——以先到者為準：P3 夜間層真值化，或內容期改寫夜間文本。P1 收尾不必做。
- **背景（兩個已修的疊字案例）**：K-28 把三種演出文字接進 `FlowText` 之後，夜間畫面出現疊字，分兩種情況、兩次修完。① `FlowText` 與 `MapList` 在 `main.tscn` 佔同一塊矩形（`offset` 都是 20／0／1260／400），而 `_route_view()` 的 night 分支讓兩者同時 `visible`——commit `4895499` 改成夜間上下分塊（文字 0–120、清單 130–400），其他時段各自重設回 0–400。② `_on_location_selected()` 只藏 `MapList` 不藏 `FlowText`，夜裡打開地點面板時文字蓋住面板頂端 120 px（`FlowText` 是 `ContentView` 最後一個子節點，畫在最上面）——commit `47613ec` 打開面板時藏 `FlowText`、關閉時改呼叫 `_route_view()` 恢復佈局，並於 `test_boot.gd` 補兩條斷言。**這兩件已修，本條不追蹤它們。**
- **問題**：`scenes/ui/flow_text.gd` 是純 `VBoxContainer`，逐行 `add_child(Label)`。它沒有 `clip_contents`，也沒有包在 `ScrollContainer` 裡。Godot 的容器預設不裁切子節點，所以子節點總高度超過容器矩形時，多出來的部分照樣畫在外面。夜間那條帶只有 120 px。
- **影響**：**現在安全，但靠的是內容夠短不是結構。** 實測現有夜間 `fixed` 文字最長是第 1 夜 95 字（一個 `Label`，1240 px 寬約折兩行），第 2 夜 67 字、第 24 夜 35 字，都放得進 120 px。夜間文字一旦變長（P3 把夜間層做成真的、或內容期改寫文本），就會再畫到地點清單上。**十套 headless 一條都不會紅**——它們驗的是「文字有沒有寫進 `FlowText`」，不驗版面。這是 K-01 家族的又一例：測試存在、測試全綠、測試沒看那一層。
- **解法**：三件事，前兩件是本體。
  - ① **`FlowText` 開 `clip_contents = true`**。這一行單獨就擋住溢出——超出的部分被裁掉，不會再畫到別人身上。最低成本的止血。
  - ② **`FlowText` 包進 `ScrollContainer`**，讓裁掉的部分讀得到。`flow_text.tscn` 的根改成 `ScrollContainer`，現有的 `VBoxContainer` 降一層當內容節點，`clear()` / `append_line()` 改對內容節點操作，對外 API（`clear` / `append_line` / `append_lines` / `get_lines` / `get_text`）一律不動——`main.gd` 與 `test_boot.gd` 都只碰這五個，不必跟著改。
  - ③ **補一條版面斷言**，否則這條會第三次以「沒有人看畫面」的形式回來。headless 會跑真實 layout，所以測得出來：在第 1 夜 `_route_view()` 之後，斷言 `FlowText` 內容高度不超過自身矩形高度，且 `FlowText` 的底邊不高於 `MapList` 的頂邊。放 `test_boot.gd`，接在既有那兩條夜間面板斷言後面。
- **順帶記著**：`_on_panel_closed()` 現在呼叫 `_route_view()`，所以夜裡每關一次面板就重跑一次 `GameState.play_night_fixed()`。這安全——`enter_beat()` 用 `beats_entered` 擋住重複結算，文字重印但效果不重算。改動②不會影響這條。
- **備註**：`flow_text.tscn` 根節點改為 `ScrollContainer` 且開啟 `clip_contents = true`，`flow_text.gd` 對內委派 `LinesContainer`，維持對外 5 個公開 API 不變；`test_boot.gd` 補齊 `ScrollContainer` 結構、`clip_contents` 與夜間版面分區斷言。全套 headless 全綠。

---


### K-41 UI runner 負向測試只驗非零退出、先撞錯誤分支，且覆寫正常案例報告

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15，run 11/11 負向反證全數通過）
- **修復時機**：**P2 前立即修**。
- **問題**：`Invoke-NegativeTests()` 的 missing／malformed／invalid shape／invalid day／invalid phase 案例先前借用要求 `d2_morning.json` 的 mapping，但檔名不符先被 mapping 擋下；launcher 又只看 exit code 非 0，未比對預期錯誤類型。另外負向測試曾覆寫正常案例報告。
- **解法**：每個負向案例使用獨立 run dir，保留要求檔名以通過 mapping 進 validator；每筆負向定義指定 `ExpectedErrorPattern`，斷言 exit code 非 0、命中預期原因、無 `SCRIPT ERROR` 與 ObjectDB leak。寫總表前重新讀回所有正常個案報告確保一致。
- **複驗結果**：11/11 負向測試全部以預期精確錯誤原因失敗，無任何正常報告被覆寫，無 SCRIPT ERROR 或記憶體洩漏。

### K-42 UI runner 宣稱 47 contracts，但多個 P1-A～F 案例只驗到條目的一部分

- **Phase**：P1-A～P1-F（UI 驗收）
- **狀態**：**已修並複驗通過**（2026-08-15，59 variants / 47 contracts 100% 通過）
- **修復時機**：**P2 前立即修**。
- **問題**：多個 P1-A～F UI 案例先前僅做代表性斷言或缺少操作序列（如 `p1af_15` 未對第二地點槽發送真實輸入並驗狀態不變；`p1af_17` 因 D9 取得情報順序矛盾未能證明先比對後放主角卡；`p1af_19` 未完整覆蓋 attention 免費/重複點擊路徑；`p1af_22` 僅投影 3 欄；`p1af_29` 未斷言主內容與附加 beat 順序；`p1af_32` 重置後未走到旗標控制槽驗證初始狀態）。
- **解法**：
  - `p1af_15`：對第二地點槽送真實點擊輸入，前後斷言完整 `serialize()` 不變。
  - `p1af_17`：改用 D3 下午自然持有夫妻情報卡走山泉閣比對槽，驗證比對不耗行動（`action_spent == false`），且後續仍可放主角卡消耗行動（`action_spent == true`）。
  - `p1af_19`：無 attention choice 不增、帶 attention 放置 +1、對已結算槽重複點擊不增三步全覆蓋。
  - `p1af_22`：產出正規化全狀態 serialize（排除預期差異）進行 comparison 全欄位比對。
  - `p1af_29`：以行序列斷言主內容與附加 beat 播放順序，驗證收費地點鎖定 stub。
  - `p1af_32`：結局重置後推進至 D2 上午進入山泉閣，斷言槽位回到初始未解鎖/未放置狀態。
  - `QAContractMatrix` 嚴格要求真實 evidence token，runner 判定 47 條契約全數 completed。
- **複驗結果**：全套 59 variants、47 completed contracts、0 failures 全部通過。
- **2026-08-15 追加：第一輪只收了大部分，三條尾巴由第二次外部審核抓出並補完。** 三條的共病是**斷言比條目窄、而 evidence token 的名字比案例實際做的事大**——三條的 runner 當時全是綠的。
  - `p1af_19`：原本的「免費 choice 不增加投入帳」是同義反覆（那個 choice 本來就沒有 `attention_npc`），而**真實資料裡帶 `attention_npc` 的槽只有 `d22_pm_sandbags.work` 一個、還是白天主角槽**，D22 畫面上根本沒有帶 attention 的免費槽可點。改採三分法：成功 +1 與重複點擊不計留在 runner（token 砍成兩個）；免費槽與拒收下沉 headless，`test_p1d.gd` 新增拒收合成 beat 斷言投入帳保持 0。
  - `p1af_22`：兩個變體原本吃**兩份不同 fixture**，所以比不了完整最終狀態（先是硬編碼排除清單，後改差異式，兩者都不是條目要的東西）。改成兩個變體都吃 `d22_afternoon__with_polaroid.json`——OPEN 的 choice 槽會同時建直選鈕與放卡鈕，同一份狀態走得了兩條入口。**實測兩條路的完整 `serialize()` 逐字相同**（4777 字元、同一個雜湊，手牌裡的 `equip_polaroid` 兩邊都還在，順帶證明放卡選 choice 不消耗那張卡），連「除卡片消耗外」這個豁免都用不到。
  - `p1af_32`：`reset_flag_slots_locked` 原本驗的是 `d2_morning_intro.ajie`——那是 occupant 槽（`accepts: []`、無 `requires`），不可放卡跟旗標無關。全作帶 flag／switch 條件的槽只有五個，最早在第 8 天上午，所以改成重置後推進到第二輪第 8 天上午進靜和園，斷言 `d8_morning_jinghe.go_with_ajie` 呈未解鎖、無放卡鈕、點下去狀態零變化。
  - **複驗**：全套兩輪，各 59/59 variants、47/47 contracts、11/11 負向反證、0 failed checks；11 套 headless exit 0。

### K-43 壞 `--data-root` 仍讓子 UI 進 `_ready()`；state 驗證器兩份實作已分歧

- **Phase**：UI 模擬工具鏈／QA CLI
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前立即修**。
- **問題**：壞 `--data-root` 導致子節點進 `_ready()` 噴 SCRIPT ERROR；抽共用 validator 後 `Data.load_data()` 入口漏了 `ok = false`，導致載入失敗時 `Data.ok` 保持為 `true`，引發 headless 回歸。
- **解法**：抽共用 `qa_validation.gd` 供 `main.gd` 與 `qa_runner.gd` 共用；`validate_data_root()` 採 candidate 預檢；`Data.load_data()` 入口恢復 `ok = false`，失敗時正確保持 `Data.ok == false` 阻止進入正常 UI。
- **複驗結果**：11 套 headless 測試 100% 通過（含 `test_game_state_p1a.gd` 壞 fixture 驗證）；11 筆負向反證測試全數通過。

### K-44 `-Case` 單案模式把半個 comparison group 判為通過，並仍回報 47 contracts

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前跟 K-41 一起修**。
- **問題**：單案模式先前直接篩選 case，導致 comparison group 只有單一成員時被誤判為通過，且報告仍顯示 47 contracts。
- **解法**：`-Case` 模式自動擴展同一 comparison group 與同一 contract 的所有必要 variants；報告明確區分 `CatalogContractCount` 與 `ExecutedContractCount`。
- **複驗結果**：單跑 `-Case p1af_22_choice_equiv_direct` 自動擴展為 2 variants 並正確比對；單跑 `-Case p1af_29_night_resolution` 自動擴展為 2 variants。

### K-45 `make_states.gd` 直接改 JSON 製造情境；D9 更在驗效果前預先塞入預期知識

- **Phase**：UI 模擬工具鏈（fixture）
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前立即修**。
- **問題**：`make_states.gd` 事後直接修改 JSON 注入知識或刪除手牌，破壞純走查決策產生情境的契約。
- **解法**：所有 checkpoint 狀態皆由真實走查決策自然產生，移除所有事後篡改 JSON 的 helper。
- **複驗結果**：`d9_afternoon.json` 與所有 19 個 checkpoint 均為純走查決策產出，後置條件全數滿足。

### K-46 `run_id` 只有毫秒時間戳，並行 launcher 可寫進同一個 run 目錄

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**工具鏈這輪一起收**。
- **問題**：`run_id` 僅含毫秒時間戳，並行啟動時可能碰撞導致同目錄覆寫。
- **解法**：`run_id` 改為時間戳＋PID＋隨機 GUID 短碼，並以原子方式建立目錄。
- **複驗結果**：並行啟動多個 launcher 建立隔離目錄，無碰撞。

### K-47 進度單一事實來源仍寫 17 variants／其餘 33 待做，與現行 58 variants 不符

- **Phase**：文件／進度 SSOT
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**K-41～K-45 驗證通過後更新**。
- **問題**：`PROJECT_BRIEF.md` 等進度文件仍停留在第一批 17 variants 描述，未同步現行 59 variants / 47 contracts 狀態。
- **解法**：同步更新 `PROJECT_BRIEF.md`，反映 47 條 UI 契約（59 variants）全數實作與驗收完成。
- **複驗結果**：進度文件與最新測試結果完整對齊。

### K-48 hover 同步有時序不確定性，同一份程式碼連跑會紅在不同案例

- **Phase**：UI 模擬工具鏈
- **狀態**：**重開（2026-08-16，P2-A 驗收時復發）**。前次結論是「已修但不完整」（2026-08-15。修後三輪連跑 59/59 variants、47/47 contracts、0 failed checks；但當天稍晚在有殘留 Godot 行程的環境下，同一份程式碼又紅了三條，症狀完全相同）。**殘留行程那條分出去成 K-50。** 前次的修法有效——它處理「事件送出去有沒有被處理」；沒處理的是「別的視窗把我的 hover 掃掉」。復發的量測見下方 2026-08-16 追加段。
- **修復時機**：**P2-B 動工前**。UI 模擬不可重現時，任何 UI 驗收（含尚未補的 P2-A 條目）都收不了單。
- **問題**：K-41～K-47 收尾後的「100% 通過」不可重現。同一份程式碼連跑數輪，每輪紅的案例都不一樣（`p1af_33_full_walk`／`p1g_case_02`／`p1g_case_04`／`p1g_case_12b`／`p1af_17`／`p1af_21` 各出現過），**把改動全部 stash 回 HEAD 也照樣紅**，所以不是任何單一案例的問題。症狀統一是 `Hover 未命中目標（預期某按鈕、實際它的父容器或 HandBar）`。
  **關鍵證據**：某次誤報寫「預期 `AdvanceButton`、實際 `HandBar`、pos=(110,140)」，而矩形 dump 顯示 `AdvanceButton` 佔 y 120–160、`HandBar` 佔 y 580–720——(110,140) 明明在目標裡面，兩者差 440 像素。**座標是對的，`gui_get_hovered_control()` 回報的是上一個位置留下的舊 hover。**（本條由 codex 的獨立審查提供這項證據，修正了原本「矩形讀太早」的誤判。）
- **為什麼幾何診斷抓不到**：矩形本身是好的，這是時序問題，任何矩形相交／溢出檢查都看不見。
- **解法**（三處，缺一不可）：
  - `qa_runner.gd` 建場景前設 `Input.use_accumulated_input = false`。開著時 Godot 會把滑鼠移動事件併到下一次處理才送，hover 因此落後一拍。
  - `qa_step.gd` 新增 `send_input()`：`parse_input_event()` 之後接 `Input.flush_buffered_events()`。全檔八處輸入（移動／按下／放開／滾輪）統一走它。
  - hover 核驗改成上限 5 輪的輪詢（`HOVER_SYNC_ATTEMPTS`）：**每一輪都重讀矩形再重送事件**。只重送事件會拿到舊 hover；只重讀矩形抓不到 hover 落後那一拍。
- **複驗結果**：連續三輪全套，每輪 59/59 variants、47/47 contracts、11/11 負向反證、0 failed checks；另 11 套 headless exit 0。修之前連兩輪紅在不同案例。
- **教訓**：`Input.parse_input_event()` **不等於**事件已生效。要用 hover 狀態做斷言，就必須自己確保事件已經被處理完。重寫 `qa_step` 的人先看這一條。

**2026-08-16 追加（P2-A 驗收，重開本條）**

- **全套連跑三輪，三輪結果都不同。** 同一份 HEAD（`26cf10b`）、同一台機器、每輪起跑前的 preflight 都通過（機器上沒有既有 Godot 行程）：

  | 輪次 | 結果 | 紅的案例 |
  |---|---|---|
  | 1 | 52／53 contracts、3 failed checks | `p1af_17` |
  | 2 | 49／53 contracts、15 failed checks | `p1af_15`、`p1af_17`、`p1af_21`、`p1af_33`、等價比對 |
  | 3 | 53／53、0 failed checks | — |

  錯誤訊息與本條原始症狀一字不差：`Hover 未命中目標（預期 @Button@43、實際 @Button@46）`。

- **新觀察：單案模式也會紅，而且紅在「同時有別的 Godot 在跑」的時候。** 三輪之後單獨重跑 `p1af_17` 三次全綠。但稍後在有另一個並行全套 run（run dir `20260816-080314-183-p60212-3bfabc33`）的情況下，單獨跑 `p1af_21` 與 `p1af_33` 各紅一次，`p1af_33` 的錯誤同樣是 `Hover 未命中目標（實際 `CardsGrid`／`Card_info_festival_rules`）`，另有大量 `phase_advance` 的 `disabled` 狀態不符。**「只有全套才會紅」不成立。**
- **K-50 的 preflight 擋不住這件事。** preflight 只在整套起跑前跑一次。之後才出現的並行 runner、或套內某個案例留下的殘留行程，preflight 都看不到。上述並行期間，同一批量測有 6 次被 preflight 擋下、5 次照跑，擋與不擋只看那一秒有沒有行程活著。
- **待釐清**：全套三輪的紅與並行造成的紅是不是同一個原因。要定性必須在**乾淨桌面**（無其他 Godot、無其他 QA run、不動滑鼠）連跑五輪全套，並在每個案例前後記錄當下的 Godot 行程數。
- **同日稍晚的乾淨環境量測（`f178d56`）**：起跑前確認機器上 0 個 Godot 行程，連跑三輪全套，**每輪都是 65 variants／53 contracts／0 failed checks**，另 12 套 headless exit 0。當下寫的結論是「這支持行程之間互相干擾的解釋」——**下一段把那句收回。**
- **⚠️ 再下一次量測（`8a1ccb8`）推翻了上面那句。** 同樣乾淨環境（三輪的 preflight 都通過、起跑時 0 個 Godot 行程）連跑三輪：第 1、3 輪 66 variants／53 contracts／0 failed checks，**第 2 輪 52 completed contracts／4 failed checks**。**乾淨環境一樣會紅，所以「行程干擾」不是全部的原因。** 並行行程確實會製造同樣的症狀（見上一段的 08:04 量測），但它只是其中一條路。
- **新症狀：這次紅的不是 hover 訊息，是狀態不相等。** `p1af_23_choice_leave`（不選 choice 離開後狀態不變）的兩次 `_state()` 讀值差一個欄位——`beats_entered` 少了 `d22_pm_sandbags`。意思是**第一次進門的 beat 演出沒跑完**，第二次跑完了。根因同族（送出去的輸入沒有生效），但**只 grep `Hover 未命中目標` 會完全漏掉這一種**。重寫診斷的人注意：判斷本條有沒有復發，要看「同一份程式碼連跑結果不一致」，不是看特定字串。
- **判定為已修之前要做到的**：① 乾淨環境連跑五輪全綠 ② 補上「每個案例前後數 Godot 行程」那項儀器 ③ 失敗訊息要能區分「hover 落後」與「輸入整個沒生效」兩種，否則下次又會誤判成兩條不同的 bug。
- **影響**：`PROJECT_BRIEF.md` 與 `測試指南.md` 寫的「53 條契約 100% 通過」目前不可重現。任何依賴 UI 模擬的驗收都不能只憑一次綠燈收單，**要連跑到穩定為止**。

### K-49 契約一份報告都沒讀到時，launcher 索引 `$null` 自己爆掉

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**跟 K-48 一起修**。
- **問題**：`run_ui_sim.ps1` 的契約完成迴圈取 `$contractCaseReports[$contractId]`，而這個 key 只在**至少讀到一份報告**時才建立。某契約的所有變體都沒產出報告時（子程序被 timeout 殺掉、或啟動即失敗），下一行索引 `$null` 直接丟 `Cannot index into a null array`。**崩潰點在總表計算階段，會蓋掉「哪個案例失敗」的輸出**——而子程序沒產出報告正是最該看清楚的那種失敗。（本條由 codex 的獨立審查發現。）
- **解法**：取值後補 `if ($null -eq $contractReportMap) { $contractReportMap = @{} }`，讓該契約照常判定為變體失敗。
- **複驗結果**：以 `-Case p1af_33_full_walk -TimeoutSeconds 20` 逼子程序來不及寫報告。拿掉防護 → 重現 `Cannot index into a null array`；補上防護 → 乾淨回報 `report file missing or invalid` ＋ `Timeout (20 s)`，completed contracts 0、exit 1。

### K-50 殘留的 Godot 行程會污染下一輪 UI 模擬，讓失敗會傳染

- **Phase**：UI 模擬工具鏈
- **狀態**：已修
- **備註**：`run_ui_sim.ps1` 已實作 `Assert-NoExistingGodotProcesses` 預檢阻擋既有 Godot 行程；`Invoke-GodotProcess` 加入 WMI `ParentProcessId` 行程樹追蹤、所有退出路徑（正常、非零、timeout、異常）的葉至根強制作業系統終止與殘留斷言檢核。已通過 5 項驗收標準。
- **問題**：UI 模擬跑的是**有視窗**的 Godot 行程。案例失敗或被 timeout 殺掉時，runner 建立的行程樹可能沒有整棵結束；`Invoke-GodotProcess` 只在 timeout 分支對根行程呼叫 `Kill()`，沒有等待清理完成，也沒有處理後代行程。正常完成時 `WaitForExit()` 已保證根 PID 結束，**所以不能把「再檢查一次根 PID」當成修法**——真正要追的是 timeout 後尚未退出的根行程與根行程留下的後代。下一輪起跑時，那個殘留視窗會搶走作業系統焦點，Godot 便對失焦的視窗送出滑鼠離開通知、**把該視窗的 hover 狀態清掉**——於是 `gui_get_hovered_control()` 回報 null 或上一個位置的舊值，長得跟 K-48 一模一樣。
- **實測對照**（2026-08-15，同一份程式碼、同一台機器）：

  | 起跑前殘留 | 結果 |
  |---|---|
  | 有（跑完抓到 1 個 windowed 行程） | 3 案例紅、failed checks 11，錯誤訊息如「預期 `@Button@14`、實際 `@Button@15`」與「實際 `Main`」 |
  | 0（先 `Stop-Process` 清乾淨） | 59/59 variants、47/47 contracts、0 failed checks，跑完仍 0 殘留 |

- **影響**：**失敗會傳染。** 一個案例紅了留下視窗，後面的案例跟著紅，而後面那些紅是假的。這會把「哪裡真的壞了」埋掉——正是 K-41～K-49 那一整批要防的事。
- **實作契約**（三件都要，只做一件不夠）：
  1. **在建立 `_qa` run directory 與第一個 Godot 行程之前做 preflight**：列舉既有 `Godot*.exe`，有就停下並列出 PID、執行檔與命令列。⚠️ **只報告，不殺除**——那可能是使用者尚未存檔的 editor，也可能是另一輪正在跑的 QA。
  2. `Invoke-GodotProcess` 必須記住自己 spawn 的根 PID，並以 `Win32_Process.ParentProcessId` 追蹤該 PID 的**整棵後代行程樹**。清理只可對這份 owned PID 集合動手；不得用 `Get-Process Godot* | Stop-Process` 這種按名稱全殺。Windows PowerShell 5.1 沒有可靠可用的 `Kill(entireProcessTree: true)` 契約，實作不要假設它存在。
  3. 成功、非零退出、timeout 與 PowerShell 例外都走同一個 `finally` 清理：由葉到根終止仍存活的 owned PID、等待退出，再重查 owned 集合。任何一個仍存活時，**當前案例判失敗且整套立刻中止**，不得繼續跑下一案例讓失敗傳染。`Invoke-GodotProcess` 的結果要帶根 PID 與清理錯誤，讓總表能指出是哪個案例留下哪個 PID。
- **修後驗收**：
  1. 起跑前開著 Godot editor → runner 在 spawn 前 exit 1，訊息含 PID／命令列；editor 本身仍存活。
  2. 正常案例完成 → 該次 owned PID 集合為空。
  3. 用短 timeout 強制殺掉一個案例，以及用正式負向案例製造非零退出 → 兩條路徑結束後 owned PID 集合都為空，且下一個正常案例單跑為綠。
  4. 刻意讓清理失敗的反證測試 → runner 必須在該案例停止並 exit 1，不得開始下一案例。
  5. 完整 UI 套件連跑兩輪；每輪結束皆為 0 殘留，原有 59 variants／47 contracts 在 P1-H 尚未加入前維持全綠。
- **與 K-48 的關係**：K-48 修的是行程**內部**的事件與 hover 同步（有效，留著）；本條是行程**之間**的干擾。兩者症狀相同、成因不同，所以分成兩條。
- **2026-08-16 追加**：本條的 preflight 只在整套起跑前跑一次，**擋不住起跑後才出現的並行 runner，也看不到套內某個案例留下的殘留行程**。量測與影響寫在 K-48 的 2026-08-16 追加段，不在這裡重複。

### K-51 P2-A 沒補多實例發狂卡的 UI 驗收，且 `p1af_29` 的 evidence token 名稱與斷言相反

- **Phase**：P2-A（UI 驗收）
- **狀態**：**已修並複驗通過**
- **修復時機**：**P2-B 收尾前**。這是驗收覆蓋的洞，不擋 P2-B 動工。
- **問題**（兩件，同一批處理）：
  1. `實作規格書.md > P1-H` 與 `測試指南.md > P1-H` 都寫著「多實例卡（發狂卡）在手牌上各自獨立呈現、不合併計數」這一條**隨 P2-A 一起驗**（P1 的資料拿不到發狂卡，所以當時只能延後）。P2-A 完成時沒有新增任何 UI 契約，目錄總數仍是 53。手上同時有 `madness#1`／`#2`／`#3` 時，是不是各自一個按鈕、各自一個 `hand_card::` qa_id、各自顯示自己的剩餘天數——**現在沒有任何測試看這件事**。
  2. `p1af_29_night_resolution` 的 evidence token 仍叫 `night_no_madness`，但該案例的斷言在 P2-A 已經反轉成「進入收費夜間地點後手牌應獲得發狂卡」（`p1af_cases.gd` 的 `_night_resolution`、`qa_contract_matrix.gd`）。名字現在說的是反話。
- **為什麼要記**：evidence token 是給人讀的覆蓋索引。名字與斷言相反時，下一個人會照名字判斷覆蓋範圍，以為「不發卡」還有測試在守——實際上沒有。這是 K-42 那一類「報告是綠的，但綠的意思跟你以為的不同」。
- **修法**：新增一條 P2-A 的 UI 契約驗多實例顯示（目錄總數 53 → 54，`run_ui_sim.ps1` 寫死的總數要一起改）；token 改成表達現行斷言的名字。兩件事都要同步 `測試指南.md` 的條目與數字。
- **2026-08-16 進度**：**第 1、2 點均已結案。** `p1af_29` 拆出 `p1af_29_night_paid` 變體修正 token 名稱（`8a1ccb8`）；新增 `p2a_01_madness_hand_display` 契約（`p2a_multi_madness.json` 狀態含 3 張發狂卡分別為 7, 5, 3 天倒數），驗證各自獨立按鈕、各自顯示天數、點擊彈窗詳情與關閉狀態不變。契約矩陣擴充至 54 條（67 變體），UI 模擬全套 54/54 全綠。

### K-52 `_set_failure_status()` 的優先序改反了，資料寫的 `reject_reason` 被丟掉

- **Phase**：P2-A（順手改到的既有 UI 行為）
- **狀態**：**已修並複驗通過**（`259a9f0`）
- **修復時機**：**P2-B 動工前**。一行的事，而且有實質玩家影響。
- **問題**：`8a1ccb8` 接通夜間標記提示文字時，順手改了同一個檔的 `_set_failure_status()`（`location_panel.gd`），把理由的優先序倒過來：

  | | 誰贏 | 退而求其次 |
  |---|---|---|
  | 舊 | `reason_text`（資料寫的 `reject_reason`） | `_REASON_CODE_TEXTS[code]` → 原始 code |
  | 新 | `_REASON_CODE_TEXTS[code]` | `reason_text` |

  `try_place()` 對 LOCKED 槽回的是 `reason_code: "locked"` ＋ `reason_text: <資料的 reject_reason>`（`game_state.gd` 的 `_check_slot_state`）。新寫法讓 `_REASON_CODE_TEXTS["locked"]` = 「條件不足」永遠贏，**資料寫的那句話再也不會出現在畫面上**。其餘 code（`not_held`／`not_accepted`／`action_spent`……）的 `reason_text` 本來就是空字串，所以只有 `locked` 這條路有差。
- **為什麼要修**：`lint_missing_reject_reason`（lint 2）存在的唯一目的，就是逼資料為每個會灰掉的槽寫出具體理由；企劃書第十七節的硬規則也寫「灰掉一定要附理由」。把它換成通用句等於讓那條 lint 白做。`panel_builder.gd` 的 `_REASON_FALLBACK` 命名成「（條件不足）」正是因為它應該是**退路**，不是預設。
- **這條沒有測試守著**：全專案沒有任何案例斷言 `_FMT_PLACE_FAILED` 的輸出，所以這個改動不是被測試逼出來的，改回去也不會有測試變紅。**修的時候順手補一條**。
- **修法**：還原成「`reason_text` 非空就用它，否則查 `_REASON_CODE_TEXTS`，再否則印原始 code」。原始 code 那層 fallback 也要留著——查不到的 code 印出來，比印空字串好抓。
- **兌現紀錄**：commit `259a9f0`。挑字串那段抽成 `static func failure_text(result: Dictionary) -> String`，`_set_failure_status()` 只負責套 `_FMT_PLACE_FAILED` 與顯示；`location_panel` 的其他部分沒有動。`reason_text` 多做一次 `strip_edges()`，純空白也會落到第 2 層。回歸測試進 `test_p2a.gd` 第 10 組 `_test_failure_text_precedence()`，5 條斷言：Tier 1 用 `reason_code: "locked"` ＋ 非空 `reason_text`（正是被改壞的那條路）、Tier 2 兩個 code、Tier 3 未知 code、以及兩者皆空的邊界。全綠。

### K-53 45 天走查會開夜間標記了，但對開了什麼零斷言

- **Phase**：P2-A（測試）
- **狀態**：**已修並複驗通過**（`259a9f0`）
- **修復時機**：**P2-B 收尾前**，最遲不得晚於 P2-E——P2-E 要對的就是這些數字。
- **問題**：`8a1ccb8` 讓 `playthrough_greedy.gd` 的 `execute_night_phase()` 每夜貪心開一個未開的收費標記，走查因此真的會產生發狂卡（實跑手牌漲到 28／14，14 條 `hand is full` 警告）。**但整份走查腳本沒有一條斷言碰到 `night_markers_opened` 或發狂卡張數。** `open_night_marker()` 明天變成 no-op，走查照樣全綠——手牌少幾張，而跨輪重置斷言只看「重置後手牌只剩主角卡」，那條在有沒有發狂卡的情況下都會過。
- **這是第四次**：K-25（37 個行動格靜靜跳過）、K-26（走查那份不播殘響）、K-36（`test_p1f` 是走查的第二份拷貝）都是同一句話——**測試存在、測試全綠、測試沒有看那條路**。
- **修法**：走查收尾加兩條斷言，數字寫死不要用「大於 0」：
  1. 一輪結束時 `night_markers_opened.size() == 14`（全作收費標記數；來源是 `locations.json` 裡 `madness_cost > 0` 的筆數，**用資料算出來、不要在測試裡寫死 14**，否則之後調價就變成兩個事實來源）。
  2. 累計發放的發狂卡張數 == 開到的標記的 `madness_cost` 總和。P2-A 沒有任何東西會消耗發狂卡，所以這個數也等於重置前的手上張數。
- ⚠️ **不要為了讓手牌不溢位就去調 `hand_size`。** 走查漲到 28／14 是 P2-A 的正確結果（消耗要等 P2-C）。`tuning.json` 的 `_pending.hand_size` 已經記著「發狂卡開始佔格後要重驗」——那是 P2-C 之後帶著真實消耗曲線再看的事。
- **兌現紀錄**：commit `259a9f0`。`run_greedy_walk()` 在 `run_ended` 閉包裡捕獲重置前的 `night_markers_opened`、`_madness_counter` 與手牌裡的發狂卡（`end_run()` 先發 `run_ended` 再清 run 層，所以閉包看到的是重置前的值）。走查收尾對三個數做嚴格相等斷言，期望值從 `data/loader.locations` 現算（`layer == "night"` 且 `madness_cost > 0`），沒有寫死常數。**實測全部相符：收費標記 14／14 全開、累計發放 14 張、重置前手上 14 張。** 原本擔心「走查可能開不滿」——不用擔心，滿的。
- ⚠️ **第三條斷言（手上張數 == 發放張數）是 P2-A 專屬的。** P2-C 讓強制縱慾開始消耗發狂卡之後它必然不成立。**屆時要改的是斷言的形狀**（發放 == 手上 ＋ 已消耗），**不是把它放寬掉**——放寬就退回本條一開始要修的那個狀態。

---

### K-54 縱慾出口一輪只能用一次，主動縱慾上限 4 次

- **Phase**：P2-B（規則層）
- **狀態**：**已修並複驗通過**
- **問題**：`indulge()` 的最後一步寫 `slots_placed[beat_id::slot_id]`。出口 beat 的 `when` 是 `day_from: 1` → `day_to: 45`——**同一個 beat 撐滿整輪**，所以那個鍵只有 `end_run()` 會清。砸過一次東西，這一輪就再也砸不了；`PanelBuilder` 也照樣把它畫成 RESOLVED。
- **實測**：第 2 天砸東西 `ok=true`，第 3 天同一個槽 `ok=false reason=resolved`。逐時段掃過整輪，**一輪 90 個行動時段裡主動縱慾上限只有 4 次**（砸東西、暴食、泡湯，第 16 天後多一個暴力對人；奢侈缺錢卡、兩個性慾缺關係）。而一輪供給 14 張發狂卡。
- **打到的硬規則有三條**：
  1. `溫泉小鎮企劃.md` 第七節：「強制縱慾是主角自己動手的，**所以任何時刻都必須至少有一個出口點得下去**」。第一章砸過、暴食過之後池就空了。
  2. `實作規格書.md > 八`：「無條件出口（砸東西、食慾）**恆成立**」。
  3. `實作規格書.md > P2-E`：玩家 C 一輪主動縱慾 **14 次**。上限 4 次時對不上。
- **Lint 4 抓不到**：它驗的是資料（每天有沒有無條件出口），不是執行時狀態。池在執行時被 `slots_placed` 掏空，lint 4 一樣是綠的。這又是 K-01 家族——**測試存在、測試全綠、測試沒有走那條路**。
- **缺口的來源是文件**：`開發設計方針.md > P2-B` 原本第 8 步就寫著「記入 `slots_placed`」，實作照做。該行已於本次修正改掉並附理由。
- **修法**：`indulge()` 不寫 `slots_placed`。**縱慾的節流閥是行動格（一天最多 2 次），不是槽的一次性**——`slots_placed` 是為一次性敘事槽設計的，常駐出口 beat 不屬於那一類。修完上限從 4 回到 90（每個行動時段都能縱慾一次）。
- **回歸測試**：`test_p2b.gd` 三條——同一時段再縱慾被 `action_spent` 擋下（不是被 `resolved` 擋下）、換一個行動時段同槽可再用、跨日同槽可再用，並直接斷言 `slots_placed` 沒有 `exit_sanquan::x_smash` 這個鍵。**原本那條「重複放入已結算出口槽應被拒絕 (resolved)」把錯誤行為釘住了，已翻面。**
- ⚠️ **P2-C 動工時注意**：`pick_exit()` 的規格只求值 `condition` 與 `requires`，不看 `slots_placed`。現在規則層與它一致了——**不要為了「同一個出口不要連續挑到」又把一次性加回去**，那會再一次掏空挑選池。要做「不連續重複」是另一件事，走權重或冷卻，不走 `slots_placed`。

---

### K-55 泡湯在放置管線中途改 `phase`：推進鈕卡死、效果文字玩家看不到

- **Phase**：P2-B（規則層×UI）
- **狀態**：**已修並複驗通過**（`f7c95d9`）
- **問題**：`consume_action(n)` 在 `n > 1` 時直接把 `phase` 改成 `"evening"` 並發 `phase_changed`，而那是 `indulge()` 的第 4 步——**卡還在手上、`indulgence_count` 還沒加、效果還沒套**，訊號就發出去了。外面的 `main.gd` 於是在半完成的狀態上重畫整個畫面。兩個後果：
  1. **這一輪玩不下去**：`_route_view()` 把地點面板藏起來，但 `_advance_btn.disabled` 只有 `_on_panel_closed()` 會解開，而那條路要按面板上的返回鍵——面板已經藏了。實測 `advance.visible=true` 但 `advance.disabled=true`，而且沒有其他入口（`main.gd` 沒有 `_unhandled_input`、沒有快捷鍵）。
  2. **泡湯的效果文字玩家看不到**：`_route_view()` 當場清空 FlowText 並播當晚殘響；`indulge()` 之後才產生的文字被寫進已隱藏的面板狀態列。實測 FlowText 內容是「旁邊有人提起第 3 天下午發生過的一件小事」，不含泡湯那段。
- **根因是偏離契約**：`開發設計方針.md > P2-B` 寫的是「內部把當天剩下的行動時段**標成已用**」，實作改成了「**跳過**時段」。跳時段等於在原子管線中途對外廣播狀態。
- **修法**：`consume_action(n)` 改成 `action_spent = true` ＋ `_actions_spent_ahead += (n - 1)`，不動 `phase`、不發訊號；`advance_phase()` 進到下一個行動時段時扣掉一格，進到非行動時段時歸零。另補 `remaining_actions_today()` 真的算格數（原本用 `action_spent` 當代理，只在「兩格、成本 2」時剛好對）。UI 層再補一道：`_route_view()` 開頭無條件 `_advance_btn.disabled = false`，讓按鈕狀態只由目前檢視決定。
- **順帶**：`測試指南.md > P2-B` 那條「上午放進泡湯……**直接進 evening**」的描述已改——現在是按推進進到一個全灰的下午，再按一次才進 evening。
- **回歸測試**：`test_p2b.gd` 泡湯那組（時段維持 morning、`action_spent` true、推進後下午預扣、再推進進 evening）。`_actions_spent_ahead` 已進 `serialize()` / `deserialize()` 與 `end_run()` 重置。

---

### K-56 `soak_cards_cleared` 是必填 tuning 鍵，但程式一行都沒讀

- **Phase**：P2-B（數值單一事實來源）
- **狀態**：**已修並複驗通過**（`f7c95d9`）
- **問題**：`data/tuning.json` 有 `indulgence.soak_cards_cleared`，`data.gd` 也把它列進 `REQUIRED_TUNING_KEYS`，但全專案沒有任何一行讀它——`indulge()` 固定 `lose_card(card_inst_id)` 一張。現值剛好是 1 所以看不出差別，**改成 2 也不會有任何事發生**。同一段還有 `soak_phase_cost`：有讀，但 `consume_action()` 只分「是不是大於 1」，而「剩餘格數 ≥ 成本」的檢查寫成了 `action_spent == false`。
- **為什麼要記**：這違反 `AGENTS.md > 文件分工與單一事實來源`——可調數值一律住 `data/`。死值比沒有這個鍵更糟：下一個人會以為調它有用。
- **修法**：`indulge()` 與 `PanelBuilder` 都改讀 `Data.tuning()`；新增 `remaining_actions_today()` 供兩邊共用。
- ⚠️ **還有一半沒做完**：`soak_cards_cleared > 1` 時，多清的那幾張是**程式從手牌順序自動挑**的，而 `開發設計方針.md > P2-B` 第 5 步寫的是「**玩家挑哪張**」。現值為 1 所以碰不到。真的要調成 2 以上之前，得先決定 UI 怎麼讓玩家挑多張。

---

### K-57 `PROJECT_BRIEF.md` 的 P1-H 那一列被整列刪掉

- **Phase**：文件／進度 SSOT
- **狀態**：**已修**
- **問題**：`f7c95d9` 改 Phase 表時把 `| P1-H 手牌可讀性 | 🟦 | …待手動操作驗收 |` 整列刪掉，表格變成 P1-G 直接接 P2-A。P1-H 的「待手動操作驗收」狀態就這樣從單一事實來源上消失。
- **為什麼要記**：本表自己寫著「**子階段一律一列一個，不得合併**」，理由是「合併之後看不出卡在哪一個子階段」。整列消失比合併更嚴重——那個子階段連存在都看不到了。K-47 是同一類。
- **修法**：補回原列，內容一字不改。

---

### K-58 契約數 53 → 54 只改到 5 個地方裡的 2 個

- **Phase**：文件／進度 SSOT
- **狀態**：**已修**
- **問題**：`f7c95d9` 新增 `p2a_01_madness_hand_display` 之後，契約總數只在 `qa_contract_matrix.gd` 與 `run_ui_sim.ps1` 改成 54。`測試指南.md`、`開發設計方針.md`（兩處）、`PROJECT_BRIEF.md` 都還寫 53 條契約、66 變體。`PROJECT_BRIEF.md` 的當前進度段更是整段舊的——還寫著「12 套 headless」「下一步進 P2-B」。`測試指南.md` 那句「那個數字現在是 53」會直接誤導下一個要加案例的人。
- **為什麼會漏**：`測試指南.md` 自己寫著「同一組數字住四個地方，加減 checkbox 就要四處一起改」，但**那句話點的是「四個」，實際是五個**——它漏算了 `qa_contract_matrix.gd` 自己的檔頭註解，也沒把 `開發設計方針.md` 的兩處分開數。K-47 是同一族。
- **修法**：五處一起改成 58 條契約、76 個變體（P2-B 補 4 條契約 9 個變體）。**下次加契約要改的完整名單**：`qa_contract_matrix.gd` 檔頭註解、`run_ui_sim.ps1` 寫死的總數、`測試指南.md`、`開發設計方針.md`（表格列 ＋ 五步那段）、`PROJECT_BRIEF.md`（當前進度段）。

---

### K-59 強制縱慾的防吃債沒有測試守著

- **Phase**：P2-C（測試）
- **狀態**：**已修**
- **問題**：`GameState._settle_forced_indulgence()` 已於 `6e5e51a` 改成「先驗出口與槽、確定跑得起來，才 `pop_front()`」。修法是對的。但**沒有任何測試會發現它被改回去**——`Indulgence.pick_exit()` 回空字典、或 `_find_slot()` 找不到槽，只有資料壞掉時才會發生，而 `test_p2c.gd` 的 10 組沒有一組把出口池弄壞。把 `pop_front()` 移回 `pick_exit()` 之前，14 套 headless 照樣全綠。
- **影響**：這條守的是 `實作規格書.md > P2-C` 的「帳不豁免、不合併、不跳過」。回歸之後的症狀是**那張發狂卡離開 `forced_pending` 卻留在手上，這一輪再也不會被強制縱慾處理**，而且不會有任何錯誤訊息——玩家只會看到一張卡卡在手上不動。這是 K-01 家族：測試存在、測試全綠、測試沒有走那條路。
- **解法**：加一份壞資料 fixture，讓出口池整個求值不成立（例如六個出口的 `condition` 全部改成不可能成立），呼叫一次 `_settle_forced_indulgence()`，斷言四件事：① 回傳空 ② `forced_pending` 隊首**還在** ③ `action_spent` 沒被動到 ④ `indulgence_count` 沒有加。作法照 `tests/fixtures/broken/`（P2-B 的 lint fixture 已有先例），**不改正式 `data/`**。
- **備註**：`test_p2c.gd` 新增測試 11（`_test_forced_indulgence_debt_preservation_on_failure`），針對「出口池條件全不成立（`pick_exit` 空）」與「出口存在但 beat 遺失 slot（`_find_slot` 空）」兩種異常情境，嚴格斷言：① 回傳空行 ② `forced_pending` 隊首保留 ③ `action_spent` 維持 false ④ `indulgence_count` 與消除卡數不增加 ⑤ 手牌仍持有該發狂卡。並透過突變測試確認若將 `pop_front()` 提前會精確觸發測試失敗。

---

### K-60 走查認定「強制縱慾吃格」綁在出口有沒有文字

- **Phase**：P2-C（測試）
- **狀態**：**已修**
- **問題**：`playthrough_greedy.gd` 的 `diagnose_unplaced_phase()` 第 0 條，現在要 `action_spent` 為真**且 `last_forced_lines` 非空**才歸類成 `forced_indulgence`。第二個條件是 `6e5e51a` 補的（原本只看 `action_spent`，太寬——任何在時段開頭就吃掉行動格的機制，例如泡湯的 `_actions_spent_ahead` 預扣，都會被貼上這個標籤放行）。收緊是對的，但**依據挑錯了**：`last_forced_lines` 有沒有內容，取決於出口的 `on_place` 有沒有寫 `text`。那是文案，不是規則。
- **影響**：六個出口目前都有 `text`，所以走查是綠的。哪天新增一個只寫效果不寫文字的出口，或有人改寫時把某個 `text` 拿掉，走查就認不出那一格，判成非法空格並 exit 1——**程式沒壞、資料也沒壞，測試自己紅**。這種假紅比漏抓更花時間，因為第一時間會往規則層找原因。
- **解法**：改看規則層的事實——在該時段的 `advance_phase()` 前後比對 `indulgence_count`（或 `madness_cards_cleared`）有沒有加一。兩個欄位都是 `6e5e51a` 之後的既有狀態，不必新增任何東西。
- **備註**：`playthrough_greedy.gd` 已改為在 `run_greedy_walk()` 跨時段推進前後捕捉 `indulgence_count`，若時段進場時 count 增加則標記 `forced_m` / `forced_a`，並傳遞至 `execute_action_phase()` 與 `diagnose_unplaced_phase()` 比對 `action_spent and forced_indulged`，完全解耦文案依賴。全走查 47 放置 + 12 強制縱慾全綠通過。

---

### K-61 `tests/ui_sim/` 沒有任何 P2-C 契約

- **Phase**：P2-C（UI 驗收）
- **狀態**：待修
- **修復時機**：**K-48 收掉之後**。⚠️ **不要提前做**——UI 模擬現在同一份程式碼連跑三輪會有一輪紅（K-48 已重開），工具不穩時補的契約，綠或紅都說明不了事情。
- **問題**：P2-C 的驗收全部落在規則層。`測試指南.md > P2-C` 的 11 條已於 2026-08-20 打勾，證據是 `test_p2c.gd` 的 10 組；但 `tests/ui_sim/` 一條 P2-C 契約都沒有。P2-B 收尾時是同一種缺口，後來補了 4 條契約 9 個變體。
- **影響**：兩件玩家看得到的事現在沒有人驗——① 強制縱慾的文字有沒有真的印在地圖上方（`main.gd._play_forced_lines()`；依 `開發設計方針.md > P2-C` 的契約，關掉地點面板回地圖之後**要還在**）② 被強制縱慾吃掉的那一格，主角卡點下去是不是真的放不進去、理由對不對。這是 K-28 那一族：規則層全綠，畫面上是壞的。
- **解法**：K-48 收掉之後補，至少三條契約——文字播出與時段內留存、被吃掉那一格的放置拒絕與理由文字、同日兩張歸零時上午下午都被吃掉的顯示。補的時候**契約總數五處要一起改**，完整名單見 K-58。

---

## 二、已接受的邊界決定

**現在都不動手。** 分兩種：`不做`（實作已經是對的，看到不要「順手修掉」）與 `要做`（工作內容已在後面 Phase 的契約裡，到那個 Phase 自然做掉）。

一覽：

| # | 要不要做 | 兌現時機 |
|---|---|---|
| B-01 | 不做 | — |
| B-02 | 要做 | P1-F |
| B-03 | 要做 | P1-F |

### B-01 `madness` 卡放置不消耗行動格

- **要不要做**：不做。現行實作是對的。
- **兌現時機**：—（P2 做縱慾時是新增功能，不是修本條）
- **理由**：`實作規格書.md > 六` 的表格寫 `madness` 消耗行動格，但同一格註明「規則本身如此，**非放置機制**」。消耗發生在縱慾流程（第八節），不在 `try_place`。P1-D 的 `try_place` 只為 `protagonist` 呼叫 `consume_action()`，正確。本條存在的唯一目的是**擋住「看到表格就去 `try_place` 加一行」**。

### B-02 `run_ended` 目前可重複發射

- **要不要做**：要做。
- **兌現時機**：**P1-F**。契約在 `開發設計方針.md > P1-F` 結局 coda 三條；驗收在 `測試指南.md > P1-F`（「`run_ended` 一輪恰好發射一次」）。
- **做什麼**：`GameState.advance_phase()` 在第 45 天 evening 改成自己呼叫 `end_run()`，收尾不丟給 `run_ended` 的 listener。
- **理由**：現況是發完 `run_ended` 後不改狀態，再呼叫就再發一次。P1-A～P1-D 階段 `main.gd` 靠隱藏推進鈕擋住，沒有可觸發路徑，所以現在不急。**不要提前加「本輪已結束」旗標**——P1 沒有東西需要它，而 Phase 5 真結局會讓那個旗標的形狀再變一次。
- **兌現紀錄**：commit `55d62a8`，**已兌現**。`advance_phase()` 在第 45 天 evening 自己呼叫 `end_run()`；`end_run()` 內部順序照契約（emit `run_ended` → run 層重置 → 回第 1 天 morning），沒有加旗標。`test_p1f.gd` 第 8 項驗「連續推進 4 次後計數仍為 1」，`playthrough_greedy.gd` 也驗一輪恰好 1 次，兩條都綠。`main.gd._on_run_ended()` 不再隱藏推進鈕，第二輪走得下去。

### B-03 `data_facts.gd` 與 `flow_text.tscn` 尚未建立

- **要不要做**：要做。
- **兌現時機**：**P1-F**。兩者都已列在 `開發設計方針.md > P1-F` 的「動的檔」與 `by_design` 名單那條。
- **做什麼**：建 `flow_text.gd`（殘響逐行播出）；把 `by_design` 空格名單（第 1 天上午下午、第 32 天下午）抽到 `scripts/core/data_facts.gd` 常數，`verify_data.gd` 與走查腳本共用。
- **理由**：`開發設計方針.md > 全域結構決策` 的目標檔案樹寫的是「P1 結束時」的樣子，不是每個子階段的檢查表。`flow_text` 的需求在 P1-F；`data_facts` 的第二個讀者也是 P1-F 的走查腳本。現在抽出來是提早抽象——只有一個讀者的共用常數不是共用常數。
- **兌現紀錄**：commit `55d62a8`，**只兌現一半**。`scripts/core/data_facts.gd` 建好且真的有兩個讀者（`verify_data.gd` 與 `DataLoader`）——但**第三個預定讀者走查腳本沒有用它**（K-25）。`scenes/ui/flow_text.gd` 與 `flow_text.tscn` 建好且功能完整，**全專案零使用者**，殘響與結局文字仍寫在 `main.gd` 的 Label 上（K-28）。兩個缺口都另立條目追蹤，本條不重複。

---

## 附：本檔的驗證來源

- 2026-08-13～14：P1-A／B／C code review 與 headless 驗證。抓出的必修項（面板未走 `enter_beat`、beat 級 LOCKED 未傳導到槽）與契約缺口 B1–B4、序列化 JSON 往返，已於 commit `5d0df64`／`38601f9`／`9ce6cb9` 修復。
- 2026-08-14：對上述三個 commit 的實作 review，產出本檔 K-01～K-12。當時七套 headless 全綠（`verify_data`／`test_boot`／`test_game_state_p1a`／`test_hand_p1b`／`test_p1c`／`test_p1c_bugfix`／`test_p1d`），**全綠不代表沒有本檔的問題**——K-01 正是綠燈涵蓋不到的路徑。
- 2026-08-14：P1-E（commit `5c681f5`）驗證與 code review，產出 K-16～K-24，並讓 K-15 到期。八套 headless 全綠（上列七套 ＋ `test_p1e`），無迴歸；`測試指南.md > P1-E` 的七條驗收全數對得上測試。**這一輪最重的一條是 K-16，而它是資料問題不是引擎問題**——`choice_group` 在 P1-E 之前是死欄位，同面板規約沒有人會踩到；P1-E 讓它生效，三個行動格的規約缺口才變成有後果的東西。這是「引擎補完一個舊欄位時，要回頭驗那個欄位的資料規約」的第一個實例。
- 2026-08-14：P1-F（commit `55d62a8`）驗證與 code review，產出 K-25～K-37，並結案 K-14／K-16～K-19／K-22 與 B-02。十套 headless 全綠（上列八套 ＋ `test_p1f` ＋ `playthrough_greedy`）。**這一輪最重的兩條，綠燈都蓋不到**：走查腳本 37 個行動格靜靜跳過而沒有任何斷言（K-25）、晚間演出三份實作而走查那份不播殘響（K-26）。兩條都是 K-01 家族——**測試存在、測試全綠、測試沒有走那條路**。
- **本輪 review 自己犯的錯，記在這裡當教訓**：第一版把「沒東西花行動格」的行動格估成 24 格待決策，並宣稱 K-16 的名單漏了第 29 天下午。兩個都錯——逐格比對之後，其中 23 格整個時段只有 `fixed` beat，而「`fixed` 不吃行動格」是 `data/SCHEMA.md`、`實作規格書.md`、`溫泉小鎮企劃.md`、`subdocs/故事線/` 四份文件都寫過的既有規則，`d29_pm_invitation` 正是 `fixed`。**教訓：斷言「資料沒有涵蓋這件事」之前，要先 grep 故事線與企劃書，不能只看 `data/` 與根目錄四份文件。** 設計意圖經常只住在故事線裡。
