# 驗證後已知問題

## 本檔範圍與邊界

驗證與 code review 抓到、**尚未修**的問題清單，加上**已接受的邊界決定**（知道、刻意不修、寫下理由）。

- 修 bug 前先看本檔，避免重複診斷同一件事。
- 本檔屬 verifier 角色。implementer 修完某條之後，在該條標 `已修` 並附 commit 短 hash，不要直接刪除——刪掉就查不到「這件事發生過」。
- **本檔不放可調數值、不放設計理由**。數值住 `data/tuning.json`；為什麼住 `溫泉小鎮企劃.md`；該做成什麼樣住 `實作規格書.md`；怎麼做住 `開發設計方針.md`。本檔只放「現在跟契約不符的地方」與「怎麼補」。
- 引用程式碼**用函式名不用行號**（行號隨編輯漂移，見 `AGENTS.md > 文件查閱規則`）。

編號規則：`K-nn` 待修、`B-nn` 已接受的邊界決定。編號只增不減，修好的條目留在原位標記狀態。

**每條的第一件事是「要不要做、什麼時候做」，理由排在後面。** K 條目用 `Phase` ＋ `狀態` ＋ `修復時機`；B 條目用 `要不要做` ＋ `兌現時機`。讀的人先要的是行動，不是論證。

⚠️ **`Phase` 是「缺口所在的階段」，不是「什麼時候修」。** 兩者常常不同——K-15 的缺口在 P1-D，但要等 P1-E 的 `choose()` 定案才動它。什麼時候修一律看 `修復時機` 欄位。`狀態` 為 `已修` 時 `修復時機` 不再適用，改看 `備註` 裡的落地紀錄。

**review 講出口的每一條都要當場進本檔，不分大小。** 只在對話裡提過的「小事」會被忘記——`_play_evening()` 自己重寫 beat 篩選這件事在第一輪 review 就看到了，沒進清單，兩輪之後才以 K-14 補記。判斷優先度是 `狀態` 欄位的事，不是決定要不要寫進來的門檻。

---

## 一、待修清單

**還沒修的：**

| # | 標題 | Phase（缺口所在） | 狀態 | **修復時機** |
|---|---|---|---|---|
| K-25 | 走查腳本不判斷「全 `fixed` 時段」也不比對名單，37 個行動格靜靜跳過 | P1-F（測試） | 已修 | **P1-F**（`playthrough_greedy.gd` 動態診斷 90 時段合法性） |
| K-26 | evening 演出有三份實作，走查那份不播殘響 | P1-F | 已修 | **P1-F**（下沉至 `GameState.play_evening` / `play_night_fixed`） |
| K-27 | lint 3 只查 choice 槽沒查比對槽（且判斷單位要改成面板）；`d42_morning_ask_paper` 排錯天、第一輪永遠出不來；`d28_morning_xiaowu` 是第二個活案例 | P1-F（lint）／資料層 | 已修 | **P1-F**（`lint_free_slot_rules` 面板分組檢驗、`d28_morning_xiaowu` 補主角卡槽） |
| K-28 | 第 45 天結局 stub 文字寫上去立刻被蓋掉；夜間 fixed beat 文字沒容器；`flow_text` 建了沒人用 | P1-F | 已修 | **P1-F**（`main.tscn` 整合 `FlowText` 容器，渲染殘響、入夜 fixed 與結局 stub） |
| K-29 | 六條 K 與 B-02／B-03 標了已修，但內文沒更新、沒備註、沒回歸測試 | 文件／測試 | 已修 | **P1-F**（`test_p1d` / `test_p1e` 補齊 K-17、K-18、K-19、K-22、K-27 回歸測試） |
| K-08 | 「（條件不足）」重複住兩個檔 | i18n 管線 | 待修 | **i18n 管線**——等 key 命名定案一起收 |
| K-24 | 規格書狀態表把「不選」寫成 `choices` 的一種值 | 文件 | 待修（低） | **下一次動規格書第三節時**——純措辭 |
| K-30 | K-14 收攏三處之後，夜間新增三處自己比對 `when` | P1-F | 已修 | **P3-C**（`bdfa5fd`：`night_beat_candidates()`＋`resolved_night_content()` 兩層唯一入口；`panel_builder.gd`／`game_state.gd`／`main.gd` 已 grep 確認無殘留 night `when` 掃描） |
| K-31 | `verify_data` 把行動格覆蓋檢查跑了兩次 | P1-F | 已修 | **P1-F**（刪除 `verify_data.gd` 內重複的 inline 行動格迴圈） |
| K-32 | 文件寫的 `d3_pm_couple` 在資料裡不存在 | 文件 | 已修 | 2026-08-15 四處全改（比原記的三處多一個 `待決事項.md`） |
| K-33 | 夜間推進一律呼叫 `sleep_night()`，不管玩家是否已選地點 | P1-F | 已修 | **P3-C**（`bdfa5fd`：`resolve_night_advance()` 在 chosen 非空時直接 `advance:true`，不呼叫 sleep；`test_p3c` 第 10 組 Case C 以同夜有 sleep 內容的 D24 狀態反證） |
| K-34 | `available_locations()` 夜間分支不檢查地點級 `requires` | P1-F | 已修 | **P3-D**（`0047aaa`：夜間 gate 走 `ConditionEval.eval(loc["requires"])`，不成立仍列出並標 LOCKED＋理由）；**P3-F**（`72b84ca`：`test_p3f` 測試 1 對六個帶門檻 row 逐一斷言 `can_enter=false`、`reason_code=="locked"`、`reason_text` 非空） |
| K-35 | `PanelBuilder` 夜間收費地點回傳資料裡不存在的假 beat id | P1-F | 已修 | **P3-B**（`2ef58f3`：`_REASON_NIGHT_LOCKED_STUB` 與合成 beat 移除）；**P3-F**（`72b84ca`：`test_p3f` 測試 1 對 28 個夜間地點的 `build()` 輸出逐一回查 `beats_by_id`，補上 K-119 指出的空斷言） |
| K-36 | `test_p1f` 第 10 項是走查腳本的第二份拷貝 | P1-F（測試） | 已修 | **P1-F**（`playthrough_greedy.gd` 抽 `run_greedy_walk` 供 `test_p1f` 共用） |
| K-37 | `map_list.gd` 檔頭仍寫「白天地點清單」 | P1-F | 已修 | **P1-F**（已更新檔頭註解為「地點清單（白天與夜間共用）」） |
| K-38 | `playthrough_greedy.gd.uid` 未進版控（K-23 第二次發生） | P1-F | 已修 | — |
| K-39 | ~~走查數字在同一份程式碼上跑出兩種結果~~ → **誤報**，原因是跨 commit 測量 | P1-F（測試） | 誤報（已釐清） | — |
| K-40 | 夜間演出文字不裁切也不捲動，現在不疊字是因為文字剛好夠短 | P1-F（UI） | 已修 | **P1-F**（`FlowText` 包 `ScrollContainer` 且開啟 `clip_contents`） |
| K-41 | UI runner 負向測試只驗非零退出、先撞錯誤分支，且覆寫正常案例報告 | UI 模擬工具鏈 | 已修 | — |
| K-42 | UI runner 宣稱 47 contracts，但多個 P1-A～F 案例只驗到條目的一部分 | P1-A～P1-F（UI 驗收） | 已修 | — |
| K-43 | 壞 `--data-root` 仍讓子 UI 進 `_ready()`；state 驗證器兩份實作已分歧 | UI 模擬工具鏈／QA CLI | 已修 | — |
| K-44 | `-Case` 單案模式把半個 comparison group 判為通過，並仍回報 47 contracts | UI 模擬工具鏈 | 已修 | — |
| K-45 | `make_states.gd` 直接改 JSON 製造情境；D9 更在驗效果前預先塞入預期知識 | UI 模擬工具鏈（fixture） | 已修 | — |
| K-46 | `run_id` 只有毫秒時間戳，並行 launcher 可寫進同一個 run 目錄 | UI 模擬工具鏈 | 已修 | — |
| K-48 | hover 同步有時序不確定性，同一份程式碼連跑會紅在不同案例 | UI 模擬工具鏈 | **已修（2026-08-20 定位到真因）** | 送出移動後立刻讀 hover；守衛 `qa_hover_regression.gd` |
| K-49 | 契約一份報告都沒讀到時，launcher 索引 `$null` 自己爆掉 | UI 模擬工具鏈 | 已修 | — |
| K-50 | 殘留的 Godot 行程會污染下一輪 UI 模擬，讓失敗會傳染 | UI 模擬工具鏈 | 已修 | — |
| K-47 | 進度單一事實來源仍寫 17 variants／其餘 33 待做，與現行 58 variants 不符 | 文件／進度 SSOT | 已修 | — |
| K-51 | P2-A 沒補多實例發狂卡的 UI 驗收；`p1af_29` 的 evidence token 名稱與斷言相反 | P2-A（UI 驗收） | 已修 | — |
| K-52 | `_set_failure_status()` 的優先序改反了，資料寫的 `reject_reason` 被丟掉 | P2-A（順手改到的既有 UI 行為） | 已修 | `259a9f0` |
| K-53 | 45 天走查會開夜間標記了，但對開了什麼零斷言 | P2-A（測試） | 已修 | `259a9f0` |
| K-54 | 縱慾出口一輪只能用一次，主動縱慾上限 4 次（供給 14 張） | P2-B（規則層） | 已修 | 本次修正 |
| K-55 | 泡湯在放置管線中途改 `phase`：推進鈕卡死、效果文字玩家看不到 | P2-B（規則層×UI） | 已修 | `f7c95d9` |
| K-56 | `indulgence.soak_cards_cleared` 是必填 tuning 鍵，但程式一行都沒讀 | P2-B（數值單一事實來源） | 已修 | `f7c95d9` |
| K-57 | `PROJECT_BRIEF.md` 的 P1-H 那一列被整列刪掉 | 文件／進度 SSOT | 已修 | 本次修正 |
| K-58 | 契約數 53 → 54 只改到 5 個地方裡的 2 個 | 文件／進度 SSOT | 已修 | 本次修正 |
| K-59 | 強制縱慾的防吃債沒有測試守著，改回舊寫法 14 套照樣全綠 | P2-C（測試） | 已修 | `test_p2c.gd` 測試 11 補齊出口失敗與槽遺失斷言（含突變驗證） |
| K-60 | 走查認定「強制縱慾吃格」綁在出口有沒有文字，改文案會讓走查假紅 | P2-C（測試） | 已修 | `playthrough_greedy.gd` 改比對推進前後之 `indulgence_count` |
| K-61 | `tests/ui_sim/` 沒有任何 P2-C 契約，操作層一條都沒驗 | P2-C（UI 驗收） | 已修 | 補 3 條契約（58→61）、2 個狀態；突變檢查過 |
| K-62 | `run_ui_sim.ps1` 靠 `Set-Location` 決定測哪個專案，但 Godot 是用 `ProcessStartInfo` 起的 | UI 模擬工具鏈 | 已修 | `$psi.WorkingDirectory = $projectRoot` |
| K-63 | 「第一輪碰不到 BE」那條沒玩任何一輪，只斷言 tuning 值——還把可調數值釘死 | P2-D（測試） | 已修 | `test_p2d.gd` 改走 `PlaythroughGreedy` 走查並動態比較 `tuning.madness_cap` |
| K-64 | 視野門檻驗在合成 beat 上，真資料那三處寫錯鍵名照樣綠 | P2-D（測試） | 已修 | `test_p2d.gd` 補齊 `ch2_nights.json` 與 `indulgence_exits.json` 真資料顯隱斷言 |
| K-65 | `EffectApply.apply()` 在 BE 觸發後仍繼續套 `flag`，會寫進新的一輪 | P2-D（規則層） | 已修 | `ba6f9b8`：`run_generation` 偵測重置，EffectApply 即停；P4-B 測試同時守同筆 flag、跨筆 report 與文字不污染新輪 |
| K-66 | cap 數 `madness_clock.size()`，方針契約寫的是「`hand` 內 madness 數」 | P2-D（契約對齊） | 已修 | `9e342de`：`_check_madness_cap()` 改數 `hand` 內 `_card_base_id() == "madness"` 的張數 |
| K-67 | BE 顯示測試直接呼叫 `main._on_run_ended()`，訊號接線斷掉照樣綠 | P2-D（測試） | 已修 | `9e342de`：`test_p2d.gd` 改以 `gain_card()` 撞破 cap，由 `GameState` 自己發 `run_ended` |
| K-68 | `tests/ui_sim/` 沒有任何 P2-D 契約，視野門檻的顯示／不顯示沒驗到畫面 | P2-D（UI 驗收） | 已修 | 三腳齊：`bdfa5fd` 讓 `p2d_01_vision_zero`／`_three` 改查畫面「二樓有人在走」（2 張看不到、3 張看得到）；**第三腳「清回 2 張又消失」於 `0047aaa` P3-D 補**——`p1af_30` 在 D24 停拍後 `lose_card` 一張，斷言 `resolved_night_content` 不再選中 `d24_night_bleed` |
| K-69 | 睡覺解析出來的 beat 文字被丟掉，玩家永遠看不到 | P1-F（UI×規則層接線） | 已修 | **P3-C**（`bdfa5fd`：`_on_advance_pressed()` 夜間分支把 lines 寫進 `FlowText` 並停一拍；`p2d_01_vision_three` UI 契約以真實點擊驗畫面上讀得到「二樓有人在走」且該次不換日） |
| K-70 | `p2d_02_be_screen` 的 `be_map_hidden` 只查地點面板，沒查地圖也沒查推進鈕 | P2-D（UI 驗收） | 待修（低） | **下次動 `p1af_cases.gd` 時順手** |
| K-71 | 契約數 61 → 63 與 headless 套數 15 → 16 又只改到一部分（K-58 同族第二次） | 文件／進度 SSOT | 已修 | 本次修正 |
| K-72 | `test_p2_sim.gd` 兩處自行重算：發狂卡認定與最重級判定 | P2-E（測試） | 待修（低） | **下次動 `test_p2_sim.gd` 時順手** |
| K-73 | 第一輪走完所有收費 row 的玩家，第二輪沒有新的發狂卡供給 | P3-B（設計真值） | 已修（文件） | 本次定案並掛回待決 25／26 |
| K-74 | D15 fixed 讓 `n_plaza` 永久免費，第一輪供給存在 13／14 兩種策略基準 | P3-F（驗收條目） | 已修（文件） | 本次定案雙基準，P3-F 重跑 |
| K-75 | P3-D 同一節兩條打架：「chosen 非空時只回 chosen」vs「已選擇後仍可查看詳情」 | P3-D（規格） | 已修（文件） | 本次定案保留全清單、全部入口停用 |
| K-76 | 夜間 resolver 的 condition／requires 求值與 phase 過濾未定義 | P3-C（契約缺口） | 已修（文件） | 本次補候選層＋GameState 求值層 |
| K-77 | 夜間 resolver 與既有 `DataFacts.beat_matches_time()` 職責重疊沒交代 | P3-C（契約缺口） | 已修（文件） | 本次拆清 dated 判準與夜間集合解析 |
| K-78 | lint 編號倒序：P3-B 引入 lint 14、P3-C 引入 lint 13 | 文件／規格 | 已修（文件） | 本次對調為 P3-B 13、P3-C 14 |
| K-79 | lint 12 對 teaser row 的 `chapter`／`earliest_night` 一致性語意未寫明 | P3-A（lint 規約） | 已修（文件） | 本次明定 teaser 仍守章節一致性 |
| K-80 | `opened_n_*` 退場範圍不精確，聚會旗標不可直接換成 `night_seen` | P3-B（方針措辭） | 已修（文件） | 本次逐旗標定義遷移方式 |
| K-81 | 測試指南 P3-C 的 `laozeng_patrol_d24` 是 flag 不是 beat id，與相鄰句子放在一起易誤讀 | 文件措辭 | 已修（文件） | 本次改寫句子 |
| K-82 | 睡覺停拍那一拍可以繞過「一夜一地點」 | P3-B／P3-D（規則層） | 已修（文件） | 本次補進規格書第九節、方針 P3-B／P3-D 與測試指南兩條反證 |
| K-83 | P3-B 的「顯示通用結果」驗收在該階段做不到，機制被方針排在 P3-C | P3-B／P3-C（階段分界） | 已修（文件） | 本次把通用結果整條移到 P3-C 驗收 |
| K-84 | K-78 只修一半：`實作規格書.md` 的 lint 編號總表 13／14 仍是舊的，與全部其他段落相反 | 文件／規格 SSOT | 已修（文件） | 本次對調總表兩條，並同批補上 K-87／K-90 的範圍 |
| K-85 | lint 12 沒守住一般 night row 的 `earliest_night`／`chapter`／`madness_cost` 必填與型別 | P3-A（lint 規約） | 已修（文件） | 本次補進 lint 12 規約與拼錯欄位名的負向 fixture |
| K-86 | `enter_night_location()`／`confirm_night_alignment()` 回單一 `reason`，重演 K-03 | P3-B／P3-E（API 形狀） | 已修（文件） | 本次三個簽名改雙欄，並升為 P3 全域結構決策 |
| K-87 | lint 13 的掃描範圍不含槽級 `condition`／`requires`／`on_place` | P3-B（lint 規約） | 已修（文件） | 本次改為遞迴掃整份含 `slots[]`，測試補槽級反證 |
| K-88 | lint 11 缺「兩個不同 counterpart 共用同一張卡」的負向案例 | P3-A（測試覆蓋） | 已修（文件） | 本次 lint 11 改成雙向單射，測試補該類 fixture |
| K-89 | P3-A 方針只要 2 個壞 fixture，測試指南列了 4 類錯誤 | P3-A（測試覆蓋） | 已修（文件） | 本次方針改「每類各一個獨立 fixture」，不寫死數量 |
| K-90 | 沒有任何一道檢查擋「同一夜出現兩個 night-layer fixed 地點」 | P3-C（lint／規則層） | 已修（文件） | 本次進 lint 14 與規格書 lint 總表，並加 helper 二層保險 |
| K-91 | P3-D 改 `available_locations()` 語意，但兩條既有 UI 契約斷言正好相反且不在動的檔清單 | P3-D（UI 契約） | 已修 | `0047aaa`：`p1af_29_night_resolution`／`_night_paid` 兩條「其他地點不可見」斷言反轉為「仍可見、`night_enter::<id>` disabled、點擊零變化」，訊息一併改；`make_states.gd`／`playthrough_greedy.gd` 不再依賴清單收斂 |
| K-92 | `saw_n_gathering_intro` 要寫在哪幾個章節變體沒寫明 | P3-B（措辭） | 已修（文件） | 本次明定 ch2 與 ch3 兩個變體都寫 |
| K-93 | `實作規格書.md > P3-F` 把已結案的 K-37 列為待收 | 文件／進度 SSOT | 已修（文件） | 本次刪掉那半句 |
| K-94 | `開發設計方針.md` 的 UI 表寫「K-68 結案」，與已知問題和測試指南相反 | 文件／進度 SSOT | 已修 | K-68 於 `0047aaa` 三腳齊備、真正結案，本次把方針 UI 表那一格改回 ✅ 並同步本檔 K-68 狀態 |
| K-95 | `subdocs/卡牌/知識卡.md` 是衍生索引，P3-A 新增 10 張卡後沒排進任何階段重建 | P3-A（衍生文件） | 已修（文件） | 本次進 P3-A 動的檔＋測試指南 code review 條目；**索引本身在 P3-A 重建** |
| K-96 | `AGENTS.md > Layer 3` 列的四個 `subdocs/` 目錄不存在 | 文件／開場指引 | 已修（文件） | 本次兩處清單改成實際的三個目錄 |
| K-97 | `CODEX_HANDOFF.md` 停在 P1-G，落後五個子階段 | 文件／交接 | 已修（文件） | 本次整份改寫為當前狀態，並把「每個子階段收尾更新」寫進 P3 全域結構決策 |
| K-98 | P3-B 八個 `reason_code` 只有 `already_chosen`／`already_slept` 兩個有測試條目 | P3-B（測試覆蓋） | 已修（文件） | 本次補六碼拒絕矩陣與 `too_early`／`locked` 順序反證；**測試本身在 P3-B 實作時才寫** |
| K-99 | P3-E 只寫「拒絕時狀態零變化」，`confirm_night_alignment()` 的 `reason_code` 沒有封閉集合也沒有負向矩陣 | P3-E（API 形狀／測試覆蓋） | 已修（文件） | 本次定六碼並補繞過 UI 的負向矩陣＋offer／confirm 對齊反證；**測試本身在 P3-E 實作時才寫** |
| K-100 | P3-A 的「變更前後同一份 checkpoint」沒規定產生方式、名稱與前置狀態，重跑性不足 | P3-A（驗收可重跑性） | 已修（文件） | 本次定 `p3a_night_baseline` 情境、D14 night 前置與固定檔名；**baseline 本身要在 P3-A 動資料之前產生** |
| K-101 | P3-F 把 verifier-owned 的 `測試指南.md` 列入 implementer 動的檔，與角色分工衝突 | 文件／角色分工 | 已修（文件） | 本次移出清單、五處總數拆兩手，並補定 `PROJECT_BRIEF.md` 歸 verifier |
| K-102 | 已結案的 K-37 仍留在 P3-F 的測試指南與方針收尾清單（K-93 只修一處） | 文件／進度 SSOT | 已修（文件） | 本次兩處各刪 `37` 並註明已於 P1-F 結案 |
| K-103 | `test_p3a.gd` 的 `night_location_names` 是死變數，且僅比對 4 個寫死名稱而非動態比對 28 個夜間名稱 | P3-A（測試覆蓋） | 已修 | `test_p3a.gd` 改動態讀入 28 個夜間名稱並逐一檢驗對位卡文字無劇透 |
| K-104 | `_test_night_names_review()` 宣稱「28 個夜間名稱審查通過」屬過度宣稱（程式僅驗字串非空與改名，beat 對照屬 code review） | P3-A（測試措辭／審查落檔） | 已修 | `test_p3a.gd` 修正斷言日誌，28 處人工審查對照表記錄於附錄 |
| K-105 | `make_states.gd` 的 baseline 後置條件寫死 `madness < 7` 違反「可調數值只住 data/」 | P3-A（數值 SSOT） | 已修 | 分兩批：`41d2d8e` 改讀 tuning 但參數沒人傳（只靠備援分支生效）；後續把四個呼叫端全部補傳 `data_node`，死參數消失 |
| K-106 | baseline 挑 paid row 採第一個 `madness_cost > 0`（K-74/P3-F 禁用取法），順序一動易假紅 | P3-A（測試穩定度） | 已修 | `make_states.gd` 明確指定目標地點為 `n_ahong_1` |
| K-107 | K-100 的「先產 baseline 再改資料」在執行時因編輯順序未守住，該條驗收在錯誤順序下會恆真 | P3-A（流程紀律） | **不修（已接受）** | 兩次 baseline 都是資料改完才產。結論成立靠 verifier 事後以 `870db64` 重跑對過，**不是靠這條驗收**。降為紀律條目，見「二、已接受的邊界決定」 |
| K-108 | Lint 12 的 teaser 分支只在 `earliest_night` 與 `chapter` 都非 null 時才驗一致性，缺一欄會漏過 | P3-A（資料檢查規約／測試覆蓋） | 已修 | 分兩批：`41d2d8e` 補 lint 沒補 fixture；後續補 `p3a_teaser_missing_time_column`（兩 row 各缺一欄、分兩條斷言），並把時間欄位檢查上提為 teaser 與一般 row 共用 |
| K-109 | 阿宏 `reject_reason` 5 與 6 共用文案未反映各自路線知識；測試過度拷貝精確文案易假紅（K-60 同族） | P3-A（資料層／測試語意斷言） | 已修 | `locations.json` 區分 5（第一段）與 6（第二段）文案；`test_p3a.gd` 改驗語意關鍵字 |
| K-110 | fixture 6 只做了「`madness_cost` 拼錯」，測試指南同一條列的缺 `earliest_night`／缺 `chapter`／型別錯誤／負值四種都沒有反證 | P3-A（測試覆蓋） | 已修 | 補四個獨立 fixture（`p3a_missing_earliest_night`／`p3a_missing_chapter`／`p3a_invalid_type_field`／`p3a_negative_madness_cost`），`test_p3a.gd` 各斷言自己的錯誤字串，負向 fixture 由 6 個增為 11 個 |
| K-112 | 跑一次 UI 模擬就把 K-100 的 baseline 重生一遍——產生掛在 `generate_all_states()` 裡，`run_ui_sim.ps1` 第 2 步必經 | P3-A（驗收工具結構） | 已修 | `4b0ba1c`：baseline 改成只有明示 `--p3a-baseline` 才產生；K-124 verifier 複驗時一般 fixture 生成 exit 0，且 baseline mtime 前後不變 |
| K-113 | fixture 11 用 `"chapter": "one"` 驗「型別錯誤」，但 `int("one") == 0` 會先被範圍檢查攔下，型別檢查其實沒被證明過 | P3-A（測試覆蓋） | 已修 | 改成兩個各自只踩一道防線的 row：`"chapter": "2"`（範圍內的字串）與 `earliest_night: 15.5`（範圍內的小數），並分兩條斷言 |
| K-114 | `p2d_near_cap` 情境寫死「6 張」與 `madness#6`／`madness#7`，把可調的 `madness_cap` 釘死在 7 | P2-D（測試 fixture／數值 SSOT） | 已修 | 產生端與後置條件都改讀 `Data.tuning("madness_cap")` 現算 `cap - 1`（K-63 同族） |
| K-111 | 測試指南 P3-A 的 teaser 那條混進了「第一、二章不列入可見集合」，可見性住 `available_locations()`，P3-A 驗不到 | 文件／階段分界 | 已修（文件） | 本次在 P3-A 該條註明由 P3-D 既有條目承接，P3-A 只驗資料與 lint（K-83 同族：同一件事指派給兩個階段） |
| K-115 | `開發設計方針.md > P3-B` 要求 UI 用檔頭 const 表把 `reason_code` 翻成玩家文案，但 P3-B 的 `main.gd` 沒有任何顯示拒絕理由的位置，該表是宣告了沒人讀的死常數 | P3-B／P3-D（階段分界） | 已修 | `0047aaa`：`location_panel.gd` 檔頭補 `_NIGHT_REJECT_TEXTS` 八碼 const 表，`show_night_details()` 於 `can_enter=false` 時渲染 `night_reason::<id>`，`reason_text` 非空優先、否則查表；`p3d_03`／teaser／阿宏三個案例驗理由可見 |
| K-116 | `main.gd._refresh_advance_hint()` 的夜間三態按鈕文字整段沒有任何測試覆蓋 | P3-C（測試覆蓋） | 已修 | `01ccff4`（`test_p3c` 新增第 12 組，載入真 `main.tscn` 讀 `AdvanceButton.text` 驗三態；verifier 重跑變異測試，刪掉 `elif` 分支後三條同時轉紅） |
| K-117 | `_record_forced_night_visit()` 拒絕覆寫後，`play_night_fixed()` 仍續播該 beat 且 meta-once 已先寫入；保險本身零測試覆蓋 | P3-C（規則層＋測試覆蓋） | 已修 | `01ccff4`（helper 改回傳 `bool`，`play_night_fixed()` 在 false 時 `continue`；`night_once_beats_seen` 寫入點移到到訪成功之後。`test_p3c` 第 10 組 7 條斷言；刪掉保險後四條轉紅） |
| K-118 | lint 14 判 night 時用 `str(wd.get("phase", "")) == "night"`，`when.phase` 寫成陣列就整條繞過 | P3-C（lint 覆蓋） | 已修 | `01ccff4`（`p_val is Array` 時用 `has("night")`，並補 fixture 5／6／7；刪掉陣列分支後 fixture 5 與 7 轉紅。負向 fixture 由 4 個增為 7 個） |
| K-119 | `test_p3c` 第 5 組掃 `loader.beats` 找 `<location>_locked` 是空斷言；同條驗收的「paid night panel 回傳真實 beat id」沒有 headless 斷言 | P3-C（測試覆蓋） | 已修 | `01ccff4`（第 5 組改對 `PanelBuilder.build()` 的輸出斷言：`n_ahong_1`／`n_source` 每個回傳 beat id 都要在 `beats_by_id` 裡，且 beats 為空即 fail，不再是空迴圈） |
| K-120 | `night_beat_candidates()` 的「定日候選」會收 `day_from`／`day_to` 區間 beat，規格寫的是「當日定日 beat」 | P3-C（契約精確度） | 已修 | `01ccff4`（候選層自加 `wd.has("day")` 且排除 `day_from`／`day_to`；第 1 組補區間反證） |
| K-121 | `_record_forced_night_visit()` 不看 `night_sleep_pending`，與 `enter_night_location()` 八碼拒絕矩陣的 `already_slept` 判準不一致 | P3-C（判準一致性） | 已修 | `01ccff4`（helper 加 `night_sleep_pending` 拒絕分支，與 K-117 同批；`test_p3c` 第 10 組另兩條斷言驗 sleep_pending 時 fixed 完全不生效） |
| K-122 | `subdocs/驗證/發狂卡機制模擬.md` 的視野天數與窗口仍是舊基準，與 `bdfa5fd` 改後的 `test_p2_sim` 期望值分岔 | P3-F（文件同步） | 已修 | **P3-F**（`98949f1`：模擬文件第四次重跑，總天數表／窗口表／對照表三處與 `test_p2_sim`／`test_p3f` 逐項對上） |
| K-123 | P3-C 的夜間按鈕三態只有 `test_p3c` 場景級證據，`tests/ui_sim/` 沒有對應契約，catalog 仍 63 | P3-C／P3-F（UI 驗收形式） | 已修 | **P3-F**（`72b84ca`：塞進 `p3d_05_sleep`，走 launcher 真實 input event 驗「直接睡」→「進入隔天」；塞既有契約故 catalog 未增） |
| K-124 | P4-A 的 D8 `n_manydoors` 發卡讓標準走查在 day8→9 提早帶入 madness 卡，靜默污染 `make_states.gd` 的精確張數 fixture（P2-A 實際 4≠3、P2-C 實際 2/3≠1/2、`p2d_near_cap` 撞 cap 後重置 day1），且 P4-C 的 `no_person` 後置條件另把「零人物卡」誤寫成 `hand.size()==1`；`run_ui_sim.ps1` 第 2 步卡死 | P4-A／P4-C（測試 fixture／走查衍生） | 已修 | `4b0ba1c`：8 組精確張數 fixture 逐案正規化、後置條件改成精確語意守衛、零人物卡改按 type 計數；verifier 複驗生成 exit 0、25 套 headless 全綠，完整 UI launcher 已能跑到底 |
| K-125 | D17 `d17_morning_phone` 首次取得人物卡時彈出委託教學 modal；`drain_beats()` 仍把被 modal 遮住的底層 `beat_advance` 視為可見並反覆送輸入，20 次後誤報無限迴圈 | P4-C（UI sim 工具鏈） | 已修 | `7a439f6`：`drain_beats()` 優先以真實輸入處理已知教學 modal、dialog 與 beat 分開計數，並補 D17 教學 observation 回歸；verifier 複驗 4 個原失敗變體及完整 UI sim 102／102 variants、79／79 contracts、0 failed |
| K-126 | 夜間定日遭遇（D8 `n_manydoors_ch1`）在真實遊戲中永遠不會啟動——`_check_fixed_encounter_for_current_phase()` 開頭直接排除 night，`play_night_fixed()` 收費播文字卻不呼叫 `start_encounter()` | P4-D（規則層接線） | 已修 | **P4-D**（`35f1f79`：`play_night_fixed()` 播完 fixed beat 後對帶 `encounter` 的 beat 呼叫 `start_encounter()`；verifier 變異測試拿掉該行 → `test_p4d` 轉紅） |
| K-127 | 遭遇自動啟動 hook 零測試覆蓋：把 `_check_fixed_encounter_for_current_phase()` 整段改成立即 `return`，26 套 headless 全綠 | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：新增 `_test_phase_hook_autostart`，D45 afternoon 走 `advance_phase()`、D8 走 `play_night_fixed()` 各驗一次自動建立 intro；廢掉 phase hook → 轉紅） |
| K-128 | 15 碼拒絕矩陣沒有任何一例比對前後 `serialize()`；在 `not_held` 分支偷改 `flags` 與 `blocked_slots` 後 26 套仍全綠 | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：15 碼矩陣每例前後 `serialize()` 逐字比對；在 `not_held` 分支注入副作用 → 轉紅） |
| K-129 | `encounter_view()` 的「不洩漏答案集合」與候選涵蓋整條零覆蓋；讓 `build_view()` 多回傳 `accepts`／`fallback`／全部 rounds 後 26 套全綠 | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：正向驗 candidates 涵蓋與 `source`，負向擋六個禁用 key；以 `accepts` 洩漏正解 → 轉紅。denylist 的殘留限制另立 K-147） |
| K-130 | `after_finish:"stay"` 不推進 零覆蓋，且 `test_p4d.gd` 檔頭第 12 項宣稱兩種都測（過度宣稱，K-104 同族）；讓 stay 也推進後 26 套全綠 | P4-D（測試覆蓋／措辭） | 已修 | **P4-D**（`35f1f79`：`_test_after_finish_stay_and_advance` 補 stay 案例斷言 day／phase 不變；讓 stay 也推進 → 轉紅） |
| K-131 | 拒絕順序驗收要求五個遭遇 mutation 入口各一組，實際只有 `respond_to_encounter` 一組；另兩組是 `enter_night_location`（不在那五個入口內） | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：五個入口共 10 組雙條件交集案例，逐條驗優先碼） |
| K-132 | `respond_to_encounter()` 把 `data_conflict` 排在 `card_not_submittable` 之前，與方針 P4-D 固定順序相反 | P4-D（契約偏差） | 已修（程式） | **P4-D**（`35f1f79`：`card_not_submittable` 已移到 `data_conflict` 之前，逐字符合方針）。**回歸測試仍缺，另立 K-145**——把順序換回去 26 套照樣全綠 |
| K-133 | `_finish_encounter()` 套完出口效果後無條件呼叫 `advance_phase()`，沒有 `delegate()` 那組 `run_generation` 守衛（K-65 同族）；現行資料三個出口只有 text+flag，暫不可觸發 | P4-D（規則層防禦） | 已修（程式） | **P4-D**（`35f1f79`：`_finish_encounter()` 加 `gen_before := run_generation`，世代變動即不推進）。**回歸測試仍缺，另立 K-145**——拿掉守衛 26 套照樣全綠 |
| K-134 | 方針寫「後續每個 round id 只能進一次」，`active_encounter` 只存當前 `round_id`、沒有 visited 集合，重進同一 round 會重複加 cost；現行線性圖靠資料形狀成立 | P4-D（規則層守衛） | 已修 | **P4-D**（`35f1f79`：`visited_round_ids` 進 `active_encounter` 與序列化，重進既訪 round 即 `data_conflict`；拿掉檢查 → 轉紅。回傳語意的殘留問題另立 K-146） |
| K-135 | `hand_size` 的 fallback 預設值分歧：`game_state.gd` 三處分別是無預設／7／7，`Encounter.build_view` 是 7，`test_p4d` 是 14，`tuning.json` 實際 14 | P4-D（數值 SSOT） | 已修 | **P4-D**（`35f1f79`：四處 fallback 統一為 14，與 `tuning.json` 一致；`_check_hand_overflow` 維持不帶預設） |
| K-136 | `_check_encounter_capacity_failure(enc)` 的 `enc` 參數整個沒被讀（死參數，K-105 同族） | P4-D（程式衛生） | 已修 | **P4-D**（`35f1f79`：`_check_encounter_capacity_failure()` 拿掉死參數，四個呼叫端同步） |
| K-137 | `Encounter._extract_base_id()` 與 `GameState._card_base_id()` 是逐字相同的第二份實作（K-14 同族） | P4-D（維護風險） | 已修 | **P4-D**（`35f1f79`：新增 `DataFacts.card_base_id()`，`Encounter._extract_base_id()` 改委派） |
| K-138 | `test_p4d.gd` 全部走合成 beat 與合成卡：`_create_mock_encounter_beat()` 直接寫 `loader.beats_by_id`、往 `loader.cards` 塞假卡且不還原；合成 beat 沒有 `fixed:true` 也不進 `beats_at()`，正式資料路徑一條都沒跑到 | P4-D（測試形式） | 已修 | **P4-D**（`35f1f79`：新增 `_clean_mock_beat()`／`_clean_mock_cards()`，每個案例結束還原；並補 D8／D45 真實資料路徑測試） |
| K-139 | 序列化往返測試自己 `append("dummy_card")` 造 attempted，不是由 `respond_to_encounter` 產生；且缺驗收要的「與未存檔路徑結果相同」對照，intro 那半還在同一個 gs 上 deserialize | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：attempted 改由真實 `respond_to_encounter()` 產生，並與未存檔對照組逐字比對最終 `serialize()`） |
| K-140 | 「victory／failure／escape 效果各只套一次」用 boolean flag 驗，套兩次與一次結果相同，驗不出重複套用 | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：出口效果改用可累加量；套兩次 → 轉紅） |
| K-141 | 「遭遇全程不消耗 action、不開縱慾或備用區」零覆蓋——`action_spent`／`indulgence_count`／`forced_pending` 在 `test_p4d.gd` 出現 0 次 | P4-D（測試覆蓋） | 已修 | **P4-D**（`35f1f79`：完整遭遇前後斷言 `action_spent`／`indulgence_count`／`forced_pending` 不變；偷設 `action_spent` → 轉紅） |
| K-142 | `playthrough_greedy.execute_action_phase()` 的遭遇處理只答一次就 return，多回合遭遇走不完；只試 hand、不試 knowledge／discard／escape，該時段一律計為 `all_fixed` | P4-D（走查覆蓋） | 已修 | **P4-D**（`35f1f79`：`solve_active_encounter_if_any()` 多回合迴圈，候選改讀 `encounter_view()`，含 discard／escape 後備） |
| K-143 | P4-D 順手刪掉 `playthrough_greedy.gd` afternoon 區塊的 `last_ind_count` 快照行（K-60 那組），追資料流後確認無功能影響，但屬本階段範圍外改動且讓四個快照點形狀不一致 | P4-D（範圍外改動） | 已修 | **P4-D**（`35f1f79`：afternoon 快照補回，且 `advance_phase()` 回 `ok:false` 時計入 `illegal_phases`） |
| K-144 | `test_p2_sim.gd` 自 P4-D 起整份失效：模擬在第 9 天因遭遇阻擋 `advance_phase()` 觸發 `assert` 而中止，`_run_simulation()` 回空 Dictionary，三個 `_verify_player_*` 又各自在第一行 `res["peak_madness"]` 崩掉並回 0，於是零條指標斷言執行、exit 仍為 0 | P4-D（整合測試失效） | 待修（阻斷） | **P4-E 開工前**——P2-E 的整合測試現在是空的 |
| K-145 | K-132（拒絕順序）與 K-133（世代守衛）的修正都沒有回歸測試：把順序換回去、把守衛拿掉，26 套照樣全綠；且測試把 `already_attempted > card_not_submittable` 標成「(K-132)」，實際 K-132 是 `card_not_submittable > data_conflict` | P4-D（測試覆蓋／標註） | 待修 | **與 K-144 同批** |
| K-146 | 遭遇 round cycle 偵測回 `{ok:false, reason_code:"data_conflict"}`，但此時已 append attempted、消耗卡片、套完 `on_resolve` 並跑完 `_finish_encounter()`——拒絕碼與「任何拒絕都不改狀態」的封閉語意衝突 | P4-D（契約語意） | 待修（低） | **P4-F**——與 K-134 的 lint 選項一起定 |
| K-147 | `encounter_view()` 的不洩漏檢查是六個 key 的 denylist 不是 allowlist：改用 `solution_cards` 之類的新欄位名洩漏正解，測試不會紅 | P4-D（測試強度） | 待修（低） | **P4-E**——UI 開始消費 view 時一起改成 key allowlist |

已修：K-01～K-07、K-09～K-23、K-25～K-33（K-30／K-33 於 P3-C 結案）、K-36～K-38、K-40～K-69、K-71、K-91、K-94、K-95、K-100、K-103～K-106、K-108～K-115、K-124～K-125（各條的 `備註` 有落地位置與回歸測試）。**K-48 已於 2026-08-20 定位到真因並結案**（前兩次都只修到症狀）。**K-65 於 2026-08-26 P4-B 複驗結案；K-68／K-91／K-94／K-115 於 2026-08-22 P3-D 結案；K-112／K-124 於 2026-08-27 隨 `4b0ba1c` 複驗結案，K-125 同日隨 `7a439f6` 複驗結案。**

**P3-A 收尾狀態（2026-08-27 更新）**：K-103～K-111 全數結案，負向 fixture 由 6 個增為 11 個，`test_p3a` 17 項全綠。**K-105／K-108／K-110 是「先修程式、隔一輪才補測試」的三條**——第一批交出來時三條的共同形狀都是「把這次的修改整段刪掉，測試不會紅」，第二批才補上反證。K-112 已隨 K-124 同批結案；K-107→B-04（不修）。
**待修（有時機）**：K-70（下次動 `p1af_cases.gd`）、K-72（下次動 `test_p2_sim.gd`）。**目前無 P4-C 關門 blocker。**

**K-103～K-109 為 2026-08-21 P3-A 實作後 review 產出並同日修復完成。**

**K-98～K-102 為 2026-08-21 第三輪複審產出，同日全數回寫（文件層），仍未動任何程式或資料。** 其中三條的東西本身要等對應階段才做得出來：K-98（六碼拒絕矩陣在 P3-B 實作時才寫成測試）、K-99（六碼負向矩陣在 P3-E 才寫）、K-100（baseline 檔本身必須在 P3-A 動資料之前產生，事後補產一律不算）。

**K-82～K-97 已於 2026-08-21 全數回寫（文件層），P3 開工閘清空。** 十六條全部改在 `實作規格書.md`／`開發設計方針.md`／`測試指南.md`／`AGENTS.md`／`CODEX_HANDOFF.md`，**沒有動任何程式或資料**。
其中三條的文件那一半做完了、**東西本身要等對應階段才做得出來**，不要看到「已修」就以為沒事：K-91（`p1af_29` 兩條斷言在 P3-D 實作時才反轉）、K-95（`知識卡.md` 在 P3-A 有卡之後才重建）、K-94（方針 UI 表要等 P3-C 補完畫面證據才改回 ✅）。
**P3 文件缺口已處理**：K-73～K-81 已依 2026-08-20 討論回寫；其中 K-74 的雙基準仍要等 P3-F 跑真流程取得完整時間軸，K-73 的第二輪內容供給仍是待決 25／26，不代表內容已完成。
**待修（早已排到後面 Phase）**：K-08（i18n 管線）、K-24（文件措辭）、K-34（等 P3-D 地點級 `requires`）、K-35（合成 beat 已消失，收尾證據等 P3-F，見 K-119）。**K-30／K-33／K-69 已於 2026-08-22 P3-C `bdfa5fd` 結案。**

**P3-C 收尾狀態（2026-08-22）**：分兩批。`bdfa5fd` 收掉 K-30／K-33／K-69 三條夜間老帳；第一輪 review 產出 K-116～K-123 八條，其中 **K-116 與 K-117 又是 P3-A 的 K-105／K-108／K-110、P3-B 那八條的同一個形狀——把這次的修改整段刪掉，測試不會紅**（`_refresh_advance_hint()` 的夜間 `elif` 刪除後四套測試全綠；`_record_forced_night_visit()` 的覆寫保險刪除後 `test_p3b`＋`test_p3c` 全綠）。K-117 另有規則層的一半：helper `return` 之後 `play_night_fixed()` 照樣播，而 `night_once_beats_seen` 在更前面就寫進去了。`01ccff4` 把 K-116～K-121 六條全數修完，`test_p3c` 由 10 組增為 12 組、69 條斷言，負向 fixture 由 4 個增為 7 個。**verifier 對修正逐條重跑變異測試**——刪 `_refresh_advance_hint()` 夜間分支／刪 chosen 覆寫保險／刪 lint 14 陣列 phase 分支，**三種全部如預期轉紅**（同樣三刀在 `bdfa5fd` 上是全綠）。剩 K-122（模擬文件，P3-F）與 K-123（按鈕三態缺 ui_sim 契約，P3-F），兩條都不擋 P3-D。

**P3-D 收尾狀態（2026-08-22）**：分兩批。`0047aaa` 實作夜間清單／詳情 UI——`PanelBuilder.location_summary()` 純 view-model、`location_panel.show_night_details()` 詳情面板、`main.gd` 拆「點清單→詳情／進入鍵→`enter_night_location()`」、`map_list` 改讀 summary 並移除 `_SUFFIX_EMPTY`；同批結 K-91（斷言反轉）、K-68 第三腳、K-115（`_NIGHT_REJECT_TEXTS` 接線）。第一輪 verifier review 抓到**同族缺口**：測試指南 13 條裡 9 條標 🤖 UI，但只有 headless view-model（`test_p3d`）證據，方針明列要動的 `tests/ui_sim/ P3-D cases` 沒補、catalog 仍 63——是 altitude 缺口不是假綠（`test_p3d` 經變異證實會咬）。`70f4fc5` 補齊 6 契約／10 變體（catalog 63→69），fixtures 走規則層入口（`enter_night_location()`＋`gain_card()`）並補後置條件。**verifier 獨立變異測試**：釘死 `is_aligned=false` → `p3d_02` 兩變體如期轉 5 紅；拿掉 `already_chosen`/`already_slept` 阻斷 → `test_p3d` 轉 2 紅，均還原乾淨。20 套 headless exit 0、UI sim 92 變體／69 契約／0 failed。K-68／K-91／K-94／K-115 結案。**下一步 P3-E 對位系統。**

> `Phase` 欄是缺口長在哪個階段，**不是什麼時候修**。K-15 的缺口在 P1-D，但要等 P1-E 才動——兩欄不同是常態。

### K-01 白天面板的 `on_enter` 結算住在 UI 層

- **Phase**：P1-C（缺口所在）／P1-F（爆點）
- **狀態**：已修
- **問題**：白天面板的 beat 呈現只有 `scenes/ui/location_panel.gd` 的 `_rebuild()` 會呼叫 `GameState.enter_beat()`。`GameState` 與 `PanelBuilder` 都沒有等價入口。而 `開發設計方針.md > P1-F` 明訂走查腳本「不 instantiate 任何 UI，只呼叫 `PanelBuilder.available_locations()` 與 `GameState.try_place()`」——那條路上沒有人呼叫 `enter_beat`。結果是白天 37 個 `fixed` beat（其中 28 個帶 `on_enter`）的效果在 headless 走查中一次都不會結算，`beats_entered` 也不會寫入。違反 `實作規格書.md > 四`：「beat 的呈現管道只有一個入口……任何管道播出 beat 而沒結算 `on_enter`＝實作錯誤」。
- **影響**：P1-F 的貪心走查會安靜地跳過那 28 個 `on_enter`。`d17_morning_phone`、`d23_morning_awei_knocks` 的 `on_enter.gain` 發不出卡，後續 `requires` 判不過，第二章起連鎖鎖死。症狀看起來像資料錯誤，不像引擎錯誤——這是它難查的原因。
- **解法**：把「開面板＝結算該面板全部 OPEN beat 的 `on_enter`」下沉成規則層入口，例如 `GameState.open_panel(location_id) -> Dictionary`：內部先對 OPEN 的 beat 逐一 `enter_beat()`，再回傳 `PanelBuilder.build()` 的結果。`location_panel` 與 `playthrough_greedy.gd` 共用它，UI 不再自己呼叫 `enter_beat`。原則同 `開發設計方針.md > P1-F` 的 `end_run()` 那條：規則層的後置條件不可依賴外部呼叫者。
- **備註**：已實作 `GameState.open_panel()` 並於 `tests/headless/test_p1c_bugfix.gd`（A3）完成規則層驗證。

### K-02 面板 view model 在 `on_enter` 結算之前算好，同面板不重求值

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_rebuild()` 先呼叫 `PanelBuilder.build()` 取得 view model，之後才逐一 `enter_beat()`。`on_enter` 寫的 flag、發的卡，這一輪的 view model 看不到，而且不重算。違反 `實作規格書.md > 五`：「同面板內任何狀態變化（放卡、**效果結算**）後全面重求值」。
- **影響**：資料裡有一個活的實例——第 23 天上午、山泉閣。`d23_morning_awei_knocks`（`fixed`）的 `on_enter` 寫 `awei_sheltering: true`；同一面板的 `d23_am_settle_grandma` 的 `condition` 正是 `{"flag": "awei_sheltering"}`。第一次打開面板時「安置阿嬤」判定 HIDDEN、整個不出現，玩家要退出面板再進來才看得到。不是死鎖，是隱形步驟。
- **解法**：與 K-01 同一個修法——先結算再 build。K-01 修好本條即消失，不必分兩次改。
- **備註**：`GameState.open_panel()` 採先連鎖結算 `enter_beat` 後呼叫 `PanelBuilder.build()`，第 23 天上午山泉閣回歸測試已在 `test_p1c_bugfix.gd`（A3）通過。

### K-03 `try_place` 的 `reason` 混了機器碼與玩家文案，而 UI 直接顯示

- **Phase**：P1-D
- **狀態**：已修
- **問題**：`GameState.try_place()` 大部分失敗回機器碼（`not_held` / `hidden` / `resolved` / `not_accepted` / `action_spent` / `unknown_beat` / `unknown_slot`，`tests/headless/test_p1d.gd` 已把它們當契約斷言），但 beat 級與槽級 LOCKED 兩條回的是資料裡的 `reject_reason`（中文玩家文案）。同一個欄位兩種語意，消費端分不出來。`location_panel.gd` 的 `_on_place_pressed()` 直接把它塞進「（無法放置：%s）」。
- **影響**：玩家在中文遊戲裡會看到「（無法放置：action_spent）」。也踩到 i18n 紀律（`chore(i18n)` commit `e33b742` 定的「玩家可見字串住檔頭 const」）——這些碼會直接漏到畫面上，且無法翻譯。
- **解法**：`try_place` 改回 `{ ok, reason, reason_code, reason_text, lines }`。`reason_code` 維持現有機器碼（`test_p1d.gd` 的契約不動）；`reason_text` 只在 LOCKED 兩條帶資料的 `reject_reason`，其餘留空。UI 用檔頭 const 表把 `reason_code` 翻成玩家文案，`reason_text` 非空時優先顯示它。
- **備註**：`try_place` 回傳結構已補齊 `reason_code` 與 `reason_text`，UI 建立檔頭 `_REASON_CODE_TEXTS`，並於 `test_p1c_bugfix.gd`（A4）完成驗證。

### K-04 `accepts` 比對邏輯出現在 UI

- **Phase**：P1-D
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_matching_held_cards()` 自己做 `accepts.has(base_id) or accepts.has(card_type)`，跟 `try_place` 步驟③重複一份。違反 `開發設計方針.md > 全域結構決策`：「UI 內出現任何 accepts／三態／`action_spent` 判斷＝架構錯誤」。
- **影響**：兩份 `accepts` 語意會各自漂移。順帶症狀：`_matching_held_cards()` 不看 `action_spent`，所以行動格用掉之後「放入：主角卡」按鈕還在，按下去才被規則層退回。
- **解法**：規則層開 `GameState.placeable_cards(beat_id, slot_id) -> Array[String]`，內部沿用 `try_place` 步驟①③④同一套判斷；UI 只負責列舉回傳值。順帶讓已消耗的行動格不再列出主角卡。
- **備註**：`GameState.placeable_cards()` 已實作，UI 的 `_matching_held_cards()` 已移除，並於 `test_p1c_bugfix.gd`（A4）驗證 `action_spent` 狀態下的過濾效果。

### K-05 多鍵 `condition` 只有第一個運算子生效，lint 抓不到

- **Phase**：P1-D（lint 1）
- **狀態**：已修
- **問題**：`ConditionEval.eval()` 是 `if d.has(<運算子>)` 逐條 `return` 的結構，`{"day": 5, "flag": "x"}` 只會求值 `day`，`flag` 靜默忽略。`DataLoader._lint_condition()` 逐鍵檢查名稱是否在封閉語彙內，但不檢查「恰好一個運算子鍵」。
- **影響**：目前資料 **0 個多鍵 condition**，所以現在不影響玩法。但這是靜默錯誤——資料端寫了會過 lint、過測試，行為卻不是作者想的。內容期資料量上去之後遲早會踩到。
- **解法**：`lint_vocabulary()` 補一條：`condition` / `requires` 的 Dictionary 必須恰好一個運算子鍵，多於一個即報錯擋開機（要 AND 就寫 `all`）。成本低，擋掉一整類錯誤。
- **備註**：`DataLoader._lint_condition()` 已補多鍵／空 Dictionary 檢查，並於 `tests/headless/test_p1d.gd` 完成驗證。

### K-06 四個 beat 有 `requires` 卻沒有 `reject_reason`

- **Phase**：資料層
- **狀態**：已修
- **問題**：lint 2 每次開機都會警告這四個：`n_ahong_3_ch1`、`d19_pm_upstream`、`d37_back_wing`、`d37_look_for_her`。`實作規格書.md > 五` 定義「灰掉而沒寫 `reject_reason`＝資料錯誤」。
- **影響**：A2 修好之後，beat 級 LOCKED 會把內部槽整組降為 LOCKED 並沿用 beat 的理由——這四個因此顯示通用文案「（條件不足）」。`d37_back_wing` 與 `d37_look_for_her` 正是第 37 天那組互斥證據卡，玩家在關鍵分歧點只會看到一句不指涉任何東西的通用字。
- **解法**：補四條 `reject_reason`，文案照資料層規約（指缺的東西、不指答案，見 `實作規格書.md > 五`）。純資料改動，不動引擎。
- **備註**：四條 `reject_reason` 已補齊，`DataLoader.lint_missing_reject_reason()` 在真實 252 個 beat 上的警告歸零，並於 `tests/headless/test_p1d.gd` 完成驗證。

### K-07 tuning 數值在程式裡留了硬編碼 fallback

- **Phase**：P1-B／P1-D 遺留
- **狀態**：已修
- **問題**：`GameState.gain_card()` 的 `Data.tuning("madness_countdown_days", 7)`、`GameState._check_hand_overflow()` 與 `scenes/ui/hand_bar.gd` 的 `Data.tuning("hand_size", 14)`。三處把可調數值複製了一份進程式。
- **影響**：`data/tuning.json` 的鍵被改名時，遊戲不會報錯，會靜默沿用程式裡那份舊數字。這個 prototype 的整個玩法就是一邊玩一邊改數值，靜默失效比崩潰更貴。
- **解法**：`Data.load_data()` 補一條必要 tuning 鍵存在性檢查（缺鍵→`ok = false`），三處 fallback 隨之拿掉。掛在既有的 lint 1／2 旁邊，同一個地方。
- **備註**：`Data.REQUIRED_TUNING_KEYS` 已加入開機檢查，程式內硬編碼 fallback 已移除，並於 `tests/headless/test_game_state_p1a.gd` 完成驗證。

### K-08 「（條件不足）」重複住兩個檔

- **Phase**：i18n 管線
- **狀態**：待修
- **修復時機**：**i18n 管線**。等 key 命名規約定案再收，現在挑位置會挑錯。
- **問題**：`PanelBuilder` 的 `_REASON_FALLBACK` 與 `GameState` 的 `_REASON_LOCKED_FALLBACK` 是同一句玩家可見文字的兩份拷貝。
- **影響**：i18n 抽取時會產生兩個 key，翻譯改一個漏一個。
- **解法**：收成一處常數，兩邊引用。實際位置等 i18n 管線定 key 命名時一起處理，不必現在挑地方。

### K-09 `enter_beat` 先寫 `beats_entered` 再驗 beat 是否存在

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`GameState.enter_beat()` 在確認 `beats_by_id` 有這個 id 之前就寫入 `beats_entered[beat_id] = true`。
- **影響**：未知 beat id 會留在集合裡，而 `beats_entered` 屬 run 層、會被 `serialize()` 寫進存檔。
- **解法**：把寫入移到存在性檢查之後。三行。
- **備註**：已在 `GameState.enter_beat()` 最前方加入存在性檢查，並於 `tests/headless/test_p1c_bugfix.gd`（A5）完成驗證。

### K-10 `core/` 三個 class 的簽名在方針與實作不一致

- **Phase**：文件（`開發設計方針.md`，implementer-owned）
- **狀態**：已修
- **問題**：方針寫 `ConditionEval.eval(cond, gs: GameState)`、`PanelBuilder.build(location_id, gs: GameState, data: Data)`、`EffectApply.apply(effect: Dictionary, gs: GameState)`；實作是 `gs: Node` / `data: Node` / `effect: Variant`。
- **影響**：下一個人會照方針寫，然後撞牆——`GameState` 是 autoload 名，在 GDScript 裡是值不是型別，而 `game_state.gd` 也不能加同名 `class_name`（Godot 報 autoload 名稱衝突）。方針要求的那個標註**寫不出來**。
- **解法**：改方針三處簽名為實作現況，並加一句寫明為什麼不能標成 `GameState`，避免同一面牆被撞第二次。
- **備註**：`開發設計方針.md` 已修正三處簽名，並註明 GDScript Autoload 為全域值而非型別名。

### K-11 P1-A 序列化測試在失敗路徑存取已釋放物件

- **Phase**：P1-A（測試）
- **狀態**：已修
- **問題**：`tests/headless/test_game_state_p1a.gd` 的 `_test_serialize_roundtrip()` 先 `gs.free()`，失敗訊息才用 `gs.get(...)` 取值。
- **影響**：只在測試失敗時發作——正好是最需要那則訊息的時候，拿到的會是錯誤或空值。
- **解法**：把 `gs.free()` 移到組完訊息之後。一行。
- **備註**：`test_game_state_p1a.gd` 已調整先保存屬性值再 free 物件。

### K-12 LOCKED beat 底下的 RESOLVED 槽被降級為 LOCKED

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`PanelBuilder.build()` 在 beat 為 LOCKED 時把全部槽降為 LOCKED，包含已經放過卡、原本應該是 RESOLVED 的槽。
- **影響**：只有在「beat 曾經 OPEN、玩家放過卡、之後 `requires` 又變成不成立」時才會出現，第一輪資料未必碰得到。碰到時玩家會看到已放過的槽變回灰槽，狀態顯示不實。
- **解法**：降級時跳過 `tri == RESOLVED` 的槽。RESOLVED 本來就不可互動，不需要靠 LOCKED 擋。
- **備註**：`PanelBuilder.build()` 已加入降級跳過 `RESOLVED` 檢查，並於 `tests/headless/test_p1c_bugfix.gd`（A6）完成驗證。

### K-13 `on_enter.text` 在白天面板不再顯示（K-01 的迴歸）

- **Phase**：P1-C
- **狀態**：已修
- **問題**：`GameState.open_panel()` 呼叫 `enter_beat(beat_id)` 但**丟掉回傳值**，而 `location_panel.gd` 的 `_rebuild()` 只渲染 view model 裡的 `beat.text`。`on_enter.text` 因此在白天面板這條管道上靜默失效。`實作規格書.md > 十四` 定義該鍵的語意是「`text` **呈現**」，`EffectApply.apply()` 也正是為了把這些行交出去才設計成有回傳值。
- **影響**：目前白天 beat 只有一個帶 `on_enter.text`——`d15_pm_clinic`（第 15 天下午、`fixed`）。它的 top-level `text` 照常顯示，所以畫面不會空白，只少一段 24 字的首次敘述。真正的問題是**管道壞了**：之後任何白天 beat 加 `on_enter.text` 都會安靜地不播。`main.gd._play_evening()` 仍然用回傳行，所以 evening 正常——**兩條管道又不一致，跟 K-01 本身同一個病**。
- **解法**：`open_panel()` 把 `enter_beat()` 的回傳行留下來，掛回對應 beat 的 view（例如每個 beat view 加 `lines: PackedStringArray`）；`location_panel` 在 beat 主文之後接著播。順帶讓走查腳本也拿得到這些行。
- **備註**：`GameState.open_panel()` 已保留回傳行掛於 view model 的 `lines`，`location_panel.gd` 已改為渲染 `lines`，並於 `tests/headless/test_p1c_bugfix.gd`（A7，以 `d15_pm_clinic`）驗收通過。

### K-14 beat 篩選邏輯散在三處，而 `DataLoader.beats_at()` 沒人用

- **Phase**：P1-C／P1-D（P1-F 做夜間面板時會變第四份）
- **狀態**：已修
- **問題**：同一組「`when.day` 與 `when.phase` 對得上當前時間」的篩選各自重寫了三份：`PanelBuilder.build()`、`GameState.open_panel()`、`main.gd._play_evening()`（外加 `fixed` 條件）。而 `DataLoader.beats_at(day, phase)` 早就提供這件事，目前只有 `verify_data.gd` 一個呼叫者。
- **影響**：篩選規則要動的時候有三個地方要改，漏一個就是兩條管道行為不一致——K-01、K-13 都屬於這個家族。現在沒有已知的行為差異，是**維護風險不是 bug**。
- **解法**：三處改走 `DataLoader.beats_at()`（`open_panel` 與 `build` 再各自加地點過濾，`_play_evening` 再加 `fixed` 過濾）。P1-F 動夜間面板時順手做，不必單獨開一次。
- **備註**：commit `55d62a8`。本條列的三處都收攏了——`PanelBuilder.build()` 白天分支改走 `beats_at()`，`GameState.open_panel()` 改為委派 `PanelBuilder.build()` 不再自己掃，`main.gd._play_evening()` 的 fixed beat 那段改走 `beats_at()`。**但同一個 commit 新增的夜間程式又自己比對 `when`，三處**——那是新的缺口，記在 K-30，不留在本條。

### K-15 `try_place` 的舊 `reason` 欄位只剩測試在讀

- **Phase**：P1-D
- **狀態**：已修
- **問題**：K-03 補上 `reason_code` / `reason_text` 之後保留了舊的 `reason` 欄位向下相容。現在正式程式碼（`location_panel.gd`）只讀新的兩個；舊 `reason` 只剩 `test_p1d.gd`（7 處）與 `test_p1c_bugfix.gd`（1 處）在斷言。
- **影響**：`reason` 保留著 K-03 原本要消滅的混合語意（大部分回機器碼、LOCKED 兩條回玩家文案）。留著不會壞，但沒有人排它的退場時間，就會一直留著，而下一個人可能會照它寫新程式。
- **解法**：把 `test_p1d.gd` 那 8 處斷言改讀 `reason_code`，然後從 `try_place` 與 `choose` 的回傳一起移除 `reason`。純機械改動。
- **備註**：commit `1a67b17`。`GameState.try_place()` 與 `choose()` 已移除 `reason` 回傳鍵，`location_panel.gd` 移除 fallback，`tests/headless/test_p1d.gd` 與 `test_p1c_bugfix.gd` 已改為讀取 `reason_code` 與 `reason_text`，八套 headless 測試全綠。`開發設計方針.md > P1-E` 同時補上 `choose()` 的回傳形狀。

### K-16 三個行動格只剩 choice 槽，沒有主角卡可放

- **Phase**：資料層（缺口所在）／P1-E（讓它第一次有後果）／P1-F（走查會撞到）
- **狀態**：已修
- **決定（2026-08-14）**：**這三格維持免費，不補主角卡槽。** 內容期補故事線時，那幾天自然會長出可放主角卡的地方。**成立條件**：新槽要加在**同一個地點的同一個時段**。加在別的地點不算數——那三格照樣沒東西可放，這一條也不會自己消失。
- **問題**：三個行動格只有 choice 槽，玩家找不到地方放主角卡：第 35 天下午（`d35_pm_answer`，clinic）、第 40 天上午（`d40_tell_someone`，sanquan）、第 43 天下午（`d43_pm_zhou` 在 riverside、`d43_conclusion` 在 sanquan）。其中違反 `data/SCHEMA.md > choice_group` 同面板規約的是三個 **beat**：`d35_pm_answer`、`d40_tell_someone`、`d43_conclusion`。**`d43_pm_zhou` 不違規**——它是 `fixed`，SCHEMA 明文允許。兩份名單長度一樣但內容不同（三個**時段** vs 三個 **beat**），引用時不要混。
- **影響**：不影響玩法——玩家按推進鈕照樣過去，不會卡住。剩兩件事要處理。① P1-F 的貪心走查在這三格找不到可放主角卡的槽，而現有 `by_design` 名單只有三筆（第 1 天上午下午、第 32 天下午），走查會撞上名單外的空格。② lint 3 若照 SCHEMA 嚴格擋，開機會被這三個 beat 擋下來——而我們剛決定接受它們。
- **解法**：三件事，全部在 P1-F 做。
  1. **兩份名單分開，不要塞進同一個常數。** `verify_data.gd` 現有的 `by_design` 語意是「該時段**沒有任何 beat**」（檢查用 `beats_at().is_empty()`）。這三格**有 beat**，混進去會讓那三天之後真的變空也沒人發現。第一份維持原樣（第 1 天上午下午、第 32 天下午），第二份新開，語意是「有 beat、無主角卡槽」，收這三個時段，給 `playthrough_greedy.gd` 讀。兩份都住 `data_facts.gd`（見 B-03）。
  2. **lint 3 做成 warning，不做 error**，豁免名單沿用第二份。理由：這條規約在內容期會反覆進出「暫時違反」的狀態，做成 error 會變成每次寫新故事線都要先關掉的東西。
  3. `verify_data.gd` 的行動格覆蓋檢查**維持原樣**（只看該時段有沒有 beat）。不要順手擴充成「有沒有主角卡槽」——那會跟第二份名單重複同一份判斷，變成兩個地方要同步。
- **備註**：commit `55d62a8`。三件事都做了——`scripts/core/data_facts.gd` 建立，兩份名單分開（`BY_DESIGN_EMPTY_PHASES` 三筆、`BY_DESIGN_CHOICE_ONLY_PHASES` 三筆），`DataLoader.lint_choice_rules()` 對豁免名單內的 beat 出 warning、名單外出 error，`verify_data` 實測「0 錯誤（3 筆已豁免警告）」。**兩個後續缺口另立條目**：走查腳本沒有讀那份名單（K-25）；lint 只查了 choice 槽，沒查比對槽，因此漏掉 `d42_morning_ask_paper`（K-27）。
- **2026-08-14 後續查證**：當初把「沒東西花行動格」的時段估成 24 格是錯的。逐格比對之後，其中 **23 格整個時段只有 `fixed` beat**，而「`fixed` 不吃行動格」是四份文件都寫過的既有規則（`data/SCHEMA.md > slots[]` 的 `fixed` 欄、`實作規格書.md > 五`、`溫泉小鎮企劃.md` 開關 6 那節點名第 12 天上午拿藥、第 15 天下午診所、第 16 天上午、`subdocs/故事線/故事線_第一輪_第一章.md`）。那 23 格是設計，不需要名單也不需要決策。真正沒被涵蓋的只有第 42 天上午一格（K-27）。**`d29_pm_invitation` 也不需要豁免**——它是 `fixed`，依同一條規則本來就不吃行動格。

### K-17 `try_place` 帶空卡 id 可以直接完成選擇

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`GameState.try_place()` 在 ①持有檢查**之前**就轉導 `choose()`（槽有 `choice_group` 時），而 `choose()` 只在 `card_id` 非空時檢查持有。`try_place("", beat_id, choice_slot_id)` 因此會成功結算。
- **影響**：`try_place` 的檔頭註解寫「①持有 → ②三態 → ③accepts → ④action_spent，順序固定」，轉導那行破壞了自己宣告的後置條件。目前沒有呼叫者傳空字串（UI 的按鈕 id 一律來自 `placeable_cards()`，走查腳本傳主角卡），所以不影響玩法。
- **解法**：轉導前先擋——`card_id` 為空時回 `not_held`，不轉導。空卡 id 走 `try_place` 本來就沒有語意；要直選就直接呼叫 `choose()`。
- **備註**：commit `55d62a8`。`try_place()` 函式最前面加了空字串檢查，回 `not_held`，在 beat 查表之前。**當時沒有回歸測試**（K-29）——回歸測試已於 K-29 收尾補齊（`test_p1d.gd` 驗 `try_place("")` → `not_held`）。

### K-18 `choose()` 與 `try_place()` 都不檢查 `when.day` / `when.phase`

- **Phase**：P1-D（`try_place`）／P1-E（`choose` 複製了第二份）
- **狀態**：已修
- **問題**：兩個入口只驗 beat 與槽的 `condition` / `requires`，不驗這個 beat 是否屬於當前天與時段。第 1 天呼叫 `choose("d29_pm_invitation", "invitation", "invite_none")` 會成功結算。
- **影響**：`try_place` 自己的註解寫「UI 是零規則顯示層，規則層要能獨立擋下非法呼叫」，這一條沒做到。目前無可觸發路徑：UI 只渲染 `PanelBuilder.build()` 過濾後的槽，走查腳本也照面板走。屬維護風險，不是現行 bug。
- **解法**：K-14 把篩選收進 `DataLoader.beats_at()` 之後，兩個入口各加一次「這個 beat 在不在當前 (day, phase)」的檢查。夜間 beat 與 `fixed` beat 的 `when` 形狀不同，收攏時一起處理。
- **備註**：commit `55d62a8`。新增 `GameState._is_beat_time_valid()`，`try_place()` 與 `choose()` 各呼叫一次，不過就回 `hidden`。夜間分三種形狀處理（定日夜 beat 比對 `when`、章節變體比對 `chapter` ≤ 當前章、附加 beat 一律通過）。**當時沒有回歸測試**（K-29）——回歸測試已於 K-29 收尾補齊（`test_p1e.gd` 驗越時 `choose()` → `hidden`）；它自己也是 K-30 列的第三處 `when` 比對。

### K-19 `location_panel` 自己判斷 choice 槽，帶卡那條分支多餘

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`location_panel.gd` 的 `_rebuild()` 讀原始資料的 `slot["choice_group"]` 算出 `is_choice`，再決定按鈕接 `_on_choose_pressed` 還是 `_on_place_pressed`。其中帶卡按鈕那條是多餘的：`try_place()` 已經轉導 `choose()`，兩條路的結果完全相同。
- **影響**：`開發設計方針.md > 全域結構決策` 寫「UI 內出現任何 accepts／三態／`action_spent` 判斷＝架構錯誤」。這是第四種判斷，字面上沒被列到、精神上是同一件事（K-04 同族）。直選按鈕那條**不能**拿掉——手上沒有卡就沒有別的入口——所以問題不是「UI 不該知道這是選擇題」，是「UI 不該自己從原始資料算出來」。
- **解法**：帶卡按鈕改回接 `_on_place_pressed`，靠 `try_place` 的轉導；`is_choice` 由 `PanelBuilder.build()` 算好放進 slot view（跟 `tri`、`reason` 並列），UI 只讀 view model。
- **備註**：commit `55d62a8`。兩件都照解法做了——`PanelBuilder.build()` 的 slot view 多一個 `is_choice` 鍵，`location_panel.gd` 改讀它；帶卡按鈕改接 `_on_place_pressed`。`test_p1e.gd` 的 9b（K-20 加的）驗的是行為不是接線，所以這次換路之後仍然通過，等於順帶驗了本條。

### K-20 UI 的帶卡選擇入口沒有斷言

- **Phase**：P1-E（測試）
- **狀態**：已修
- **問題**：`tests/headless/test_p1e.gd` 的 `_test_choice_ui_buttons()` 只找出並點擊直選按鈕（「選擇：① 他走路的方式」）。「放入：拍立得」那一顆從沒被按過。
- **影響**：`測試指南.md > P1-E` 明寫兩個入口都要通，`開發設計方針.md > 跨 Phase 實作紀律` 也寫「新增規則層入口時，UI 路徑與走查路徑各要有一條斷言」。
- **解法**：測試 9 補一次帶卡按鈕的點擊，驗 `choices` 與 `slots_placed` 的結果與直選相同。
- **備註**：commit `1a67b17`。`tests/headless/test_p1e.gd` 測試 9 已分拆為 9a（直選）與 9b（帶卡按鈕「放入：夫妻留下的拍立得」點擊），確認點擊後正確寫入 `choices`、`slots_placed` 並獲得效果卡片。9b 驗的是行為不是接線，所以 K-19 把那顆按鈕改走 `try_place` 轉導之後，這條斷言仍然成立。

### K-21 直選／帶卡的狀態等價性只比六個欄位

- **Phase**：P1-E（測試）
- **狀態**：已修
- **問題**：`_test_choice_state_equivalence()` 逐欄比 `choices`、`slots_placed`、`flags`、`switches`、`relations`、`action_spent`。漏掉 `beats_entered`、`switch_progress`、`npc_action_counts`、`madness_clock`、`_madness_counter`，以及整個 `meta`（知識集合）。
- **影響**：驗收原文是「除卡片消耗外**完全一致**」。逐欄列舉的寫法，之後 `serialize()` 每加一個欄位就漏驗一個。
- **解法**：兩邊的 `run` 各 `erase("hand")` 之後整包比對，`meta` 也比。比逐欄列舉短，而且不會隨欄位增加而過期。
- **備註**：commit `1a67b17`。`tests/headless/test_p1e.gd` 測試 3 已改為比對整包 `run`（移除 `hand` 後）與整包 `meta`，全域狀態欄位比對完整。之後 `serialize()` 新增欄位會自動進入比對範圍，不必回頭補。

### K-22 `placeable_cards()` 對 choice 槽仍套 `action_spent` 過濾

- **Phase**：P1-E
- **狀態**：已修
- **問題**：`placeable_cards()` 對主角卡套 `action_spent` 過濾（K-04 加的，對一般槽正確）。`choose()` 不檢查 `action_spent`（對 choice 槽正確，SCHEMA 寫明不吃格）。兩者對同一個槽會給出不同答案。
- **影響**：只在「choice 槽的 `accepts` 含 `protagonist`」時發作——UI 會在行動格用掉之後藏起按鈕，規則層卻放行。目前全部 choice 槽的 `accepts` 不是缺席就是情報卡／NPC 卡，沒有一個含 `protagonist`，所以現在不發作。
- **解法**：P1-F 寫 lint 3 時多加一條：choice 槽的 `accepts` 不得含 `protagonist`。SCHEMA 已規定 choice 不吃格，而主角卡放進不吃格的槽沒有語意。擋在資料端之後，`placeable_cards()` 與 `choose()` 都不必改。
- **備註**：commit `55d62a8`。`DataLoader.lint_choice_rules()` 對「choice 槽的 `accepts` 含 `protagonist`」出 error，`placeable_cards()` 與 `choose()` 都沒動，照解法做。**這條 error 分支從來沒有被執行過**——真實資料沒有違規案例，測試也沒有餵合成 beat 進去（K-29）。**已於 K-29 收尾補上**——`test_p1d.gd` 的 lint 區塊餵一個合成 beat（choice 槽收 `protagonist`），證明 error 分支真的會報。

### K-23 `test_p1e.gd.uid` 未進版控

- **Phase**：P1-E
- **狀態**：已修
- **問題**：其他七個測試腳本的 `.uid` 都在 repo 裡，`tests/headless/test_p1e.gd.uid` 沒有跟著 `5c681f5` 進來，要等下一次 `--import` 才生成。
- **影響**：每個人 clone 之後第一次 import 都會多出一個未追蹤檔，而且不同機器可能為同一個腳本產生不同 uid。
- **備註**：本次 commit 一併補進版控。

### K-24 規格書狀態表把「不選」寫成 `choices` 的一種值

- **Phase**：文件（`實作規格書.md`）
- **狀態**：待修（低）
- **修復時機**：**下一次動規格書第三節時**。純措辭，不影響實作。
- **問題**：`實作規格書.md > 三` 的狀態表寫「選擇：已結算的 `choice_group`（組 id → 選了哪個槽**或「不選」**）」。`開發設計方針.md > P1-E` 寫「不選＝不寫 `choices`」，實作照方針做。兩份文件對同一個欄位的形狀講法不同。
- **影響**：照規格書那句寫的人，會去找一個代表「不選」的哨兵值。實際上沒有，也不需要——`d29_pm_invitation` 的「不邀」是資料明寫的組員（`invite_none`）。看得見的「不選」由資料承擔，不由引擎。
- **解法**：規格書那半句改成「組 id → 選了哪個槽；未寫入＝不選」。`實作規格書.md` 不屬 verifier 角色，本條只列建議、不直接改。

### K-25 走查腳本不判斷「全 `fixed` 時段」也不比對名單，37 個行動格靜靜跳過

- **Phase**：P1-F（測試）
- **狀態**：已修
- **問題**：`tests/headless/playthrough_greedy.gd` 第 11 行 `preload` 了 `DataFacts`，然後一次都沒有呼叫。`DataFacts.is_choice_only_phase_by_design()` 全專案零呼叫者。`_execute_action_phase()` 放不下就回 0，呼叫端只把它加進總數，不比對、不報告、不失敗。而 `開發設計方針.md > P1-F` 寫的是「找不到就跳過該格（**比對 `DataFacts` 名單**）」，`測試指南.md > P1-F` 寫的是「exit 0（第 1 天上午下午、第 32 天下午允許空格，名單同 `by_design`）」。
- **影響**：實測 90 個行動格只用掉 53，37 格空的，而測試全綠。拆開來看：**30 格在資料層就沒有主角卡槽**（3 格完全沒 beat ＋ 3 格只有 choice 槽 ＋ 23 格整個時段只有 `fixed` beat ＋ 第 42 天上午，見 K-27），另 **7 格是條件沒過**。名單只涵蓋 6 格。現在沒有人知道哪 37 格是空的，也沒有人會發現其中哪一格是因為 bug 才變空的——這是 K-01 同一種病，綠燈涵蓋不到的路徑。
- **解法**：**不要手寫名單。** 那 23 格是 `fixed` 敘事時段，而「`fixed` 不吃行動格」是既有規則（見 K-16 的後續查證），從資料就推得出來。走查腳本改成：放不下時，先看該時段是否①在 `BY_DESIGN_EMPTY_PHASES`、②在 `BY_DESIGN_CHOICE_ONLY_PHASES`、③該時段所有 beat 都是 `fixed`。三者皆非就是錯誤，印出天數時段並 exit 1。順帶把每天用掉幾格印出來，讓走查的輸出能被人讀。
- **備註**：`playthrough_greedy.gd` 已實作 `_diagnose_unplaced_phase()`，未放卡時動態從資料與狀態推導合法分類（刻意留空、刻意 choice-only、全 fixed 敘事時段、條件分支未解鎖、前置門檻未達成、純比對槽），並加入無 beat 且不在留空名單即判 ok: false 的防呆。走查過程逐日印出上午／下午行動摘要，並於收尾輸出 90 個時段的完整統計分類表，全部 90 個行動時段透明診斷通過。

### K-26 evening 演出有三份實作，走查那份不播殘響

- **Phase**：P1-F
- **狀態**：已修
- **問題**：同一件事寫了三份，而且三份內容都不一樣：`scenes/main.gd._play_evening()`（fixed beat ＋ 殘響）、`tests/headless/playthrough_greedy.gd._execute_evening_phase()`（**只有 fixed beat，沒有殘響**）、`tests/headless/test_p1f.gd._collect_evening_echoes()`（**只有殘響**）。夜間同病兩份：`main.gd._play_night_fixed()` 與 `playthrough_greedy.gd._execute_night_phase()`。
- **影響**：兩件事。① 45 天貪心走查從頭到尾一條殘響都沒播過，而驗收要求「錯過的 beat 的 echo 在正確的天播出」。② 驗殘響的測試驗的是它自己那份拷貝——`main.gd._play_evening()` 整個壞掉，十套測試仍然全綠。`開發設計方針.md > 跨 Phase 實作紀律` 對這件事寫得很明白：「凡是 UI 與 headless 走查都會用到的動作，本檔要明寫入口住規則層」，並且拿 `end_run()` 當範例。晚間演出正是這種動作，而它住在 UI 與測試裡。
- **解法**：下沉成 `GameState.play_evening() -> PackedStringArray`，內部固定順序（先當日 evening `fixed` beat 依陣列順序 `enter_beat()`，再掃 `echo.day == day` 的殘響），回傳要播的行。`main.gd`、走查腳本、`test_p1f` 三個呼叫端共用。夜間同樣下沉成 `GameState.play_night_fixed()`。
- **備註**：已於 `GameState` 實作 `play_evening()` 與 `play_night_fixed()`。`main.gd`、`playthrough_greedy.gd` 與 `test_p1f.gd` 統一委派至這兩個規則層入口，刪除測試內的自製殘響拷貝，並於 `test_p1f.gd`（測試 1、2、3、10）完成全綠驗收。

### K-27 lint 3 只查 choice 槽，沒查比對槽；`d42_morning_ask_paper` 與 `d28_morning_xiaowu` 是活的違規

- **Phase**：P1-F（lint）／資料層
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**，跟 K-25 同一次收——兩者都在算「哪些行動格合法地沒事可做」。資料的天數錯誤可以更早改，它不依賴任何引擎工作。
- **問題**：規約的原文涵蓋**所有**不收主角卡的槽，不只選擇題。`實作規格書.md > 六`：「『不收主角卡的槽必須跟收主角卡的槽同面板』是**資料規約**（`SCHEMA.md` 已載）；引擎不強制，**驗證腳本要 lint**」；`data/SCHEMA.md` 對比對槽也寫「因為不收主角卡，所以**不吃行動格**……成本是**你人得在那裡**」。而 `DataLoader.lint_choice_rules()` 只在槽有 `choice_group` 時才計入，比對槽那一半沒做。
- **影響**：資料裡有一個活的違規案例——`d42_morning_ask_paper`（第 42 天上午、寺廟）：**非 `fixed`**，兩個槽都只收 `item_old_paper`（比對槽），沒有 `choice_group`，而且是該時段唯一的 beat。玩家那個上午沒有地方放主角卡，而 lint 3 因為它沒有 `choice_group` 完全看不到它。它同時是 K-25 那 30 格裡唯一沒被任何規則涵蓋的一格。
- **解法**：兩件事。① `lint_choice_rules()` 的判斷條件從「有 `choice_group` 的槽」放寬成「不收 `protagonist` 的槽」，函式改名為 `lint_free_slot_rules()` 較貼切。**照這句的字面寫會誤報，判斷單位要一起改成面板——見下方「動工前定案」。** ② `d42_morning_ask_paper` 的天數要改（見下方「更嚴重的第二層」與「決定」）。
- **備註**：已實作 `DataLoader.lint_free_slot_rules()`（以面板為單位檢查主角卡槽與 choice accepts 規約），並修正 `d28_morning_xiaowu` 補上主角卡槽。`verify_data` 實測 4 筆已豁免警告、0 錯誤；`test_p1d.gd` 補齊合成違規 beat 回歸測試全綠。

#### 更嚴重的第二層：這個 beat 第一輪永遠不會出現（2026-08-14 查證）

本條原本只記 lint 少查一種槽。查故事線之後發現底下壓著一個更嚴重的資料錯誤，**嚴重度因此從中拉到高**。

- **時間對不上**：`d42_morning_ask_paper` 的 `condition` 是 `{ "has_card": "item_old_paper" }`，掛在第 42 天**上午**。而 `item_old_paper` 全作只有一個來源——`d42_pm_room`（第 42 天**下午**、`oldhouse_room`、`search` 槽 `requires` 有 `item_room_key`）。上午早於下午，所以那張紙在第 42 天上午不可能在手上。**這個 beat 的出現條件永遠不成立。**
- **後果三件**：① 第 42 天上午的面板永遠是空的（那是該時段唯一的 beat）。② 金伯解釋拜契、叔叔含糊帶過這兩段第三章關鍵資訊，第一輪玩不到。③ 現有檢查全部抓不到——`verify_data` 的行動格覆蓋檢查只看「該時段有沒有 beat」，不求值 `condition`，所以是綠的。
- **故事線寫的順序本來就是對的**（`subdocs/故事線/故事線_第一輪_第三章.md > 第 42 天——那間房`）：先在下午打開那間房拿到紙，**然後**「拿去問金伯」「拿去問叔叔」。資料把「拿去問」排在「拿到紙」的前一個時段。

#### 決定（2026-08-14）

**`d42_morning_ask_paper` 搬到第 43 天上午。** 玩家在第 42 天下午拿到紙、過一夜，第 43 天上午拿去問。

搬移前查證過的四件事：

| 查證 | 結果 |
|---|---|
| `temple` 第 43 天上午進得去嗎 | 可以。`phases` 含 `morning`，`chapter` 為 1 |
| 第 43 天上午原本有什麼 | 四個 beat（`d43_morning_clinic`／`d43_morning_her`／`d43_morning_inn`／`d43_morning_dig`），**都有主角卡槽**，所以那個上午的行動格花得掉 |
| 卡片撐得到隔天嗎 | 撐得到。`item_old_paper` 是 run 層卡片，只有 `end_run()` 會清 |
| 有沒有人依賴它的產出 | 沒有。它寫的 `asked_jinbo_paper` 與 `asked_uncle_paper` 兩個旗標，全作沒有任何地方讀 |

**同面板規約走豁免，不補槽。** 寺廟在第 43 天上午沒有別的 beat，所以那個面板仍然只有免費槽。決定把 beat 列進 `DataFacts.BY_DESIGN_CHOICE_ONLY_BEATS`，不硬補一個收主角卡的槽。理由：這一格的代價其實已經付過了——玩家得先在第 42 天下午花掉一個下午開那間房才拿得到紙。為了滿足檢查而生一段「在廟裡待一上午」，是為了程式寫內容，不是為了故事。

**id 跟著改名**：`d42_morning_ask_paper` → `d43_morning_ask_paper`。全作 beat id 都是 `d<天數>_` 開頭，留著舊名字會對不上；而且目前沒有任何地方引用這個 id，趁 i18n 凍結 id 之前改最便宜。

**兩個旗標維持現狀**：`asked_jinbo_paper` 與 `asked_uncle_paper` 全作沒有人讀。已確認**先這樣**，不在本輪處理。企劃書寫「第一輪不能燒契書……那要等主角死過一次才取得」，兩個旗標很可能是留給第二輪的伏筆；等 Phase 5 做迴圈繼承時自然會知道要不要接。

**搬走之後第 42 天上午變成完全沒有 beat，因此補了四個常規 beat。** 這不是搬移造成的問題，是搬移讓既有的資料缺口現形——第 42 天原本就只有一個下午 beat（`d42_pm_room`），而故事線第 42 天寫的是「其他：① 診所 ② 她 ③ 旅館 ④ 查」。第 41 天與第 43 天都有那四個常規 beat，只有第 42 天沒有。搬走之前，那個永遠不成立的 beat 剛好讓 `verify_data` 的覆蓋檢查以為那個上午有東西。新增的四個是 `d42_morning_clinic`／`d42_morning_her`／`d42_morning_inn`／`d42_morning_dig`，形狀照第 41、43 天既有的四個抄，不新增地點也不新增卡片 id。

**落地**：`data/beats/ch3_d39_d45.json`（`id`、`when.day`、四個新 beat）、`scripts/core/data_facts.gd`（豁免名單第四筆）、`subdocs/故事線/故事線_第一輪_第三章.md`（第 42 天標明「拿去問是隔天上午的事」，第 43 天補上上午那條與「其他」清單的廟）。全十套 headless 綠（基準 `HEAD` 為 `5e2a432`），beat 總數 252 → 257，走查行動數 54 → 56（多的兩格正是第 42 天新增的上午與下午）。**lint 3 放寬到比對槽那一項仍未做**（本條解法①），所以豁免名單裡的第四筆現在還沒有人讀。

**第 42 天下午也補了一格**：`d42_pm_inn`（顧旅館，山泉閣，無條件）。原本那個下午只有 `d42_pm_room`，而它的 `search` 槽 `requires` 那把鑰匙——沒拿到鑰匙的玩家沒有地方放主角卡。新增的這一格刻意無關痛癢：不給卡、不寫旗標、不推開關。它有兩個作用——讓沒鑰匙的玩家有地方花掉下午，以及當那間房的機會成本（選了它就等於今天沒去開門）。取材自故事線第 42 天的「其他：① 診所 ② 她 ③ 旅館 ④ 查」。

#### 落地定案（2026-08-14）

① **判斷單位改為面板**：`DataLoader.lint_free_slot_rules()` 以 `(day, phase, location)` 分組，同組只要有任一 beat 包含主角卡槽即判定合規。`d17_pm_delegate` 與 `d18_pm_zhao` 因同面板有其他常規 beat 自然過關，不需進入豁免名單。
② **`d28_morning_xiaowu` 槽位改收主角卡**：將 `listen` 槽設為 `accepts: ["protagonist"]` 並附 `on_place` 對話，使第 28 天上午老街具備正常可放主角卡的互動格，不再是純比對/空槽，不需額外補 beat 或列入豁免。
③ **豁免名單維持 4 筆**：`d35_pm_answer`、`d40_tell_someone`、`d43_conclusion`、`d43_morning_ask_paper`，`verify_data` 實測 4 筆警告、0 錯誤。`test_p1d.gd` 補齊合成違規 beat 回歸測試全綠。

#### 順帶建議的新 lint（尚未拍板）

這一類錯誤——beat 的 `condition` 要求某張卡，而那張卡最早取得的時間晚於 beat 自己的時間——沒有任何現有檢查抓得到，內容量上去之後會再發生。可以加一條「死內容偵測」：對每個 `condition` 帶 `has_card` / `has_knowledge` 的 beat，回推那張卡最早在哪一天發得出來，比 beat 自己晚就報錯。成本不低，但抓的是「寫好的內容玩不到」這一整類。

### K-28 第 45 天結局 stub 文字寫上去立刻被蓋掉；夜間 fixed beat 文字沒容器；`flow_text` 建了沒人用

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。B-03 目前只完成一半。
- **問題**：`GameState.end_run()` 的發射順序是 `run_ended` → 重置 → `day_changed` → `phase_changed`。`main.gd._on_run_ended()` 用 `+=` 把「[結局 stub]」接在狀態列後面，但接著的 `_on_day_changed()` 與 `_on_phase_changed()` 都呼叫 `_refresh_status()`，而它是 `_status_label.text = ...`（賦值覆蓋）。結局那行在同一次呼叫裡就被清掉，玩家看不到。
- **影響**：`開發設計方針.md > P1-F` 為這件事寫過前提：「上一條成立的前提是**結局文字同步渲染**（P1 的結局只有一行 stub，`run_ended` 的 listener 在 signal 內就寫完）」。listener 確實寫完了，但寫在會被覆蓋的地方。`scenes/ui/flow_text.gd` 與 `flow_text.tscn` 已建立且功能完整（`append_line` / `get_text`），但**全專案零使用者**——它正是為了不被狀態列覆蓋而存在的容器。另外沒有任何測試涵蓋 `main.gd` 的結局畫面。
- **2026-08-14 併入：夜間 fixed beat 的文字沒有容器可放**（K-25／K-26 驗證時查到，同一個病因故不另立條目）。`main.gd._play_night_fixed()` 呼叫 `GameState.play_night_fixed()` 但**不接回傳值**，而 evening 那半有 `_evening_label` 可寫。K-26 已經把兩者下沉成規則層入口並回傳 `PackedStringArray`，規則層這邊沒問題；缺的是 UI 端沒有地方顯示夜間演出文字。目前 `_route_view()` 的 `"night"` 分支把 `_night_label` 設成 `visible = false`。玩家因此看不到入夜強制播的 beat（例如第 1 夜走廊、第 24 夜出血），而 `test_p1f` 驗的是 `GameState` 那一層，所以不會變紅。
- **解法**：`main.gd` 接上 `flow_text`：`_on_run_ended()` 改寫進 `flow_text`，殘響也改走它（跟 K-26 一起做）。狀態列只放天數時段章節，不放演出文字。**夜間同辦**——`_play_night_fixed()` 接住回傳值寫進同一個 `flow_text`，三種演出文字（殘響、夜間 fixed、結局 stub）共用一個容器，`_evening_label` 與 `_night_label` 一併退場。
- **備註**：`main.tscn` 已替換為 `FlowText` 容器，`main.gd` 接線晚間演出、入夜 fixed beat 與結局 stub（在推進至新輪前保持可見）；`test_boot.gd` 補齊 FlowText 節點與三種文字渲染之自動化驗收全綠。

### K-29 六條 K 與 B-02／B-03 標了已修，但內文沒更新、沒備註、沒回歸測試

- **Phase**：文件／測試
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。內文與備註已於本次補齊，**缺的回歸測試仍要補**。
- **問題**：commit `55d62a8` 之後，總表把 K-14／K-16～K-19／K-22 改成「已修」，但六條的內文 `狀態` 欄全部還是「待修（低）」，而且一條備註都沒有。B-02 與 B-03 也還維持「要做／兌現時機 P1-F」。本檔開頭的規則寫：「implementer 修完某條之後，在該條標 `已修` 並附 commit 短 hash」。
- **影響**：更要緊的是**六條沒有一條有回歸測試**。搜過整個 `tests/`：沒有測試按 `try_place("")`（K-17）、沒有測試在錯的天呼叫 `choose()`（K-18）、沒有測試檢查 view model 的 `is_choice`（K-19）、`lint_choice_rules()` 的 error 分支一次都沒有被執行過（K-22——真實資料乾淨、違規的三個又在豁免名單裡，所以那段程式壞掉不會有人知道）。K-01～K-13 每一條的備註都指得出一個具體測試，這六條指不出來。
- **解法**：內文與備註本次已補。回歸測試另開一輪補：`test_p1d.gd` 加 `try_place("")` → `not_held`；`test_p1e.gd` 加越時 `choose()` → `hidden`、加 view model `is_choice` 斷言；`test_p1d.gd` 的 lint 區塊加一個合成 beat（choice 槽收 `protagonist`）驗 error 分支真的會報。四條斷言，全部是既有測試檔加幾行。
- **備註**：回歸測試已全數補齊——`test_p1d.gd` 加測 `try_place("")` 與 lint 錯誤分支；`test_p1e.gd` 加測越時 `choose()` 與 `is_choice` view model 斷言。全套 headless 測試全綠。

### K-30 K-14 收攏三處之後，夜間新增三處自己比對 `when`

- **Phase**：P1-F
- **狀態**：已修（`bdfa5fd`，P3-C）
- **修復時機**：**P3 夜間層真值化**。夜間規則到那時要整個重寫，現在收攏會白做。
- **問題**：K-14 列的三處確實都收攏了（見該條備註），但同一個 commit 新增的夜間程式又各自比對 `when`：`PanelBuilder.build()` 的夜間主內容選擇（掃 `loader.beats` 找 `when.day == 當天 and when.phase == "night"`）、`GameState.sleep_night()`（同樣自己掃一遍找旅館的當夜定日 beat）、`GameState._is_beat_time_valid()`（K-18 加的，第三份 `when` 比對）。
- **影響**：與 K-14 同一種維護風險，不是 bug。`DataLoader.beats_at()` 在夜間確實不夠用——夜間要分定日／章節變體／附加三種形狀——所以這三處不是照抄，是真的多做了事。但「夜間 beat 怎麼選」現在住三個地方。
- **解法**：P3-C 已定好落點：DataLoader 的 `night_beat_candidates()` 收斂結構候選，GameState 的 `resolved_night_content()` 統一求值 condition；`PanelBuilder.build()`、`sleep_night()` 與 night non-fixed 合法性檢查共用 resolved 結果。`DataFacts.beat_matches_time()` 只留 dated 日期／時段判準。P1／P2 實作仍不提前動，等 P3-C 兌現後再把本條標已修。

### K-31 `verify_data` 把行動格覆蓋檢查跑了兩次

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**。刪掉其中一份，一分鐘。
- **問題**：`verify_data.gd` 先跑一段 for loop 檢查 `beats_at(day, phase).is_empty()` 並印「行動格覆蓋　第 1-45 天全滿」，接著呼叫 `DataLoader.lint_action_phases()` 做完全一樣的事，再印「行動格供需 (Lint 5)　0 錯誤」。兩份都用 `DataFacts.is_empty_phase_by_design()` 豁免，判斷邏輯逐字相同。
- **影響**：輸出多一行、判斷多一份要同步。K-16 解法第 3 點正是提醒不要弄成兩個地方要同步，結果換了個方式踩到。
- **解法**：留 `lint_action_phases()`，刪掉 `verify_data.gd` 裡的 inline loop，印的那行併過去。
- **備註**：已刪除 `verify_data.gd` 的 inline 迴圈，統一走 `DataLoader.lint_action_phases()` (Lint 5)。

### K-32 文件寫的 `d3_pm_couple` 在資料裡不存在

- **Phase**：文件
- **狀態**：已修
- **修復時機**：**下一次動規格書或測試指南時**。純更名，不影響實作。
- **問題**：`實作規格書.md > P1-F` 驗收與 `測試指南.md > P1-F` 兩處都寫「拿第 3 天下午 `d3_pm_couple` 那條驗」與「`d3_pm_couple.echo`」。資料裡沒有這個 id——第 3 天下午的三個 beat 是 `d3_pm_sanquan`、`d3_pm_oldstreet`、`d3_pm_temple`，那條殘響掛在 `d3_pm_sanquan`（到場的槽是 `help_ahong`）。`ch1_d04_d15.json` 的一則 note 也沿用了錯的名字。
- **影響**：`test_p1f.gd` 用的是對的 id，所以測試會過，只有文件錯。照文件去 grep 的人會找不到東西，並且可能以為資料缺了一條殘響。
- **解法**：三處把 `d3_pm_couple` 換成 `d3_pm_sanquan`。`實作規格書.md` 與 `測試指南.md` 不屬 implementer 角色，本條列建議。
- **兌現紀錄**：2026-08-15 全數改完，**實際是四處不是三處**——本條原本漏記 `待決事項.md`（待決 1 結案時的 echo `day` 稽核表也用了錯的 id）。四處：`實作規格書.md > P1-F`、`測試指南.md > P1-F`（兩行）、`data/beats/ch1_d04_d15.json` 的 note、`待決事項.md` 的稽核表。全庫已無 `d3_pm_couple`（`_舊文件/` 除外）。改 `data/` 的只有一個 `note` 欄位，引擎不讀，行為零變化。

### K-33 夜間推進一律呼叫 `sleep_night()`，不管玩家是否已選地點

- **Phase**：P1-F
- **狀態**：已修（`bdfa5fd`，P3-C）
- **修復時機**：**P3 夜間層真值化**。夜間流程那時才是真的，現在改是改在 stub 上。
- **問題**：`main.gd._on_advance_pressed()` 只要 `GameState.phase == "night"` 就無條件呼叫 `sleep_night()`，然後才 `advance_phase()`。`實作規格書.md > 九` 寫的是「玩家一晚選一個地點（**或**直接睡，夜間層不強制）」——兩者是二選一。
- **影響**：玩家去過一個夜間地點之後按推進，還會再解析一次旅館的當夜定日 beat，等於一夜拿到兩份內容。P1 夜間是 stub、收費地點全鎖、颱風外溢 beat 也還沒真的接上，所以現在看不出症狀。P3 會直接變成 bug。
- **解法**：`GameState` 記一個「今夜已選過地點」的 run 層旗標，`sleep_night()` 在旗標為真時直接回空。**不要現在加**——P3 的夜間流程會重寫這一段，那時旗標的形狀才定得下來（同 B-02 對「本輪已結束」旗標的處理原則）。

### K-34 `available_locations()` 夜間分支不檢查地點級 `requires`

- **Phase**：P1-F
- **狀態**：已修（`0047aaa` 實作、`72b84ca` 補結案證據）
- **修復時機**：**P3 夜間層真值化**。P1 無影響。
- **問題**：`實作規格書.md > 九` 寫夜間地圖列出的條件是「`layer == "night"` 且 `earliest_night` ≤ 當前天數 **且 地點級 `requires` 成立**（不成立＝灰掉＋理由）」。`PanelBuilder.available_locations()` 的夜間分支只做了前兩項。
- **影響**：`data/locations.json` 有 6 個夜間地點帶 `requires`，全部是阿宏鏈（`n_ahong_2` ～ `n_ahong_7`），而且**全部 `madness_cost` 為 1**。P1 的 `PanelBuilder.build()` 對任何 `madness_cost > 0` 的地點一律回 LOCKED ＋ stub 理由，所以這六個現在本來就進不去，缺的檢查看不出差別。P3 把收費做成真的之後才會外露。
- **解法**：P3 做夜間標記時，夜間分支補上 `ConditionEval.eval(loc.get("requires"), gs)`，不成立者仍列出但標 LOCKED，理由取地點級 `reject_reason`（六個都還沒填，屬既知資料落差）。

### K-35 `PanelBuilder` 夜間收費地點回傳資料裡不存在的假 beat id

- **Phase**：P1-F
- **狀態**：已修（`2ef58f3` 移除合成 beat、`72b84ca` 補結案證據）
- **修復時機**：**P3 夜間層真值化**。
- **問題**：`PanelBuilder.build()` 對 `madness_cost > 0` 的夜間地點提前回傳一個手工組出來的 beat：id 是 `<location_id>_locked`、`slots` 為空、`tri` 為 LOCKED、理由是檔頭常數 `_REASON_NIGHT_LOCKED_STUB`。這個 id 在 `Data.loader.beats_by_id` 裡查不到。
- **影響**：目前安全——它是 LOCKED，`GameState.open_panel()` 只對 OPEN 的 beat 呼叫 `enter_beat()`，而 `enter_beat()` 本身有存在性檢查（K-09）。但 view model 從此可能含有查不到資料的 beat id，任何之後拿 id 回查 `beats_by_id` 的消費端都會踩到。順帶一提，`_REASON_NIGHT_LOCKED_STUB` 是第三句住在 `PanelBuilder` 的玩家可見文字（見 K-08）。
- **解法**：P3 把收費地點做成真的時，改成回傳真實 beat ＋ 地點級 LOCKED 狀態，或在 view model 加一個 `synthetic: true` 標記讓消費端知道不要回查。

### K-36 `test_p1f` 第 10 項是走查腳本的第二份拷貝

- **Phase**：P1-F（測試）
- **狀態**：已修
- **修復時機**：**P1-F 收尾前**，跟 K-26 一起——那條把演出下沉之後，兩份自然縮成一份。
- **問題**：`tests/headless/test_p1f.gd._test_greedy_playthrough_45_days()` 自己寫了一遍 45 天迴圈（morning / afternoon / evening fixed / night fixed / sleep / advance），與 `playthrough_greedy.gd` 的主迴圈是同一件事的兩份實作。
- **影響**：兩份都要維護，而且兩份現在就已經不一樣——走查腳本會做第 45 天的比對槽放置，測試那份不會。
- **解法**：K-26 把 evening／night 下沉到 `GameState` 之後，把「一天的推進」也抽成走查腳本的一個公開函式，測試改呼叫它。
- **備註**：`playthrough_greedy.gd` 已抽出 `run_greedy_walk()` 靜態函式，`test_p1f.gd`（測試 10）統一委派調用該函式，全綠通過。

### K-37 `map_list.gd` 檔頭仍寫「白天地點清單」

- **Phase**：P1-F
- **狀態**：已修
- **修復時機**：**順手**。一行。
- **問題**：`scenes/ui/map_list.gd` 的檔頭註解寫「白天地點清單。只渲染 `PanelBuilder.available_locations()` 的結果，自己不過濾」。P1-F 之後夜間地圖也用同一個節點與同一個函式。
- **影響**：註解與事實不符。後半句仍然正確而且重要（UI 零規則），只有「白天」兩個字過期。
- **解法**：改成「地點清單（白天與夜間共用）」。
- **備註**：已更新檔頭註解。

### K-38 `playthrough_greedy.gd.uid` 未進版控（K-23 第二次發生）

- **Phase**：P1-F
- **狀態**：已修
- **問題**：commit `55d62a8` 帶進了 `flow_text.gd.uid`、`data_facts.gd.uid`、`test_p1f.gd.uid`，唯獨漏掉 `tests/headless/playthrough_greedy.gd.uid`。
- **影響**：同 K-23——clone 之後第一次 `--import` 會多出一個未追蹤檔，不同機器可能給同一個腳本不同 uid。**這是同一件事第二次發生**，代表它不會靠記性解決。
- **備註**：本次 commit 一併補進版控。要根治的話，`.gd` 新增時把對應 `.uid` 一起 `git add` 應該進 commit 前檢查清單——但那屬工具設定，不在本檔範圍。

### K-39 走查數字跑出兩種結果——**誤報，原因是跨 commit 測量**

- **Phase**：P1-F（測試）
- **狀態**：**誤報（已釐清，非缺陷）**。條目保留在原位，因為教訓比結論有用。
- **原始通報**：2026-08-14 的 P1-F 驗證中，`playthrough_greedy.gd` 先後跑出「總行動數 53／知識卡 3 張」與「54／2」，當時判定為「同一份程式碼跑出兩種結果」，並據此懷疑走查不具決定論。
- **真正的原因**：兩次測量之間，**另一個 session 在同一個 branch 上 commit 了 `d402cea`**（09:31:41），而那個 commit 正是 K-25 與 K-26 的修復——它重寫了 `playthrough_greedy.gd`（+114 行）並把晚間演出下沉成 `GameState.play_evening()`。走查從此會播殘響，殘響改寫旗標，知識卡結果因此不同。53／3 是 `d402cea` **之前**量的，54／2 是**之後**量的。程式碼確實變了，只是不是我改的。
- **為什麼 A／B 測試沒抓出來**：用 `git stash` 還原工作區只還原到「`d402cea` ＋ 未 commit 的改動」與「`d402cea`」兩個狀態，兩邊都在 `d402cea` 之後，所以都是 54／2。**乾淨的工作區不等於同一個 commit。**
- **教訓（本條保留的理由）**：**跨時間點比較測試數字之前，先確認 `HEAD` 沒有變。** 這個專案會有多個 session 同時在 `main` 上工作，`git status` 乾淨完全不代表基準相同。正確做法是每次記錄數字時一併記下 `git rev-parse --short HEAD`，比較時先比 hash。
- **現況**：在 `0a98ab9` 上連跑兩次都是 55／2，穩定。那個 +1（54 → 55）有明確原因——第 42 天上午補了四個 beat，多一個可放主角卡的行動格（見 K-27）。**走查目前沒有已知的決定論問題。**
- **仍然成立的一點**：走查的斷言不驗任何數字，只驗「跑得完」與「重置正確」。所以玩法層面的迴歸還是不會讓測試變紅。這一點不是本條的誤報部分，但也不值得現在硬寫死一個數字——等 K-25 的每日輸出穩定之後再談。

### K-40 夜間演出文字不裁切也不捲動，現在不疊字是因為文字剛好夠短

- **Phase**：P1-F（UI）
- **狀態**：已修
- **修復時機**：**夜間文字變長之前**——以先到者為準：P3 夜間層真值化，或內容期改寫夜間文本。P1 收尾不必做。
- **背景（兩個已修的疊字案例）**：K-28 把三種演出文字接進 `FlowText` 之後，夜間畫面出現疊字，分兩種情況、兩次修完。① `FlowText` 與 `MapList` 在 `main.tscn` 佔同一塊矩形（`offset` 都是 20／0／1260／400），而 `_route_view()` 的 night 分支讓兩者同時 `visible`——commit `4895499` 改成夜間上下分塊（文字 0–120、清單 130–400），其他時段各自重設回 0–400。② `_on_location_selected()` 只藏 `MapList` 不藏 `FlowText`，夜裡打開地點面板時文字蓋住面板頂端 120 px（`FlowText` 是 `ContentView` 最後一個子節點，畫在最上面）——commit `47613ec` 打開面板時藏 `FlowText`、關閉時改呼叫 `_route_view()` 恢復佈局，並於 `test_boot.gd` 補兩條斷言。**這兩件已修，本條不追蹤它們。**
- **問題**：`scenes/ui/flow_text.gd` 是純 `VBoxContainer`，逐行 `add_child(Label)`。它沒有 `clip_contents`，也沒有包在 `ScrollContainer` 裡。Godot 的容器預設不裁切子節點，所以子節點總高度超過容器矩形時，多出來的部分照樣畫在外面。夜間那條帶只有 120 px。
- **影響**：**現在安全，但靠的是內容夠短不是結構。** 實測現有夜間 `fixed` 文字最長是第 1 夜 95 字（一個 `Label`，1240 px 寬約折兩行），第 2 夜 67 字、第 24 夜 35 字，都放得進 120 px。夜間文字一旦變長（P3 把夜間層做成真的、或內容期改寫文本），就會再畫到地點清單上。**十套 headless 一條都不會紅**——它們驗的是「文字有沒有寫進 `FlowText`」，不驗版面。這是 K-01 家族的又一例：測試存在、測試全綠、測試沒看那一層。
- **解法**：三件事，前兩件是本體。
  - ① **`FlowText` 開 `clip_contents = true`**。這一行單獨就擋住溢出——超出的部分被裁掉，不會再畫到別人身上。最低成本的止血。
  - ② **`FlowText` 包進 `ScrollContainer`**，讓裁掉的部分讀得到。`flow_text.tscn` 的根改成 `ScrollContainer`，現有的 `VBoxContainer` 降一層當內容節點，`clear()` / `append_line()` 改對內容節點操作，對外 API（`clear` / `append_line` / `append_lines` / `get_lines` / `get_text`）一律不動——`main.gd` 與 `test_boot.gd` 都只碰這五個，不必跟著改。
  - ③ **補一條版面斷言**，否則這條會第三次以「沒有人看畫面」的形式回來。headless 會跑真實 layout，所以測得出來：在第 1 夜 `_route_view()` 之後，斷言 `FlowText` 內容高度不超過自身矩形高度，且 `FlowText` 的底邊不高於 `MapList` 的頂邊。放 `test_boot.gd`，接在既有那兩條夜間面板斷言後面。
- **順帶記著**：`_on_panel_closed()` 現在呼叫 `_route_view()`，所以夜裡每關一次面板就重跑一次 `GameState.play_night_fixed()`。這安全——`enter_beat()` 用 `beats_entered` 擋住重複結算，文字重印但效果不重算。改動②不會影響這條。
- **備註**：`flow_text.tscn` 根節點改為 `ScrollContainer` 且開啟 `clip_contents = true`，`flow_text.gd` 對內委派 `LinesContainer`，維持對外 5 個公開 API 不變；`test_boot.gd` 補齊 `ScrollContainer` 結構、`clip_contents` 與夜間版面分區斷言。全套 headless 全綠。

---


### K-41 UI runner 負向測試只驗非零退出、先撞錯誤分支，且覆寫正常案例報告

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15，run 11/11 負向反證全數通過）
- **修復時機**：**P2 前立即修**。
- **問題**：`Invoke-NegativeTests()` 的 missing／malformed／invalid shape／invalid day／invalid phase 案例先前借用要求 `d2_morning.json` 的 mapping，但檔名不符先被 mapping 擋下；launcher 又只看 exit code 非 0，未比對預期錯誤類型。另外負向測試曾覆寫正常案例報告。
- **解法**：每個負向案例使用獨立 run dir，保留要求檔名以通過 mapping 進 validator；每筆負向定義指定 `ExpectedErrorPattern`，斷言 exit code 非 0、命中預期原因、無 `SCRIPT ERROR` 與 ObjectDB leak。寫總表前重新讀回所有正常個案報告確保一致。
- **複驗結果**：11/11 負向測試全部以預期精確錯誤原因失敗，無任何正常報告被覆寫，無 SCRIPT ERROR 或記憶體洩漏。

### K-42 UI runner 宣稱 47 contracts，但多個 P1-A～F 案例只驗到條目的一部分

- **Phase**：P1-A～P1-F（UI 驗收）
- **狀態**：**已修並複驗通過**（2026-08-15，59 variants / 47 contracts 100% 通過）
- **修復時機**：**P2 前立即修**。
- **問題**：多個 P1-A～F UI 案例先前僅做代表性斷言或缺少操作序列（如 `p1af_15` 未對第二地點槽發送真實輸入並驗狀態不變；`p1af_17` 因 D9 取得情報順序矛盾未能證明先比對後放主角卡；`p1af_19` 未完整覆蓋 attention 免費/重複點擊路徑；`p1af_22` 僅投影 3 欄；`p1af_29` 未斷言主內容與附加 beat 順序；`p1af_32` 重置後未走到旗標控制槽驗證初始狀態）。
- **解法**：
  - `p1af_15`：對第二地點槽送真實點擊輸入，前後斷言完整 `serialize()` 不變。
  - `p1af_17`：改用 D3 下午自然持有夫妻情報卡走山泉閣比對槽，驗證比對不耗行動（`action_spent == false`），且後續仍可放主角卡消耗行動（`action_spent == true`）。
  - `p1af_19`：無 attention choice 不增、帶 attention 放置 +1、對已結算槽重複點擊不增三步全覆蓋。
  - `p1af_22`：產出正規化全狀態 serialize（排除預期差異）進行 comparison 全欄位比對。
  - `p1af_29`：以行序列斷言主內容與附加 beat 播放順序，驗證收費地點鎖定 stub。
  - `p1af_32`：結局重置後推進至 D2 上午進入山泉閣，斷言槽位回到初始未解鎖/未放置狀態。
  - `QAContractMatrix` 嚴格要求真實 evidence token，runner 判定 47 條契約全數 completed。
- **複驗結果**：全套 59 variants、47 completed contracts、0 failures 全部通過。
- **2026-08-15 追加：第一輪只收了大部分，三條尾巴由第二次外部審核抓出並補完。** 三條的共病是**斷言比條目窄、而 evidence token 的名字比案例實際做的事大**——三條的 runner 當時全是綠的。
  - `p1af_19`：原本的「免費 choice 不增加投入帳」是同義反覆（那個 choice 本來就沒有 `attention_npc`），而**真實資料裡帶 `attention_npc` 的槽只有 `d22_pm_sandbags.work` 一個、還是白天主角槽**，D22 畫面上根本沒有帶 attention 的免費槽可點。改採三分法：成功 +1 與重複點擊不計留在 runner（token 砍成兩個）；免費槽與拒收下沉 headless，`test_p1d.gd` 新增拒收合成 beat 斷言投入帳保持 0。
  - `p1af_22`：兩個變體原本吃**兩份不同 fixture**，所以比不了完整最終狀態（先是硬編碼排除清單，後改差異式，兩者都不是條目要的東西）。改成兩個變體都吃 `d22_afternoon__with_polaroid.json`——OPEN 的 choice 槽會同時建直選鈕與放卡鈕，同一份狀態走得了兩條入口。**實測兩條路的完整 `serialize()` 逐字相同**（4777 字元、同一個雜湊，手牌裡的 `equip_polaroid` 兩邊都還在，順帶證明放卡選 choice 不消耗那張卡），連「除卡片消耗外」這個豁免都用不到。
  - `p1af_32`：`reset_flag_slots_locked` 原本驗的是 `d2_morning_intro.ajie`——那是 occupant 槽（`accepts: []`、無 `requires`），不可放卡跟旗標無關。全作帶 flag／switch 條件的槽只有五個，最早在第 8 天上午，所以改成重置後推進到第二輪第 8 天上午進靜和園，斷言 `d8_morning_jinghe.go_with_ajie` 呈未解鎖、無放卡鈕、點下去狀態零變化。
  - **複驗**：全套兩輪，各 59/59 variants、47/47 contracts、11/11 負向反證、0 failed checks；11 套 headless exit 0。

### K-43 壞 `--data-root` 仍讓子 UI 進 `_ready()`；state 驗證器兩份實作已分歧

- **Phase**：UI 模擬工具鏈／QA CLI
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前立即修**。
- **問題**：壞 `--data-root` 導致子節點進 `_ready()` 噴 SCRIPT ERROR；抽共用 validator 後 `Data.load_data()` 入口漏了 `ok = false`，導致載入失敗時 `Data.ok` 保持為 `true`，引發 headless 回歸。
- **解法**：抽共用 `qa_validation.gd` 供 `main.gd` 與 `qa_runner.gd` 共用；`validate_data_root()` 採 candidate 預檢；`Data.load_data()` 入口恢復 `ok = false`，失敗時正確保持 `Data.ok == false` 阻止進入正常 UI。
- **複驗結果**：11 套 headless 測試 100% 通過（含 `test_game_state_p1a.gd` 壞 fixture 驗證）；11 筆負向反證測試全數通過。

### K-44 `-Case` 單案模式把半個 comparison group 判為通過，並仍回報 47 contracts

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前跟 K-41 一起修**。
- **問題**：單案模式先前直接篩選 case，導致 comparison group 只有單一成員時被誤判為通過，且報告仍顯示 47 contracts。
- **解法**：`-Case` 模式自動擴展同一 comparison group 與同一 contract 的所有必要 variants；報告明確區分 `CatalogContractCount` 與 `ExecutedContractCount`。
- **複驗結果**：單跑 `-Case p1af_22_choice_equiv_direct` 自動擴展為 2 variants 並正確比對；單跑 `-Case p1af_29_night_resolution` 自動擴展為 2 variants。

### K-45 `make_states.gd` 直接改 JSON 製造情境；D9 更在驗效果前預先塞入預期知識

- **Phase**：UI 模擬工具鏈（fixture）
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**P2 前立即修**。
- **問題**：`make_states.gd` 事後直接修改 JSON 注入知識或刪除手牌，破壞純走查決策產生情境的契約。
- **解法**：所有 checkpoint 狀態皆由真實走查決策自然產生，移除所有事後篡改 JSON 的 helper。
- **複驗結果**：`d9_afternoon.json` 與所有 19 個 checkpoint 均為純走查決策產出，後置條件全數滿足。

### K-46 `run_id` 只有毫秒時間戳，並行 launcher 可寫進同一個 run 目錄

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**工具鏈這輪一起收**。
- **問題**：`run_id` 僅含毫秒時間戳，並行啟動時可能碰撞導致同目錄覆寫。
- **解法**：`run_id` 改為時間戳＋PID＋隨機 GUID 短碼，並以原子方式建立目錄。
- **複驗結果**：並行啟動多個 launcher 建立隔離目錄，無碰撞。

### K-47 進度單一事實來源仍寫 17 variants／其餘 33 待做，與現行 58 variants 不符

- **Phase**：文件／進度 SSOT
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**K-41～K-45 驗證通過後更新**。
- **問題**：`PROJECT_BRIEF.md` 等進度文件仍停留在第一批 17 variants 描述，未同步現行 59 variants / 47 contracts 狀態。
- **解法**：同步更新 `PROJECT_BRIEF.md`，反映 47 條 UI 契約（59 variants）全數實作與驗收完成。
- **複驗結果**：進度文件與最新測試結果完整對齊。

### K-48 hover 同步有時序不確定性，同一份程式碼連跑會紅在不同案例

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過（2026-08-20）**。前兩次（2026-08-15、2026-08-16）都只修到症狀，真因見下方「2026-08-20 定案」段。
- **歷史狀態**：2026-08-16 重開。前次結論是「已修但不完整」（2026-08-15。修後三輪連跑 59/59 variants、47/47 contracts、0 failed checks；但當天稍晚在有殘留 Godot 行程的環境下，同一份程式碼又紅了三條，症狀完全相同）。**殘留行程那條分出去成 K-50。** 前次的修法有效——它處理「事件送出去有沒有被處理」；沒處理的是下面那一段。復發的量測見 2026-08-16 追加段。
- **問題**：K-41～K-47 收尾後的「100% 通過」不可重現。同一份程式碼連跑數輪，每輪紅的案例都不一樣（`p1af_33_full_walk`／`p1g_case_02`／`p1g_case_04`／`p1g_case_12b`／`p1af_17`／`p1af_21` 各出現過），**把改動全部 stash 回 HEAD 也照樣紅**，所以不是任何單一案例的問題。症狀統一是 `Hover 未命中目標（預期某按鈕、實際它的父容器或 HandBar）`。
  **關鍵證據**：某次誤報寫「預期 `AdvanceButton`、實際 `HandBar`、pos=(110,140)」，而矩形 dump 顯示 `AdvanceButton` 佔 y 120–160、`HandBar` 佔 y 580–720——(110,140) 明明在目標裡面，兩者差 440 像素。**座標是對的，`gui_get_hovered_control()` 回報的是上一個位置留下的舊 hover。**（本條由 codex 的獨立審查提供這項證據，修正了原本「矩形讀太早」的誤判。）
- **為什麼幾何診斷抓不到**：矩形本身是好的，這是時序問題，任何矩形相交／溢出檢查都看不見。
- **解法**（三處，缺一不可）：
  - `qa_runner.gd` 建場景前設 `Input.use_accumulated_input = false`。開著時 Godot 會把滑鼠移動事件併到下一次處理才送，hover 因此落後一拍。
  - `qa_step.gd` 新增 `send_input()`：`parse_input_event()` 之後接 `Input.flush_buffered_events()`。全檔八處輸入（移動／按下／放開／滾輪）統一走它。
  - hover 核驗改成上限 5 輪的輪詢（`HOVER_SYNC_ATTEMPTS`）：**每一輪都重讀矩形再重送事件**。只重送事件會拿到舊 hover；只重讀矩形抓不到 hover 落後那一拍。
- **複驗結果**：連續三輪全套，每輪 59/59 variants、47/47 contracts、11/11 負向反證、0 failed checks；另 11 套 headless exit 0。修之前連兩輪紅在不同案例。
- **教訓**：`Input.parse_input_event()` **不等於**事件已生效。要用 hover 狀態做斷言，就必須自己確保事件已經被處理完。重寫 `qa_step` 的人先看這一條。

**2026-08-16 追加（P2-A 驗收，重開本條）**

- **全套連跑三輪，三輪結果都不同。** 同一份 HEAD（`26cf10b`）、同一台機器、每輪起跑前的 preflight 都通過（機器上沒有既有 Godot 行程）：

  | 輪次 | 結果 | 紅的案例 |
  |---|---|---|
  | 1 | 52／53 contracts、3 failed checks | `p1af_17` |
  | 2 | 49／53 contracts、15 failed checks | `p1af_15`、`p1af_17`、`p1af_21`、`p1af_33`、等價比對 |
  | 3 | 53／53、0 failed checks | — |

  錯誤訊息與本條原始症狀一字不差：`Hover 未命中目標（預期 @Button@43、實際 @Button@46）`。

- **新觀察：單案模式也會紅，而且紅在「同時有別的 Godot 在跑」的時候。** 三輪之後單獨重跑 `p1af_17` 三次全綠。但稍後在有另一個並行全套 run（run dir `20260816-080314-183-p60212-3bfabc33`）的情況下，單獨跑 `p1af_21` 與 `p1af_33` 各紅一次，`p1af_33` 的錯誤同樣是 `Hover 未命中目標（實際 `CardsGrid`／`Card_info_festival_rules`）`，另有大量 `phase_advance` 的 `disabled` 狀態不符。**「只有全套才會紅」不成立。**
- **K-50 的 preflight 擋不住這件事。** preflight 只在整套起跑前跑一次。之後才出現的並行 runner、或套內某個案例留下的殘留行程，preflight 都看不到。上述並行期間，同一批量測有 6 次被 preflight 擋下、5 次照跑，擋與不擋只看那一秒有沒有行程活著。
- **待釐清**：全套三輪的紅與並行造成的紅是不是同一個原因。要定性必須在**乾淨桌面**（無其他 Godot、無其他 QA run、不動滑鼠）連跑五輪全套，並在每個案例前後記錄當下的 Godot 行程數。
- **同日稍晚的乾淨環境量測（`f178d56`）**：起跑前確認機器上 0 個 Godot 行程，連跑三輪全套，**每輪都是 65 variants／53 contracts／0 failed checks**，另 12 套 headless exit 0。當下寫的結論是「這支持行程之間互相干擾的解釋」——**下一段把那句收回。**
- **⚠️ 再下一次量測（`8a1ccb8`）推翻了上面那句。** 同樣乾淨環境（三輪的 preflight 都通過、起跑時 0 個 Godot 行程）連跑三輪：第 1、3 輪 66 variants／53 contracts／0 failed checks，**第 2 輪 52 completed contracts／4 failed checks**。**乾淨環境一樣會紅，所以「行程干擾」不是全部的原因。** 並行行程確實會製造同樣的症狀（見上一段的 08:04 量測），但它只是其中一條路。
- **新症狀：這次紅的不是 hover 訊息，是狀態不相等。** `p1af_23_choice_leave`（不選 choice 離開後狀態不變）的兩次 `_state()` 讀值差一個欄位——`beats_entered` 少了 `d22_pm_sandbags`。意思是**第一次進門的 beat 演出沒跑完**，第二次跑完了。根因同族（送出去的輸入沒有生效），但**只 grep `Hover 未命中目標` 會完全漏掉這一種**。重寫診斷的人注意：判斷本條有沒有復發，要看「同一份程式碼連跑結果不一致」，不是看特定字串。
- **判定為已修之前要做到的**：① 乾淨環境連跑五輪全綠 ② 補上「每個案例前後數 Godot 行程」那項儀器 ③ 失敗訊息要能區分「hover 落後」與「輸入整個沒生效」兩種，否則下次又會誤判成兩條不同的 bug。
- **影響**：`PROJECT_BRIEF.md` 與 `測試指南.md` 寫的「53 條契約 100% 通過」目前不可重現。任何依賴 UI 模擬的驗收都不能只憑一次綠燈收單，**要連跑到穩定為止**。

**2026-08-20 定案：真因是「送出滑鼠移動之後等了 2 幀才讀 hover」，被使用者的實體滑鼠插隊。**

- **機制**：案例是**開真視窗**跑的（`run_ui_sim.ps1` 只有 `make_states` 帶 `--headless`），桌面上還有一支實體滑鼠。`Viewport.gui_get_hovered_control()` 記的是「**最後一個滑鼠移動事件打到誰**」，不是每幀重算的值。`qa_step.click()` 送出移動後 `await wait_draw_frames(tree, 2)` 才讀，這段空檔裡只要使用者動一下滑鼠，作業系統補進來的真實移動事件就把 hover 蓋掉。
- **最小重現（`tests/ui_sim/k48_min3.gd` 的暫存版，未進版控）**：注入移動 → 立刻讀得到 `AdvanceBeatButton` → 補一發打到別處的移動 → 等 2 幀 → 讀到 `Main`。**與當日真實失敗訊息一字不差**（`預期: AdvanceBeatButton [...]，實際: Main [/root/Main]`）。
- **為什麼前兩次沒抓到**：這種紅**不可重現**——紅不紅取決於量測當下有沒有人碰滑鼠，不取決於程式對不對。本條原文自己寫著量測條件是「不動滑鼠」，已經摸到邊，但當時把它當成環境衛生，沒當成成因。「乾淨環境連跑三輪」這個收案標準因此無效：它證明的是那三輪沒人動滑鼠。
- **同源的第二個缺口（一併修掉）**：按下與放開之間也等了 2 幀。中間讓出執行權時，按鈕會收到 `MOUSE_EXIT`，`BaseButton.pressing_inside` 被清掉，放開時就不發 `pressed`。症狀是「click 回報成功但畫面什麼都沒發生」，與上面 2026-08-16 記的「狀態不相等、輸入整個沒生效」那一族相符。
- **修法（`qa_step.gd`）**：① 送出移動後**立刻**讀 hover，不 await；要等排版落定就等在下一輪重試的開頭 ② 按下與放開之間不再等幀。
- **防再犯**：新增 `tests/ui_sim/qa_hover_regression.gd`，用「每一幀補送一發打到別處的移動事件」扮演實體滑鼠，**不需要真的動游標、不靠運氣**。已接進 `run_ui_sim.ps1` 第 2 步，不過就整套中止。**已驗證它咬得動**：把修法還原成舊寫法，守衛立刻紅，訊息形狀與真實失敗相同（`預期 RegressionTarget、實際 RegressionHost`）。
- **複驗**：修後連跑三輪全套，每輪 **76 variants／58 contracts／0 failed checks**，守衛三輪皆過；另 14 套 headless exit 0。
- **教訓**：**「連跑 N 輪全綠」對這一族缺陷沒有證明力。** 干擾源在測試程序之外時，要嘛把干擾源模擬進測試裡（本次做法），要嘛承認驗收無效。K-39 是同一種錯誤的反面——那次是把環境差異誤判成缺陷，這次是把缺陷誤判成環境。

### K-49 契約一份報告都沒讀到時，launcher 索引 `$null` 自己爆掉

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**（2026-08-15）
- **修復時機**：**跟 K-48 一起修**。
- **問題**：`run_ui_sim.ps1` 的契約完成迴圈取 `$contractCaseReports[$contractId]`，而這個 key 只在**至少讀到一份報告**時才建立。某契約的所有變體都沒產出報告時（子程序被 timeout 殺掉、或啟動即失敗），下一行索引 `$null` 直接丟 `Cannot index into a null array`。**崩潰點在總表計算階段，會蓋掉「哪個案例失敗」的輸出**——而子程序沒產出報告正是最該看清楚的那種失敗。（本條由 codex 的獨立審查發現。）
- **解法**：取值後補 `if ($null -eq $contractReportMap) { $contractReportMap = @{} }`，讓該契約照常判定為變體失敗。
- **複驗結果**：以 `-Case p1af_33_full_walk -TimeoutSeconds 20` 逼子程序來不及寫報告。拿掉防護 → 重現 `Cannot index into a null array`；補上防護 → 乾淨回報 `report file missing or invalid` ＋ `Timeout (20 s)`，completed contracts 0、exit 1。

### K-50 殘留的 Godot 行程會污染下一輪 UI 模擬，讓失敗會傳染

- **Phase**：UI 模擬工具鏈
- **狀態**：已修
- **備註**：`run_ui_sim.ps1` 已實作 `Assert-NoExistingGodotProcesses` 預檢阻擋既有 Godot 行程；`Invoke-GodotProcess` 加入 WMI `ParentProcessId` 行程樹追蹤、所有退出路徑（正常、非零、timeout、異常）的葉至根強制作業系統終止與殘留斷言檢核。已通過 5 項驗收標準。
- **問題**：UI 模擬跑的是**有視窗**的 Godot 行程。案例失敗或被 timeout 殺掉時，runner 建立的行程樹可能沒有整棵結束；`Invoke-GodotProcess` 只在 timeout 分支對根行程呼叫 `Kill()`，沒有等待清理完成，也沒有處理後代行程。正常完成時 `WaitForExit()` 已保證根 PID 結束，**所以不能把「再檢查一次根 PID」當成修法**——真正要追的是 timeout 後尚未退出的根行程與根行程留下的後代。下一輪起跑時，那個殘留視窗會搶走作業系統焦點，Godot 便對失焦的視窗送出滑鼠離開通知、**把該視窗的 hover 狀態清掉**——於是 `gui_get_hovered_control()` 回報 null 或上一個位置的舊值，長得跟 K-48 一模一樣。
- **實測對照**（2026-08-15，同一份程式碼、同一台機器）：

  | 起跑前殘留 | 結果 |
  |---|---|
  | 有（跑完抓到 1 個 windowed 行程） | 3 案例紅、failed checks 11，錯誤訊息如「預期 `@Button@14`、實際 `@Button@15`」與「實際 `Main`」 |
  | 0（先 `Stop-Process` 清乾淨） | 59/59 variants、47/47 contracts、0 failed checks，跑完仍 0 殘留 |

- **影響**：**失敗會傳染。** 一個案例紅了留下視窗，後面的案例跟著紅，而後面那些紅是假的。這會把「哪裡真的壞了」埋掉——正是 K-41～K-49 那一整批要防的事。
- **實作契約**（三件都要，只做一件不夠）：
  1. **在建立 `_qa` run directory 與第一個 Godot 行程之前做 preflight**：列舉既有 `Godot*.exe`，有就停下並列出 PID、執行檔與命令列。⚠️ **只報告，不殺除**——那可能是使用者尚未存檔的 editor，也可能是另一輪正在跑的 QA。
  2. `Invoke-GodotProcess` 必須記住自己 spawn 的根 PID，並以 `Win32_Process.ParentProcessId` 追蹤該 PID 的**整棵後代行程樹**。清理只可對這份 owned PID 集合動手；不得用 `Get-Process Godot* | Stop-Process` 這種按名稱全殺。Windows PowerShell 5.1 沒有可靠可用的 `Kill(entireProcessTree: true)` 契約，實作不要假設它存在。
  3. 成功、非零退出、timeout 與 PowerShell 例外都走同一個 `finally` 清理：由葉到根終止仍存活的 owned PID、等待退出，再重查 owned 集合。任何一個仍存活時，**當前案例判失敗且整套立刻中止**，不得繼續跑下一案例讓失敗傳染。`Invoke-GodotProcess` 的結果要帶根 PID 與清理錯誤，讓總表能指出是哪個案例留下哪個 PID。
- **修後驗收**：
  1. 起跑前開著 Godot editor → runner 在 spawn 前 exit 1，訊息含 PID／命令列；editor 本身仍存活。
  2. 正常案例完成 → 該次 owned PID 集合為空。
  3. 用短 timeout 強制殺掉一個案例，以及用正式負向案例製造非零退出 → 兩條路徑結束後 owned PID 集合都為空，且下一個正常案例單跑為綠。
  4. 刻意讓清理失敗的反證測試 → runner 必須在該案例停止並 exit 1，不得開始下一案例。
  5. 完整 UI 套件連跑兩輪；每輪結束皆為 0 殘留，原有 59 variants／47 contracts 在 P1-H 尚未加入前維持全綠。
- **與 K-48 的關係**：K-48 修的是行程**內部**的事件與 hover 同步（有效，留著）；本條是行程**之間**的干擾。兩者症狀相同、成因不同，所以分成兩條。
- **2026-08-16 追加**：本條的 preflight 只在整套起跑前跑一次，**擋不住起跑後才出現的並行 runner，也看不到套內某個案例留下的殘留行程**。量測與影響寫在 K-48 的 2026-08-16 追加段，不在這裡重複。

### K-51 P2-A 沒補多實例發狂卡的 UI 驗收，且 `p1af_29` 的 evidence token 名稱與斷言相反

- **Phase**：P2-A（UI 驗收）
- **狀態**：**已修並複驗通過**
- **修復時機**：**P2-B 收尾前**。這是驗收覆蓋的洞，不擋 P2-B 動工。
- **問題**（兩件，同一批處理）：
  1. `實作規格書.md > P1-H` 與 `測試指南.md > P1-H` 都寫著「多實例卡（發狂卡）在手牌上各自獨立呈現、不合併計數」這一條**隨 P2-A 一起驗**（P1 的資料拿不到發狂卡，所以當時只能延後）。P2-A 完成時沒有新增任何 UI 契約，目錄總數仍是 53。手上同時有 `madness#1`／`#2`／`#3` 時，是不是各自一個按鈕、各自一個 `hand_card::` qa_id、各自顯示自己的剩餘天數——**現在沒有任何測試看這件事**。
  2. `p1af_29_night_resolution` 的 evidence token 仍叫 `night_no_madness`，但該案例的斷言在 P2-A 已經反轉成「進入收費夜間地點後手牌應獲得發狂卡」（`p1af_cases.gd` 的 `_night_resolution`、`qa_contract_matrix.gd`）。名字現在說的是反話。
- **為什麼要記**：evidence token 是給人讀的覆蓋索引。名字與斷言相反時，下一個人會照名字判斷覆蓋範圍，以為「不發卡」還有測試在守——實際上沒有。這是 K-42 那一類「報告是綠的，但綠的意思跟你以為的不同」。
- **修法**：新增一條 P2-A 的 UI 契約驗多實例顯示（目錄總數 53 → 54，`run_ui_sim.ps1` 寫死的總數要一起改）；token 改成表達現行斷言的名字。兩件事都要同步 `測試指南.md` 的條目與數字。
- **2026-08-16 進度**：**第 1、2 點均已結案。** `p1af_29` 拆出 `p1af_29_night_paid` 變體修正 token 名稱（`8a1ccb8`）；新增 `p2a_01_madness_hand_display` 契約（`p2a_multi_madness.json` 狀態含 3 張發狂卡分別為 7, 5, 3 天倒數），驗證各自獨立按鈕、各自顯示天數、點擊彈窗詳情與關閉狀態不變。契約矩陣擴充至 54 條（67 變體），UI 模擬全套 54/54 全綠。

### K-52 `_set_failure_status()` 的優先序改反了，資料寫的 `reject_reason` 被丟掉

- **Phase**：P2-A（順手改到的既有 UI 行為）
- **狀態**：**已修並複驗通過**（`259a9f0`）
- **修復時機**：**P2-B 動工前**。一行的事，而且有實質玩家影響。
- **問題**：`8a1ccb8` 接通夜間標記提示文字時，順手改了同一個檔的 `_set_failure_status()`（`location_panel.gd`），把理由的優先序倒過來：

  | | 誰贏 | 退而求其次 |
  |---|---|---|
  | 舊 | `reason_text`（資料寫的 `reject_reason`） | `_REASON_CODE_TEXTS[code]` → 原始 code |
  | 新 | `_REASON_CODE_TEXTS[code]` | `reason_text` |

  `try_place()` 對 LOCKED 槽回的是 `reason_code: "locked"` ＋ `reason_text: <資料的 reject_reason>`（`game_state.gd` 的 `_check_slot_state`）。新寫法讓 `_REASON_CODE_TEXTS["locked"]` = 「條件不足」永遠贏，**資料寫的那句話再也不會出現在畫面上**。其餘 code（`not_held`／`not_accepted`／`action_spent`……）的 `reason_text` 本來就是空字串，所以只有 `locked` 這條路有差。
- **為什麼要修**：`lint_missing_reject_reason`（lint 2）存在的唯一目的，就是逼資料為每個會灰掉的槽寫出具體理由；企劃書第十七節的硬規則也寫「灰掉一定要附理由」。把它換成通用句等於讓那條 lint 白做。`panel_builder.gd` 的 `_REASON_FALLBACK` 命名成「（條件不足）」正是因為它應該是**退路**，不是預設。
- **這條沒有測試守著**：全專案沒有任何案例斷言 `_FMT_PLACE_FAILED` 的輸出，所以這個改動不是被測試逼出來的，改回去也不會有測試變紅。**修的時候順手補一條**。
- **修法**：還原成「`reason_text` 非空就用它，否則查 `_REASON_CODE_TEXTS`，再否則印原始 code」。原始 code 那層 fallback 也要留著——查不到的 code 印出來，比印空字串好抓。
- **兌現紀錄**：commit `259a9f0`。挑字串那段抽成 `static func failure_text(result: Dictionary) -> String`，`_set_failure_status()` 只負責套 `_FMT_PLACE_FAILED` 與顯示；`location_panel` 的其他部分沒有動。`reason_text` 多做一次 `strip_edges()`，純空白也會落到第 2 層。回歸測試進 `test_p2a.gd` 第 10 組 `_test_failure_text_precedence()`，5 條斷言：Tier 1 用 `reason_code: "locked"` ＋ 非空 `reason_text`（正是被改壞的那條路）、Tier 2 兩個 code、Tier 3 未知 code、以及兩者皆空的邊界。全綠。

### K-53 45 天走查會開夜間標記了，但對開了什麼零斷言

- **Phase**：P2-A（測試）
- **狀態**：**已修並複驗通過**（`259a9f0`）
- **修復時機**：**P2-B 收尾前**，最遲不得晚於 P2-E——P2-E 要對的就是這些數字。
- **問題**：`8a1ccb8` 讓 `playthrough_greedy.gd` 的 `execute_night_phase()` 每夜貪心開一個未開的收費標記，走查因此真的會產生發狂卡（實跑手牌漲到 28／14，14 條 `hand is full` 警告）。**但整份走查腳本沒有一條斷言碰到 `night_markers_opened` 或發狂卡張數。** `open_night_marker()` 明天變成 no-op，走查照樣全綠——手牌少幾張，而跨輪重置斷言只看「重置後手牌只剩主角卡」，那條在有沒有發狂卡的情況下都會過。
- **這是第四次**：K-25（37 個行動格靜靜跳過）、K-26（走查那份不播殘響）、K-36（`test_p1f` 是走查的第二份拷貝）都是同一句話——**測試存在、測試全綠、測試沒有看那條路**。
- **修法**：走查收尾加兩條斷言，數字寫死不要用「大於 0」：
  1. 一輪結束時 `night_markers_opened.size() == 14`（全作收費標記數；來源是 `locations.json` 裡 `madness_cost > 0` 的筆數，**用資料算出來、不要在測試裡寫死 14**，否則之後調價就變成兩個事實來源）。
  2. 累計發放的發狂卡張數 == 開到的標記的 `madness_cost` 總和。P2-A 沒有任何東西會消耗發狂卡，所以這個數也等於重置前的手上張數。
- ⚠️ **不要為了讓手牌不溢位就去調 `hand_size`。** 走查漲到 28／14 是 P2-A 的正確結果（消耗要等 P2-C）。`tuning.json` 的 `_pending.hand_size` 已經記著「發狂卡開始佔格後要重驗」——那是 P2-C 之後帶著真實消耗曲線再看的事。
- **兌現紀錄**：commit `259a9f0`。`run_greedy_walk()` 在 `run_ended` 閉包裡捕獲重置前的 `night_markers_opened`、`_madness_counter` 與手牌裡的發狂卡（`end_run()` 先發 `run_ended` 再清 run 層，所以閉包看到的是重置前的值）。走查收尾對三個數做嚴格相等斷言，期望值從 `data/loader.locations` 現算（`layer == "night"` 且 `madness_cost > 0`），沒有寫死常數。**實測全部相符：收費標記 14／14 全開、累計發放 14 張、重置前手上 14 張。** 原本擔心「走查可能開不滿」——不用擔心，滿的。
- ⚠️ **第三條斷言（手上張數 == 發放張數）是 P2-A 專屬的。** P2-C 讓強制縱慾開始消耗發狂卡之後它必然不成立。**屆時要改的是斷言的形狀**（發放 == 手上 ＋ 已消耗），**不是把它放寬掉**——放寬就退回本條一開始要修的那個狀態。

---

### K-54 縱慾出口一輪只能用一次，主動縱慾上限 4 次

- **Phase**：P2-B（規則層）
- **狀態**：**已修並複驗通過**
- **問題**：`indulge()` 的最後一步寫 `slots_placed[beat_id::slot_id]`。出口 beat 的 `when` 是 `day_from: 1` → `day_to: 45`——**同一個 beat 撐滿整輪**，所以那個鍵只有 `end_run()` 會清。砸過一次東西，這一輪就再也砸不了；`PanelBuilder` 也照樣把它畫成 RESOLVED。
- **實測**：第 2 天砸東西 `ok=true`，第 3 天同一個槽 `ok=false reason=resolved`。逐時段掃過整輪，**一輪 90 個行動時段裡主動縱慾上限只有 4 次**（砸東西、暴食、泡湯，第 16 天後多一個暴力對人；奢侈缺錢卡、兩個性慾缺關係）。而一輪供給 14 張發狂卡。
- **打到的硬規則有三條**：
  1. `溫泉小鎮企劃.md` 第七節：「強制縱慾是主角自己動手的，**所以任何時刻都必須至少有一個出口點得下去**」。第一章砸過、暴食過之後池就空了。
  2. `實作規格書.md > 八`：「無條件出口（砸東西、食慾）**恆成立**」。
  3. `實作規格書.md > P2-E`：玩家 C 一輪主動縱慾 **14 次**。上限 4 次時對不上。
- **Lint 4 抓不到**：它驗的是資料（每天有沒有無條件出口），不是執行時狀態。池在執行時被 `slots_placed` 掏空，lint 4 一樣是綠的。這又是 K-01 家族——**測試存在、測試全綠、測試沒有走那條路**。
- **缺口的來源是文件**：`開發設計方針.md > P2-B` 原本第 8 步就寫著「記入 `slots_placed`」，實作照做。該行已於本次修正改掉並附理由。
- **修法**：`indulge()` 不寫 `slots_placed`。**縱慾的節流閥是行動格（一天最多 2 次），不是槽的一次性**——`slots_placed` 是為一次性敘事槽設計的，常駐出口 beat 不屬於那一類。修完上限從 4 回到 90（每個行動時段都能縱慾一次）。
- **回歸測試**：`test_p2b.gd` 三條——同一時段再縱慾被 `action_spent` 擋下（不是被 `resolved` 擋下）、換一個行動時段同槽可再用、跨日同槽可再用，並直接斷言 `slots_placed` 沒有 `exit_sanquan::x_smash` 這個鍵。**原本那條「重複放入已結算出口槽應被拒絕 (resolved)」把錯誤行為釘住了，已翻面。**
- ⚠️ **P2-C 動工時注意**：`pick_exit()` 的規格只求值 `condition` 與 `requires`，不看 `slots_placed`。現在規則層與它一致了——**不要為了「同一個出口不要連續挑到」又把一次性加回去**，那會再一次掏空挑選池。要做「不連續重複」是另一件事，走權重或冷卻，不走 `slots_placed`。

---

### K-55 泡湯在放置管線中途改 `phase`：推進鈕卡死、效果文字玩家看不到

- **Phase**：P2-B（規則層×UI）
- **狀態**：**已修並複驗通過**（`f7c95d9`）
- **問題**：`consume_action(n)` 在 `n > 1` 時直接把 `phase` 改成 `"evening"` 並發 `phase_changed`，而那是 `indulge()` 的第 4 步——**卡還在手上、`indulgence_count` 還沒加、效果還沒套**，訊號就發出去了。外面的 `main.gd` 於是在半完成的狀態上重畫整個畫面。兩個後果：
  1. **這一輪玩不下去**：`_route_view()` 把地點面板藏起來，但 `_advance_btn.disabled` 只有 `_on_panel_closed()` 會解開，而那條路要按面板上的返回鍵——面板已經藏了。實測 `advance.visible=true` 但 `advance.disabled=true`，而且沒有其他入口（`main.gd` 沒有 `_unhandled_input`、沒有快捷鍵）。
  2. **泡湯的效果文字玩家看不到**：`_route_view()` 當場清空 FlowText 並播當晚殘響；`indulge()` 之後才產生的文字被寫進已隱藏的面板狀態列。實測 FlowText 內容是「旁邊有人提起第 3 天下午發生過的一件小事」，不含泡湯那段。
- **根因是偏離契約**：`開發設計方針.md > P2-B` 寫的是「內部把當天剩下的行動時段**標成已用**」，實作改成了「**跳過**時段」。跳時段等於在原子管線中途對外廣播狀態。
- **修法**：`consume_action(n)` 改成 `action_spent = true` ＋ `_actions_spent_ahead += (n - 1)`，不動 `phase`、不發訊號；`advance_phase()` 進到下一個行動時段時扣掉一格，進到非行動時段時歸零。另補 `remaining_actions_today()` 真的算格數（原本用 `action_spent` 當代理，只在「兩格、成本 2」時剛好對）。UI 層再補一道：`_route_view()` 開頭無條件 `_advance_btn.disabled = false`，讓按鈕狀態只由目前檢視決定。
- **順帶**：`測試指南.md > P2-B` 那條「上午放進泡湯……**直接進 evening**」的描述已改——現在是按推進進到一個全灰的下午，再按一次才進 evening。
- **回歸測試**：`test_p2b.gd` 泡湯那組（時段維持 morning、`action_spent` true、推進後下午預扣、再推進進 evening）。`_actions_spent_ahead` 已進 `serialize()` / `deserialize()` 與 `end_run()` 重置。

---

### K-56 `soak_cards_cleared` 是必填 tuning 鍵，但程式一行都沒讀

- **Phase**：P2-B（數值單一事實來源）
- **狀態**：**已修並複驗通過**（`f7c95d9`）
- **問題**：`data/tuning.json` 有 `indulgence.soak_cards_cleared`，`data.gd` 也把它列進 `REQUIRED_TUNING_KEYS`，但全專案沒有任何一行讀它——`indulge()` 固定 `lose_card(card_inst_id)` 一張。現值剛好是 1 所以看不出差別，**改成 2 也不會有任何事發生**。同一段還有 `soak_phase_cost`：有讀，但 `consume_action()` 只分「是不是大於 1」，而「剩餘格數 ≥ 成本」的檢查寫成了 `action_spent == false`。
- **為什麼要記**：這違反 `AGENTS.md > 文件分工與單一事實來源`——可調數值一律住 `data/`。死值比沒有這個鍵更糟：下一個人會以為調它有用。
- **修法**：`indulge()` 與 `PanelBuilder` 都改讀 `Data.tuning()`；新增 `remaining_actions_today()` 供兩邊共用。
- ⚠️ **還有一半沒做完**：`soak_cards_cleared > 1` 時，多清的那幾張是**程式從手牌順序自動挑**的，而 `開發設計方針.md > P2-B` 第 5 步寫的是「**玩家挑哪張**」。現值為 1 所以碰不到。真的要調成 2 以上之前，得先決定 UI 怎麼讓玩家挑多張。

---

### K-57 `PROJECT_BRIEF.md` 的 P1-H 那一列被整列刪掉

- **Phase**：文件／進度 SSOT
- **狀態**：**已修**
- **問題**：`f7c95d9` 改 Phase 表時把 `| P1-H 手牌可讀性 | 🟦 | …待手動操作驗收 |` 整列刪掉，表格變成 P1-G 直接接 P2-A。P1-H 的「待手動操作驗收」狀態就這樣從單一事實來源上消失。
- **為什麼要記**：本表自己寫著「**子階段一律一列一個，不得合併**」，理由是「合併之後看不出卡在哪一個子階段」。整列消失比合併更嚴重——那個子階段連存在都看不到了。K-47 是同一類。
- **修法**：補回原列，內容一字不改。

---

### K-58 契約數 53 → 54 只改到 5 個地方裡的 2 個

- **Phase**：文件／進度 SSOT
- **狀態**：**已修**
- **問題**：`f7c95d9` 新增 `p2a_01_madness_hand_display` 之後，契約總數只在 `qa_contract_matrix.gd` 與 `run_ui_sim.ps1` 改成 54。`測試指南.md`、`開發設計方針.md`（兩處）、`PROJECT_BRIEF.md` 都還寫 53 條契約、66 變體。`PROJECT_BRIEF.md` 的當前進度段更是整段舊的——還寫著「12 套 headless」「下一步進 P2-B」。`測試指南.md` 那句「那個數字現在是 53」會直接誤導下一個要加案例的人。
- **為什麼會漏**：`測試指南.md` 自己寫著「同一組數字住四個地方，加減 checkbox 就要四處一起改」，但**那句話點的是「四個」，實際是五個**——它漏算了 `qa_contract_matrix.gd` 自己的檔頭註解，也沒把 `開發設計方針.md` 的兩處分開數。K-47 是同一族。
- **修法**：五處一起改成 58 條契約、76 個變體（P2-B 補 4 條契約 9 個變體）。**下次加契約要改的完整名單**：`qa_contract_matrix.gd` 檔頭註解、`run_ui_sim.ps1` 寫死的總數、`測試指南.md`、`開發設計方針.md`（表格列 ＋ 五步那段）、`PROJECT_BRIEF.md`（當前進度段）。

---

### K-59 強制縱慾的防吃債沒有測試守著

- **Phase**：P2-C（測試）
- **狀態**：**已修**
- **問題**：`GameState._settle_forced_indulgence()` 已於 `6e5e51a` 改成「先驗出口與槽、確定跑得起來，才 `pop_front()`」。修法是對的。但**沒有任何測試會發現它被改回去**——`Indulgence.pick_exit()` 回空字典、或 `_find_slot()` 找不到槽，只有資料壞掉時才會發生，而 `test_p2c.gd` 的 10 組沒有一組把出口池弄壞。把 `pop_front()` 移回 `pick_exit()` 之前，14 套 headless 照樣全綠。
- **影響**：這條守的是 `實作規格書.md > P2-C` 的「帳不豁免、不合併、不跳過」。回歸之後的症狀是**那張發狂卡離開 `forced_pending` 卻留在手上，這一輪再也不會被強制縱慾處理**，而且不會有任何錯誤訊息——玩家只會看到一張卡卡在手上不動。這是 K-01 家族：測試存在、測試全綠、測試沒有走那條路。
- **解法**：加一份壞資料 fixture，讓出口池整個求值不成立（例如六個出口的 `condition` 全部改成不可能成立），呼叫一次 `_settle_forced_indulgence()`，斷言四件事：① 回傳空 ② `forced_pending` 隊首**還在** ③ `action_spent` 沒被動到 ④ `indulgence_count` 沒有加。作法照 `tests/fixtures/broken/`（P2-B 的 lint fixture 已有先例），**不改正式 `data/`**。
- **備註**：`test_p2c.gd` 新增測試 11（`_test_forced_indulgence_debt_preservation_on_failure`），針對「出口池條件全不成立（`pick_exit` 空）」與「出口存在但 beat 遺失 slot（`_find_slot` 空）」兩種異常情境，嚴格斷言：① 回傳空行 ② `forced_pending` 隊首保留 ③ `action_spent` 維持 false ④ `indulgence_count` 與消除卡數不增加 ⑤ 手牌仍持有該發狂卡。並透過突變測試確認若將 `pop_front()` 提前會精確觸發測試失敗。

---

### K-60 走查認定「強制縱慾吃格」綁在出口有沒有文字

- **Phase**：P2-C（測試）
- **狀態**：**已修**
- **問題**：`playthrough_greedy.gd` 的 `diagnose_unplaced_phase()` 第 0 條，現在要 `action_spent` 為真**且 `last_forced_lines` 非空**才歸類成 `forced_indulgence`。第二個條件是 `6e5e51a` 補的（原本只看 `action_spent`，太寬——任何在時段開頭就吃掉行動格的機制，例如泡湯的 `_actions_spent_ahead` 預扣，都會被貼上這個標籤放行）。收緊是對的，但**依據挑錯了**：`last_forced_lines` 有沒有內容，取決於出口的 `on_place` 有沒有寫 `text`。那是文案，不是規則。
- **影響**：六個出口目前都有 `text`，所以走查是綠的。哪天新增一個只寫效果不寫文字的出口，或有人改寫時把某個 `text` 拿掉，走查就認不出那一格，判成非法空格並 exit 1——**程式沒壞、資料也沒壞，測試自己紅**。這種假紅比漏抓更花時間，因為第一時間會往規則層找原因。
- **解法**：改看規則層的事實——在該時段的 `advance_phase()` 前後比對 `indulgence_count`（或 `madness_cards_cleared`）有沒有加一。兩個欄位都是 `6e5e51a` 之後的既有狀態，不必新增任何東西。
- **備註**：`playthrough_greedy.gd` 已改為在 `run_greedy_walk()` 跨時段推進前後捕捉 `indulgence_count`，若時段進場時 count 增加則標記 `forced_m` / `forced_a`，並傳遞至 `execute_action_phase()` 與 `diagnose_unplaced_phase()` 比對 `action_spent and forced_indulged`，完全解耦文案依賴。全走查 47 放置 + 12 強制縱慾全綠通過。

---

### K-61 `tests/ui_sim/` 沒有任何 P2-C 契約

- **Phase**：P2-C（UI 驗收）
- **狀態**：**已修並複驗通過**（2026-08-20，K-48 結案之後才動工）
- **原修復時機**：**K-48 收掉之後**。⚠️ 不要提前做——工具不穩時補的契約，綠或紅都說明不了事情。這個順序有守住。
- **問題**：P2-C 的驗收全部落在規則層。`測試指南.md > P2-C` 的 11 條已於 2026-08-20 打勾，證據是 `test_p2c.gd` 的 10 組；但 `tests/ui_sim/` 一條 P2-C 契約都沒有。P2-B 收尾時是同一種缺口，後來補了 4 條契約 9 個變體。
- **影響**：兩件玩家看得到的事現在沒有人驗——① 強制縱慾的文字有沒有真的印在地圖上方（`main.gd._play_forced_lines()`；依 `開發設計方針.md > P2-C` 的契約，關掉地點面板回地圖之後**要還在**）② 被強制縱慾吃掉的那一格，主角卡點下去是不是真的放不進去、理由對不對。這是 K-28 那一族：規則層全綠，畫面上是壞的。
- **解法**：K-48 收掉之後補，至少三條契約——文字播出與時段內留存、被吃掉那一格的放置拒絕與理由文字、同日兩張歸零時上午下午都被吃掉的顯示。補的時候**契約總數五處要一起改**，完整名單見 K-58。
- **備註**：已補 3 條契約、3 個變體、2 個狀態（契約 58 → 61、變體 76 → 79；K-58 的五處數字已同步改完）。
  - `p2c_01_forced_text`：效果文字播在地圖上、第一次是輕的且明講下次更重、**關掉地點面板回地圖仍留存**。
  - `p2c_02_forced_action_spent`：行動格被吃掉、卡被消掉、計數 +1，且**兩個地點**都沒有主角卡放入入口（擋的是行動格，不是單一面板）。
  - `p2c_03_forced_two_same_day`：同日兩張歸零，上午一格、下午一格，那一天完全沒有行動格。
  - 狀態 `p2c_night_one_forced` / `p2c_night_two_forced`：第 10 夜、發狂卡倒數壓到 1，UI 推進一次就跨日 tick 歸零並自動結算。倒數值直接寫 `madness_clock`，作法沿用 `p2a_multi_madness`。
  - **突變檢查（確認不是同義反覆）**：① 拿掉 `main.gd._route_view()` 裡的 `_play_forced_lines()` → `p2c_01` 紅三條 ② 拿掉 `_settle_forced_indulgence()` 的 `consume_action(1)` → `p2c_02` 與 `p2c_03` 各紅三條。
  - 地點不寫死：案例從當下地圖上可進的地點取前一到兩個，資料改了不會假紅。

---

### K-62 `run_ui_sim.ps1` 靠 `Set-Location` 決定測哪個專案，但 Godot 不吃那個位置

- **Phase**：UI 模擬工具鏈
- **狀態**：**已修並複驗通過**
- **問題**：launcher 開頭 `Set-Location $projectRoot`，之後用 `System.Diagnostics.ProcessStartInfo` 起 Godot，參數是 `--path .`。但 `Set-Location` 只改 PowerShell 自己的位置，**不改行程的工作目錄**；`ProcessStartInfo.WorkingDirectory` 沒設時吃的是行程的工作目錄。所以 `.` 指向的是「呼叫這支腳本時 shell 所在的目錄」，不是腳本自己所在的專案。
- **影響**：從別的目錄呼叫 launcher，它會**安靜地去測那個目錄下的專案**，而 `--run-dir` 又是絕對路徑，所以產物照樣寫進正確的地方——看起來一切正常。K-48 診斷過程中真的踩到：把專案複製一份、在副本裡加了探針、跑了十分鐘，跑的其實是正式專案，探針一行都沒印。這是「假綠」家族的新品種：**測了，但測的不是你以為的那份程式碼**。
- **平常為什麼沒事**：使用者都在 repo 根目錄下命令，兩個路徑剛好相同。
- **解法**：`$psi.UseShellExecute = $false` 後面補一行 `$psi.WorkingDirectory = $projectRoot`。全部 `--path .` 的呼叫點（守衛、`make_states`、manifest、逐案例、負向測試）共用同一個 `Invoke-GodotProcess`，所以一處就全收。
- **備註**：已於 2026-08-20 修正並複驗。**驗法**：把一份**故意弄壞**的專案副本當作行程的工作目錄，從那裡呼叫正式 repo 的 launcher——修好之後它仍然正確地測正式專案（run dir 落在正式 repo、壞掉那份沒被載入、案例通過）。反向證據是本次 K-48 診斷的中段：修之前，加了探針的副本跑了十分鐘，探針一行都沒印，因為跑的是正式專案。

---

### K-63 「第一輪碰不到 BE」那條沒玩任何一輪，只斷言 tuning 值

- **Phase**：P2-D（測試）
- **狀態**：已修
- **問題**：`測試指南.md > P2-D` 的最後一條寫「正式資料上（cap 7）**第一輪碰不到 BE**——三種玩家重演都不觸發」，該條已於 2026-08-20 打勾。但 `test_p2d.gd` 的 `_test_real_data_cap_unreached_in_run1()` 全部內容只有兩行斷言：`tuning.madness_cap == 7`、`tuning.madness_vision_threshold == 3`。**它沒有玩任何一輪。**
- **影響**（兩件事，第二件比較嚴重）：
  1. 條目宣稱的事沒有人驗。引擎哪天讓第一輪撞到 BE，這條照樣綠。這是 K-42 家族。
  2. **它把可調數值釘死在測試裡。** `data/tuning.json` 自己的註解就寫著「第二輪調價要挑幾個深標記調」，也就是 `madness_cap` 本來就預期會動；一動這條就變假紅——**程式沒錯、資料也沒錯，測試自己紅**。這與 K-60 同族，也直接違反專案「文件與測試不寫可調數值，一律指 `data/tuning.json`」的規則。
- **解法**：真正的斷言**已經存在**——`playthrough_greedy.gd` 在真資料上走完 45 天，並斷言「`run_ended` 恰好發射 1 次、`ending_id` 為 `ending_default`」。把這條指過去：在 `test_p2d.gd` 呼叫 `PlaythroughGreedy.run_greedy_walk()` 之後斷言收到的結局不是 `ending_madness_be`（順帶記錄該輪桌面峰值），並**刪掉那兩行 tuning 值斷言**。
- **備註**：已於 2026-08-20 修復。`test_p2d.gd` 測試 6 改呼叫 `PlaythroughGreedy.run_greedy_walk()` 完整走完 45 天，斷言 `last_ending_id == "ending_default"` 且非 `ending_madness_be`、`run_ended_count == 1`、重置前持卡數低於 `tuning.madness_cap`，並刪除硬編碼數值斷言。

---

### K-64 視野門檻驗在合成 beat 上，不是真資料

- **Phase**：P2-D（測試）
- **狀態**：已修
- **問題**：`實作規格書.md > P2-D` 與 `開發設計方針.md > P2-D` 都寫「`madness_at_least` **在真資料上**生效」——方針那句更明確：「P2-D 不動求值器，只驗它在真資料上生效」。但 `test_p2d.gd` 的 `_test_vision_threshold_visibility()` 自己造了一個 `test_beat_vision_threshold` 掛 `condition: { madness_at_least: 3 }`，塞進 `loader.beats` 再驗。
- **影響**：這個寫法證明的是求值器與 `PanelBuilder` 會動——那是 P1-D 早就驗過的事。**真資料那三處若鍵名寫錯（例如打成 `madness_atleast`），這個測試照樣綠**，而規格要驗的正是那三處。
- **解法**：真資料的三處住 `data/beats/ch2_nights.json`（颱風四夜）。該檔註解自己說第一輪摸不到那個門檻（最貪心的玩家只有 2 張），所以**測試要自己注入 3 張**再斷言那個**真** beat 出現、降到 2 張再消失。合成 beat 那段可以保留當單元層，但真資料那條不能少。
- **備註**：已於 2026-08-20 修復。`test_p2d.gd` 測試 1 補齊真資料 `ch2_nights.json` 之 `d23_night_bleed`、`d24_night_bleed` 與 `indulgence_exits.json` 之 `exit_sanquan` 的 `madness_at_least` 求值顯隱驗證（0/2 張 false、3 張/1 張 true、降回後 false）。

---

### K-65 `EffectApply.apply()` 在 BE 觸發後仍繼續套 `flag`

- **Phase**：P2-D（規則層）
- **狀態**：已修
- **問題**：`apply()` 的鍵順序是 `text` → `gain` → `lose` → `switch` → `switch_progress` → `relation` → `madness` → `flag`。P2-D 讓 `madness` 那一步在批次發完後呼叫 `_check_madness_cap()`，撞破 cap 就 `end_run()`。**但 `flag` 排在 `madness` 後面**，所以 `end_run()` 重置之後，`flag` 會寫進**新的一輪**。
- **影響**：這正是 `實作規格書.md > P2-D` 的「第二張之後的 `gain_card` 落在已重置的 run 上」要擋的那一族——P2-D 擋了發卡，沒擋後面的鍵。現行真資料摸不到：三處 `madness: 1`（`data/beats/ch1_d01_d03.json`）都只帶 `text`，沒有 `flag`。
- **解法**：`_check_madness_cap()` 之後判斷本輪是否已重置（例如比對呼叫前後的 `day`／`phase`，或讓 `_check_madness_cap()` 回傳有沒有觸發），已重置就 `return`，不再套後面的鍵。順帶在 `開發設計方針.md > P2-D` 補一句「效果鍵在 BE 之後不再套用」，否則下一個人會再踩一次。
- **落地**：`ba6f9b8` 新增單調遞增且不進存檔的 `run_generation`。`EffectApply.apply()` 在 lose／gain／switch／switch_progress／relation／madness 各鍵後檢查世代，偵測 `end_run()` 即回傳空文字、不再套後置鍵；P4-B 延遲回報結算器亦在 append 文字前檢查世代並立即返回。`test_p4b.gd` 第 9 組用同一筆 `text + madness + flag` 觸發 BE，另排第二筆 report，嚴格斷言同筆 flag、跨筆 flag 與回報文字均未污染新輪。verifier 專屬與全套 24 套 headless 均 exit 0。

---

### K-66 cap 數的是 `madness_clock.size()`，契約寫的是「`hand` 內 madness 數」

- **Phase**：P2-D（契約對齊）
- **狀態**：**已修**（`9e342de`，2026-08-20）
- **落地**：`_check_madness_cap()` 改成數 `hand` 裡 `_card_base_id() == "madness"` 的張數，與方針契約一致，也與檔內其他地方共用同一個 base id 取法。驗證：16 套 headless 全綠、`test_p2d.gd` 六組全綠。
- **問題**：`開發設計方針.md > P2-D` 寫「cap 檢查掛在 `gain_card()` 加入 madness 實例之後那一步：**`hand` 內 madness 數** ≥ `madness_cap`」。實作 `_check_madness_cap()` 數的是 `madness_clock.size()`。
- **影響**：兩者目前恆等——`gain_card()` 與 `lose_card()` 同步維護兩邊，`_check_hand_overflow()` 只警告不丟卡。所以**現在沒有行為差異**。但契約與實作講的是兩個不同的東西，哪天出現「在手上但沒有錶」或「有錶但不在手上」的路徑，就會靜默分歧。
- **解法**：擇一對齊。建議改實作去數 `hand` 裡 base id 為 `madness` 的張數（契約講的是玩家看得到的東西），或把方針那句改成 `madness_clock.size()` 並寫明兩者恆等的理由。

---

### K-67 BE 顯示測試直接呼叫 `main._on_run_ended()`，訊號接線沒驗到

- **Phase**：P2-D（測試）
- **狀態**：**已修**（`9e342de`，2026-08-20）
- **落地**：`_test_be_flow_text_display()` 改成用 fixture 把 `madness_cap` 壓到 2，再連呼兩次 `gain_card("madness")` 撞破 cap，由 `GameState` 自己發 `run_ended`；一般結局那半也改走 `gs.end_run("ending_default")`。訊號接線斷掉這條會紅。跑完把 `madness_cap` 還原。
- **問題（原文）**：`test_p2d.gd` 的 `_test_be_flow_text_display()` 實例化 `main.tscn` 之後，直接 `main.call("_on_run_ended", "ending_madness_be")`。
- **影響**：測到的是顯示分支，不是接線。**`GameState.run_ended` 與 `_on_run_ended` 的連線斷掉，這條照樣綠**——而那正是玩家會看到「輪結束了但畫面沒反應」的情況。專案自己的 UI 模擬教條寫著「不准用 `button.pressed.emit()`，那樣測到的是訊號接線不是使用者點得到點不到」；這裡是同一個問題的反面：測到分支，沒測到接線。
- **解法**：改成用真的規則層入口把 cap 撞破（`gain_card` 到 `madness_cap`），讓 `GameState` 自己發 `run_ended`，再驗畫面上的字。

---

### K-68 `tests/ui_sim/` 沒有任何 P2-D 契約

- **Phase**：P2-D（UI 驗收）
- **狀態**：已修（`0047aaa`，P3-D 補上第三腳）
- **修復時機**：**P3-D。** K-69 已於 P3-C 解封，剩下的那一腳不再有結構障礙。
- **2026-08-22 進度（`bdfa5fd`，P3-C）**：K-69 修好之後畫面上有節點可查了。`p2d_01_vision_zero` 加了「2 張時畫面上看不到『二樓有人在走』」、`p2d_01_vision_three` 加了「3 張時看得到」，兩條都讀 UI 文字而非 `flags`。**①的三腳現在成立兩腳**，缺的是「清掉一張又消失」——現有兩個案例是兩個獨立定態 fixture，不是同一個狀態的轉換，所以證不了「清回去會消失」。
- **2026-08-20 進度（`9e342de`）**：補了兩條契約（61 → 63 條、79 → 82 個變體）。
  - ② `p2d_02_be_screen` **算補完了**：查 `[發瘋 BE]` 有沒有印在畫面上、有沒有誤播一般結局骨架、地點面板有沒有收掉。三項都讀 UI dump。（餘下的地圖與推進鈕另立 K-70。）
  - ① `p2d_01_vision_visibility` **沒補到位**：`p2d_01_vision_zero` 與 `p2d_01_vision_three` 判定通過與否的依據是 `serialize()` 裡的 `flags.awei_heard_it`，不是畫面上有沒有那個節點。**驗到的東西跟 `test_p2d.gd` 是同一件事**，本條要擋的正是這個。
  - **①做不到的真正原因是 K-69**：真資料裡掛 `madness_at_least: 3` 的三格（`d23`／`d24`／`d25_night_bleed`）都是旅館的當夜定日 beat，走 `sleep_night()` 解析；而那條路徑的文字被 `main.gd` 丟掉，畫面上兩種情況都是空的。**沒有可查的節點，就分不出「看不到」與「灰掉」。** 先修 K-69。
- **問題**：`測試指南.md > P2-D` 的前兩條是操作層條目——「桌上 2 張時掛 `madness_at_least: 3` 的內容**看不到**（不是灰掉）」「3 張時出現，清掉一張又消失」。兩條已打勾，證據是 `test_p2d.gd` 的規則層與 view model 斷言；`tests/ui_sim/` 一條 P2-D 契約都沒有（契約數仍 61）。
- **影響**：「不是灰掉」這句話**只有畫面驗得出來**——規則層回傳 HIDDEN 與畫面上真的沒有那個節點，是兩件事（K-28 家族：規則層全綠、畫面上是壞的）。另外 BE 觸發當下的畫面（`[發瘋 BE]` 有沒有真的印出來、地圖與面板有沒有收掉）也沒有契約在守。
- **解法**：至少兩條契約——① 視野門檻的三態（2 張看不到、3 張出現、清掉又消失），驗的是**節點存不存在**不是 disabled ② BE 觸發後的畫面（結局文字出現、地圖與地點面板收掉、推進鈕的狀態）。補的時候契約總數五處要一起改，名單見 K-58。

---

### K-69 睡覺解析出來的 beat 文字被丟掉，玩家永遠看不到

- **Phase**：P1-F（UI×規則層接線）
- **狀態**：**已修（2026-08-22，P3-C `bdfa5fd`）**
- **兌現**：`_on_advance_pressed()` 夜間分支改走 `GameState.resolve_night_advance()`；`advance == false` 時把 lines 寫進 `FlowText` 並 `return`，本次點擊不換日，第二次點擊才 `advance_phase()`。證據是 `p2d_01_vision_three` 的兩次真實點擊：第一次後 day 仍為 24、`night_sleep_pending` 為 true、畫面上讀得到「二樓有人在走」，第二次才進 D25。
- **問題**：`main.gd._on_advance_pressed()` 在夜間呼叫 `GameState.sleep_night()`，**拿到回傳的文字行卻沒有用**，下一行就 `advance_phase()`。推進到隔天上午會走 `_route_view()` → `_play_forced_lines()` → `_flow_text.clear()`。那幾行字從頭到尾沒有進過 `FlowText`。
- **影響**：`實作規格書.md > 九` 寫「『直接睡』不是空動作：解析掛在旅館（`sanquan`）上的當夜定日 beat，condition 通過才播」。**規則層有播（旗標會落帳），畫面沒播。** 打到的內容是颱風四夜的外溢 beat：`d23_night_bleed`（邊界壞掉）、`d24_night_bleed`（二樓有人在走）、`d25_night_bleed`（湯屋有水聲）。這三格是第一輪玩家「意識到夜鎮不需要等到晚上」的那一段，玩家目前一個字都看不到。
  - 連帶擋住 **K-68 ①**：沒有節點可查，就驗不了「看不到，不是灰掉」。
  - `測試指南.md` 第 24 夜那條已打勾，但它只證明了規則層。已在該條補上 ⚠️。
- **解法**：`_on_advance_pressed()` 夜間分支把 `sleep_night()` 的回傳行接進 `_flow_text`，並且**停一拍讓玩家讀完再推進**（照 `_play_evening()` 的作法：先顯示，下一次點推進才換時段）。停一拍是必要的——現在的流程是一鍵直接跨到隔天上午，接了字也會立刻被 `clear()` 蓋掉。⚠️ 這一改會動到夜間推進的節奏，要跟 K-33（夜間推進無條件呼叫 `sleep_night()`）一起想。

---

### K-70 `be_map_hidden` 這個 evidence token 名不符實

- **Phase**：P2-D（UI 驗收）
- **狀態**：待修（低）
- **修復時機**：**下次動 `tests/ui_sim/cases/p1af_cases.gd` 時順手。** 兩行斷言的事。
- **問題**：`p2d_02_be_screen` 送出的 evidence token 有 `be_map_hidden`，但實際斷言查的是 `panel_back`（地點面板的返回鈕）有沒有可見，**沒有查地圖**。地圖按鈕的 qa_id 是 `location::<id>` 前綴，案例基底類別已經有現成的 `_visible_ids(tree, "location::")` 可以用。K-68 解法原文另外要求驗「推進鈕的狀態」，也沒驗。
- **影響**：BE 觸發後地圖沒收掉的話，這條契約照樣綠。`_route_view()` 的結局分支確實有 `_map_list.visible = false`，所以現在行為是對的——壞的是守衛，不是實作。
- **解法**：補兩行——`assert_true(_visible_ids(tree, "location::").is_empty(), ...)` 與推進鈕可見且未 disabled 的斷言。token 名維持 `be_map_hidden`，補完之後名實才相符。

---

### K-71 契約數 61 → 63 與 headless 套數 15 → 16 又只改到一部分

- **Phase**：文件／進度 SSOT
- **狀態**：**已修**（本次修正，2026-08-20）
- **問題**：K-58 已經把「加契約要改的五個地方」列成名單。`9e342de` 補完 P2-D 兩條契約之後，只改到三處（`qa_contract_matrix.gd` 檔頭、`run_ui_sim.ps1` 寫死的總數、`開發設計方針.md` 兩處）。`測試指南.md` 與 `PROJECT_BRIEF.md` 還寫 61 條、79 個變體。同一批還漏了另外兩個數字：
  - **headless 套數**：新增 `test_p2_sim.gd` 之後實際是 **16 套**，commit message 與 `PROJECT_BRIEF.md` 都還寫 15 套。
  - **走查基準**：`測試指南.md > P2-E` 要求把走查 90 格的新分類記進 commit message 當下次基準，`9e342de` 沒記。
- **為什麼又漏**：K-58 的名單只涵蓋「契約數」，沒有涵蓋「headless 套數」與「走查分類」。這兩個數字散在 `PROJECT_BRIEF.md` 的當前進度段與資料層現況段，加測試的人不會想到要去看。
- **修法**：五處全部補齊為 63 條契約、82 個變體；headless 套數改 16；走查新數字寫進 `PROJECT_BRIEF.md` 的資料層現況段（**不是只寫在 commit message 裡**——commit message 查起來要翻 `git log`，而下一個人是在 brief 裡找基準的）。**下次加測試要改的完整名單**：`qa_contract_matrix.gd` 檔頭註解、`run_ui_sim.ps1` 寫死的總數、`測試指南.md`、`開發設計方針.md`（表格列 ＋ 五步那段）、`PROJECT_BRIEF.md`（當前進度段 ＋ 資料層現況段的走查數字）。

---

### K-72 `test_p2_sim.gd` 兩處自行重算，不是讀引擎的結果

- **Phase**：P2-E（測試）
- **狀態**：待修（低）
- **修復時機**：**下次動 `test_p2_sim.gd` 時順手。** 目前兩處都與引擎等價，沒有行為差異。
- **問題**：兩處：
  1. `_count_madness_in_hand()` 用 `str(card).begins_with("madness")` 認定發狂卡，`game_state.gd` 用 `_card_base_id()`。目前等價，因為沒有別的卡 id 以 `madness` 開頭。哪天有了，測試會多數幾張。
  2. 最重級次數是測試自己呼叫 `Indulgence.level_for(indulgence_count, tuning)` 重算的，不是讀引擎實際套用的級別。用的是同一個生產函式，所以不算另寫一份模型；但「引擎套用的級別」與「重算的級別」哪天分岔，這個指標測不出來。
- **影響**：現在沒有。是守衛的精準度問題，不是行為問題。
- **解法**：① 改用 `DataFacts` 或與 `game_state.gd` 共用的 base id 取法 ② 讓 `_settle_forced_indulgence()` 把套用的級別記進一個 run 層欄位或回傳值，測試改讀那個。②要動規則層，成本比①高，可以只做①。

---

### K-73 第一輪走完所有收費 row 的玩家，第二輪沒有新的發狂卡供給

- **Phase**：P3-B（設計真值）
- **狀態**：已修（文件）
- **修復時機**：已於 P3 開工前掛回待決 25／26；**機制本身不在 P3 解決**。
- **問題**：**以現行資料為準**，全作發狂卡只有兩個來源，實測過：
  1. 14 個 `locations.json > night[].madness_cost`（marker cost）。
  2. `EffectApply` 的 `madness` 鍵——全資料只有 `n3_map_opens` 的三個槽在用，而 `開發設計方針.md > P3-C` 明文要刪掉它們。

  P3-B 把 marker cost 改成**終身第一次主動到訪**才收，於是**第一輪已經走完全部收費 row 的玩家，第二輪拿不到任何新的發狂卡**，倒數、強制縱慾、視野門檻、發瘋 BE 在第二輪都不會觸發。
- **邊界（不要讀成全稱）**：這**不是所有玩家必定為零**。第一輪漏掉的收費 row，第二輪第一次去仍然會發卡。但 `subdocs/驗證/發狂卡機制模擬.md` 的 A 深潛／B 典型／C 謹慎三種正式模擬**都開滿 14／14**，所以對現行基準而言第二輪確實是零——問題出在基準，不是出在規則。
- **另一半：第二輪的故事還沒寫。** 目前只討論完第一輪（待決 25 開局後續輪次解鎖清單、待決 26 第二輪差異表都還開著）。第二輪的新內容本來就可能帶進新的收費 row 或新的發狂來源，所以這條**不是「設計有洞」，是「現在還算不出第二輪的供給」**。要防的是在那些內容進來之前，先把「第二輪 0 張」當成正確行為釘死。
- **影響**：`實作規格書.md > P3-F` 把「第二輪 seen paid 不發卡」寫成驗收綠燈。那條驗的是規則（終身首次收費）本身，沒有錯；但它旁邊沒有任何一句話說「所以現行基準下第二輪壓力是 0，替代來源待第二輪內容決定」，綠燈之後就不會再有人想起來。連帶 P5（迴圈）與內容期的第二輪設計。
- **兌現**：待決 25／26 已記「第二輪供給隨第二輪內容一起定」與現行邊界。方向定為：主要壓力來自第二輪新增／深化的收費地點或 row，事件發卡只作補充；P3-F 只驗終身首次收費規則，不把現有 row 的第二輪 0 張當成壓力驗收，也不提前補內容。

---

### K-74 D15 fixed 讓 `n_plaza` 出現 13／14 兩種第一輪供給路徑

- **Phase**：P3-F（驗收條目）
- **狀態**：已修（文件；基準待 P3-F 真流程重跑）
- **修復時機**：規格已先改；完整時間軸在 P3-F 重跑。
- **問題**：`n15_everyone` 掛在 `n_plaza`——**14 個收費 row 裡唯一一個掛 night-layer fixed 的**（`n_corridor`／`n_exit` 都是 `madness_cost` 0）。`實作規格書.md > 九` 拍板「fixed 強制到訪免 marker cost」，所以第 15 夜之後 `n_plaza` 對所有輪次永久免費。它的 `earliest_night` 是 14，玩家只有第 14 夜一個窗口能選擇是否先主動付那 1 張；第 14 夜改走同樣剛開的 `n_ahong_3`，就是效率路徑 13，先走 `n_plaza` 則是最大壓力路徑 14。加上 P3-C 新增「night-layer fixed 消耗今晚」，第 1、2、15 三夜從探索池被拿走。
- **影響**：P2-E 的程式基準依 `available_locations()` 資料順序拿第一個未見收費 row；`locations.json` 恰好把 `n_plaza` 排在 `n_ahong_3` 前，所以現行模擬會走 14。手算若按阿宏鏈優先則走 13。兩者都合法，差異是策略而不是資料錯誤；把 row 對調只會把隱含策略換掉，不是修法。原三份文件仍有契約不一致：
  - `實作規格書.md > P3-F`：「第一輪走查的發狂卡帳**仍**逐項對上 P2 模擬」
  - `開發設計方針.md > P3-F`：「若數值改變，先更新模擬，再同步 P2-E 基準」
  - `測試指南.md > P3-F`：「若不一致，先審固定演出占夜與策略差異，再更新基準」

  規格書把單一路徑當唯一基準；模擬又把資料順序誤當玩家策略，兩邊都需要改成具名案例。
- **兌現**：不把 13 或 14 當唯一正解，P3-F 明列兩條語意策略：① 路徑效率——D14 走 `n_ahong_3`、D15 fixed 免費取得 `n_plaza`，走滿收費 row 收 13 marker cost；② 最大壓力——D14 主動走 `n_plaza`，收 14。測試必須明示 id，不再取資料順序第一筆，也不對調 `locations.json` 製造結果；完整倒數／縱慾時間軸仍要按模擬文件流程重跑。

---

### K-75 P3-D 同一節兩條打架：選定之後地圖還剩幾個項目

- **Phase**：P3-D（規格）
- **狀態**：已修（文件）
- **修復時機**：已於 P3-D 開工前拍板。
- **問題**：`開發設計方針.md > P3-D` 一節裡：
  - `available_locations()` night 分支「**chosen 非空時只回 chosen**」
  - `can_enter` 那條又說「今晚已選擇或被 fixed 強制帶入後**仍可查看詳情**，但進入按鈕停用」

  前者成立時，後者只剩下自己那一格可看，是空話。`實作規格書.md > 九`「地圖項目本身只負責開詳情」偏向保留全清單。
- **影響**：P3-D 至少 2 條 UI 契約的預期值；以及 D1／D2／D15 被 fixed 強制帶走的那三夜，玩家能不能翻看別的夜間地點。
- **兌現**：選擇保留全清單。`available_locations()` 不按 chosen 過濾；主動選擇或 fixed 強制帶入後，所有可見 row 保持原排序且詳情仍可看，只有 `location_summary().can_enter` 將全部進入鍵停用。

---

### K-76 `sleep_night()` 走 resolver 之後，condition 誰來求值沒寫

- **Phase**：P3-C（契約缺口）
- **狀態**：已修（文件）
- **修復時機**：已於 P3-C 開工前補進 `開發設計方針.md`。
- **問題**：兩處：
  1. `night_beats_at()` 明文「只做資料形狀選擇，**不求值 condition／requires**」，但現行 `sleep_night()` 是自己 `ConditionEval.eval()` 過才播的，`d23`～`d26_night_bleed` 四個颱風夜全靠 condition 控制。方針沒有一句話說呼叫端要把求值補回去——最省事的實作會把四夜的 condition 弄丟，而且四夜都會照播。
  2. `night_beats_at(day, location_id, current_chapter)` 沒說要濾 `when.phase == "night"`。睡覺解析吃的是 `sanquan`，而 `sanquan` 有 40 幾筆白天 dated beat；不濾 phase 會挑錯 primary。
- **影響**：颱風外溢四夜的觸發條件；睡覺解析的正確性。兩條都在 K-69 修好之後才會被玩家看到，所以錯了不容易當場發現。
- **補查後發現原修法仍不夠**：若結構 resolver 先只回唯一 primary，呼叫端才發現 exact dated `condition=false`，章節變體已被丟掉，無法 fallback。正式資料的 `n_gathering_d31` 就是這種案例；`requires` 又不能用同一規則，因為它應保留選中內容並呈 LOCKED，而不是退回舊章節。
- **兌現**：DataLoader 改回傳 ordered `primaries`＋`addons` 候選，定日候選嚴格用 `DataFacts.beat_matches_time(..., "night")` 排除白天 beat；GameState 的 `resolved_night_content()` 再求值 condition，選第一個成立 primary 並過濾 addons。requires 不參與 fallback；PanelBuilder 呈 LOCKED，sleep 跳過不能執行的內容。測試加入白天反證、condition fallback、requires 不 fallback 與 D31 跨輪案例。

---

### K-77 `night_beats_at()` 與既有 `DataFacts.beat_matches_time()` 職責重疊沒交代

- **Phase**：P3-C（契約缺口）
- **狀態**：已修（文件）
- **修復時機**：已與 K-76 同批處理。
- **問題**：`DataFacts.beat_matches_time()` 已經在處理夜間三種形態（無 `when` ＋ `chapter` ≤ 當前 → true；無 `when` 無 `chapter` → true），而 `GameState._is_beat_time_valid()` 就是它的 wrapper。`開發設計方針.md > P3-C` 要求 `PanelBuilder.build()`／`sleep_night()`／`_is_beat_time_valid()` 三處都改走 `night_beats_at()`，卻沒說 `beat_matches_time()` 的夜間分支要不要退場，也沒說 `_is_beat_time_valid()` 改接 resolver 之後白天 beat 走哪一條。
- **影響**：收尾可能留下兩套夜間時間判準，而 K-30 的整條理由就是「不要有多份自己掃 `when` 的程式」。
- **兌現**：職責拆成兩層而非互相呼叫：`DataFacts.beat_matches_time()` 只管有 `when` beat 的日期／時段，無 `when` 一律 false；`night_beat_candidates()` 專管夜間集合與版本優先序；`resolved_night_content()` 專管有狀態的 condition 求值。`_is_beat_time_valid()` 非 night／night fixed 走 DataFacts，night non-fixed 只接受 resolved 結果，收尾不留第二套夜間 `when` 掃描。

---

### K-78 lint 編號倒序：P3-B 引入 lint 14、P3-C 引入 lint 13

- **Phase**：文件／規格
- **狀態**：已修（文件）
- **修復時機**：已在 P3-B 開工前對調。
- **問題**：`開發設計方針.md` 裡 P3-A 引入 lint 11／12、**P3-B 引入 lint 14（`lint_legacy_night_flags`）**、P3-C 引入 lint 13（`lint_night_once`）。照階段順序實作會先有 14 才有 13。
- **影響**：純命名。`verify_data.gd` 的輸出順序與文件敘述會有一段時間對不上。
- **兌現**：P3-B 的 `lint_legacy_night_flags` 改為 lint 13，P3-C 的 `lint_night_once` 改為 lint 14；規格、方針與測試指南引用一起對調。

---

### K-79 lint 12 對 teaser row 仍要求 `chapter` 與 `earliest_night` 一致

- **Phase**：P3-A（lint 規約）
- **狀態**：已修（文件）
- **修復時機**：已在 P3-A 契約拍板。
- **問題**：`開發設計方針.md > P3-A` 的 lint 12 驗 `chapter == chapter_for_day(earliest_night)`，對所有 night row 一視同仁。但 `實作規格書.md > 九` 明說 teaser「從該 row 的 `chapter` 起顯示，**永遠 LOCKED，不受 `earliest_night` 解鎖**」，`data/SCHEMA.md` 也只要求 teaser 的 `madness_cost`／`day_counterpart`／`night_reveal` 為 null，沒管 `earliest_night`。等於逼作者維護一個規格說沒意義的欄位。
- **影響**：現行資料剛好過（`n_corridor_end` 是 chapter 3／earliest 45，`chapter_for_day(45)` 也是 3），所以不會馬上紅。日後新增 teaser 才會踩到。
- **兌現**：採 ②。teaser 顯示只看 `chapter`、不由 `earliest_night` 解鎖，但 authored `earliest_night` 仍必須落在同章，作為資料一致性守衛；規格、方針與 Schema 已寫明。

---

### K-80 方針把 `opened_n_*` 退場講得比實際大

- **Phase**：P3-B（方針措辭）
- **狀態**：已修（文件）
- **修復時機**：已在 P3-B 開工前把範圍寫準。
- **問題**：實測全資料 13 個 `opened_n_*` write，只有 4 個被讀：
  - `opened_n_ahong_1`／`_2`／`_3`：`locations.json` 裡 `n_ahong_2`／`_3`／`_4` 的 `requires`，另外 `_1`／`_2` 還各被一個 beat 的 `requires` 讀
  - `opened_n_gathering`：一個 beat 的 `condition`

  其餘 9 個（`opened_n_ahong_4`／`_5`／`_6`、`opened_n_litcorridor`、`opened_n_higher`、`opened_n_emptyspot`、`opened_n_emptyspot_2`、`opened_n_steam_below`、`opened_n_behind_temple`）是純寫入的死旗標，直接刪即可。
- **補查後的影響**：原提案把 4 個被讀旗標全改 `night_seen` 會真的改錯。`enter_night_location()` 在內容解析前先寫 meta seen；若 D31 首次進聚會，`night_seen: n_gathering` 會立刻成立，使特殊內容誤播，而且該條件還會錯誤跨輪。原 `opened_n_gathering` 實際代表本輪較早看過聚會常態內容，不是單純到訪。
- **兌現**：P3-B 文件已寫定：阿宏前三級的 route／beat requires 改 `night_seen`，`n_ahong_5`～`_7` 的 `k_ahong_point_*` 不動；9 個純寫入旗標刪除；聚會旗標改名為 run flag `saw_n_gathering_intro`，由常態內容寫入、D31 特殊內容讀取。lint 仍全面禁止殘留 `opened_n_*`。本條結的是契約缺口，資料遷移仍由 P3-B 實作。

---

### K-81 `laozeng_patrol_d24` 是 flag 不是 beat id

- **Phase**：文件措辭
- **狀態**：已修（文件）
- **修復時機**：已隨本次 P3-C 文件修正。
- **問題**：`測試指南.md > P3-C` 寫「D24 第一輪與第二輪各播一次，`laozeng_patrol_d24` 每輪成立」。`laozeng_patrol_d24` 是 beat `d24_night_laozeng` 的 `on_enter.flag`，不是 beat id。字面沒有錯（run 層 flag 每輪重置，所以「每輪成立」是對的），但和前半句的「各播一次」放在一起，很容易被讀成 beat id。
- **影響**：無行為影響。寫測試的人可能去找一個不存在的 beat。
- **兌現**：測試句改成「第一輪與第二輪各播一次 `d24_night_laozeng`；每次播出後，其 `on_enter.flag` `laozeng_patrol_d24` 於該輪成立」。

---

### K-82 睡覺停拍那一拍可以繞過「一夜一地點」

- **Phase**：P3-B／P3-D（規則層）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-B 開工前**。這是規則層的入口契約，寫下去之後 P3-D 只是照抄，補在後面要改兩個地方。
- **問題**：`開發設計方針.md > P3-B` 的拒絕檢查順序最後一步只有「今夜尚未選」，`P3-D` 的 `can_enter` 也只要求 `night_location_chosen` 為空。但睡覺解析出內容之後、玩家還沒按「進入隔天」的那一拍，`night_sleep_pending == true` 而 `night_location_chosen` 仍是空字串——**進場入口與進入鍵都是開的**。
- **影響**：玩家可以「先睡、看完外溢文字、再去探索一個地點」，等於一夜拿到兩份夜間內容。颱風外溢四夜（`d23`～`d26_night_bleed`）正好是這個組合最好用的時候。而且畫面同時矛盾：推進鈕那時顯示「進入隔天」，地圖卻還讓人進得去。
- **修法**：進場規則與 `can_enter` 都改成要求 `night_location_chosen.is_empty() and not night_sleep_pending`；拒絕檢查順序把它併進最後一步並改寫成「今夜尚未選、也尚未睡」。測試指南 P3-B 補一條規則層反證（pending 時 `enter_night_location()` → `ok=false` 且 serialize 零變化），P3-D 補一條 UI 反證（pending 時所有 `night_enter::<id>` disabled）。
- **兌現**（2026-08-21）：`實作規格書.md > 九 > 到訪、收費與一夜一地點` 補「『或』是互斥的」那條規則真值；`開發設計方針.md > P3-B` 拒絕檢查順序最後一步改為「今夜尚未選、也尚未睡」；`P3-D` 的 `can_enter` 加 `night_sleep_pending` 為 false；`實作規格書.md > P3-D` 的清單條目補上睡覺停拍那一態。測試指南 P3-B 補規則層反證（`reason_code == "already_slept"`）、P3-D 補 UI 反證。

---

### K-83 P3-B 的「顯示通用結果」驗收在該階段做不到

- **Phase**：P3-B／P3-C（階段分界）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-B 開工前**。不改的話 P3-B 收尾會有一條永遠打不了勾的驗收。
- **問題**：`實作規格書.md > P3-B` 最後一條驗收寫「空內容地點仍完整走進場管線、**顯示通用結果**，且不製造不存在的 beat id」。但 `開發設計方針.md > P3-B` 同一階段明寫「空內容通用文字由 P3-C resolver／面板處理，不塞進本入口」，而 `empty_result`、刪 `<location>_locked` 假 beat 也都排在 P3-C。同一件事被指派給兩個階段。
- **影響**：P3-B 驗收條目與實作契約打架。實作者若照規格書做，就會把通用文字寫進 `enter_night_location()`，正好是方針明文禁止的那件事。
- **修法**：把「顯示通用結果」整句移到 `實作規格書.md > P3-C` 的驗收；P3-B 只留「空內容地點仍完整走進場管線、記到訪、收費、消耗今晚，且不製造不存在的 beat id」。測試指南 P3-B 對應那條同步改成只驗進場管線與 `beats_entered` 不新增鍵。
- **兌現**（2026-08-21）：`實作規格書.md > P3-B` 那條改成只驗進場管線（記到訪、收費、消耗今晚、不製造假 beat id），並註明通用結果文字屬 P3-C；`P3-C` 驗收新增一條 `empty_result`。

---

### K-84 K-78 只修一半：規格書的 lint 編號總表沒跟著對調

- **Phase**：文件／規格 SSOT
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-A 開工前**。lint 編號會直接變成函式名與 `verify_data.gd` 的輸出行，錯了要回頭改程式。
- **問題**：K-78 把 `lint_legacy_night_flags` 改成 13、`lint_night_once` 改成 14，P3-B／P3-C 段落、方針與測試指南都改了，但 `實作規格書.md` 的 lint 編號總表沒改，仍寫著「13＝夜間一次性 beat 完整性（P3-C）」「14＝舊夜間旗標退場（P3-B）」，與其他所有段落**完全相反**。
- **為什麼漏**：那份總表用的是編號清單（行首 `13. `），不是 `lint 13` 這個字串。上一輪複審 grep `lint 13` 沒抓到它，還回報成「八處引用全部一致」。**下次改 lint 編號要 grep 兩種形式**：`lint N` 與行首的 `N. `。
- **修法**：總表兩條對調，並在 K-78 補記這條漏網。
- **兌現**（2026-08-21）：`實作規格書.md` 的 lint 編號總表 13／14 對調，13＝舊夜間旗標退場（P3-B）、14＝夜間一次性 beat 完整性（P3-C）。同批把 K-87 的槽級掃描範圍與 K-90 的「同日 night-layer fixed 至多一個」寫進總表。

---

### K-85 lint 12 沒守住一般 night row 的必填欄位與型別

- **Phase**：P3-A（lint 規約）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-A**，與 lint 12 同一批寫。
- **問題**：lint 12 目前只驗「`chapter` 與 `earliest_night` 所屬章節一致」與 teaser-only 的 null 欄位／理由。一般 night row 的 `earliest_night`、`chapter`、`madness_cost` **沒有必填與型別檢查**，也沒有 `madness_cost >= 0`。而進場入口沿用 `int(loc.get("madness_cost", 0))`，鍵拼錯會安靜取到 0。
- **影響**：一個拼錯的欄位名會讓收費地點變成免費地點，而且沒有任何一條檢查會紅。K-74 才剛把第一輪供給定成 13／14 兩條基準，這正是會安靜污染那組數字的路徑。
- **修法**：lint 12 補三條——一般 row（非 teaser）必須有 `earliest_night`（int，1–45）、`chapter`（int，1–3）、`madness_cost`（int，>= 0）；三者任一缺失或型別錯誤即報錯。壞資料 fixture 加一筆「`madness_cost` 拼成 `madness_costs`」證明會紅。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-A` 的 lint 12 條目改成先驗一般 row 的 `earliest_night`／`chapter`／`madness_cost` 必填與型別（含 `>= 0`），再驗章節一致性；測試指南 P3-A 補一條負向 fixture，明確要求其中一筆是「`madness_cost` 拼成 `madness_costs`」。

---

### K-86 兩個新入口回單一 `reason`，重演 K-03

- **Phase**：P3-B／P3-E（API 形狀）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-B 開工前**改簽名。P3-E 的 `confirm_night_alignment()` 沿用同一形狀，一次定完。
- **問題**：`enter_night_location() -> {ok, reason, lines}` 與 `confirm_night_alignment() -> {ok, reason, knowledge_id}` 都只有一個 `reason: String`。而拒絕理由同時有兩種來源：機器碼（`not_night`、`teaser`、`too_early`、`already_chosen` 之類）與資料裡的中文 `reject_reason`。這正是 K-03 修過的形狀。
- **影響**：UI 拿到 `reason` 分不出該直接顯示還是要查文案表，最後會像 K-03 那樣把機器碼印在畫面上；也違反「玩家可見字串住檔頭 const」的 i18n 前置紀律。
- **修法**：兩個入口都改回 `{ok, reason_code, reason_text, ...}`，與 `try_place()`／`_check_slot_state()` 同形。`reason_code` 是封閉機器碼集合，`reason_text` 只在 location gate 不成立時帶 `reject_reason`。`location_summary().reason` 同批一起改，否則 UI 又要在兩種形狀之間翻譯。
- **兌現**（2026-08-21）：`enter_night_location()`、`confirm_night_alignment()`、`location_summary()` 三個簽名都改成雙欄；`開發設計方針.md > P3-B` 補上封閉機器碼集合與 `reason_text` 的使用規則；並升格寫進 `全域結構決策（P3 期間建立）`，讓 P3 之後新增的規則層入口一律照這個形狀。測試指南 P3-B 補 code review 條目。

---

### K-87 lint 13 的掃描範圍不含槽級

- **Phase**：P3-B（lint 規約）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-B**，與 lint 13 同一批寫。
- **問題**：方針寫「遞迴掃 location／beat 的 `condition`、`requires`、`on_enter`，任何 key 或值含 `opened_n_` 都報錯」。槽級的 `condition`、`requires`、`on_place` 不在名單裡。
- **影響**：`opened_n_*` 可以從槽級重新長回來而 lint 全綠。P3-B 的整個目的就是讓那組旗標退場，守衛留一個缺口等於守了一半。
- **修法**：改成「遞迴掃整份 location 與 beat（含 `slots[]` 的 `condition`／`requires`／`on_place`）」，負向 fixture 加一筆槽級的。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-B` 的 lint 13 改成「遞迴掃整份 location 與 beat，含 `slots[]` 的 `condition`／`requires`／`on_place`」；`實作規格書.md` 的 lint 總表第 13 條同步；測試指南 P3-B 要求槽級那一筆獨立驗。

---

### K-88 lint 11 缺「兩個 counterpart 共用同一張卡」的負向案例

- **Phase**：P3-A（測試覆蓋）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-A**。
- **問題**：lint 11 的意圖是「每個 distinct `day_counterpart` 恰好一張獨立 knowledge 卡」。測試指南 P3-A 的負向 fixture 只有「同一 counterpart 指到兩張卡」與「對位卡非 knowledge／非 slotless」，**沒有反方向的「兩個不同 counterpart 指到同一張卡」**。
- **影響**：複製貼上 `night_reveal` 是最容易犯的資料錯誤之一，而它會讓兩個白天地點共用一張對位卡——對位系統的一對一前提直接破掉，lint 卻是綠的。
- **修法**：測試指南 P3-A 加一條負向 fixture；lint 11 同批確認它真的檢查「knowledge id → counterpart」是單射。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-A` 的 lint 11 條目改成雙向——同一 counterpart 不得指兩張卡，同一張卡也不得被兩個 counterpart 引用；測試指南 P3-A 補該類負向 fixture。

---

### K-89 P3-A 的壞 fixture 數與測試指南列的錯誤類對不上

- **Phase**：P3-A（測試覆蓋）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-A**，與 K-88 同批。
- **問題**：`開發設計方針.md > P3-A` 寫「兩個壞 fixture 分別證明⋯會 exit 1」，但 `測試指南.md > P3-A` 列了四類錯誤（前兩類 lint 11、後兩類 lint 12）。加上 K-85 與 K-88 之後會更多。
- **影響**：若把多類錯誤塞進同一個 fixture，第一個報錯會遮蔽後面的，某條 lint 其實沒被證明過也看不出來。
- **修法**：方針改成「每一類錯誤各一個獨立 fixture」，數量以測試指南的清單為準、不在方針寫死數字。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-A` 改成「每一類錯誤各一個獨立壞 fixture，不得合併」，類別清單以測試指南為準、方針不寫死數量；測試指南 P3-A 的負向條目同批加註這條規約。

---

### K-90 沒有檢查擋「同一夜兩個 night-layer fixed 地點」

- **Phase**：P3-C（lint／規則層）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-C**。現行資料 D1／D2／D15 各在不同天，碰不到。
- **問題**：night-layer fixed 會呼叫 `_record_forced_night_visit()` 寫 chosen＋seen 並用掉今晚。若日後同一天出現兩個掛在不同 night-layer 地點的 fixed，第二個會覆蓋第一個的 chosen，兩個地點都被記成到訪、今晚被用掉兩次，而沒有任何一道檢查會紅。
- **影響**：現在為零，是資料擴充時的地雷。
- **修法**：二選一——① lint 14 加一條「同一 `when.day` 的 night-layer fixed 至多一個」② `_record_forced_night_visit()` 在 chosen 已非空時 `push_error` 並拒絕。①能在資料階段就抓到，比較早；建議做①，②可順手加。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-C` 的 lint 14 加一條「同一個 `when.day` 的 night-layer fixed 至多一個」，並要求 `_record_forced_night_visit()` 加 `push_error`＋拒絕當第二層保險；`實作規格書.md` lint 總表第 14 條與測試指南 P3-C 的負向 fixture（三種變四種）同步。

---

### K-91 P3-D 改 `available_locations()` 語意，兩條既有 UI 契約斷言正好相反

- **Phase**：P3-D（UI 契約）
- **狀態**：已修（`0047aaa`，P3-D 那一批已反轉兩條斷言）
- **修復時機**：**P3-D 開工時**。不先講，收尾跑「既有 UI 全套無迴歸」會紅兩條，實作者要花時間判斷是自己寫壞還是契約過期。
- **問題**：K-75 定案「chosen 非空時仍回傳全部當下可見 row」。但 `tests/ui_sim/cases/p1af_cases.gd` 的兩條既有斷言要的正好相反——`p1af_29_night_resolution` 斷言 `location::n_ahong_1` 不可見、`p1af_29_night_paid` 斷言 `location::n_landmark` 不可見，訊息都是「一夜最多開一個地點，已選過地點後其他地點不可見」。同一根因還會碰到 `playthrough_greedy.gd` 與 `tests/ui_sim/make_states.gd`——兩者都靠「chosen 非空時清單收斂成一個」決定當夜行為，而它們分別只列在 P3-F 與 P3-B 的動的檔。
- **影響**：P3-D 收尾必紅兩條 UI 契約。斷言訊息本身也把舊行為寫成了設計規則，讀的人會以為是規格。
- **修法**：`開發設計方針.md > P3-D` 的動的檔補上 `p1af_cases.gd`、`playthrough_greedy.gd`、`make_states.gd`；測試指南 P3-D 明寫「`p1af_29` 兩個變體的『其他地點不可見』改為『其他地點仍可見、但 `night_enter::<id>` 全部 disabled』」，訊息一起改。
- **兌現**（2026-08-21，**只做了文件那一半**）：`開發設計方針.md > P3-D` 的動的檔補上 `p1af_cases.gd`、`make_states.gd`、`playthrough_greedy.gd`，並寫明為什麼；測試指南 P3-D 新增一條 code review，明確要求把兩條斷言反轉成「其他地點仍可見、`night_enter::<id>` 全部 disabled」並改掉訊息。**斷言本身沒有現在改**——`available_locations()` 目前還在過濾，現在改會讓已綠的 UI 全套變紅；動作留在 P3-D 實作那一批。

---

### K-92 `saw_n_gathering_intro` 要寫在哪幾個章節變體沒寫明

- **Phase**：P3-B（措辭）
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-B 實作到那一行時**，順手把措辭寫準。
- **問題**：方針寫「聚會章節常態 beat 的 `on_enter` 寫它」。`n_gathering` 實際有兩個章節變體：`n_gathering_ch2`（現在帶著 `opened_n_gathering`）與 `n_gathering_ch3`（沒有）。唯一讀者 `n_gathering_d31` 是第 31 天定日、`chapter: 2`。
- **影響**：只寫 ch2 在功能上夠用（第 31 天當下取的就是 ch2），但旗標名寫的是「本輪已看過聚會常態內容」，第三章首次進聚會不會設，語意與名字對不上。日後有第二個讀者就會踩到。
- **修法**：方針明寫其一——「ch2 與 ch3 兩個變體的 `on_enter` 都寫」或「只有 ch2 需要寫，因為唯一讀者是 D31 定日 beat」。建議前者，名字才對得上語意。
- **兌現**（2026-08-21）：`開發設計方針.md > P3-B` 與測試指南 P3-B 都明寫寫入端是 `n_gathering_ch2` 與 `n_gathering_ch3` 兩個章節變體的 `on_enter`。

---

### K-93 規格書 P3-F 把已結案的 K-37 列為待收

- **Phase**：文件／進度 SSOT
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-F 之前任何一次動 `實作規格書.md` 時**順手。
- **問題**：`實作規格書.md > P3-F` 寫「K-37 的過期註解隨實作檔同批修正」，但 K-37（`map_list.gd` 檔頭仍寫「白天地點清單」）早在 P1-F 就結案，本檔標的是「已修」。
- **影響**：P3-F 的收尾清單多一條找不到工作的項目。
- **修法**：從那一句刪掉 K-37。
- **兌現**（2026-08-21）：`實作規格書.md > P3-F` 刪掉「K-37 的過期註解隨實作檔同批修正」半句。

---

### K-94 方針的 UI 表寫「K-68 結案」，與已知問題和測試指南相反

- **Phase**：文件／進度 SSOT
- **狀態**：已修（`0047aaa`，方針 UI 表已改回 ✅）
- **修復時機**：**P3-C**，K-68 真正解封（K-69 修好、視野門檻補上畫面層證據）那一批一起改。
- **問題**：`開發設計方針.md` 的 UI 契約表寫「P2-D 視野與 BE｜2｜✅（⋯K-68 結案）」。但本檔標 K-68 待修，`測試指南.md` 也用一整段警告說那兩條的勾要打折看（判定依據是 `serialize()` 的旗標，不是畫面節點）。三份文件兩種說法。
- **影響**：K-58／K-71 的同族第三次——狀態只改到一部分。看方針的人會以為 K-68 沒事了。
- **修法**：方針那一格先改成「⚠️ 規則層已驗、畫面層待 K-69」；等 P3-C 真正補完再改 ✅，並同批更新測試指南那段警告與本檔的 K-68 狀態。
- **兌現**（2026-08-21，**暫置狀態**）：`開發設計方針.md` 的 UI 契約表那一格改成「⚠️ 規則層已驗、畫面層待 K-69」，並指回本檔 K-68。**還不是最終狀態**——P3-C 補完睡覺文字與視野門檻的畫面證據之後，才把它改回 ✅ 並同批更新測試指南那段警告與 K-68 本身。

---

### K-95 `subdocs/卡牌/知識卡.md` 沒排進任何階段重建

- **Phase**：P3-A（衍生文件）
- **狀態**：已修（文件，2026-08-21；索引本身已於 P3-A 重建，含 10 張 `k_night_*`）
- **修復時機**：**P3-A 收尾**，與 10 張對位卡同批。
- **問題**：`subdocs/卡牌/知識卡.md` 是知識卡的衍生索引。P3-A 要新增 10 張 `k_night_<day_counterpart>` 對位 knowledge 卡，但三份 P3 文件的「動的檔」都沒有它。
- **影響**：索引會落後 10 張卡。這份檔是設計時翻的，落後之後查知識卡會漏掉整組夜間對位卡。
- **修法**：`開發設計方針.md > P3-A` 的動的檔加上它；測試指南 P3-A 加一條 code review「衍生索引已含 10 張新卡且與 `cards.json` 一致」。
- **兌現**（2026-08-21，**只做了文件那一半**）：`開發設計方針.md > P3-A` 的動的檔補上 `subdocs/卡牌/知識卡.md`；測試指南 P3-A 新增 code review 條目，要求索引與 `cards.json` 的 id／name／text 一致。**索引本身要等 P3-A 真的建出 10 張卡才重建。**

---

### K-96 `AGENTS.md > Layer 3` 列的四個 `subdocs/` 目錄不存在

- **Phase**：文件／開場指引
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**下次動 `AGENTS.md` 時順手。**
- **問題**：`AGENTS.md > New Conversation Opening Check > Layer 3` 列了 `subdocs/地點/`、`subdocs/人/`、`subdocs/卡片/`、`subdocs/驗證/`、`subdocs/歸檔/`。實際只有三個目錄：`卡牌/`、`故事線/`、`驗證/`。`文件查閱規則` 那張表的「其餘 `subdocs/`（驗證／人／卡片／地點）」同一個問題。
- **影響**：每個新 session 的開場指引指向四個不存在的路徑。
- **修法**：兩處清單改成實際的三個目錄；`卡片` 更名為 `卡牌`。
- **兌現**（2026-08-21）：`AGENTS.md > Layer 3` 改成實際存在的三個目錄（`故事線/`、`卡牌/`、`驗證/`）並註明地點與 NPC 資料住 `data/`、歸檔目錄尚未建立；`文件查閱規則` 那張表的「其餘 `subdocs/`」同步。

---

### K-97 `CODEX_HANDOFF.md` 停在 P1-G，落後五個子階段

- **Phase**：文件／交接
- **狀態**：已修（文件，2026-08-21）
- **修復時機**：**P3-A 開工時一併更新**，之後每個子階段收尾更新一次。
- **問題**：`CODEX_HANDOFF.md` 的「目前階段」仍寫 P1-A～P1-G、「下一個任務」仍寫「開始實作 P2-A」。實際 P2-A～P2-E 全部完工、P3 規格已寫完。
- **影響**：它是給交接與長專案續作用的檔，內容過期比沒有更糟——接手的人會照著它去做已經做完的 P2-A。`AGENTS.md > Layer 0` 也註明過 P2-E 驗證那一場該更新它而沒更新。
- **修法**：改寫成當前狀態（P2 全數完工、P3 規格完成、下一步 P3-A），並在 `開發設計方針.md > P3-A`～`P3-F` 各段的收尾驗收加一句「更新 `CODEX_HANDOFF.md`」，讓它跟著子階段走而不是靠人記得。
- **兌現**（2026-08-21）：`CODEX_HANDOFF.md` 整份改寫為當前狀態（P2 全數完工、P3 規格完成、下一步 P3-A），含驗證狀態與已知風險。並把「每個 P3 子階段收尾更新本檔」寫進 `開發設計方針.md > 全域結構決策（P3 期間建立）`，另把 `CODEX_HANDOFF.md` 列進 P3-A 動的檔與測試指南 P3-A 的 code review 條目——靠人記得不管用。

### K-98 P3-B 八個 `reason_code` 只有兩個有測試條目

- **Phase**：P3-B（測試覆蓋）
- **狀態**：已修（文件）
- **修復時機**：**P3-B 實作時逐條寫成測試**；本次先把條目補齊。
- **問題**：方針 P3-B 定了封閉的八碼集合（`not_night`／`unknown_location`／`not_night_layer`／`teaser`／`too_early`／`locked`／`already_chosen`／`already_slept`），但 `測試指南.md > P3-B` 只有 `already_chosen` 與 `already_slept` 兩條有驗收條目，其餘六碼一條都沒有。
- **影響**：拒絕檢查是照固定順序串下來的，順序寫錯會讓後面的碼永遠取不到——而**只驗 `ok == false` 的測試在順序寫錯時照樣全綠**。`too_early` 與 `locked` 尤其容易反：阿宏 row 在 `earliest_night` 之前同時也 gate 不成立，順序反了玩家先看到門檻理由而不是「時候未到」，等於提前洩漏這條線的存在。這是 K-01 家族（測試存在、全綠、沒走那條路）。
- **修法**：方針 P3-B 補一條「八碼各要自己的案例，逐條驗 code＋前後 `serialize()` 逐字相同」；測試指南 P3-B 補兩條——六碼的拒絕矩陣，以及 `too_early` 對 `locked` 的順序反證；規格書 P3-B 驗收補一句「八種拒絕都分辨得出來，且兩者同時成立時顯示『時候未到』」。

### K-99 P3-E 只寫「拒絕時狀態零變化」，沒有負向矩陣也沒有封閉碼集合

- **Phase**：P3-E（API 形狀／測試覆蓋）
- **狀態**：已修（文件）
- **修復時機**：**P3-E 實作時逐條寫成測試**；本次先把契約與條目補齊。
- **問題**：方針 P3-E 的 `confirm_night_alignment()` 簽名回 `{ok, reason_code, reason_text, knowledge_id}`，但**從沒定義 `reason_code` 的合法值**——P3-B 有封閉集合，這裡沒有。驗收也只有一句「拒絕路徑完整 `serialize()` 零變化」。測試指南 P3-E 現有條目全部走 UI（按鈕沒出現、取消彈窗），沒有一條直接呼叫這個規則層入口。
- **影響**：兩個洞。一是碼集合沒定，實作者會現編字串，K-03「機器碼與玩家文案混在一起」就從這裡長回來；二是**「按鈕沒出現」驗的是 `alignment_offer()`，不是 `confirm_night_alignment()` 自己的防線**——這個入口繞過畫面也叫得動，兩邊判準分歧就是第三份對位狀態偷偷長出來的前兆，而 P3-E 的整個設計前提正是「對位狀態純衍生、不存第三份」。
- **修法**：方針 P3-E 補封閉六碼（`not_day_phase`／`unknown_location`／`not_day_layer`／`no_seen_row`／`already_known`／`data_conflict`，`reason_text` 一律留空、`data_conflict` 同時 `push_error`）與「六碼各一案例」契約；測試指南 P3-E 補兩條——繞過 UI 的六碼矩陣，以及 offer 與 confirm 判準逐項對齊的反證；規格書 P3-E 驗收補一句。

### K-100 P3-A 的 baseline checkpoint 沒規定產生方式、名稱與前置狀態

- **Phase**：P3-A（驗收可重跑性）
- **狀態**：已修（文件）
- **修復時機**：**P3-A 開工的第一件事**（baseline 必須在動資料之前產生）。
- **問題**：`測試指南.md > P3-A` 最後一條要求「變更前後用同一份 P2 night checkpoint 跑一次」，但沒寫這份 checkpoint 由誰產生、叫什麼名字、停在哪一天哪一個時段。方針 P3-A 的動的檔也沒有列 `make_states.gd`。
- **影響**：三層。一是**產不出來**——換一個人做就換一個前置狀態，「結果相同」比的是兩份不同的東西；二是**驗收會恆真**——不寫明「前」那一半必須在改資料之前產生，實作者會在改完之後才想到要跑 baseline，此時舊程式與舊資料都不在了，拿到的「前」其實是「後」，這條驗收永遠綠；三是**存放位置**沒寫，放進 `_qa/runs/<run_id>/` 的話下一輪就換目錄，跨不過「改資料之前」與「改資料之後」兩次執行。
- **修法**：方針 P3-A 新增一整段契約：產生器 `make_states.gd` 的 `p3a_night_baseline` 情境；前置狀態第 14 天 night、當夜未動作，並列四條當場斷言的後置條件（選第 14 夜是因為它同時摸得到清單有未 seen 收費 row、以及再推一夜就是 D15 fixed）；檔名 `_qa/p3a_baseline/p3a_night_baseline.json` 與 `.expected.json`，刻意放在 run 目錄外、不進版控；並明寫「前」那一半要在改資料之前產生並把 commit sha 記進 `CODEX_HANDOFF.md`，「後」那一半是**載入同一份狀態 JSON**而不是重跑 generator。測試指南 P3-A 那一條加上指向與一條 code review 反證。
- **順帶**：`make_states.gd` 進 P3-A 動的檔清單。

### K-101 P3-F 把 verifier-owned 的 `測試指南.md` 列進 implementer 的動的檔

- **Phase**：文件／角色分工
- **狀態**：已修（文件）
- **修復時機**：本次。
- **問題**：`開發設計方針.md > P3-F` 的「主要動測試與走查」把 `測試指南.md` 的驗證紀錄列進去，「新增 UI 契約時五處總數一起更新」也把測試指南算成 implementer 要改的一處。但 `AGENTS.md > 角色分離` 寫的是 implementer 只動程式碼、測試、fixture 與 `開發設計方針.md`，`測試指南.md` 與 `驗證後已知問題.md` 屬 verifier。
- **影響**：等於要求 implementer 自己勾自己的驗收，與 `測試指南.md > Phase 3` 開頭那句「實作者不得修改本節打勾；完成後由 verifier 依證據更新」正面衝突。兩份文件對同一件事給相反指令時，照誰做都會被另一份判違規。
- **修法**：方針 P3-F 的動的檔清單以 `subdocs/驗證/發狂卡機制模擬.md` 取代 `測試指南.md`（那份確實是 implementer 要同步的），並補一段引言明寫兩份 verifier 檔不在清單裡、implementer 只提供證據（exit code、報告路徑、catalog 重數的實際數字）。五處總數那條拆兩手：implementer 改 matrix／catalog／launcher 三處並回報數字，`測試指南.md` 與 `PROJECT_BRIEF.md` 由 verifier 同步。
- **本次一併定案**：`PROJECT_BRIEF.md` 歸 verifier 寫。它是 Phase 收尾才更新的進度 SSOT，而收尾打勾的人是 verifier。`AGENTS.md > 實作守則 4` 原本沒指派這一份，本次補進去（角色分工的單一事實來源在 AGENTS.md，方針只指向、不重複定義）。

### K-102 已結案的 K-37 仍留在 P3-F 的兩份收尾清單

- **Phase**：文件／進度 SSOT
- **狀態**：已修（文件）
- **修復時機**：本次。
- **問題**：K-93 只清掉 `實作規格書.md > P3-F` 那一處。`測試指南.md > P3-F` 的 code review 條目仍寫「K-30／33／34／35／37／69 的舊形狀已不存在」，`開發設計方針.md > P3-F` 的收尾仍寫「K-34／35／68／70／37 的結案證據逐條落 `驗證後已知問題.md`」。K-37（`map_list.gd` 檔頭註解）在 P1-F 就已結案。
- **影響**：與 K-93 同族——待收清單混進已結案項目，P3-F 收尾時會去找一條不存在的證據，找不到就變成「這個 Phase 沒收乾淨」的假警報。K-93 修一處、剩兩處沒修，是 K-58／K-71「總數只改到一部分」那個模式的第三次。
- **修法**：兩處各刪掉 `37`，並各加一句「K-37 已於 P1-F 結案，不列入本階段」，讓下一次 grep `K-37` 的人看到的是結論而不是空白。

### K-103 `test_p3a.gd` 的 `night_location_names` 是死變數且僅比對 4 個寫死名稱

- **Phase**：P3-A（測試覆蓋）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`test_p3a.gd` 收集了 28 個夜間名稱之後從沒被讀，實際比對的是寫死的四個名字 `["很長的走廊","有蒸氣的樓下","數木牌的屋子","有音樂的地方"]`。測試指南要的是「文字不含同一 counterpart 下尚未到訪的其他夜間 row 名稱」——用其餘 24 個名字寫的劇透會整個穿過去。
- **影響**：對位卡文案在內容期改寫時沒有劇透防護。
- **修法**：`test_p3a.gd` 讀入全部 28 個夜間地點名稱，遍歷對位卡文字動態斷言不包含任何夜間地點名稱。

### K-104 `_test_night_names_review()` 宣稱「28 個夜間名稱審查通過」屬過度宣稱

- **Phase**：P3-A（測試措辭／審查落檔）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`_test_night_names_review()` 僅驗證 `n_ahong_2` 精確字串、夜間地點總數 == 28、名稱非空。測試指南那一條本來就是 code review 項（逐一把 28 個 name 與 primary beat 對照），headless 做不到——但 `_ok()` 訊息讓讀 log 的人以為做完了，而其餘 27 個名稱的人工審查結果沒有任何地方留下記錄。K-01／K-53 同族。
- **影響**：人工審查記錄遺失，且測試日誌產生過度宣稱。
- **修法**：修正 `test_p3a.gd` 斷言與日誌措辭，並將 28 個夜間地點名稱與 primary beat 的人工審查對照表記錄於本檔附錄。

### K-105 `make_states.gd` baseline 後置條件寫死 `madness < 7`

- **Phase**：P3-A（數值 SSOT）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`make_states.gd` 的 baseline 後置條件把 `madness_cap` 寫死成 7（`if madness_count >= 7:`），沒讀 `Data.tuning("madness_cap")`。
- **影響**：違反「可調數值只住 data/」，若未來調低 cap 會導致後置條件誤報。
- **修法**：`make_states.gd` 改讀 `Data.tuning("madness_cap", 7)`。
- **兌現**（`41d2d8e`）與**驗收結果（2026-08-21，只過一半）**：值確實動態了，但**生效的路徑不是修法宣稱的那一條**。`_verify_checkpoint_postcondition()` 加了 `data_node: Node = null` 參數，然而四個呼叫端（`make_states.gd` 的 117、140、396、480 行）一個都沒傳——`p3a_night_baseline` 走的是 396 行那個兩參數呼叫。實際讀到 tuning 是靠 `elif Engine.has_singleton("Data")` 這條備援（`PlaythroughGreedy.setup_data()` 有 `Engine.register_singleton("Data", ...)`，所以通）。- **第二批兌現（2026-08-21，已結案）**：四個呼叫端（117、140、396、480 行）全部補傳 `data_node`，`_write_p2b_state()` 也一路把它帶下去，死參數消失。備援分支改成直接讀 `res://data/tuning.json`。**殘留但不追**：`var madness_cap: int = 7` 與 `tuning("madness_cap", 7)` 兩處字面值仍在，且備援分支自己解析 JSON 而不走 DataLoader——但四個呼叫端都會傳 `data_node` 了，那條路走不到。下次動這個函式時順手收乾淨即可，不另立條目。

### K-106 baseline 挑 paid row 採第一個 `madness_cost > 0` 易假紅

- **Phase**：P3-A（測試穩定度）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：baseline 挑 paid row 用「`avail_locs` 裡第一個 `madness_cost > 0`」——正是 K-74／P3-F 禁的取法。`locations.json` 一改順序，逐字比對的 `expected.json` 就假紅，而且紅在看不出原因的地方。
- **影響**：測試重演脆弱。
- **修法**：`make_states.gd` 改為明確指定目標地點為 `"n_ahong_1"` 並斷言其為收費標記。

### K-107 K-100 的「先產 baseline 再改資料」編輯順序未守住

- **Phase**：P3-A（流程紀律）
- **狀態**：不修（已接受，降為 B-04）
- **修復時機**：本次。
- **問題**：mtime 依序是 `locations.json 19:51:05` → `cards.json 19:52:59` → `baseline 19:53:50`，baseline 是資料改完後才寫出來的，該條驗收當下其實恆真（拿改後資料產 expected，再拿改後資料比）。
- **影響**：驗收流程失去防護效果（結論仍成立是因為事後手動用舊資料重跑對過）。
- **修法**：記錄教訓，並以修正後的 generator（明示 `n_ahong_1` 與動態 `madness_cap`）重跑產出基準。
- **驗收結果（2026-08-21，改判為不修）**：重跑出來的 baseline **仍然是資料改完之後才寫出來的**（`locations.json` 21:48:11 → `make_states.gd` 21:49:50 → baseline 21:51:10），跟 `ce7c41a` 那次同一個順序。也就是說「重跑一次」沒有改變任何事——這條驗收在錯誤順序下依舊恆真。**P3-A 的結論之所以成立，是 verifier 兩次都把 worktree 開在 `870db64`、用當時的資料重跑 generator，拿到與現檔逐字相同的兩個檔**；靠的是這個獨立重跑，不是靠這條驗收。
- **處置**：降為紀律條目，移進「二、已接受的邊界決定」。要真的擋住它需要新機制（例如 baseline 產生時把當下 `data/` 的 hash 一併寫進 `expected.json`，比對時 hash 等於當前 `data/` 就直接紅），那是新需求不是修 bug，**目前不做**。替代防線寫進測試指南 P3-A 那條：這條保持未打勾，並註明上一條的勾是靠獨立重跑取得的。

### K-108 Lint 12 的 teaser 分支未強制 `earliest_night` 與 `chapter` 雙欄完整

- **Phase**：P3-A（資料檢查規約）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`lint 12` 的 teaser 分支只在 `earliest_night` 與 `chapter` 都非 null 時才比一致性，只寫其中一欄會靜靜通過。
- **影響**：現行資料碰不到，但無法防護資料擴充時遺漏欄位。
- **修法**：`DataLoader.lint_night_locations` 要求 teaser_only 地點若提供時間必須兩欄均有效且一致。
- **兌現**（`41d2d8e`）與**驗收結果（2026-08-21，未過）**：lint 那一半做對了，**但沒有任何反證守著**。`p3a_teaser_bad` 的 teaser row 是 `chapter: 3` ＋ `earliest_night: 45` 兩欄都在，它觸發的是既有的「帶價碼」與「缺 reject_reason」兩條；新加的兩段判斷刪掉，六個 fixture 照樣全綠。這正是 K-59 那句話——**沒有測試守著的修正，等於下一次改壞了沒人知道**。- **第二批兌現（2026-08-21，已結案）**：新增 `p3a_teaser_missing_time_column`，兩個 row 分別只缺 `earliest_night` 與只缺 `chapter`，`test_p3a.gd` **逐 row 分兩條斷言**（比對 row id ＋ 錯誤字串），兩條都不成立才算過。同批把時間欄位檢查從 teaser／一般兩個分支**上提為共用**——teaser 與一般 row 走同一套判斷，錯誤訊息也統一成「缺少有效的 earliest_night／chapter」，原本兩份幾乎相同的程式碼收成一份。兩個 row 放同一個 fixture 不違反「不得合併」：`lint_night_locations()` 收集全部錯誤而非遇錯即停，不會互相遮蔽，而斷言本來就分開兩條。

### K-109 阿宏 `reject_reason` 5 與 6 共用文案且測試過度拷貝文案

- **Phase**：P3-A（資料層／測試語意斷言）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`n_ahong_5` 與 `n_ahong_6` 共用同一字串「你手上的路線知識還少了一段。」，但兩者要求不同知識卡（`k_ahong_point_1` vs `k_ahong_point_2`）。同時 `_test_ahong_reject_reasons()` 把 6 句完整文案抄進測試當期望值——驗的是「等於測試裡的拷貝」而非語意對應，文案一改就假紅（K-60 同族）。
- **影響**：玩家回饋未區分要求階段，測試易發生脆弱假紅。
- **修法**：`locations.json` 區分 `n_ahong_5`（第一段）與 `n_ahong_6`（第二段）；`test_p3a.gd` 改為驗證非空、非通用、指向上一段痕跡／第一段知識／第二段知識／三個對位點之語意斷言。
- **驗收結果（2026-08-21）**：過。資料兩句已分開，測試改成關鍵字語意斷言並加了 `r5 == r6` 反證——文案微調不會假紅，但兩句寫成一樣會紅，正好是要的方向。

### K-110 fixture 6 只覆蓋六種錯誤裡的一種

- **Phase**：P3-A（測試覆蓋）
- **狀態**：已修（2026-08-21）
- **修復時機**：**P3-B 開工前，與 K-108 同批**。
- **問題**：`測試指南.md > P3-A` 那條要的是「一般 night row 缺 `earliest_night`／缺 `chapter`／缺 `madness_cost` 任一欄，或型別錯誤，或 `madness_cost` 為負 → lint 12 紅」，但 `p3a_missing_field_or_misspelled` 只有一個 row，只做了「`madness_cost` 拼成 `madness_costs`」那一種；`test_p3a.gd` 也只斷言「缺少 madness_cost 欄位」這一個字串。
- **影響**：lint 12 一般 row 分支有四段判斷（`earliest_night` 有效性、`chapter` 有效性、型別、負值）**一段都沒有被證明過**，刪掉照樣全綠。與 K-59、K-108 同族——修正存在、測試存在、測試沒走那條路。
- **修法**：照「每一類一個獨立 fixture、不得合併」的規約補其餘四類（合併會讓第一個報錯遮住後面的），`test_p3a.gd` 每一類各斷言自己的錯誤字串。
- **兌現（2026-08-21，已結案）**：補 `p3a_missing_earliest_night`、`p3a_missing_chapter`、`p3a_invalid_type_field`（`chapter: "one"`）、`p3a_negative_madness_cost`（`madness_cost: -1`）四個獨立 fixture，各自斷言自己的錯誤字串。P3-A 的負向 fixture 由 6 個增為 11 個，`test_p3a` 由 12 項增為 17 項，verifier 自跑全綠。

### K-112 跑一次 UI 模擬就把 K-100 的 baseline 重生一遍

- **Phase**：P3-A（驗收工具結構）
- **狀態**：已修（`4b0ba1c`，隨 K-124 同批）
- **修復時機**：已於 2026-08-27 兌現。
- **問題**：`_generate_p3a_baseline()` 掛在 `generate_all_states()` 裡，而 `run_ui_sim.ps1` 的第 2 步「Generating scenario states」必定呼叫它。也就是**任何人跑一次 UI 模擬，`_qa/p3a_baseline/` 的兩個檔就被當下的 `data/` 重寫一次**。verifier 本次實測：UI 模擬跑在 22:20:24，baseline 兩個檔的 mtime 就是 22:20:27。
- **影響**：K-107 想守的那件事（「前」那一半要在改資料之前產生）**在這個結構下留不住**——就算有人乖乖先產了 baseline，之後任何一次 UI 模擬都會把它蓋成當前資料的版本，而 `test_p3a` 的 baseline 比對從此恆真。這比 K-107 原本描述的「人為順序沒守住」嚴重：那是紀律問題，這是**工具自己會把證據抹掉**。
- **修法**：把 baseline 產生從 `generate_all_states()` 拆出來，改成明示旗標（例：`make_states.gd -- --p3a-baseline`）或獨立入口，不隨 UI 模擬跑。**實作者工作，但不擋 P3-B**——目前這條驗收的真正防線是 verifier 用舊 commit 另開 worktree 重跑（見 B-04）。
- **兌現與複驗**：`4b0ba1c` 把 `_generate_p3a_baseline()` 改成只有明示 `--p3a-baseline` 才呼叫；一般 UI fixture 生成仍 exit 0，verifier 量測 baseline mtime 前後不變。K-112 結案；B-04 保留其歷史紀律結論，不因工具修好而回溯改寫。

### K-113 fixture 11 的「型別錯誤」其實是被範圍檢查攔下的

- **Phase**：P3-A（測試覆蓋）
- **狀態**：已修
- **修復時機**：本次。
- **問題**：`p3a_invalid_type_field` 原本寫 `"chapter": "one"`。lint 12 的 chapter 判斷是一整串 `or`：null → 型別 → 整數性 → 範圍。`int("one")` 在 GDScript 是 `0`，所以這筆資料**在型別檢查之後還會被範圍檢查（`< 1`）攔一次**——把型別與整數性兩段整個刪掉，錯誤照樣報得出來。
- **實測**（變異測試）：只拿掉 `not (ch_val is int or ch_val is float)` 與 `float(int(ch_val)) != float(ch_val)`，保留 null 與範圍 → `test_p3a` **exit 0，fixture 11 仍然是 ok**。這條反證等於沒有。
- **影響**：真正會漏的是「數值落在合法範圍、但型別寫錯」那一類——`"chapter": "2"`（字串數字）與 `earliest_night: 15.5`（小數）。前者會讓一個 String 混進章節比較，後者會讓 `earliest_night` 被 `int()` 悄悄截斷。兩種現行 lint 都抓得到，但沒有任何測試證明它抓得到。與 K-108／K-110 同族——**反證存在、反證全綠、反證沒走那條路**。
- **修法**：fixture 改成兩個各自只踩一道防線的 row：`n_string_chapter`（`"chapter": "2"`，`earliest_night: 15`，範圍全合法）與 `n_fractional_earliest`（`earliest_night: 15.5`，`chapter: 2`，章節一致）。`test_p3a.gd` 逐 row 分兩條斷言。
- **兌現實測**：修好後拿掉 chapter 型別檢查 → exit 1；拿掉 earliest 整數性檢查 → exit 1；未變異 → exit 0。兩道防線各自被守住。

### K-114 `p2d_near_cap` 把 `madness_cap` 釘死在 7

- **Phase**：P2-D（測試 fixture／數值 SSOT）
- **狀態**：已修
- **修復時機**：本次（驗 K-105 時順帶撞到）。
- **問題**：`make_states.gd` 的 `p2d_near_cap` 情境寫死 `for i in range(6)`，後置條件寫死 `hand.has("madness#6") and not hand.has("madness#7")`。註解也寫「6 張……即達 cap 7」。
- **實測**：把 `tuning.json` 的 `madness_cap` 改成 5，`make_states` 直接以「`p2d_near_cap` 後置條件失敗」中止，整個 UI 模擬跑不起來。
- **影響**：`madness_cap` 是 `data/tuning.json` 的可調數值，而 `待決事項.md > 35` 明講第二輪調價時要動它。現在調它會讓 UI 模擬整組掛掉，而且錯誤訊息指向 fixture 而不是「你改了 cap」。更糟的情況是調高：情境不會掛，但 `p2d_02_be_screen` 會變成「離 cap 還有距離」，畫面上不會出現 BE，契約卻仍宣稱自己在驗 cap 邊界。K-63 同族（「還把可調數值釘死」）。
- **修法**：產生端改 `int(data_node.tuning("madness_cap", 7)) - 1`，後置條件同樣現算 cap 並組出 `madness#(cap-1)`／`madness#cap` 的 instance 名。註解改寫成「手上 cap-1 張」，不再寫死數字。
- **沒有一起改的**：`p2d_d24_two_cards`／`p2d_d24_three_cards` 的 2 張／3 張綁的是 `madness_vision_threshold`，同一族但不同數值，本次不動——那兩個是門檻的上下邊界案例，改成動態要連 UI 契約的描述一起改，屬於另一批工作。**先記在這裡**，下次動視野門檻時一起收。

### K-111 測試指南 P3-A 的 teaser 條目混進了 P3-D 的可見性

- **Phase**：文件／階段分界
- **狀態**：已修（文件）
- **修復時機**：本次。
- **問題**：`測試指南.md > P3-A` 的 teaser 條目寫「…；第一、二章不列入 teaser 可見集合」。可見性由 `available_locations()` 決定，而 `開發設計方針.md > P3-A` 明寫「本階段不得改夜間進入／收費／fixed 流程」——這半句在 P3-A 驗不到。
- **影響**：與 K-83 同族（同一件事指派給兩個階段）。實務後果是 P3-A 收尾時這一條會卡住，或者被含糊打勾——`ce7c41a` 走的是後者，`test_p3a.gd` 只驗了 `chapter == 3`。
- **修法**：P3-A 該條註明只驗資料與 lint，可見性由 P3-D 既有那條「teaser 第一、二章不存在，第三章出現」承接（該條已存在，不需新增）。

---

### K-115 P3-B 的拒絕文案 const 表在該階段沒有消費端

- **Phase**：P3-B／P3-D（階段分界）
- **嚴重度**：低
- **狀態**：已修（`0047aaa`，P3-D 接上 `_NIGHT_REJECT_TEXTS`）
- **修復時機**：**P3-D**，與詳情面板的「理由可見」同一批。
- **問題**：`開發設計方針.md > P3-B` 寫「UI 用檔頭 const 表把 code 翻成玩家文案，`reason_text` 非空時優先顯示」。但同一段又寫「P3-B 的 `main.gd` 暫時沿用既有『點清單即進場』路徑，`ok` 才開面板，拒絕則不開」——**拒絕時畫面上根本沒有顯示理由的位置**。`2ef58f3` 照字面寫了 `NIGHT_REJECT_MESSAGES`，全檔沒有任何讀取點；`7c5184b` 依「不寫沒人用的抽象」把它刪掉。
- **影響**：`測試指南.md > P3-B` 最後一條 code review 的後半（「玩家文案住檔頭 const 表」）在本階段驗不到。前半（雙欄形狀、UI 沒把 `reason_code` 印在畫面上）成立。
- **修法**：P3-D 做「先詳情、後進入」時，把 code→文案的對照表與 `reason_text` 優先顯示一起接上，屆時 `測試指南.md > P3-D` 的「門檻不成立時詳情可讀、理由可見」那條就是它的驗收。不要為了本階段打勾而先把表放回去。

### K-116 夜間三態按鈕文字整段沒有測試覆蓋

- **Phase**：P3-C（測試覆蓋）
- **嚴重度**：中（原本擋兩條驗收打勾）
- **狀態**：**已修（2026-08-22，`01ccff4`）**
- **問題**：`bdfa5fd` 在 `main.gd` 加了 `_MSG_ADVANCE_SLEEP`／`_MSG_ADVANCE_NEXT_DAY`／`_MSG_ADVANCE_END_NIGHT` 三個常數與 `_refresh_advance_hint()` 的 `elif GameState.phase == "night":` 分支。全 repo 沒有任何一處斷言這三個字串。
- **反證（2026-08-22 實跑）**：把那整段 `elif` 刪掉，`test_boot`、`test_p3c`、`test_p1f` 與 `p2d_01_vision_zero`／`_three` UI 契約**全部仍然綠**。
- **影響**：`測試指南.md > P3-C` 的「按鈕變『進入隔天』」與「按鈕為『結束今晚』」兩條打不了勾。停拍與換日次數本身有覆蓋（`p2d_01_vision_three` 兩次點擊、`test_p3c` 第 10 組），缺的純粹是「玩家看到的是哪三個字」。
- **修法**：在 `p2d_01_vision_three` 的兩次點擊之間各斷言一次按鈕文字，並補一個「已到訪 → 按鈕為結束今晚」的 UI 變體（可沿用 `p1af_29_night_paid` 的到訪後狀態）。斷言讀 UI dump 的按鈕節點文字，不要讀 `main.gd` 的常數本身——讀常數等於自己跟自己比。

- **兌現（2026-08-22，`01ccff4`）**：`test_p3c` 新增第 12 組，載入真的 `main.tscn`、讀 `AdvanceButton.text`，驗「直接睡 → 進入隔天 → 結束今晚」三態，中間夾兩次 `_on_advance_pressed()`。**verifier 重跑變異測試**：把 `_refresh_advance_hint()` 的夜間 `elif` 整段刪掉，三條斷言同時轉紅（先前是全綠）。⚠️ 這是場景級證據，不是 `tests/ui_sim/` 契約，也沒有走真實 input event——差額另立 K-123。
---

### K-117 `_record_forced_night_visit()` 拒絕之後，fixed beat 照樣播、meta-once 照樣燒

- **Phase**：P3-C（規則層＋測試覆蓋）
- **嚴重度**：中
- **狀態**：**已修（2026-08-22，`01ccff4`）**
- **問題**：兩半。
  1. **規則層**：`play_night_fixed()` 的順序是「先寫 `night_once_beats_seen[bid]` → 再呼叫 `_record_forced_night_visit()` → 再 `play_beat(bid)`」。helper 遇 `night_location_chosen` 非空時 `push_error` 並 `return`，但**呼叫端不看回傳、照樣往下播**。所以被拒絕的那一支：內容播出去、meta-once 永久燒掉、只有到訪沒記。K-90 要防的「今晚被用掉兩次」沒防到，防到的只有 chosen 欄位不被覆寫。
  2. **測試層**：這道保險零覆蓋。`test_p3b` 第 8 組只驗 helper 正常寫入，`test_p3c` 完全沒碰它。
- **反證（2026-08-22 實跑）**：把 helper 開頭的 `push_error`＋`return` 三行刪掉，`test_p3b` 與 `test_p3c` **都仍然 exit 0**。
- **加重因素**：`test_p3c` 第 6 組每次 `play_night_fixed()` 之前都手動 `gs.set("night_location_chosen", "")`，等於把這條路徑的前置條件抹平，真流程裡的交互作用測不到。
- **修法**：helper 改為回傳 `bool`（或回 `{ok, reason_code}`，與八碼矩陣同形），`play_night_fixed()` 在 false 時**中止該 beat**——不播、不寫 meta-once（寫入點一併移到播放成功之後）。測試補一個「chosen 已非空時第二個 night-layer fixed 完全不生效」的斷言，驗 lines 為空、`night_once_beats_seen` 不新增鍵、chosen 維持原值。

- **兌現（2026-08-22，`01ccff4`）**：helper 改回傳 `bool`，`play_night_fixed()` 拿到 false 就 `continue`（不播、不寫），`night_once_beats_seen` 的寫入點移到到訪成功之後。`test_p3c` 第 10 組 7 條斷言：chosen 已佔用時 lines 為空、meta 不寫入、chosen 不被污染，sleep_pending 時同理，乾淨狀態回 true、重複呼叫回 false。**verifier 重跑變異測試**：刪掉 chosen 保險後四條轉紅（先前是全綠）。
---

### K-118 lint 14 判 night 只認字串 phase，陣列寫法整條繞過

- **Phase**：P3-C（lint 覆蓋）
- **嚴重度**：中（現行資料碰不到，但這條 lint 的存在目的就是擋資料擴充）
- **狀態**：**已修（2026-08-22，`01ccff4`）**
- **問題**：`data_loader.gd > lint_night_once()` 用 `str(wd.get("phase", "")) == "night"` 判斷 exact night。`when.phase` 在本專案允許寫成陣列（`indulgence_exits.json` 有三筆 `["morning", "afternoon"]`，`DataFacts.beat_matches_time()` 也支援），而 `str(["night"])` 不等於 `"night"`。所以 `"phase": ["night"]` 的 night-layer fixed 會同時繞過三條規則：meta_once 用途檢查、night-layer 必須 meta-once、同日至多一個。
- **影響**：現行 10 個 beat 檔沒有陣列 phase 的夜間 beat，lint 掃正式資料 0 錯誤是真的。風險純屬未來資料擴充——而擴充正是 K-90／lint 14 被建立的理由。
- **修法**：抽一個共用的 phase 比對（`p_val is Array` 時用 `has("night")`，否則字串比對），lint 14 與 `beat_matches_time()` 共用同一份判準。負向 fixture 補第五種：`"when": { "day": 1, "phase": ["night"] }` 的 night-layer fixed 缺 `meta_once`，必須被抓到。

- **兌現（2026-08-22，`01ccff4`）**：`p_val is String` 走等值、`p_val is Array` 走 `has("night")`，並同批把 `day_to` 一起排除在 exact night 之外。負向 fixture 由 4 個增為 7 個（5＝陣列 phase 缺 meta_once、6＝陣列 phase 未含 night 卻標 meta_once、7＝陣列與字串 phase 同夜衝突）。**verifier 重跑變異測試**：刪掉陣列分支後 fixture 5 與 7 轉紅。
---

### K-119 「0 筆 `<location>_locked` 假 beat」是空斷言

- **Phase**：P3-C（測試覆蓋）
- **嚴重度**：低（結論成立，但不是這條斷言證明的）
- **狀態**：**已修（2026-08-22，`01ccff4`）**
- **問題**：`test_p3c` 第 5 組掃 `loader.beats` 找 id 以 `_locked` 結尾者。那個假 id **從來只存在於 `panel_builder.gd` 的合成物**，不曾寫進任何資料檔；而且合成邏輯已於 P3-B `2ef58f3` 移除，不是 P3-C 移除的。所以這條斷言在改動前後都會是 0，證不了任何東西。
- **同條驗收的另一半**：`測試指南.md > P3-C` 那條前半是「paid night panel 回傳真實 beat id」，`test_p3c` 沒有對應斷言。目前只有 `p1af_29_night_paid` UI 契約間接覆蓋。
- **修法**：改成對 `PanelBuilder.build()` 的輸出斷言——取一個收費夜間地點，驗回傳的每個 beat id 都存在於 `loader.beats_by_id`。這樣才擋得住「面板憑空生 id」這個原始行為。

- **兌現（2026-08-22，`01ccff4`）**：第 5 組改成對 `PanelBuilder.build()` 的輸出斷言——`n_ahong_1` 與 `n_source` 兩個收費地點，回傳的每個 beat id 都必須在 `loader.beats_by_id` 裡且不得以 `_locked` 結尾；**beats 為空直接 fail**，所以不會再退化成空迴圈。K-35 的收尾證據仍留在 P3-F（那裡才有 28-row matrix）。
---

### K-120 夜間定日候選會收區間 beat

- **Phase**：P3-C（契約精確度）
- **嚴重度**：低（現行資料碰不到）
- **狀態**：**已修（2026-08-22，`01ccff4`，與 K-118 同批）**
- **問題**：`night_beat_candidates()` 第一段用 `DataFacts.beat_matches_time(b, day, "night")` 收定日候選，而該函式對 `day_from`／`day_to` 區間回 true。`實作規格書.md > P3-C` 寫的是「當日**定日** beat > 章節變體」，區間 beat 被收進來會壓過所有章節變體。
- **影響**：現行夜間資料沒有區間 beat（`day_from` 只出現在 `indulgence_exits.json` 的白天出口），所以行為正確。屬資料擴充風險。
- **修法**：候選層自己再加一道 `wd.has("day")`，或在 lint 層禁止夜間 beat 使用區間 `when`。兩種都行，選一種寫進方針即可。

- **兌現（2026-08-22，`01ccff4`）**：候選層第一段自加 `wd.has("day")` 並排除 `day_from`／`day_to`，不再只靠 `beat_matches_time()`。第 1 組補一條區間反證。
---

### K-121 fixed 到訪 helper 不看 `night_sleep_pending`

- **Phase**：P3-C（判準一致性）
- **嚴重度**：低
- **狀態**：**已修（2026-08-22，`01ccff4`，與 K-117 同批）**
- **問題**：`enter_night_location()` 的八碼拒絕矩陣同時擋 `already_chosen` 與 `already_slept`；`_record_forced_night_visit()` 只擋 chosen。兩個入口對「今晚已經用掉了」的判準不同。
- **影響**：現行流程碰不到——fixed 在入夜 `_route_view()` 時就播完，meta-once 播完即封，不會在玩家睡完之後才觸發。是一致性問題，不是行為問題。
- **修法**：K-117 把 helper 改成回傳拒絕碼時，把 `night_sleep_pending` 一起納入同一組判準。

- **兌現（2026-08-22，`01ccff4`）**：helper 加第二道拒絕分支 `if night_sleep_pending`，與 chosen 那道同形（`push_error` ＋ 回 false）。兩個入口對「今晚已經用掉了」現在是同一組判準。
---

### K-123 P3-C 的按鈕三態沒有 `tests/ui_sim/` 契約

- **Phase**：P3-C／P3-F（UI 驗收形式）
- **嚴重度**：低
- **狀態**：已修（`72b84ca`，P3-F）
- **修復時機**：**P3-F**——「P3-C～E 所有 UI 契約以真實 input event 跑過」那條就是它的驗收。
- **問題**：K-116 的修法是 `test_p3c` 第 12 組——載入真 `main.tscn`、讀真 `AdvanceButton.text`、呼叫真 `_on_advance_pressed()`。行為證據是實的，但**它不是 `tests/ui_sim/` 契約**：沒有走 launcher、沒有真實 input event、catalog 仍是 63 條沒動。`測試指南.md > P3-C` 那兩條掛的是 🤖 UI 標記。
- **影響**：現在打勾成立（行為有證據、變異測試會紅），但 P3-F 收尾時若照 catalog 數字盤點，這兩條不會出現在 UI 契約那一側。不先寫下來，到那時就會變成「明明驗過卻找不到契約」。
- **修法**：P3-D 動 `p1af_cases.gd` 時順手把三態按鈕文字塞進既有的 `p2d_01_vision_three` 與一個到訪後變體，契約總數五處一起改（名單見 K-58）。
- **兌現（2026-08-25，`72b84ca`）**：改塞進 `p3d_05_sleep` 契約——停拍前斷言推進按鈕文字為「直接睡」、進入 `night_sleep_pending` 後為「進入隔天」，走 launcher 真實 input event。UI sim 97 變體／74 catalog 全綠含此條。**catalog 未增**（塞進既有契約，非新契約），五處數字不變。**已修。**

---

### K-122 發狂卡模擬文件與 P2-E 新基準分岔

- **Phase**：P3-F（文件同步）
- **嚴重度**：中（文件現在是錯的）
- **狀態**：已修（`98949f1`，P3-F）
- **修復時機**：**P3-F**——完整第一輪重演在那裡跑。
- **問題**：`bdfa5fd` 因為「D15 night-layer fixed 消耗今晚」把 `test_p2_sim` 的期望值改成 A 桌上 ≥3 張 18／45、B 21／45，視野窗口各挖掉第 15 天。`subdocs/驗證/發狂卡機制模擬.md` 仍寫 19／45、22／45、主窗口第 8–23／8–26 天。
- **影響**：`開發設計方針.md > P3-F` 的規矩是「若數值改變，**先更新模擬，再同步 P2-E 基準**」，這次順序反過來，於是程式基準跑在文件前面。K-74 已經預告過 D15 會產生 13／14 兩條路徑基準，本條是它的實際落地差額。
- **修法**：P3-F 用明示 id 的兩條策略重演之後，一次把模擬文件的三處數字（總天數表、窗口表、第 232 行對照表）與 P2-E 期望值對齊。**不要現在憑 `test_p2_sim` 的新數字回填文件**——那等於用測試期望值當事實來源，方向剛好相反。
- **兌現（2026-08-25，`98949f1`）**：模擬文件改為「第四次重跑（P3-F 真流程版）」，補齊路徑效率（13 Cost）與最大壓力（14 Cost）兩條具名策略章節，總天數表／窗口表／差異對照表三處同步。verifier 交叉核對：文件的 A 最大壓力視野 18／B 典型 21／窗口 8–14＋16–23＋42–44，與 `test_p2_sim.gd:425,438,552` 現行斷言逐項相符；供給 13／14、消除 11／12、留手 2 與 `test_p3f` 測試 2／3 相符。**已修。**

### K-124 P4-A 的 D8 發卡改變標準走查軌跡，靜默污染 day9 後所有衍生 fixture

- **Phase**：P4-A（測試 fixture／走查衍生）
- **嚴重度**：高（`run_ui_sim.ps1` 整條跑不動，且含不報錯卻語意已錯的情境）
- **狀態**：已修（`4b0ba1c`）
- **修復時機**：**P4-C UI 驗收前**——`make_states.gd` 是 launcher 第 2/5 步，卡住則 catalog／matrix／launcher 全部無法產出，P4-C 的 🤖 UI 條目與既有 74 契約迴歸覆蓋一起變暗。
- **問題**：P4-A 新增 D8 `n_manydoors`（`charge_first_visit` 夜間遭遇，收費發一張 madness 卡、計入 cap，`test_p4a` 已驗其撞上限觸發 BE）。標準走查（`custom_decisions=[]`）在 day8 必經此定日遭遇，於是 `d9_morning` 起手牌就帶著 `madness#1`（`d8_morning` 尚無）。`make_states.gd` 所有從標準走查 checkpoint 衍生、再手工 `gain_card("madness")` 的情境都繼承這張多出來的卡，產生三類後果：
  - **id 錯位**：`_madness_counter` 已是 1，手工 `gain_card("madness")` 生出的是 `madness#2` 不是 `madness#1`。`p2b_d17_madness`／`p2b_d17_ajie`／`p2d_d24_two_cards`／`p2d_d24_three_cards` 的 `hand.has("madness#1")` 後置條件因此翻紅——**這才是 launcher 卡死的真因**。（原先誤判為 `relations.ajie`；實測 `d17_morning` 的 `relations={"awei":1.0}`，ajie 不在字典讀 0，那個 clause 是過的。）
  - **佔格偏移**：多一張真卡使 `hand_slots_used`／溢位／逼近 cap 全部偏移。`p2d_near_cap` 以 `d10_night`（本身已帶 day9 那張 `madness#1`）為底補到 `cap-1=6`，總數撞 `madness_cap=7`，觸發發瘋 BE、`end_run()` 靜默重置回 day1——**生成不報錯，手動探測 final hand 只剩 `protagonist` 才發現**。
  - **clock 目標已證實錯位**：`p2c_night_one_forced` 直接寫 `madness_clock["madness#1"]=1`；實測該狀態有舊卡 `madness#1` 與新卡 `madness#2`，clock 是 `#1=1／#2=7`，所以被壓到 1 的確實是 D8 舊卡。雙卡案例則實際有三張（`#1=1／#2=1／#3=7`）。案例仍可能觸發一次／兩次強制縱慾，卻在結算後多留一張卡，屬於語意假綠。
- **2026-08-27 獨立量測**：以現行 dirty worktree 執行 `make_states.gd`、輸出至系統暫存目錄，結果 `exit 1`，停在 `p2d_near_cap 後置條件失敗`。量到的關鍵狀態如下：
  - `d9_afternoon`：1 張（`madness#1`，clock 6）；`d10_night`：仍 1 張（clock 5）；`p3a_night_baseline`：仍 1 張（clock 1）。
  - `p2a_multi_madness`：宣稱 3 張，實際 `madness#1～#4` 共 4 張；既有後置條件只查 `#1～#3` 存在、沒查 `#4` 不存在，所以靜默通過。
  - `p2c_night_one_forced`／`two_forced`：宣稱 1／2 張，實際 2／3 張；clock 錯位如上。
  - `p2d_near_cap`：從已帶 1 張的 `d10_night` 再補 `cap-1` 張，達 cap 後觸發 BE；輸出檔實際是 `day=1／phase=morning／hand=[protagonist]`。
  - D8 卡在 D15 上午歸零後，標準路徑確實執行一次強制縱慾：`indulgence_count=1`、`madness_cards_cleared=1`、留下 `binge_eaten=true`，且該上午行動格已由強制縱慾消耗。這證明在 D17／D24 checkpoint 載入後才清卡，無法把整段歷史還原成 P4-A 前的路徑。
- **影響邊界**：
  - 真實標準 checkpoint 應保留 D8→D15 的現行歷史；那是正式規則的真實結果，不應為了維持舊 fixture 而改 D8 資料或假裝未發生。
  - 需要修的是宣稱「精確 N 張 madness／精確 clock／逼近 cap」的合成 fixture：至少 P2-A multi、P2-B D17 兩案、P2-C 兩案、P2-D D24 兩案與 near-cap。P3-D 等只借用真實 checkpoint、未宣稱乾淨發狂歷史者，先保留真實背景，再由完整 UI 回歸確認沒有交叉影響。
  - P4-C 功能實作（`a7d26f7`）暫未發現被 D8 規則直接破壞：25 套 headless 全綠、5 個新 UI 案例曾個別以真實 input event 通過；但完整 UI 驗收被共用 fixture pipeline 擋住，**不能因此判 P4-C 已驗收**。
  - K-124 修過 near-cap 後還會遇到下一個獨立 fixture blocker：`p4c_d17_no_person` 後置條件用 `hand.size()==1` 代表「零人物卡」，但實測 `d17_morning` 有 8 張合法非人物卡。契約只要求 person card 數量為 0，應按卡片 type 計數，不能要求整手只剩主角。
- **建議處理步驟**：
  1. **正式資料與 production 規則不動**：保留 D8 `n_manydoors` 的 `charge_first_visit` 與發卡；問題是 fixture 假設過期。
  2. **先撤換試驗性 counter-only 修法**：不是在幾站裸設 `_madness_counter=0`；那只修 id，對多餘手牌、clock、forced queue 與 cap 都無效。
  3. **在 `make_states.gd` 建 fixture-only 正規化 helper**：只供精確張數合成情境使用；於 `deserialize()` 後移除 hand 內現存 madness instances、清對應 `madness_clock`、清 `forced_pending`、確認沒有殘留 instance 後重設 `_madness_counter=0`，再以 `gain_card()` 建出該案例原意張數。不要順手清標準路徑已合法產生的其他 flags／relations／歷史計數；若某案例真的要求「從未縱慾」，應另選較早的乾淨來源並明寫該前提，不能假裝逆轉歷史。
  4. **逐案套用而非全域洗白**：至少處理 P2-A multi、P2-B D17 兩案、P2-C 兩案、P2-D D24 兩案與 near-cap；P1-H／標準 checkpoint／P3-A baseline 保留真實 D8 卡與倒數。
  5. **把後置條件改成語意守衛**：逐案斷言 madness 張數恰好等於預期、clock key 集合與手牌 madness instance 完全一致、目標倒數值正確、day／phase 未被 BE 重置；near-cap 必須恰好 `cap-1`，P2-A／P2-C 必須明確拒絕第 N+1 張。至少做一次變異檢查：拿掉正規化呼叫後，P2-A／P2-C／near-cap 應轉紅。
  6. **修 P4-C 零人物卡後置條件**：以 `Data.loader.cards[_card_base_id(card)].type == "person"`（或等價共用 helper）計數為 0，不用 `hand.size()==1`。
  7. **同檔順手結 K-112**：既然本次必動 `make_states.gd`，把 `_generate_p3a_baseline()` 改成明示 `--p3a-baseline` 才執行；一般 UI launcher 不再重寫 K-100 baseline。K-70 也已到「下次動 `p1af_cases.gd`」的時機，P4-C 關門批次應一併補齊，但仍在 K-70 條目記自己的修復證據。
  8. **P4-C 關門驗證**：`make_states.gd` exit 0 → `test_p4c`／`test_p4b`／`playthrough_greedy` → 全部 25 套 headless → 完整 UI sim（現行 catalog 應為 79 contracts；全部 variants／contracts 0 failed，11 條負向反證以預期原因失敗）。完成 verifier 全面複審後，才由 verifier 更新 `測試指南.md`、本檔、`PROJECT_BRIEF.md`、`CODEX_HANDOFF.md`，P4-C 才能轉 ✅ 並進 P4-D。
- **修復（`4b0ba1c`）**：
  - 新增 fixture-only `_normalize_madness_only()`，只套在 P2-A multi、P2-B D17 兩案、P2-C 兩案、P2-D D24 兩案與 near-cap 共 8 組精確張數情境；移除既有 madness instances、清 `madness_clock`／`forced_pending`、重設 counter 後再以正式 `gain_card()` 建案例，不洗掉其他合法歷史。
  - 後置條件改驗 madness 張數恰等於預期、clock key 集合與手牌一致、目標倒數正確；near-cap 恰為 tuning 的 `cap - 1`，不再寫死數字。
  - `p4c_d17_no_person` 改按卡片 type 計算人物卡為 0，不再要求整手只剩主角；同批把 K-112 baseline 產生改成明示 `--p3a-baseline`。
- **verifier 複驗（2026-08-27）**：
  - 以系統暫存目錄獨立執行 `make_states.gd`：exit 0；8 組 fixture 的張數／clock／day／phase 守衛全部成立，`p4c_d17_no_person` 成功產生；一般生成前後 P3-A baseline mtime 不變。
  - `tests/run_all_headless.ps1`：25 套全部通過，含 `test_p4b`、`test_p4c`、`playthrough_greedy`；正式資料 64／48／18／261，引用與 lint 1～16 全 0。
  - 完整 `run_ui_sim.ps1` 已從原本第 2 步卡死進展到 79 contracts 全部執行，證明 K-124 的 pipeline blocker 已解除。其餘 4 個變體失敗另由 K-125 追蹤，不回掛 K-124。
- **結論**：K-124 與同批 K-112 結案；正式 D8 資料與 production 規則維持不變。K-125 後續亦已修復並完成完整 UI sim 關門，P4-C 轉 ✅。

### K-125 委託教學 modal 遮住 beat 推進，UI sim 誤報 D17 無限迴圈

- **Phase**：P4-C（UI sim 工具鏈／真實輸入流程）
- **嚴重度**：高（阻塞 P4-C 關門驗收）；**不是 production 玩家死鎖**。
- **狀態**：已修（`7a439f6`）
- **發現方式**：K-124 打通 `make_states.gd` 後，2026-08-27 首次讓完整 UI sim 真正跑到底。Run `20260827-074010-986-p50004-a1c03a80`：102 variants、79 catalog／79 executed、98 passed、4 failed；76／79 contracts completed，11 條負向反證全部按預期失敗。
- **問題與根因**：
  - D17 morning 第一次推進 `d17_morning_phone` 時，`on_enter` 依正式故事效果取得第一張人物卡；P4-C 契約正確 emit `delegation_tutorial_available`，`main.gd` 彈出 `DelegationTutorialDialog` modal。
  - `QAStep.has_visible_qa_id()` 只檢查 `is_visible_in_tree()`，不知道 control 已被 modal 遮擋。因此 dump 同時把 `dialog_confirm::delegation_tutorial` 與底層 `beat_advance` 判為可見。
  - `drain_beats()` 只要看見 `beat_advance` 就送真實滑鼠輸入；事件實際被 modal 攔截，底層 `_on_advance_beat_pressed()` 沒再執行。helper 卻把每次送入都算成一次推進，20 次後才回「疑似無限迴圈」。
  - `LocationPanel` 沒有重新排入 beat；`GameState.play_beat()` 也沒有重複套 `on_enter`。失敗截圖清楚顯示教學視窗蓋在「繼續演出」上，故原先「`d17_morning_phone` beat 無限迴圈」假說排除。
- **影響邊界**：
  - 直接失敗 4 variants：`p1g_case_03_reenter_no_duplicate_on_enter`、`p2b_04_violence_after`、`p2b_04_lust_relation`、`p1af_33_full_walk`；對應 3 contracts 未完成。後續地點不可見、`phase_advance` disabled 與 oldstreet 再次報上限都是第一個 modal 沒關掉後的連鎖，不是額外根因。
  - P4-C 新增 `p4c_01`～`p4c_05` 五個 variants 全部通過；25 套 headless 全綠。玩家會看到教學並可按 OK 正常繼續，production 行為符合「UI 關閉後才寫 `delegation_tutorial_seen`」契約。
  - 同族風險在所有「modal 出現時仍以 `is_visible_in_tree()` 判底層 control 可操作」的 UI sim helper；本次只確認委託教學命中，不擴張成 production UI 缺陷。
- **建議處理步驟**：
  1. **production／正式資料不動**：保留 D17 取得人物卡與第一次取得時彈教學；不可為了讓測試綠而關掉 modal、延後故事效果或預先把所有 fixture 的 `delegation_tutorial_seen` 洗成 true。
  2. **讓 `drain_beats()` 對已知教學 modal 有優先序**：每輪先檢查 `dialog_confirm::delegation_tutorial`；若可見，透過既有 `QAStep.click()` 真實點擊關閉，確認視窗消失後才繼續 `beat_advance`。只處理明示允許的 dialog；未知 modal 應回具名阻塞錯誤，不能全部靜默關閉。
  3. **分開計數**：教學關閉不計入 beat 的 20 次上限；另設小型 dialog safety cap／handled ids，避免 dialog 自己重彈時再次被誤報成 beat loop。回傳 observation 應記錄處理過的 dialog，讓案例仍能驗到教學確實發生。
  4. **補 D17 回歸證據**：第一次推進後教學出現；真實關閉後 `delegation_tutorial_seen == true`；演出在有限步數內結束；離開重進只重播文字，完整 serialize 不重複結算 `on_enter`。
  5. **先跑四案再跑全套**：先重跑上述 4 variants，確認沒有連鎖失敗；再跑完整 launcher。關門標準是 102／102 variants、79／79 contracts、0 failed checks，11 條負向反證仍以預期原因失敗。
  6. **P4-C 文件收尾**：全綠後才由 verifier 打勾 `測試指南.md > P4-C`、把 `PROJECT_BRIEF.md` 的 P4-C 轉 ✅、更新 `CODEX_HANDOFF.md`，再進 P4-D。
- **修復（`7a439f6`）**：`QAStep.drain_beats()` 先處理明示列入 `KNOWN_BLOCKING_DIALOGS` 的委託教學 modal，再推進底層 beat；dialog 使用真實輸入且另設 5 次 safety cap，不占用 20 次 beat 上限。回傳值新增 `dialogs_handled` observation，D17 案例明示斷言教學曾被處理且 `delegation_tutorial_seen == true`。
- **verifier 複驗（2026-08-27）**：原 4 個失敗變體全部通過；完整 run `20260827-081934-525-p21024-40718853` 為 102／102 variants、79／79 catalog／executed／completed contracts、0 failed，11 條負向反證仍全部按預期失敗。報告：`_qa/runs/20260827-081934-525-p21024-40718853/report.json`。
- **非阻塞觀察**：已知 modal 路徑有明確處理；若未來出現未列入白名單的未知 modal，現行 helper 最終仍可能以 beat 上限錯誤收束，而不是立即回具名 blocking-dialog 錯誤。這是低優先診斷精度風險，不影響本次功能或關門證據。
- **結論**：K-125 結案；K-124 的 25 套 headless 證據與本次完整 UI regression 合併滿足 P4-C 七條驗收，P4-C 轉 ✅，下一階段為 P4-D。


### K-126 夜間定日遭遇永遠不會啟動，D8 的三回合是死內容

- **Phase**：P4-D（規則層接線）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：全專案 `start_encounter()` 只有一個非測試呼叫點——`_check_fixed_encounter_for_current_phase()`，而該函式第三行就是 `if phase == "night": return`。夜間 fixed beat 走的是 `play_night_fixed()`，它做了 `charge_first_visit` 收費與 `play_beat()`，但從不呼叫 `start_encounter()`。
- **實測（runtime probe，第 8 天夜、真實 `advance_phase()` 推進路徑）**：`active_encounter` 在 `play_night_fixed()` 前後都是 `{}`；`beats_entered` 已含 `n_manydoors_ch1`；`hand == ["madness#1"]`（首次到訪費照收）；下一次 `advance_phase()` 直接到 day 9 morning，仍無遭遇。
- **影響**：玩家付了一張發狂卡的代價，卻整場跳過 D8 的 `name_since_when`／`who_remembers`／`what_remains` 三回合；`repeat_each_run`、`escape_cost:1`、`allow_discard:true` 與三組 `responses` 全部是死資料。這不是 P4-E 的 UI 缺口——P4-E 只做畫面，不會補規則層的啟動接線。
- **契約依據**：`開發設計方針.md > P4-D`「由 phase hook 啟動的強制 D8 不走此玩家入口，因此仍發生並承受 penalty」；`測試指南.md > P4-D`「超載玩家進新夜間地點回 `overloaded`，強制 D8 仍啟動並追加 penalty」；`實作規格書.md > P4-D`「定日時段到達時由規則層建立 `stage == "intro"` 的 active encounter」（未排除 night）。
- **建議修法**：`_check_fixed_encounter_for_current_phase()` 拿掉 night 排除，或在 `play_night_fixed()` 的 `charge_first_visit` 分支之後、`play_beat()` 播完文字之後呼叫 `start_encounter(bid)`；兩者擇一但要與 K-134 的「一夜一遭遇」語意對齊。收費若已觸發 BE（現有 `if phase != "night": return lines` 那道）必須維持不啟動遭遇。

### K-127 遭遇自動啟動 hook 零測試覆蓋

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**與 K-126 同批**。
- **問題**：`test_p4d.gd` 的遭遇全部由測試自己呼叫 `start_encounter()` 建立；`_create_mock_encounter_beat()` 造的 beat 沒有 `fixed:true`、也沒進 `beats_at()` 索引，所以 phase hook 這條路一次都沒被走過。
- **變異驗證（2026-08-27，detached worktree）**：把 `_check_fixed_encounter_for_current_phase()` 整段改成立即 `return`，**26 套 headless 全部 exit 0**。
- **影響**：`測試指南.md > P4-D`「phase 進入定日遭遇時自動建立 intro」這條打不了勾；K-126 這個等級的缺口不會被任何測試擋住。
- **建議修法**：用正式資料驗——推進到第 45 天 afternoon 斷言 `d45_encounter` 自動建立 `stage:"intro"`；D8 修好後同樣推進到第 8 天 night 斷言 `n_manydoors_ch1` 建立 intro 且 marker cost 只收一次。

### K-128 15 碼拒絕矩陣沒有一例比對 serialize 零變化

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`serialize()` 在 `test_p4d.gd` 全檔只出現兩次，都在序列化往返測試裡。15 碼矩陣與四條「零變化」驗收（`card_not_submittable`、discard 拒絕、escape 拒絕、拒絕矩陣本身）沒有任何一例比對前後狀態，只驗 `reason_code`。
- **變異驗證（2026-08-27）**：在 `respond_to_encounter()` 的 `not_held` 分支加 `flags["MUTATION_side_effect_on_reject"] = true` 與 `blocked_slots += 3` 再回原拒絕碼，**26 套全綠**。
- **影響**：這是 K-98 家族——「只驗 `ok == false`／只驗 code 的測試，在實作偷改狀態時照樣全綠」。
- **建議修法**：矩陣每一例前後各取一次 `serialize()` 並逐字比對（P3-B 的八碼矩陣已有現成寫法可抄）。

### K-129 `encounter_view()` 的不洩漏與候選涵蓋整條零覆蓋

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`；denylist 殘留限制見 K-147）
- **修復時機**：**P4-D 通過前**。
- **問題**：全檔沒有任何 view 的負向斷言，也沒有正向斷言：候選是否精確等於 `hand ∪ knowledge`、是否標 `source`、容量是否只計 hand、`not_held` 與 `has_card()` 是否一致。測試 1 的 view 檢查只看 intro 的 `stage` 與 `blocked_slots`。
- **變異驗證（2026-08-27）**：讓 `Encounter.build_view()` 的 round 分支額外回傳 `LEAK_responses`（含 `accepts` 正解集合）、`LEAK_fallback`、`LEAK_all_rounds`，**26 套全綠**。
- **影響**：`實作規格書.md > P4-D`「view model 標出來源與是否可選，不洩漏答案集合」與 `測試指南.md > P4-D` 第 2 條整條沒有證據；P4-E 的 UI 會直接吃這個 view。
- **建議修法**：正向驗候選集合與 `source`；負向逐字斷言 view 的 key 集合不含 `accepts`／`fallback`／`rounds`／未到回合的 demand。

### K-130 `after_finish:"stay"` 零覆蓋，且檔頭註解過度宣稱

- **Phase**：P4-D（測試覆蓋／措辭）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`_test_victory_and_after_finish()` 只寫了「遭遇 A: after_finish advance_phase」一段就 return，沒有 stay 的案例；但 `test_p4d.gd` 檔頭第 12 項寫「after_finish: "advance_phase" 推進時段、"stay" 停留原時段」。
- **變異驗證（2026-08-27）**：`_finish_encounter()` 改成 `if after_finish == "advance_phase" or after_finish == "stay"` 也推進，**26 套全綠**。
- **影響**：K-104 同族（測試措辭宣稱了程式沒做的事）。D8 正是 `after_finish:"stay"`，它若誤推進會直接吃掉玩家一個夜晚。
- **建議修法**：補 stay 案例，斷言結算後 `day`／`phase` 逐字不變；檔頭註解在補上之前先改成實際涵蓋範圍。

### K-131 拒絕順序驗收五個入口只做到一個

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`測試指南.md > P4-D` 要求「為五個遭遇 mutation 入口各做至少一組『兩個拒絕條件同時成立』案例」。`_test_rejection_priority_orders()` 實際只有三條：`respond_to_encounter` 的 `madness_blocked > already_attempted`，以及 `enter_night_location` 的 `not_night > encounter_active`、`encounter_active > overloaded`——後兩條的入口不在那五個之內。
- **影響**：`start_encounter`、`acknowledge_encounter_intro`、`discard_in_encounter`、`escape_encounter` 四個入口的固定順序完全沒被證明；順序寫反不會紅（K-98 同族）。
- **建議修法**：四個入口各補一組交集案例，逐字比對方針列的優先序；`escape_encounter` 至少要驗 `cannot_escape > wrong_escape_count` 與 `duplicate_payment > not_held` 兩個交集。

### K-132 `respond_to_encounter()` 的 data_conflict 排在 card_not_submittable 之前

- **Phase**：P4-D（契約偏差）
- **狀態**：已修（程式，`35f1f79`）；**回歸測試仍缺，見 K-145**
- **修復時機**：**與 K-131 同批**。
- **問題**：方針 P4-D 的固定順序是「⋯ → attempted → card not submittable → data conflict」。實作在 attempted 之後先查 `round_data.is_empty()` 回 `data_conflict`，之後才做 `card_not_submittable`。
- **影響**：兩條件同時成立（round 資料遺失＋提交不可丟棄卡）時回錯碼。要壞 fixture 才觸發，但那正是 K-131 缺的那類測試。
- **建議修法**：二選一——把實作的 round 查表移到 `card_not_submittable` 之後，或修方針把 data_conflict 的位置寫成實際順序並說明理由。不要兩份文件各留一種說法。

### K-133 `_finish_encounter()` 缺 `run_generation` 守衛

- **Phase**：P4-D（規則層防禦）
- **狀態**：已修（程式，`35f1f79`）；**回歸測試仍缺，見 K-145**
- **修復時機**：**P5 之前**。
- **問題**：`_finish_encounter()` 先 `EffectApply.apply(出口效果)`，接著若 `after_finish == "advance_phase"` 就無條件呼叫 `advance_phase()`。同一支檔案的 `delegate()` 延遲回報（`gen_before := run_generation` 那段）有守衛，遭遇出口沒有。
- **影響**：出口效果一旦能發狂卡並撞上 `madness_cap`，`end_run()` 會把本輪重置回第 1 天 morning，接著這行 `advance_phase()` 會把**新一輪**推到 day1 afternoon——K-65 一模一樣的形狀。現行正式資料的 `on_victory`／`on_failure`／`on_escape` 只有 `text` 與 `flag`，**目前不可觸發**。
- **建議修法**：套效果前存 `gen_before`，效果後 `run_generation` 變了就直接 return，不推進。

### K-134 「每個 round id 只能進一次」規則層沒有守

- **Phase**：P4-D（規則層守衛）
- **狀態**：已修（`35f1f79`；回傳語意見 K-146）
- **修復時機**：**P4-F**。
- **問題**：方針 P4-D 寫「後續每個 round id 也只能進一次、各加一次」。`active_encounter` 只存當前 `round_id`，沒有 visited 集合；資料若出現能回到既訪 round 的圖，會重複加 cost 而沒有任何檢查會紅。
- **影響**：現行 D8／D45 都是線性圖，靠資料形狀成立，不是靠規則層。SCHEMA 只禁「無出口 cycle」，有出口的 cycle 沒有被明文擋掉。
- **建議修法**：`active_encounter` 加 `visited_round_ids`，重進既訪 round 時 `push_error` 並回 `data_conflict`；或在 P4-A 的 graph lint 明文禁止任何 cycle，二選一並在文件寫清楚選了哪個。

### K-135 `hand_size` 的 fallback 預設值四處分歧

- **Phase**：P4-D（數值 SSOT）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**下次動這幾支時順手**。
- **問題**：`tuning.json` 的 `hand_size` 是 14。但 `game_state.gd` 的三個讀取點分別是「無預設」（`_check_hand_overflow`）、`7`（`is_overloaded`）、`7`（`_check_encounter_capacity_failure`）；`Encounter.build_view()` 也是 `7`；`test_p4d.gd` 兩處用 `14`。
- **影響**：tuning 鍵一旦缺失或拼錯，容量會靜靜砍半而不是報錯，且測試與實作會用不同的假設值——K-105／K-114 同族。
- **建議修法**：統一成不帶預設（缺鍵就讓它報錯），或四處共用同一個常數；測試那兩處也改成同一個取法。

### K-136 `_check_encounter_capacity_failure(enc)` 的 `enc` 是死參數

- **Phase**：P4-D（程式衛生）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**與 K-128 同批**。
- **問題**：函式簽名收 `enc: Dictionary`，函式體只讀 `Data.tuning`、`hand` 與 `active_encounter`，`enc` 一次都沒被讀。四個呼叫端都傳了它。
- **影響**：K-105 同族（參數加了沒人用）。讀的人會以為容量規則跟遭遇資料有關。
- **建議修法**：拿掉參數，或真的讓它讀遭遇級的容量覆寫（目前 SCHEMA 沒有這個欄位，所以應該是前者）。

### K-137 `_extract_base_id` 是 `_card_base_id` 的第二份實作

- **Phase**：P4-D（維護風險）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**下次動 `encounter.gd` 時順手**。
- **問題**：`Encounter._extract_base_id()` 與 `GameState._card_base_id()` 逐字相同（`rfind("#")` 切前綴）。
- **影響**：K-14 同族，維護風險不是 bug。base id 規則之後若要改（例如支援多層後綴），有兩個地方要同步。
- **建議修法**：`Encounter` 的靜態函式已經收得到 gs，改呼叫 `gs._card_base_id()`；或把它提到共用的 `data_facts.gd`。

### K-138 P4-D 測試全部走合成 beat 與合成卡，且污染 loader 不還原

- **Phase**：P4-D（測試形式）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`_create_mock_encounter_beat()` 直接寫 `data_node.loader.beats_by_id[beat_id]`；各測試另外往 `loader.cards` 塞 `k_fugu_cure`／`k_clue`／`k_a`／`k_b`／`k_sol` 等假卡，跑完不還原。合成 beat 沒有 `fixed:true`，也沒進 `beats_at()` 索引。
- **影響**：兩件事——① 正式資料的遭遇路徑一條都沒跑到（K-126／K-127 就是從這個縫掉出去的）；② 假卡留在同 process 的 loader 裡，後面的測試段落與之後新增的測試都會看到它們（K-45／K-124 同族）。P4-C 才因為同一件事被要求改走真實入口（`247c716`）。
- **建議修法**：保留合成 fixture 驗邊界，但每個驗收條目至少要有一條走 `n_manydoors_ch1` 或 `d45_encounter` 的真實路徑；假卡改成測試段落結束時移除，或另建隔離的 loader。

### K-139 序列化往返測試自捏 attempted，且缺未存檔對照

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`_test_serialization_roundtrip()` 用 `(gs.active_encounter["attempted_card_ids"] as Array).append("dummy_card")` 直接造 attempted，而不是由 `respond_to_encounter()` 產生；intro 那半在同一個 gs 上 `deserialize(ser1)`，沒有先 `_reset_gs()`，所以證不了「從乾淨狀態能還原」。
- **影響**：`測試指南.md > P4-D`「從 intro 與 round 各自還原後，結果都與**未存檔路徑**相同」這半條完全沒做——沒有任何未存檔對照組。`deserialize()` 對 `active_encounter` 也沒有任何健全度檢查（未知 beat_id、非法 stage、不存在的 round_id 都照收），目前無測試會紅。
- **建議修法**：attempted 改由真實 respond 產生；intro 與 round 各跑「存檔→重置→讀檔→繼續操作」與「不存檔直接繼續操作」兩條，逐字比對最終 `serialize()`。

### K-140 「效果只套一次」用 boolean flag 驗不出重複套用

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：勝利測試的 `on_victory` 是 `{ "flag": { "adv_victory": true } }`，斷言只查該 flag 為 true。同一個效果套兩次，結果與套一次逐字相同。failure 與 escape 也是同樣寫法。
- **影響**：`測試指南.md > P4-D`「victory／failure／escape 的效果各只套一次」驗不到；`_finish_encounter()` 之後若有人加第二次呼叫，測試不會紅。
- **建議修法**：出口效果改用可累加的量（例如 `relation` 加值或 `gain` 一張卡）並斷言精確數值／張數。

### K-141 「不消耗 action、不開縱慾或備用區」零覆蓋

- **Phase**：P4-D（測試覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-D 通過前**。
- **問題**：`action_spent`、`indulgence_count`、`forced_pending` 三個識別字在 `test_p4d.gd` 出現 **0 次**。
- **影響**：`實作規格書.md > P4-D`「遭遇不消耗行動、不開縱慾與備用區」與 `測試指南.md > P4-D` 對應那半條沒有證據。遭遇全程會呼叫 `EffectApply`，那正是會動到這些欄位的地方。
- **建議修法**：在一場完整遭遇（intro → 多回合 → 出口）前後斷言三者逐字不變。

### K-142 走查的遭遇處理只答一次，且只試 hand

- **Phase**：P4-D（走查覆蓋）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**P4-F**。
- **問題**：`playthrough_greedy.execute_action_phase()` 新增的遭遇分支只 `acknowledge` 一次、對 hand 逐張試 `respond_to_encounter()`、第一張 ok 就 `return`。多回合遭遇不會被走完；knowledge 卡、`discard_in_encounter()`、`escape_encounter()` 一律不試；該時段固定計為 `all_fixed`。
- **影響**：目前因 K-126 走查根本碰不到夜間遭遇，D45 又只有一回合，所以不外露。D8 修好之後，走查會在夜間卡在 round 1，而夜間推進被 `encounter_active` 擋住，`assert(phase == "morning")` 會直接讓走查爆掉。
- **建議修法**：遭遇分支改成 while 迴圈直到 `active_encounter` 清空（帶步數上限），候選集合改讀 `encounter_view().candidates`（含 knowledge），無合法提交時再試 discard／escape。

### K-143 P4-D 順手刪掉走查腳本 afternoon 的 `last_ind_count` 快照

- **Phase**：P4-D（範圍外改動）
- **狀態**：已修（`35f1f79`）
- **修復時機**：**下次動走查腳本時**。
- **問題**：`f82b1e8` 把 afternoon 區塊的 `last_ind_count = int(gs.get("indulgence_count"))` 刪掉，換成 `if str(gs.get("phase")) == "afternoon": gs.advance_phase()`。
- **影響**：**追過資料流，沒有功能迴歸**——該快照在 evening 區塊（`execute_evening_phase()` 之後那行）會被覆寫，`forced_m`／`forced_a` 的判定不受影響。但它屬 K-60 那組守衛的形狀，且刪掉之後四個快照點只剩三個，讀的人會以為 afternoon 那格不需要。條件式推進本身也把 `advance_phase()` 的拒絕結果整個吞掉（方針 P4-D 寫「所有呼叫者必須消費結果」）。
- **建議修法**：四個快照點形狀統一（補回或四處一起改成別的寫法），並讓走查在 `advance_phase()` 回 `ok:false` 時明示失敗而不是靠後面的 assert 才炸。


### K-144 `test_p2_sim.gd` 自 P4-D 起整份失效，零條指標斷言會執行

- **Phase**：P4-D（整合測試失效）
- **狀態**：待修（阻斷）
- **修復時機**：**P4-E 開工前**。
- **問題**：三段連鎖——① 遭遇啟動後 `advance_phase()` 回 `encounter_active`，而 `test_p2_sim` 沒有任何遭遇處理，時間停在遭遇當下；② 下一天的 `assert(int(gs.get("day")) == d and ...)` 失敗，Godot 在此中止 `_run_simulation()` 並回傳型別預設值（空 Dictionary）；③ `_verify_player_a/b/c()` 第一行就是 `res["peak_madness"]`，對空 Dictionary 取值再次中止函式並回傳 `int` 預設值 `0`，於是 `failed` 永遠是 0。
- **實測（2026-08-27，HEAD `35f1f79`）**：`test_p2_sim.gd` 單跑 exit 0，但輸出裡 **`[OK]` 0 行、`FAIL` 0 行**，另有 6 筆 `SCRIPT ERROR: Assertion failed`（`_run_simulation:131`）與 4 筆 `Invalid access to property or key 'peak_madness'`。在 `_run_simulation()` 結尾插 print 探針，**5 次模擬沒有任何一次到達**（SIMEND count = 0）；逐日 trace 顯示五次模擬全部停在 `d=9 day=8 phase=night enc=intro`（`n_manydoors_ch1`），總 TRACE 行數 45 ＝ 5 × 9 天。
- **回歸起點**：`37cd849`（P4-D 之前）0 筆 assert 失敗；`c73b754`（P4-D 第一版）已是 5 筆；`35f1f79`（18 條修復後）仍是 5 筆。**是 P4-D 第一版打壞的，18 條修復沒有涵蓋到它**——那一批把 `solve_active_encounter_if_any()` 補進 `playthrough_greedy.gd` 與 `test_p3f.gd`，漏了 `test_p2_sim.gd`。
- **影響**：`test_p2_sim` 是 P2-E 的整合測試（三種玩家 45 天重演、八個指標、A 的 26 項時間軸）。它現在一條指標都沒驗，卻印「P2-E: all simulation tests passed successfully (exit 0)」，`run_all_headless.ps1` 也照報 ALL PASSED。P2 的所有規則從此沒有整合層保護。
- **同類掃描**：以「assert 失敗 / Invalid access」逐套掃過全部 headless 腳本，**只有 `test_p2_sim` 中招**，其餘 25 套乾淨。
- **建議修法**：① `test_p2_sim` 的四個時段推進點前後補 `PlaythroughGreedy.solve_active_encounter_if_any(gs)`（與 `test_p3f` 同一手法）；② 把 `assert()` 換成會累加 `failed` 的顯式檢查，或在 `_verify_player_*` 開頭先擋 `res.is_empty()` 並記一條 FAIL——**中止即回預設值、預設值又剛好等於「沒有失敗」** 是這次假綠的根因，不修這半條，下次任何一個 assert 失敗仍會靜靜變成綠燈；③ `run_all_headless.ps1` 增加「stderr 出現 SCRIPT ERROR 即視為失敗」的守門。

### K-145 K-132／K-133 的修正沒有回歸測試，且測試標錯條目

- **Phase**：P4-D（測試覆蓋／標註）
- **狀態**：待修
- **修復時機**：**與 K-144 同批**。
- **問題**：兩條程式都確實改對了，但沒有任何測試會咬——變異測試把 `respond_to_encounter()` 的 `data_conflict` 換回 `card_not_submittable` 之前、把 `_finish_encounter()` 的 `run_generation` 守衛整段拿掉，`test_p4d` 兩次都 exit 0。全檔搜不到 `run_generation`／`gen_before`。
- **另一半**：`_test_rejection_priority_orders_all_endpoints()` 把 `already_attempted > card_not_submittable` 那條的訊息標成「(K-132)」，但 K-132 講的是 `card_not_submittable > data_conflict`。標註指向錯的契約，讀的人會以為已經驗過。
- **影響**：這正是本專案反覆出現的形狀——修正做了、測試沒跟上，把實作改回去測試照樣全綠（K-105／K-108／K-110／P3-B 那八條同族）。
- **建議修法**：① 用壞 round fixture 造「round 資料遺失＋提交不可丟棄卡」的交集，斷言回 `card_not_submittable`；② 用會撞 `madness_cap` 的出口效果造 BE，斷言 `_finish_encounter()` 之後 `day`／`phase` 停在重置後的第 1 天 morning、沒有被多推一格；③ 把那條訊息的 K 編號改對。

### K-146 cycle 偵測回拒絕碼，但狀態已經被改過

- **Phase**：P4-D（契約語意）
- **狀態**：待修（低）
- **修復時機**：**P4-F**。
- **問題**：`respond_to_encounter()` 偵測到 `visited_round_ids` 重複時回 `{ok:false, reason_code:"data_conflict"}`，但那一刻已經 `attempted.append()`、依 `consume_card`／fallback 消耗過卡片、套完 `on_resolve`，而且緊接著呼叫了 `_finish_encounter("failure", ...)`。
- **影響**：方針 P4-D 寫「任一步拒絕都回對應精確碼且完整狀態零變化」，而 K-128 補的零變化測試只覆蓋前置檢查那幾碼，抓不到這一條。同一個 `data_conflict` 碼現在有兩種語義：前置拒絕（零變化）與事後偵錯（已結算）。
- **建議修法**：二選一並寫進方針——① 改回 `ok:true` 並帶 failure 出口（它實際上就是失敗結算）；② 把 cycle 檢查前移到 `attempted.append()` 之前，讓它成為真正的零變化拒絕。

### K-147 `encounter_view()` 的不洩漏檢查是 denylist 不是 allowlist

- **Phase**：P4-D（測試強度）
- **狀態**：待修（低）
- **修復時機**：**P4-E**。
- **問題**：`_test_view_model_completeness_and_non_leakage()` 檢查的是六個禁用 key（`accepts`／`fallback`／`on_resolve`／`responses`／`rounds`／`next_round`）不存在。**實測**：以 `accepts` 為 key 洩漏 `responses` → 轉紅（防線有效）；改用 `solution_cards` 這個不在名單上的 key 洩漏同一份資料 → exit 0，沒有被抓到。
- **影響**：現行實作沒有這個問題，是測試強度的上限問題。P4-E 的 UI 會直接消費 view，屆時新增欄位的機會最多。
- **建議修法**：改成 key allowlist——view 的 key 集合必須精確等於契約列出的那一組，多一個就紅。

## 二、已接受的邊界決定

**現在都不動手。** 分兩種：`不做`（實作已經是對的，看到不要「順手修掉」）與 `要做`（工作內容已在後面 Phase 的契約裡，到那個 Phase 自然做掉）。

一覽：

| # | 要不要做 | 兌現時機 |
|---|---|---|
| B-01 | 不做 | — |
| B-02 | 要做（已兌現） | P1-F（`55d62a8`） |
| B-03 | 要做（已兌現） | P1-F（`55d62a8`；當時只兌現一半，兩個缺口 K-25／K-28 亦已結案） |
| B-04（原 K-107） | 不做 | — |
| B-05 | 不做 | — |

### B-01 `madness` 卡放置不消耗行動格

- **要不要做**：不做。現行實作是對的。
- **兌現時機**：—（P2 做縱慾時是新增功能，不是修本條）
- **理由**：`實作規格書.md > 六` 的表格寫 `madness` 消耗行動格，但同一格註明「規則本身如此，**非放置機制**」。消耗發生在縱慾流程（第八節），不在 `try_place`。P1-D 的 `try_place` 只為 `protagonist` 呼叫 `consume_action()`，正確。本條存在的唯一目的是**擋住「看到表格就去 `try_place` 加一行」**。

### B-04（原 K-107）baseline 的「先產再改」只能靠自陳，沒有機器防線

- **要不要做**：不做。
- **兌現時機**：—
- **現況**：`測試指南.md > P3-A` 那條要求 baseline 的「前」那一半在動 P3-A 資料之前產生。實際兩次（`ce7c41a`、`41d2d8e`）都是資料改完之後才寫出檔案，所以那條驗收當下是恆真的——**拿改後資料產 expected，再拿改後資料去比**。
- **為什麼結論仍然成立**：verifier 兩次都另外把 worktree 開在 `870db64`（P3-A 之前的資料）、用現行 generator 重跑，拿到與 `_qa/p3a_baseline/` 逐字相同的兩個檔。「P3-A 沒有提前改玩家流程」是靠這個獨立重跑證明的。
- **為什麼不做**：要真的擋住它得加新機制（baseline 產生時把當下 `data/` 的 hash 一併寫進 `expected.json`，比對時 hash 等於當前 `data/` 就直接紅）。那是新需求不是修 bug，而 P3-A 只會做這一次；P3-B 之後的階段沒有等價的「改資料前後對照」驗收。
- **另外還有一層，靠紀律更擋不住**：baseline 產生掛在 `generate_all_states()` 裡，**任何人跑一次 UI 模擬就把它重寫成當前資料的版本**（K-112，verifier 實測 mtime 對得上 UI 模擬的執行時刻）。就算有人乖乖先產了 baseline，下一次 UI 模擬也會把證據蓋掉。K-112 修掉之後這一條才有討論「要不要用紀律守」的空間；在那之前，**唯一有效的防線是 verifier 用舊 commit 另開 worktree 重跑對照**（本輪做了三次，三次都逐字相同）。
- **本條存在的目的**：擋住兩件事——**看到那條驗收未打勾就去把它勾掉**，以及**以為重跑一次 generator 就算補救了**。下次真的需要跨版本對照時（例如 P6 存檔遷移），再把 hash 那個機制當新需求開。

### B-05 手牌上限目前是 warning-only 軟上限

- **要不要做**：不做（現行實作對 P3 是對的）。
- **兌現時機**：—（超載／備用區的真正處置＝待決 34，觸發點在 P4 內容齊備後的實際遊玩）
- **現況**：`game_state.gd > _check_hand_overflow()` 只 `push_warning`、卡照樣上桌，手牌實際張數可超過 `hand_size`。P3-F 的 `test_p3f` 第一輪 greedy 走查因此會噴 `hand is full (17/14)`。
- **為什麼不做**：① 這是**刻意的軟上限佔位**——「滿了怎麼辦」＝超載／備用區，是待決 34（明文「無上限，要不要加硬上限等實際遊玩才知道」），方針也把它排除在 P2 之外；`實作規格書.md > 八` 更寫明超載中「卡照樣上桌，手牌實際張數可以超過 `hand_size`」，warning-only 與規格一致。② 溢位**只在 greedy 走查測試裡出現**（見卡就撿、從不管理手牌），真人會放卡／丟卡 churn 手牌，正常玩碰不到（待決 35：第一輪碰不到 cap）。③ P4 的人物卡外出／回報會改變「什麼卡在手牌流動」，正是待決 34 要等的前提——現在定超載規則，P4 內容進來又得重定，順序反了。
- **本條存在的目的**：擋住「看到 `test_p3f` 的 `hand is full` warning 就去 `_check_hand_overflow` 加 clamp／拒收」。真要處置等待決 34 於 P4 後拍板。
- ⚠️ **保留 warning 的價值**：它之後若蓋住「某張該消耗的卡沒被消耗」的真 bug 會失去示警作用——那是測試衛生問題，不擋 P4，但值得記一筆。

### B-02 `run_ended` 目前可重複發射

- **要不要做**：要做。
- **兌現時機**：**P1-F**。契約在 `開發設計方針.md > P1-F` 結局 coda 三條；驗收在 `測試指南.md > P1-F`（「`run_ended` 一輪恰好發射一次」）。
- **做什麼**：`GameState.advance_phase()` 在第 45 天 evening 改成自己呼叫 `end_run()`，收尾不丟給 `run_ended` 的 listener。
- **理由**：現況是發完 `run_ended` 後不改狀態，再呼叫就再發一次。P1-A～P1-D 階段 `main.gd` 靠隱藏推進鈕擋住，沒有可觸發路徑，所以現在不急。**不要提前加「本輪已結束」旗標**——P1 沒有東西需要它，而 Phase 5 真結局會讓那個旗標的形狀再變一次。
- **兌現紀錄**：commit `55d62a8`，**已兌現**。`advance_phase()` 在第 45 天 evening 自己呼叫 `end_run()`；`end_run()` 內部順序照契約（emit `run_ended` → run 層重置 → 回第 1 天 morning），沒有加旗標。`test_p1f.gd` 第 8 項驗「連續推進 4 次後計數仍為 1」，`playthrough_greedy.gd` 也驗一輪恰好 1 次，兩條都綠。`main.gd._on_run_ended()` 不再隱藏推進鈕，第二輪走得下去。

### B-03 `data_facts.gd` 與 `flow_text.tscn` 尚未建立

- **要不要做**：要做。
- **兌現時機**：**P1-F**。兩者都已列在 `開發設計方針.md > P1-F` 的「動的檔」與 `by_design` 名單那條。
- **做什麼**：建 `flow_text.gd`（殘響逐行播出）；把 `by_design` 空格名單（第 1 天上午下午、第 32 天下午）抽到 `scripts/core/data_facts.gd` 常數，`verify_data.gd` 與走查腳本共用。
- **理由**：`開發設計方針.md > 全域結構決策` 的目標檔案樹寫的是「P1 結束時」的樣子，不是每個子階段的檢查表。`flow_text` 的需求在 P1-F；`data_facts` 的第二個讀者也是 P1-F 的走查腳本。現在抽出來是提早抽象——只有一個讀者的共用常數不是共用常數。
- **兌現紀錄**：commit `55d62a8`，**只兌現一半**。`scripts/core/data_facts.gd` 建好且真的有兩個讀者（`verify_data.gd` 與 `DataLoader`）——但**第三個預定讀者走查腳本沒有用它**（K-25）。`scenes/ui/flow_text.gd` 與 `flow_text.tscn` 建好且功能完整，**全專案零使用者**，殘響與結局文字仍寫在 `main.gd` 的 Label 上（K-28）。兩個缺口都另立條目追蹤，本條不重複。

---

## 附：本檔的驗證來源

- 2026-08-13～14：P1-A／B／C code review 與 headless 驗證。抓出的必修項（面板未走 `enter_beat`、beat 級 LOCKED 未傳導到槽）與契約缺口 B1–B4、序列化 JSON 往返，已於 commit `5d0df64`／`38601f9`／`9ce6cb9` 修復。
- 2026-08-14：對上述三個 commit 的實作 review，產出本檔 K-01～K-12。當時七套 headless 全綠（`verify_data`／`test_boot`／`test_game_state_p1a`／`test_hand_p1b`／`test_p1c`／`test_p1c_bugfix`／`test_p1d`），**全綠不代表沒有本檔的問題**——K-01 正是綠燈涵蓋不到的路徑。
- 2026-08-14：P1-E（commit `5c681f5`）驗證與 code review，產出 K-16～K-24，並讓 K-15 到期。八套 headless 全綠（上列七套 ＋ `test_p1e`），無迴歸；`測試指南.md > P1-E` 的七條驗收全數對得上測試。**這一輪最重的一條是 K-16，而它是資料問題不是引擎問題**——`choice_group` 在 P1-E 之前是死欄位，同面板規約沒有人會踩到；P1-E 讓它生效，三個行動格的規約缺口才變成有後果的東西。這是「引擎補完一個舊欄位時，要回頭驗那個欄位的資料規約」的第一個實例。
- 2026-08-14：P1-F（commit `55d62a8`）驗證與 code review，產出 K-25～K-37，並結案 K-14／K-16～K-19／K-22 與 B-02。十套 headless 全綠（上列八套 ＋ `test_p1f` ＋ `playthrough_greedy`）。**這一輪最重的兩條，綠燈都蓋不到**：走查腳本 37 個行動格靜靜跳過而沒有任何斷言（K-25）、晚間演出三份實作而走查那份不播殘響（K-26）。兩條都是 K-01 家族——**測試存在、測試全綠、測試沒有走那條路**。
- **本輪 review 自己犯的錯，記在這裡當教訓**：第一版把「沒東西花行動格」的行動格估成 24 格待決策，並宣稱 K-16 的名單漏了第 29 天下午。兩個都錯——逐格比對之後，其中 23 格整個時段只有 `fixed` beat，而「`fixed` 不吃行動格」是 `data/SCHEMA.md`、`實作規格書.md`、`溫泉小鎮企劃.md`、`subdocs/故事線/` 四份文件都寫過的既有規則，`d29_pm_invitation` 正是 `fixed`。**教訓：斷言「資料沒有涵蓋這件事」之前，要先 grep 故事線與企劃書，不能只看 `data/` 與根目錄四份文件。** 設計意圖經常只住在故事線裡。
- 2026-08-20：**P3-A～P3-F 三份文件與 `data/SCHEMA.md` 契約的規格審核**（HEAD `92383e6`，未跑測試，純文件與資料靜態核對），產出 K-73～K-81。規格本身對得上資料——28 個 night row／12 個可對位／10 個 distinct `day_counterpart`／16 個夜間限定／6 個阿宏門檻／14 個收費 row 全部與 `data/locations.json` 實測相符，lint 12 的 `chapter ↔ earliest_night` 一致性現行資料 0 筆違規，方針引用的既有 API 全部存在。K-73 抓到走滿既有 row 的玩家第二輪 marker 供給為 0；第一版寫成「所有玩家必定為 0」是全稱句過頭，使用者當場補正漏掉的 row 與尚未寫的第二輪內容。K-74 第一版又把 D15 後果寫成單一 14 → 13，漏了玩家可在 D14 主動付費與真實模擬按資料順序先選 `n_plaza`；討論後修正為路徑效率 13／最大壓力 14 雙基準。兩次都證明同一件事：**規格寫得完整，不代表規格算過；算過一次，也不代表策略分支已窮舉。**
- 2026-08-21：**P3 文件第三輪複審**（HEAD `e9bf8bf`，純文件核對，未跑測試），產出 K-98～K-102 共 5 條。**五條全部是「規格寫完了，但驗收證不出來」這一類**，不是規格本身寫錯：八碼拒絕集合定了卻只驗兩碼（K-98）、對位入口的碼集合根本沒定（K-99）、baseline checkpoint 沒有產生方式所以那條驗收會恆真（K-100）。另兩條是舊帳——角色分工在 P3-F 被違反（K-101）、K-93 修一處剩兩處（K-102，同一個「總數只改到一部分」模式的第三次）。**教訓：規格審核除了問「這條寫得對不對」，還要問「照這條寫出來的測試，寫錯了會不會紅」。** K-98 與 K-100 都是寫錯不會紅的那一種。
- 2026-08-21：**P3 文件第二輪複審**（工作區為 K-73～K-81 回寫後的未提交狀態），產出 K-82～K-97 共 16 條。其中 K-91／K-92 是本輪自己抓到、口頭回報後**忘了當場落檔**，由使用者提醒才補上——`本檔範圍與邊界` 那條「review 講出口的每一條都要當場進本檔」第二次被自己違反。另 **K-84 是本輪的誤判**：複審時 grep `lint 13` 回報「八處引用全部一致」，但 `實作規格書.md` 的 lint 總表用的是行首編號 `13. `，字串不匹配所以沒抓到，實際仍是舊編號。**教訓：驗「編號有沒有全部改到」不能只 grep 一種書寫形式。**
- 2026-08-21：**P3-A 修復後複驗**（HEAD `41d2d8e`），K-103～K-109 七條裡 **5 條通過、2 條未過**（K-105 半過、K-108 未過），另抓出 K-110、K-111。**三條未結案的共同形狀完全一樣：修正做了、測試沒跟上**——K-108 的 lint 補了沒有 fixture、K-110 的四段判斷一段都沒反證、K-105 的參數加了沒有呼叫端。**教訓：驗一條「已修」不能只看程式改了什麼，要問「把這次的修改整段刪掉，測試會不會紅」。** 這三條的答案當時都是不會。同日第二批補完後複驗全過：負向 fixture 6 → 11 個，`test_p3a` 12 → 17 項，17 套 headless 全部 exit 0，UI 全套 82 變體／63 契約／0 failed／11 個負向如預期失敗。
- 2026-08-21：**對 K-105／K-108／K-110 做變異測試複驗**（把修正整段拿掉，看測試會不會紅），前兩條通過、K-110 只過四分之三。K-105 用 probe 證明 `madness_cap` 隨 `tuning.json` 變動（5→5、7→7）且 `data_node=true`，參數確實是活的。K-108 拿掉兩條檢查後 fixture 7 的兩條斷言各自指名自己的 row 紅掉。**K-110 的「型別錯誤」那一項是假的**——fixture 用 `"chapter": "one"`，`int("one") == 0` 會被範圍檢查代勞，型別與整數性兩段刪光測試照樣 exit 0（K-113）。改成 `"chapter": "2"` 與 `earliest_night: 15.5` 之後兩道防線才各自被守住。**教訓：負向 fixture 要挑「只踩得到那一道防線」的值**；隨手寫一個明顯壞掉的值，通常會先撞到別條檢查，於是你以為在驗 A、其實驗的是 B。順帶撞到 K-114（`p2d_near_cap` 釘死 `madness_cap`）。
- 2026-08-21：**K-100 的結論由 verifier 獨立證明三次**（`ce7c41a`、`41d2d8e`、第二批修正後各一次）：把 worktree 開在 `870db64`（P3-A 之前的資料）＋當下的 generator 重跑，產出的 `p3a_night_baseline.json` 與 `.expected.json` 與現檔逐字相同。**做這件事的過程中抓到 K-112**——baseline 產生掛在 `generate_all_states()` 裡，跑一次 UI 模擬就重寫一次（實測 baseline mtime 22:20:27 ＝ 本次 UI 模擬的執行時刻）。**所以 K-107 不是紀律鬆懈，是工具會自己抹掉證據**；那條驗收在 K-112 修好之前沒有辦法靠人守，已降為 B-04。
- 2026-08-22：**P3-B 實作後 review**（HEAD `2ef58f3`），產出 8 條並同日由實作者全數修復（`7c5184b`）。**8 條共同形狀與 P3-A 的 K-105／K-108／K-110 一樣——「修正做了、測試沒跟上」，把那段實作整段刪掉測試照樣全綠**：lint 13 的槽級負向 fixture 缺席（K-87 的修正等於白修）、跨輪重進不收費沒測（「終身首次收費」是 P3-B 的主規則）、`night_seen` 引用檢查沒有負向 fixture、`main.gd` 真入口的 BE 路徑沒驗、`NIGHT_REJECT_MESSAGES` 是死常數（→ K-115）、`if day != 1 or phase == "night"` 是「本輪沒結束」的隱式代理、舊 API 名稱殘留在檔頭註解與 timeline 字串、阿宏 gate 沒有結構性斷言。修復後 `test_p3b` 由 9 組增為 12 組。
- 2026-08-22：**對 `7c5184b` 的 8 條修正逐條做變異測試複驗**（在 scratchpad 另開 detached worktree，跑完即移除，主工作目錄全程未動）。七種變異**全部如預期轉紅**：① `_scan_legacy_flags()` 不再遞迴進 Array → `slots[].requires`／`on_place`／`condition` **三筆各自分開紅**，location 與 beat 那兩筆仍綠（K-87 要的「槽級獨立驗」確實兌現）；② 收費集合改回 run 層、`end_run()` 清掉但 meta seen 保留 → **只有跨輪重進那條紅**，meta 保留那條仍綠（證明新斷言不是舊斷言的重複）；③ 刪 `data_loader.gd` 的 `night_seen` 引用檢查分支 → 未知 id 與非夜間 layer 兩條各自紅；④ 拿掉 `main.gd` 的 `_is_showing_ending` guard → `panel_vis=true` 紅；⑤ `if not will_end_run:` → `if true:` 紅；⑥ `will_end_run` 改在寫 seen 之後才算 → 紅（`would_night_entry_end_run()` 對已 seen 一律回 false，順序這件事本身被測住）；⑦ `n_ahong_2` 的 gate 改回 run flag → 紅。刪除類兩條（死常數、舊 API 名稱）以全域 grep 驗，各 0 筆。另核對 `would_night_entry_end_run()`（進場前張數＋cost）與 `_check_madness_cap()`（發完卡後張數）等價——`_check_hand_overflow()` 只 `push_warning` 不丟卡，所以第 ⑤⑥ 條的改寫沒有改行為。
- 2026-08-21：**P3-A 實作後 review**（HEAD `ce7c41a`），產出 K-103～K-109 共 7 條並同日修復（複驗結果見上一則）：
  - K-103（測試死變數與寫死名稱改為動態掃描 28 名稱）、K-104（測試日誌宣稱過度修訂並落檔人工審查表）、K-105（後置條件讀 `Data.tuning`）、K-106（baseline 指定 `n_ahong_1`）、K-107（baseline 產生紀律）、K-108（Lint 12 teaser 雙欄必填與一致性）、K-109（阿宏 reject_reason 語意區分與測試語意斷言）。
  - **28 個夜間地點名稱與 primary beat 人工 code review 對照表記錄**：

| id | 夜間名稱（locations.json） | 對應 primary beat / 概念 | 審查結論 |
|---|---|---|---|
| `n_corridor` | 很長的走廊 | `n_corridor_ch1`（靜和園內部後棟走廊） | ✅ 符合進場前視覺引子名 |
| `n_exit` | 石階外的鎮 | `n_exit_ch1`（山泉閣石階外、空無一人的鎮） | ✅ 符合進場前視覺引子名 |
| `n_queue` | 很多人排隊的地方 | `n_queue_ch1`（診所前排隊名單） | ✅ 符合進場前視覺引子名 |
| `n_litwindow` | 亮著的窗 | `n_litwindow_ch1`（等候未歸人） | ✅ 符合進場前視覺引子名 |
| `n_watersound` | 有水聲的地方 | `n_watersound_ch1`（河邊乾涸水聲從地底來） | ✅ 符合進場前視覺引子名 |
| `n_ahong_1` | 有人走過的路 | `n_ahong_1`（工作鞋腳印、常用小物件） | ✅ 符合進場前視覺引子名 |
| `n_woodtags` | 數木牌的屋子 | `n_woodtags_ch1`（廟裡數木牌） | ✅ 符合進場前視覺引子名 |
| `n_source` | 上坡／源頭 | `n_source_ch1`（上游黑水熱氣、看見自己） | ✅ 符合進場前視覺引子名 |
| `n_manydoors` | 很多門的巷子 | `n_manydoors_ch1`（遭遇「你叫什麼名字」） | ✅ 符合進場前視覺引子名 |
| `n_music` | 有音樂的地方 | `n_music_ch1`（廟埕儀式音樂與唱名） | ✅ 符合進場前視覺引子名 |
| `n_lockedhouse` | 上鎖的屋子 | `n_lockedhouse_ch1`（老家房間門開著但大門鎖著） | ✅ 符合進場前視覺引子名 |
| `n_ahong_2` | 有血跡的地方 | `n_ahong_2`（血跡是新割傷） | ✅ 已由「血還是新的」更名為引子名 |
| `n_nodoor` | 沒有門的建築 | `n_nodoor_ch1`（靜和園正棟） | ✅ 符合進場前視覺引子名 |
| `n_landmark` | 地標 | `n_landmark_ch1`（長歪的樹／斷石碑／轉角） | ✅ 符合進場前視覺引子名 |
| `n_plaza` | 中心廣場 | `n15_everyone`（廣場攤簿子、看見叔叔） | ✅ 符合進場前視覺引子名 |
| `n_ahong_3` | 有人叫他的名字 | `n_ahong_3`（夜裡叫出主角名字） | ✅ 符合進場前視覺引子名 |
| `n_litcorridor` | 有燈的走廊 | `n_litcorridor_ch2`（診所後棟夜版） | ✅ 符合進場前視覺引子名 |
| `n_higher` | 再往上 | `n_higher_ch2`（源頭更上方水流起點） | ✅ 符合進場前視覺引子名 |
| `n_ahong_4` | 聲音停下來的地方 | `n_ahong_4`（搬貨聲音停下、第一對位點） | ✅ 符合進場前視覺引子名 |
| `n_emptyspot` | 空的位子 | `n_emptyspot_ch2`（阿財原本站的位置） | ✅ 符合進場前視覺引子名 |
| `n_gathering` | 集合的隊伍 | `n_gathering_ch2`／`_ch3`（遊行隊伍夜間集合） | ✅ 符合進場前視覺引子名 |
| `n_ahong_5` | 被留下的東西 | `n_ahong_5`（第二對位點物件） | ✅ 符合進場前視覺引子名 |
| `n_steam_below` | 有蒸氣的樓下 | `n_steam_below_ch3`（靜和園後棟地下溫泉池） | ✅ 符合進場前視覺引子名 |
| `n_ahong_6` | 新的腳印 | `n_ahong_6`（第三對位點新鮮腳印） | ✅ 符合進場前視覺引子名 |
| `n_emptyspot_2` | 又一個空的位子 | `n_emptyspot_2_ch3`（阿財之後的另一空位） | ✅ 符合進場前視覺引子名 |
| `n_behind_temple` | 廟的後面 | `n_behind_temple_ch3`（燒金爐、冷火） | ✅ 符合進場前視覺引子名 |
| `n_ahong_7` | 三個點指向的地方 | `n_ahong_7`（三個對位點指向空白處） | ✅ 符合進場前視覺引子名 |
| `n_corridor_end` | 走廊的盡頭 | teaser 標記（第 45 夜不存在） | ✅ 符合進場前視覺與 teaser 語意 |
