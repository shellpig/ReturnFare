# 開發設計方針

> 實作層文件：檔案結構、Autoload 簽名、signal、接線規則、實作決策。
> **角色歸屬**：implementer-owned（AGENTS.md 實作守則第 4 條）。驗收意圖住 `實作規格書.md`，測試流程住 `測試指南.md`，已知問題住 `驗證後已知問題.md`（第一波驗證收尾時才建）。

## 本檔範圍與邊界

| 寫什麼 | 去哪 |
|---|---|
| 引擎必須做什麼、各 Phase 驗收意圖 | `實作規格書.md` |
| 規則的形狀與為什麼 | `溫泉小鎮企劃.md` |
| 資料欄位 | `data/SCHEMA.md`；可調數值 `data/tuning.json` |
| **檔案清單、autoload 簽名、signal、接線、實作決策** | **本檔** |
| 操作層驗收步驟 | `測試指南.md` |

## 起始範圍

- 本檔自 **Phase 1** 起寫。資料層時期（`git log` 裡 Phase 1 之前的全部 commit）的細節不 backfill，以 `data/SCHEMA.md` 與 git log 為準。
- 現有程式資產：`scripts/data_loader.gd`（`class_name DataLoader`，載入＋引用檢查，**沿用不改寫**）、`scripts/verify_data.gd`（headless 資料驗證，會隨 lint 需求擴充）。
  - **「沿用不改寫」的範圍是 P1 引擎期**：資料層自己新增檔案時，DataLoader 要跟著長出對應的載入與引用檢查，否則新資料第一天就沒人守。已發生一次：2026-08-13 待決 20 新增 `data/npcs.json`，DataLoader 加了 `npcs` 索引與兩條檢查（NPC 的 `at[].location` 存在、`at[].phases` ⊆ 該地點 `phases`），並補了夜間地點 `day_counterpart` 的引用檢查。**P1 的既有 API 一個都沒動**，`beats_at()` 與 `verify_references()` 的簽名不變。
- 完成的 Phase 段落照歸檔紀律搬 `subdocs/歸檔/開發設計方針_歸檔.md`，原位留同標題 stub。

## Phase 分期總表

> 狀態住 `PROJECT_BRIEF.md > Phase 進度`，本表只管內容。展開細節於各 Phase 排到時才寫（規格書同步）。

**i18n 為什麼排在 P5 之後、P6 之前**：卡它的是**資料 schema 定案**，不是引擎完工。schema 最後一次長新玩法欄位在 P4（委託與遭遇形狀），P5 補完結局與開局，之後就不再動玩家可見欄位。放在 P6 之前是因為 P6 的 version 遷移會定存檔格式——先做 i18n，存檔格式一次定稿，不必為了 i18n 再遷移一次。

**它不併進 P5**：P5 是玩法（三結局、開局選單、繼承），i18n 是基礎建設，兩者的驗收方式完全不同。合併只會讓 P5 的驗收清單同時混兩種東西。

**在那之前只守一條紀律**：玩家可見字串不寫在 `.gd` 行內，一律進各檔頂部 const（見下方「全域結構決策」）。`beat` 與 `slot` 的 `id` 在 i18n 管線建好之前可以自由改名，之後凍結。

| Phase | 一句話 |
|---|---|
| **1 最小可玩迴圈** | 選地點 → 放卡 → 推進，全文字，45 天走到底（本檔下方，可動工） |
| 2 發狂時鐘與縱慾 | 各走各的鐘、六個縱慾出口、強制縱慾演算法、視野與發瘋 BE；驗收＝headless 重演 `發狂卡機制模擬.md` 三種玩家（本檔下方，可動工）。**備用區與超載不在 P2**——容量未定（待決 34），而且第一輪碰不到 cap，做了也壓力測不到 |
| 3 夜間層真值化 | 終身首次收費、跨輪到訪、唯一夜間 resolver、狀態化文字清單、白天免費對位；拆 P3-A～P3-F（本檔下方，可動工） |
| 4 委託與遭遇 | 人物每日一次受託、立即／隔日上午回報、遭遇回合循環、第 8 夜與第 45 天真值化；拆 P4-A～P4-F（本檔下方） |
| 5 結局、開局與迴圈 | 三結局真流程、開局十分鐘選單、繼承完整版 |
| 6 存檔 UI | 槽位、version 與遷移、輪中存讀 |
| i18n 管線 | 抽取工具、可翻譯欄位規約、`beat`／`slot` id 凍結、引擎改走翻譯查詢、程式字串進 CSV。**銜接工作，不是 Phase**——排在 P5 之後、P6 之前 |
| 內容期 | 文本、演出、UI 正式化、實際翻譯、第二輪差異包——**引擎期玩過之後才排** |

---

## Phase 1 — 最小可玩迴圈（實作契約）

> 驗收意圖見 `實作規格書.md > Phase 1`；操作清單見 `測試指南.md > Phase 1`。
> 每個子階段完成後，遊戲都必須保持可跑（`main.tscn` 起得來、headless 驗證綠）。

### 全域結構決策（P1 期間建立，之後 Phase 沿用）

目標檔案樹（P1 結束時）：

```text
scripts/
├── autoload/
│   ├── data.gd          # Autoload「Data」：啟動時跑 DataLoader，失敗擋進遊戲
│   └── game_state.gd    # Autoload「GameState」：全部 runtime 狀態（規格書第一節窮舉）
├── core/                # 純邏輯，零 UI 依賴，headless 直接測
│   ├── condition_eval.gd   # class_name ConditionEval（static）
│   ├── effect_apply.gd     # class_name EffectApply（static）
│   ├── panel_builder.gd    # class_name PanelBuilder（static）
│   └── data_facts.gd       # class_name DataFacts：資料層既定事實常數（by_design 空格名單等）
├── data_loader.gd       # 既有，不動
└── verify_data.gd       # 既有，隨 lint 擴充
scenes/
├── main.tscn / main.gd  # 宿主：按 GameState 時段切換顯示流
└── ui/                  # 全文字元件（正式 UI 會整層換掉，見規格書架構基線第 2 條）
    ├── hand_bar.tscn        # 手牌一排＋知識清單
    ├── map_list.tscn        # 地點清單（文字按鈕）
    ├── location_panel.tscn  # 面板：beats 與槽
    └── flow_text.tscn       # evening／夜間 stub／結局的逐行文字流
tests/
├── headless/
│   └── playthrough_greedy.gd   # P1-F 自動走查（SceneTree script）
└── fixtures/broken/            # 壞資料 fixture（破壞性測試不碰正式 data/）
```

- **Autoload 只有兩個**：`Data`（唯讀資料）與 `GameState`（可變狀態）。UI 一律讀這兩個、發意圖呼叫 GameState 方法；**UI 之間不互相引用、不持有狀態**。
- **規則層入口統一，UI 零規則**：合法性判斷與結算只存在於 `GameState.try_place()` / `choose()` / `enter_beat()` / `advance_phase()` 與 `PanelBuilder.build()` / `available_locations()`。UI 與 headless 走查呼叫**同一組 API**；UI 內出現任何 accepts／三態／action_spent 判斷＝架構錯誤。
- `DataLoader` 的路徑常數改為可選建構參數（預設 `res://data/`），供壞資料 fixture 測試載入 `tests/fixtures/broken/`——這是「沿用不改寫」的唯一豁免。
- `core/` 三個 class 全部 static、無狀態、不 preload 任何場景——這是「headless 可驗證」（規格書架構基線第 5 條）的落實位置。
- 玩家可見字串不寫在 `.gd` 裡；引擎自己的提示（stub 文字、錯誤）先集中在各檔頂部 const，方便日後抽 i18n。
- **autoload 不是 Engine singleton。** 它是掛在 `/root` 底下的節點；`Engine.get_singleton()` 只認引擎自己的 singleton 與 `Engine.register_singleton()` 註冊過的東西。跨 autoload 取用一律走 `get_node_or_null("/root/<Name>")`，或直接用 GDScript 全域識別字。要保留 fixture 注入接縫就照 `data.gd > _find_game_state()`：**先 `Engine.has_singleton()` 問過再 `get_singleton()`**（不問的話找不到時它自己會 push_error），沒有才退回 `/root`。
- **測試的組裝方式與正式啟動不同，所以兩條都要驗。** `test_p1c` 那類測試自己 `new()` 出 GameState／Data 再 `register_singleton()`，走的是注入路徑；正式遊戲走 `project.godot` 的 autoload 路徑。`tests/headless/test_boot.gd` 是後者的唯一守門員——它**刻意不註冊任何 singleton**，驗 autoload 掛得上、`Data.ok` 為真、`main.tscn` 進得到正常分支。在裡面手動註冊 singleton 會讓這一檔失去意義。
- Headless 執行方式與 sandbox 限制照 `AGENTS.md > Godot Headless 驗證`（escalated 權限、先 `--import`）。

### P1-A 遊戲狀態與時段狀態機

動的檔：`scripts/autoload/data.gd`（新建）、`scripts/autoload/game_state.gd`（新建）、`project.godot`（註冊兩個 autoload）、`scenes/main.tscn/gd`（顯示當前天／時段／章節的最小殼）。

`Data`：

```gdscript
var loader: DataLoader     # _ready 時 load_all() + verify_references()，任一失敗 → ok = false
var ok: bool
func tuning(key: String, fallback: Variant = null) -> Variant   # 讀 tuning.json，點路徑（"indulgence.soak_phase_cost"）
```

`GameState`（本子階段只做時間群與序列化骨架，其餘群先立空欄位）：

```gdscript
# 章節界線與時段名都是結構常數（規格書第二節），集中在這裡
const CHAPTER_START_DAYS := [1, 16, 33]
const LAST_DAY := 45
const PHASES := ["morning", "afternoon", "evening", "night"]
const ACTION_PHASES := ["morning", "afternoon"]   # tuning.phases_per_day 只做一致性驗證（Data 啟動 lint）

var day: int               # 1..45
var phase: String          # "morning" | "afternoon" | "evening" | "night"
func chapter() -> int      # 由 day 導出，不儲存

signal phase_changed(day: int, phase: String)
signal day_changed(day: int)
signal chapter_changed(chapter: int)
signal run_ended(ending_id: String)

func advance_phase() -> void    # morning→afternoon→evening→night→次日 morning；
                                # 第 45 天：afternoon → evening（結局 coda，播 d45_then）→ emit run_ended，不進 night
                                # （P1-A 到此為止；P1-F 起這條路徑改為呼叫 end_run()，見 P1-F 段）
func serialize() -> Dictionary      # { "run": {...}, "meta": {...} } 兩層分開（規格書第十六節）
func deserialize(d: Dictionary) -> void
```

- 行動格的「已放主角卡」旗標（`action_spent: bool`）本子階段就進 run 層，P1-D 才有人寫它。
- `main.gd` 只做：檢查 `Data.ok`（失敗顯示錯誤停住）→ 顯示狀態列 → 一顆「推進」鈕呼叫 `advance_phase()`。

### P1-B 卡片與手牌

動的檔：`game_state.gd`（手牌群）、`scenes/ui/hand_bar.tscn/gd`（新建）、`main.tscn`（掛 hand_bar）。

`GameState` 增加：

```gdscript
var hand: Array[String]              # 佔格卡 id，有序；主角卡恆在 index 0
var knowledge: Dictionary            # id -> true（Set；knowledge 卡唯一的家，屬 meta 層——serialize 進 "meta"，end_run 不清）
var madness_clock: Dictionary        # 發狂卡實例 id -> 剩餘天數（P1 只建結構，P2 才走錶）

signal hand_changed
signal knowledge_changed

func gain_card(id: String) -> void   # 按 Data 卡型別分流：slotless → knowledge；其餘 → hand。
                                     # 除 madness 外全卡 unique：已持有＝no-op（冪等，規格書第三節）
func lose_card(id: String) -> void   # 可作用於 hand 與 knowledge（規格書第七節）；
                                     # lose 主角卡 → push_error（資料錯誤，不是玩法）
func has_card(id: String) -> bool    # hand ∪ knowledge（規格書第十四節：備用區不算）
func has_knowledge(id: String) -> bool
func hand_slots_used() -> int
```

- 型別操作矩陣（規格書第三節）P1 只落實兩行：protagonist 釘死（`lose_card` 擋）、knowledge 不進 hand。其餘矩陣行為到各自 Phase 才有入口，不預寫。
- 發狂卡是同一張卡的多實例：`hand` 裡存實例 id（`"madness#1"`），卡面查 `Data` 時剝掉 `#` 後綴。實例編號計數器進 run 層。
- `hand_bar` 訂閱兩個 signal 重繪；顯示佔格數／上限（`Data.tuning("hand_size")`）。

### P1-C 地圖、面板與三態

動的檔：`scripts/core/condition_eval.gd`（新建）、`scripts/core/panel_builder.gd`（新建）、`scenes/ui/map_list.tscn/gd`、`scenes/ui/location_panel.tscn/gd`（新建）、`game_state.gd`（事件群：`beats_entered`）、`main.gd`（行動時段顯示地圖）。

```gdscript
# ConditionEval —— 規格書第十四節的十個運算子，封閉語彙
# （註：gs/data 標為 Node 而非 GameState/Data，因為 Autoload 在 GDScript 內是值不是型別，且腳本不能與 Autoload 同名 class_name）
static func eval(cond: Variant, gs: Node) -> bool   # null / 缺欄 = true；未知鍵 push_error + return false

# PanelBuilder —— 純函式：算出一個地點面板的 view model
static func build(location_id: String, gs: Node, data: Node) -> Dictionary
# 回傳：{ "beats": [ { "beat": Dictionary, "tri": TriState, "reason": String,
#                      "slots": [ { "slot": Dictionary, "tri": TriState, "reason": String } ] } ] }
# TriState：HIDDEN / LOCKED / OPEN / RESOLVED（規格書第五節求值順序；含槽級 condition、
#           一次性槽收起、choice_group 收起與被選項 RESOLVED、evening 主角卡槽強制 LOCKED）
static func available_locations(gs: Node, data: Node) -> Array[String]
# 當前天／時段可去的地點 id（layer / chapter / phases 過濾）。map_list 與走查腳本共用
```

```gdscript
# GameState —— beat 呈現的唯一入口（規格書第四節）
func enter_beat(beat_id: String) -> PackedStringArray
# 一次性判定（beats_entered）→ on_enter 結算 → 回傳要播的文字行。
# 面板開啟、evening 演出、夜間層、結局 coda 全部走這條
```

- `beats_at(day, phase)` 沿用 DataLoader 既有方法；三態求值進 PanelBuilder，**不改 DataLoader**（fixture 路徑參數除外，見全域結構決策）。
- `map_list` 只渲染 `available_locations()` 的結果，自己不過濾。
- P1-C 期間 `enter_beat` 內部先用最小效果結算（只支援 `text` 與 `gain`），P1-D 換成正式 `EffectApply`——這是唯一允許的暫時簡化，P1-D 收掉。
- evening 流程：`flow_text` 逐條對當日 `when.phase == "evening"` 的 fixed beat 呼叫 `GameState.enter_beat()` 播回傳文字（跨地點全部，規格書第四節例外條）——**第 1 天到站發主角卡走的就是這條**。
- 面板內任何狀態變化 → 重呼叫 `PanelBuilder.build` 整包重繪（P1 資料量下全量重算即可，不做增量）。

**2026-08-13 bugfix（code review 於 P1-D 完成後才發現，補回 P1-C 的兩個契約缺口）：**
- **A1**：`location_panel.gd._rebuild()` 原本直接讀 `beat["text"]`，OPEN beat 從沒走過 `enter_beat()`——白天／固定面板全程沒結算 `on_enter`、`beats_entered` 也沒寫入（只有 evening 演出流走了這條入口）。修法：OPEN 才呼叫 `GameState.enter_beat(beat_id)` 取得要播的行，LOCKED 只顯示原始 `beat.text`（不結算 `on_enter`——呈灰卡不算「呈現給玩家」，效果先發等於白送，這條也順帶回答了規格書留的那個未決點）。
- **A2**：`PanelBuilder.build()` 原本 beat 級 `tri` 與槽級 `tri` 各自獨立求值，beat LOCKED 時槽照樣能算出 OPEN。修法：slots_result 算完後，若 `beat_tri == LOCKED`，全部槽的 `tri`/`reason` 降級為 LOCKED／beat 的 reason。**這只補了 view model；`GameState.try_place()` 本身完全沒查 beat 級 `condition`/`requires`，是獨立於 A2 的第三個洞**——UI 是零規則顯示層，view model 修好不代表規則層擋得住繞過 UI 的呼叫（例如 headless 走查或未來別的呼叫端）。已在 `try_place()` 的②三態檢查最前面補上 beat 級 `condition`（HIDDEN）與 `requires`（LOCKED，理由取 beat 的 `reject_reason`）。
- 回歸測試：`tests/headless/test_p1c_bugfix.gd`，拿真實資料驗（A1 用 `d1_arrival`／`busstop`；A2 用 `d19_pm_upstream`／`upstream`，驗 view model 與 `try_place` 兩層都擋得住，補卡後兩層都放行）。

### P1-D 放置與效果結算（已實作，2026-08-13）

動的檔：`scripts/core/effect_apply.gd`（新建，收掉 P1-C 內聯版）、`location_panel.gd`（放卡流程）、`game_state.gd`（旗標／開關／關係群＋`consume_action()`／`try_place()`）、`data.gd`（啟動時語彙 lint）、`data_loader.gd`（`relation_scale` 載入＋`lint_vocabulary()`／`lint_missing_reject_reason()` 靜態函式，供 `data.gd` 呼叫也供測試直接餵合成 beat）、`condition_eval.gd`（補 `relation_at_least`＋`KNOWN_KEYS` 常數）。比寫這段時多動了 `data_loader.gd` 與 `condition_eval.gd`——lint 1 需要一份跨檔可查的封閉鍵表，放在 `ConditionEval.KNOWN_KEYS` / `EffectApply.KNOWN_KEYS` 上比塞進 `data.gd` 好測；`relation_at_least` 委派給 `GameState.relation_at_least()`（讀 `Data.loader.relation_scale`），跟其餘運算子委派 `gs.call()` 的寫法一致。

```gdscript
# EffectApply —— 規格書第十四節效果鍵，固定順序 text → lose → gain → 其餘
static func apply(effect: Variant, gs: Node) -> PackedStringArray  # 回傳 UI 要播的文字行
const CARD_ENTRY_KEYS := ["card", "if"]  # gain / lose 的物件形態元素（見下方「帶條件的卡片項目」）

# GameState 增加
var flags: Dictionary          # name -> true
var switches: Dictionary       # id -> true
var switch_progress: Dictionary# id -> int
var relations: Dictionary      # npc -> int（單軸整數暫行案，規格書第十二節）
var action_spent: bool
var slots_placed: Dictionary       # "beat_id::slot_id" -> true（槽預設一次性，規格書第六節）
var npc_action_counts: Dictionary  # npc_id -> 本輪投入的主角行動數（規格書第十二節；run 層、入序列化）
func consume_action() -> void  # 標記當前行動格已用；advance 時檢查
func set_flag(name: String) / open_switch(id: String) / add_switch_progress(id: String, n: int)
func add_relation(npc: String, delta: int)

# 放卡的唯一入口（UI 與 headless 走查共用；UI 內不做任何判斷）
func try_place(card_id: String, beat_id: String, slot_id: String) -> Dictionary
# 回傳 { "ok": bool, "reason_code": String, "reason_text": String, "lines": PackedStringArray }
```

`try_place` 內部順序（全部在規則層，效果結算前檢查完；規格書第六節的四步）：

1. **持有**：`card_id` 此刻必須在 `hand` 或 `knowledge`（發狂卡實例剝 `#` 後綴再查卡表）。
2. 槽 tri 必須 OPEN（含一次性未放過）且卡符合 `accepts`（型別名或卡 id）；主角卡**且當前為行動格**時再檢查 `action_spent`（night 放主角卡合法且不耗格，規格書第六節）。
3. 任一不過 → `ok=false` ＋一致理由，**GameState 零變化**。全過 → `EffectApply.apply(slot.on_place)` → `slots_placed["beat_id::slot_id"] = true`。
4. 卡是 `protagonist` 且在行動格 → `consume_action()`；槽有 `attention_npc` → `npc_action_counts[npc] += 1`（只在這一步加，失敗與免費槽不計）。UI 收到 `ok` 後重建面板（PanelBuilder 全量重算）。

- `relation_at_least` 求值：讀 `data/relation_scale.json`（新建：狀態名 → 最低整數值的序數表，屬拍板過的暫行案）。**序數本身是實作時暫填的占位**——目前資料只用到「疑似」（1）與「恩人」（3），其餘五個狀態（陌生 0／債主 -1／死敵 -2／同謀 2／戀人 4）先擺在看起來合理的位置，沒有額外設計依據。企劃書第五節的兩軸狀態機本來就留到文本階段，這張表到時候要整個重看，不是現在就要拍板。
- **帶條件的卡片項目（`gain` / `lose` 的物件形態）**：陣列元素除了卡 id 字串，也可以是 `{ "card": "<id>", "if": <condition> }`，`if` 走既有的 `ConditionEval` 語彙、**不另立第二套條件詞**；缺 `if` 等於恆成立（同 `ConditionEval.eval(null)` 契約）。實作是 `EffectApply._entry_card_id()` ／ `_entry_passes()` 兩個 static helper，`gain` 與 `lose` 共用。
  - **守衛下在單張卡上，不是整個效果塊。** 同一個 `on_enter` 裡的 `text` / `flag` / `switch` 照跑，只有那一張卡被跳過。**這一點不能改成整塊求值**——升級型比對那一格通常還要寫旗標（`d45_then` 的 `loop1_end` 就是），整塊擋掉會連坐。
  - 存在的理由是**跨輪**：`knowledge` 住 meta 層不隨輪重置，而 `beats_entered` 每輪清空，所以第二輪會重演同一個 beat 並再結算一次 `on_enter`。一般卡沒差（`gain_card` 冪等、佔格卡 unique），**升級型比對會出事**——舊版被 `lose` 掉之後第二輪又發回來，玩家同時持有升級前後兩張。守衛讓升級保持單向。
  - 連帶要改兩處 lint：`DataLoader._check_card_refs()` 的 `gain`/`lose` 分支要認得物件形態（否則把 Dictionary 當卡 id 查，誤報「引用不存在的卡」），並遞迴進 `if` 讓裡面的 `has_knowledge` 也吃到引用檢查；`_lint_card_entries()` 驗元素形態（未知鍵、缺 `card`、`if` 內的運算子）。回歸測試在 `test_p1d.gd`（守衛兩個分支＋三種壞形狀）與 `test_p1f.gd`（第二輪重播 `d45_then` 不重發舊版、`flag` 不被連坐）。
- 語彙封閉性 lint（規格書第十七節 lint 1）做在 `Data` 啟動：走一遍全部 beats 的 `condition` / `requires` / `on_place` / `on_enter` / `echo.condition` 鍵，未知鍵 → `ok = false`。同時做 lint 2（有 `requires` 無 `reject_reason` 報警告，不擋開機）。兩個 lint 函式是 `DataLoader` 的 static func（吃 `beats: Array[Dictionary]`），`data.gd._ready()` 呼叫；測試可以不經過檔案系統，直接餵合成 beat 陣列進去驗未知鍵抓不抓得到。

### P1-E choice_group

動的檔：`panel_builder.gd`（組的呈現規則）、`location_panel.gd`（選定流程）、`game_state.gd`（`choices: Dictionary`）。

- PanelBuilder：同組槽全部照常渲染；`gs.choices` 已有該組 → **整組只渲染被選的那個，狀態為 RESOLVED（唯讀展示，不可再互動）**。choice 的顯示規則**優先於**一次性槽的收起規則——被選槽即使在 `slots_placed` 裡也要以 RESOLVED 現身，不是消失。
- 選定的唯一入口：`GameState.choose(beat_id, group_id, slot_id, card_id := "")`，**原子操作**，兩條路共用同一套驗證與結算 helper：①驗證該組未結算（已結算＝no-op，不重複套效果）②驗證槽的 condition／requires ③帶卡時走 `try_place` 的持有＋`accepts` 檢查 ④結算該槽 `on_place`（兩條路同一個，結果相同）⑤一致寫入 `choices["beat_id::group_id"] = slot_id` **與** `slots_placed`——兩條路留下的狀態除卡片消耗外完全相同。**寫入 `choices` 即定案，無反悔路徑**（已拍板）。
- 組內 `accepts` 只約束「帶卡選」那條路，不擋「直接選」。
- 不選＝不寫 `choices`，離開面板無任何結算。
- **回傳形狀與 `try_place` 同一份**（K-15 已把兩個 API 的舊 `reason` 鍵一起拿掉）：

```gdscript
# 選擇題選定的唯一入口（UI 與 headless 走查共用）
func choose(beat_id: String, group_id: String, slot_id: String, card_id: String = "") -> Dictionary
# 回傳 { "ok": bool, "reason_code": String, "reason_text": String, "lines": PackedStringArray }
# 重複選擇同一組 → { ok = false, reason_code = "resolved" }，效果不重複套用
```

- 帶 `choice_group` 的槽走 `try_place()` 時，`try_place` 直接轉導 `choose()`——規則層只有一個結算入口，兩條路不可能分岔。

### P1-F 45 天全程走通與迴圈 stub

動的檔：`main.gd`（evening／night／結局流程接線）、`flow_text.gd`（殘響行）、`game_state.gd`（`end_run()` 與 run 層重置）、`scripts/verify_data.gd`（lint 3、5 補齊）、`tests/headless/playthrough_greedy.gd`（新建）。

- evening 流程順序（規格書第十一節）：**先**依資料陣列順序逐個 `enter_beat()` 當日 evening 的 fixed beat（同檔按 `beats[]` 順序，跨檔按 `data/beats/` 檔名字典序——`Data` 載入時就以這個順序建表，`main.gd` 不另外排），**再**播殘響。後面的 beat 因此讀得到前面 `on_enter` 寫的 flag（第 27 天 `d27_evening_awei_confirm` → `d27_evening_cover_knowledge`）。
- 殘響：evening 流程尾端，掃全部 beats 中 `echo.day == day` 且 `ConditionEval.eval(echo.condition)` 為真者，逐行播 `echo.text`。
- 夜間 stub：night 時段顯示夜間地圖（`earliest_night` ≤ day），`madness_cost > 0` 的地點一律 LOCKED、理由給 stub 文字；免費地點可進。**夜間解析（規格書第九節四步）P1 就做真的，P3 直接沿用**：入夜 fixed 定日 beat 強制播（走 `enter_beat`，不收費不解鎖）→ 選地點後主內容（定日優先於章節變體）＋附加 beat 並列 → **「睡」解析旅館（`sanquan`）的當夜定日 beat**。不開標記、不給發狂卡。
- 結局 coda：第 45 天 evening 播 `d45_then`（走 `enter_beat`，其免費比對槽經 `try_place` 可用）。玩家再推進時，`advance_phase()` **自己呼叫 `GameState.end_run()`**，不把收尾丟回給呼叫者。`end_run()` 內部順序固定：emit `run_ended(ending_id)`（listener 同步拿到要播的東西，`flow_text` 在這裡寫結局 stub 文字）→ meta 層不動（knowledge 本來就在裡面）→ run 層全部重置且**手牌重建為 `[主角卡]`**（規格書第十六節）→ 回第 1 天 morning（開局選單留待 Phase 5，直接跳過）。
- **`advance_phase()` 的後置條件不可依賴外部呼叫者。** 收尾若留給 `run_ended` 的 listener 做，`main.gd` 與 `playthrough_greedy.gd` 就各要寫一份同樣的順序；漏寫的那條會停在第 45 天 evening 不動，而且每推進一次就重發一次 `run_ended`——走查腳本等於無窮迴圈。這跟 `test_boot.gd` 擋的是同一類病：兩條組裝路徑，只有一條被驗過。
- 上一條成立的前提是**結局文字同步渲染**（P1 的結局只有一行 stub，`run_ended` 的 listener 在 signal 內就寫完）。Phase 5 的真結局要逐行播、又要讀 run 層資料時，改成「結局流程播完才呼叫 `end_run()`」＋run 層加一個「本輪已結束」旗標擋重發——那時 GameState 已經有結局群，成本合理。**在那之前不要預先加那個旗標**，P1 沒有東西需要它。
- `by_design` 空格名單（第 1 天上午下午、第 32 天下午）抽到 `scripts/core/data_facts.gd` 常數；`verify_data.gd` 與走查腳本都引用它，不各留一份。
- `playthrough_greedy.gd`（`extends SceneTree`，不 instantiate 任何 UI）：只呼叫 `PanelBuilder.available_locations()` 與 `GameState.try_place()` ——**與 UI 完全同一組規則層 API**。每個行動格依地點順序找第一個 OPEN 且 `accepts` 含 protagonist 的槽放主角卡，找不到就跳過該格（比對 `DataFacts` 名單）；night 一律睡。跑到結局、再驗第二輪初態（meta 在、run 空），全綠 exit 0。**腳本不呼叫 `end_run()`**——它由 `advance_phase()` 自己接（見結局 coda 那條），腳本只管一路推進。
- lint 3（免費槽同面板規約）、lint 5、lint 7（夜間可達性：定日 beat 日期 ≥ 地點 `earliest_night`；非 fixed 夜 beat 必須掛 night layer 地點或旅館）與 lint 8（殘響可播出性：`echo` 必有 `day`、`echo.day > when.day`、`echo.text` 全域不重複）補進 `verify_data.gd`。

### P1-G 面板互動模型

> 驗收意圖見 `實作規格書.md > P1-G`。本節只寫怎麼做。
>
> **這是回頭改 P1-C／P1-D 做出來的東西**，不是新功能疊加。動工前先讀規格書第四節「面板的兩個階段」與第五節「槽要先告訴玩家它收什麼」。

動的檔：`game_state.gd`（`open_panel()` 拆兩個入口、新增預覽入口與「還有沒有事可做」查詢）、`panel_builder.gd`（view model 帶槽型別）、`data_loader.gd`（載入 `card_types.json`、lint 9）、`location_panel.gd`（兩階段渲染、右鍵預覽、型別標示）、`map_list.gd`（沿用現有的空面板停用）、`main.gd`（推進按鈕提示狀態）、`verify_data.gd`（lint 9）。

**規則層必須先拆成三個入口，UI 才有得接。** 這一條是 P1-G 的地基，先做它：

| 入口 | 簽名 | 做什麼 | 誰呼叫 |
|---|---|---|---|
| 求值 | `build_panel(location_id) -> Dictionary` | 純計算 view model，**零狀態變化** | UI 進地點時、走查腳本、預覽 |
| 演出 | `play_beat(beat_id) -> PackedStringArray` | 結算該 beat 的 `on_enter`（每輪至多一次）並回傳要演的行 | UI 逐一推進演出時、走查腳本 |
| 預覽 | `preview_slot(beat_id, slot_id) -> Dictionary` | 回傳 `{ cards: [...], reason: String }`，**零狀態變化** | UI 右鍵時 |

- 現行 `open_panel()` 把求值與結算綁在同一個呼叫裡（內含 while 迴圈連鎖結算），**P1-G 之後不該再有那個形狀**。拆完把 `open_panel()` 刪掉，不要留一個沒人用的舊入口——K-14／K-15 就是這樣長出來的。
- `preview_slot()` **必須與 `try_place()` 共用同一組判斷**（持有、三態、`accepts`、`action_spent`），不可各寫一份。作法：把 `try_place()` 的前四步抽成一個 `_check_place(card_id, beat, slot) -> reason_code`，兩邊都呼叫它。**兩份實作遲早會分岔，而分岔的那天玩家會看到「預覽說可以、放下去被拒絕」。**
- **走查腳本要走演出那條路。** `playthrough_greedy.gd` 現在靠 `open_panel()` 順便結算 `on_enter`；拆開之後它必須自己呼叫 `play_beat()`，否則 28 個白天 fixed beat 的 `on_enter` 全部不結算——這就是 K-01 的病，換一個位置再犯一次。UI 路徑與走查路徑各要有一條斷言（見「跨 Phase 實作紀律」）。

演出流程（UI 側）：

- 進地點：呼叫 `build_panel()` 取得成立的 beat 清單，**依陣列順序**建一個待演佇列（順序同 `Data` 載入順序，不另外排）。
- 逐一演出：玩家按一下才 `play_beat()` 下一個。**每演完一個要重新 `build_panel()`**——前一個的 `on_enter` 可能讓後面的 beat 或槽改變三態。
- 演出期間卡槽節點不建立，或建立但 `disabled`。**擇一寫死在契約裡，不要讓兩個地方各做各的**：採「不建立」，演完才建，理由是灰掉的槽在第五節有既定語意（`reject_reason`），拿來表示「還沒輪到你」會混掉一詞一義。
- 佇列空了進入常態階段：渲染 `locations.json` 的 `desc`（缺欄位就只顯示 `name`）、`occupant` 槽的 `label`、可放卡的槽。
- 已演過的 beat（`beats_entered` 有記錄）**照樣進佇列、照樣要按一下**，但 `play_beat()` 內部只回傳文字、不套效果。冪等由規則層保證，不是靠 UI 記得跳過。

槽型別標示：

- `card_types.json` 由 `DataLoader` 載入，`Data.card_type_name(type_id) -> String` 提供查詢。
- 換算住 `PanelBuilder`：view model 的每個槽多帶 `accept_types: PackedStringArray`（顯示名，已去重、已保序）。**UI 不查 `cards.json`、不碰 `accepts` 原始值**——K-04 就是 `accepts` 比對邏輯跑到 UI 去的那一條。
- 串接符號用「、」，寫成 `PanelBuilder` 的常數，不要散在 UI。

推進按鈕提示：

- `GameState.has_any_legal_action() -> bool`：掃當前時段所有可用地點 × 成立 beat × 槽；任一組合經 `_check_place()` 通過，或存在 OPEN 且尚未結算的 `choice_group` 槽，就回 `true`。已結算或 LOCKED 的 choice 不算。
- **這是查詢，不是命令**——它不改狀態、不推進時段。`main.gd` 拿它決定按鈕外觀，僅此而已。
- 成本：白天最多 20 個地點，一次掃過去在 P1 規模可接受。**每次狀態變化才重算，不要放進 `_process()`。**

`card_types.json` 與 lint 9：

- lint 9 掛在 `Data._ready()`，與 lint 1／2 同一批。雙向檢查：`cards.json` 的每個 `type` 在 `card_types.json` 有一筆；`card_types.json` 的每個 `id` 是合法型別名。
- **`card_types.json` 有 12 筆而 `cards.json` 只用到 10 種，這不是錯誤**（`group`、`consumable` 第一輪沒有卡）。lint 9 只擋「用到卻沒定義」，不擋「定義了沒用到」。

### P1-H 手牌可讀性

驗收意圖見 `實作規格書.md > P1-H`。**動工前先確認 K-50（UI 模擬的孤兒行程污染下一輪）已修**——否則 P1-H 的驗收紅了無法歸因，分不出是版面改壞還是工具在鬧。

檔案：

| 檔案 | 動作 |
|---|---|
| `scenes/ui/hand_bar.tscn` / `.gd` | 改成「頂列一行資訊 ＋ 底下 `ScrollContainer > GridContainer`」。**節點結構可以整個換掉**，但外框位置與高度不動 |
| `scenes/ui/card_detail.tscn` / `.gd` | 新增。卡片詳情與可捲動的知識清單**共用這一個元件**，不要寫兩份 |
| `scripts/autoload/data.gd` | 新增 `card_display_name(card_id)`，見下方「顯示名要抽共用」 |
| `scenes/ui/location_panel.gd` | 刪掉本地的 `_card_display_name()`，改呼叫 `Data.card_display_name()` |
| `tests/ui_sim/make_states.gd` | 新增完整知識清單的 UI 壓力狀態；透過正式 `gain_card()` 產生，不手改 checkpoint JSON |
| `tests/ui_sim/cases/p1af_cases.gd` | `p1af_06` 改寫；新增下方定義的 6 條 P1-H 契約案例 |
| `tests/ui_sim/qa_contract_matrix.gd` | 契約目錄新增 P1-H 條目 |
| `tests/ui_sim/run_ui_sim.ps1` | 新增超長卡名 data variant，並把寫死的契約總數改成下方定案值 |

版面：

- `GridContainer` 的 `columns` 固定 **7**；它的 child 數永遠等於 `GameState.hand.size()`，**不從 `hand_size` 建空格，也不為列數寫分支**。
- **只畫有卡的格子**，不畫空格。容量壓力由既有的 `手牌 n / 上限` 文字承擔；`hand_size` 是規則上限，不是 `GridContainer` 的 child 數。
- 一張卡一個 `Button`，`text` 是 `Data.card_display_name()`。放不下時截斷並補省略符號——用 Godot 的文字截斷設定，不要自己算字數（中英文寬度不同，自己算一定錯）。
- 頂列：左邊沿用 `手牌 n / 上限`；**知識入口放在它右邊、隔開一段距離**（已拍板不緊貼）。入口文字要**帶張數**。
- 高度預算：外框 140，頂列約 24，剩下兩列各約 52。底部 `ScrollContainer` 關閉水平捲動、只在卡片超過可視兩列時提供垂直捲動。**外框不要動**——動了就會連帶動到 `ContentView` 與幾何保護區。

詳情元件：

- 沿用 P1-G 預覽彈窗那一套（`AcceptDialog` ＋ `get_ok_button()` 掛 `qa_id`），不要另外發明一種對話框。內容區設最大高度並可垂直捲動，彈窗不得超出 1280×720 視口。
- **唯讀。** 不接任何會改狀態的訊號，元件內不得出現 `place::` / `choose::` 控制項。
- 卡片詳情：顯示名 ＋ `text`。知識清單：逐條列出全部知識卡的顯示名與 `text`。

UI 壓力 fixture：

- 一般 P1-H 案例用既有 `d10_night__knowledge.json`；它同時持有 `info_husband_version`、`info_wife_version` 與 `k_forty_something`。**不要用 `d9_afternoon.json`——那份狀態的 `meta.knowledge` 是空的。**
- `make_states.gd` 另產生 `p1h_knowledge_full.json`：從合法狀態出發，經 `GameState.gain_card()` 加入 `Data.loader.cards` 中所有 `slotless: true` 的卡，再 `serialize()`；禁止直接改 checkpoint JSON。這是知識清單的容量／捲動 fixture，不宣稱單輪自然路徑會同時取得全部知識。
- `run_ui_sim.ps1` 另建 `long_card_name` data variant，只把當時手上已有的 `info_husband_version.name` 換成必定超過單格寬度的測試字串。正式 `data/` 不動；驗收完仍要在詳情看見未截斷全名。

顯示名要抽共用：

- 現在 `location_panel.gd` 有一份私有的 `_card_display_name()`，它做了一件關鍵的事——**先用 `#` 切掉實例後綴再查表**（`madness#1` → `madness`）。手牌列一定會拿到實例 id，所以不能各寫一份、也不能忘了剝後綴。
- 抽成 `Data.card_display_name(card_id)`，與既有的 `Data.card_type_name()` 並列，兩個呼叫端共用。查不到時**退回原本的 id**（不要回空字串——畫面上看到 id 至少還能除錯，看到空白就什麼線索都沒有）。

`qa_id` 契約（**定案，之後不要改**）：

| 節點 | `qa_id` |
|---|---|
| 佔格計數 | `hand_slots`（沿用，`p1af_05` 在讀它的 `/ N`） |
| 手牌上的每一張卡 | `hand_card::<hand 陣列裡的那個 id>`（例：`hand_card::madness#1`） |
| 知識入口 | `knowledge_entry` |
| 詳情彈窗關閉鈕 | `dialog_confirm::card_detail` |

- **用 `hand` 陣列裡的原始 id，不要用 base id。** `hand` 裡的每一筆本來就唯一（發狂卡存的是 `madness#1`、`madness#2`），用原始 id 天然滿足 runner 的唯一性契約；用 base id 會在 P2-A 一次撞三個同名。
- 舊的 `hand_cards` / `knowledge_cards` 兩個 Label 會消失。**這是刻意的**：`p1af_06` 改成查 `hand_card::<id>` 存不存在，不再比對整串文字。改用顯示名之後文字比對本來就不可靠——54 張卡裡有 **10 組顯示名互為子字串**，最極端的是主角卡的顯示名只有一個字，它出現在三張別的卡名裡。

會絆倒人的兩件事：

1. **`run_ui_sim.ps1` 寫死了契約總數 47**（`if ($catalogContractCount -ne 47 ...) { exit 1 }`）。P1-H 本批新增 6 條機器契約，id 固定為 `p1h_01_hand_cards`、`p1h_02_card_detail`、`p1h_03_detail_readonly`、`p1h_04_knowledge_detail`、`p1h_05_name_truncation`、`p1h_06_handbar_geometry`；`p1af_05`／`p1af_06` 是改寫既有契約，不重複計數。**本批完成後目錄總數固定 53**，matrix、launcher 與 `測試指南.md` 三邊必須一致。P2-A 的多實例卡驗收尚未加入本批，不算第 54 條。
2. **`Data.tuning("hand_size")` 的資料變體測試已經存在**（`p1af_05` 用隔離資料跑兩個值），但它目前只驗 `hand_slots` 的 `/ N` 文字，**不會替 P1-H 驗格線**。保留它驗容量單一事實來源；格線固定 7 欄、child 數等於 `hand.size()`、沒有空框與外框矩形不變，改由 `p1h_06_handbar_geometry` 驗。

不做（寫下來免得順手擴大）：

- 不碰 UI 型態（`待決事項.md` 23）、不做拖放、不做美術與配色。
- 不改地點面板的放卡按鈕——**它已經顯示中文名了**，只是把私有 helper 換成共用的。
- 不處理夜間演出文字的節奏（那條是 `測試指南.md` 的 👤 條目，等人玩過再決定，**不要在 P1-H 順手改掉**）。
- 不補 `locations.json` 的 48 個 `desc`，那是內容角色的事。

---

## Phase 2 — 發狂時鐘與縱慾（實作契約）

> 驗收意圖見 `實作規格書.md > Phase 2`；操作清單見 `測試指南.md > Phase 2`。
> 動工前先讀規格書第八節整節，以及第九節「夜間地圖與標記開啟」前半（P2 只搬發卡那一條）。
> **P2 不動 UI 架構**——P1-G 的兩階段面板模型照舊，出口槽就是一種普通的槽。

### 全域結構決策（P2 期間建立）

新增檔：

```text
scripts/core/
└── indulgence.gd            # class_name Indulgence（static）：挑選演算法與強度級查表
data/beats/
└── indulgence_exits.json    # 六個出口，常駐白天 beat
tests/headless/
├── test_p2a.gd ~ test_p2d.gd
└── test_p2_sim.gd           # 三種玩家重演
```

- **挑選與查表放 `core/`，執行放 `GameState`。** 理由同 `ConditionEval` / `EffectApply`：純函式好測，而且強制縱慾要在 headless 重演裡被單獨呼叫幾百次。
- `GameState` 新增的 run 層欄位（全部要進 `serialize()` / `deserialize()`）：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `indulgence_count` | `int` | 本輪縱慾次數，**主動＋強制合計**。強度級查表用它 |
| `forced_pending` | `Array[String]` | 已歸零、還沒吃到行動格的發狂卡實例 id。順延靠它 |
| `night_markers_opened` | `Dictionary` | P2 過渡用的本輪收費標記集合；P3-B 由 meta `night_locations_seen` 取代 |

`madness_clock` 與 `_madness_counter` P1-B 已建好，P2 只是開始寫它們。

> 上表是 P2 的過渡狀態。P3-B 會刪除 `night_markers_opened`，改以 meta `night_locations_seen` 作唯一到訪真值；P3 不建立「已對位集合」。不要把本段的歷史形狀帶進 P3。

### P2-A 發狂卡的產生與倒數

動的檔：`game_state.gd`（tick、開標記發卡）、`main.gd`（夜間選地點時呼叫開標記）、`hand_bar.gd`（卡面顯示剩餘天數）。

| 入口 | 簽名 | 做什麼 |
|---|---|---|
| 走錶 | `tick_madness() -> Array[String]` | 全體 −1，回傳**這次歸零**的實例 id 陣列 |
| 開標記 | `open_night_marker(location_id) -> PackedStringArray` | 首次進入該夜間地點時發 `madness_cost` 張卡，回傳要播的文字行 |

- **`tick_madness()` 由 `advance_phase()` 在跨日進 morning 的那一步呼叫**，不由 `main.gd` 或走查腳本各自呼叫。這是 K-01／`end_run()` 那個病的第三個候選位置——**收尾不丟給 listener**。
- 「產生當天不倒數」**不寫任何旗標**：發卡發生在 night、tick 發生在次日 morning，順序自然成立。加旗標等於為一個不存在的情況付永久的複雜度。
- `open_night_marker()` 內部順序：① 查 `night_markers_opened`，已開就 no-op 回空 → ② 記入集合 → ③ 依 `madness_cost` 呼叫 `gain_card("madness")` 那麼多次 → ④ 回傳文字。**先記集合再發卡**，這樣發卡途中撞到 cap 觸發 BE 時，集合狀態仍然是對的。
- **一夜一個標記**由夜間流程管：玩家選過地點之後，同一夜的其他地點入口關閉。P1-F 的夜間 stub 已經是「選一個地點」的形狀，P2 只是讓它真的發卡。
- 卡面天數：`hand_bar.gd` 讀 `madness_clock[inst]`。**UI 只讀不寫。**

### P2-B 縱慾出口與主動縱慾

動的檔：`data/beats/indulgence_exits.json`（新）、`data_loader.gd`（`when.day_from` / `day_to`、`phase` 陣列、lint 10）、`game_state.gd`（`indulge()`、`try_place()` 轉導）、`panel_builder.gd`（出口槽的三態）、`verify_data.gd`（lint 10）。

**六個出口的落點**（依企劃書第七節那張表；文字與效果是佔位，正式文本屬內容期）：

| slot id | 出口 | 地點 | 時段 | `condition` 額外條件 | `weight` | `auto` |
|---|---|---|---|---|---:|:---:|
| `x_smash` | 砸東西 | `sanquan` 山泉閣 | 上午＋下午 | 無 | 1 | ✓ |
| `x_binge` | 食慾 | `sanquan` | 上午＋下午 | 無 | 2 | ✓ |
| `x_splurge` | 奢侈 | `oldstreet` 老街 | 上午＋下午 | `has_card` 錢卡 | 3 | ✓ |
| `x_violence` | 暴力對人 | `sanquan` | 上午＋下午 | `day_at_least: 16` | 4 | ✓ |
| `x_lust_ajie` | 性慾（阿婕） | `sanquan` | 上午＋下午 | `relation_at_least{ajie, 疑似}` | 5 | ✓ |
| `x_lust_awei` | 性慾（阿薇） | `jinghe` 靜和園 | 上午＋下午 | `relation_at_least{awei, 疑似}` | 5 | ✓ |
| `x_soak` | 泡湯 | `sanquan` | **只有上午** | 無 | — | ✗（`soak: true`） |

七個槽對六個出口——性慾有兩個對象，各一個槽。**全部再加一條 `condition: {madness_at_least: 1}`**（手上沒卡就不顯示）。

三個落點的理由：

- **三個出口都在山泉閣**（砸東西、食慾、泡湯），這正是企劃書第七節那句「沒有別的地方可以發洩的時候，他拿家業出氣」。山泉閣三章都開、上午下午都在，**保底規則因此不需要任何特例**。
- **暴力對人擋在第 16 天之後**（第二章起）。叔叔從第 1 天就常駐山泉閣，不擋的話「對象在場」從第一天就恆成立，**強制縱慾一到最重級就固定砸向叔叔**，砸東西與食慾永遠不會被自動挑中。企劃書第七節也寫「第一章特別需要無條件出口——那時候關係都還沒到」。現行 `forced_normal_until` 為 7 時，第一次最重落在第 27 天（機制模擬 A），離第 16 天很遠，這條門檻不影響曲線。
- **性慾用 `relation_at_least` 疑似**，對應企劃書「一定的關係程度」。阿婕在山泉閣（第 2–22 天、第 27 天起），阿薇在靜和園（第 10 天起）——`npcs.json` 的 `at[]` 已經是這個形狀，**槽的條件不重寫在場表**，只寫關係門檻，在場與否由地點決定。

⚠️ **兩個已知的資料缺口，P2 不補**（都屬 `待決事項.md` 22 卡片清單）：

- **錢卡不存在**，所以奢侈出口在第一輪恆為 LOCKED。**不影響保底**（砸東西與食慾無條件），也不影響機制模擬的數字（模擬裡玩家 A、B 從不主動縱慾，C 有無條件出口可用）。**不要為了讓它亮起來就臨時發明一張錢卡**——卡片清單是整批要談的事。
- **食慾的「日常卡往心境卡掉」沒有卡可掛**（`routine` 只有 `routine_debt`、`mood` 只有 `mood_watched`，語意都不對）。P2-B 先用旗標與叔叔的關係代價頂著。

**主動縱慾的唯一入口**（形狀照 `choose()`，P1-E 那條路已驗過）：

```gdscript
func indulge(beat_id: String, slot_id: String, card_inst_id: String) -> Dictionary
```

內部固定順序，任一步不過＝回傳拒絕理由且 **GameState 零變化**：

1. 卡在手上、且 base id 是 `madness`
2. 槽存在、`indulgence` 欄位存在、三態為 OPEN
3. 泡湯特例：`soak == true` 時檢查 `phase == "morning"` 且當天剩餘行動格 ≥ `soak_phase_cost`
4. 消耗行動格（泡湯消 `soak_phase_cost` 格，其餘消 1 格）
5. 消掉卡（泡湯消 `soak_cards_cleared` 張，**玩家挑哪張**；其餘消傳入那張）
6. `indulgence_count += 1`
7. 套 `on_place`，再套 `on_place_by_level` 當次強度級那一組

⚠️ **不寫 `slots_placed`**（K-54，2026-08-16 修正；本節原本寫「8. 記入 `slots_placed`」，那是錯的）。出口 beat 的 `when` 是 `day_from: 1` → `day_to: 45`，**同一個 `beat_id::slot_id` 撐滿整輪**，而 `slots_placed` 只有 `end_run()` 會清——寫進去就等於「砸東西一輪只能砸一次」。實測是一輪 90 個行動時段裡主動縱慾上限 4 次，而一輪供給 14 張發狂卡。**縱慾的節流閥是行動格（一天最多 2 次），不是槽的一次性**；企劃書第七節「任何時刻都必須至少有一個出口點得下去」與規格書 P2-E「玩家 C 主動縱慾 14 次」都靠這一點成立。回歸測試住 `test_p2b.gd`（跨時段與跨日各驗一次，並斷言 `slots_placed` 沒有那個鍵）。

- **`try_place()` 收到 `madness` 卡時轉導 `indulge()`**，不自己走一遍——與 P1-E 的 `try_place()` → `choose()` 同一個作法。B-01 說的「不要看到第六節那張表就去 `try_place` 加一行 `consume_action()`」在這裡仍然成立：消耗住 `indulge()`。
- **泡湯吃兩格怎麼實作**：`action_spent` 是布林，吃不了「兩格」。P2 改成 `consume_action(n: int = 1)`，內部把當天剩下的行動時段標成已用（`forced_pending` 的順延也需要這個能力）。**不要另開一個 `soak_day` 旗標。**

`data_loader.gd` 的三個小改：

- `beats_at(day, phase)` 支援 `day_from` / `day_to` 與 `phase` 陣列。**`_is_beat_time_valid()`（`game_state.gd`）要一起改**，兩處現在各有一份 `when` 比對——K-14／K-30 就是這個形狀，P2 動它的時候順手收攏成一個 `DataFacts.beat_matches_time(beat, day, phase) -> bool`，兩邊都呼叫。
- 有 `day` 又有 `day_from` ＝載入錯誤。
- lint 10 掛 `Data._ready()`，與 lint 1／2／9 同一批。

### P2-C 強制縱慾

動的檔：`scripts/core/indulgence.gd`（新）、`game_state.gd`（執行與順延）、`main.gd`（播出文字）。

`Indulgence`（`static`，零狀態）：

| 函式 | 簽名 | 做什麼 |
|---|---|---|
| 挑出口 | `pick_exit(gs, data) -> Dictionary` | 回傳 `{beat_id, slot_id}`；池空＝回空字典並 `push_error`（那是資料錯誤，lint 4 應該先擋下） |
| 查強度 | `level_for(n: int, tuning: Dictionary) -> String` | 回 `"light"` / `"normal"` / `"heavy"` |

- `pick_exit()` 掃全部 `accepts` 含 `madness` 的槽，**過濾掉 `auto == false`**，求值 `condition` 與 `requires`，取 `weight` 最大；同分取**資料載入順序**第一個。
- **不看地點可達性、不看 `when`**——強制縱慾是主角自己動手，不是玩家走過去（規格書第八節）。這一條要寫成註解留在函式裡，否則下一個讀的人會覺得漏了時段檢查。
- `level_for()`：`n <= forced_light_count` → light；`n <= forced_normal_until` → normal；否則 heavy。現值（1／7）＝第 1 次輕、第 2–7 次正常、第 8 次起最重，對上機制模擬「11 次其中最重 4 次」。

`GameState` 執行流程：

- `tick_madness()` 回傳的歸零清單**接進 `forced_pending` 尾端**，不立刻執行。
- `_settle_forced_indulgence()` 在**每個行動時段開始時**呼叫：`forced_pending` 非空且該時段行動格未使用 → 先檢視有效卡片與出口有效性，**成功挑選後才 pop_front**（帳不豁免、不吃債），執行一次（`pick_exit` → 消行動格 → 消卡 → `indulgence_count += 1` → 套效果），**一個時段最多一次**。
- 當天兩個行動格都用完而 `forced_pending` 還有東西 → 什麼都不做，次日 morning 自然接著吃。**順延不需要額外程式碼，只要不清空佇列。**
- **順序**：跨日進 morning 時先 `tick_madness()` 再 `_settle_forced_indulgence()`——當天歸零的卡當天就要開始還帳（機制模擬 A 的第 13 天就是這樣算的）。
- 「第一次是預告」由資料承擔：`on_place_by_level.light` 的 `text` 寫預告。**引擎不寫「下次更重」這種字串。**
- **帳目計數**：`GameState.madness_cards_cleared` 累計消除卡數（在 `lose_card` 扣發狂卡時 `+1`），供走查嚴格斷言總卡數（不受泡湯每次清幾張的數值調校影響）。
- **UI 演出文字留存**：`last_forced_lines` 在當前時段內常駐於地圖上方（關閉地點面板回地圖時維持顯示），直到下次時段推進才清空，確保玩家在行動格被吃掉的時段能隨時看清原因（比照夜間 fixed beat）。

### P2-D 視野門檻與發瘋 BE

動的檔：`game_state.gd`（cap 檢查）、`main.gd`（BE 顯示）。

- `madness_at_least` 在 `ConditionEval` 已經實作（P1-D），P2-D **不動求值器**，只驗它在真資料上生效。
- cap 檢查掛在 `gain_card()` 加入 madness 實例之後那一步：`hand` 內 madness 數 ≥ `madness_cap` → 呼叫 `end_run("ending_madness_be")`。
- **一輪只結束一次**：`end_run()` 已經在 P1-F 收斂成自己收尾（B-02）。若 `open_night_marker()` 一次發多張而中途撞 cap，第二張之後的 `gain_card()` 落在已重置的 run 上——**所以 cap 檢查要在發完那一批之後做一次，不是每張都做**。`open_night_marker()` 的迴圈跑完再檢查。
- BE **不播後日談骨架**：`main.gd` 收到 `run_ended("ending_madness_be")` 時走與一般結局不同的顯示分支，但**收尾流程共用同一條**。不要為 BE 開第二條輪結束路徑——P5 會把結局流程整個換掉。

### P2-E headless 重演三種玩家

動的檔：`tests/headless/test_p2_sim.gd`（新）。

- 三種玩家寫成三個策略函式（`fn(gs) -> 今晚開哪個地點 / 要不要主動縱慾`），主迴圈共用。策略定義照 `subdocs/驗證/發狂卡機制模擬.md > 二`。
- **走的是真的規則層入口**（`open_night_marker()` / `indulge()` / `advance_phase()`），不是另寫一份模型——這一條是 P2-E 存在的全部理由。重寫模型只會再驗一次模擬自己。
- 逐項比對規格書 P2-E 那張表，**任何一格不合就 exit 1 並印出實際值與期望值**。
- 走查腳本 `playthrough_greedy.gd` 也要跑：**P2 之後它會開始撞到強制縱慾吃行動格**，90 格的分類統計會變。變了是對的，但**要在 commit message 裡記下新數字**，下次比對才有基準。

---

## Phase 3 — 夜間層真值化（實作契約）

> 驗收意圖見 `實作規格書.md > P3-A`～`P3-F`；操作清單見 `測試指南.md > Phase 3`。
> 前置：P2-E 全綠。P3 不做 P4 遭遇、不做 P5 結局／開局、不解待決 29、不把文字清單換成座標地圖。
> **子階段依序做，不得跳接。** P3-A 先讓資料成為真值；P3-B 才改 meta 與收費；P3-C 接內容流程；P3-D 接夜間 UI；P3-E 接白天對位；P3-F 才跑雙輪整合。

### 全域結構決策（P3 期間建立）

P3 完成後，夜間狀態只有以下四筆；不得再加 matched／aligned 集合：

| 欄位 | 層 | 型別 | 生命週期／用途 |
|---|---|---|---|
| `night_locations_seen` | meta | `Dictionary` | location id → true；終身到訪，marker cost 與 `night_seen` 的唯一真值 |
| `night_once_beats_seen` | meta | `Dictionary` | beat id → true；D1／D2／D15 fixed 與 D3 引導整份存檔只播一次 |
| `night_location_chosen` | run | `String` | 今夜已主動或強制到訪的 location id；空字串＝尚未選 |
| `night_sleep_pending` | run | `bool` | 睡覺內容已播、正等下一次操作進隔天 |

- 刪除 P2 的 `night_markers_opened`。P6 才做正式存檔 version／遷移；目前沒有存檔 UI，舊 P2 checkpoint 由 `make_states.gd` 重建，P3 不留相容欄位或雙寫。
- 對位狀態只由 `night_locations_seen`＋`knowledge`＋location 的 `night_reveal` 推導。**不建立 `matched_night_locations`、`aligned_locations` 或每輪免費集合。**
- UI 零規則維持不變：合法性、收費、到訪、對位與流程停拍全部在 `GameState`／`PanelBuilder`；`main.gd` 只路由結果，`map_list.gd`／`location_panel.gd` 只渲染 view model 與發意圖。
- 玩家可見字串依既有 i18n 前置紀律放檔頭 const 或資料，不散在函式內。狀態文字、空結果、風險警告與三個夜間按鈕文字各只有一份。
- 規則層回傳的拒絕理由一律 `{reason_code, reason_text}` 雙欄，不得退回單一 `reason`（K-03 契約）：`reason_code` 是封閉機器碼，`reason_text` 只帶資料裡的 `reject_reason`。
- **每個 P3 子階段收尾更新 `CODEX_HANDOFF.md`**（階段、最近完成、驗證狀態、下一個任務）。它曾停在 P1-G 五個子階段之久，落檔為 K-97；靠人記得不管用，所以寫進收尾契約。

### P3-A 夜間資料真值化

**前置**：P2-E。**本階段不得改夜間進入／收費／fixed 流程。**

動的檔：`data/cards.json`、`data/locations.json`、`data/SCHEMA.md`、`scripts/data_loader.gd`、`scripts/verify_data.gd`、P3-A 壞資料 fixture、`tests/headless/test_p3a.gd`、`tests/ui_sim/make_states.gd`（新增下述 baseline 情境）、`subdocs/卡牌/知識卡.md`（衍生索引，隨 10 張新卡重建）、`CODEX_HANDOFF.md`。

**「變更前後同一份 checkpoint」的產生方式（`測試指南.md > P3-A` 最後一條的實作契約）：**

- **產生器**：`tests/ui_sim/make_states.gd` 新增情境 `p3a_night_baseline`，沿用既有「走查策略＋指定決策＋phase 執行前 checkpoint」三件套，不手寫 JSON。
- **前置狀態**：走到**第 14 天 night、當夜尚未做任何動作**。產完當場斷言四條後置條件，不成立就中止並報是哪一條：`day == 14`、`phase == "night"`、`night_markers_opened` 為空（P3-B 之後改成 `night_location_chosen` 為空）、手上 madness 張數嚴格小於 `Data.tuning("madness_cap")`。選第 14 夜是因為它同時摸得到這條驗收要的三件事——當夜清單有未 seen 的收費 row（驗 `available_locations()` 與首次發卡），再推一夜就是 D15 fixed（驗 fixed 流程）。
- **檔名與位置**：`_qa/p3a_baseline/p3a_night_baseline.json`（狀態）與 `p3a_night_baseline.expected.json`（三項輸出的基準值）。**刻意不放 `_qa/runs/<run_id>/`**——那裡每跑一輪就換目錄，而這份要橫跨「改資料之前」與「改資料之後」兩次執行。沿用既有規約不進版控；進版控的是產生它的情境定義與後置條件。
- **「前」那一半怎麼來**：改任何 P3-A 資料**之前**，先在乾淨工作區跑一次產生上述兩個檔，並把當時的 commit sha 記進 `CODEX_HANDOFF.md`。事後補做不算數——舊程式與舊資料已經不在了，屆時拿到的「前」其實是「後」。改完之後**載入同一份狀態 JSON**（不是重跑 generator）比對 `expected`，三項全等才算過。狀態檔是 `serialize()` 輸出，P3-A 只新增卡與欄位、不動狀態 schema，所以改完仍載得起來；載不起來本身就是「本階段偷改了玩家流程」的證據，直接紅。

資料落法：

- 掃 `locations.json > night[]` 的 distinct `day_counterpart`，每個建立 `k_night_<day_counterpart>` knowledge 卡；`name` 為「夜裡可以走回＋白天地點顯示名」，`text` 使用不列舉夜間分區的共通敘述。12 個 row 的 `night_reveal` 由 `day_counterpart` 推出；**不要在測試或文件另寫 12 筆對照表。**
- `n_ahong_2.name` 至少要由結果句「血還是新的」改為進場前引子「有血跡的地方」；其餘 27 筆逐一比對 beat，只改同類型的結果劇透，不潤飾無關文案。
- 6 個有門檻的阿宏 location 各自補 `reject_reason`，且必須忠實對應現有條件：`n_ahong_2`～`n_ahong_4` 指向尚未跟完前一段痕跡，`n_ahong_5`／`n_ahong_6` 指向仍缺各自要求的路線知識，`n_ahong_7` 指向三個點尚未連齊。字串住 `locations.json`，通用 fallback 不算完成。
- `n_corridor_end` 加 `teaser_only: true` 與正式理由；`madness_cost`、`day_counterpart`、`night_reveal` 三欄維持 `null`。
- `locations.json > _pending.night_reveal` 在 10 張卡與 12 個引用落地後刪除；只刪已兌現的 pending 鍵，不碰 `madness_cost` 那筆。

新增資料檢查入口：

```gdscript
static func lint_night_alignment(loader: DataLoader) -> PackedStringArray   # lint 11
static func lint_night_locations(loader: DataLoader) -> PackedStringArray   # lint 12
```

- lint 11 從真資料計算 distinct counterpart、共用卡與 null 規約，並驗卡型別／slotless；不得把 10／12／16 寫成唯一判準，否則資料日後增減會假紅。數量只作當前資料的額外斷言。**雙向都要驗**：同一 counterpart 不得指到兩張卡，同一張卡也不得被兩個不同 counterpart 引用（`night_reveal` 到 counterpart 是單射）。後者是複製貼上最容易犯的錯，會讓兩個白天地點共用一張對位卡，把對位的一對一前提整個打掉。
- lint 12 先驗一般 row（非 teaser）的核心欄位必填與型別：`earliest_night`（int，1–45）、`chapter`（int，1–3）、`madness_cost`（int，>= 0），三者缺一或型別錯誤即報錯。**這條擋的是拼錯欄位名**——進場入口取的是帶預設值 0 的 `madness_cost`，鍵名寫錯會安靜變成免費地點，直接污染 P3-F 的 13／14 供給基準。再對所有 night row（含 teaser-only）驗 `chapter == chapter_for_day(earliest_night)`，並驗 teaser-only 的 null 欄位與理由。teaser 顯示仍只看 `chapter`；這條只守資料作者沒有把無效的 `earliest_night` 寫到另一章。章節計算共用 `GameState.CHAPTER_START_DAYS` 會造成 DataLoader 反向依賴；把常數與純計算移到 `DataFacts.CHAPTER_START_DAYS`／`chapter_for_day(day)`，`GameState.CHAPTER_START_DAYS` 只作相容 alias，`GameState.chapter()` 與 lint 一起呼叫。
- `verify_data.gd` 依既有 lint 樣式印出結果。**每一類錯誤各一個獨立壞 fixture，不得合併**——合併之後第一個報錯會遮蔽後面的，某條 lint 其實沒被證明過也看不出來。類別清單以 `測試指南.md > P3-A` 的負向條目為準，方針不寫死數量。

收尾驗：`verify_data.gd`、`test_p3a.gd`、`test_boot.gd`、既有 16 套 headless。P3-A 只新增目前流程尚未使用的資料，不得讓 P2 玩家路徑改變。

### P3-B 夜間狀態與進入規則

**前置**：P3-A。動的檔：`scripts/autoload/game_state.gd`、`scripts/core/condition_eval.gd`、`scripts/data_loader.gd`、`scripts/verify_data.gd`、`scenes/main.gd`、`data/locations.json`、相關 night beat JSON、`tests/headless/test_p3b.gd`、既有 P2 測試與 checkpoint 產生器。

新增／取代的規則層入口：

```gdscript
func night_location_seen(location_id: String) -> bool
func enter_night_location(location_id: String) -> Dictionary
# {ok: bool, reason_code: String, reason_text: String, lines: PackedStringArray}
func would_night_entry_end_run(location_id: String) -> bool
```

- `enter_night_location()` 取代 `open_night_marker()`；不留 wrapper。所有 UI、走查與測試同批換入口。
- **拒絕理由沿用 K-03 定下的雙欄形狀，不得退回單一 `reason`**：`reason_code` 是封閉機器碼集合（`not_night`／`unknown_location`／`not_night_layer`／`teaser`／`too_early`／`locked`／`already_chosen`／`already_slept`），`reason_text` 只在 `locked` 那條帶資料的 `reject_reason`，其餘留空。UI 用檔頭 const 表把 code 翻成玩家文案，`reason_text` 非空時優先顯示。`location_summary()`（P3-D）與 `confirm_night_alignment()`（P3-E）同形；同一個欄位兩種語意就是 K-03 那個坑。
- P3-B 的 `main.gd` 暫時沿用既有「點清單即進場」路徑，但改讀 Dictionary：`ok` 才開面板，拒絕則不開。P3-D 再把它拆成「先詳情、後進入」；不得為了等 P3-D 留下已不存在的舊 API 呼叫。
- 拒絕檢查順序固定：phase 是 night → location 存在且 layer night → 非 teaser → `earliest_night` 已到 → location `requires` 成立 → **今夜尚未選、也尚未睡**（`night_location_chosen.is_empty() and not night_sleep_pending`）。任一步失敗回 `{ok:false}` 且完整 `serialize()` 零變化。
- **八個 `reason_code` 每一個都要有自己的測試案例，不得只驗其中兩三條**：`not_night`（在 morning／afternoon／evening 呼叫）、`unknown_location`（id 不在 `locations.json`）、`not_night_layer`（傳白天地點 id）、`teaser`（`n_corridor_end` 於第三章）、`too_early`（`earliest_night` 未到）、`locked`（阿宏 row 前級未走，且 `reason_text` 等於資料的 `reject_reason`）、`already_chosen`、`already_slept`。逐條都要驗「回傳的 code 正確」＋「前後完整 `serialize()` 逐字相同」兩件事。只驗 `ok == false` 不算——**檢查順序寫錯會讓後面的 code 永遠取不到，而只驗布林值的測試照樣全綠**。`locked` 與 `too_early` 的先後尤其容易寫反：同一個 row 在 `earliest_night` 之前同時也 gate 不成立，順序錯了玩家會先看到門檻理由而不是「時候未到」。
- 成功順序固定：`night_location_chosen = id` → `night_locations_seen[id] = true` → 若此前未 seen，依 `madness_cost` 發卡並批次檢查 cap → 未結束本輪才回傳 marker 文字。**seen 在 cap 之前寫。**cap 觸發 `run_ended` 後，`main.gd` 沿用既有同步 `_is_showing_ending` guard，必須在開地點面板或解析內容前直接返回；不能讓 BE 後的新一輪狀態接著播放舊夜內容。
- `madness_cost <= 0` 不回「免費」文字；首次 paid 才回既有發卡提示。空內容通用文字由 P3-C resolver／面板處理，不塞進本入口。
- fixed 到訪使用 GameState 私有 helper `_record_forced_night_visit(location_id)`：驗 location 後寫 chosen＋seen，明確跳過 marker cost。不要讓 UI 能傳 `forced=true` 繞過收費。
- `would_night_entry_end_run()` 是純查詢，只計算目前可確定的首次 marker cost 是否會讓手上 madness 數達 `Data.tuning("madness_cap")`；已 seen、免費、LOCKED 與 teaser 一律 false。現行資料能確定的立即 BE 來源只有 marker cost；日後若新增無條件的進場即時發狂效果，必須同步擴充此 helper 與測試。UI 不自行數卡。
- `night_locations_seen`、`night_once_beats_seen` 進 `serialize().meta`；`night_location_chosen`、`night_sleep_pending` 進 run。`end_run()` 只清後兩者；`advance_phase()` 每次離開 night 清當夜狀態。

`ConditionEval` 增加 `night_seen`，委派 `gs.call("night_location_seen", id)`；`KNOWN_KEYS` 同批更新。引用檢查要證明值是存在的 night location id。

正式資料同批退掉 `opened_n_*`：

- `opened_n_ahong_1`／`_2`／`_3` 的 location gate 與 beat `requires` 改讀對應前一 row 的 `night_seen`，原 `on_enter.flag` 刪除。`n_ahong_5`～`_7` 的 `k_ahong_point_*` 門檻不動。
- 9 個純寫入旗標直接刪除：`opened_n_ahong_4`／`_5`／`_6`、`opened_n_litcorridor`、`opened_n_higher`、`opened_n_emptyspot`、`opened_n_emptyspot_2`、`opened_n_steam_below`、`opened_n_behind_temple`。
- `opened_n_gathering` 不得改成 `night_seen`：進場入口會先寫 meta seen，直接替換會讓 D31 首次到訪也誤播特殊內容，且錯誤跨輪。改名為 run flag `saw_n_gathering_intro`；`n_gathering_d31.condition` 讀它。**寫入端是 `n_gathering_ch2` 與 `n_gathering_ch3` 兩個章節變體的 `on_enter`，兩個都要寫**——只寫 ch2 在第 31 天當下夠用（那天取的就是 ch2），但旗標名是「本輪已看過聚會常態內容」，第三章首次進聚會不設就名實不符，日後多一個讀者就會踩到。
- `info_ahong_private` 與其他真正本輪故事 flag 保留。D19 上游那筆同時缺「登山隊路線卡」，仍照原 note 留待卡片資料決定；不得趁本階段用另一張卡猜著補。

新增 `lint_legacy_night_flags(loader) -> PackedStringArray`（lint 13），**遞迴掃整份 location 與 beat，含 `slots[]` 的 `condition`／`requires`／`on_place`**，任何 key 或值含 `opened_n_` 都報錯。只掃 beat 級三個鍵會留一個缺口——旗標可以從槽級長回來而 lint 全綠。P3-B 收尾正式資料必須 0 筆。

### P3-C 夜間內容流程

**前置**：P3-B。動的檔：`scripts/data_loader.gd`、`scripts/core/panel_builder.gd`、`scripts/autoload/game_state.gd`、`scenes/main.gd`、`data/beats/ch1_nights.json` 與 fixed 所在 JSON、`scripts/verify_data.gd`、`tests/headless/test_p3c.gd`。

DataLoader 新增夜間結構候選唯一入口：

```gdscript
func night_beat_candidates(day: int, location_id: String, current_chapter: int) -> Dictionary
# {primaries: Array[Dictionary], addons: Array[Dictionary]}
```

- 只做資料形狀選擇，不求值 condition／requires。`primaries` 先收同 location、non-fixed、由 `DataFacts.beat_matches_time(beat, day, "night")` 確認為當夜的定日候選（資料順序），再收同 location、無 `when`、`chapter <= current_chapter` 的章節變體（chapter 由高至低，同章維持資料順序）。`addons` 是同 location、無 `when`、無 `chapter` 者，保持資料順序。
- `DataFacts.beat_matches_time()` 同批縮成「有 `when` beat 的日期／時段判準」；無 `when` 一律 false，移除原本把夜間章節變體／add-on 視為時間成立的分支。`DataLoader.beats_at()` 與所有 dated／fixed 流程繼續共用它，夜間無 `when` 內容只由候選入口分類。

GameState 新增唯一的有狀態求值入口：

```gdscript
func resolved_night_content(location_id: String) -> Dictionary
# {primary: Dictionary, addons: Array[Dictionary]}
```

- 取得 `night_beat_candidates(day, location_id, chapter())` 後依序求值 `condition`：第一個成立的 primary 勝出，所以定日候選不成立時會退回章節變體；add-ons 只保留 condition 成立者。
- `requires` 不參與候選選擇：不成立的 primary 仍是選中的 LOCKED 內容，不得退回下一個章節版本。`PanelBuilder.build()` 用同一結果產生 OPEN／LOCKED；`sleep_night()` 用同一結果但只播放 requires 成立的內容。
- `GameState._is_beat_time_valid()` 依 phase 分流：非 night 與 night fixed 呼叫 `DataFacts.beat_matches_time()`；night non-fixed 只接受該 location 當下 `resolved_night_content()` 選中的 primary／addons id。三處不得另寫 night `when.day`／`when.phase` 掃描（K-30）。
- `PanelBuilder.build()` 刪除 `<location>_locked` 假 beat；marker cost 與 location gate 不再靠合成 beat 表達。合法但零 beat 就回空 beats，P3-D 仍允許進入。

fixed 流程：

- `play_night_fixed()` 遇 `meta_once: true` 且 id 已在 `night_once_beats_seen` 就跳過。第一次先記 once，再播放；若掛 night-layer location，先呼叫 `_record_forced_night_visit()`。非 meta-once fixed 若本輪 `beats_entered` 已有 id，也不因 `_route_view()` 重建而自動重播。
- D1／D2／D15 加 `meta_once: true`；D24 不加。D3 `n3_map_opens` 移到 `sanquan`、改 fixed＋meta_once、刪三個 slots 與 madness 效果；它因此是環境引導，不會被當作 night-layer 到訪。
- 新增 `lint_night_once(loader) -> PackedStringArray`（lint 14），驗 schema 所列限制，**另加一條「同一個 `when.day` 的 night-layer fixed 至多一個」**。兩個會互相覆蓋 `night_location_chosen`，兩個地點都被記成到訪、今晚被用掉兩次，而現在沒有任何檢查會紅。現行資料 D1／D2／D15 各在不同天，碰不到；這條擋的是資料擴充。`_record_forced_night_visit()` 順手加一道「chosen 已非空就 `push_error` 並拒絕」當第二層保險。

夜間推進由一個入口決定，`main.gd` 不自行組合 sleep＋advance：

```gdscript
func resolve_night_advance() -> Dictionary
# {advance: bool, lines: PackedStringArray}
```

- 有 `night_location_chosen` → `{advance:true}`；不得呼叫 sleep。
- `night_sleep_pending == true` → 清 pending，`{advance:true}`。
- 尚未選擇 → 呼叫 `sleep_night()`；有 lines 時設 pending 並回 `{advance:false, lines}`，無 lines 回 `{advance:true}`。
- `main.gd._on_advance_pressed()` 只照 `advance` 決定顯示 lines 或呼叫 `advance_phase()`。lines 先進 `FlowText`，本次點擊不得換日（K-69）。
- `_refresh_advance_hint()` 的 night 分支只讀狀態：pending＝「進入隔天」；chosen 非空＝「結束今晚」；其餘＝「直接睡」。

空內容用 view model 的 `empty_result: true` 表達，由 UI 顯示檔頭 const；不得合成 beat id。收尾驗 D1／D2／D15 跨輪、D24 每輪、D3 once、sleep 有／無內容與 K-30／33／69。

### P3-D 夜間地點清單與詳情 UI

**前置**：P3-C。動的檔：`scripts/core/panel_builder.gd`、`scenes/ui/map_list.gd`、`scenes/ui/location_panel.tscn/gd`、`scenes/main.gd`、`tests/headless/test_p3d.gd`、`tests/ui_sim/` P3-D cases、`tests/ui_sim/cases/p1af_cases.gd`、`tests/ui_sim/make_states.gd`、`tests/headless/playthrough_greedy.gd`。

**後三個是既有檔，因為 `available_locations()` 的語意在本階段改變**：`p1af_29_night_resolution` 與 `p1af_29_night_paid` 現有的兩條斷言「已選過地點後其他地點不可見」與新規則正好相反，必須反轉為「其他地點仍可見、但 `night_enter::<id>` 全部 disabled」，斷言訊息一起改（現在那句話把舊行為寫成了設計規則）。`make_states.gd` 與 `playthrough_greedy.gd` 都靠「chosen 非空時清單收斂成一個」決定當夜行為，同批確認。不先列進來，收尾跑既有 UI 全套會紅兩條而歸因不到這裡。

PanelBuilder 新增純 view-model 入口：

```gdscript
static func location_summary(location_id: String, gs: Node, data: Node) -> Dictionary
# {display_name, status_text, can_enter, reason_code, reason_text, warning, teaser_only}
```

- `available_locations()` night 分支只負責出現時機與穩定排序：一般 row 看 earliest；teaser 看 chapter。`night_location_chosen` 非空時仍回傳全部當下可見 row，location `requires` 不成立也**不從陣列移除**。
- `location_summary()` 是名稱、狀態、location gate 與風險警告的唯一來源。多對一判定從 loader.locations 現算，不存 count 欄位。`can_enter` 除了檢查地點本身，也要求 `night_location_chosen` 為空**且 `night_sleep_pending` 為 false**；今晚已選擇、被 fixed 強制帶入、或已按過「直接睡」看完內容之後，仍可查看詳情，但進入按鈕停用。
- `map_list.gd` 不再用 `build_panel().beats.is_empty()` 停用地點，也移除 `_SUFFIX_EMPTY`。按鈕文字只組合 summary 的 display name＋status；所有 visible row 都可點詳情。

`location_panel.tscn` 新增明示節點 `NightEnterButton`；`location_panel.gd` 新增：

```gdscript
signal night_entry_requested(id: String)
func show_night_details(location_id: String) -> void
```

- `show_night_details()` 不呼叫 `play_beat()`、不排演出佇列，只渲染 summary。`NightEnterButton` 依 `can_enter` 設 disabled 並顯示 reason／warning。
- 成功主動進場或 fixed 強制帶入後重建清單，row 數量與排序不變；每個 row 仍能開 `show_night_details()`，但所有 `NightEnterButton` 都因 chosen 非空而 disabled。
- `show_location()` 保留給白天地點與「夜間成功進入後」的正式演出；`main.gd` 收到 entry signal 才呼叫 `enter_night_location()`，成功後再 `show_location(id, lines)`。失敗只顯示 rules result，不自行改狀態。
- qa_id 固定：`night_enter::<location_id>`、`night_status::<location_id>`、`night_reason::<location_id>`、`night_warning::<location_id>`。文字清單既有 `location::<id>` 保留。
- 狀態與按鈕不可只靠顏色；1280×720 下長名稱、狀態、理由與 warning 必須在 scroll 範圍可達，沿用現有幾何診斷。

### P3-E 對位系統

**前置**：P3-D。動的檔：`scripts/core/panel_builder.gd`、`scripts/autoload/game_state.gd`、`scenes/ui/location_panel.tscn/gd`、`scenes/main.gd`（若 signal 由 main 轉接）、`tests/headless/test_p3e.gd`、`tests/ui_sim/` P3-E cases。

規則層與 view model：

```gdscript
static func alignment_offer(day_location_id: String, gs: Node, data: Node) -> Dictionary
# {available: bool, knowledge_id: String, night_ids: Array[String]}
func confirm_night_alignment(day_location_id: String) -> Dictionary
# {ok: bool, reason_code: String, reason_text: String, knowledge_id: String}
```

- `alignment_offer()` 反查所有 `day_counterpart == day_location_id` 的 row，只納入 seen；零筆、knowledge 已持有、非白天地點或當前不在 morning／afternoon 都 available false。多 row 必須驗出同一 `night_reveal`，不同代表資料錯誤而不是顯示多顆按鈕。
- `confirm_night_alignment()` 重新跑相同驗證後呼叫 `gain_card(knowledge_id)`；不寫 action、slot、choice 或 matched state。拒絕路徑完整 `serialize()` 零變化。
- **`reason_code` 是封閉集合，與 P3-B 同形**：`not_day_phase`（不在 morning／afternoon）、`unknown_location`（id 不在 `locations.json`）、`not_day_layer`（傳的是夜間 row id）、`no_seen_row`（該白天地點的 counterpart row 一個都沒 seen）、`already_known`（共用 knowledge 已在手）、`data_conflict`（多 row 指到不同 `night_reveal`）。本組沒有資料側 `reject_reason`，`reason_text` 一律留空；UI 照 P3-B 的檔頭 const 表翻譯。`data_conflict` 同時 `push_error`——那是資料錯誤，不是玩家做錯事。
- **六個 code 每一個都要有自己的負向案例**，逐條驗「code 正確」＋「前後完整 `serialize()` 逐字相同」。`confirm_night_alignment()` 是可以被直接呼叫的規則層入口，不能只靠「UI 上按鈕沒出現」當證明——按鈕沒出現驗的是 `alignment_offer()`，繞過 UI 直呼這個入口才是它自己的防線。這條與 `alignment_offer().available == false` 的判準必須逐項對得上：同一個狀態下 offer 不給、confirm 也必須拒，兩邊分歧就是第三份狀態偷偷長出來了。
- `location_panel.tscn` 新增 `NightAlignButton` 與 `ConfirmationDialog`。按鈕只在白天地點演出佇列清空、進入常態畫面後出現；白天地圖不讀 offer。
- qa_id 固定：`night_align::<day_location_id>`、`dialog_confirm::night_align`。彈窗打開／取消狀態不變；確認後 knowledge 恰好多一張、按鈕消失。
- 對位後夜間改名完全由 P3-D 的 `location_summary()` 衍生；knowledge signal 只觸發重建，不把顯示名存回狀態。

### P3-F 全流程驗收

**前置**：P3-E。主要動測試與走查：`tests/headless/test_p3f.gd`、`tests/headless/playthrough_greedy.gd`、`tests/headless/test_p2_sim.gd`（若第一輪基準受 fixed 消耗夜晚影響）、`tests/ui_sim/`、`subdocs/驗證/發狂卡機制模擬.md`。不得藉整合階段重構已綠的 P1／P2。

> **`測試指南.md`、`驗證後已知問題.md` 與 `PROJECT_BRIEF.md` 不在上面這張清單裡，本階段也不會進去。** 三份都是 verifier 角色的檔，規約住 `AGENTS.md > 實作守則 4`（本檔不重複定義）。這裡原本把 `測試指南.md` 寫成「主要動的檔」，等於要求 implementer 自己勾自己的驗收，與測試指南 Phase 3 開頭「實作者不得修改本節打勾」直接打架。

- 建一份從 `Data.loader.locations` 動態產生的 28-row matrix；斷言分類與 mapping 時由資料推導，只有 D1／D2／D3／D15／D24 這些敘事特例可寫 id。
- 第一輪策略仍走正式 `enter_night_location()`／`resolve_night_advance()`；原 P2 `open_night_marker()` 呼叫全部退場。策略必須按語意明示目標 id，不得再取資料順序中的第一個未見收費 row：至少跑「D14 `n_ahong_3`、D15 fixed 取得 `n_plaza`」的 13-cost 路徑效率案例，以及「D14 主動 `n_plaza`」的 14-cost 最大壓力案例。先更新 `subdocs/驗證/發狂卡機制模擬.md`，再同步 P2-E 基準，不能只放寬測試或對調 `locations.json` 順序。
- 第二輪從第一輪真實 `end_run()` 後繼續跑：抽樣 seen paid、unseen paid、seen free、fixed once、shared counterpart 五條路，逐項驗 meta、手牌與內容。
- 第二輪這條只驗終身首次收費與既有內容重演，不斷言壓力供給足夠；第二輪新增／深化收費 row 為主要供給、事件發卡為補充的內容決定仍住 `待決事項.md > 26`，P3 不實作。
- UI 契約至少覆蓋：穩定排序、四種狀態文字與 teaser 鎖定案例、隱藏價碼／無「免費」、詳情不進場、LOCKED reason、三種主按鈕、sleep 停拍、風險 warning、一對一對位、多對一與未見 row 自動對位。
- 新增 UI 契約時總數仍要五處一致，但**分兩手**：implementer 更新自己這三處（matrix／catalog／launcher）並回報 catalog 重數出來的實際數字；`測試指南.md` 與 `PROJECT_BRIEF.md` 由 verifier 依那個數字同步。不要先猜最終數字，實作完以 catalog 重數。（K-58／K-71 就是總數只改到一部分，分兩手之後「誰漏了」才看得出來。）
- 收尾完整跑既有 headless、P3-A～F、`test_boot.gd`、UI 全套與負向反證；再做規格書 P3-F 要求的人工體感驗收。K-34／35／68／70 的結案證據逐條落 `驗證後已知問題.md`，但由 verifier 寫，不由 implementer 改。（**K-37 已於 P1-F 結案，不在本階段的待收清單**。）

---

## Phase 4 — 委託與遭遇（實作契約）

> 驗收意圖見 `實作規格書.md > P4-A`～`P4-F`；操作清單見 `測試指南.md > Phase 4`。
> 前置：P3-F 機器層全綠。P4 不做 P5 結局／開局、不做正式存檔 UI，不加成功率、職業推導、人物專用容量或通用永久失去人物。
> **子階段依序做。** P4-A 先定資料；P4-B／C 完成委託規則與 UI；P4-D／E 完成遭遇規則與 UI；P4-F 才跑跨輪整合。

### 全域結構決策（P4 期間建立）

新增的 run 狀態只有三筆；全部進 `serialize().run`，`end_run()` 清空：

| 欄位 | 型別 | 用途 |
|---|---|---|
| `delegates_used_today` | `Dictionary` | person card id → true；隔日上午換日才清 |
| `pending_delegation_reports` | `Array[Dictionary]` | `{due_day, beat_id, slot_id, person_id}`；只存接點，陣列順序就是回報順序 |
| `active_encounter` | `Dictionary` | 空＝無遭遇；非空只含 `beat_id`、`round_id`、`blocked_slots`、`attempted_card_ids` |

- 人物卡仍在 `hand`，不建立 `companions`、`away_people` 或 partner capacity；「今日已受託」由 daily set 推導。
- 委託成功寫既有 `choices_made`／`slots_placed`，不另建 resolved 狀態。遭遇佔格只住 `active_encounter`，UI 用 placeholder 渲染，不塞假卡進 hand。
- mutation 入口維持 `{ok, reason_code, reason_text, lines}`；失敗完整 `serialize()` 零變化。玩家字串放資料或檔頭 const。
- 每個 P4 子階段收尾更新 `CODEX_HANDOFF.md`；實作者只交測試證據，不替 verifier 修改或打勾測試文件。

### P4-A 資料與 SCHEMA 真值化

**前置**：P3-F 機器層。**不得接新玩家操作。**

動的檔：`data/SCHEMA.md`、`data/cards.json`、D8／D17～19／D45 beat JSON、`scripts/data_loader.gd`、`scripts/verify_data.gd`、P4-A 壞資料 fixture、`tests/headless/test_p4a.gd`、需要重建的 checkpoint、`CODEX_HANDOFF.md`。

- 移除 D17 自動發三張人物卡。人物卡取得／失去只留在具名故事效果：阿婕讀信任破壞／修復事件，阿珠讀 D9 診所揭露路線，阿才由 D17～19 與他共事後的事件發放；不以關係數值自動增刪。
- 處方 beat 改成 D17～19 持續窗；親自處理與人物候選各一 slot，共用 choice_group。未持有人物卡靠 `condition.has_card` 隱藏，其他門檻靠 `requires`＋理由。
- D8 是 `repeat_each_run:true`、`charge_first_visit:true`、`allow_discard:true`、非 meta-once 的 dated night fixed encounter。beat text 放固定姓名開場；rounds 只存三個互動要求。D45 改為一 round、`escape_cost:null`、`allow_discard:false`，各正式卡路線有獨立 response。
- `on_resolve`、fallback 與三種出口共用 `EffectApply` 封閉效果鍵，不讓 encounter 偷讀任意鍵。

新增 lint：

```gdscript
static func lint_delegations(loader: DataLoader) -> PackedStringArray   # lint 15
static func lint_encounters(loader: DataLoader) -> PackedStringArray   # lint 16
```

- lint 15 驗 delegation 只收一個明確 person id、choice_group 完整、timing/report 配對、next_morning 不會落到第 45 天之後、理由與引用；不得以 `"person"` 泛型接受未來所有人物。
- lint 16 驗 round graph、response 引用與重疊、fallback／出口、`allow_discard` 等型別，以及 per-run fixed／首次收費適用範圍；從首回合走訪，不可達 round、懸空 next_round、無法結束的 cycle 都報錯。**定日／dated＝有明確整數 `when.day`，不限制 phase，dated night beat 也算；night-layer＝beat 所掛 location 的 `layer == "night"`。**`charge_first_visit:true` 另做聯合檢查：beat 必須 fixed、`when.day` 為整數、`when.phase == "night"`、location layer 為 night；不得把它擴成所有 night beat 的全域限制，D3／D24 等旅館 fixed 仍合法。D8 同時符合 repeat 與 charge 兩組限制。
- lint 14 只加窄例外：`fixed && when.day is int && when.phase == "night" && encounter.repeat_each_run` 可不 meta-once。每個錯誤類別各有 fixture；正向測試從資料動態數出，不複製完整表。

### P4-B 委託規則

**前置**：P4-A。動的檔：`scripts/autoload/game_state.gd`、必要的 `panel_builder.gd`／`effect_apply.gd` 接線、`tests/headless/test_p4b.gd`、既有序列化／時段測試、`CODEX_HANDOFF.md`。

```gdscript
func delegate(beat_id: String, slot_id: String, person_card_id: String) -> Dictionary
func delegation_status(person_card_id: String) -> Dictionary
# mutation 回傳 {ok, reason_code, reason_text, lines}
```

`delegate()` 檢查順序：白天行動時段 → beat 當下成立 → slot 存在且是 delegation → 卡在手且 type person → accepts 命中 → 今日未受託 → requires 成立 → choice_group 未結算 → timing/report 結構合法。code：`not_action_phase`、`unknown_beat`、`unknown_slot`、`not_delegation`、`not_held`、`not_person`、`not_accepted`、`already_delegated_today`、`locked`、`already_resolved`、`data_conflict`。

成功順序：重取 slot 真值 → 寫 choice／placed → 寫 daily set → immediate 套 `on_place`，或 next_morning append 接點。UI 不得先寫狀態。

- 委託不呼叫行動消耗、不增加 `npc_action_counts`、不移動 person card；關係等結果仍由效果明寫。
- next-morning 的 `on_place` 是派出當下效果，`delegation.report` 才是回報。換日上午先完成既有發狂倒數／強制縱慾，再依 pending 順序回查並套 report，最後清前一日 daily set；清完即可再委託。
- 回報不因人物卡後來被事件拿掉而取消。還原時接點失效視為資料衝突，不靜默丟棄。

### P4-C 委託 UI 與首個案例

**前置**：P4-B。動的檔：`panel_builder.gd`、`location_panel.gd/.tscn`、`hand_bar.gd`、`card_detail.gd`、`main.gd`、P4-C UI sim／checkpoints、`CODEX_HANDOFF.md`。

- 不建獨立委託畫面：委託槽留在 location panel，沿用既有卡片放置與確認。view model 只增加 `{result_timing, preview, tendency, delegation_state}`，UI 不讀原始 JSON。
- 未取得候選不出現；今日用過者留在原位、LOCKED 顯示「今日已受託」；其餘缺條件者顯示資料理由。排序永遠照資料。
- 確認只顯示任務、preview、tendency 與「立即／隔日上午回報」，不帶完整效果。`main.gd` 只送意圖給 `delegate()`、把 lines 送 FlowText 並刷新；取消零變化。
- HandBar 由 `delegation_status()` 顯示人物狀態。第一次 `gain_card()` 令玩家由零變一張 person card 時，寫 meta 教學旗標並顯示一次；不讀日期或特定人物。

### P4-D 遭遇規則

**前置**：P4-C。動的檔：新增 `scripts/core/encounter.gd`、`game_state.gd`、必要的 `effect_apply.gd` 接線、`tests/headless/test_p4d.gd`、序列化測試、`CODEX_HANDOFF.md`。

```gdscript
func start_encounter(beat_id: String) -> Dictionary
func encounter_view() -> Dictionary
func respond_to_encounter(card_id: String) -> Dictionary
func discard_in_encounter(card_id: String) -> Dictionary
func escape_encounter(card_ids: Array[String]) -> Dictionary
```

- `Encounter` 負責 graph 與純判斷，狀態只由 GameState 持有。`encounter_view()` 只回當前 demand、blocked／available、能否逃、可嘗試手牌與 attempted；絕不回 accepts、fallback 或未到回合。
- `start_encounter()` 建 state 後直接呼叫 `_enter_encounter_round(first_round_id)`；**start 本身不加 cost**，第一 round 只由 helper 加一次。後續每個 round id 也只能進一次、各加一次。超載是唯一例外：呼叫第一 round 前先加一次額外 penalty，所以超載進場刻意是「penalty 一次＋第一 round 一次」。每次增加後 `capacity - hand.size() - blocked_slots <= 0` 都立即 failure。
- response 先拒絕 madness、非手牌、已嘗試，再依資料順序找唯一 response；無命中走 fallback。先記 attempted；命中時依 response 的 `consume_card` 處理、套效果並固定釋放本回合 cost；fallback 固定消耗可丟棄卡、保留 cost，再套效果。不可丟棄卡遇 fallback 時留手但仍記 attempted，照 `next_round` 推進。最後走 next_round 或結束。
- `allow_discard:true` 時主動丟棄只移除可丟棄卡，不改 blocked、不推回合；false 時回 `discard_disabled`。escape 要求 id 唯一、數量恰等 cost、全可丟棄；全部驗完才一次結算；null 回 `cannot_escape`。
- 每次進 round、response 或 discard 結算後呼叫 `_finish_if_no_legal_move()`：若沒有尚未嘗試的非發狂卡、不能合法主動丟棄，且不能用目前可丟棄卡支付 escape（`escape_cost == 0` 視為可逃），立即走 failure。這是規則出口，不回拒絕碼。
- 三種出口共用 `_finish_encounter()`：保存出口資料、清 active state、套效果一次。`reason_code` 是下列封閉字串，不得自造同義碼：`encounter_active`、`no_active_encounter`、`unknown_beat`、`unknown_card`、`not_held`、`madness_blocked`、`already_attempted`、`discard_disabled`、`not_discardable`、`cannot_escape`、`wrong_escape_count`、`duplicate_payment`、`data_conflict`。
- 檢查順序按入口固定：`start_encounter()`＝active → unknown beat → data conflict；`respond_to_encounter()`＝inactive → unknown card → not held → madness → attempted → data conflict；`discard_in_encounter()`＝inactive → discard disabled → unknown card → not held → not discardable；`escape_encounter()`＝inactive → cannot escape → wrong count → duplicate payment → unknown card → not held → not discardable → data conflict。任一步拒絕都回對應精確碼且完整狀態零變化。

### P4-E 遭遇 UI 與 D8／D45

**前置**：P4-D。動的檔：新增 `scenes/ui/encounter_panel.gd/.tscn`、`main.gd`、`hand_bar.gd`、D8／D45 接線、P4-E UI sim／checkpoints、`CODEX_HANDOFF.md`。

- encounter panel 是特殊卡槽呈現，不擁有規則；只渲染 view，重用 HandBar／CardDetail。壓力格是 placeholder，不產生 card id。
- 點卡先確認；確認後才呼叫 response／discard／escape。未消耗但已嘗試的卡顯示不可重用，發狂卡顯示被壓死；disabled 理由由 code 對應 const。
- D8 fixed 看到 `charge_first_visit:true` 時走 GameState 私有的「強制選定＋共用首次收費」helper：**先把 `was_seen = night_location_seen(location_id)` 存下來，再寫 chosen／seen；只有 `not was_seen` 才按 location `madness_cost` 收費。**不得使用 P3 一般 fixed 的免收費 helper，也不在 scene 再發卡；收費若先觸發 BE 就不得開始舊遭遇。現有 `n_manydoors` 最早第 8 夜可見且 D8 強制，因此第一輪實際不可能預付，但 helper 仍守通用終身首次語意。beat text 播完才開始遭遇，結束才恢復「結束今晚」。
- D45 afternoon fixed 開 encounter；任一 response 結束後只推進一次到 evening。escape 依 `escape_cost` 顯隱；discard 依 `allow_discard` 顯隱，D45 兩者都不顯示。沒有其他隱含 gate。

### P4-F 全流程與跨輪驗收

**前置**：P4-E。主要動測試：`test_p4f.gd`、`playthrough_greedy.gd`、`run_all_headless.ps1`、P4 UI sim catalog／matrix、必要 checkpoint、`CODEX_HANDOFF.md`。不得改玩法資料迎合 expected。

- 委託 matrix 從正式 slots 衍生，覆蓋未取得／可用／今日已受託／條件不足／兩種回報／親自做互斥；不另寫人物職業表。
- 遭遇 matrix 從 graph 衍生，覆蓋 response、fallback、failure、escape、discard、不可丟棄卡與特殊轉化；D8／D45 另跑具名敘事斷言。
- 第一輪至少走零人物卡與首批三人各一條處方路線；第二輪證明 meta 教學／知識／seen 保留，三筆 P4 run state 全清。
- greedy 使用明示策略跨過兩場遭遇，不偷讀 accepts。若正式卡片損失改變既有 expected，先列因果再更新。
- 收尾：全套 headless → UI sim 正／負 catalog 對數 → 全範圍 review → 真人四項體感；實際數字交 verifier 落檔。

---

## UI 模擬驗證（`tests/ui_sim/`）

> **這一節是工具契約，不綁任何 Phase。** P1-G 是第一個使用者，P2 之後照用。
> 操作層的分工（哪幾條由 runner 跑、哪幾條仍要人玩）住 `測試指南.md > Phase 1`。

### 為什麼要有它

`測試指南.md > Phase 1` 有 **50 條**手動驗收，而**它們全部住在 headless 看不見的那一層**。十套 headless 全綠也看不到畫面——K-28（第 45 天結局文字被狀態列蓋掉）就是從這個縫隙漏出來的。

但純手動有兩個成本會直接壓垮驗收本身：

- **走到那一天很貴。** 第 32 天上午的寺廟，光走過去就要 130 次以上的互動。而 P6 存檔 UI 還沒做，沒有存檔可以跳。
- **修完要重驗。** 每撞到一個 bug，重驗又是一次全程。

所以要把「呈現層的事實判斷」變成可重跑的東西，把人留給只有人能判斷的部分。

### 硬前提：什麼算作弊

**測試開始之後，不得直接呼叫 `GameState` 的規則層入口。**

注入狀態只能發生在**啟動前**。啟動之後所有互動一律走真實輸入事件——找到 Control、算出中心點、發滑鼠事件。

**不准用 `button.pressed.emit()`。** 那樣測到的是訊號接線，不是使用者點得到點不到。用座標點才會順手抓到「被遮住」「超出畫面」「事件被別的節點攔截」這三類問題——它們是自動抓到的，不是靠寫測試的人想到要檢查。

**觀察白名單（唯讀，只有這一個）**：

| 允許 | `GameState.serialize()` —— 案例要靠它比對點擊前後的差異 |
|---|---|
| **禁止** | `build_panel()`、`preview_slot()`、`placeable_cards()`、`has_any_legal_action()`，以及所有命令入口（`try_place()`、`choose()`、`play_beat()`、`advance_phase()`…） |

> **為什麼連唯讀的求值也禁。** `preview_slot()` 回傳的是「規則層認為可以放什麼」，拿它來斷言等於用規則層驗規則層——UI 沒把它畫出來也照樣綠。**要驗的是畫面上有沒有**，所以只能讀 UI dump。`serialize()` 是例外因為它是狀態快照，不是求值。

### 檔案樹

```text
tests/ui_sim/
├── run_ui_sim.ps1        # launcher：建目錄、產狀態、逐個 case 起程序、彙整報告
├── qa_runner.gd          # extends SceneTree：自己載入 main.tscn、跑一個 case、寫產物
├── qa_step.gd            # 單步 helper（下方「固定輸入流程」的唯一實作）
├── qa_hover_regression.gd # K-48 守衛：釘住「送出移動後立刻讀 hover」與「按下放開不讓出執行權」
├── make_states.gd        # 產生情境狀態：走查 + 指定決策，checkpoint 存 serialize()
└── cases/                # 案例：步驟與斷言
_qa/                      # 全部暫存產物（gitignore；內含 .gdignore）
└── runs/<run_id>/        # 一次執行一個目錄，run_id = 時間戳
    ├── godot.log
    ├── states/           # 這一次產生的狀態 JSON
    ├── dumps/            # UI 樹 dump
    ├── shots/            # 截圖
    └── report.json       # 這一次的報告
```

> **產物一定要放在 `runs/<run_id>/` 底下，不能用固定的 `_qa/states/`。** 固定路徑的話，這一次的狀態產生失敗時 runner 會吃到上一次殘留的檔案——**跑出來是綠的，但驗的是舊狀態**。假綠比紅燈危險，這是同一條原則的第二個應用（第一個是 `--state` 三分支）。

### Runner 的啟動架構

**`--script` 跑的是 `MainLoop`，Godot 不會替你起主場景。** 所以 `qa_runner.gd` 要 `extends SceneTree`，自己 `load("res://scenes/main.tscn").instantiate()` 再 `get_root().add_child(...)`。

`qa_runner.gd` 的 CLI（都走 `OS.get_cmdline_user_args()`）：

| 參數 | 用途 |
|---|---|
| `--case <id>` | 要跑哪一個案例 |
| `--state <path>` | 注入的狀態檔（同 `main.gd` 那個，由 runner 轉交） |
| `--data-root <abs-path>` | 資料變體根目錄（同上） |
| `--run-dir <abs-path>` | 這一次的 `_qa/runs/<run_id>/`，產物全部寫這裡 |

**一個 case variant 起一個獨立的 Godot 程序。** 這不是效能取捨，是硬需求——**狀態只能在啟動前注入**，而 D32 ×2、D35／D40／D43、choice 的持卡與不持卡兩份，全都需要不同的起始狀態，共用程序做不到。

`run_ui_sim.ps1` 的職責，順序寫死：

1. 建 `_qa/runs/<run_id>/`（`run_id` 用時間戳）
2. 跑 `qa_hover_regression.gd`（K-48 守衛）。**不過就整套中止，不往下跑。** 這一條守的是「送出滑鼠移動之後立刻讀 hover」與「按下與放開之間不讓出執行權」兩條規則；規則被改回去時，整套 UI 模擬**照樣可能全綠**（紅不紅取決於當下有沒有人動實體滑鼠），所以只能用這支守
3. 跑 `make_states.gd` 產生這一次要用的狀態檔
4. 逐個 case variant 起獨立 Godot 程序，各自帶 `--case` / `--state` / `--run-dir`
5. 收集每個程序的 exit code 與各自的報告，彙整成 `report.json`

**三種情況一律算失敗，不准當成通過**：程序 crash、逾時（launcher 設上限）、非零退出。**沒有回報就是失敗**，不是「大概沒事」。

### 狀態注入

- `main.gd` **在 `_enter_tree()` 注入，不在 `_ready()`**。Godot 的順序是 `_enter_tree()` 由上往下（父先於子）、`_ready()` 由下往上（子先於父）——所以 `main.gd._ready()` 跑在 `hand_bar._ready()` **之後**。放 `_ready()` 注入的話，HandBar 早就讀完舊狀態了。
- 參數走 `OS.get_cmdline_user_args()`（`--` 之後那一段），看到 `--state <path>` 就讀 JSON 丟給 `GameState.deserialize()`。
- **三個分支，不准合併成兩個**：

| 情況 | 行為 |
|---|---|
| 沒傳 `--state` | 正常開新局。正式建置走這條，必須無害 |
| 傳了 `--state` 且成功 | 注入狀態，繼續 |
| **傳了 `--state` 但失敗**（路徑錯、JSON 壞、欄位不合法） | **輸出原因、非零退出、不進遊戲** |

> **第三條不能寫成「照常開新遊戲」。** 那樣 runner 會對著第 1 天的畫面繼續驗下去，然後回報通過——**假綠比紅燈危險**。明確要求了某個狀態卻沒拿到，唯一正確的反應是停下來。

**「欄位不合法」怎麼判斷——`GameState.deserialize()` 自己判斷不了。** 它整份用 `run.get(key, 預設值)` 讀，缺欄位補預設、型別不對就強制轉，而且**沒有回傳值**。少一半欄位的 JSON 餵進去照樣「成功」，只是狀態悄悄不對。

所以檢查要放在 QA loader 裡，**在呼叫 `deserialize()` 之前**做完：

1. **拿當前的 `GameState.serialize()` 當形狀模板**——不要另外手寫一份欄位清單。
2. 遞迴比對必要 key 是否齊全、型別是否相符。**型別比對不可以直接比 `typeof()`**，見下。
3. 另驗兩個值域：`day` 在 1–45、`phase` 是 `PHASES` 四個之一。
4. 任一條不符＝輸出哪個欄位錯、非零退出。

⚠️ **數字型別不能跟模板做嚴格相等——Godot 的 JSON 沒有整數。** 實測（4.6.3，headless）：

```
JSON.stringify({"day": 3})  →  {"day":3}
JSON.parse_string(...)["day"]  →  typeof = 3（TYPE_FLOAT），不是 2（TYPE_INT）
```

`day`、`_madness_counter`、`relations` 的值、`switch_progress` 的值在模板裡都是 `int`，讀回來全是 `float`。**直接比 `typeof()` 的話，每一份合法的狀態檔都會被判成壞檔。**

所以型別比對的規則寫死三條：

- `TYPE_INT` 與 `TYPE_FLOAT` **一律視為同一類「JSON number」**，不分辨。
- 模板那一格是 `int` 時，額外要求該值**有限**（不是 `INF` / `NAN`）且**沒有小數部分**。
- **轉型交給 `deserialize()`**，它本來就用 `int(...)` 包過。檢查層只負責擋，不負責改。

> **用 `serialize()` 當模板是關鍵。** P2-A 到 P2-C 要加 `indulgence_count`、`forced_pending`、`night_markers_opened` 三個欄位——模板會自動跟上，不需要有人記得回來改這份檢查。手寫欄位清單的話，那三個欄位會靜靜地漏檢。

⚠️ **模板驗不到空容器的元素型別，契約只承諾到這裡。** 新局的 `flags`、`relations`、`switches`、`knowledge` 都是空 `Dictionary`，`hand` 是空 `Array`——模板裡看不出元素該是什麼，所以 `{"flags": {"x": "false"}}`（字串而非布林）**推不出是非法的**。

兩條路，**先走第一條**：

1. **縮小承諾**：契約明寫「容器只驗頂層 key 存在與容器型別，不驗元素」。夠用，因為狀態檔是我們自己產的，不是使用者手寫的。
2. 真的需要時再為這幾個容器補 path-specific 的元素檢查（`flags` 值必須是 `bool`、`relations` 值必須是 number……）。**現在不做**——只有一個產生者的檢查不是檢查。

- 這個入口只在 debug／QA 執行啟用。
- `deserialize()` 不碰 `Data`，所以 autoload 有沒有載完不影響這一步。

### 情境狀態：產生的，不手寫，不進版控

**手寫的狀態 fixture 一定會過期。** `GameState` 每長一個欄位，手寫的那份就少一欄——P2-A 到 P2-C 馬上要加 `indulgence_count`、`forced_pending`、`night_markers_opened` 三個欄位，加上 `madness_clock` 開始有值。

所以：

- 情境 ＝ **走查策略 ＋ 一小段指定決策**，`make_states.gd` 每次跑測試時現生。全程走查只要幾秒。
- **指定決策是必要的，不是選配。** 第 32 天上午的寺廟需要三個不同狀態——邀阿婕、邀阿薇、兩者皆無——而貪心走查只會給你其中一條路。**generator 一律產三份；P1-G 第一批只用到「邀阿婕」與「兩者皆無」兩份，第三份先產著。** 這一句要明寫，否則實作者會自己挑要產幾份。
- 產生出來的 JSON 放 `_qa/runs/<run_id>/states/`，**不進版控**。進版控的是產生它的策略與決策清單。

**`run_greedy_walk()` 現在的形狀擋住這件事，要先改它。** 核對過 `tests/headless/playthrough_greedy.gd`：簽名只有 `(gs, data_node, verbose)`，**沒有任何 hook**，而且迴圈一進 morning 就直接 `execute_action_phase()`。照現況取快照的話：

- D22 的快照**行動格已經花掉了**，choice 相關的案例站不住
- D32 的快照**寺廟的 beat 已經結算完**，「演出逐一」根本沒東西可演
- **產不出**阿婕／阿薇／無邀請三個分支

所以 `make_states.gd` 的契約要補五樣，缺一樣就產不出可用的快照：

| 要補 | 內容 |
|---|---|
| **決策格式** | `{day, phase, location, beat_id, slot_id, card_id}` 之類的一筆一個動作。**明寫欄位，不要留給實作者發明** |
| **決策失敗即中止** | 指定的動作當下不合法（槽不在、卡沒有、格已用掉）＝**立刻中止並報哪一筆錯**。不准跳過繼續走——跳過的話後面每一份快照都是錯的，而且看起來正常 |
| **phase 執行前的 checkpoint** | 每個時段**執行動作之前**給一次快照機會。D22／D32 要的正是「已經走到那一天，但這一格還沒動」 |
| **快照命名** | 固定規則（例：`d32_morning__ajie.json`），launcher 與 case 靠它對上 |
| **每份狀態的後置條件** | 產完當場斷言（例：D32 那份要 `invited_ajie` 為真、`action_spent` 為假）。**產錯了要當場紅**，不要等到 case 跑起來才發現畫面不對 |

### 資料變體（改 `tuning.json` 那一類案例）

`測試指南.md > P1-B` 有一條要「改 `hand_size` 後重啟即反映」。**絕對不能改正式 `data/`。**

做法：`make_states.gd` 把 `data/` 複製到 `res://_qa/data_variant/`，只改副本裡要動的那個值。

**傳入介面（debug-only，與 `--state` 同一組）**：

```
--data-root <absolute-path>
```

| 情況 | 行為 |
|---|---|
| 沒傳 | 用 `Data` autoload 開機時載好的 `res://data/`，不重載 |
| 傳了且載入成功 | 重新載入該資料根，**然後**才處理 `--state` |
| **傳了但載入失敗** | 輸出原因、非零退出、不進遊戲（同 `--state` 第三分支） |

- **由 `main.gd._enter_tree()` 處理，順序寫死：先 `--data-root` 再 `--state`。** 反過來的話狀態注入完才換資料，`hand_size` 這種上限值會對不上剛注入的手牌。
- ⚠️ **路徑要正規化，尾端斜線由 `main.gd` 補。** `DataLoader` 是直接字串相接的：

  ```gdscript
  DATA_DIR = data_dir
  BEATS_DIR = data_dir + "beats/"
  ```

  傳 `C:\...\_qa\data_variant` 進去會組成 `data_varianttuning.json` 與 `data_variantbeats/`——**兩個路徑都錯，而且錯得很難看出來**。正規化三步，寫死在 `main.gd`，不要改 `DataLoader`：① `\` 轉 `/` ② 去掉既有的尾端 `/` ③ 補一個 `/`。先正規化再交給 `Data.load_data()`。
- `Data` autoload 的 `_ready()` 在主場景之前就跑過了，所以 `--data-root` 實際上是**重載一次**。多載一次的成本在 QA 執行可接受，換來的是不用去動 autoload 的啟動順序。

這條路走得通，因為 `DataLoader._init(data_dir)` 把 `DATA_DIR` 與 `BEATS_DIR` **兩個都從參數推出來**（`BEATS_DIR` 是 `var` 不是 `const`），`Data.load_data()` 也已經有這個參數——它本來就是給壞資料 fixture 用的豁免口，這裡是第二個使用者。

> **`_qa/` 要放一個 `.gdignore`。** 否則 Godot 會去掃裡面的截圖、JSON 與 log，污染 `.godot/imported` 並拖慢每一次啟動。`.gdignore` 只擋編輯器掃描，`DirAccess` 與 `FileAccess` 照樣讀得到，所以資料變體不受影響。

### `qa_id` 命名規約

動態建立的控制項要掛 `qa_id` metadata（`set_meta("qa_id", ...)`）。**不准靠中文字或節點順序找按鈕**——中文字在 i18n 管線之後會全爛，節點順序改一次版面就爛。

分隔符一律 `::`，與 `實作規格書.md > 六` 的槽身份鍵（`beat_id + slot_id`）一致：

```
location::<location_id>          # 地圖上的地點按鈕
panel_back                       # 離開面板
phase_advance                    # 主畫面的時段推進按鈕
beat_advance                     # 演出階段的「按一下推進下一個 beat」
slot::<beat_id>::<slot_id>       # 槽本身的 Label（右鍵預覽的目標）
preview::<beat_id>::<slot_id>    # 左鍵「預覽」按鈕
dialog_confirm::preview          # 預覽 AcceptDialog 的關閉鈕
place::<beat_id>::<slot_id>::<card_id>
choose::<beat_id>::<group_id>::<slot_id>
```

- 掛在 `map_list.gd`（地點按鈕）與 `location_panel.gd`（槽 Label、預覽鈕、選項鈕、放置鈕）、`main.gd`（推進鈕）。
- ⚠️ **預覽有兩個入口，兩個都要有 `qa_id`。** 核對過 `location_panel.gd`：一個是 `_beat_container` 底下的左鍵「預覽」Button，另一個是槽 Label 的 `gui_input` 收右鍵（`_on_slot_gui_input`）。兩者最後都走 `_show_preview()`，但玩家用的是不同的手，**P1-G 第 7、8 條驗的是右鍵那個**。只掛 `preview::` 的話那兩條沒有目標可點。
- ⚠️ **預覽開的是 `AcceptDialog`，關閉也要有目標。** P1-G 第 7 條要驗「關掉之後狀態全部不變」——關不掉就驗不了。`dialog_confirm::preview` 掛在該 dialog 的確認鈕上；不想掛的話要另外提供一個 `InputEventKey` 送 Escape 的 helper，**二選一寫死在契約裡，不要讓各案例自己想辦法**。
- **`qa_id` 只給測試用，不是玩家可見字串，不進 i18n 清單。**
- 寫案例時 `<beat_id>` 這些要用資料裡真的存在的 id，**動工時逐個 grep 核對**。示意 id 寫進契約會被下一個人當成真的。

### 固定輸入流程（寫死在 `qa_step.gd`，案例只能呼叫它）

**所有案例一律呼叫這個 helper，不得自行 timer、不得自訂等待幀數。**

1. 等場景完成 **兩次** `RenderingServer.frame_post_draw`。
2. 依 `qa_id` 找 Control，然後確認：
   - **命中數恰好 1 個。** 0 個或 2 個以上都是失敗，**不准任取第一個**——重複的 `qa_id` 代表 UI 建了兩份，那本身就是 bug。
   - `is_visible_in_tree()` 為真。
   - **`disabled` 只有在 `target is BaseButton` 時才檢查。** `disabled` 是 `BaseButton` 的屬性，而 `slot::<beat>::<slot>` 明確指向 `Label`——對 Label 讀 `disabled` 會直接噴錯。
   - 中心點落在 viewport 範圍內。
   - **另提供「預期 disabled」模式**：驗灰掉的控制項時，`disabled` 為真才算通過。沒有這個模式的話，「灰槽」那幾條驗收沒有寫法。
3. 用 `Input.parse_input_event()` 送 `InputEventMouseMotion` 到中心點。**（有 hover 狀態的元件需要它；成本是零，省掉一整類「只有某幾個按鈕會抖」的追查。）**
4. **立刻**讀 `Viewport.gui_get_hovered_control()` 核驗命中，**中間不准等幀**（見下方 K-48 警告）。沒中就重讀矩形、重送事件再試，上限 5 輪；**要等排版落定就等在下一輪的開頭**。
5. 用 `Input.parse_input_event()` 送 `InputEventMouseButton`，`pressed = true`。**`button_index` 由案例指定**——P1-G 有兩條驗收是右鍵預覽，不能寫死左鍵。
6. **緊接著**送 `InputEventMouseButton`，`pressed = false`。**按下與放開之間不准等幀**（同一個 K-48 警告）。
7. 等 **兩次** `frame_post_draw`。
8. 做狀態斷言、UI dump、截圖。
9. **斷言失敗時，用 `Viewport.gui_get_hovered_control()` 取當下命中的 Control**，印出它的節點路徑與 `qa_id`。

> **事件一律走 `Input.parse_input_event()`**（Godot 官方 API），不要自己呼叫 `Control._gui_input()` 或 `Viewport.push_input()` 繞過事件系統——繞過去就等於沒在測命中。

> ⚠️ **第 3→4 與 5→6 這兩段之間，一格幀都不准等**（K-48 真因，2026-08-20 定案）。
> 案例是**開真視窗**跑的，桌面上還有一支實體滑鼠。`gui_get_hovered_control()` 記的是
> 「最後一個滑鼠移動事件打到誰」，不是每幀重算的值；`BaseButton` 的 `pressing_inside`
> 也一樣會被 `MOUSE_EXIT` 清掉。只要在這兩段之間讓出執行權，使用者手動一下滑鼠，
> 作業系統補進來的真實移動事件就會蓋掉狀態，於是出現兩種症狀：
> 「預期某按鈕、實際某個不相干的容器」（座標明明是對的），以及「click 回報成功但畫面沒反應」。
> **這種紅不可重現**——紅不紅取決於當下有沒有人碰滑鼠，不取決於程式對不對，所以 K-48 才會
> 復發兩次。守衛住在 `qa_hover_regression.gd`，由 launcher 第 2 步強制執行。

> **第 9 步不是裝飾。** 被遮住的時候，點擊會落到遮住它的那個節點，斷言只會說「狀態沒變」——那種報告會讓人跑去查規則層，而問題在畫面。有了命中掃描，報告直接說「被 `FlowText` 擋住」。

> **為什麼禁用 timer。** 用時間等會做出一套**會偶爾紅的驗收**，而那比沒有驗收更糟——之後每次紅都要先判斷「是真的壞了還是又抖了」，判斷成本會吃掉這套工具全部的價值。幀數是決定論的，時間不是。

### 矩形診斷（真實輸入抓不到的那一半）

**真實輸入抓的是「互動元件點不到」。K-28 與 K-40 抓不到。**

⚠️ **不要寫成「Label 不吃點擊」——那是錯的。** `location_panel.gd` 的槽 Label 就明確設成 `MOUSE_FILTER_STOP` 並接了 `gui_input`（右鍵預覽靠它）。正確的說法是：**K-28／K-40 那些文字 Label 沒有互動語意**，沒有人要點它們，所以「讀不讀得到」無法由操作結果推出來，只能用幾何判斷。

所以要另外做一組**幾何診斷**，跟輸入測試並行，**每個案例都跑一次**。

**遮蓋判定（K-28 那一族）。** 天真的做法是「算 `get_global_rect()` 相交」，那會噴滿假陽性——父子、Container 與內容、背景與文字本來就一直相交。**假陽性多的診斷會被人關掉，等於白做。** 所以五條防呆一條都不能少：

1. 用 `is_visible_in_tree()`，不是只看 `visible`。父層隱藏時子節點的 `visible` 仍可能是 `true`。
2. **排除祖先與子孫**——它們相交是正常的。
3. 限制在同一個 Viewport／CanvasLayer 內比較。跨層的重疊是設計，不是 bug。
4. 排除沒有實際繪製內容的結構型 Control（純 Container、`Panel` 之外的空容器）。
5. 只有**繪製順序在後**且真的畫了東西的節點，才算「蓋住」。

**溢出判定（K-40 那一族）。**

- **`ScrollContainer` 的內容比可視區大不是錯**，那正是它存在的理由。只有這三種才算失敗：**沒有裁切**（`clip_contents` 為假）、**捲不到**（內容超出但沒有可用的捲軸）、**侵入保護區域**（壓到狀態列或地點清單那一類固定元件）。
- 比較 `get_combined_minimum_size()` 與實際配置矩形，**不要只看 `get_global_rect()`**——後者只告訴你它現在多大，不告訴你它需要多大。

**截圖一起存**，人要覆核時看得到。

**兩套一起做才完整，不能拿其中一套取代另一套。**

### 負向驗證不擴充正式 UI

`location_panel.gd` 的放置按鈕**只為 `GameState.placeable_cards()` 回傳的卡建立**（核對過）。所以不相容的卡根本沒有可點的 `place::` 控制項——在「開始後禁止直呼 GameState」的前提下，runner **做不出「把不相容的卡放進槽」這個操作**。

**不要為了測試在正式 UI 上長出一顆「放不進去的卡」的按鈕。** 那個 UI 的設計本來就是「不能放的卡不給你按鈕」，那本身是正確行為；測試需求不該反過來改變產品的形狀。

改成三條分工：

| 驗什麼 | 誰驗 |
|---|---|
| 未列出的卡**沒有** `place::` 控制項 | runner（UI dump 找不到即通過） |
| 預覽列出的每一張卡，經 UI 放下去**都成功** | runner（真的點） |
| 規則層「不相容的卡被拒收、狀態零變化」 | **headless**，維持現狀 |

> 三條合起來才等於原本那一條的意圖。少了第一條，UI 可能悄悄長出不該有的按鈕而沒人發現。

### 兩層驗證

| 層 | 做什麼 | 抓得到什麼 | 抓不到什麼 |
|---|---|---|---|
| **精準情境** | 注入狀態直接到第 9／22／32／35／40／43／45 天 | 各條件組合下的呈現與互動，快 | **跨日流程**——跳關把它跳掉了 |
| **完整走查** | 從第 1 天開始，**全部互動走真實輸入事件**，不注入任何狀態 | 日夜切換、殘響、結局 coda、迴圈重置 | 慢；只走一條路徑 |

**兩層都要有。** 只做第一層會讓「第 45 天 coda 播畢回到第 1 天」這種東西永遠沒被真的走過——而那正是 P1-F 的核心驗收。

### 產物分工

| 進版控 | 不進版控（全部放 `_qa/`） |
|---|---|
| `qa_runner.gd` / `qa_step.gd` / `make_states.gd` | 產生的狀態 JSON |
| `cases/` 底下的步驟與斷言 | UI dump |
| 走查策略與指定決策清單 | 截圖 |
| `map_list.gd` / `location_panel.gd` 的 `qa_id` | 執行 log 與 `report.json` |

`_qa/` 要加進 `.gitignore`。

### 執行方式

- **不能用 `--headless`。** 真實滑鼠事件的座標命中需要真的排版與 viewport；截圖需要繪製裝置。兩樣 headless 都沒有。
- ⚠️ **載入主場景之前，先寫死 `get_root().gui_embed_subwindows = true`。** 預覽開的是 `AcceptDialog`，沒有內嵌的話它是一個獨立的 OS 視窗——**不在主 viewport 的座標系裡，也不在截圖裡**，於是 `dialog_confirm::preview` 找得到卻點不到。
  實測（4.6.3 headless）：`ProjectSettings` 的 `display/window/subwindows/embed_subwindows` 是 `true`，但 `get_root().gui_embed_subwindows` 讀到 `false`。**執行期值不保證等於專案設定**，所以不能靠 `project.godot`，要在 runner 裡明寫。（有視窗的情況未實測，寫死之後兩種情況都對。）
- 滑鼠事件的 `position` 與 `global_position` **兩個都要設**。若之後真的需要非內嵌的多 Window，還得處理 `window_id`——**現在不做**，靠內嵌避開。
- log 用 `--log-file`，**傳絕對路徑**，並由執行腳本預先建立 `_qa/`。這是為了完全避開 `user://logs`——`AGENTS.md > Godot Headless 驗證` 記著那條路在 sandbox 內會 crash（signal 11）。
- ⚠️ **`--log-file` 吃相對路徑會解析成什麼，尚未實測。** 第一次跑完要確認 `%APPDATA%\Godot\app_userdata\` 底下沒有長出東西再繼續。**不要把這個 flag 的行為當成已知事實**，那顆雷已經炸過一次。

### 實作順序

**59 條手動驗收的實際分類**（`測試指南.md > Phase 1` 重數）：

| | 條數 | 狀態 |
|---|---:|---|
| UI runner 要接的 | **57** | — |
| ├ P1-G 第一批 | 14 | ✅ |
| ├ P1-A～F 後續 | **33** | ✅（以上兩批共 47 條、59 個案例變體，全綠） |
| └ P1-H 手牌可讀性 | **8** | ✅（併入後共 53 條契約、65 個案例變體；P2-A 補 `p1af_29_night_paid` 後為 **66** 個變體，契約數不變） |
| └ P2-A 多實例發狂卡顯示 | **1** | ✅（`p2a_01_madness_hand_display`，K-51 結案；54 條契約、67 個變體） |
| └ P2-B 縱慾出口 | **4** | ✅（`p2b_01_exit_visibility` 2 變體／`p2b_02_exit_place`／`p2b_03_soak` 3 變體／`p2b_04_exit_thresholds` 3 變體；58 條契約、76 個變體） |
| └ P2-C 強制縱慾 | **3** | ✅（`p2c_01_forced_text`／`p2c_02_forced_action_spent`／`p2c_03_forced_two_same_day`，各 1 變體，K-61 結案；61 條契約、79 個變體） |
| └ P2-D 視野與 BE | **2** | ✅（`p2d_01_vision_visibility` 2 變體讀畫面「二樓有人在走」、`p2d_02_be_screen` 1 變體讀 UI dump；K-68 三腳齊備並於 2026-08-22 P3-D 結案，第三腳「清回 2 張又消失」在 `p1af_30`。**63 條契約、82 個變體**） |
| └ P3-D 夜間清單與詳情 | **6** | ✅（`p3d_01_night_status_texts` 3 變體／`p3d_02_night_aligned_names` 2 變體／`p3d_03_night_detail_gated` 2 變體／`p3d_04_night_risk_warning`／`p3d_05_sleep_pending_disabled`／`p3d_06_night_list_geometry`；K-91／K-115 結案；`70f4fc5` 補齊，**69 條契約、92 個變體**） |
| 純人工（感受，runner 接不走） | 3 | 待人 |
| 命令列（跑 `verify_data` / `test_boot`，不點畫面） | 1 | ✅ |

順序：

1. **會走路的骨架**：`main.gd` 兩個參數 → `qa_runner.gd` 起主場景 → `qa_step.gd` 九步 → **跑通一個最簡單的案例**（建議 P1-G 第 4 條槽型別標示：不跳天也不碰夜間）。
2. 拿實跑結果**回填產物格式**——case 介面、UI dump 欄位、`report.json` schema、幾何診斷的判準。**這些是工具自己的產物，不是遊戲程式要遵守的契約**，所以先跑再落檔；契約該先寫的部分（上面各節）已經寫完。
3. 補齊 P1-G 全部 14 條。
4. 管線證明可信之後，再把其餘 33 條加進去。
5. 完整走查那一層最後做。

> **五步都做完了**（2026-08-20）。全套 **82** 個案例變體、**63** 條契約、11 條負向反證測試（沿革：P1-H 併入前 59 變體／47 契約 → P1-H 併入後 65／53 → 2026-08-16 P2-A 拆 `p1af_29_night_paid` 變 66／53、補 `p2a_01` 變 67／54 → P2-B 補 4 條契約 9 個變體變 76／58 → P2-C 補 3 條契約 3 個變體變 79／61 → P2-D 補 2 條契約 3 個變體變 **82／63**）。契約完成判定住 `qa_contract_matrix.gd`，**evidence 分兩層查**：變體層要求每個變體送齊分派給它的 token（`VARIANT_EVIDENCE`），契約層再查整份 token 與變體是否漏跑。兩層都在的理由是——只查契約層時，同一契約的另一個變體會把缺的 token 補掉，缺陷就看不見了。

> **一個會說謊的驗收工具比沒有工具危險。** 14 條夠證明管線是通的，不夠的話再多寫 30 條也只是把錯誤放大。

---

## 跨 Phase 實作紀律

- **Phase 動工的第一件事是把驗收抄成空測試，不是寫實作。** 把 `實作規格書.md > P1-X 驗收` 的每一條抄成一個測試函式名，函式體先 `return _fail("TODO: <驗收原文>")`。收尾時清單上不准留 TODO——留著就是還沒做完。這條擋的是「驗收寫了七條、測試只覆蓋三條」那種漏法：P1-A 的驗收有 7 條，第一版測試漏掉一致性驗證是否真的擋開機、45 天全程推進、以及兩條面板斷言，而三套測試全綠（`驗證後已知問題.md > 附`）。**機械式執行、不需要判斷力**，所以趕進度時也跳不掉。
- API 簽名以本檔為準寫，但**動工前先 grep 現檔核對**（AGENTS.md 實作守則第 1 條——本檔可能落後於程式碼，落後時改本檔）。
- **凡是 UI 與 headless 走查都會用到的動作，本檔要明寫入口住規則層，並列出簽名。** 只寫「做什麼」不寫「住哪一層」，實作者會照最近的讀法擺：`enter_beat` 的「面板開啟走這條」被讀成 UI 的事，於是 `location_panel.gd` 有、規則層沒有，走查那條路上 28 個 fixed beat 的 `on_enter` 一次都不結算（`驗證後已知問題.md > K-01`）。`end_run()` 是同一個病的第二例。**「UI 零規則」是總則，總則不會自己下放到個案**——每個 Phase 的契約要自己標。格式照 P1-F 結局 coda 那三條：誰呼叫、內部順序、以及「後置條件不可依賴外部呼叫者」。
- 承上，**測試也要標明走哪條路**。實作者寫的測試會自然走自己剛走過的那條，兩條路的等價性因此沒人驗——`test_boot.gd` 是這個問題的第一個解藥（正式啟動路徑 vs 注入路徑），但它只守開機。新增規則層入口時，UI 路徑與走查路徑各要有一條斷言。
- `core/` 不准 import `scenes/` 任何東西；違反即架構錯誤。
- **新增 `.gd` 時，同名 `.uid` 一起進版控。** Godot 4 為每個腳本生一個 `.uid`，沒進 git 的話每個 clone 各自生一個隨機值，同一份腳本在不同機器上是不同 UID——`.tscn` 用 UID 引用腳本時就會斷。收尾前用 `git status` 看有沒有 `?? *.gd.uid`，有就一起 `git add`。**這條寫下來是因為它已經發生三次**：`test_p1e.gd.uid`（K-23）、`playthrough_greedy.gd.uid`（K-38）、`test_p1g.gd.uid`。前兩次都當成個案補檔，沒有留規矩，所以第三次照樣發生。
- 每個子階段收尾：headless 全綠（`verify_data.gd` ＋ `test_boot.gd` ＋ 既有測試）才算完成；編譯錯誤同 turn 修完（實作守則第 3 條）。`test_boot.gd` 每次都要跑——**接線越多，正式啟動路徑越容易在沒人看的地方斷掉**。
- 每個子階段收尾**重跑序列化往返測試**：新增的狀態欄位（手牌、旗標、事件、選擇……）都要接進 `serialize()` / `deserialize()` 並驗往返相等，不是 P1-A 跑一次就算（規格書 Phase 1 通則驗收）。
- 破壞性測試（壞 JSON、未知語彙鍵）一律用 `tests/fixtures/broken/` 的 fixture，**不改正式 `data/`**。
