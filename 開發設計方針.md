# 開發設計方針

> 實作層文件：檔案結構、Autoload 簽名、signal、接線規則、實作決策。
> **角色歸屬**：implementer-owned（AGENTS.md 實作守則第 4 條）。驗收意圖住 `實作規格書.md`，測試流程住 `測試指南.md`，已知問題住 `驗證後已知問題.md`（第一波驗證收尾時才建）。

## 本檔範圍與邊界

| 寫什麼 | 去哪 |
|---|---|
| 引擎必須做什麼、各 Phase 驗收意圖 | `實作規格書.md` |
| 規則的形狀與為什麼 | `溫泉小鎮企劃.md` |
| 資料欄位 | `data/SCHEMA.md`；可調數值 `data/tuning.json` |
| **檔案清單、autoload 簽名、signal、接線、實作決策** | **本檔** |
| 操作層驗收步驟 | `測試指南.md` |

## 起始範圍

- 本檔自 **Phase 1** 起寫。資料層時期（`git log` 裡 Phase 1 之前的全部 commit）的細節不 backfill，以 `data/SCHEMA.md` 與 git log 為準。
- 現有程式資產：`scripts/data_loader.gd`（`class_name DataLoader`，載入＋引用檢查，**沿用不改寫**）、`scripts/verify_data.gd`（headless 資料驗證，會隨 lint 需求擴充）。
- 完成的 Phase 段落照歸檔紀律搬 `subdocs/歸檔/開發設計方針_歸檔.md`，原位留同標題 stub。

## Phase 分期總表

> 狀態住 `PROJECT_BRIEF.md > Phase 進度`，本表只管內容。展開細節於各 Phase 排到時才寫（規格書同步）。

| Phase | 一句話 |
|---|---|
| **1 最小可玩迴圈** | 選地點 → 放卡 → 推進，全文字，45 天走到底（本檔下方，可動工） |
| 2 發狂時鐘與縱慾 | 各走各的鐘、強制縱慾演算法、備用區與超載；驗收＝headless 重演 `發狂卡機制模擬.md` 三種玩家 |
| 3 夜間層真值化 | 標記收費、門檻灰掉、章節變體、對位改名、meta 免費 |
| 4 委託與遭遇 | 人物卡外出／回報、遭遇回合循環、第 8 夜與第 45 天真值化 |
| 5 結局、開局與迴圈 | 三結局真流程、開局十分鐘選單、繼承完整版 |
| 6 存檔 UI | 槽位、version 與遷移、輪中存讀 |
| 內容期 | 文本、演出、UI 正式化、i18n、第二輪差異包——**引擎期玩過之後才排** |

---

## Phase 1 — 最小可玩迴圈（實作契約）

> 驗收意圖見 `實作規格書.md > Phase 1`；操作清單見 `測試指南.md > Phase 1`。
> 每個子階段完成後，遊戲都必須保持可跑（`main.tscn` 起得來、headless 驗證綠）。

### 全域結構決策（P1 期間建立，之後 Phase 沿用）

目標檔案樹（P1 結束時）：

```text
scripts/
├── autoload/
│   ├── data.gd          # Autoload「Data」：啟動時跑 DataLoader，失敗擋進遊戲
│   └── game_state.gd    # Autoload「GameState」：全部 runtime 狀態（規格書第一節窮舉）
├── core/                # 純邏輯，零 UI 依賴，headless 直接測
│   ├── condition_eval.gd   # class_name ConditionEval（static）
│   ├── effect_apply.gd     # class_name EffectApply（static）
│   ├── panel_builder.gd    # class_name PanelBuilder（static）
│   └── data_facts.gd       # class_name DataFacts：資料層既定事實常數（by_design 空格名單等）
├── data_loader.gd       # 既有，不動
└── verify_data.gd       # 既有，隨 lint 擴充
scenes/
├── main.tscn / main.gd  # 宿主：按 GameState 時段切換顯示流
└── ui/                  # 全文字元件（正式 UI 會整層換掉，見規格書架構基線第 2 條）
    ├── hand_bar.tscn        # 手牌一排＋知識清單
    ├── map_list.tscn        # 地點清單（文字按鈕）
    ├── location_panel.tscn  # 面板：beats 與槽
    └── flow_text.tscn       # evening／夜間 stub／結局的逐行文字流
tests/
├── headless/
│   └── playthrough_greedy.gd   # P1-F 自動走查（SceneTree script）
└── fixtures/broken/            # 壞資料 fixture（破壞性測試不碰正式 data/）
```

- **Autoload 只有兩個**：`Data`（唯讀資料）與 `GameState`（可變狀態）。UI 一律讀這兩個、發意圖呼叫 GameState 方法；**UI 之間不互相引用、不持有狀態**。
- **規則層入口統一，UI 零規則**：合法性判斷與結算只存在於 `GameState.try_place()` / `choose()` / `enter_beat()` / `advance_phase()` 與 `PanelBuilder.build()` / `available_locations()`。UI 與 headless 走查呼叫**同一組 API**；UI 內出現任何 accepts／三態／action_spent 判斷＝架構錯誤。
- `DataLoader` 的路徑常數改為可選建構參數（預設 `res://data/`），供壞資料 fixture 測試載入 `tests/fixtures/broken/`——這是「沿用不改寫」的唯一豁免。
- `core/` 三個 class 全部 static、無狀態、不 preload 任何場景——這是「headless 可驗證」（規格書架構基線第 5 條）的落實位置。
- 玩家可見字串不寫在 `.gd` 裡；引擎自己的提示（stub 文字、錯誤）先集中在各檔頂部 const，方便日後抽 i18n。
- Headless 執行方式與 sandbox 限制照 `AGENTS.md > Godot Headless 驗證`（escalated 權限、先 `--import`）。

### P1-A 遊戲狀態與時段狀態機

動的檔：`scripts/autoload/data.gd`（新建）、`scripts/autoload/game_state.gd`（新建）、`project.godot`（註冊兩個 autoload）、`scenes/main.tscn/gd`（顯示當前天／時段／章節的最小殼）。

`Data`：

```gdscript
var loader: DataLoader     # _ready 時 load_all() + verify_references()，任一失敗 → ok = false
var ok: bool
func tuning(key: String, fallback: Variant = null) -> Variant   # 讀 tuning.json，點路徑（"indulgence.soak_phase_cost"）
```

`GameState`（本子階段只做時間群與序列化骨架，其餘群先立空欄位）：

```gdscript
# 章節界線與時段名都是結構常數（規格書第二節），集中在這裡
const CHAPTER_START_DAYS := [1, 16, 33]
const LAST_DAY := 45
const PHASES := ["morning", "afternoon", "evening", "night"]
const ACTION_PHASES := ["morning", "afternoon"]   # tuning.phases_per_day 只做一致性驗證（Data 啟動 lint）

var day: int               # 1..45
var phase: String          # "morning" | "afternoon" | "evening" | "night"
func chapter() -> int      # 由 day 導出，不儲存

signal phase_changed(day: int, phase: String)
signal day_changed(day: int)
signal chapter_changed(chapter: int)
signal run_ended(ending_id: String)

func advance_phase() -> void    # morning→afternoon→evening→night→次日 morning；
                                # 第 45 天：afternoon → evening（結局 coda，播 d45_then）→ emit run_ended，不進 night
func serialize() -> Dictionary      # { "run": {...}, "meta": {...} } 兩層分開（規格書第十六節）
func deserialize(d: Dictionary) -> void
```

- 行動格的「已放主角卡」旗標（`action_spent: bool`）本子階段就進 run 層，P1-D 才有人寫它。
- `main.gd` 只做：檢查 `Data.ok`（失敗顯示錯誤停住）→ 顯示狀態列 → 一顆「推進」鈕呼叫 `advance_phase()`。

### P1-B 卡片與手牌

動的檔：`game_state.gd`（手牌群）、`scenes/ui/hand_bar.tscn/gd`（新建）、`main.tscn`（掛 hand_bar）。

`GameState` 增加：

```gdscript
var hand: Array[String]              # 佔格卡 id，有序；主角卡恆在 index 0
var knowledge: Dictionary            # id -> true（Set；knowledge 卡唯一的家，屬 meta 層——serialize 進 "meta"，end_run 不清）
var madness_clock: Dictionary        # 發狂卡實例 id -> 剩餘天數（P1 只建結構，P2 才走錶）

signal hand_changed
signal knowledge_changed

func gain_card(id: String) -> void   # 按 Data 卡型別分流：slotless → knowledge；其餘 → hand。
                                     # 除 madness 外全卡 unique：已持有＝no-op（冪等，規格書第三節）
func lose_card(id: String) -> void   # 可作用於 hand 與 knowledge（規格書第七節）；
                                     # lose 主角卡 → push_error（資料錯誤，不是玩法）
func has_card(id: String) -> bool    # hand ∪ knowledge（規格書第十四節：備用區不算）
func has_knowledge(id: String) -> bool
func hand_slots_used() -> int
```

- 型別操作矩陣（規格書第三節）P1 只落實兩行：protagonist 釘死（`lose_card` 擋）、knowledge 不進 hand。其餘矩陣行為到各自 Phase 才有入口，不預寫。
- 發狂卡是同一張卡的多實例：`hand` 裡存實例 id（`"madness#1"`），卡面查 `Data` 時剝掉 `#` 後綴。實例編號計數器進 run 層。
- `hand_bar` 訂閱兩個 signal 重繪；顯示佔格數／上限（`Data.tuning("hand_size")`）。

### P1-C 地圖、面板與三態

動的檔：`scripts/core/condition_eval.gd`（新建）、`scripts/core/panel_builder.gd`（新建）、`scenes/ui/map_list.tscn/gd`、`scenes/ui/location_panel.tscn/gd`（新建）、`game_state.gd`（事件群：`beats_entered`）、`main.gd`（行動時段顯示地圖）。

```gdscript
# ConditionEval —— 規格書第十四節的十個運算子，封閉語彙
static func eval(cond: Variant, gs: GameState) -> bool   # null / 缺欄 = true；未知鍵 push_error + return false

# PanelBuilder —— 純函式：算出一個地點面板的 view model
static func build(location_id: String, gs: GameState, data: Data) -> Dictionary
# 回傳：{ "beats": [ { "beat": Dictionary, "tri": TriState, "reason": String,
#                      "slots": [ { "slot": Dictionary, "tri": TriState, "reason": String } ] } ] }
# TriState：HIDDEN / LOCKED / OPEN / RESOLVED（規格書第五節求值順序；含槽級 condition、
#           一次性槽收起、choice_group 收起與被選項 RESOLVED、evening 主角卡槽強制 LOCKED）
static func available_locations(gs: GameState, data: Data) -> Array[String]
# 當前天／時段可去的地點 id（layer / chapter / phases 過濾）。map_list 與走查腳本共用
```

```gdscript
# GameState —— beat 呈現的唯一入口（規格書第四節）
func enter_beat(beat_id: String) -> PackedStringArray
# 一次性判定（beats_entered）→ on_enter 結算 → 回傳要播的文字行。
# 面板開啟、evening 演出、夜間層、結局 coda 全部走這條
```

- `beats_at(day, phase)` 沿用 DataLoader 既有方法；三態求值進 PanelBuilder，**不改 DataLoader**（fixture 路徑參數除外，見全域結構決策）。
- `map_list` 只渲染 `available_locations()` 的結果，自己不過濾。
- P1-C 期間 `enter_beat` 內部先用最小效果結算（只支援 `text` 與 `gain`），P1-D 換成正式 `EffectApply`——這是唯一允許的暫時簡化，P1-D 收掉。
- evening 流程：`flow_text` 逐條對當日 `when.phase == "evening"` 的 fixed beat 呼叫 `GameState.enter_beat()` 播回傳文字（跨地點全部，規格書第四節例外條）——**第 1 天到站發主角卡走的就是這條**。
- 面板內任何狀態變化 → 重呼叫 `PanelBuilder.build` 整包重繪（P1 資料量下全量重算即可，不做增量）。

### P1-D 放置與效果結算

動的檔：`scripts/core/effect_apply.gd`（新建，收掉 P1-C 內聯版）、`location_panel.gd`（放卡流程）、`game_state.gd`（旗標／開關／關係群＋`consume_action()`）、`data.gd`（啟動時語彙 lint）。

```gdscript
# EffectApply —— 規格書第十四節效果鍵，固定順序 text → lose → gain → 其餘
static func apply(effect: Dictionary, gs: GameState) -> PackedStringArray  # 回傳 UI 要播的文字行

# GameState 增加
var flags: Dictionary          # name -> true
var switches: Dictionary       # id -> true
var switch_progress: Dictionary# id -> int
var relations: Dictionary      # npc -> int（單軸整數暫行案，規格書第十二節）
var action_spent: bool
var slots_placed: Dictionary       # "beat_id::slot_id" -> true（槽預設一次性，規格書第六節）
var npc_action_counts: Dictionary  # npc_id -> 本輪投入的主角行動數（規格書第十二節；run 層、入序列化）
func consume_action() -> void  # 標記當前行動格已用；advance 時檢查
func set_flag(name: String) / open_switch(id: String) / add_switch_progress(id: String, n: int)
func add_relation(npc: String, delta: int)

# 放卡的唯一入口（UI 與 headless 走查共用；UI 內不做任何判斷）
func try_place(card_id: String, beat_id: String, slot_id: String) -> Dictionary
# 回傳 { "ok": bool, "reason": String, "lines": PackedStringArray }
```

`try_place` 內部順序（全部在規則層，效果結算前檢查完；規格書第六節的四步）：

1. **持有**：`card_id` 此刻必須在 `hand` 或 `knowledge`（發狂卡實例剝 `#` 後綴再查卡表）。
2. 槽 tri 必須 OPEN（含一次性未放過）且卡符合 `accepts`（型別名或卡 id）；主角卡**且當前為行動格**時再檢查 `action_spent`（night 放主角卡合法且不耗格，規格書第六節）。
3. 任一不過 → `ok=false` ＋一致理由，**GameState 零變化**。全過 → `EffectApply.apply(slot.on_place)` → `slots_placed["beat_id::slot_id"] = true`。
4. 卡是 `protagonist` 且在行動格 → `consume_action()`；槽有 `attention_npc` → `npc_action_counts[npc] += 1`（只在這一步加，失敗與免費槽不計）。UI 收到 `ok` 後重建面板（PanelBuilder 全量重算）。

- `relation_at_least` 求值：讀 `data/relation_scale.json`（新建：狀態名 → 最低整數值的序數表，屬拍板過的暫行案）。
- 語彙封閉性 lint（規格書第十七節 lint 1）做在 `Data` 啟動：走一遍全部 beats 的 `condition` / `requires` / `on_place` / `on_enter` 鍵，未知鍵 → `ok = false`。同時做 lint 2（有 `requires` 無 `reject_reason` 報警告）。

### P1-E choice_group

動的檔：`panel_builder.gd`（組的呈現規則）、`location_panel.gd`（選定流程）、`game_state.gd`（`choices: Dictionary`）。

- PanelBuilder：同組槽全部照常渲染；`gs.choices` 已有該組 → **整組只渲染被選的那個，狀態為 RESOLVED（唯讀展示，不可再互動）**。choice 的顯示規則**優先於**一次性槽的收起規則——被選槽即使在 `slots_placed` 裡也要以 RESOLVED 現身，不是消失。
- 選定的唯一入口：`GameState.choose(beat_id, group_id, slot_id, card_id := "")`，**原子操作**，兩條路共用同一套驗證與結算 helper：①驗證該組未結算（已結算＝no-op，不重複套效果）②驗證槽的 condition／requires ③帶卡時走 `try_place` 的持有＋`accepts` 檢查 ④結算該槽 `on_place`（兩條路同一個，結果相同）⑤一致寫入 `choices["beat_id::group_id"] = slot_id` **與** `slots_placed`——兩條路留下的狀態除卡片消耗外完全相同。**寫入 `choices` 即定案，無反悔路徑**（已拍板）。
- 組內 `accepts` 只約束「帶卡選」那條路，不擋「直接選」。
- 不選＝不寫 `choices`，離開面板無任何結算。

### P1-F 45 天全程走通與迴圈 stub

動的檔：`main.gd`（evening／night／結局流程接線）、`flow_text.gd`（殘響行）、`game_state.gd`（`end_run()` 與 run 層重置）、`scripts/verify_data.gd`（lint 3、5 補齊）、`tests/headless/playthrough_greedy.gd`（新建）。

- 殘響：evening 流程尾端，掃全部 beats 中 `echo.day == day` 且 `ConditionEval.eval(echo.condition)` 為真者，逐行播 `echo.text`。
- 夜間 stub：night 時段顯示夜間地圖（`earliest_night` ≤ day），`madness_cost > 0` 的地點一律 LOCKED、理由給 stub 文字；免費地點可進。**夜間解析（規格書第九節四步）P1 就做真的，P3 直接沿用**：入夜 fixed 定日 beat 強制播（走 `enter_beat`，不收費不解鎖）→ 選地點後主內容（定日優先於章節變體）＋附加 beat 並列 → **「睡」解析旅館（`sanquan`）的當夜定日 beat**。不開標記、不給發狂卡。
- 結局 coda：第 45 天 evening 播 `d45_then`（走 `enter_beat`，其免費比對槽經 `try_place` 可用）→ `run_ended` → `flow_text` 播結局 stub 文字 → `GameState.end_run()`：meta 層不動（knowledge 本來就在裡面）、run 層全部重置且**手牌重建為 `[主角卡]`**（規格書第十六節）→ 回第 1 天 morning（開局選單留待 Phase 5，直接跳過）。
- `by_design` 空格名單（第 1 天上午下午、第 32 天下午）抽到 `scripts/core/data_facts.gd` 常數；`verify_data.gd` 與走查腳本都引用它，不各留一份。
- `playthrough_greedy.gd`（`extends SceneTree`，不 instantiate 任何 UI）：只呼叫 `PanelBuilder.available_locations()` 與 `GameState.try_place()` ——**與 UI 完全同一組規則層 API**。每個行動格依地點順序找第一個 OPEN 且 `accepts` 含 protagonist 的槽放主角卡，找不到就跳過該格（比對 `DataFacts` 名單）；night 一律睡。跑到結局、再驗第二輪初態（meta 在、run 空），全綠 exit 0。
- lint 3（免費槽同面板規約）、lint 5 與 lint 7（夜間可達性：定日 beat 日期 ≥ 地點 `earliest_night`；非 fixed 夜 beat 必須掛 night layer 地點或旅館）補進 `verify_data.gd`。

---

## 跨 Phase 實作紀律

- API 簽名以本檔為準寫，但**動工前先 grep 現檔核對**（AGENTS.md 實作守則第 1 條——本檔可能落後於程式碼，落後時改本檔）。
- `core/` 不准 import `scenes/` 任何東西；違反即架構錯誤。
- 每個子階段收尾：headless 全綠（`verify_data.gd` ＋ 既有測試）才算完成；編譯錯誤同 turn 修完（實作守則第 3 條）。
- 每個子階段收尾**重跑序列化往返測試**：新增的狀態欄位（手牌、旗標、事件、選擇……）都要接進 `serialize()` / `deserialize()` 並驗往返相等，不是 P1-A 跑一次就算（規格書 Phase 1 通則驗收）。
- 破壞性測試（壞 JSON、未知語彙鍵）一律用 `tests/fixtures/broken/` 的 fixture，**不改正式 `data/`**。
