# P1 文件審核

> 覆審日期：2026-08-13  
> 覆審範圍：`實作規格書.md`、`開發設計方針.md`、`測試指南.md`、`data/` 現行資料  
> 專案階段：第一輪可玩 prototype／Phase 1 實作前  
> 本文件性質：verifier 審核紀錄，不取代既有規格的單一事實來源

## 結論

前次審核提出的主要問題大多已修正，資料格式、引用與日期覆蓋驗證亦已通過。不過，目前仍有五個會影響 P1 實作正確性的契約缺口。

- P1-A／P1-B 可先開始。
- 進入 P1-D 前，應先處理缺口一、四、五。
- 進入 P1-E 前，應先處理缺口二。
- 進入 P1-F 前，應先處理缺口三。

## 缺口一：`try_place()` 未驗證玩家是否持有卡片

### 現況

`開發設計方針.md` 的 `GameState.try_place(card_id, beat_id, slot_id)` 契約會檢查卡槽狀態、卡槽接受條件及主角行動是否已消耗，但沒有要求傳入的卡片實例必須存在於玩家目前的 `hand` 或 `knowledge`。

UI 已被定義為零規則顯示層，因此不能依賴 UI 阻止非法卡片 ID。headless 測試、除錯工具或未來其他呼叫端都可以直接呼叫這個 API。

### 影響範圍

- 玩家可能放置尚未取得的合法 registry 卡片。
- 玩家可能在第一天抵達事件前使用主角卡。
- 未來若有暫時離隊、不可用或外借中的卡片，也可能被錯誤使用。
- UI 與 headless 路徑可能產生不同結果。

### 建議修法

在卡槽 `accepts` 判定前完成以下檢查：

1. 解析傳入的卡片實例與 base card ID。
2. 確認該實例目前確實位於 `hand` 或 `knowledge`。
3. 若卡片具有暫時不可用狀態，也要確認目前可使用。
4. 任一檢查失敗時回傳一致的 reject reason，且不得改變任何 GameState。

### 驗收條件

- registry 存在、但玩家未持有的卡片放置失敗。
- 玩家持有的手牌與知識卡可依原規則正常放置。
- 非法嘗試後，手牌、知識、行動消耗、卡槽紀錄與效果狀態完全不變。
- UI 操作與直接呼叫 `try_place()` 得到相同結果。

## 缺口二：`choice_group` 的用卡與直接選擇路徑狀態不一致

### 現況

一般卡槽結算後會寫入 `slots_placed`，並依 one-shot 規則隱藏。選擇題的「使用卡片」路徑會經過 `try_place()`，但「直接選擇」路徑只套用效果並寫入 `choices`。

因此，同一個選項可能因玩家採用不同操作方式，在重新開啟面板後呈現不同結果：用卡路徑的 slot 被隱藏，直接選擇路徑的 slot 仍可見。這也與「重新開啟面板後只保留已選項」的驗收意圖衝突。

### 影響範圍

- 同一選擇的兩條合法操作路徑會留下不同 GameState。
- 已選項可能消失、仍顯示為可選，或再次觸發。
- headless 驗證通過的路徑不一定等同實際 UI 路徑。
- choice group 的 one-shot、顯示與防重複觸發語意不明確。

### 建議修法

讓 `choose()` 成為選擇題唯一的原子化入口，並讓兩條路徑共用同一套 slot 驗證與效果結算 helper：

1. 驗證 choice group 尚未結算。
2. 驗證選項的 condition、requires 與卡片需求。
3. 用卡路徑另驗證玩家持有該卡。
4. 套用效果後，一致寫入 `choices` 與必要的 slot resolution 紀錄。
5. PanelBuilder 將已選項輸出為 `RESOLVED`／唯讀狀態；choice group 的顯示規則優先於普通 one-shot 隱藏規則。

### 驗收條件

- 用卡與直接選擇同一選項後，除卡片消耗等本來就應不同的部分外，其餘狀態完全一致。
- 重新開啟面板只顯示已選項，且不可再次操作。
- 未選選項不再顯示或不可操作，依最終 UI 契約統一。
- 重複呼叫 `choose()` 不會再次套用效果。

## 缺口三：五個既有夜間事件依目前流程不可達

### 現況

夜間三階段解析流程已補上，但現行資料仍有以下不可達事件：

- `d23_night_bleed`
- `d24_night_bleed`
- `d25_night_bleed`
- `d26_night_bleed`

以上四個 dated night beat 綁在日間地點 `sanquan`。夜間地圖只允許選擇 night layer 地點，而非 fixed 的 dated night beat 又必須在玩家選定夜間地點後解析，因此四個事件沒有合法入口。

另有 `n_take_something` 限定第 10 天，但其地點 `n_landmark` 的 `earliest_night` 是第 13 天，因此也不可能觸發。

### 影響範圍

- 第 23～26 天的颱風夜間內容無法播放。
- `n_take_something` 永遠不會出現。
- 現行資料驗證仍會通過，因為驗證器只檢查格式、引用及基本日期覆蓋，沒有檢查語意可達性。
- fixed scheduled beat 位於付費夜間地點時，是否應開啟地標或收取發狂卡成本仍不明確。

### 建議修法

先拍板颱風夜間的設計語意，再採用其中一種最小修法：

- 若四個流血事件在條件成立時必播：改為 fixed scheduled beat。
- 若它們代表玩家選擇睡覺／留在旅館後發生：在夜間流程明訂「睡覺」如何解析住宿地點或日間所在地上的 dated night beat。

`n_take_something` 則應對齊事件日期與地點開放日：依設計意圖調早 `earliest_night`，或移除精確第 10 天限制並改用條件控制。不要只在驗證器加入例外。

此外，需在夜間契約中明訂 fixed scheduled beat 是否：

- 自動開啟尚未解鎖的夜間地標。
- 收取該地標原本的發狂卡費用。
- 算作玩家主動選擇該夜間地點。

### 驗收條件

- 第 23～26 天在預期操作下各能觸發對應事件。
- `n_take_something` 至少存在一條合法、可重現的觸發路徑。
- fixed scheduled beat 的解鎖與費用行為有明確測試。
- 增加靜態或 headless 可達性測試，至少檢查 dated beat 的日期不早於地點開放日，並攔截非 night layer 的非 fixed 夜間事件。

## 缺口四：跨輪後可能重複取得主角卡

### 現況

第一天抵達事件的 `on_enter` 會取得主角卡。另一方面，第二輪驗收要求回到第一天時手上已保留主角卡；由於 `beats_entered` 會在新一輪重置，玩家再次進入抵達事件時，可能再取得一張主角卡。

目前 `gain_card` 沒有定義唯一卡、可堆疊卡或重複取得時的處理方式，schema 也沒有 `stackable` 或 `unique` 欄位。

### 影響範圍

- 第二輪起可能出現兩張以上主角卡。
- 多張主角卡可能繞過一天只能花一次主角行動的 UI 或卡槽假設。
- 其他具有多個取得來源的非發狂卡，也可能遇到相同問題。
- 存檔與卡片實例 ID 的預期基數不明確。

### 建議修法

依目前跨輪驗收意圖，採用以下契約：

- 主角卡是 unique card。
- `gain_card("protagonist")` 必須冪等；玩家已持有時不建立第二個實例。
- 明訂其他 registry 卡片的重複取得政策。若 prototype 階段除發狂卡外都不允許重複，可先採用「非發狂卡預設 unique」的簡單規則，不必立即新增泛化 schema。
- 若某張卡未來確實需要堆疊，再新增明確的資料欄位與測試。

### 驗收條件

- 第一輪抵達後恰好持有一張主角卡。
- 跨輪回到第一天並再次進入抵達事件後，仍恰好一張。
- 重複執行同一 gain effect 不會建立新的主角卡實例。
- 發狂卡的既有多張／上限行為不受 unique 規則影響。

## 缺口五：NPC 行動投入統計缺少可實作的資料契約

### 現況

規格要求從第一天起統計玩家在每位 NPC 身上花費的行動數，供「玩家沒有主動邀請任何人時，由系統選出投入時間最多者」使用。

但目前 GameState 沒有對應的 `npc_action_counts`，beat 與 slot 也沒有可靠欄位表示一次主角行動應計入哪位 NPC。不能穩定地從 `occupant` 推導，因為部分事件沒有 occupant，另一些事件同時涉及多人。

### 影響範圍

- 陪伴者自動選擇規則無法依規格實作。
- 若等到後期才新增，先前遊玩過程的投入次數無法從現有狀態重建。
- 以 occupant、地點或關係值代替都可能產生與玩家行為不一致的結果。
- 存檔缺少後續結局判定所需的必要狀態。

### 建議修法

在 P1-D 就建立最小完整契約：

1. GameState 新增 `npc_action_counts: Dictionary`，key 為 NPC ID，value 為本輪已投入的主角行動數。
2. 在會消耗主角行動的 slot 或 beat 上新增明確欄位，例如 `attention_npc`；若一次行動確實可同時計入多人，再使用 `attention_npcs`。
3. 只有 `try_place()` 成功且實際消耗主角行動後才累加。
4. 將統計納入存檔、讀檔與跨輪重置契約。
5. 明訂最高值同分時的決勝方式；推薦使用固定 NPC ID 順序，確保結果可重現。

### 驗收條件

- 指定 NPC 的主角行動成功後，只有該 NPC 的計數增加一次。
- 放置失敗、重複操作、NPC 卡或知識卡不會增加計數。
- 存檔再讀檔後計數不變。
- 新一輪開始時依規格正確重置。
- 沒有主動邀請時可穩定選出最高者；同分結果可重現且符合明訂規則。

## 修正順序建議

1. **P1-D 前**：補 `try_place()` 卡片持有權、主角卡唯一性、NPC 行動投入統計。
2. **P1-E 前**：統一 choice group 的兩條結算路徑與已選狀態顯示。
3. **P1-F 前**：修正五個夜間事件的可達性，並補 fixed scheduled beat 的費用／解鎖契約。
4. 各缺口拍板後，同步修改其單一事實來源，再更新對應的實作契約與測試驗收；本審核文件保留作為修正追蹤紀錄。

## 覆審驗證紀錄

執行 Godot 4.6.3 headless 資料驗證：

- 卡片：54
- 地點：48
- beats：251
- 引用錯誤：0
- 日期覆蓋：通過，含 3 個設計上刻意留白

上述五個缺口主要屬於跨文件契約與語意可達性問題，因此現行資料驗證通過不代表這些問題已被覆蓋。
